# 问题集合 v51 — 紧凑手机响应式布局系统修复

> **本文件是本轮需求真源**（v51.7，制定日期：2026-09-03，状态：**执行中：W0–W4 已完成，W5 进行中**）。
> **用户目标**：针对 iPhone SE 已发现的训练计时换行、最小化训练入口遮挡 Tab、力度/瞄准竖条过长，以及同类固定尺寸问题，完成系统排查、共享根因修复和跨设备回归；测试只是发现手段，发现的问题必须修复并复验。
> **执行方式**：W0–W5 在同一个 Codex 任务中串行自动推进，每批内部验收后自动进入下一批，**不设置用户中途审核闸**。只有需要改变产品语义、缺少执行权限，或模拟器基础设施连续三次无法恢复时才停下报告。
> **设备边界**：本轮没有真机，全部测试、截图、定位、代码修复和模拟器复验由 Codex 完成；结论只覆盖 iOS Simulator，不冒充真机性能、触感、硬件音视频或生产账号验收。
> **方案方法**：按 `issue-collection-restructure` 的需求回显、真实锚点、批次依赖、完成标准和版本记录契约编写；执行阶段使用 `simulator-matrix-qa` 闭环。
> **执行基线**：当前工作区原有大量未提交改动；v51 只接管本文件列出的候选实现、测试和证据，不清理、不回退、不提交其他用户工作。

### 执行状态

| 批次 | 状态 | 当前实证 |
|---|---|---|
| W0 | ✅ 已完成（2026-09-03） | 写盘审计命中 124 个测试文件并分类；保存 dirty 工作区/模拟器基线；修正 v51 Light 深链证据和 iPad 断言前提；A1/B2/B4 的 Light 单测+UI 共 6/6 单元通过，每台 5 张命名截图；v50 报告已追加状态覆盖勘误 |
| W1 | ✅ 已完成（2026-09-03） | A1/B2/B4 的 Light/Dark 训练聚焦用例覆盖长计时、暂停、跳过计时、组间休息与五 Tab 短/长浮标；计时单行、关键命中区 ≥44pt、浮标零相交并可恢复同一 100 小时会话 |
| W2 | ✅ 已完成（2026-09-03） | iOS 17 SE 最终源码下 10 项布局测试通过；可用高度、180pt/45%、iPad 440pt、两档球径、44pt 槽位、世界几何及 82/180/264pt 力度映射已锁定；15 个目标球逐一点击通过，iOS 26 SE 相邻 Runtime 复验通过 |
| W3 | ✅ 已完成（2026-09-03） | 12 个生产/共享页面族统一消费 `ShotStageProxy`；全工程构建通过；A1 紧凑巡游 66/66 与联系表审查通过；补修打点入口 44pt 命中区并验证可打开共享打点盘 |
| W4 | ✅ 已完成（2026-09-03） | 最终源码指纹下 C1–C8 Light/Dark 16/16、C1/C2/C4/C6 AX5 4/4 均有通过单元；原尺寸复核确认 SE 长计时单行、五 Tab 浮标无相交、紧凑竖轨/球盘无重叠、iPad 顶部 Tab 与右下浮标分离。返工补修 iPad 顶部 Tab 场景浮标定位、100 小时时长格式和 AX5 跳过状态图标膨胀；Dark 并发下 3 次已合成 tap 未投递，单 worker 原设备复跑全部通过，未归为产品缺陷 |
| W5 | 🔄 进行中 | 开始 16 个 66 页全巡游、12 个合约单元、双 Runtime 安全单测、门禁与最终报告 |

---

## 一、需求回显与语义基准

| ID | 用户要求 | 本方案固定口径 |
|---|---|---|
| R1 | iPhone SE 训练计时变成两行 | 核心计时在所有支持的手机宽度上一行完整显示，不能靠裁切、隐藏末位或缩小到不可读解决 |
| R2 | 最小化后“继续训练”遮挡 Tab | 跨 Tab 入口不得与任一 Tab 可见区或点击区相交，五个 Tab 必须始终可见、可点 |
| R3 | 力度条、瞄准条在小手机过长 | 两条共享同一响应式尺度；紧凑手机降低视觉长度，但不改变力度/角度语义、步进和拖动精度 |
| R4 | 重点是不同尺寸下的各种固定尺寸问题 | 不按设备名打补丁；按实际可用宽度、高度、安全区和内容长度选择布局 |
| R5 | 测试不只是发现，还要修复 | 每个可复现产品缺陷必须完成“证据→根因→共享修复→受影响页复验→矩阵回归” |
| R6 | 全部用模拟器、全部由 Codex 完成 | 不设置真机或用户手工点验为本轮完成前置；模拟器做不到的硬件项明确列为边界 |
| R7 | 截图应自动生成 | 复用 `ScreenshotTourUITests.testDesignerPageDump` 与命名目录，不手工逐页截取 |
| R8 | 方案执行中不需要中间审核 | W0–W5 自动串行；阶段截图由 Codex 自审，不等待用户逐批确认 |

### 1.1 不改变的产品语义

1. 训练计时继续表示整场真实累计时长；紧凑显示不得改写持久化数据或计时逻辑。
2. 44pt 关键点击目标保留；通过减少同排信息、备用布局和空间参与解决，禁止把按钮缩成难点区域。
3. “继续训练”仍是跨 Tab 返回活动训练会话的入口。
4. 力度范围、非线性映射、步进、触感调用和瞄准角度精度保持不变；只调整 UI 行程和布局。
5. 球桌物理尺寸、世界坐标、袋口和轨迹几何不因 UI 响应式修复而改变。
6. 5 Tab 顺序、导航目的地和现有训练/休息状态机保持不变。

---

## 二、已确认问题与根因基线

### 2.1 当前开放问题

| ID | 严重度 | 已确认现象 | 根因 | 共享影响面 |
|---|---:|---|---|---|
| V51-UI-001 | P1 | 活动训练顶栏 `00:00:06` 等八位时间在 SE/临界宽度手机折成两行 | 28pt 等宽时间与 4 个 44pt 控件、固定间距、固定边距同排；无单行约束和紧凑变体 | `ActiveTrainingView` |
| V51-UI-002 | P1 | 最小化后的“继续训练”胶囊遮挡底部 Tab | `MainTabView` 以 `ZStack` 覆盖并硬编码 `.padding(.bottom, 60)`，未读取真实 Tab/safe-area 几何 | 5 个 Tab 全部 |
| V51-UI-003 | P2 | SE 上瞄准条/力度条视觉过长，右柱总高占球桌大部分 | `barLength` 固定在 150…264pt；SE 竖屏仍命中 264pt 最大值；顶部固定区另占 72pt | 所有 `ShotStageProxy` 击打/解球页 |
| V51-UI-004 | P2；命中错球则升 P1 | 两行球库拥挤、相邻球视觉重叠 | 球库 8 列绑定球桌宽度；球视觉 36pt、槽高 38pt；`compactDiameter=30` 无调用方 | 自由击球、走位、反解、图谱、瞄准等 |
| V51-UI-005 | P2 | 思路训练等页导航副标题被省略，关键状态末尾不可见 | `BTSolverNavStatus` 固定单行；长状态只放在窄 `principal` 区 | 暗色球桌页共享导航件 |
| V51-UI-006 | P2 风险 | 顶部模式 Chip 在紧凑宽度/大字号下接近或超过一行容量 | `BTChipRow` 支持滚动，但 Silu/PlanThree/Snooker/Solver 等调用方显式 `scrollable: false` | 多个解球/训练页 |

### 2.2 关键数值事实

- 活动训练顶栏右侧固定宽度约为 `4×44 + 3×12 = 212pt`；SE 第三代 375pt 屏宽扣除 32pt 页面边距后仅 343pt，八位 28pt 等宽时间与控件组合超限。
- `ShotStageMetrics` 当前为 `aimWheelWidth=38`、`instrumentWidth=38`、`instrumentTopReserve=72`、`barLength=150…264`、`topRowHeight=46`、`bottomBarHeight=94/132`。
- 球库每个球的当前视觉直径为 36pt、槽高为 38pt；8 列无重叠至少需要约 304pt。SE 截图中球库使用球桌宽度，明显小于该下限。
- 当前 `barLength = max(minBarLength, min(maxBarLength, available))`；若未来可用高度低于 150pt，最小值仍可能反向超过真实空间。

### 2.3 v50 验收结论的勘误边界

v50 的 66 张巡游证明了命名、数量、解码和默认页可达，不足以证明本轮状态布局正确：

1. 66 张 manifest 不含活动训练和跨 Tab 最小化状态。
2. `testDesignerPageDump` 的活动页面只覆盖默认入场态；不会自动展开本轮所有交互状态。
3. 原图门禁不检查 Text 换行、控件相交、球槽重叠或比例上限。
4. 缩略联系表会弱化一位数字换行、8pt 球重叠和“控件过长”这类问题。
5. v50 的 iOS 17 SE 原图已能看到长力度柱、球库重叠和副标题省略，说明它们是历史漏检，不是 iOS 26 新回归。

因此执行 W0 时须给 v50 报告增加后续勘误指针；不得继续引用“紧凑手机开放缺陷为 0”描述当前状态。

### 2.4 当前工作区接管基线（2026-09-03 v51.2 实读）

当前 dirty 工作区已经出现以下与 v51 同方向的候选改动，但尚未形成完整矩阵证据，不能标记为修复完成：

1. `ActiveTrainingView` 已有 `ViewThatFits` 顶栏候选实现，计时增加单行和紧凑布局约束。
2. `MainTabView` 已有读取真实 `UITabBar` frame 的候选实现；`BTFloatingIndicator` 已有紧凑内容和宽度上限。
3. `ShotStageMetrics` / `ShotStageProxy` 已有紧凑竖条、球库宽度和球直径计算候选实现，多个生产页面也已有调用迁移痕迹。
4. `BTChipRow`、`BTSolverNavStatus`、球库、瞄准轮和力度柱已有可访问性或响应式候选调整。
5. `V51ResponsiveLayoutTests.swift`、`V51ResponsiveLayoutUITests.swift` 与矩阵 suite 已存在，但用例覆盖目前只代表候选起点，不能替代 C1–C8、AX5 和 66 页全量验收。

执行纪律：不得回退、覆盖或重复实现这些改动；W0 先用 diff、构建、聚焦测试和原尺寸截图逐项判定“可保留 / 需返工 / 尚缺失”，后续 W1–W3 只处理未满足完成标准的部分。

---

## 三、响应式设计决策

### 3.1 判断依据

1. 使用容器实际 `width/height`、safe-area 和内容固有尺寸，不比较 `UIDevice` 型号字符串。
2. 同一组件提供 regular/compact 两种布局或由几何约束连续计算；禁止新增“如果是 iPhone SE”分支。
3. 固定值只用于不可压缩语义，如最低触摸面积、线宽、球桌物理几何；组合布局必须验证总预算。
4. 宽度不足时优先级：核心状态完整 > 关键主操作可达 > 次级操作直显 > 装饰与冗余信息。
5. 高度不足时优先级：球桌主任务面积 > 主操作 > 仪表读数 > 装饰留白。

### 3.2 活动训练顶栏

- regular 形态保留完整时间和现有直接操作。
- compact 形态至少保留“一行时间 + 暂停/继续 + 休息 + 更多”；独立结束按钮与“更多”菜单内的结束动作重复，可在紧凑形态只保留菜单入口。
- 时间文本必须 `.lineLimit(1)`，但不得只靠极端缩放止血；不足一小时可采用 `MM:SS` 紧凑视觉，完整时长继续用于状态、持久化和无障碍标签。
- 同时验证运行、暂停、跳过计时、休息中和休息最小化五种状态。

### 3.3 跨 Tab “继续训练”入口

- 入口必须进入底部布局/安全区计算并为自己预留空间；禁止继续使用猜测 Tab 高度的固定 `bottom` 偏移。
- 胶囊与五个 Tab 的可见 frame、点击 frame 均不得相交。
- 紧凑宽度允许使用更短视觉文案或独立全宽条，但无障碍名称仍为“继续训练 + 完整时间”。
- 时间增长到三位/四位分钟时不得跑出屏幕或覆盖更多 Tab。

### 3.4 球桌舞台与竖条

- `ShotStageProxy` 必须保证计算结果先服从真实可用高度，再服从视觉最小值；任何最小值都不能造成越界。
- 紧凑手机的力度/瞄准轨道目标为约 168–180pt，且轨道不超过球桌可见高度的 45%；regular 形态可保留更长行程，上限仍为 264pt。
- 力度柱顶部读数区可有 compact 度量，但打点入口和关键交互命中区不得小于 44pt。
- 左瞄准条与右力度条保持等长、同底；拖动映射继续按归一化比例计算，不因视觉变短改变速度范围或步进。

### 3.5 球库与底部栏

- 紧凑形态使用 30pt 视觉球，regular 使用 36pt；视觉尺寸与命中尺寸分离，交互命中区保持至少 44pt。
- 球库布局使用页面安全可用宽度并设置合理最大宽度，不再把球桌宽度当作唯一宽度真源。
- 15 球两行排布中，任意两个槽位不得相交，首末球不得越出安全区。
- compact/regular 底栏高度由实际槽位高度派生；不再用与内容脱节的固定档位制造空白或挤压场景。

### 3.6 状态副标题与 Chip

- 关键状态不能只存在于会被截断的导航副标题；紧凑形态使用短状态或把完整状态放入场景 HUD/无障碍值。
- 非滚动 Chip 只有在测量确认完整容纳时使用；紧凑宽度和 AX 字号自动切到横向滚动，并保证当前选中项可见。
- 不通过缩小到低于现有可读字号或低于 44pt 命中面积换取单行。

---

## 四、覆盖矩阵与不变量

### 4.1 设备与 Runtime

| 组 | Runtime / 设备 | 目的 |
|---|---|---|
| C1 | iOS 17.0 / iPhone SE 第三代 | 最低 Runtime + 紧凑手机完整回归 |
| C2 | iOS 26.2 / iPhone SE 第三代 | 最新 Runtime + 用户问题主复现环境 |
| C3 | iOS 26.2 / iPhone 16e | 计时顶栏临界宽度、刘海安全区 |
| C4 | iOS 26.2 / iPhone 17 Pro | 当前常用基准手机 |
| C5 | iOS 26.2 / iPhone 17 Pro Max | 大屏手机，防止紧凑修复造成松散或异常放大 |
| C6 | iOS 17.0 / iPad mini 第六代 | 最低 Runtime 窄 iPad |
| C7 | iOS 26.2 / iPad mini A17 Pro | 最新 Runtime 窄 iPad |
| C8 | iOS 26.2 / iPad Pro 13 英寸 | 最大画布与 max-width 约束 |

所有 C1–C8 跑 Light/Dark；C1、C2、C4、C6 另跑 AX5。最终总回归仍复用 v50 A1–A8 与 B1–B6 配置，避免只验证本表代表设备。

### 4.2 必测状态

1. 活动训练：`00:00:00`、`00:59:59`、`01:00:00`、长时长、暂停、休息中、跳过计时。
2. 会话最小化：训练/动作库/角度/历史/我的五个 Tab，各验证短时间和长时间。
3. 球桌页：默认、自由瞄准、进袋/求解、打开打点盘、击球中、回放/上一杆、开球流程。
4. 球库页：15 球完整、部分球已在台面、拖动 ghost、点击首球/中间球/末球。
5. 顶部状态：短状态、长状态、求解中、无解、对局比分/轮到谁。
6. Chip：默认选中、末项选中、AX5、长文案状态。

### 4.3 机器不变量

| INV | 必须满足 |
|---|---|
| L-01 | 训练计时 Text 的渲染高度保持单行，frame 不与右侧操作组相交 |
| L-02 | 所有保留的训练顶栏按钮命中区宽高均不小于 44pt |
| L-03 | “继续训练”frame 不与任一 Tab 项 frame 相交，五个 Tab 均存在且可点击 |
| L-04 | 浮标、Tab、主 CTA 全部落在窗口安全可见范围内 |
| L-05 | `aimWheelFrame`、`instrumentFrame` 不越出 scene；两轨等长同底 |
| L-06 | compact 轨道不超过球桌可见高度的 45%，且视觉长度落在约 168–180pt 目标档 |
| L-07 | 球库相邻槽位无交叠，视觉球不越出各自槽位，首末槽不越安全区 |
| L-08 | 15 个球分别可被准确点击；点击一个不得触发相邻球 |
| L-09 | 顶部 Chip 全部可达，AX5 下允许滚动但不得硬裁切 |
| L-10 | 导航副标题截断时，完整关键状态必须在同屏其他位置或无障碍值可获得 |
| L-11 | iPad 上舞台控件不随画布无限拉长；内容保持合理 max-width 和居中 |
| L-12 | 响应式 UI 改动不得改变球桌世界坐标、力度范围、瞄准步进和轨迹结果 |

---

## 五、批次表与依赖

| 批次 | 范围 | 量级 | 依赖 | 主要交付 |
|---|---|---:|---|---|
| W0 | 接管现有候选改动、冻结证据、补齐测试入口、登记 v50 勘误 | M | 无 | 候选改动审计表、可复现基线、几何 JSON、命名截图、测试覆盖台账 |
| W1 | 活动训练顶栏 + 跨 Tab 最小化入口 | M | W0 | V51-UI-001/002 修复及五 Tab 复验 |
| W2 | 球桌共享响应式尺寸引擎 | L | W0、W1 | 竖条、球库、底栏的共享计算与纯函数测试 |
| W3 | 全部生产调用页迁移 + 导航状态/Chip 收口 | L | W2 | V51-UI-003…006 全路由修复 |
| W4 | 聚焦设备/状态矩阵与返工循环 | L | W1–W3 | C1–C8 Light/Dark/AX 聚焦截图与零开放缺陷 |
| W5 | 全量回归、66 页自动巡游、最终审查与报告 | XL（自动运行） | W4 | v51 总报告、v50 勘误、进度与真源收官 |

### 5.1 依赖图

```text
W0 可信基线
 ├─→ W1 训练 chrome
 └─→ W2 球桌共享布局
        └─→ W3 全调用页迁移
W1 + W3 ─→ W4 聚焦矩阵/返工 ─→ W5 全量矩阵/收官
```

执行按 `W0 → W1 → W2 → W3 → W4 → W5` 串行，不启用并行 worktree，避免当前 dirty 主工作区内多方同时修改共享组件和测试配置。

---

## 六、逐批范围与完成标准

### W0 — 当前工作区接管、可信基线与响应式测试入口

**范围**

1. 保存执行前 `git status`、相关跟踪文件哈希和当前源码指纹；把现有 v51 候选改动与其他用户 dirty 变更分开建账，不得清理、覆盖或回退。
2. 将用户 5 张截图的文字结论与现有 v50 SE 原图对照，建立 V51-UI-001…006 证据表。
3. 审核现有 `QiuJiUITests/V51ResponsiveLayoutUITests.swift`：确认计时、浮标、五 Tab、竖条、球库、状态栏和 Chip 都有稳定 identifier、frame 采集与正确的设备分档断言；缺项才补。
4. 审核现有 `QiuJiTests/V51ResponsiveLayoutTests.swift`：补齐紧凑/标准/iPad 容器、可用高度优先、44pt 命中区和几何不变量；禁止以生产特例喂绿断言。
5. 审核 `scripts/simulator-matrix.json` 的 v51 suite、设备映射与产物根目录；所有截图继续按 runtime/device/appearance/state/suite 隔离。
6. 给 v50 最终报告增加“2026-09-03 后续勘误”指针，只追加事实，不改写或删除原始运行证据。

**完成标准**

- 输出“现有候选改动接管表”：每项列原始问题、当前 diff、已有测试、实际结果和“保留/返工/缺失”结论。
- C1/C2/C3 至少各产出一组当前状态截图和 frame JSON；能直接复现的缺陷保留修复前证据，已被候选实现改变的项目以用户原图、v50 原图和 Git diff 作为修复前证据，不为补截图而回退代码。
- 诊断模式不得因“预期当前存在缺陷”污染默认测试总集；W1–W3 完成后同一用例切为阻断性断言。
- 写盘目录只位于 `build/v51/` 或专用 `/tmp/qiuji-v51/`；不得覆盖 `docs/ui-polish` 既有 PNG。
- `git diff --check` 通过；工作区原有变更与 v51 预期变更能区分。

### W1 — 训练顶栏与最小化入口

**范围**

1. 验收并按需修正 `ActiveTrainingView.frostedTopBar` 现有 regular/compact 候选布局，确保由真实可用宽度驱动。
2. 保持计时单行；验证不足一小时、超过一小时和长时长的视觉/无障碍值。
3. compact 形态去除与“更多”菜单重复的独立结束直显，保留 44pt 关键命中区。
4. 验收并按需修正 `BTFloatingIndicator` 与真实安全区/Tab frame 的接入；最终不得残留 `.padding(.bottom, 60)` 式设备假设。
5. 为浮标增加稳定 identifier、长时间宽度策略和 compact 形态。
6. 保持会话最小化、恢复、休息最小化和计时状态机行为不变。

**完成标准**

- C1/C2/C3 的运行、暂停、休息、跳过计时截图中，时间一行完整且与按钮无相交。
- 五个 Tab × Light/Dark × 短/长时间下，浮标与 Tab 零相交、五个 Tab 全部可点，点击浮标能恢复同一会话。
- `ActiveTrainingViewModelTests` 及 v51 训练聚焦 UI 用例通过。
- 不新增机型名称判断，不把任何关键点击区缩到 44pt 以下。

### W2 — 球桌共享响应式尺寸引擎

**范围**

1. 验收并按需修正 `ShotStageMetrics` / `ShotStageProxy` 现有候选计算，让竖条、顶部固定区、底栏和球库尺寸由可用空间派生。
2. 修正 `barLength` 最小值可超过 available 的公式风险；紧凑轨道落入 v51 compact 目标。
3. `BTShotInstrumentColumn` 与 `BTAimWheel` 共享轨道长度，保留归一化拖动映射与力度/角度步进。
4. 激活 `BTBallPaletteMetrics.compactDiameter`；将视觉球尺寸、槽位和命中区解耦。
5. 球库宽度改为页面安全可用宽度 + max-width 约束，不再只取 `tableRect.width`。
6. 底栏高度按槽位/附加行内容派生，保证 scene 计算使用最终底栏高度。

**完成标准**

- `V51ResponsiveLayoutTests` 对紧凑、标准和 iPad 容器全部满足 L-05…L-08、L-11。
- compact 轨道约 168–180pt 且不超过球桌高度 45%；regular 轨道不超过 264pt。
- 15 球两行无视觉重叠，命中区至少 44pt，首/中/末球点击结果正确。
- 物理、瞄准、力度映射相关既有单测结果零变化。

### W3 — 生产调用页迁移与页面级收口

**范围**

迁移并逐页核实以下生产页面族：

- `ShotSimulationView`
- `PositionPlayComposerView`
- `FreePlayView`
- `SiluTrainerView`
- `PlanThreeView`
- `SnookerTacticsView`
- `SeparationAngleAtlasView`
- `CushionEnglishAtlasView`
- `SolverStageChrome` 覆盖的 Bank/Diamond 页面
- `SceneAimingView`、`AimPointSceneTrainingView`
- 开球流程复用的 `BreakInstrumentsOverlay`

同时处理 `BTSolverNavStatus` 长状态和 `BTChipRow(scrollable: false)` 的紧凑/AX 可达性；simulator-only 批量工具页同步迁移共享 API，但不计入生产通过率。

**完成标准**

- 每个生产页面族至少有 compact 默认态截图；含交互的共享族另有打开打点盘/求解/击球态证据。
- 页面不保留手写旧尺寸、逐页 SE offset 或重复的 compact 判断。
- 导航状态不丢关键语义；所有模式 Chip 在 C1/C2 AX5 下可达。
- 全部受影响页面编译通过，聚焦 UI 测试通过，截图目视无裁切、重叠或比例失衡。

### W4 — 聚焦矩阵与缺陷返工循环

**范围**

1. C1–C8 跑 Light/Dark `v51-responsive` suite；C1/C2/C4/C6 加 AX5。
2. 自动生成每单元命名 PNG、frame JSON、xcresult、日志和联系表。
3. 先逐张审查原尺寸关键截图，再看联系表；禁止只看缩略图下结论。
4. 任一不变量失败或视觉问题均回到对应共享根因修复，并重跑：发现设备 + 同形态邻接设备 + 基准设备。
5. P0/P1/P2 产品缺陷未清零前不得进入 W5。

**完成标准**

- C1–C8 Light/Dark 全部选定单元退出码 0；C1/C2/C4/C6 AX5 通过。
- L-01…L-12 全部通过；V51-UI-001…006 状态均为已修复。
- 每次返工保存“修复前/修复后”证据，不删除首次失败日志。
- UI Reviewer 原尺寸审查确认开放 P0/P1/P2 为 0。

### W5 — 全量回归与一次性交付

**范围**

1. 使用现有矩阵运行器重跑 A1–A8 × Light/Dark 的 66 页 `testDesignerPageDump`，以及 B1–B6 的合约冒烟。
2. 跑 iOS 17.0 / iOS 26.2 安全单测分片、受影响聚焦测试、`make build` 与仓库门禁。
3. 运行写盘审计；比较执行前基线，只允许 v51 预期源码/测试/文档和 `build/v51` 产物。
4. 生成 `docs/research/20260903-v51-响应式布局验收报告.md` 和 `tasks/ui-reviews/UR-20260903-v51-responsive-layout.md`。
5. 更新本真源状态、`tasks/PROGRESS.md`、需要登记的 FL/DR/PD 与 `UI-IMPLEMENTATION-SPEC.md` Changelog。

**完成标准**

- A 级 16 个完整巡游单元各 66/66 命名 PNG；缺图、解码失败、非授权重复为 0。
- B 级 12 个合约单元全部通过；v51 聚焦 suite 在最终源码指纹下全部通过。
- iOS 17/26 安全单测、`make build`、`verify-gate`、`verify-doc-size` 真实退出码为 0。
- 最终报告逐项回答设备、Runtime、外观、状态、缺陷、修复、复验和模拟器边界。
- 不以“截图都生成了”代替 L-01…L-12；不以缩略联系表代替关键原图审查。

---

## 七、测试产物与写盘契约

| 产物 | 路径 | Git 跟踪 |
|---|---|---|
| 本轮真源 | `问题集合_v51.md` | 是 |
| 聚焦 UI 测试 | `QiuJiUITests/V51ResponsiveLayoutUITests.swift`（已存在未验收候选） | 是 |
| 纯布局单测 | `QiuJiTests/V51ResponsiveLayoutTests.swift`（已存在未验收候选） | 是 |
| 矩阵配置 | `scripts/simulator-matrix.json` | 是 |
| 原始日志/截图/JSON/xcresult | `build/v51/` | 否 |
| UI 审查 | `tasks/ui-reviews/UR-20260903-v51-responsive-layout.md`（拟新增） | 是 |
| 最终报告 | `docs/research/20260903-v51-响应式布局验收报告.md`（拟新增） | 是 |

执行前运行 `scripts/audit_simulator_test_writes.py` 并核对 scheme/test selector；不得把全仓库 clean 作为完成条件。验收比较的是“执行前 dirty 基线 + v51 预期变更 + 无意外写盘”，现有用户改动必须原样保留。

---

## 八、停止条件与自动恢复

1. 单个模拟器/Runner 故障：保存失败证据，重启服务或新建同型号实例，再聚焦复跑。
2. 同一基础设施故障连续三次且无法以同型号干净实例恢复：停止并报告，不把它记为产品失败或伪造通过。
3. 测试发现产品缺陷：自动进入根因修复和邻接设备复验，不等待用户确认。
4. 若需要改变训练状态机、Tab 信息架构、力度/瞄准业务语义或球桌几何真源：停止并向用户说明，这是唯一需要产品拍板的范围。
5. 不允许通过提高阈值、删除断言、隐藏控件、硬编码 SE 分支或仅改截图门禁来换绿。

---

## 九、锚点索引（2026-09-03 已核实）

| 领域 | 已核实锚点 | 现实含义 |
|---|---|---|
| 训练顶栏 | `QiuJi/Features/Training/Views/ActiveTrainingView.swift:332–496` | 已有 `ViewThatFits`、单行时间和紧凑操作组候选；须验证全部计时状态 |
| 时间格式 | `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift:249–254` | 永远输出 `HH:MM:SS` |
| 跨 Tab 浮标定位 | `QiuJi/App/MainTabView.swift:5–88,280–326` | 已有真实 `UITabBar` frame 读取候选；须跨 iOS 17/26 验证 |
| 浮标内容 | `QiuJi/Core/Components/BTFloatingIndicator.swift:12–61` | 已有 compact/max-width 候选；须验证长时长和 AX5 |
| 舞台度量 | `QiuJi/Core/Components/ShotTableLayout.swift:70–141` | 已有紧凑轨道、球库直径/宽度候选计算 |
| 舞台计算 | `QiuJi/Core/Components/ShotTableLayout.swift:175–229` | 已接入响应式 barLength、libraryWidth 和 paletteBallDiameter 候选 |
| 力度柱 | `QiuJi/Core/Components/BTShotInstrumentColumn.swift:41–155` | 顶部内容 + GeometryReader 轨道，拖动按高度归一化 |
| 球库 | `QiuJi/Core/Components/BTBallPaletteBar.swift:6–27,121–270` | 已有 44pt 槽位和多种球库候选；须逐个验证 15 球点击与无重叠 |
| 导航状态 | `QiuJi/Core/Components/BTShotPageChrome.swift:280–305` | 副标题固定单行省略 |
| Chip | `QiuJi/Core/Components/BTChipRow.swift:7–41` | 组件支持滚动，部分页面主动关闭 |
| FreePlay | `QiuJi/Features/PositionPlay/Views/FreePlayView.swift:31–52,111–170,443–455` | 固定三段高度并复用仪表/球库 |
| Silu | `QiuJi/Features/PositionPlay/Views/SiluTrainerView.swift:31–53,99–156,180–205,324–345` | 固定三段、非滚动 Chip、共享仪表/球库 |
| 66 页巡游 | `QiuJiUITests/ScreenshotTourUITests.swift:332–359,2193–2332` | 自动默认页巡游，不覆盖本轮全部状态 |
| v51 聚焦测试 | `QiuJiTests/V51ResponsiveLayoutTests.swift`、`QiuJiUITests/V51ResponsiveLayoutUITests.swift` | 文件已存在，覆盖仍需按 W0 台账补齐和实跑 |
| 矩阵设备 | `scripts/simulator-matrix.json:19–39` | v50 A/B 设备与 v51 suite 已存在，须验证 C1–C8/AX5 映射完整性 |
| 历史报告 | `docs/research/20260902-v50-模拟器矩阵验收报告.md:38–61` | 需追加后续勘误，不删除原证据 |

拟新增文件在执行前不存在，已在表中明确标为“拟新增”；其余锚点均已实读。

---

## 十、最终报告必须回答的问题

1. 六项初始问题分别在哪些设备/状态复现，最终如何修复？
2. 训练计时在短时、跨小时和长时长下是否始终单行且按钮仍可达？
3. “继续训练”在五个 Tab、两种 Runtime 和不同 Tab Bar 形态下是否零遮挡？
4. compact/regular 的力度条、瞄准条、球库和底栏最终尺寸是多少，计算依据是什么？
5. 哪些生产页面因共享修复受益，哪些需要过页面级迁移？
6. 原有力度/瞄准/球桌几何和训练状态机是否有行为变化？
7. v50 为什么漏检，本轮新增哪些机器不变量防止再次发生？
8. 全量 66 页、聚焦状态矩阵、双 Runtime 单测和构建的真实结果是什么？
9. 哪些结论仍因没有真机而不能证明？

---

## 十一、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v51.7 | 2026-09-03 | W4 收官：最终源码下 8 台 Light、8 台 Dark、4 台 AX5 聚焦矩阵全部通过；修复 iPad 顶部 Tab 场景浮标不可见、长会话浮标时长格式和 AX5 跳过图标膨胀，完成 SE/iPad 原尺寸审查；Dark 并发 tap 抖动在单 worker 原设备复跑均通过；W5 启动 |
| v51.6 | 2026-09-03 | W3 收官：生产页面族共享 API 静态审计、全工程构建、A1 66/66 紧凑默认态巡游与联系表通过；页面审查补发现并修复 30pt 打点 Button 命中区，最终 frame 44×44 且共享打点盘展开测试通过；W4 启动 |
| v51.5 | 2026-09-03 | W2 收官：共享轨道、球库宽度/球径、底栏和几何边界由 10 项纯布局测试锁定；15 个目标球逐项点击及双 Runtime SE 复验通过；修复 `BTChipRow` 内层容器 identifier 覆盖子 Button 标识的问题，W3 开始页面族收口 |
| v51.4 | 2026-09-03 | W1 收官：A1/B2/B4 Light/Dark 覆盖运行、暂停、跳过、组间休息、五 Tab 短/长浮标及同一会话恢复；修复测试夹具错误、冷启动深链竞态和休息按钮 44pt 命中区，W2 开始共享响应式组件审计与补测 |
| v51.3 | 2026-09-03 | 开始连续执行：W0 完成候选接管、写盘/工作区基线、测试证据可信度修复、A1/B2/B4 Light 聚焦验证与 v50 勘误；W1 进入 Dark/休息/恢复状态补验 |
| v51.2 | 2026-09-03 | 按当前 dirty 工作区重新实读：确认训练顶栏、跨 Tab 浮标、球桌响应式、球库/Chip/状态及 v51 测试已有未提交候选改动；方案由“从零实施”改为“W0 先接管验收，W1–W3 只保留/返工/补缺口”，不把候选实现或历史运行误记为完成。本次仍仅交付方案，不继续实施或跑测试 |
| v51.1 | 2026-09-03 | 初版真源：基于用户 5 张截图、现有 SwiftUI 组件和 v50 原始 SE 证据，登记 2 个 P1、3 个 P2 和 1 个 P2 风险；建立训练 chrome、球桌共享布局、全调用页迁移、聚焦矩阵与全量回归 6 批连续执行方案，并明确无中途审核、模拟器-only 和 v50 漏检勘误口径 |
