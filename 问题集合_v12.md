# 问题集合 v12 — 学卡片页「瞄准修正」（自动求解 vs 标准角度瞄准法的偏差，交互式实况插图）

> **本文件是本轮需求的真源。** 日期：2026-07-17　状态：✅ **全部批次收官**（Z1–Z4 均已合并主干 2026-07-18；Z4 合并 e071ebf）
> 全部锚点已读代码核实（2026-07-17，见 §五）。编号约定：条目 **M1**；批次 **Z1–Z4**；拍板项 **D-v12-N**。

---

## 一、需求回显（语义基准，不增删）

| # | 用户诉求（原意） | 验收语义 |
|---|---|---|
| M1 | 自动求解方案（指定目标球、袋口、力度、加塞 → 自动找到正确瞄准方向）中，进球线固定，但不同力度、加塞会影响瞄准方向，与理想的角度瞄准法有偏差：**投掷效应**；**高杆容易把球打薄、低杆容易把球打后**（「打后」=「打厚」，D-v12-2 已确认 2026-07-17）；**加塞需考虑母球行进轨迹**。即不同力度、不同左右加塞量下，瞄准方向与标准角度瞄准法的差异——自动求解模式通过算法弥补了这些。需要**单独的学卡片页**以**图文形式**讲这部分内容 | 学区新增独立卡片页，内容覆盖：投掷效应、高低杆对有效厚度的影响、加塞下母球轨迹（挤偏+弧线）、以及自动求解如何弥补这些偏差；图文样式与既有学页一致 |
| M1-补 | **已拍板（2026-07-17 用户：「这个内容很重要，做成交互式实况插图吧」）**：插图采用**交互式实况**方案——控件（力度/高低杆/左右塞）驱动插图实时重算，数值与自动求解**同一份引擎真源** | ③④⑤ 节插图由引擎实时计算（非静态画死图），拖动控件时瞄准线/轨迹/偏差角实时更新 |

---

## 二、内容口径（写作时的事实来源，全部对应引擎实现）

> 本节口径 2026-07-17 由主控对照引擎代码整理（非网络调研）；每条都有代码真源，写作时禁止临场编造数值。页面章节结构已在 2026-07-17 会话中向用户展示并获认可（6 节）。

### 2.1 页面章节结构（6 节，每节一图）

| 节 | 内容 | 图 | 图的实现方式 |
|---|---|---|---|
| ① 理想 vs 现实 | 角度瞄准法假设「碰撞无摩擦、母球走直线」，真实球台两条都不成立；进球线固定但「怎么瞄」随力度/杆法变化；目录预告三个偏差源 | 同一局面两条瞄准线：几何幽灵球方向（虚线）vs 求解方向（实线），夹角 Δ 局部放大 | 实况：Δ = `positionAimOffset` 直接读出（见 §2.2） |
| ② 投掷效应（Throw） | 碰撞瞬间接触面摩擦把目标球拽离理想进球线。分两小段：**切角投掷 CIT**（不加塞也有，切角越大越明显）、**塞致投掷 SIT**（左塞把目标球向右带）。力度规律单独强调：**越慢投掷越大**（Alciatore 摩擦拟合，速度越低摩擦越大）——解释「轻推薄球容易不进」 | 碰撞点放大图：接触点、摩擦力箭头、目标球实际离开方向 vs 理想进球线（Δ 夸大绘制） | 静态 Canvas 示意 + 离开方向数值从 `resolveBallBallPure` 取样核验（禁脑算） |
| ③ 高低杆改变有效厚度 | 用户口径：同一瞄准点下**高杆容易打薄、低杆容易打厚**（D-v12-2 已确认）。解释路径：上下旋改变碰撞瞬间接触面相对滑动方向 → 摩擦力水平分量变化 → 投掷方向变化 ≈ 「厚度变了」。实战翻译：高杆瞄厚一点、低杆瞄薄一点 | 三联对比（低杆/中杆/高杆）：同一瞄准线下目标球离开方向的微差，标注相对进球线的偏向 | 实况：三档 spinY 各跑一次解析层，目标球碰后方向取自 `AnalyticAim.outcome().objPostContactDir` |
| ④ 加塞：挤偏（Squirt）+ 弧线（Swerve） | 挤偏：杆头打在球侧，出杆瞬间母球出发方向偏向加塞**反侧**（立即、与行程无关）。弧线：滑动段侧旋与台呢摩擦使轨迹微弯（越远/越慢越明显）。净效果 = 挤偏 + 弧线 + 到达后的 SIT，三者叠加无简单口诀 → 引出⑤ | 俯视轨迹图：杆指向线（瞄准线）、母球实际行进曲线、幽灵球位置三者分离，标注挤偏角与弧线段 | 实况：挤偏角 `CueBallStrike.squirtAngle`/`actualDirection`；碰前曲线 `AnalyticAim.outcome(collectSegments: true).preContactSegments` 采样 |
| ⑤ 自动求解在替你算什么 | 收拢①–④：求解器用完整物理链（杆-球碰撞含挤偏 → 滑动/滚动含弧线 → 球-球摩擦碰撞含投掷）**反向搜索**瞄准方向，使目标球碰后方向正对袋口；力度和塞是搜索输入，每换一档瞄准线重算。配定性速查表（方向不给度数）：轻力→投掷更大；大切角→投掷更大；高杆→偏薄需瞄厚；低杆→偏厚需瞄薄；左塞→挤偏向右+目标球被向右带…（表中每条符号须经 Z1 数值草稿核验后定稿） | 同一局面两档参数（中杆中速 vs 低杆轻推+左塞）下求解瞄准线对比 | 实况：两组参数各调 `prepareAim` + `positionAimOffset`，画 `aimDir.rotatedY(offset)` |
| ⑥ 实战启示 + 导流 | 边界感知：中杆中速小切角时偏差可忽略（角度瞄准法够用）；大切角+慢速+加塞必须补偿。页末导流「思路训练」 | 无图（或复用⑤图） | `PracticeCTA` → `.positionPlaySolver` |

### 2.2 引擎事实（正文与实况插图的数值真源）

- **几何基准瞄准 = 幽灵球方向**：`ShotPredictor.prepareAim` 返回 `AimContext.aimDir`（含进球管道修正的幽灵球方向）。
- **求解瞄准 = 基准 + 偏移**：`positionAimOffset(input:context:)` 一维搜索出 `aimOffset`（弧度），最终瞄准 = `aimDir.rotatedY(offset)`。**页面要讲的「与角度瞄准法的差异」在代码里就是这一个数：`aimOffset`**（①⑤ 节的 Δ 直接读它，不另造口径）。
- **挤偏**：`CueBallStrike.squirtAngle(a:)`（pooltool 移植，负值=向右偏；左塞 a>0 → 母球向右挤偏）；仅旋转速度方向不旋转角速度。
- **投掷**：`CollisionResolver.resolveBallBallPure`（pooltool frictional_inelastic 移植，Alciatore 摩擦拟合——接触面相对速度越低摩擦系数越大 ⇒ 慢球投掷更大）。
- **弧线/碰前轨迹**：`AnalyticAim.outcome(collectSegments: true)` 输出 `preContactSegments`（滑动→滚动常加速度段），段内位置 = `p + v·t + ½a·t²` 闭式采样。
- **交互控件档位**：力度 `ShotTuning.velocityRange = 0.5...8.0`（默认 1.5，γ=1.8）；打点上限 `CuePhysics.miscueLimitFraction = 0.5`。
- **性能**：`positionAimOffset` 走解析层一维搜索（黄金分割，单次数十候选），拖滑杆实时调用需去抖——复用 `SolveDebounceScheduler` 范式（20ms 去抖 + 单飞 + 末班车）。

**红线**：几何/角度/方向类正文、插图、速查表符号，执行时一律遵循 `geometry-spatial-reasoning`（坐标契约回显 + 数值草稿），禁止脑算断言偏向；术语过 `tasks/UI-IMPLEMENTATION-SPEC.md` §8.8 词表。

### 2.3 与 v11 Y2「旋转与加塞」的边界（D-v12-1 已拍板生效，2026-07-17）

v11 Y2（⏳未开工）原计划覆盖：母球旋转状态（分离角联动）+ **投掷效应** + **加塞三件套（squirt/swerve/throw）**——与本页深度重叠。已按 D-v12-1 重定：Y2 聚焦「母球旋转状态与分离角」，投掷/挤偏/弧线深讲归本页，两页互链（变更已记回 `问题集合_v11.md` v11.3）。

### 2.4 力度（母球速度）与瞄准修正的关系（2026-07-17 用户问询，口径确认已覆盖）

- **投掷**：速度影响最大——Alciatore 摩擦拟合下接触面相对速度越低摩擦越大 ⇒ **越慢投掷越大**（② 节主讲点）。
- **弧线**：慢速行程时间长 ⇒ 弧线累积更多（④ 节）。
- **挤偏**：基本与速度无关，只由横向打点决定（④ 节点明这一对比：三偏差源中唯一不随力度变的）。
- 力度是求解器搜索输入之一，①⑤ 节 Δ 实况图配力度滑杆即为此；正文无需新增节。

---

## 三、代码现状（事实，已核实 2026-07-17）

### 3.1 学区结构与注册点（同 v11 §3.1，复核仍有效）

- 学区 = 硬编码 SwiftUI 文档页。注册四处：`AngleRoute` 枚举 + `learnEntries`（`AngleHomeView.swift:5–35, 107–128`，现 5 卡含 Y1 已落的「瞄准方法」）、封面色（`AngleCoverPalette.swift:13–34` 学分组）、destination 映射（`MainTabView.swift:110–158`）。`project.yml` folder 源，新 .swift 自动进 target。学区无 Freemium 锁。
- 图文样式真源：`BTTableFigure`（`Core/Components/BTTableFigure.swift:15`）+ `FigureLine` + 段落卡（`.btTitle`/`.btBody`/`.btCaption` + `.btBGSecondary` 圆角卡）。
- 交互学页范式（本页直接模仿）：`AimingMethodsView.swift`——共享滑杆 + 多节实况 Figure 联动 + `accessibilityIdentifier("aimingMethods.*")` 供 UI 测试。
- 页末导流：`PracticeCTA`（`AngleHomeView.swift:514`），走 `NavigationLink(value: AngleRoute)`。

### 3.2 实况计算入口（本页新增依赖，v11 未涉及）

- `ShotPredictor.AimContext`（`ShotPredictor.swift:187–193`：`aimPoint/ghost/aimDir/geometry/pocketCenter`）；`prepareAim`（L197，可行性闸门，nil=几何不可进袋）；`positionAimOffset`（L1516，解析层）/`positionAimOffsetSimulated`（L1542，整程模拟口径，慢）。**页面用解析层版本即可**（与求解器搜索同路径）。
- `AnalyticAim.outcome`（`Core/Physics/AnalyticAimModel.swift:66–220`）：输入 aimDir/velocity/ShotInput，输出 `objPostContactDir`（目标球碰后方向）、`preContactSegments`（碰前路径段）、`cueAfterContact`。
- `CueBallStrike.squirtAngle`（`CueBallStrike.swift:92–98`）、`actualDirection`（L137–143，瞄准辅助线专用）。
- `CollisionResolver.resolveBallBallPure`（`CollisionResolver.swift`，throw 真源）。
- `ShotInput` 构造范式：`PositionPlaySolver.evaluate`（`PositionPlaySolver.swift:301–305`）。
- 打点盘 `BTSpinPad`（`Core/Components/BTSpinPad.swift:11–71`；spinY 正=高杆）；力度条可参考 `BTShotInstrumentColumn`（`BTShotInstrumentColumn.swift:14–22`）但学页嵌卡片内联更合适（对齐 AimingMethodsView 滑杆样式），执行批自行取舍并记入页面契约。
- 去抖范式：`SolveDebounceScheduler`（`PositionPlayViewModel.swift:1643`）。

### 3.3 UI 测试挂点

- `ScreenshotTourUITests.testAngleLearningPages`（`ScreenshotTourUITests.swift:170`）：学区轻量巡游，新页照 Y1 模式扩帧（`aimingMethods.*` identifier 先例 L224/239）。

---

## 四、拍板项（开工前需用户确认；带推荐）

| # | 问题 | 推荐 | 状态 |
|---|---|---|---|
| D-v12-1 | 与 v11 Y2「旋转与加塞」的内容边界（Y2 未开工，原范围含投掷效应+加塞三件套，与本页重叠） | **重定 Y2 边界**：Y2 改为聚焦「母球旋转状态与分离角」（滑动/滚动/前旋/后旋 + T01/T02/T03 + 最小加塞 T09、打滑极限），其中投掷/挤偏/弧线只留一句话概念 + CTA 指向本新页；深讲与「对瞄准的影响」全部归本页。v11 批次表同步记变更 | ✅ 按推荐（2026-07-17 用户代拍） |
| D-v12-2 | 术语确认（FL-026 强制）：用户原话「低杆容易把球打**后**」→ 采用定义「低杆容易把球打**厚**」（同一瞄准点下目标球离开方向比理想进球线更接近正碰侧）？ | 推测「后」为「厚」的笔误 | ✅ 采用「打厚」（2026-07-17 用户代拍） |
| D-v12-3 | 页名与卡片文案 | 标题「**瞄准修正**」，glyph「偏」，副标题「投掷 · 塞偏 · 弧线：几何之外的偏差」；封面色用暖橙-棕系（学分组现深绿/青绿/蓝/紫/灰，橙系无冲突，具体 RGB 执行批定 + 复用 `AngleCoverPalette` 注释惯例） | ✅ 按推荐（2026-07-17 用户代拍） |
| D-v12-4 | 执行模型（01 规则；按批次风险推荐路由） | **Z1**（横切基座：实况计算真源层 + 数值草稿，判断密集）→ `claude-opus-4-8-thinking-medium`；**Z2/Z3**（页面节实现，锚点明确）→ `cursor-grok-4.5-high-fast` + 主控逐文件读 diff 验收（沿用 D-v11-5 纪律） | ✅ 按推荐分批路由（2026-07-17 用户代拍） |

---

## 五、锚点索引（全部读代码核实，2026-07-17）

| 锚点 | 位置 | 用途 |
|---|---|---|
| `AngleRoute` / `learnEntries` | `QiuJi/Features/AngleTraining/Views/AngleHomeView.swift:5–35, 107–128` | 新页注册 |
| destination 映射 | `QiuJi/App/MainTabView.swift:110–158` | 新页注册 |
| 封面色（学分组） | `QiuJi/Core/DesignSystem/AngleCoverPalette.swift:13–34` | 新卡封面 |
| 交互学页范式 | `QiuJi/Features/AngleTraining/Views/AimingMethodsView.swift`（共享滑杆 L14–63；AX identifier 先例） | 页面骨架样式 |
| 导流卡 | `AngleHomeView.swift:514`（`PracticeCTA`） | ⑥ 节导流 |
| 瞄准上下文/求解偏移 | `QiuJi/Core/Physics/ShotPredictor.swift:187–193`（`AimContext`）、L197（`prepareAim`）、L1516（`positionAimOffset`）、L1542（Simulated 版，不用） | ①⑤ 节 Δ 真源 |
| 解析推演 | `QiuJi/Core/Physics/AnalyticAimModel.swift:66–220`（`outcome`；`objPostContactDir` L37、`preContactSegments` L53） | ③④ 节实况数据 |
| 挤偏 | `QiuJi/Core/Physics/CueBallStrike.swift:92–98`（`squirtAngle`）、L137–143（`actualDirection`） | ④ 节 |
| 投掷 | `QiuJi/Core/Physics/CollisionResolver.swift`（`resolveBallBallPure`，Alciatore 摩擦 L59 附近） | ② 节取样核验 |
| `ShotInput` 构造范式 | `QiuJi/Core/PositionPlay/PositionPlaySolver.swift:301–305` | 实况层输入 |
| 力度量程/打滑极限 | `QiuJi/Core/Physics/BTPhysicsConstants.swift:179–211, 233` | 控件档位 |
| 打点盘 | `QiuJi/Core/Components/BTSpinPad.swift:11–71`（spinY 正=高杆） | 控件 |
| 仪表柱（参考） | `QiuJi/Core/Components/BTShotInstrumentColumn.swift:14–22` | 控件取舍参考 |
| 去抖范式 | `QiuJi/Features/PositionPlay/ViewModels/PositionPlayViewModel.swift:1643`（`SolveDebounceScheduler`） | 滑杆实时重算 |
| 插图基建 | `QiuJi/Core/Components/BTTableFigure.swift:15` | 各节插图 |
| UI 测试挂点 | `QiuJiUITests/ScreenshotTourUITests.swift:170`（`testAngleLearningPages`） | Z3 扩帧 |
| 词表/口径 | `tasks/UI-IMPLEMENTATION-SPEC.md` §8.8 | 全批术语红线 |

---

## 六、批次表（Z1–Z3）

三批强顺序依赖（Z2/Z3 消费 Z1 的实况计算层）⇒ Z 批之间**全串行**，每批一次会话可完成并验收。与 v11 Y 批共享注册文件 ⇒ **Z 批与 Y 批不同波并行**——**该约束已失效（Y1–Y3 已于 2026-07-18 全部收官）**。
**跨方案并波（2026-07-18 用户确认）**：Z1 加入 v8 波2（**X4 ∥ X6 ∥ Z1**，worktree ×3）；业务代码零交集实证与合并顺序（X6→X4→Z1，Z1 合并后主树补跑 Z1 单测）见 `问题集合_v8.md` §三波次表注。Z2 可与 v8 波3（X5）同波（交集已核零重叠），Z3 收口串行。

| 批次 | 范围 | 量级 | 依赖 | 建议角色 | 模型（D-v12-4 拍板后回填） | 状态 |
|---|---|---|---|---|---|---|
| **Z1** | ①横切基座：新页 `AimingCorrectionView`（页名已定「瞄准修正」，D-v12-3）注册 4 处 + 页骨架（① 开篇节 + ⑥ 导流节 + 交叉引用）；②**实况计算真源层**（暂名 `AimingCorrectionMath`）：固定教学局面（~30° 切角典型位）下封装——几何基准 `aimDir`、求解偏移 Δ（`prepareAim`+`positionAimOffset`）、挤偏角、碰前轨迹采样、目标球碰后方向（`AnalyticAim.outcome`）；③**数值草稿**（坐标契约回显 + `build/z1-evidence/` 落盘）：核验 Δ 符号/量级、②⑤ 节速查表全部定性符号（高杆偏薄?低杆偏厚?左塞挤偏向右?轻力投掷更大?）——**这是内容正确性的地基，符号以草稿为准，正文不得先写后验**；④真源层单测 | L | D-v12-1/2/3 | iOS Architect + 物理接线（**几何红线：`geometry-spatial-reasoning` 强制**） | `cursor-grok-4.5-high-fast`（2026-07-18 用户改派，原 D-v12-4 定 opus-4.8；验收升级：数值草稿主控逐项核验 + 逐文件读 diff） | ✅ 2026-07-18（合并 a83bdce；速查符号 ALL_PASS：左塞挤右/轻力投掷大/高杆偏薄/低杆偏厚；留档：①节台面虚实线特写帧未单独取证，Z2 补） |
| **Z2** | ② 投掷效应节（碰撞放大 Canvas 图 + `resolveBallBallPure` 取样标注）+ ③ 高低杆节（三联实况对比图）+ 共享控件组第一期（高低杆三档 + 力度滑杆，接 Z1 真源层 + 去抖） | M | Z1 | SwiftUI Developer | `cursor-grok-4.5-high-fast`（D-v12-4 拍板；主控逐文件读 diff 验收） | ✅ 2026-07-18（合并 b1807e2；Z2 数值补稿 ALL_PASS：左塞 SIT 向右〔squirt 补偿隔离〕/CIT 半球见顶/慢速投掷大；①节特写帧留档已补；明暗 UI 巡游均 TEST SUCCEEDED；scheme 漂移已回退） |
| **Z3** | **（v12.5 改版，含风格重做）** ①**②③图风格重做**：`ThrowCollisionFigure` / `ThicknessTripleFigure` 从裸 Canvas（硬编码 RGB + 原生 Text 标签）改为学区统一插图语言——`BTTableFigure`（closeup 特写）+ `FigureLine` 色板 + `BTFigureBall`/`BTGhostCircle`/`BTContactDot`/`BTFigureTag`（③ 参照 `SpinAndEnglishView.SeparationPathsFigure` 单图三线+选中高亮范式）；②④ 加塞节（挤偏+弧线俯视轨迹实况图，控件组加左右塞轴）+ ⑤ 求解对比节（两档参数瞄准线对比 + 定性速查表，符号引 Z1/Z2 草稿结论）+ ⑥ 节收尾 + `ScreenshotTourUITests` 扩帧（a16）+ 明/暗截图核验 + SPEC 页面契约 + PROGRESS/hub 回写 | XL | Z2 | SwiftUI Developer | `cursor-grok-4.5-high-fast`（用户 2026-07-18 指定；主控逐文件读 diff 验收） | ✅ 2026-07-18（合并 424b9d2；②③→BTTableFigure；④左右塞+俯视轨迹；⑤A/B对比+速查；z3 草稿：平杆纯侧旋弧线≈0、组合档越慢越大；单测 14/0 + 明/暗 UI/a16 巡游绿；主树 build SUCCEEDED） |
| **Z4** | **（v12.5 新增）「旋转与加塞」页重做**（v11 Y2 产物 `SpinAndEnglishView`，用户反馈风格差异大+内容不完整）：①交互补齐至学区交互页水准（对齐 `AimingMethodsView` 范式：至少一处滑杆/动画级交互，如切角或力度驱动三路径实况联动）；②「加塞」半边内容补完（打点↔旋转产生机制、左右塞对吃库反弹的影响与实战用途——**深讲边界不越 D-v12-1**：挤偏/弧线/投掷深讲仍归「瞄准修正」页，此处只讲旋转本体+吃库效应+交叉引用）；③ `MiscueLimitFigure` 等自绘图对齐 token/组件语言；④ teaser「该页尚未上线」改为真 `PracticeCTA` 跳转 `.aimingCorrection`；⑤ a14 巡游帧适配 + 明/暗核验 + SPEC 回写 | L | Z3 | SwiftUI Developer | `cursor-grok-4.5-high-fast`（用户 2026-07-18 指定；主控逐文件读 diff 验收） | ✅ 2026-07-18（合并 e071ebf；θ 滑杆+教学折线 90/60/120；打点/吃库节；CTA 真跳转已 UI 测断言；几何 4/0 + Z4 UI 明/暗绿；全量 a14 巡游遇模拟器 signal-kill 环境抖，以专测+截图为准；证据 `build/z4-*`） |

### 依赖图

```
Z1 ─▶ Z2 ─▶ Z3 ─▶ Z4   （D-v12-1~4 已全部拍板；Z3 ✅ → Z4 可开工）
```

### 各批完成标准

**Z1**
1. `make build` BUILD SUCCEEDED（真实输出）。
2. 新卡出现在学分段（「瞄准方法」之后），导航可达，① ⑥ 节明/暗截图核验。
3. 数值草稿落盘 `build/z1-evidence/`：固定局面下 Δ(力度×spinY×spinX) 扫描表 + 速查表全部符号结论；坐标契约回显在会话中输出。
4. 真源层单测通过（至少：左塞 squirt 偏向断言、慢速 vs 快速 CIT 大小关系断言、Δ 与 `positionAimOffset` 一致性断言）。
5. 交叉引用不重复既有页成段内容（d=2R·sinθ 一句话带过 + 跳转）。

**Z2 / Z3（页面节）**
1. `make build` BUILD SUCCEEDED；Z1 单测回归通过。
2. 各节插图实况联动：拖控件插图实时更新、不卡顿（去抖路径有代码契约）；明/暗两态整页截图逐节核验（插图无溢出/标签无遮挡）。
3. 正文所有定性符号（打薄/打厚/偏左/偏右/更大/更小）与 Z1 草稿结论逐条对得上，禁止新增未核验断言。
4. 术语过 §8.8 词表；与「瞄准原理」「瞄准方法」「瞄准点对照表」重复处改交叉引用。
5. Z3 额外：`ScreenshotTourUITests` 学区巡游扩新页帧并通过（真实运行输出）；AX identifier 命名对齐 `aimingMethods.*` 先例；SPEC 页面契约 + Changelog、PROGRESS、project-hub 状态卡回写。
6. Z3 风格重做额外（v12.5）：②③插图不再含裸 Canvas 硬编码 RGB 球体/原生 Text 标签——球体/幽灵球/接触点/标签/线色一律走 `BTFigureBall`/`BTGhostCircle`/`BTContactDot`/`BTFigureTag`/`FigureLine`；夸大倍率标注保留且读数仍为真实数值。

**Z4（旋转与加塞重做）**
1. `make build` BUILD SUCCEEDED；`SpinAndEnglishGeometry` 既有不变量单测回归通过（90°/60°/120° 锁定不被破坏；几何扩展需配新单测）。
2. 页面获得至少一处连续交互（滑杆或动画），插图实况联动；明/暗整页截图逐节核验。
3. 加塞新内容全部有真源（引擎代码/数值草稿/项目 16 定理），禁止临场编造数值与定性符号；不越 D-v12-1 边界（挤偏/弧线/投掷深讲不搬回）。
4. teaser 替换为真跳转 `.aimingCorrection`；术语过 §8.8 词表。
5. a14 巡游帧适配通过（真实运行输出）；SPEC Changelog + PROGRESS + hub 回写。

---

## 七、明确不做（本轮范围外）

- 不改自动求解算法本身（`PositionPlaySolver` / `ShotPredictor` 求解路径零改动；真源层只读调用）。
- 不改「瞄准原理」「瞄准方法」页现有结构（只加交叉引用）。
- 不做 3D/站位视角版插图（俯视 2D 足够表达偏差）。
- 不做定量补偿口诀表（只给定性方向；具体数值随局面变化，引导用户用求解模式看实况）。
- 不给学区加 Freemium 锁；不做视频/出片素材。
- v11 的 Y2/Y3 本身的执行不在本文件范围（仅 D-v12-1 重定 Y2 边界，变更记回 v11 版本记录）。

---

## 八、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v12.0 | 2026-07-17 | 初版：M1 需求回显（交互式实况插图已拍板）+ 6 节内容口径（引擎真源对照）+ 锚点核实 + Z1–Z3 批次表 + D-v12-1~4 拍板项；标注与 v11 Y2 的边界重定依赖 |
| v12.1 | 2026-07-17 | D-v12-1~4 全部按推荐拍板（用户代拍）：Y2 边界重定（记回 v11 v11.3）、「打后」=「打厚」定稿、页名「瞄准修正」、模型分批路由（Z1 opus-4.8 / Z2·Z3 grok-4.5）回填批次表；新增 §2.4 力度与瞄准修正关系（用户问询，确认口径已覆盖、无需新增节） |
| v12.2 | 2026-07-18 | 跨方案并波定稿（用户确认）：Z1 加入 v8 波2（X4∥X6∥Z1，worktree ×3，文件交集实证零重叠、合并顺序 X6→X4→Z1）；Y 批约束失效标注（v11 已收官）；Z2 可与 v8 波3（X5）同波 |
| v12.3 | 2026-07-18 | Z1 模型改派 `cursor-grok-4.5-high-fast`（用户指定，原 D-v12-4 定 opus-4.8）；对冲：数值草稿由主控逐项核验 + 逐文件读 diff |
| v12.4 | 2026-07-18 | Z2 完成并合并主干（b1807e2，0 轮返工）：②投掷节（碰撞放大 Canvas + SIT/CIT 数值补稿，含两次首跑证伪记录——SIT 需 squirt 补偿隔离、CIT 非全程单调半球见顶）、③高低杆三联对比、共享控件第一期（力度滑杆+高低杆三档，20ms 去抖）；Z1 留档①节特写帧已补；证据归档主树 `build/z2-evidence/`、`build/z2-logs/`；剩 Z3 收口 |
| v12.5 | 2026-07-18 | **两页重做扩围（用户反馈：与其他学页风格差异大、内容不完整）**：Z3 改版=②③图风格重做（裸 Canvas → `BTTableFigure`/`FigureLine`/`BTFigureTag` 学区统一语言）+ 原④⑤⑥收口一次成型（量级 M→XL）；新增 Z4=「旋转与加塞」页重做（交互补齐 + 加塞内容补完〔不越 D-v12-1〕+ 图形 token 化 + teaser 改真跳转 `.aimingCorrection`）；Z3→Z4 串行，模型均 `cursor-grok-4.5-high-fast`（用户指定） |
| v12.6 | 2026-07-18 | Z3 完成并合并主干（424b9d2，0 轮返工）：②③→`BTTableFigure` 统一语言；④加塞节（左右塞轴+打滑极限钳制+俯视挤偏/轨迹）；⑤两档求解对比（A 中杆中速 / B 低杆轻推+左塞）+ 定性速查；z3 数值补稿 ALL_PASS（挤偏无关力度；平杆纯侧旋弧线≈0，正文未写「必弯」；组合档越慢越大）；证据 `build/z3-{evidence,logs,screenshots}/`；剩 Z4 |
| v12.7 | 2026-07-18 | **Z4 完成并合并主干（e071ebf）→ v12 全部批次收官**：切角 θ 滑杆联动三路径（示意角教学折线，非半球诚实标注）；打点→旋转 + 吃库顺/逆塞定性示意（T09/Jewett，非引擎实况）；teaser→真 CTA `.aimingCorrection`（UI 测断言导航栏「瞄准修正」）；SPEC §9.3；证据 `build/z4-*`。留档：全量 `testAngleLearningPages` 巡游多次遇模拟器 signal-kill（环境抖），页面专测明/暗已绿 |
