# 问题集合 v19 — 瞄准线与球杆同现（显隐口径统一）

> **本文件是本轮需求真源。** 高版本覆盖低版本；与 v18（可行打点谓词，⏳ 待拍板）文件域近乎零交集（本轮只加 `updateCueStick` 调用点，不改仰角/碰撞公式）。
>
> - **版本**：v19.5
> - **日期**：2026-07-27
> - **状态**：✅ 全部批次收官（W1，主控验收 0 轮返工，`cursor-grok-4.5-high-fast`）
> - **来源**：2026-07-27 会话——用户感觉「球杆出现逻辑不统一」，确认较正确的口径是「出现瞄准线时球杆就应该出现」；主控全场景读码后用户指令「该改的一起改」+ `/issue-collection-restructure`；同日确认要「挪瞄准杆跟着转」+ 认可回放/序列衔接口径并写入 W1
> - **排期前置**：无硬前置。可与 v18 并行（v18 动 `CueStick`/`BTSpinPad`/求解器谓词；本轮不碰）。`HUMAN-REQUIRED` 现存 ⏳ 均为发布类，不阻塞。
> - **锚点核实日期**：2026-07-27（§六全部逐条读代码确认）

---

## 一、需求回显

### 1.1 用户原始诉求

1. 看完所有球杆出现逻辑后，感觉**不统一**。
2. 较正确的口径：**当出现瞄准线的时候，球杆就应该出现**。
3. 「该改的一起改」——按主控给出的「该改 / 可留例外」清单整包落地，不是只改一页。

### 1.2 语义基准（不增删）

| # | 条目 | 验收语义 |
|---|---|---|
| **C1** | 产品默认契约 | **有稳定瞄准方向且台面正在画瞄准线 ⇒ 同步摆杆**（`updateCueStick`）；无线 / 无方向 ⇒ 藏杆 |
| **C2** | 翻袋 / 反射求解模式 | 解上屏画轨迹时**必须**摆杆（与自由模式 `refreshFreeAim` 对齐）；无解 / 清线时藏杆 |
| **C3** | 开球瞄准期 | `BreakFlowRunner` 在 `.racked` 画瞄准线时同步摆杆；离开瞄准（开球计算/取消）藏杆或交给 `runCueStroke` |
| **C4** | Composer / 自由走位拖动预览 | G14 拖动中的闭式瞄准线预览期间**同步摆杆**（不再 `showGeometryPreviewOnly` 里硬藏） |
| **C5** | 3D 瞄准点测验（AimPointScene） | 瞄准阶段用户白线出现时同步摆杆（沿**用户**瞄准，不泄正确答案）——见 D-v19-1 |
| **C6** | 明确不改（例外） | SceneAiming / AimingQuiz 辅助与结果可视化**继续无杆**（防泄题）；`.blocked` 仍藏杆；出杆动画由 `runCueStroke` 接管；缩略图/图文渲染/分离角图谱等无瞄准任务页保持藏杆 |
| **C7** | 瞄准 → 运杆 / 回放衔接（**已拍板** 2026-07-27） | 见下节 1.2.1 |

#### 1.2.1 C7 详细口径（瞄准态 ↔ `runCueStroke` ↔ 回放/序列）

**硬红线（全部主击 / 回放 / 序列演示）**

1. 瞄准态与出杆必须使用**同一** `aim` + `CueStroke.strikePosition(cue:aim:spinX:)`（禁止瞄准带加塞偏移、出杆却喂球心）。
2. **禁止**在即将 `runCueStroke` 前无故 `hideCueStick`（会造成藏→show 闪帧）。`runCueStroke` 本身从 `drive(0)+show` 起手，已是瞄准贴球姿态。
3. 触球后仍由现有跟杆 / 淡出收杆；**不要**在球体回放全程把杆杵在桌上。

**直播击球**：瞄准期已摆好的杆 → 直接 `runCueStroke`（无缝开始回杆），中间不藏。

**「上一杆」回放**：摆回击打前盘面 → 用该杆 `aim`/`spinX` 摆静止瞄准（定格 **0～~0.2s**，允许 0：杆一出现即运杆）→ `runCueStroke`（出杆前不 hide）。

**序列逐步演示**：每步摆盘 → 有预告线则按 C1 同现杆 → 静止瞄准定格 **~0.3～0.5s**（教学可读，接近导出器 `showCueAtRest`）→ `runCueStroke`（出杆前不 hide）。

### 1.3 主控理解 / 假设 / 不确定点

**理解的需求**：把「线」与「杆」从各页私有习惯收敛成一条全局交互契约，修掉已确认的违规调用点，并写进 SPEC，防回流；瞄准跟手 + 出杆/回放无缝衔接。

**假设**（与推荐拍板一致，不符请在 D-v19-* 纠正）：

- A1：测验里「辅助线 = 正确答案几何」的页（SceneAiming）继续无杆；「线 = 用户当前瞄准」的页（AimPointScene）要有杆。
- A2：本轮**只改显隐时机与衔接调用顺序**，不改杆几何 / 抬杆 / `CueClearance` / 物理。
- A3：Silu / PlanThree / Snooker / PositionPlay **停稳有解后**、Bank/Diamond **自由模式**、导出器已基本符合 C1，本轮以**回归不破坏**为主，不重构它们。
- A4（已确认）：用户要「挪瞄准时杆跟着转」；回放/序列按 C7，不出杆前闪藏。

**不确定点 → 拍板项**：见 §五（D-v19-1/2/4 仍待定；D-v19-3 与 C7 已定）。

---

## 二、现状诊断（事实）

### 2.1 单一底层、多页政策

显隐最终只经 `AngleTrainingScene.updateCueStick` / `hideCueStick` / `runCueStroke`。各 VM 自行决定何时调用——**没有**「画瞄准线必须摆杆」的共享约束，故出现同 App 多套政策。

### 2.2 违规矩阵（相对 C1）

| 场景 | 有瞄准线时 | 球杆 | 相对 C1 |
|---|---|---|---|
| Silu / PlanThree / Snooker（解上屏） | `TrajectoryRenderer` | 同刻 `updateCueStickAiming` | ✅ 已对齐 |
| PositionPlay 停稳有解 | 轨迹线 | 同刻 `updateCueStickAiming` | ✅ |
| PositionPlay **G14 拖动预览** | `drawFreeAimPreviewLine` | `showGeometryPreviewOnly` **硬藏** | ❌ → C4 |
| Bank / Diamond **自由** | `refreshFreeAim` | 同函数 `updateCueStick` | ✅ |
| Bank / Diamond **求解** | `drawSolution` → `TrajectoryRenderer` | `toggleMode→solve` 藏杆且 `drawSolution` **从不摆杆** | ❌ → C2 |
| 开球 `BreakFlowRunner` | `drawAimLine` | 瞄准期无调用；仅 `breakNow→runCueStroke` | ❌ → C3 |
| AimPointScene 瞄准 | `redrawLines` 白线 | `nextQuestion` 藏杆，瞄准期不摆 | ❌ → C5（待 D-v19-1） |
| SceneAiming 辅助/结果 | `updateVisualization` | 故意 `hideCueStick` | 例外 → C6 |
| 导出器 | `drawAimLines` | `showCueAtRest` 成对 | ✅ 保持 |
| Composer「上一杆」/序列演示 | 出杆前常先 `hideCueStick` | 再 `runCueStroke`→show | ❌ 闪帧 → C7 |
| Bank 求解主击 | 出杆喂 `cueNode.position` | 自由瞄准用 `strikePosition(spinX)` | ❌ 参数不一致 → C7 |

### 2.3 根因一句话

不是球杆组件坏了，而是**「画线」与「摆杆」从未被规定为同一生命周期事件**——翻袋求解、开球、Composer 拖动、AimPoint 瞄准各自漏接；回放/序列又在出杆前多藏一次杆，打断 `drive(0)` 连续衔接。

---

## 三、批次表

### 依赖图

```
W1（全量调用点 + SPEC 契约 + 验收）
  └── 无前置；与 v18 无硬依赖
```

> **批次数量取 1**：六处改动均为「在已有画线函数旁补/改 `updateCueStick`」量级（约 5–20 行/处），共享同一验收口径；拆多批会重复灌入本诊断与锚点，得不偿失。若执行中上下文吃紧，允许执行者先交 C2+C3，再同会话续 C4+C5，但仍算 **W1 一单**。

### W1 — 瞄准线 ⇔ 球杆同现

| 项 | 内容 |
|---|---|
| **范围** | C1～C7 全落地：Bank/Diamond `drawSolution` 摆杆 + 求解出杆改 `strikePosition(spinX)`；Break `drawAimLine` 摆杆；PositionPlay `showGeometryPreviewOnly` 跟杆；**AimPointScene `redrawLines` 跟杆（D-v19-1 ✅）**；回放/序列去掉出杆前 hide + 按 C7 短定格；**SceneAiming 保持无杆（D-v19-2 ✅）**；SPEC §8.9 追加契约 **h**（含衔接红线）；grep 审计无回流 |
| **量级** | S～M（约 6–8 业务文件 + SPEC + 可选轻量单测/UI 冒烟） |
| **依赖** | 拍板齐；D-v19-4 已写入 |
| **模型** | `cursor-grok-4.5-high-fast`（2026-07-27 用户确认「都用 grok4.5」） |
| **状态** | ✅ 2026-07-27（返工 0 轮；主控落盘产物亲验） |
| **不动** | `CueStick`/`CueClearance`/`CueStroke` **公式**（可改调用参数与时机）；引擎；SceneAiming 泄题例外；v18 谓词 |

**主控验收结论（2026-07-27，0 轮返工）**

- diff 逐文件亲读（Bank/Diamond/Break/PositionPlay/AimPointScene）；未越界公式/v18/SceneAiming。
- 落盘日志亲核：`make-build.log` L45 SUCCEEDED；`unit-tests.log` 42/0；`ui-tests.log` 6/0；`ui-composer-free.log` 1/0。
- 主控亲跑主树 `make build` **BUILD SUCCEEDED**（`orchestrator-main-build.log`）。
- 截图 01–06 逐张目视：翻袋/开球/Composer 自由/AimPointScene(2D 模式同 VM)/回放静止瞄准有杆；SceneAiming 辅助有线无杆。
- SPEC §8.9 **h** + Changelog + DR-028 已在位。
- **留档**：Diamond 无单独截图（代码镜像）；序列逐步无独立录屏（C7 路径靠代码+上一杆帧）；全量 QiuJiTests 未跑；AimPoint 取证为 Scene 的 2D 入口（`AimPointSceneQuizViewModel` 同源，非旧 `AimPointTrainingView`）。

**完成标准（全部满足才算 ✅）**

1. **行为（瞄准同现）**
   - 翻袋 / 反射：求解模式有解时台面有瞄准相关线 **且** 球杆可见、朝向与 `prediction.aimDirection` 一致；切自由再切回求解，杆不丢；无解/清空无线时杆隐藏。
   - 开球：`.racked` 有瞄准线时杆可见；拖母球/调向杆跟随；点开球后由 `runCueStroke` 接管（无「双杆」残影）。
   - Composer / ShotSim / FreePlay（皆 `PositionPlayViewModel`）：自由模式拖瞄准期间有预览线时杆**实时跟随转向**；松手求解完成后杆仍在；袋口模式拖动中若仍无线则仍无杆（遵守 C1「有线才有杆」）。
   - AimPointScene（D-v19-1=纳入时）：瞄准阶段杆沿用户白线；提交后出杆无缝衔接。
   - SceneAiming：辅助开/结果态仍无瞄准杆（回归）。
2. **行为（C7 衔接）**
   - 凡主击 / 「上一杆」/ 序列演示：出杆前 **无** `hideCueStick` → `runCueStroke` 的紧邻配对（grep 复核）。
   - 瞄准态与出杆 `aim` + `strikePosition(..., spinX:)` 一致（含 Bank/Diamond 求解主击，禁止球心冒充击球点）。
   - 「上一杆」：击打前盘面恢复后有静止瞄准（0～0.2s 可）再运杆，无闪藏。
   - 序列逐步：每步有 ~0.3～0.5s 静止瞄准（有预告线则杆同现）再运杆，无闪藏；触球后杆仍淡出。
3. **契约回写**：`tasks/UI-IMPLEMENTATION-SPEC.md` §8.9 追加 **h. 球杆与瞄准线同现**（含 C7 衔接红线），Changelog 记一行；来源本文件。
4. **审计**：`rg 'hideCueStick|updateCueStick|drawAimLine|drawSolution|showGeometryPreviewOnly|redrawLines|runCueStroke' QiuJi` 人工过一遍——确认无「画线后无条件藏杆」、无「`runCueStroke` 前无故 hide」。
5. **构建**：`make build` **BUILD SUCCEEDED**（真实日志）。
6. **测试**：至少跑与改动页相关的既有 UI/单测子集不红（Bank/Diamond ScreenshotTour 或等价；Break / Composer / AimPoint 相关套件若环境可跑则跑；跑不了须如实写「未验证」+ 截图取证）。**禁止**为变绿改断言语义。
7. **截图取证**（建议目录 `build/v19-evidence/`）：翻袋求解有杆、开球瞄准有杆、Composer 拖动中有杆、AimPoint 瞄准有杆（若纳入）、SceneAiming 辅助仍无杆、序列或上一杆「出杆前无闪藏」各 ≥1 张（或录屏短片段）。
8. **台账**：`PROGRESS.md` + Hub 状态卡回写；契约级变更记 `DR-028`（球杆显隐与瞄准线绑定 + 运杆衔接）。

---

## 四、明确不做（本轮）

| 不做 | 原因 |
|---|---|
| 抽共享 `AimLineAndCuePresenter` 大重构 | 各页画线 API 不同（`TrajectoryRenderer` / 手写 `addLine` / `AimLineGeometry`），强抽收益低；本轮以调用点对齐 + SPEC 契约防回流即可 |
| 改 SceneAiming 为有杆 | 辅助线含正确答案几何，摆杆等于泄题（C6） |
| 改仰角 / 穿模 / 可行打点 | 属 DR-027 / v18 |
| 为「看起来齐」在无瞄准线的浏览态硬插杆 | 违反 C1 与线语言「不为装饰画线」 |
| 回放/序列全程不显示杆，或触球后杆一直留在桌上 | 与 C7 / 现有收杆语义冲突 |

---

## 五、拍板项

| 编号 | 问题 | **推荐** | 状态 |
|---|---|---|---|
| **D-v19-1** | AimPointScene（3D 瞄准点）瞄准期是否跟杆？ | **纳入（跟杆）**——线是用户瞄准，不泄题 | ✅ **已拍板**（2026-07-27，「D-v19-1 和 D-v19-2 都加上」＝按推荐纳入） |
| **D-v19-2** | SceneAiming / AimingQuiz 是否继续无杆？ | **继续无杆** | ✅ **已拍板**（2026-07-27，同上＝按推荐保持无杆） |
| **D-v19-3** | Composer G14 拖动预览是否跟杆？ | **跟杆**（挪瞄准杆实时转向） | ✅ **已拍板**（2026-07-27，用户确认要真实跟手） |
| **D-v19-3b** | 回放/序列衔接是否按 C7？ | **是**（禁出杆前 hide；上一杆短定格；序列 0.3～0.5s；同 `strikePosition`） | ✅ **已拍板**（2026-07-27，「好的，可以」） |
| **D-v19-4** | 执行模型 | **子智能体执行 + 主控独立验收** | ✅ **已拍板**（2026-07-27，「都用 grok4.5」→ `cursor-grok-4.5-high-fast`） |

> 拍板齐；W1 已派发执行子智能体。

---

## 六、锚点索引（2026-07-27 核实）

| 锚点 | 路径 | 说明 |
|---|---|---|
| 摆杆/藏杆 API | `QiuJi/Core/Scene/AngleTrainingScene.swift:315-356` | `updateCueStick` / `hideCueStick`；`.blocked` 内藏 |
| 出杆接管 | `QiuJi/Core/Scene/CueStroke.swift:141+` | `runCueStroke` 自 `show`，跟杆后淡出 |
| Bank 自由（已对齐范本） | `…/BankShotViewModel.swift:583-623` | `refreshFreeAim` 画线 + `updateCueStick` |
| Bank 求解（待修） | 同文件 `:528-547` `toggleMode` 进求解藏杆；`:1098-1135` `drawSolution` 只画线；`:1000-1012` `presentDisplayedSolution` → `drawSolution` | 修：`drawSolution` 末尾按 `sol.prediction.aimDirection` + `sol.spinX` 摆杆；`clearPath`/无解路径藏杆 |
| Diamond（镜像） | `…/DiamondSystemViewModel.swift:562-599` / `:1012+` / `:940-951` | 与 Bank 同口径改 |
| 开球瞄准（待修） | `QiuJi/Core/Rack/BreakFlowRunner.swift:188-223` `drawAimLine`；`:241` 开球清线 | 修：画线成功后 `updateCueStick`；early-return / 清线时 `hideCueStick`（`runCueStroke` 会再 show） |
| Composer 拖动（待修） | `…/PositionPlayViewModel.swift:696-707` `showGeometryPreviewOnly` 现硬藏；`:710-722` 预览线；`:896-905` 停稳摆杆范本 | 修：有 `freeAimDir`（或等价方向）时预览期 `updateCueStick`，勿无条件 hide |
| ShotSim 宿主 | `…/ShotSimulationView.swift:9-10` | 复用 `PositionPlayViewModel`，无第二套逻辑 |
| AimPointScene（待修） | `…/AimPointSceneTrainingView.swift:71` 出题藏杆；`:247-307` `redrawLines`；`:211-212` 验证出杆 | 修：`redrawLines` 在 aiming 且有用户线时摆杆（`spinX:0` 与现出杆一致） |
| SceneAiming 例外 | `…/AimingQuizViewModel.swift:301-321` / `:343` | **保持** `hideCueStick` |
| 已对齐三页 | Silu `:566` / PlanThree `:709` / Snooker `:448` + 各自 `updateCueStickAiming` | 回归勿破坏 |
| 导出器已对齐 | `SequenceVideoExporter.swift` `drawAimLines` + `showCueAtRest` | 不动；序列定格时长可对照 |
| Composer 上一杆出杆前 hide（待修） | `PositionPlayViewModel.swift:1196` 附近 | 删紧邻 hide；补静止瞄准再 `runCueStroke` |
| 序列演示出杆前 hide（待修） | 同文件 `:1481` 附近 | 同上 + 0.3～0.5s 定格 |
| Bank 求解出杆球心（待修） | `BankShotViewModel.swift:330-332` | 改 `CueStroke.strikePosition(..., spinX:)` |
| `runCueStroke` 起手 | `CueStroke.swift:172-174` | `drive(0)+show`——衔接真源，勿在调用前 hide |
| SPEC 落点 | `tasks/UI-IMPLEMENTATION-SPEC.md` §8.9（现 a–g；本轮追加 **h**，含 C7） | Changelog 一行 |

### 建议补丁形态（执行指引，非替代读码）

```swift
// Bank/Diamond.drawSolution 末尾（有 cue + aimDirection 时）
scene.updateCueStick(
    cueBallPosition: CueStroke.strikePosition(cue: cue.position, aim: pred.aimDirection, spinX: Double(sol.spinX)),
    aimDirection: pred.aimDirection
)

// BreakFlowRunner.drawAimLine 成功画线后
scene.updateCueStick(
    cueBallPosition: CueStroke.strikePosition(cue: cue.position, aim: dir, spinX: spinX),
    aimDirection: dir
)
// early-return 或 clear aimNodes 且不再画线 → hideCueStick()

// PositionPlay.showGeometryPreviewOnly：删除无条件 hide；
// 自由模式在 drawFreeAimPreviewLine 同时 updateCueStick(dir: freeAimDir)
```

---

## 七、与相邻方案的边界

| 方案 | 关系 |
|---|---|
| **v18 可行打点** | 正交。v18 回答「这个打点摆不摆得出杆」；v19 回答「该不该在瞄准时显示杆」。并行时勿改 `requiredElevation` / `isCuePoseClear`。 |
| **DR-027 穿模** | 已落地。v19 继续走现有 `updateCueStick`（含 `.blocked` 藏杆），不回退守卫。 |
| **SPEC §8.9 G14** | v19 **不推翻**「拖动中不求解」；只改预览期球杆显隐，求解去抖时间不变。 |

---

## 八、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v19.5 | 2026-07-27 | **W1 ✅ 收官**（主控验收 0 轮返工）：落盘产物亲验 + 主树 build 绿；v19 全部批次完成 |
| v19.4 | 2026-07-27 | W1 执行交卷：C1–C7 + SPEC h + DR-028 + 证据 `build/v19-evidence/`；待主控验收改 ✅ |
| v19.3 | 2026-07-27 | D-v19-4 ✅：全程 `cursor-grok-4.5-high-fast`；W1 派发执行 |
| v19.2 | 2026-07-27 | D-v19-1（AimPoint 纳入）/ D-v19-2（SceneAiming 继续无杆）✅；用户要求子智能体执行；批次表增「模型」列待写入 |
| v19.1 | 2026-07-27 | 增补 **C7**（瞄准→运杆 / 回放·序列衔接）：禁出杆前 hide、同 `strikePosition`、上一杆短定格、序列 0.3～0.5s；D-v19-3 / D-v19-3b ✅；W1 完成标准与锚点同步 |
| v19.0 | 2026-07-27 | 首次落盘：C1–C6 + 单批 W1 + 拍板 D-v19-1～4；锚点全核实 |
