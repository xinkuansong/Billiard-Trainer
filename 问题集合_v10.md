# 问题集合 v10 — 训练/记录 drill 缩略图统一修复

> **本文件是本轮需求的真源。** 日期：2026-07-17　状态：✅ W1 收官（主控落盘产物亲验通过，0 轮返工）　排期前置：无
> 与 `问题集合_v8.md`（练习 Tab 反馈）、`问题集合_v9.md`（分享保存相册卡死）均**无关**，互不覆盖。本轮仅收敛「drill 缩略图不统一」这一个 bug。
> 全部锚点已读代码核实（2026-07-17，见 §五）。编号约定：本轮单条缺陷用 **B1**；批次用 **W1**。

---

## 一、需求回显（语义基准，不增删）

| # | 用户诉求（原意） | 验收语义 |
|---|---|---|
| B1 | 训练流程与训练记录中，drill 仍残留旧的「每个 drill 的 canvas 手绘图标」，没有用「动作库」里 drill 的缩略图 | 训练总结、历史详情等所有列出 drill 的地方，缩略图统一为动作库同款（离线烘焙的顶视 PNG，按 drillId 取图），不同 drill 显示不同缩略图 |

---

## 二、代码现状与根因假设（事实 / 推测分栏）

### 事实（已读代码，2026-07-17）

「正确」缩略图 = 共享组件 `BTBakedDrillTable(drillId:)` → `DrillThumbnailStore` → `Bundle/DrillThumbnails/<drillId>.png`。以下场景**已对齐**：动作库列表/收藏、训练进行中总览/记分行（`BTExerciseRow`）、计划详情迷你台。旧 per-drill Canvas 台（`BTMiniTable`/`BTDrillTableView`）已删除（DR-015/016）。

**残留未收齐的三处**：

| 场景 | 当前渲染 | 数据是否具备 drillId |
|---|---|---|
| 训练总结明细 | `BTTrainingIcon`（Canvas 三球+杆，所有 drill 同图） | ❌ `DrillSummary` 结构体缺 `drillId`（需补字段） |
| 历史训练详情 | 私有 `miniTableThumbnail`（通用绿毡台，所有行同图） | ✅ `DrillEntry.drillId` 已有，未接线 |
| 自定义计划构建器 | 私有 `miniTableThumbnail`（同上） | ✅ `CustomDrillItem.drillId` 已有，未接线 |

### 根因假设（推测，执行时验证）

迁移到烘焙缩略图（DR-016）时**训练收尾页与历史详情未收齐**，仍用与具体 drill 无关的通用图标。修复方向：三处统一改为 `BTBakedDrillTable(drillId:)`，其中总结页需先给 `DrillSummary` 补 `drillId`。

> 无需改动作库、`DrillContent` schema，或重新引入 Canvas 手绘台。Tab 图标 `BTTrainingIcon` 保留（非 per-drill 用途）。

---

## 三、批次表

本轮工作量小、三处共用同一修复模式（换成 `BTBakedDrillTable(drillId:)`）、验收方式一致，**合并为单批次 W1**，一次会话可完成并验收。

| 批次 | 范围 | 量级 | 依赖 | 建议角色 | 模型 | 状态 |
|---|---|---|---|---|---|---|
| **W1** | B1：训练/记录 drill 缩略图统一为烘焙图 | S（2 View + 1 ViewModel + 1 计划构建器） | 无 | SwiftUI Developer | `cursor-grok-4.5-high-fast`（2026-07-17 用户确认） | ✅ |

### W1 改动点

| 项 | 改动点 | 说明 | 依赖 |
|---|---|---|---|
| W1-1 | `DrillSummary` 增加 `drillId: String`；`drillSummaries` 计算属性填入 `drill.drillId` | 数据缺口修补 | 无（W1-2 前置） |
| W1-2 | `TrainingSummaryView.drillThumbnail` → `BTBakedDrillTable(drillId: drill.drillId)` | 替换 `BTTrainingIcon`，保留 48×48 尺寸与圆角/描边 | W1-1 |
| W1-3 | `TrainingDetailView.drillCard` → `miniTableThumbnail` 换为 `BTBakedDrillTable(drillId: entry.drillId)` | setRow 有 `.padding(.leading, 52)` 需与缩略图宽度对齐 | 无 |
| W1-4 | `CustomPlanBuilderView.drillRow` → `miniTableThumbnail` 换为 `BTBakedDrillTable(drillId: item.drillId)` | 一并收齐 | 无 |

### 依赖图

```
W1-1 ──▶ W1-2
W1-3  (独立)
W1-4  (独立)
```

---

## 四、W1 完成标准（可验证）

- [x] 训练总结页每个 drill 卡显示**该 drill 对应的烘焙缩略图**（不同 drill 图不同），非 `BTTrainingIcon`。
- [x] 历史训练详情每行 drill 显示对应烘焙缩略图，非通用毡台。
- [x] 自定义计划构建器每行显示对应烘焙缩略图。
- [x] 缺失 PNG 时回退到 `BTBakedDrillTable` 内置台呢占位（不崩溃、不空白）。
- [x] 尺寸/圆角/描边与原布局协调（总结 48、历史 40、计划 56，或按视觉统一）；历史详情 setRow `.padding(.leading, 52)` 与新缩略图对齐。
- [x] `xcodebuild` 构建通过（附真实输出）。
- [x] 三处各截一张图佐证。注：UITest 全页导航不稳定已放弃；改用 ImageRenderer 落盘三处生产同尺寸/同 `BTBakedDrillTable(drillId:)` 接线镜像 + 缺图占位对照（见 `build/w10-1-screenshots/`）。

### 红线

- 只做缩略图数据源替换，不借机重做卡片视觉/布局。
- 删除私有 `miniTableThumbnail` 后须确认无其他调用点。
- 缺图不得静默空白，须走 `BTBakedDrillTable` 占位。

---

## 五、锚点索引（2026-07-17 读码核实）

| 锚点 | 路径 | 行号/符号 | 备注 |
|---|---|---|---|
| 正解组件 | `QiuJi/Core/Components/BTBakedDrillTable.swift` | L6–46 `DrillThumbnailStore` / `BTBakedDrillTable` | 按 drillId 读 PNG |
| 已对齐参考实现 | `QiuJi/Core/Components/BTExerciseRow.swift` | L49–62 `thumbnail` | drillId → 烘焙图 + fallback |
| `DrillSummary`（缺 drillId） | `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift` | L66–84 | 需补 `drillId` 字段 |
| `drillSummaries` 构造 | 同文件 | L534–551（`ActiveDrill.drillId` @ L11） | 可取 `drill.drillId` |
| 训练总结缩略图 | `QiuJi/Features/Training/Views/TrainingSummaryView.swift` | L247–258 `drillThumbnail`（`drillCard` L197–245） | 现用 `BTTrainingIcon` |
| 历史详情缩略图 | `QiuJi/Features/History/Views/TrainingDetailView.swift` | L154–171 `miniTableThumbnail`（`drillCard` L114–153，缩进 L139） | 现用私有毡台，`entry.drillId` 已有 |
| 自定义计划缩略图 | `QiuJi/Features/Training/Views/CustomPlanBuilderView.swift` | L253–277 `miniTableThumbnail`（`drillRow` L216–252） | `CustomDrillItem.drillId` 已有 |
| `CustomDrillItem.drillId` | `QiuJi/Features/Training/ViewModels/CustomPlanBuilderViewModel.swift` | L4–6 | — |

---

## 六、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v10 | 2026-07-17 | 立项：B1 训练/记录 drill 缩略图残留旧 canvas 图标；单批 W1（4 改动点）；锚点经子智能体 + 主控读码双重核实。原写入 v9 被另一方案覆盖，重写为 v10 与之划界 |
| v10.1 | 2026-07-17 | W1 收官（plan-delegated-execution：grok-4.5 执行 + 主控亲验，0 轮返工）。三处接线 diff 亲读、`miniTableThumbnail` 全仓 0 命中、build 日志 L1767 SUCCEEDED、渲染测 4/0、截图 01–04 逐张目视核验（不同 drill 图不同、缺图占位非空白）、主控亲跑主树构建 SUCCEEDED。**留档偏差**：① destination 本机无 iPhone 16 Pro → iPhone 17 Pro；② 截图为 `W10_1_BakedThumbnailRenderTests` ImageRenderer 同尺寸接线镜像（UITest 全页导航不稳定），非模拟器全页导航截图——真实页面导航效果留待用户日常使用/人工冒烟确认 |

---

## 七、执行入口

新会话说：**「执行问题集合 v10 批次 W1」**（或「统一训练/记录 drill 缩略图」）。

执行前按 `plan-batch-execution`：锁本文件为真源 → 回显锚点与红线 → 改码 → 按 §四 完成标准实证验收 → 回写本文件状态与 `tasks/PROGRESS.md`。
