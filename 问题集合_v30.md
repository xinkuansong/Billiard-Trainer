# 问题集合 v30 — 16 理论内容导入练习 Tab 学区（球理中心第一批）

> **真源声明**：本文件是「把 16.billiard_theory 理论内容放进 App 练习 Tab 学区」这一轮需求的**真源**，
> 供 `plan-batch-execution` / `plan-delegated-execution` 执行。
> **日期**：2026-08-07 **状态**：待拍板（D-v30-1/2 未裁定，不得开工）
> **与既有线的关系**：
> - 本方案落实并部分提前 `tasks/phases/P12-content-system-theory.md` 的 T-P12-02（理论挂接 schema + vendor contracts）、
>   T-P12-03（理论详情页 + 深链）、T-P12-04（闭环记账）、T-P12-06（学区升级为球理索引，**提前到本轮**）。
>   选址结论沿用 ADR-P12-01（理论之家 = 练习 Tab 学区，非动作库、非第 6 Tab），本轮不再重议。
> - 与 `问题集合_v26`（精讲迁移线，执行中）**正交**：v26 只动 drill 精讲/元数据，本方案只动学区 + 理论内容 +
>   少量挂接字段；唯一交叠点是 W5 给 c042 等 drill 精讲加理论 chip，排在 v26 W7（positioning·B 含 c042）之后执行以免冲突。
> **上游内容真源**：`/Users/song/projects/16.billiard_theory/`（theory/*.md + contracts/*.json，
> 版本 v1.0-rc4，定理状态 candidate）。本方案 vendor 快照进 13，后续 16 修订走版本升级通道（见 W0）。

---

## 一 需求回显（用户拍板结论，语义基准）

1. **选址（2026-08-07 拍板）**：理论内容放**练习 Tab 学区**（「学」侧栏分类下），不放动作库、
   不加与「学」并列的新类别。学区升级为「球理」中心的结构化索引（ADR-P12-01 阶段 2 提前）。
2. **范围（2026-08-07 拍板，用户嫌 5 篇太少、认可 12 篇）**：第一批 = **10 条定理（T01–T10）+
   5 步决策流程 + 速查表，共 12 篇**。M01–M06 固定解模块明确推后（等战术类 drill 上线时与之配套导入）。
3. **配图分级（成本账，用户认可）**：
   - T01/T02/T03/T09：复用现有 `AngleTrainingScene` 标注能力 / 现有学页互链，近零成本；
   - T08 矩阵 / T10 层级 / 5 步流程 / 速查表：**原生 SwiftUI 组件呈现**（表格/清单/步骤卡），不做图片；
   - T04：文字版 + 深链现有分离角演示页（5 档速度已消费 T04），参数扫描叠加图**本轮不做**；
   - T05/T06/T07：**文字先行**，战术示意局面图登记待办（人工编排台录制，红线：AI 不生成球形坐标）。
4. **转写纪律（本会话确认的判断）**：16 原文是调研级文本，**不得原样搬**。每篇产出 App 版：
   保留一句话陈述 / 直觉解释（教练话）/ 适用边界 / 常见误区；剔除来源编号、推导、Status 元数据、英文引文；
   正文禁教材腔（禁「根据定理 TXX」句式，编号只作数据标签与页面编号展示）。
5. **顺带闭环（用户认可「顺手闭环」）**：vendor `16/contracts/*.json` + `theoremIds` 可选字段 +
   ≥1 条 drill（c042）实际挂接消费，达成 16 项目 v1.0 final 闭环条件，双方记账。
6. **默认假设（未拍板，如不同意请推翻）**：理论页全部免费（不进 Freemium 门）；理论详情页深链在
   当前 Tab 栈内 push（不跳 Tab）；16 定理仍为 candidate 状态不阻塞导入，但页面数据结构须留修订升级通道。

---

## 二 现状关键事实（2026-08-07 读码/实跑核实）

- **学区现状**：练习 Tab（`AppTab.angle`，标题「练习」）首页 `AngleHomeView` 侧栏 学/练/打/解；
  「学」入口卡 9 张（`learnEntries` 硬编码数组）。现有学页（瞄准原理/旋转与加塞/分离角图谱等 9 个）
  概念上覆盖 T01–T03/T09 的图解，但无编号体系、彼此靠 `LearnDocTextLink` 零星互链。
- **无理论基建**：`QiuJi/Resources/` 无 `Theory/` 目录；`DrillContent` 无 `theoremIds`；
  `TutorialSection` 无 `theoremRefs`；App 运行时数据中无任何 T/M 编号（curriculum-map 与写作工具层有）。
- **16 侧输入现成**：`contracts/theorem-tags.json` 每条定理含 `name_zh` / `statement_one_liner`（≤60 字）/
  `key_formula` / `key_data` / `common_errors` / `applicable_modules` / `doc_path`；
  `run-out-flow.json` 是 5 步流程的完整状态机（含各步 tasks/outputs/时间预算）；
  定理正文 `theory/theorems/T01–T10.md` 每篇 107–220 行；`quick-reference.md` 218 行。
  12 篇合计约 2000 行 markdown 输入。
- **JSON + 渲染器先例现成**：`DrillContentService` 的 Bundle JSON decode 模式 + `DrillTutorialView`
  的 section 渲染（tutorialKind / items / 富块）可整套参照，新建理论线不必发明新模式。
- **T04 已被消费**：`BTPhysicsConstants.swift` L137–173 `StrokePhysics.SpeedLevel` 五档速度
  注明来自 16 T04（2026-06-03 落地），理论页可反向链接该演示。
- **v26 执行中**：W1 试点三轮返工中，W2–W12 内容批未开工。本方案 W5 与 v26 W7 有 c042 交叠，已排序规避。

## 三 待拍板项

> 每项标注它卡住哪一批。**未拍板不得开工对应批次。**

| 编号 | 事项 | 卡住 | 裁定 |
|---|---|---|---|
| **D-v30-1** | **理论内容数据形态**。推荐 **A：Bundle JSON + 原生渲染器**（`Resources/Theory/theorems/*.json` + `TheoryContentService` + 块渲染视图；块类型：正文/要点/表格/步骤/公式/误区卡/深链 chip）。理由：12 篇（终局 20+ 篇）硬编码维护成本高；16 定理是 candidate 会修订，JSON 换文案不动代码；与 Drills 内容线格局一致，未来可 OTA。**B：硬编码 SwiftUI 页**（沿现有学页模式）：首批省一个渲染器，但每次 16 修订都要改代码、20 篇后必然返工 | W0 起全部 | ⏳ |
| **D-v30-2** | **模型路由**（按 `01-subagent-model-selection`，写入批次表后不再询问）。推荐按批次风险：W0/W5（代码基建/挂接横切批）与 W1（定调批）用强推理模型；W2–W4（内容转写批，输入输出边界清晰）可用默认 `cursor-grok-4.5-high-fast` + 主控抽查全文复核 | 全部 | ⏳ |
| **D-v30-3** | **现有 9 张学页在球理索引中的归置**。推荐：本轮只做「归组 + 互链」（索引页按主题分组，把现有学页与定理详情页混编列出，理论页与学页互相深链），**不改写不合并**现有学页正文；「同一原理讲两遍」的正文级合并留到 M01–M06 批 | W6 | ⏳ |

---

## 四 批次表

> 每批 = 一次子智能体会话可完成并验收的粒度。转写批每批 ≤4 篇——每篇需读 16 原文
> （107–220 行）+ contracts 条目 + 写回 JSON，4 篇以上叠加渲染验证会临近上下文上限。
> **每批执行前必读**：本文件该批小节 + §六红线 + W1 产出的转写模板（W2 起）。
> 涉及几何表述（切线/分离角/袋口方位）的批：先读 `geometry-spatial-reasoning` 技能；
> 方位词一律用 portrait 屏幕系（沿用 v26 DR-063 裁定）。

### W0 — 横切基建：理论内容管线 + 球理入口（代码批）

- **范围**：
  1. 按 D-v30-1 落数据形态。若 A：定义理论页 JSON schema（页头：id/名称/一句话陈述/分组/关联定理/
     相关 drill 检索方式；正文：块数组）写入 `QiuJi/Resources/Drills/schema.md` 同级新节或独立
     `Resources/Theory/schema.md`；新建 `TheoryContentService`（参照 `DrillContentService` L297–330
     的 Bundle decode 模式）+ 块渲染视图（复用 `LearnDocSectionCard`/`LearnDocText`/`LearnDocFormulaNest`
     视觉件，新增表格/步骤/误区卡块）。
  2. vendor `16/contracts/theorem-tags.json`、`module-tags.json`、`run-out-flow.json` 快照进
     `QiuJi/Resources/Theory/contracts/`，文件头或伴随 README 注明源版本（v1.0-rc4）与同步日期；
     建立版本升级通道说明（16 修订 → 重新 vendor → 内容 JSON 按 diff 修订）。
  3. 球理索引页（分组列表：碰撞与瞄准 / 旋转与走位 / 战术与决策 / 流程与速查；含「即将上线」占位
     规则待 D-v30-3）+ `AngleRoute` 新 case（`AngleHomeView.swift` L4–45）+ `learnEntries` 加
     「球理」入口卡（L194–204，glyph/palette switch L76–134，`CoverPalette.PracticeMulticolor`）+
     `MainTabView.angleDestination` 接线（L118–178）。
  4. 理论详情页路由按 `theoremId` 参数化（一个渲染器视图 + id），为 W5 的 drill 栈深链预留
     （届时在 `drillLibraryPath` 栈也注册同类型 destination）。
  5. 在 `tasks/phases/P12-content-system-theory.md` 末尾追加 ADR（命中触发：跨模块边界——13 首次
     vendor 16 contracts；新内容/数据策略——理论内容 JSON 线）；更新 T-P12-02/03/06 任务状态引用本方案。
- **红线**：⛔ 本批不写任何理论正文（可放 1 篇假数据样例页供渲染验证，标明 placeholder 不进最终包
  或以 T03 骨架占位）；⛔ 不动现有 9 张学页。
- **依赖**：D-v30-1。
- **完成标准**：`make build` SUCCEEDED（附输出）；样例理论页渲染截图（全部块类型各出现一次）；
  球理入口卡 → 索引页 → 详情页导航链路截图；vendor 文件与源 diff 为零（附校验命令输出）；
  schema 文档落档；ADR 落档。

### W1 — 试点定调（2 篇：T03 + T08，覆盖两类模板）

- **范围**：
  1. `T03 切线法则`——物理定理模板样板（标注图复用型）：转写正文 + 复用/截取 `AngleTrainingScene`
     标注能力出静图或深链「分离角图谱」学页 + 常见误区卡 + 「相关训练」暂缺说明（W5 前 theoremIds 未挂）。
  2. `T08 风险收益矩阵`——原生组件模板样板（矩阵表 + 三问清单，无图）。
  3. **产出转写模板**（页结构、语域规则、块类型选用规则、16 原文 → App 版的取舍对照表），
     落 `docs/research/20260807-v30理论转写模板.md`，W2–W4 直接引用。
- **红线**：⛔ 转写只准取材 16 原文与 contracts 条目，禁止自行补充理论断言（发现 16 缺口 → 登记 §七
  回传 16 gap-log，不得脑补）；⛔ 禁教材腔；⛔ 几何表述遵循 geometry 技能与屏幕系方位词。
- **依赖**：W0。**试点未经用户人工验收通过，W2–W4 不得开工。**
- **完成标准**：2 篇页面截图；正文与 16 原文的取舍对照表（保留/剔除/改写三栏）经用户批准；
  一句话陈述与 `theorem-tags.json.statement_one_liner` 一致（允许口语化微调并注明）；
  JSON 可解码 + `make build` SUCCEEDED；转写模板落档。

### W2 — 物理定理批（4 篇：T01 / T02 / T09 / T04）

- **范围**：按 W1 模板转写。特殊注意：
  - T01：`key_data` 的 30° 恒定区间与半球规则是核心卖点，误区卡取 `common_errors`；
    深链「分离角图谱」「瞄准原理」学页。
  - T02：与 T01 成对（stun vs roll），页内互链 T03。
  - T09：含加塞流派神话破除段（16 原文 §，业余玩家高价值）；深链「旋转与加塞」「加塞吃库图谱」学页。
  - T04：文字版 + 深链分离角演示页（`ShotSimulationView`，注明 App 内五档即 T04 落地）；
    参数扫描叠加图**不做**，页内不留「图待补」空洞（文字自洽成篇）。
- **依赖**：W1。
- **完成标准**：同 W1 通用项（截图 / 取舍对照表 / 一句话陈述一致性 / decode + build）；
  批内 4 篇与 T03 无可互换段落（不重复原则）。

### W3 — 战术定理批（4 篇：T05 / T06 / T07 / T10，文字先行）

- **范围**：按 W1 模板转写。特殊注意：
  - T05 倒推规划 / T06 关键球 / T07 球团管理：文字 + 结构化决策要点；示意局面图**本轮不做**，
    每篇登记 1–2 个候选示意局面的文字描述（球形要点，不含坐标）到 §七待办区，
    供日后人工编排台录制（⛔ AI 不生成球形坐标红线）。
  - T10 防守层级：层级清单用原生步骤/列表组件（参照 T08 样板）。
  - 战术定理与 M01–M06 引用密切，正文提及模块概念时用人话展开，**不引用 M 编号**（模块页未上线）。
- **依赖**：W1（与 W2 互不相交，可并行）。
- **完成标准**：同 W1 通用项；候选示意局面登记完成。

### W4 — 流程与速查批（2 篇：5 步决策流程 + 速查表）

- **范围**：
  1. 5 步流程页：以 `run-out-flow.json` 状态机为数据源（步骤卡组件：目的/任务/产出/时间预算），
     正文取 `execution-guide.md` 转写；步内引用定理时挂深链 chip。
  2. 速查表页：`quick-reference.md` 转写，接近原样（本就面向业余读者），按主题分节 + 逐条链回定理详情页。
- **依赖**：W1（内容依赖 W2/W3 的定理页存在以便深链——若并行执行，深链目标 id 先写、渲染端
  对未上线 id 降级为纯文本，W6 复验全通）。
- **完成标准**：同 W1 通用项；速查表逐条的定理深链点击可达（或降级策略生效截图）。

### W5 — 理论挂接闭环（代码 + 内容小批）

- **范围**：
  1. `DrillContent` 加可选 `theoremIds: [String]?`、`TutorialSection` 加可选 `theoremRefs: [String]?`
     （承袭 `shotIntent`/`formations` 加法式先例，旧 77 条零改动）；schema.md 登记。
  2. 精讲渲染端：`theoremRefs` 呈现为节尾安静 chip（显示定理 `name_zh`），点击 → 层 2 弹
     `statement_one_liner`（读 vendored contracts）→ 再点 → 层 3 push 理论详情页
     （在 `drillLibraryPath` 栈注册理论详情 destination）。
  3. 首批挂接：c042 挂 `theoremIds: [T03, T01, T04]`（依据 curriculum-map L51–60 绑定表），
     其精讲相关节挂 `theoremRefs`；酌情再挂 2–3 条已完成 v26 迁移的 drill（如 c032/c053，
     以 v26 届时实际完成状态为准，挂标不改其正文）。
  4. 闭环记账：新建 `tasks/THEORY-CONSUMPTION-LOG.md`（哪条 drill 消费哪些定理、日期）；
     回写 16 项目 hub 状态卡（v1.0 final 闭环条件达成）+ 本项目状态卡。
- **红线**：⛔ 不改 v26 已交付精讲的正文文字，只加字段与 chip；⛔ `theoremIds` 取值必须来自
  vendored `theorem-tags.json` 的合法 id（解码时校验或测试断言）。
- **依赖**：W0 + W2（T01/T03/T04 详情页需存在）+ **v26 W7 完成后**（c042 精讲届时定稿，避免双线同文件冲突）。
- **完成标准**：`make build` SUCCEEDED + `QiuJiTests` 全绿（附输出，含新增 theoremIds 合法性校验）；
  c042 详情 → 精讲 → chip → 一句话 → 理论详情页全链路截图；两处记账落档；16 hub 卡回写。

### W6 — 收尾：学区归组 + 全链路复验 + 记账

- **范围**：
  1. 按 D-v30-3 裁定归置现有 9 张学页：球理索引页分组混编（如「碰撞与瞄准」组 = T01/T02/T03 +
     瞄准原理/瞄准方法/分离角图谱……），理论页与学页互链补全；学区入口卡文案/排序微调。
  2. 全量复验：12 篇逐页渲染走查（截图）、页间深链全通（含 W4 降级项复验转正）、
     Dark Mode 抽查、空态/异常（缺 contracts 条目、非法 id）降级正常。
  3. 记账：`tasks/PROGRESS.md`（P12 任务状态：T-P12-02/03/04 ✅、T-P12-06 部分完成注明范围）、
     本项目与 16 的 hub 状态卡、`docs/00-讨论记录.md` 追加本轮决策摘要。
  4. M01–M06 后续批的输入整理：每模块对应的战术 drill 缺口（对照 curriculum-map）列成待办清单。
- **依赖**：W2–W5 全部完成 + D-v30-3。
- **完成标准**：`make build` SUCCEEDED；12 篇截图 + 深链矩阵勾验表；三处记账完成；
  M 批待办清单落 §七。

### 依赖图

```
D-v30-1/2 拍板 ─→ W0（基建 + 入口）─→ W1（试点 T03/T08，用户人工验收）
                                        │
                        ┌───────────────┼───────────────┐
                        ▼               ▼               ▼
                   W2（物理批）    W3（战术批）    W4（流程速查批）
                        │        （三批互不相交，可并行）
                        ▼
                   W5（挂接闭环；另需 v26 W7 先完成）
                        │
                        ▼
                   W6（收尾归组 + 复验；另需 D-v30-3）
```

### 批次一览

| 批次 | 范围 | 依赖 | 模型（D-v30-2 待拍板） | 状态 |
|---|---|---|---|---|
| W0 基建 + 入口 | schema/service/渲染器/vendor/索引页/入口卡/ADR | D-v30-1 | ⏳（推荐强推理） | ⏳ |
| W1 试点定调 | T03 + T08 + 转写模板 | W0 | ⏳（推荐强推理） | ⏳ |
| W2 物理定理批 | T01 T02 T09 T04 | W1 | ⏳（可默认 fast） | ⏳ |
| W3 战术定理批 | T05 T06 T07 T10 | W1 | ⏳（可默认 fast） | ⏳ |
| W4 流程速查批 | 5 步流程 + 速查表 | W1 | ⏳（可默认 fast） | ⏳ |
| W5 挂接闭环 | theoremIds/theoremRefs + c042 + 记账 | W0+W2 + v26 W7 | ⏳（推荐强推理） | ⏳ |
| W6 收尾 | 学区归组 + 复验 + 记账 + M 批输入 | W2–W5 + D-v30-3 | ⏳（可默认 fast） | ⏳ |

---

## 五 锚点索引（2026-08-07 读码/实跑核实）

| 锚点 | 路径 | 批次 |
|---|---|---|
| 学区入口/路由 | `QiuJi/Features/AngleTraining/Views/AngleHomeView.swift`（`AngleRoute` L4–45；`AngleEntry` L49–135；`learnEntries` L194–204；入栈 L410–416） | W0 |
| 入口卡配色 | `QiuJi/Core/DesignSystem/CoverPalette.swift`（`PracticeMulticolor` ~L92+） | W0 |
| destination 接线 | `QiuJi/App/MainTabView.swift`（`angleDestination` L118–178；练习 Tab `navigationDestination(for: AngleRoute.self)` L46–48；动作库 `for: String.self` L34–36） | W0/W5 |
| Tab/Path 定义 | `QiuJi/App/AppRouter.swift`（`AppTab` L3–8；`anglePath` L38） | W0 |
| 学页视觉件 | `QiuJi/Features/AngleTraining/LearnDocChrome.swift`（`LearnDocText`/`LearnDocSectionCard`/`LearnDocFormulaNest`/`LearnDocTextLink`，~191 行）；`LearnControlStrip.swift`（~211 行） | W0/W1 |
| 现有 9 张学页 | 同目录 `AimingPrincipleView`(623)/`AimingMethodsView`(811)/`AimingCorrectionView`(833)/`SpinAndEnglishView`(552)/`AngleDynamicView`(302)/`SeparationAngleAtlasView`(325)/`CushionEnglishAtlasView`(330)/`BallFeelView`(374)/`ContactPointTableView`(486) | W2/W6 只读互链、W6 归组 |
| JSON 加载先例 | `QiuJi/Data/Services/DrillContentService.swift`（`DrillContent` L5–24 无 theoremIds；`DrillTutorial` L106–125；`TutorialSection` L137–167 无 theoremRefs；Bundle decode L297–330） | W0/W5 |
| 标注场景 | `QiuJi/Core/Scene/AngleTrainingScene.swift`（假想球/瞄准线/接触点 ~L1510–1600；切角弧 L1645–1709；切线/分离线 L1578–1600、L1260–1279） | W1/W2 |
| T04 已落地处 | `QiuJi/Core/Physics/BTPhysicsConstants.swift` L137–173（`StrokePhysics.SpeedLevel` 五档，注释标 16 T04）；演示页 `ShotSimulationView.swift` | W2 |
| 类别→定理绑定 | `tasks/curriculum-map.md` L51–60 | W5 |
| 首挂 drill | `QiuJi/Resources/Drills/positioning/drill_c042.json`（现状无 theoremIds） | W5 |
| P12 任务/ADR 落点 | `tasks/phases/P12-content-system-theory.md` | W0/W6 |
| 16 内容源 | `/Users/song/projects/16.billiard_theory/theory/theorems/T01–T10.md`（107–220 行/篇）、`execution-guide.md`(302)、`quick-reference.md`(218)、`contracts/*.json`（schema 1.1.0，theory v1.0-rc4） | W0–W4 只读 |
| 16 hub 状态卡 | `/Users/song/projects/project-hub/projects/16.billiard_theory.md` | W5/W6 |

---

## 六 红线汇总（每批执行前回显）

1. ⛔ **诚实交付**：声称「完成/通过」必须附真实输出（build 日志、decode 遍历、截图）；
   未验证如实说未验证。
2. ⛔ **转写不造理论**：正文断言只准取材 16 原文与 contracts；16 没讲的不许补，发现缺口登记
   §七并回传 16 `research/_agent-gap-log.md` 线索，禁止脑补断言（16 的来源纪律是它的立身之本）。
3. ⛔ **禁教材腔**：正文不出现「根据定理 TXX」「法则告诉我们」；编号只用于页面标识、数据标签、chip。
4. ⛔ **几何纪律**：几何表述遵循 `geometry-spatial-reasoning`；用户可见方位词用 portrait 屏幕系
   （v26 DR-063）；力度/杆法用定性词汇为主、数值退括号。
5. ⛔ **AI 不生成球形坐标**：战术示意局面只写文字描述登记待办，图待人工编排台录制。
6. ⛔ **不动 v26 战场**：不改任何 drill 精讲正文（W5 只加字段与 chip）；不动序列/产物目录。
7. ⛔ **范围纪律**：M01–M06、参数扫描图、学页正文合并、Tab 改名均不在本轮；批外发现登记 §七。
8. ⛔ **加法式 schema**：所有新字段可选、旧内容零改动零回归（decode 遍历验证）。

---

## 七 执行期待办登记区

> 各批发现的批外问题登记于此，由主控决定归入哪批或另立。

| ID | 发现批 | 事项 | 建议归属 |
|---|---|---|---|
| （空） | | | |

---

## 八 版本记录

| 版本 | 日期 | 变更 | 来源 |
|---|---|---|---|
| v30.0 | 2026-08-07 | 首版：选址拍板（练习 Tab 学区）+ 第一批 12 篇（T01–T10 + 5 步流程 + 速查表）+ 挂接闭环（c042）+ 学区归组；M01–M06 推后；W0–W6 批次化；待拍板 D-v30-1～3 | 用户 2026-08-07 对话逐步拍板（选址→全量→第一批扩容→「先整个方案吧，放到学中」） |
