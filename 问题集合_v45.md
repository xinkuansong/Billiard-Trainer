# 问题集合 v45 — Tab 去大标题 + 台球颜色氛围封面

> **本文件是本轮需求真源**（v45.2，状态：**全案收官 ✅ 2026-08-29；2026-08-30 用户撤回 Tab 矮顶带 + 学/理 hero**）。
> 来源：2026-08-29 用户在 14 出图后迁入 13，原话多轮——① 现有框架不动；② 今日安排加背景；③ 训练计划改成图加文字；④ 练习页同样图加文字；⑤ 图不必和内容完全相关，但要有感觉；⑥ 点开计划，上面的图和卡片相同；⑦ 每个 Tab 增加顶部的图；⑧ 拿掉各 Tab 页内大标题（训练 / 动作库 / 练习 / 记录 / 我的），占空间且没用；⑨ **氛围感 = 颜色气候，不是暗色球房**；⑩ 氛围图稍微深一点，题材要和台球相关（台呢 / 母球 / 库边）。⑪ **练习子页跟卡同图**（Q-v45-1 拍板）；⑫ **气氛图 = Bundle 静物 PNG + 色罩**（Q-v45-2 拍板）。
> **覆盖关系**：不接管 v43（模版内容）/ v44（课表网格）。本轮只动 **封面层与 Tab 页眉**，不改课表 JSON、Drill 台型图、开练流程。
> **排期前置**：工作区可能仍有未提交改动（v43 模版、DR-079 等）。本轮 diff 必须能单独看出氛围/页眉范围，**禁止顺手提交或改无关文件**。开工前不必清空工作区。
> **对照图**（气质参考，不是像素稿）：会话资产 `color-01b`～`color-06b`（14 迁入前最后一版：偏深台呢/母球/库边）。
> **模型**：执行若走子智能体，按仓库 `01-subagent-model-selection` 先问用户。未答复则默认 `cursor-grok-4.6-high-fast`，并回写本表「模型」列。

---

## 一、需求回显（语义基准）

用户原话与已拍板/推荐写入的结论（不增删语义）：

| # | 需求 | 细化口径 | 状态 |
|---|------|----------|------|
| R1 | 大体框架不动 | 5 Tab、今日清单动作行、官方/模版分段、动作库侧栏+双列、练习侧栏+网格、日历/统计、开练 FAB、底栏文案**全部保留** | 本轮遵守 |
| R2 | 今日安排加背景 | 「今日安排」信息卡从 `btBGSecondary` 纯色改为**偏深台球色场**；字（计划名、0/3、下一项）叠在色上，需蒙版保对比 | 2026-08-30 **用户撤回**（改回 `btBGSecondary` 灰卡） |
| R3 | 训练计划改图+字 | 官方计划卡上半截从纯渐变色块改为**气氛图 + 原等级色罩 + 原水印/期号**；下半截标题/周次仍走 `BTContentGridCard` | 本轮做 |
| R4 | 练习页改图+字 | 练习网格卡上半截同样改为气氛图 + **现有分类色罩**（瞄准绿、修正棕…）；白底标题区不动 | 本轮做 |
| R5 | 图不必完全对应内容 | 按**母题**复用，不按 12 课 / 20+ 练习入口各拍一张准内容图。感觉在色温 + 台球物件，不在故事 | 本轮遵守 |
| R6 | 计划详情头图 = 卡片图 | 继续共用 `BTPlanCover`（`.list` / `.hero` 同一资源，不同裁切）。禁止详情另找一张「更美的」 | 本轮做 |
| R7 | 每个 Tab 加顶部的图 | 各 Tab 一条**矮色带**（台球色场）。训练 Tab 的顶 = 今日安排卡本身，**不再叠第二条** | 2026-08-30 **用户撤回**（`git restore` 顶带；今日安排改回灰卡） |
| R8 | 拿掉 Tab 大标题 | 去掉五页 `Text("训练|动作库|练习|记录|我的").font(.btLargeTitle)`。底栏 `AppTab.title` **不删**。二级 push 页（计划列表「训练计划」、设置等）**不删** | 本轮做（撤回顶带后仍保留） |
| R9 | 氛围 = 颜色气候 | 不是夜球房全暗、不是金衬线会所、不是山水风景。色场比现平涂深一档，题材限台呢/球/杆/库边 | 本轮遵守 |
| R10 | 动作库台型不换 | `BTBakedDrillTable` / 动作详情俯视台 **本轮不换**气氛图（教具层） | 本轮遵守 |
| R11 | 练习子页跟卡同图 | 点开网格卡，**有页眉图槽**的目的地用同一 `AtmosphereKey` + 同一色罩（list / hero 裁切可不同）。全屏台 / 求解器 / `DrillDetailView` 不插大图 | 2026-08-30 **用户撤回 hero**（学/理页回到导航标题下直接正文；卡面真图仍留） |
| R12 | 气氛图用 Bundle PNG | 6 张静物进包，叠现有色罩。禁止程序化台呢当主路径 | 本轮做 |

**术语对照（FL-026）**

| 你的原话 | 采用定义 | 状态 |
|----------|----------|------|
| 「框架」 | 信息架构与交互骨架，不是「一个像素都不能动」 | ✅ 原话 |
| 「背景图 / 顶部的图 / 氛围图」 | **颜色气候色场**：一张偏深的台球静物（或强染色静物）+ 同系列色罩。不是暗室纪录片，不是山水 | ✅ 用户后两轮澄清 |
| 「图加文字」 | 卡上半气氛图、下半原标题/副标题。不是整卡变成海报、字全叠在图上 | ✅ |
| 「不一定和实际内容完全相关」 | 同一母题可复用；入门不必出现「握杆教学照」 | ✅ |
| 「有些感觉」 | 能看出台球 + 该系列色温（绿/蓝/棕…） | ✅ |
| 「点开每个训练计划，上面的图片也和原来的卡片图片相同」 | 同一 `AtmosphereKey` / 同一张图，list 与 hero 两种裁 | ✅ |
| 「每个 tab 页」 | 底栏五个根页各加顶带；练习 **学/理文档子页** 另加 hero（R11），不是所有 push 子页 | ✅ 拍板后扩 |
| 「练习模块点进去跟卡同图」 | 与计划详情同一契约：同一 key / 同一张 PNG / 同一色罩，hero 裁切。只落在有页眉槽的学/理页 | ✅ 2026-08-29 拍板 |
| 「Bundle 静物 PNG + 色罩」 | Asset Catalog 入库 6 张可解码 PNG；色罩仍走 `CoverPalette`；缺图才回退渐变 | ✅ 2026-08-29 拍板 |
| 「标题拿掉」 | 页内 `.btLargeTitle` 那五个字。不是底栏 Label | ✅ |
| 「稍微深色点」 | 比 v28 杂志色块再沉一档，页面主体仍是浅色 iOS | ✅ |
| 「和台球相关」 | 画面里要有台呢/球/杆/库边之一，禁止山、林、水墨 | ✅ |

---

## 二、本轮裁定

| 编号 | 裁定 | 依据 |
|------|------|------|
| D-v45-1 | **训练 Tab 只有一层顶图**：有今日安排时，今日安排卡 = 该 Tab 顶图；无安排时，用**同一 motif 的矮色带**（约 100–120pt）托住右侧好友/菜单，`quickStartBanner` 文案结构不改成大图 | 用户要顶图，也要去标题省空间；叠两层会更堵 |
| D-v45-2 | 色场深度：**比现 `CoverPalette.PlanStyle` / `PracticeMulticolor` 平涂再沉**，加台球静物；**禁止**全黑球房、金细线衬线、山水 | R9 + 用户看 `color-01b` 后的口头修正 |
| D-v45-3 | 官方计划封面 **6 个母题图** 复用，靠 `CoverPalette.PlanStyle.forLevel` 色罩区分系列。映射见 §3.2。不按 12 个 `planId` 各一张 | R5 |
| D-v45-4 | `BTPlanCover` 一次改 `.list` + `.hero`，详情与首页/计划列表自动同图 | R6；代码已共用该组件 |
| D-v45-5 | **练习子页跟卡同图**（Q-v45-1）。查找只走 `AtmosphereCatalog` 对 `AngleRoute` 的 key+palette；卡与 hero 禁止各写一份映射。落地分两档，见 §3.5 | 2026-08-29 拍板；推翻原「本轮不改子页」 |
| D-v45-6 | **不要**把现成 `BTPracticeCover`（几何台型微图）接到 AngleHome。那是示意图语言，和本轮「颜色氛围 + 静物」不是同一套 | 亲读 `BTPracticeCover.swift`；生产路径本就未接入 |
| D-v45-7 | 自定义模版无官方摄影：用 **库存母题按 `CustomPlan.id` hash 轮换**，列表与「用于今日」不必另图。不要求用户上传 | R3 含「每个训练计划」 |
| D-v45-8 | 底栏五个字保留。UI 测试里用 `tabBars.buttons["动作库"]` 的继续有效；断言**页内**大标题的必须改完成标准（见各批） | R8 |
| D-v45-9 | 不回写 14 `design-system` / 不开 Phase G。Token 若新增，只进 13 `CoverPalette` / 新 `AtmosphereCatalog` | 14 已收官；本轮在 13 落地 |
| D-v45-10 | 不触发 ADR 强制清单（无新 SPM、无 Schema 迁移、无同步策略变更）。Asset Catalog 气氛图属资源，不单独立 ADR | `10-ios-architect` |
| D-v45-11 | 完成标准**不**写「全量测试后工作区零 diff」。写盘 UI 测试（`ScreenshotTourUITests` 等）本轮不进默认 gate。各批只跑聚焦单测 + `make build` + 该批点名的 UI 测 | PD-027：本轮不依赖零 diff |
| D-v45-12 | 水印对比度继续走 `CoverPalette.Glyph`（罩色 `top`，不是照片平均色）。改完后 `CoverPaletteContrastTests` 必须仍绿 | 现网约束 A |
| D-v45-13 | 气氛图真源 = **Bundle 6 张静物 PNG + 色罩**（Q-v45-2）。优先放 `QiuJi/Resources/Assets.xcassets/Atmosphere/`（`feltEntry` 等 imageset），走现有 xcassets 进包，**不必**新开 folder reference。禁止程序化台呢/虚焦球当主路径；缺图只回退现网渐变。气质对照 `color-01b`～`06b`，禁止山水。交空文件 / 1×1 占位 = 未完成 | 2026-08-29 拍板 |
| D-v45-14 | **禁止**在 `MainTabView.angleDestination` 外包一层 hero。会破坏 SceneKit 全屏页、`SolverStageChrome` 与 `DrillDetailView`。hero 只进 §3.5 白名单页 / `theoryPageChrome` | 亲读 `MainTabView` L115–178 |

推翻先改本文件升 v45.x。Q-v45-1 / Q-v45-2 已于 2026-08-29 拍板，收入 D-v45-5 / D-v45-13，不再待决。

---

## 三、逻辑真源（执行时不得偏离）

### 3.1 根因

v27/v28 已把计划/练习做成「杂志色块 + 大字水印」（`CoverPalette` + `BTPlanCover` + `AngleGridCard`）。色是**平涂标签**，没有扩散成气候；五 Tab 根页又各占一行 `.btLargeTitle`，和底栏重复。今日安排仍是 `btBGSecondary` 白/灰卡。所以整 App 素，不是缺功能。

`BTPlanCover` 文件头已写：「等 15 素材到位可把渐变层替换为图片，结构不变」。本轮不按 15 的 drill 视频帧来，按用户指定的**母题静物 + 色罩**先换上。

### 3.2 母题映射（W0 写入 `AtmosphereCatalog`）

**计划（`planId` → 母题）**

| 母题 key | 色温（沿用 `PlanStyle.forLevel`） | 静物提示 | `planId` |
|----------|----------------------------------|----------|----------|
| `feltEntry` | L0→L1 暖绿 | 绒面 + 一颗母球 | `plan_beginner` |
| `feltAim` | L1 冷蓝 | 母球 + 目标球虚影 | `plan_accuracy`, `plan_intermediate`, `plan_accuracy3` |
| `feltCue` | L1→L2 青 / L3 棕金 | 杆头或加塞球 | `plan_cueball`, `plan_english` |
| `feltRoute` | L1→L2 青 | 多球 / 走位疏影 | `plan_positioning`, `plan_positioning2` |
| `feltForce` | L2 炭灰 | 深绒 + 球 | `plan_force`, `plan_separation` |
| `feltMix` | L3 / L3→L4 | 多色球浅景深 | `plan_advanced`, `plan_fullskill` |

**Tab 顶**

| Tab | 母题 key | 用法 |
|-----|----------|------|
| 训练 | `feltEntry` | 今日安排卡 / 空状态矮带 |
| 动作库 | `feltRoute` | 搜索条上方矮带 |
| 练习 | `feltCue` | 搜索条上方矮带 |
| 记录 | `feltForce` | 「日历/统计」分段坐在带上 |
| 我的 | `feltAim` | 登录行坐在带上（可比计划卡浅半档，仍要台球物件） |

练习网格卡：每张仍用 `AngleEntry.palette` 作色罩；底层图按 `AngleRoute` 归到上表 6 key 之一（瞄准类 `feltAim`，杆法/旋转 `feltCue`，走位/编排 `feltRoute`，其余 `feltEntry`）。**一张图可服务多卡**，靠色罩认模块。

### 3.3 明确不做

- 不换动作库卡面 / 动作详情台型 / 今日清单左侧 `BTDrillListThumbnail`
- 不改底栏图标与五字
- 不改计划 JSON、剂量、Freemium
- 不把全 App 改成 Dark Mode
- 不启用 `BTPracticeCover` 作为本轮封面
- 不写 14 Phase G
- 不在全屏台 / 求解器 / 动作详情上插气氛 hero（D-v45-14）

### 3.4 无障碍 / 对比

- 叠在色场上的字：白或 `btText` + 底部/整体半透明黑或台呢绿蒙版（详情 Hero 已有底渐变，可保留）
- `CoverPaletteContrastTests` 继续对**罩色**测水印，不改阈值糊弄
- 去大标题后，VoiceOver 仍能从底栏 Tab 听到页名；今日安排卡保持现有 combine 策略
- 学/理 hero 对读屏可标为装饰（`accessibilityHidden`），页名仍走现有 `.navigationTitle`

### 3.5 练习子页同图分档（D-v45-5 / D-v45-14）

`AngleEntry.palette` 现为 `private`，W0 Catalog 必须提供**公开** `cover(for: AngleRoute) -> (key, pair)`，W2 卡与 W4 hero 只读它。`theoryPage(TheoryPageID)` 按现网 `AngleEntry.palette(for:)` 的 `TheoryGroup` 微差走同一 pair。

**加 hero（浅色文档页，ScrollView 顶，约 140–180pt，同图 hero 裁）**

| 入口 | 锚点 | 接入点 |
|------|------|--------|
| 球理索引 | `TheoryIndexView` L25 `.navigationTitle("球理")` **保留** | ScrollView 顶 |
| 球理 12 篇 | `theoryPageChrome`（`TheoryPageChrome.swift` L226） | **一处改 12 页**，用 `AngleRoute.theoryPage(pageID)` |
| 瞄准原理 / 方法 / 修正 | `AimingPrincipleView` L30、`AimingMethodsView` L35、`AimingCorrectionView` L29 | ScrollView 顶 |
| 旋转与加塞 / 浅谈球感 / 瞄准点对照表 | `SpinAndEnglishView` L40、`BallFeelView` L27、`ContactPointTableView` L62 | ScrollView 顶 |

**不加 hero（全屏台 / 黑顶栏 / 教具）**

| 原因 | route / 壳 |
|------|-------------|
| SceneKit / 黑顶栏训练 | `angleDynamic`、`geometricQuiz`、`sceneAiming2D/3D`、`aimPointTraining`、`aimPointScene2D/3D`、`shotSimulation` |
| 图谱台（黑顶栏） | `separationAngleAtlas`、`cushionEnglishAtlas` |
| `SolverStageChrome` | `bankShot`、`diamondSystem` |
| 编排 / 对局 / 求解 | `positionPlayComposer`、`freePlay`、`positionPlaySolver`、`planThree`、`snookerTactics` |
| 工具 / 教具 | `ballExtraction`、`batchDrillStudio`、`drillDetail`（`DrillDetailView` 俯视台，R10） |

这些页的「同图」只保证 Catalog 与卡一致；页内不出现气氛大图。若日后出现页眉槽，必须读同一 key。

---

## 四、批次表

```
W0 资源+BTPlanCover ──┬── W1 训练壳
                      ├── W2 练习壳（卡+顶带）── W4 练习学/理 hero
                      └── W3 动作库+记录+我的
W1 / W2 / W3 在 W0 后可并行。W4 等 W0+W2（卡已能对照同图）。
```

| 批 | 范围 | 量级 | 依赖 | 模型 |
|----|------|------|------|------|
| W0 | 6 张 Bundle PNG + `AtmosphereCatalog`（含公开 `cover(for: AngleRoute)`）+ `BTAtmosphereLayer` + **改 `BTPlanCover`** | 中 | 无 | `cursor-grok-4.6-high-fast` ✅ 2026-08-29 |
| W1 | 训练根页去「训练」；今日安排色场；空状态矮带；自定义模版缩略图改母题轮换 | 中 | W0 | `cursor-grok-4.6-high-fast` ✅ 2026-08-29 |
| W2 | 练习根页去「练习」；顶带；`AngleGridCard` 改图+色罩 | 中 | W0 | `cursor-grok-4.6-high-fast` ✅ 2026-08-29 |
| W3 | 动作库 / 记录 / 我的 去大标题 + 矮顶带；动作库卡面不动 | 中 | W0 | `cursor-grok-4.6-high-fast` ✅ 2026-08-29 |
| W4 | 学/理文档子页 hero = 卡同图；`theoryPageChrome` 一处覆盖 12 篇 | 中 | W0、W2 | `cursor-grok-4.6-high-fast` ✅ 2026-08-29 |

### W0 — 共享层与官方计划封面

**做**

1. 新增 `AtmosphereCatalog`（key、`planId` 映射、Tab 映射、**公开** `cover(for: AngleRoute)` 含 `theoryPage`）。缺图回退：仅 `CoverPalette` 渐变（现网行为）。
2. 入库 6 张**可解码**母题 PNG 到 `Assets.xcassets/Atmosphere/`（`feltEntry` / `feltAim` / `feltCue` / `feltRoute` / `feltForce` / `feltMix`）。构图可虚、可浅景深，必须台球物件，禁止山水。对照 `color-01b`～`06b`。⛔ 空 imageset / 1×1 占位不算完成。走现有 xcassets 进包，不新开 `Resources/Atmosphere/` folder reference（免 FL-031）。
3. 新增 `BTAtmosphereLayer`：Bundle 图 + 色罩（`Pair` / `PlanStyle`）+ 可选底蒙版。主路径读 PNG，不写程序化台呢。
4. `BTPlanCover`：渐变层换成 `BTAtmosphereLayer`；水印、期号、`.list`/`.hero` 参数保留。文件头注释从「方向 A 无图片」改为「方向 B：母题图 + 色罩」。
5. 扩展 `CoverPaletteContrastTests`：映射表 12 个官方 `planId` 都能解析到 key；水印仍对罩色 `top` 测。Catalog 单测覆盖 `AngleRoute` 代表值（`aimingPrinciple`、`theoryPage(.t01)`、`theoryIndex`、未知回退）。

**不做**：五 Tab 去标题、今日安排、练习卡、模版卡。

**完成标准**

- `make build` SUCCEEDED
- `CoverPaletteContrastTests` 全绿
- 新增 Catalog 单测：12 `planId` + 5 Tab 均有 key；未知 id 回退 `feltEntry`；`AngleRoute` 代表值有 key
- 6 个 Atmosphere imageset 均可 `UIImage(named:)` 非空（不是 1×1 占位）
- 模拟器点开任一官方计划：列表卡上半与详情 Hero 能看出是**同一张图**的不同裁
- 工作区 diff 不含 Plans JSON / Drill JSON

### W1 — 训练 Tab 壳

**做**

1. 删除 `TrainingHomeView.pageHeader` 里的 `Text("训练").font(.btLargeTitle)`。好友/菜单 44pt 保留，叠在今日安排色场右上，或紧贴色场顶的工具条（不得丢）。
2. `todayScheduleHeader`：`btBGSecondary` 改为训练母题 `BTAtmosphereLayer` + 字色改浅；进度条仍在底。Dark Mode 蒙版加厚。
3. 无 `todaySession`：`pageHeader` 改为 `feltEntry` 矮带 + 原右侧按钮；`quickStartBanner` 文案保留。
4. `customIssueThumbnail` / `PlanListView.customThumbnail`：按模版 id hash 取 6 母题之一 + 淡色罩，去掉「01 + 锤子」主视觉（期号可改成角标）。
5. 改 `P4_TrainingLogUITests.testTrainingHomeTitle`：不再断言页内「训练」大标题；改为断言「今日安排」或「官方计划」或空状态「选择一个计划开始训练」之一，**且** `tabBars.buttons["训练"]` 仍在。

**不做**：改 `BTPlanCover`（W0 已做）；改练习/其它 Tab。

**完成标准**

- `make build` SUCCEEDED
- `P4_TrainingLogUITests` 聚焦绿（含改写后的 title 测）
- Light：有计划时首页第一屏无 34pt「训练」，今日安排能看出台呢/球；点一张官方计划，Hero 与卡同图（W0 回归）
- `PlanListView` 导航标题「训练计划」仍在

### W2 — 练习 Tab 壳

**做**

1. 删除 `AngleHomeView.pageHeader` 的 `Text("练习")`。
2. 搜索条上方加练习母题矮带（`feltCue`）。
3. `AngleGridCard.coverArea`：底图按 Catalog 归并 + **现有 `entry.palette` 色罩** + 原 glyph/序号/PRO。不要接 `BTPracticeCover`。
4. 改 `P5_AngleTrainingUITests.testAngleHomeTitle`：不断言页内「练习」大标题；改断言侧栏「全部」或卡「瞄准原理」，且 `tabBars.buttons["练习"]` 仍在。

**不做**：练习子页 hero（W4）；练习子页 `.navigationTitle` 不删。

**完成标准**

- `make build` SUCCEEDED
- `P5_AngleTrainingUITests` 聚焦绿（`testFiveSegmentTabs` / `testSegmentCards` 不得回归）
- Light：练习首页无 34pt「练习」；卡顶能看出色不同（绿/棕/橙…）且有台球静物，不是六张一模一样的图无色罩

### W3 — 动作库 / 记录 / 我的

**做**

1. `DrillListView.pageHeader`：去掉「动作库」大标题；上方矮带 `feltRoute`；搜索/等级 chip/侧栏不动；**卡面 `BTBakedDrillTable` 不动**。Preview 里的 `.navigationTitle("动作库")` 可留（仅 Preview）。
2. `HistoryCalendarView`：去掉「记录」；矮带 `feltForce`；`BTSegmentedTab` 日历/统计叠在带上或紧贴带下。
3. `ProfileView`：去掉「我的」；矮带 `feltAim`；`guestHeader` / 已登录头放在带上。列表图标本轮**不强制**改彩色圆井（用户未点名）。
4. 测试：
   - `P6_HistoryStatsUITests.testHistoryTabTitle`：改断言分段「日历」或「统计」+ 底栏「记录」
   - `P2_DataLayerUITests.testS04_DrillLibraryShowsDrills`：`staticTexts["动作库"]` 在去标题后仍可能命中**底栏**。改为 `tabBars.buttons["动作库"]` + 至少一张 `drillCard_*` 或「搜索动作」，避免误把底栏当页标题

**不做**：动作详情台型、统计图表改版。

**完成标准**

- `make build` SUCCEEDED
- 上述改写后的 P2/P6 聚焦绿
- Light：三页均无 34pt 根标题；顶带是台球色场；动作库双列仍是俯视台型图（抽查一张 `中袋直线出杆`）

### W4 — 练习学/理子页跟卡同图

**做**

1. 新增 `BTPracticeAtmosphereHero(route:)`（或等价）：读 Catalog 的 key+palette，`.hero` 裁 + 底蒙版。与 `AngleGridCard` **同一** `cover(for:)`。
2. 白名单页 ScrollView 顶插入 hero（§3.5 上表）。`.navigationTitle` / `.inline` **全部保留**。
3. `theoryPageChrome(title:)` 增加 hero，12 篇球理一次到位；`TheoryPageHeader` 结论卡仍在 hero 下。
4. Catalog 单测：`aimingPrinciple` / `theoryIndex` / `theoryPage(.t01)` 的 hero key 与卡 key、pair 的 `top` 一致。
5. 可选：`P5` 增一条「点瞄准原理 → 仍见导航标题瞄准原理 + hero 标识存在」；不删现有分段/卡片测。

**不做**：改 `MainTabView.angleDestination` 包一层；§3.5 下表任何页；删子页标题；换图谱/训练台。

**完成标准**

- `make build` SUCCEEDED
- Catalog 同图单测绿；W2 的 `P5_AngleTrainingUITests` 不回归
- Light：练习首页点「瞄准原理」——顶图与该卡同一张 PNG、瞄准绿罩仍在，导航栏标题仍是「瞄准原理」
- Light：理区点进任一篇（如 T01）——hero 与该卡同图；`theoryHeader_*` 仍在
- Light：点「角度与瞄准」或「分离角与走位」——第一屏仍是黑顶栏 + 台面，**无**气氛 hero
- 从练习进 `drillDetail`（若有入口）俯视台仍在，无气氛大图

---

## 五、锚点索引（2026-08-29 亲读，v45.1 补练习子页）

| 符号 | 路径 | 行号（约） | 本轮关系 |
|------|------|------------|----------|
| `MainTabView` | `QiuJi/App/MainTabView.swift` | L3–72 | 根页 `toolbar(.hidden)`；底栏不改 |
| `AppTab.title` | `QiuJi/App/AppRouter.swift` | L10–17 | 底栏五字，不删 |
| `TrainingHomeView.pageHeader` | `QiuJi/Features/Training/Views/TrainingHomeView.swift` | L139–185 | W1 去「训练」 |
| `todayScheduleHeader` | 同文件 | L227–258 | W1 色场 |
| `scheduleInfoBlock` | 同文件 | L260– | W1 字色 |
| `emptyStateContent` / `quickStartBanner` | 同文件 | L806–837 | W1 矮带 + 文案保留 |
| `planPosterCard` | 同文件 | L552–572 | W0 后自动吃新封面 |
| `customIssueThumbnail` | 同文件 | L574–599 | W1 模版兜底 |
| `BTPlanCover` | `QiuJi/Core/Components/BTPlanCover.swift` | L1–98 | **W0 主改**；注释 L3–6「无图片」作废 |
| `PlanCoverLabel` | 同文件 | L101–126 | 水印文案保留 |
| `PlanDetailView.heroHeader` | `QiuJi/Features/Training/Views/PlanDetailView.swift` | L91–123 | W0 后同图；底渐变可留 |
| `PlanCard` / `customThumbnail` | `QiuJi/Features/Training/Views/PlanListView.swift` | L232–343, L425–454 | 官方卡随 W0；模版随 W1；`.navigationTitle("训练计划")` L65 **保留** |
| `OfficialPlan` / `loadAllPlans` | `QiuJi/Data/Services/PlanContentService.swift` | L5–16, L146–183 | 只读，不改 JSON |
| `CoverPalette.PlanStyle` | `QiuJi/Core/DesignSystem/CoverPalette.swift` | L336–381 | 色罩真源，W0 当 overlay |
| `CoverPalette.PracticeMulticolor` | 同文件 | L92–213 | 练习卡色罩 |
| `AngleHomeView.pageHeader` | `QiuJi/Features/AngleTraining/Views/AngleHomeView.swift` | L443–452 | W2 去「练习」 |
| `AngleGridCard.coverArea` | 同文件 | L597–663 | W2 图+色罩 |
| `AngleEntry.palette` | 同文件 | L146–190 | 现 private；W0 抽到 Catalog 公开 API |
| `BTPracticeCover` | `QiuJi/Core/Components/BTPracticeCover.swift` | L6–55 | **本轮禁止当封面**（D-v45-6） |
| `angleDestination` | `QiuJi/App/MainTabView.swift` | L115–178 | **禁止外包 hero**（D-v45-14） |
| `theoryPageChrome` | `QiuJi/Features/AngleTraining/Theory/TheoryPageChrome.swift` | L226–232 | W4 一处加 12 篇 hero |
| `TheoryIndexView` | `QiuJi/Features/AngleTraining/Theory/TheoryIndexView.swift` | L11–27 | W4 hero；标题「球理」留 |
| `AimingPrincipleView` 等学页 | `AimingPrincipleView` L30；`AimingMethodsView` L35；`AimingCorrectionView` L29；`SpinAndEnglishView` L40；`BallFeelView` L27；`ContactPointTableView` L62 | — | W4 白名单 |
| 黑顶栏训练/图谱 | `AngleDynamicView` L46；`ShotSimulationView` L71；`SceneAimingView` L92；`SeparationAngleAtlasView` L51；`CushionEnglishAtlasView` L54；`GeometricAngleQuizView` L56 | — | W4 **不加** hero |
| `SolverStageChrome` | `QiuJi/Features/AngleTraining/Views/SolverStageChrome.swift` | L174–179 | `bankShot` / `diamondSystem` 不加 |
| `Assets.xcassets` | `QiuJi/Resources/Assets.xcassets`（`project.yml` L108 已进包） | — | W0 气氛 PNG 落点 |
| `DrillListView.pageHeader` | `QiuJi/Features/DrillLibrary/Views/DrillListView.swift` | L51–60 | W3 去「动作库」 |
| `BTDrillGridCard` / `BTBakedDrillTable` | `QiuJi/Core/Components/BTDrillCard.swift` L90+；`BTBakedDrillTable.swift` | — | **不改** |
| `HistoryCalendarView` 大标题 | `QiuJi/Features/History/Views/HistoryCalendarView.swift` | L23–34 | W3 |
| `ProfileView` 大标题 | `QiuJi/Features/Profile/Views/ProfileView.swift` | L17–24 | W3 |
| `BTContentGridCard` | `QiuJi/Core/Components/BTContentGridCard.swift` | L7–54 | 壳不改，只换 cover 闭包内容 |
| `CoverPaletteContrastTests` | `QiuJiTests/CoverPaletteContrastTests.swift` | L8–38 | W0 必绿 |
| `testTrainingHomeTitle` | `QiuJiUITests/P4_TrainingLogUITests.swift` | L16–18 | W1 改断言 |
| `testAngleHomeTitle` | `QiuJiUITests/P5_AngleTrainingUITests.swift` | L27–31 | W2 改断言 |
| `testHistoryTabTitle` | `QiuJiUITests/P6_HistoryStatsUITests.swift` | L16–18 | W3 改断言 |
| `testS04_DrillLibraryShowsDrills` | `QiuJiUITests/P2_DataLayerUITests.swift` | L32–36 | W3 改断言（底栏 vs 页标题） |
| `testS02_AllFiveTabsSwitchable` | 同文件 L18–24 | 只点 `tabBars`，W3 不应坏 |

核实日期：**2026-08-29**。

---

## 六、依赖与风险

| 风险 | 处理 |
|------|------|
| 去标题后 `staticTexts["训练"]` 仍命中底栏，测试假绿 | 各批改断言：页内内容 + `tabBars.buttons` 分开写 |
| 照片把水印吃掉 | 色罩不透明度过低会糊；W0 先保证罩色 `top` 仍过 ContrastTests，字叠在蒙版上 |
| 六张图看起来一样 | 色罩必须保留；验收看练习卡是否还能靠色认模块 |
| 误接 `BTPracticeCover` | D-v45-6；委派提示写「禁止」 |
| 在 `angleDestination` 外包 hero 毁掉训练台 | D-v45-14；W4 只改白名单 + `theoryPageChrome` |
| 程序化台呢当主封面 | D-v45-13；W0 完成标准看 Bundle 能解码 6 张 PNG |
| 误改动作库台型 | R10；W3 完成标准抽查俯视图 |
| 与未提交 v43/DR-079 diff 缠在一起 | 只 stage 本轮文件 |

---

## 七、版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v45.0 | 2026-08-29 | 初稿。按 `issue-collection-restructure` 对照代码拆 W0–W3。默认裁定 D-v45-1～12 写入，Q-v45-1/2 推荐默认不挡开工。 |
| v45.1 | 2026-08-29 | Q-v45-1 / Q-v45-2 拍板。D-v45-5 改为子页同图；新增 D-v45-13（Bundle PNG）、D-v45-14（禁止 destination 外包）、§3.5 分档、**W4**。W0 气氛图锁定 Asset Catalog 六张 PNG。 |
| v45.1 执行 | 2026-08-29 | W0–W4 全程委派收官，返工 0。模型全案 `cursor-grok-4.6-high-fast`。 |
| v45.2 | 2026-08-30 | 用户要用 git 撤掉各 Tab 顶框 + 练习学/理 hero。学/理文件 `git restore`；训练/练习首页只改页眉以免误伤 v46 卡面。R8 去大标题保留。官方计划卡 / 练习网格卡真图不动。 |
