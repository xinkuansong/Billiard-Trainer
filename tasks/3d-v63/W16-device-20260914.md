# v63 真机续验（2026-09-14）

用户授权手机连接后继续 v63；扎杆/跳球 H01–H04 按此前明确决定仍延期，不恢复开发。

## 环境与取证

- `devicectl` 实读 iPhone 16 Pro、iOS 26.6.2、wired/connected、Developer Mode enabled。
- 当前工作区构建，独立 `build/DerivedData-v63-device`；测试仍使用 Debug 默认配置/覆盖率，不能作为 Release 性能达标证据。
- 证据目录 `output/3d-v63/device-20260914/`。真机截图保留为 xcresult 的 keepAlways attachments；旧测试 Mac 绝对写盘路径改为手机临时目录，模拟器路径与所有业务断言保持。
- 不提交/推送，不做正式发布；保留其它任务未提交改动。

## 自动化覆盖与进度

| 项目 | 状态 |
|---|---|
| 试打：进袋、打点、2D/3D、回放、重打、返回序列 | 真机通过 55.580s |
| 试打：暂停、重播、模式切换与只读打点 | 真机通过 43.015s |
| 试打：完整8杆逐杆暂停与终局复位 | 真机通过 85.818s |
| 分离角与走位、自由开球、补回母球 | 4 项真机通过；92.311/38.889/112.700/34.472s |
| 详情换球形/播放中退出重入 | 2 项真机通过；35.274/45.152s |
| 支架库存/在途动作所有权释放、实际详情场景释放 | 支架所有权测试失败；实际详情三轮释放通过 2.261s |
| 手机渲染基准与默认求解计时 | 帧率导出受阻；默认防守求解 39.676s，未达流畅交互要求 |

## 需要单独保留的人工验收

以下不能由按钮自动点击、截图或短时渲染基准替代，未获用户反馈前不勾选：

- [ ] 实际进袋下落、支架滚速和遮挡观感符合预期。
- [ ] 手指旋转/缩放及球台区域与页面滚动不抢手势。
- [ ] 开启 VoiceOver 后，瞄准/力度/打点/模式切换按真实读屏顺序可操作。
- [ ] 持续实际训练的发热、耗电和手感；与短时基准分开。

本报告只补当前手机证据，不能覆盖 iPad、最低系统及其余未完成平台。v63 整体保持未完成直至逐项闭合。

## 页面截图与短时采样

- 两批共 9 项 UI 测试通过，xcresult 时间戳 `23-53-00` / `23-57-25`；附件已导出至证据目录 `tryout-attachments` / `pages-attachments`。
- 已目视手机原图：试打回放、暂停只读打点、八杆复位、九球开球停稳、分离角默认进袋及低位袋口观察。控件可见、未发现遮挡操作；静态图不能证明下落/滚动全过程或实际手势手感。
- Activity Monitor 进程 22941，在自由开球 UI 自动化期间记录 21.107s / 21 个物理内存样本：1100.69–1181.24 MiB，首尾 1146.47→1153.33 MiB；thermal 为 Fair。来源 `activity-monitor-process-live.xml`、`device-thermal-state-intervals.xml`、`ui-memory-summary.json`。
- 这是 Debug + 覆盖率 + XCTest + Instruments 下的绝对量，尚无相同条件旧版基线、20 轮进出或长期采样，不能据此认定内存泄漏、回归或预算达标。ledger 导出 exit 139，不用于结论。
- Game Performance 记录成功，TOC 首次导出 exit 139，重试成功；元数据实际保留 10s（窗口模式），不是完整请求的 20s。逐秒显示数据导出仍 exit 139 且文件为空，暂不提供 FPS 结论。原始 trace 保留，后续需恢复工具或用独立 presented-time 探针补测。

## 2026-09-15 00:07 停点：真机核心回归发现未闭合问题

当前批次已自然结束，`core-r1.log` 对应 xcresult `Test-QiuJi-2026.09.15_00-04-40-+0800.xcresult`，总结果 TEST FAILED。逐条日志显示 10 个选定方法中 8 个通过、2 个失败；失败中途 XCTest 重启/分段汇总，不能仅抄最后一段 Executed 数。

1. **实时/导出支架不一致**：八杆一致性测试 69.936s 失败。首个断言导出包含 `_1,_2`，实时预期只有 `_1`；运动帧从 t=0 即出现额外支架球，后续差异扩大。四档 4638 帧真机验收没有完成，也未生成通过后的影片附件。前轮模拟器通过不外推。尚未区分 Bundle formation 加载状态、实时消费时序与离屏导出实现，不能认定具体根因。
2. **动作所有权测试失败**：active/complete 两种状态在 autoreleasepool 后 root/source/inventory 均未立即释放（6 个断言）。实际详情场景三轮 dismantle 测试通过；尚未排除 SceneKit 延迟回收或测试生命周期假设，不直接宣称生产页面泄漏。需独立生命周期取证后再修复。
3. **默认防守求解慢**：标准搜索 wall=39.675689s，得到 6 个完整合法结果，正确性方法通过但性能不接受。当前 Debug+coverage 配置，与此前优化配置不同，不能报性能改善比例或直接外推 Release。

通过的其余项目：15 球默认开球 0.626s、开球附收集尾段 0.649s、存档续编/上一杆/清空/重置及支架近景 4.384s、取消/清空旧动作/球标识快照 3 项。支架近景手机附件已导出，仍只证明单一静态角度。

按用户此前“难解决或需要定夺就停下来”约定，在状态同步/生命周期/性能根因尚未明确处暂停进一步实现。本轮没有修改生产实现，也没有通过放宽断言隐藏失败；保留测试适配、失败日志与截图。下一步是分别制作最小实时/导出复现、异步释放证据、同配置求解分析，再决定修复范围。扎杆/跳球仍延期。

收尾：verify-gate FAIL0、verify-doc-size、git diff --check 通过；功能核心测试仍失败，静态门禁不能替代它。所有测试/采样进程已结束，00:08 使用 devicectl 正常启动手机 App。未提交/推送。

## 用户授权继续后的定位（2026-09-15）

- diagnostic-r2 两测试：完整八杆仍失败，逐杆日志显示丢球所在杆不固定；裸 SceneKit 节点 + 空 customAction 在同步 autoreleasepool 后仍存活，2s 主线程让出后归零。原立即判空前提不成立。
- 修复方向：正常杆末清动作前提交支架终态，避免等时长节点完成回调乱序时保存的是杆前库存。取消仍恢复杆前，不能当正常完成。
- fix-r3：真实进程崩溃报告 `fix-r3-crash.ips` 明确 main-thread 的 finishPlayback/render 与 SceneKit renderingQueue 的 render 同时写字典；其余三项通过，释放用例在 0.137s 内归零。新增终态提交暴露了原库存“仅主线程”假设错误。
- fix-r4：SCNTransaction.lock 不能保护库存 Swift 集合，仍同位置竞争，报告 `fix-r4-crash.ips`。不保留这个无效保护。Apple 的 [事务锁文档](https://developer.apple.com/documentation/scenekit/scntransaction/lock%28%29)针对场景图操作，不能用它替代自有集合并发验证。
- fix-r5：候选使用 NSRecursiveLock 串行化库存读写及节点更新；withActiveShot 把代次检查和源节点更新放在同一临界区，正常完成先提交、取消保持回滚；watch Timer 显式回主线程创建。运行结果待填。
- 范围审计另见 [2D/3D 漏页清单](W16-mode-coverage-20260915.md)。


### fix-r5 真机复验通过

`Test-QiuJi-2026.09.15_00-19-14-+0800.xcresult` / `fix-r5.log`：6 项，0 失败，TEST SUCCEEDED。

- 完整八杆实时/离线导出对照 **306.449s**；30fps×1=517、60fps×1=1031、30fps×0.5=1031、60fps×0.5=2059，共 **4638 运动帧**，以及各杆起点/终点/事件全部通过。八杆实时支架状态逐杆完整保留，导出影片附 xcresult。
- 迟到回调/杆末提交 0.004s、取消 0.002s、清除后旧动作拒绝 0.002s、主动/完成状态释放 0.109s、三页历史恢复 1.674s 均通过。释放验证未调用 clear/removeAllActions 来隐藏引用环。
- 当前 NSRecursiveLock 候选未出现 fix-r3/r4 的库存竞争崩溃，两个失败 .ips 保留。SCNTransaction 锁尝试已撤出；不把有限回归等同于全程无竞态证明。
- 后续 optimized-final-r6 采用 Debug + `SWIFT_OPTIMIZATION_LEVEL=-O` + `-enableCodeCoverage NO`，通过 Makefile 新增显式测试参数运行。默认 make test 仍为 Debug/coverage YES，生产构建配置未改。


### optimized-final-r6 核心结果

Debug `-O` / coverage NO，同一 iPhone16Pro/iOS26.6.2：PocketNetPresentationTests 全部 **22 项**通过（6.983s），标准防守搜索正确性 **1 项**通过（4.990s）。搜索本体 wall=**4.059613s**，6 个结果，thermalStart=0/thermalEnd=0（nominal）、lowPower=false。与首轮39.675689s差异主要对应构建条件变化，但首轮温度/负载不同，不给严格同条件加速比；没有改变搜索网格、物理精度或解约束，仍不能作为Release/全场景时延达标证明。

Makefile新增 TEST_CODE_COVERAGE（默认YES）和 TEST_BUILD_SETTINGS（默认空）以显式复现诊断配置；普通测试默认未改。最终三UI回归全部通过：详情播放中退出重入37.916s、8杆逐杆/终局复位83.053s、15/9球开球/续打/撤销116.741s。整批23核心+3UI=26项，0失败，TEST SUCCEEDED；xcresult时间戳00-25-48。附件已导出到optimized-final-attachments，复位和9球停稳原图已目视。

通过影片附件已导出为 `fix-r5-attachments/CB75DA9E-433D-48CC-AFF0-5926FD212F47.mp4`，ffprobe=76.55s/10666565bytes；34s（第4杆）抽帧已查看。离线渲染仍用其既有绿色台呢/背景，不把几何与库存逐帧一致扩展为与手机用户外观配置完全一致。


## 本轮最终状态（2026-09-15 00:32）

- 已修复并经真机定向验收：支架正常杆末状态提交/跨线程集合竞争；八杆四档导出一致、迟到回调拒绝、取消与历史恢复通过。FL-072本轮定向闭合。
- 对象释放失败是立即判空的测试前提错误：裸SceneKit基线及不主动清理的实际库存释放均通过，未据此宣称其他场景无泄漏。
- 搜索性能已明确配置影响：优化Debug4.06s，未改算法；Release、长期热机、帧率/内存预算仍未完整验收。
- 两页确实漏3D（翻袋、反射），两类训练是分入口固定模式；本轮完成审计，尚未接入新页面切换。完整清单在W16-mode-coverage-20260915。
- 最终源码通过手机构建与26项优化Debug回归，gate FAIL0；doc-size/diff检查通过。未提交/推送。v63整体仍未完成；扎杆/跳球继续延期。
