# W07 当前页面可见流程复核

日期：2026-09-13。状态：当前默认模式的单页流程通过；W07 正式局部物理接入与 W08 高度回放未完成。

## 本轮结论

按用户关于固定终态与投入的反馈，本轮停止扩大袋内物理研究，优先验证实际页面与生产入口。真源 v63.10 已包含软袋收集与短可见收尾，无须重新发明袋内求解器。

实际执行 `S2_ShotPagesLayoutUITests/testShotSimulation3DPilot`，iPhone 17 Pro / iOS 26.3，独立 UDID `EC19B1C4-AFE2-4E9E-B12C-1A2FDC6415DC`。xcodebuild 退出 0；日志实际方法通过，Executed 1 test / 0 failures / 91.511 seconds。构建包含当前工作区；未修改产品源码或切换默认物理。

- 日志：[visible-flow-r1.log](../../../output/3d-v63/W07/visible-flow-r1.log)（仓库实际位置为 `output/3d-v63/W07/visible-flow-r1.log`）。
- 原始结果：`output/3d-v63/W07/visible-flow-r1.xcresult`。
- 独立附件：`output/3d-v63/W07/visible-flow-r1-attachments/`，14 张截图及 2 个文本附件；manifest 保存命名映射。
- 流程覆盖：2D/3D；全桌/母球/目标球/袋口观察；低位近袋手势；回到瞄准；旋转缩放；后台恢复；模式往返；打点；击球中切换；回放；重打。
- 已直接打开本轮 7 张原图：初始 3D、全桌观察、袋口观察、低位近袋、打点面板、击球后 2D、回放重打后 3D。可见桌面与球体、投影轨迹、操作控件。没有逐帧审查动态视频。
- 重打后的 3D 原图中，母球靠近右侧打点入口；须核对手动观察状态保留与回到瞄准职责后再判定是否缺陷。不能仅按截图移动相机或添加偏移。

## 生产入口差距（本轮源码核对）

后续更新：DR-229–234已处理开球/图谱/验证/规划/反射入口，默认于DR-234切为局部模式并通过新的页面流程。以下保留切换前检查证据，非当前默认状态。

1. `ShotPredictor.ShotInput.simulationModel` 与 `simulateFree` 默认仍为 `.planarReference`。QiuJi 产品代码没有 `.localPockets(...)` 调用点；显式局部测试通过不能代表用户页面已使用新版进袋。
2. `BreakSimulator.breakShot` 仍直接调用 `engine.simulate`；`settled` 由 XZ 剩余速度 `< 0.3` 推导，尚未使用统一终止原因。`BreakFlowRunner` 直接起播结果。切换时须一起处理，防止普通杆与开球采用不同捕获语义。
3. 分离角/加塞吃库图谱从 `simulateFree` 提取事件片段后清空提示；尚未区分所需片段已完整与模拟截断。应按片段端点判定，不能机械要求整桌停稳。
4. 瞄准点验证击球只检查 recorder / duration，未检查终止原因。
5. 共享回放与导出已有 collection tail 分支，但当前页面测试没有启用新版物理，也没有验证实际视频导出或动态进袋遮挡。

## 下一步与验收边界

优先完成上述真实消费者与统一入口，再用角袋/中袋固定输入验证 App 实时进袋、重播和导出；保留临界吐袋判定、普通远端平面行为和理论评分。新问题以是否改变击球结果、是否出现在可见运动、是否阻断常用流程决定优先级。长期袋内堆积不是前置。

本次仅标准模拟器当前页面功能流程取证，不代表新版进袋、全部页面、真机性能、iPad、VoiceOver、导出或完整 3D 方案验收。
