# 开发进度（PROGRESS）

> Orchestrator 每次会话开始时读取本文件，结束时更新。
> 另须读取 `tasks/UI-IMPLEMENTATION-SPEC.md` § Changelog（若存在）。

---

## 任务状态（四态）

| 符号 | 含义 | 使用说明 |
|------|------|----------|
| ⏳ | 待开始 | 尚未开工 |
| 🔄 | 进行中 | 附 DoD 进度，例：`🔄 进行中（DoD 2/5）`；会话可能中断时**必须**写入，便于恢复 |
| ⚠️ | 返工 | 附 `见 FL-xxx`，对应 [`tasks/IMPLEMENTATION-LOG.md`](IMPLEMENTATION-LOG.md) 条目；修复后改回 ⏳ 或 🔄 |
| ✅ | 已完成 | Phase 任务卡 DoD 全部满足 |

---

## 当前状态

- **台面演示回放钮播放中状态 + 杆边界可暂停 ✅（2026-08-06，DR-060）**：`DrillSceneController` 引入 `PlaybackState` 四态（idle/playing/pausingAfterShot/paused），按钮改 `togglePlayback()` 单一入口，图标 `play.fill` ⇄ `pause.fill`、标签「回放/暂停/本杆结束后暂停/继续」，**不再 disabled**；暂停只在杆边界（`scheduleNextStep`）生效——当前杆完整播完并落静止位后停住，HUD 保留，再点从下一杆继续。**覆盖 F-SC-01「回放不可打断」约束**（已在 `docs/ui-polish/11-台面演示与场景组件.md` 原条目标注，防止后人改回 disabled）。验证：`make build` 绿；`DrillSceneThreeBeatUITests` 4/0（新增暂停行为测试，含静置 6s 断言读数不变以证未擅自播下一杆）；`BTShotHUDBarRenderTests` 2/0。
- **详情/训练顶栏改整条序列逐杆演示 ✅（2026-08-06，DR-059）**：①播放从「代表性单杆」改为走完整条 `steps`（按 `source.token` 与静帧同源取序列；逐杆亮方案→运杆→触球清线→物理回放→落 `finalPositions`→杆间停顿 0.7s；首杆亮方案 1.5s、后续 0.45s；无序列退回单杆）；②瞄准位摆杆改 `scene.updateCueStick`，与试打页 `runSequenceStep` 同源，运杆/减速跟杆/淡出全交 `CueStroke`，删掉 DR-058 的复位补偿改照抄试打页时序；③HUD 改「播放前隐藏 → 播放中逐杆换值 → 整条播完隐藏」，条件渲染 + 恒定占位条高。验证：`make build` 绿；`DrillSceneThreeBeatUITests` 3/0（含 c078 16 杆逐杆参数轮询证伪「只显示首杆」）；`BTShotHUDBarRenderTests` 2/0；`DrillTryoutBoardStoreTests` 9/0。**下一步：模拟器手动翻几条长序列（c071/c078）目视确认节奏与杆间衔接。**
- **详情/训练球桌演示对齐导出视频 ✅（2026-08-06，DR-058）**：回放改三拍叙事（读球形 1.5s → 亮方案 1.5s → `runCueStroke` 真运杆/出杆/跟杆，触球清线转物理回放）；打点/力度从台面浮层改为球桌下方固定 HUD 条，抽出共享组件 `BTShotHUDBar`（导出与 App 单一真源），删 `DrillShotOverlay`/`DrillPowerBar` 及避让启发式；球桌方向保持横版；训练页「球台示意」复用同组件自动同步。验证：`make build` → `BUILD SUCCEEDED`；`BTShotHUDBarRenderTests` 2/0 + `DrillSceneThreeBeatUITests` 2/0（`build/drill-scene-three-beat/`、`build/hud-bar-render/`）。**已知未决**：`CueStroke` 在 `CueStick.requiredElevation` 判 `.blocked` 时 `stick.hide()` 后直接发球（贴库/被挡球形），故这类球形第 3 拍无可见出杆——该行为与试打页、导出器共用同一 `CueStroke`，非本次引入；但命中数量未统计、未做降级表现。**下一步：模拟器手动翻几条带塞/贴库 drill 目视；决定 blocked 球形是否需降级。**
- **详情页去台面试打 + 精讲降权 ✅（2026-08-05，DR-057）**：`DrillSceneView` 移除右下「上手试打」覆层（入口仅底栏）；「查看精讲」改训练要点标题 trailing 文字链。验证：`make build` → `BUILD SUCCEEDED`（`build/dr057-detail-cta-build.log`）。**下一步：模拟器目视确认详情页层级。**
- **封面深墨调浅 + 单行字号 2/3 ✅（2026-08-05，DR-056）**：`darkOpacity` 0.85→0.52；练习网格 56→37；计划单行 scale 0.40/0.32；暗底按亮度门槛回退金色。验证：`CoverPaletteContrastTests` 10/0（`build/cover-glyph-soft-test.log`）。**下一步：模拟器目视确认浓度与字号。**
- **封面大字统一深墨 ✅（2026-08-05，DR-055）**：训练/练习封面水印统一 `Glyph.color(against:)`；废除半透明白与按档硬编码。
- **动作库网格卡去掉台面特征胶囊 ✅（2026-08-05）**：`BTDrillGridCard` 不再叠「0.2台·中上袋」等距离/袋口标注；台面仅留等级与 Pro/收藏。验证：`make build` → `BUILD SUCCEEDED`；`V28VisualUnificationUITests/testV28_LightScreenshots` 1/0。
- **动作库侧栏图标状态色修正 ✅（2026-08-05）**：`BTDrillCategoryIcon` 未选中描边态统一为 `btTextSecondary` 中性灰，选中填充态保留品牌绿 + 金色，表现与练习页侧栏一致；Section Header / 统计页等填充态不变。验证：`make build` → `BUILD SUCCEEDED`；`V28VisualUnificationUITests/testV28_LightScreenshots` 1/0；截图确认「全部」选中有色、其余分类未选中为灰色。
- **练习卡封面视觉重心调整 ✅（2026-08-05，DR-054）**：两字水印按字号 6% 下移，对齐训练计划卡；`物理 / 走位 / 2D / 3D / 识别 / SIM` 类型标签移到彩色封面右下，Pro 徽标保留右上。验证：`make build` → `BUILD SUCCEEDED`；`V28VisualUnificationUITests/testV28_LightScreenshots` 1/0；练/打/解截图目视确认无重叠。**下一步：用户确认视觉位置。**
- **训练 / 练习封面信息层级与 Pro 分层 ✅（2026-08-04，DR-053）**：训练计划封面删除重复两位数编号，仅留「第 N 期」；`plan_advanced` 统一为「加塞与多库专项」并以「加塞／多库」两行水印展示。练习卡改为两字语义水印、各分组从 01 独立编号；学/练/打/解各挑高级入口显示 PRO 并接真实订阅门控。验证：`make build` → `BUILD SUCCEEDED`；Light 截图巡检 1/0；强制非 Pro 门控 UI 测试 1/0。截图已确认角标、PRO 徽标与 chip 无重叠。
- **瞄准教学视频脚本 v2.0 ✅（2026-08-04，制作级，纯文档）**：`docs/research/20260804-瞄准教学视频脚本.md`——横版长视频（估 25–30 min）42 拍制作级逐拍稿：§0 制作总规范（线色语义同 App、公式卡范式带代码出处、1°≈17.5mm/m 换算约定、慢放四档、对比范式 C1–C3、标注 @t 时序语法）+ §1 盘面库 B0–B6；每拍含旁白全文/图规格/轨迹/公式卡（G1 ghost、G2 d=2R·sinθ、G3 sinθ=1−overlap、G4 Δ、squirt/Alciatore/击杆公式）；第二章四法作图与 `AimingMethodsView` 同源；拍 32 = A11 反直觉完整分镜。断言全挂 A#/G#；镜头缺口 S1–S8、数据缺口 D1–D6（D1 开场盘面搜索最高优先）。**下一步：用户审 v2；随后跑 D1–D6 扫描 → 扩 `SequenceVideoExporter`（S2/S3/S4）。**
- **瞄准理论总纲 v1.0 ✅（2026-08-04，纯分析文档）**：`docs/research/20260804-瞄准理论总纲.md`——瞄准教学体系理论真源（下游：教学长视频脚本 / 学区 L0–L4 课程重组 / 瞄准类内容）。公理层（进球线固定⇒假想球唯一 + 管道法）、正演链（executeStrike/squirt → 三态飞行 swerve → resolveBallBallPure throw → Han2005）、反解链（prepareAim → positionAimOffset Δ → AnalyticAim → 全保真终验）、断言库 A1–A16（全部锚定 Z1–Z3 既有实测与代码，含 4 条 ⛔ 证伪红线）、边界声明、扫描矩阵 backlog（§8：CIT 全曲线 / Δ 等高线 / 距离维度等，需跑引擎补证）。零代码改动。**下一步：用户确认总纲口径；随后可做 §8 扫描测试或视频脚本。**
- **练习首页恢复 pre-v27 逐卡多彩单字封面 ✅（2026-08-04，DR-052）**：在 DR-051 保留的语义单字与 v28 共享卡片壳 / 搜索 / 栏目头基础上，进一步将学绿 / 练金 / 打蓝 / 解灰四分区色阶恢复为历史逐卡独立渐变（绿、青、橙、琥珀、玫红、蓝、紫、红等）；历史 RGB 收口到 `CoverPalette.PracticeMulticolor`。同时补齐 iOS 26 浮动 Tab 的 UI 测试导航回退。验证：`xcodebuild ... build` → `BUILD SUCCEEDED`；`V28VisualUnificationUITests/testV28_LightScreenshots` 1/0；截图 `build/v28-screenshots/practice-{all,学,练,打,解}-Light.png` 已目视复核。**下一步：用户确认多彩版本；随后决定是否合入 main。**
- **训练计划封面课程主题化 ✅（2026-08-04，DR-048）**：封面文字不再用等级单字/双字，而按 planId 映射为入门、杆法、准度、控力、分离角、加塞、走位与三档综合；2–4 字分档缩放并留水平安全区。`BTPlanCover` 新增 `planId`，三处生产调用+Preview 已接线。验证：`CoverPaletteContrastTests` 7/0；`V28VisualUnificationUITests` Light/Dark 各 1/0；截图 `build/v28-screenshots/training-home-{Light,Dark}.png`。
- **训练计划封面六色大字版恢复 ✅（2026-08-04，DR-047）**：文生图三方向对比后用户否决，`CoverPalette.PlanStyle` 从 teal 单色阶恢复 v27 W2 前六色编辑式色板；保留 v28 共享卡片壳与 list/hero API。验证：`CoverPaletteContrastTests` 6/0；`V28VisualUnificationUITests` Light/Dark 各 1/0；截图 `build/v28-screenshots/training-home-{Light,Dark}.png` 已目视确认绿/蓝/青色计划卡恢复。
- **训练首页「今日安排」重设计 ✅（2026-08-04，DR-046）**：摘要卡压成严格两行，进度改 2pt 底边线；动作卡以 `.fill` 90×50 专用裁口只裁 PNG 透明留边、完整保留球桌并取消暗角，删除图上序号、阶段色降为小色点；保留完成/当前/排队及训练启动业务。`make run` 成功，模拟器截图 `build/training-home-daily-redesign-v4.png` 已目视复核。**下一步：用户确认视觉方向；如需再调，仅微调密度，不改业务。**
- **问题集合 v28 全部批次收官 ✅（2026-08-04，v28.1，DR-045）**：三 Tab 表层语法 + 练习混合封面。W0 共享壳（`BTContentGridCard`/`BTLibrarySearchBar`/`BTLibrarySectionHeader`）；W1 学=几何微插图、练/打/解=真台预览（`PracticeCoverCatalog` 24+SIM）；W2 计划列表/Hero 分态保留，teal 色阶后由 DR-047 恢复六色大字版；W3 动作库一行等级 chip、球种进菜单、完成/旧新版进元信息行；W4 `V28VisualUnificationUITests` Light/Dark。证据 `build/v28-*`。真源 `问题集合_v28.md`。**未合并** v27 W3 薄黑台垫。**下一步：用户确认六色封面；或合入 main。**
- **批量出片台隐藏退役 drill ✅（2026-08-02）**：`BatchDrillCatalog.retiredDrillIds` 排除 `drill_c019`（v21 并入 c018；源目录可仍留项目 15）。列表不再出现无 `nameZh` 的空壳行。
- **动作库引擎媒体全量出片回填 ✅（2026-08-01，本地预览）**：静帧 **1440**、2D/3D 视频 **1080×2040**、**不出 4K / 不出单杆 mp4**（保留 `initial`/`sNN_still`/`final`）；`make position-export` **76/76** 可渲序列；`import-engine-export-to-app.py --prune` 回填 **60** drill（Videos≈11G + Tutorials≈2.1G），旧 `take_*` 在已回填 drill 上清除；`make tryout-sync` + `make build` SUCCEEDED。17 条无引擎片（多为 0 杆空序列或无序列）。**下一步：模拟器翻详情页看 2D/3D；其余 0 杆球形需补录后再出片；正式分发仍走 OTA。**
- **打点盘全宽贴库边 ✅（2026-07-31 / 08-01，DR-042）**：宽=`playingRect.width`、底=`spinPadBottomPadding`（贴击球区内框下沿）；白盘封顶 168；stage 拦截关闭；9 处接线。证据 build + layout/spin 单测绿。**下一步：模拟器点验下沿对齐；或翻袋备选/v24 点验 / H-22。**
- **直击失败翻袋备选 ✅（2026-07-30，DR-041）**：`DirectPotBankFallback` 复用 `BankKickSolvePipeline.solveBank`；仅直击几何失败时触发；编排台 / 自由击球 / 思路 / 打三；文案「翻袋备选」+ 多解下一解。证据：`make build` SUCCEEDED；`DirectPotBankFallbackTests` **6/0**（`build/direct-pot-bank-fallback-test.log`）。SPEC §8.9 i。**下一步：模拟器点验四处页切角过大时出翻袋备选；或 v24/v23 点验 / H-22。**
- **问题集合 v24 全部收官 ✅（2026-07-29，v24.2，主控亲做，0 返工，DR-040）**：E1 网格/骨架封面 **4:3→2:1**；E2 列表 `ballScale=1.8` 重烘 **77/77**；E3 详情 live 仍 1.0、不双套 PNG；方槽 D-v24-1=C 维持 fill。证据 `build/v24-w{1,2}-*`。真源 `问题集合_v24.md`。**下一步：模拟器点验动作库网格完整桌+大球 / 详情顶栏真尺寸；或 v23/角度预测点验 / H-22。**
- **Drill 缩略图静帧对齐 ✅（2026-07-29，DR-039）**：共享 `DrillStaticPreview`——代表性球形（A1→legacy→A*）首杆 + 真球号 + 瞄准位球杆 + 线语言 v2 + ghost；详情首帧同契约。契约后经 **DR-040 / v24** 修订显示槽与列表 1.8。
- **角度预测竖直 0° 题面 ✅（2026-07-29）**：方案 A——0°=竖直正对、左右各摆至 90°、答题无符号；出题随机左右；参考双侧镜像；几何真源 `AnglePredictionGeometry`。改 `GeometricAngleQuizView/VM`、`PracticeStorageKey`、SPEC §9.3。证据：`make build` SUCCEEDED；`AnglePredictionGeometryTests`+`PracticeStorageKeyTests` **5/0**（`build/angle-prediction-vertical-*.log`）。**下一步：模拟器点验左右题面与参考扇；或继续 v23 点验。**
- **问题集合 v23 D-v23-5.1 ✅ 空象限对角 + 瞄准线 keepout（2026-07-29，v23.13，DR-038）**：主方向 = 开边水平×垂直对角（更大空区）；keepout 含瞄准线+进球线+袋口；0.92d 靠球。证据：Placement **19/0**（`build/v23-open-quadrant-test.log`）。**下一步：模拟器点验；再做 W2/W3 抽样点验后收官。**
- **问题集合 v23 D-v23-5 formal → 被 5.1 取代（v23.12 / DR-037）**：单轴边距过渡方案。
- **问题集合 v23 D-v23-5⁗ ✅（v23.11，DR-036）**：进球线/袋口 keepout 基座。
- **问题集合 v23 W3 ✅ HUD 扩页（2026-07-28，v23.10，DR-035）**：`AimCloseupBuilder`+`AimCloseupGate`+`BTAimCloseupOverlay` → 自由击球/编排台/分离角/翻袋·反射；AimPoint 改用 overlay。证据：`build/v23-w3-test.log` 12/0。抽样点验待。
- **问题集合 v23 W2 ✅ 增益铺开 + 特写开关（2026-07-28，v23.9，DR-034）**：5 轮页毫米增益 + 三点菜单「瞄准特写」；单测 31/0（`build/v23-w2-test.log`）。抽样点验待。
- **问题集合 v23 W1b ✅ 位置/颜色返修（2026-07-28，v23.8）**：相对焦点偏移 + 扁平台呢；已被 5⁗ 叠加进球视线避让。
- **问题集合 v23 W1 ✅（v23.3）+ 立项 v23.0–v23.2（2026-07-28）**：近区+毫米增益落地；初版示意 HUD 已被 W1b 取代。真源 `问题集合_v23.md`。
- **瞄准点验证击球可信反馈 ✅（2026-07-28，DR-031）**：验证杆速 1.5→3.3 m/s；\|误差\|≤2mm 用几何正解出杆，否则用户线；HUD 文案区分。改 `ShotTuning` / `AimPointSceneTrainingView`；SPEC §9.3 + Changelog。**下一步：模拟器点验「接近正确→多半进袋」；或拍板 v23；或 v22 货架点验 / H-22。**
- **问题集合 v22 全部批次收官 ✅（2026-07-28，v22.6，plan-delegated-execution：`cursor-grok-4.5-high-fast`；W1–W6 均 0 返工）**：四套专项（准度免费 / 力度·分离角·加塞 Pro）+ 文档 Freemium 同步；index **v6 / 10 套**。主控亲核 W6：docs/SKILL=D-v22-1、导流句未回滚 v21 钩子、`plan_advanced` 零 diff、截图准度免 PRO + 加塞详情、主树 build 绿。留档：`docs/09` 仍写「共 6 套」（非 §2.5）。证据 `build/v22-w{1..6}-*`。**下一步：可选模拟器点验货架；或拍板 v16；或 H-22 录制。**
- **问题集合 v22 W6 ✅（2026-07-28，主控验收 0 返工）**：docs/04·07·08 + SKILL；description 导流；截图 01–09；真源 v22.6。证据 `build/v22-w6-*`。
- **问题集合 v22 W5 ✅（2026-07-28，主控验收 0 返工）**：`plan_english`；index v6（10 套）；focused c073–c078 93/93 无 c018；亲读日志/截图 + 主树 build 绿。真源曾 v22.5。证据 `build/v22-w5-*`。
- **问题集合 v22 W4 ✅（2026-07-28，主控验收 0 返工）**：`plan_separation`；index v5。证据 `build/v22-w4-*`。
- **问题集合 v22 W1–W3 ✅（2026-07-28）**：定稿 + accuracy + force。证据 `build/v22-w{1,2,3}-*`。
- **问题集合 v22 W3 ✅（2026-07-28，主控验收 0 返工）**：`plan_force` 4×3×70 Pro；focused forceControl 67/67；亲读 self-check/日志/截图 + 主树 build 绿。留档：详情 CTA 在模拟器可能因 StoreKit Pro 残留显示「开始此计划」（列表 PRO 标正确）。
- **问题集合 v22 W2 ✅（2026-07-28，主控验收 0 返工）**：`plan_accuracy` + index v3；证据 `build/v22-w2-*`。真源曾 v22.2。
- **问题集合 v22 W1 ✅（2026-07-28，主控验收 0 返工）**：真源 v22.1 拍板齐+定稿；证据 `build/v22-w1-evidence/`。
- **问题集合 v22 拍板齐 ✅（2026-07-28）**：D-v22-1～6 全按推荐；**D-v22-2=A**。真源 `问题集合_v22.md`。
- **问题集合 v21 全部批次收官 ✅（2026-07-28，v21.8，plan-delegated-execution：grok-4.5；W1–W5 均 0 返工）**：加塞训练线（让点/挤偏）+ R4；计数 **77**；计划接入；学区 CTA→c073（**DR-030**）；审计 M6+R4；H-22 录制异步。证据 `build/v21-w{1..5}-*`。
- **问题集合 v22 立项 ✅（2026-07-28）**：官方专项计划货架（准度/力度/分离角/加塞）；W1–W6。真源 `问题集合_v22.md`。加塞专项依赖 v21 ✅（已满足）。
- **问题集合 v21 W5 ✅（2026-07-28，主控验收 0 返工）**：计划 cueball W6 / intermediate W1；`AngleRoute.drillDetail`+两学页 CTA→c073；审计/SPEC/DR-030/H-22。主控亲读日志/截图 + 主树 build 绿。证据 `build/v21-w5-*`。
- **问题集合 v21 W4 ✅（2026-07-28，主控验收 0 返工）**：R4；bake 18/18；c019 退役；计数 77。证据 `build/v21-w4-*`。
- **问题集合 v21 W1–W3 ✅（2026-07-28）**：加塞新课 6 条入库。
- **问题集合 v21 W3 ✅（2026-07-28，plan-delegated-execution：grok-4.5，主控验收 0 返工）**：加塞准度 c076/c077/c078。主控亲读 bake 16/16 ALL_OK、validation 20/0、截图（带塞列表/详情）、`spinKind` 与 spin 一致性 True；主树 `make build` SUCCEEDED。证据 `build/v21-w3-*`。真源升 v21.6。
- **问题集合 v21 W2 ✅（2026-07-28，plan-delegated-execution：grok-4.5，主控验收 0 返工）**：基础加塞 c073–c075。主控亲读 bake 14/14 ALL_OK、validation 20/0、截图 01–03、profile spin、仅进袋、c073 免费；主树 `make build` SUCCEEDED。证据 `build/v21-w2-*`。真源升 v21.4。
- **问题集合 v21 W1 ✅（2026-07-28，plan-delegated-execution：grok-4.5，0 轮返工）**：口径与承载基座。主控亲读：`s1-squirt.md`（左塞挤右→瞄准向左让；c018 旧文案 W4 修正）、`squirt-vs-power.md`（挤偏与力度无关）、`straight-scratch-scan.md`（dtp∈[0.12,0.25]）、`schema.md` spin 字段、`20260728-v21-W1口径备忘.md` 定稿表+六要素；日志 3/0 + TEST SUCCEEDED；主树 `make build` SUCCEEDED。证据 `build/v21-evidence/`。真源升 v21.2。
- **问题集合 v21.1 拍板齐 ✅（2026-07-28）**：D-v21-1～7 全部按主控推荐 + 用户指定模型落地。**模型**：全批统一 `cursor-grok-4.5-high-fast`。**决议摘要**：拆开放；R4 并入；profile 扩 spin；c073 免费；录制内容先行；c018 吸 c019、终计数 77。真源 `问题集合_v21.md`。
- **问题集合 v21 立项 ✅（2026-07-28，Orchestrator，纯文档，issue-collection-restructure）**：主题＝带塞进袋准度（让点/挤偏补偿），⛔ 非碰库变线。形态 A 跟打 drill；新增 c073–c078 ≈30 球形 + R4；批次 W1–W5。新发现：计划零引用加塞 drill；profile 无 spin。存疑 S1 让点方向 / S2 洗袋。真源 `问题集合_v21.md`（已升 v21.1）。
- **母球初始朝向随机 ✅（2026-07-28）**：只随机母球；新摆球 `CueBallPosePolicy.reseat` 抽均匀四元数写入 `cueBallHomeOrientation`；重打/复位 `.home` 保持；散局落定/回放收尾 `.unchanged`。目标球仍单位姿态；缩略图烘焙钉死 identity。覆盖编排台/思路/三杆/斯诺克/翻袋·反射复位/开球/图谱/抽球确认/导出器等。证据：`make build` SUCCEEDED；`TrajectoryPlaybackSpinTests` **9/0**。**下一步：模拟器点验母球红点不再总在顶；或继续 v20.5/v19 点验；v16 待拍板；v18 已暂缓。**
- **问题集合 v20 ✅ + v20.5 修订（2026-07-28）**：收官后用户点验「碰前只画一条」→ 按实况改画 **碰前 8 色线**（废止淡灰单条；D-v20-5 修订）。改 `CushionEnglishAtlasViewModel.drawTrajectories` + HUD/SPEC/真源。
- **问题集合 v20 全部批次收官 ✅（2026-07-27，v20.4，plan-delegated-execution：`cursor-grok-4.5-high-fast`；W1+W2 均 0 轮返工）**：学区新卡「加塞吃库图谱」——中杆×8 档左右塞×碰后首库出射扇形；挤偏补偿隔离；独立学卡。**W1** Geometry+单测；**W2** View/VM+注册4处+SpinAndEnglish 导流+SPEC/DR-029+a17/交互测。证据 `build/v20-{logs,evidence,screenshots}/`。真源 `问题集合_v20.md`。
- **问题集合 v19 全部批次收官 ✅（2026-07-27，v19.5，plan-delegated-execution：grok-4.5 执行 + 主控独立验收，0 轮返工）**：瞄准线⇔球杆同现。C1–C7：Bank/Diamond 求解画线摆杆 + 出杆 `strikePosition(spinX:)`；Break `drawAimLine` 摆杆；Composer G14 预览跟杆；AimPointScene 瞄准跟杆；SceneAiming 保持无杆；回放/序列禁出杆前 hide + 短定格；SPEC §8.9 **h** + DR-028。**主控验收**：diff 逐文件亲读；落盘 build/单测 42/0/UI 6+1/0 日志尾亲核；截图 01–06 目视；主树 `make build` SUCCEEDED。留档：Diamond 无单独截图、序列无独立录屏、全量单测未跑、AimPoint 取证为 Scene 2D 入口（同 VM）。证据 `build/v19-evidence/`。**下一步：模拟器点验跟手/衔接手感；或拍板 v16 / v20；v18 已暂缓。**
- **问题集合 v18 ⏸ 暂缓（2026-07-27，v18.1）**：议题「不穿模前提下母球可行打点」真源已落盘（`问题集合_v18.md`），**不执行 W1–W4**。用户决议：**并入后续「高度维度」议题再做**（与仰角一等公民入口 / `v_eff` 同轴，单独先做易重工）。**D-v18-1（spinY 杆轴系）仍有效**；D-v18-2～7 未拍。穿模底线靠 DR-027；暂缓期接受打点盘可选摆不出的低杆、`.blocked` 藏杆仍发球。重启条件见真源「暂缓决议」。
- **球杆穿模守卫 ✅（2026-07-27，DR-027，plan-delegated-execution：cursor-grok-4.5 执行 + 主控独立验收，返工 1 轮）**：用户报两类穿模——①杆身后方有球时平放穿过球模型；②跟杆定格期间倒旋母球/吃库返回球/连锁碰撞球撞进杆身。**三块交付**：(A) `CueStick.requiredElevation` 新增球遮挡项与库边项取 max（`atan2(R + shaftR(s) + 2mm, s)`，锥体半径按 s 插值，遮挡区间覆盖 `maxPullBack=0.15m` 以防「回杆到最远端才穿」），上限 31.5°→60°、超限返回 `.blocked` 隐藏球杆而非硬画；障碍球由 `AngleTrainingScene.updateCueStick` 内部取 `visibleBalls()`，**20+ 调用点零改动**；新增 `elevationOverride` 冻结整杆仰角（导出器逐帧循环否则随球移动抖动）。(B) 新增 `CueClearance` 通用碰撞守卫——遍历**全场球**（非仅母球）× 球杆线段逐帧求首个相交时刻，仅在预测到真会撞时于 `t*−0.12s` 起抽杆淡出，否则保持原 1.5s 定格；`clearanceProbe=nil` 时与改前逐点等价。(C) `clampedFollowThroughPull = −min(3R, 前方表面间隙)` 防跟杆捅进正前方贴球。接线 PositionPlay/Silu/SnookerTactics/PlanThree + 导出器同口径；Bank/Diamond/AimPoint/BreakFlow 传 nil 保持原行为。**返工 1 轮（主控数值验收打回）**：首版碰撞守卫在 v≲1.4 m/s 的软杆上于 τ=1/60 **误报**（根因＝`tipOffset=R+1mm` 小于碰撞阈值 `R+tipR+margin`，静止贴球姿态本身即违反阈值，而守卫只跳过 i=0；触球瞬间杆速=球速故软杆母球跑不开杆头），后果是跟杆整段被跳过、球杆 0.18s 就消失，违反 D2；且 `worldPoint` 手推欧拉展开无 SceneKit 交叉验证、两条测试接近自证。修复采用**按球分离闩锁**（每球须曾真正脱离阈值一次才成为候选，探针改 `[String: SCNVector3]` 防下标错位），无任何速度/跳帧特例分支。**主控独立验收**：逐文件读全量 diff；亲跑 `make build` **BUILD SUCCEEDED** ×2；亲跑 `CueClearanceTests` **12/0**（含 v∈{0.4…3.0} 八档软杆无误报、倒旋分离后仍捕获、目标球返回捕获、`test_shaftSegment_matchesSceneKitNode` 用真实 `SCNNode.convertPosition` 三组含 30°/37°/−120° 验证手推欧拉展开一致）；亲跑 **`QiuJiTests` 全量 684/0 失败（2 skipped）**（基线 672 + 新增 12，无回归）。**留档**：`.blocked` 分支在合法盘面**不可达**（球遮挡峰值 32.26° @ s=2R、库边峰值 23.20° 受 `max(0.05, dist)` 地板封顶），仅作防御性护栏，未为可达而放大公式——即原问题本质是「完全没有球遮挡逻辑」而非「上限太低」；大仰角渲染时引擎仍按平杆积分（物理不自洽，本轮不改引擎，未做「需架杆/立杆」UI 提示）；无 SceneKit 离屏截图，**抬杆姿态与提前收杆时机需用户模拟器/真机点验**。证据 `build/cue-clearance-evidence/`。
- **Logo「O 不居中」修正 ✅（2026-07-25，DR-026）**：用户反馈 logo 里的环看着偏左。实测确证成因＝snail-QJ 由「环」+「右下尾撇」组成，原摆位按**整体外接框居中**，尾撇把框心拉右、环被挤到画布左侧（SVG −7.8%、App Icon −5.5% 画布宽）。改为**按环光学居中**（回收 60% 偏差；未做 100% 居中因严格居中需把 scale 压到 1.4658 且尾尖贴死画布右边缘、左侧留大片空白反更失衡）。改动：`brand.logo-mark{,-dark}.svg` transform `translate(-559 -443)`→`translate(-456.4 -443)`（scale 1.492 不变）；`AppIcon.png` 白色图形右移 34px——**非像素平移拷贝**，而是对背景渐变做逐通道线性最小二乘拟合（rms 0.325/255）反解图形 alpha（保住抗锯齿与极细尾尖）后在重建底上合成。坐标契约：SVG 用户坐标 `viewBox 0 0 2048 2048`，x 右 y 下，画布中心 (1024,1024)。**同轮追加「占比有点小」→ 放大 ✅**：App Icon 以环心为中心等比放大 **1.25×**（**从矢量路径重渲染**，4× 超采样 + Lanczos 降采样，不做位图放大；裸路径环圆拟合 `center=(954.365,990.360) r=475.288` → 1024 画布 `translate(-137.259 -135.195) scale(0.656917)`）；`BTBrandLogo` `.onTile` 内边距 **16%→10%**（`tile(_:inset:)` 加参数），`.onDisc` 显式保持 16%（内切圆比 squircle 窄，缩边会让尾尖顶出圆外）。上限提示：尾尖是唯一硬约束，1.25× 后右侧余 92px，约 1.53× 即贴死右边缘。**证据**：环圆最小二乘拟合环心 dx SVG **−79.7px(−7.78%) → −28.4px(−2.77%)**、Icon **−56.3px(−5.50%) → −22.5px(−2.20%)**；占比 App Icon 图形宽 **58.7%→73.7%**、环直径 **48.8%→60.9%**（留白 L177 R92 T205 B205，未裁切未触圆角），`.onTile` 图形宽 **57.2%→67.8%**、环直径 **47.0%→55.4%**；竖直位置全程不变；App Icon 非图形区域与原图最大差 2/255、均值 0.10/255；`make build` **BUILD SUCCEEDED** ×2 + `Assets.car` 重编译 + Bundle 内 `AppIcon60x60@2x` 随动（宽占比 53.3%→68.3%）。上游真源 `18.qiuji_icon_design` 已同步摆位契约（`ICON-INVENTORY.md` §7.1 + `*.optical-center.svg`）防回退。**未纳入**：`QiuJiLiveActivity/Assets.xcassets/AppIcon.appiconset/` 三张是旧版 3D 写实图标（不含环、与扁平 snail-QJ 不同源），且三张文件字节完全相同即 Dark/Tinted 实际未做区分——**独立待办，本轮未修**。**下一步：模拟器/真机点验图标与 Onboarding/Login/About/ShareCard 的 logo 观感。**
- **问题集合 v17 全部批次收官 ✅（2026-07-25，主控亲做 D-v17-6，W1+W2 均无返工）**：加塞旋转不上屏根治——根因是**物理层每帧算出角速度 ω 但渲染层从不消费**（球体朝向由位移弧长反推、轴写死 `ŷ × v̂`），故加塞竖轴分量整体丢失、低杆倒旋方向反、`.spinning` 原地自转被「有位移才转」门槛卡死。**W1**：`PlaybackBallState` 透出 `angularVelocity`，新增 `BallSpinIntegrator`（`quat(|ω|·Δt, ω̂)` + 前后帧 ω 梯形均值 + 每帧归一化 + `resetPose`），`TrajectoryPlayback.action` 改按模拟时间积分，姿态复位收口 `AngleTrainingScene.showBall/applyBallLayout`（+ `DrillSceneView.resetBalls`、Bank/Diamond `restoreBall`），清 `rotationPrefixSums` 一族。**W2**：删死代码 `SceneKitBridge.swift` + `TrajectoryRecorder.action`（`make xcodegen` 重生，pbxproj 零残留）；离线出片 `drill_c019 右塞一库变线`（spinX=−0.405）验证同源。**D-v17-4 可见性闸门：可辨 → 继续**（3D 斜视明确；顶视在红点离极点时可辨，红点正朝上时纯 ω_y 近乎不可辨＝麻点球贴图固有，未改材质、未加指示器）。**证据**：`make build` SUCCEEDED；`QiuJiTests` 全量 **672/0 失败**（2 skipped，`build/v17-evidence/logs/w2-full-unit-tests.log`），物理网与 `TrajectoryPlaybackSettleTests` 全绿；新增 `TrajectoryPlaybackSpinTests` 8 例（纯滚动新旧口径偏差 <1%）+ `SpinExportParityTests` 2 例（真实 `action` 闭包经 `SCNRenderer` 时钟驱动 vs 导出器循环 121 帧最大偏差 **6.9e-4 rad**；视频抽帧 vs App 同相位裁剪红点偏移每帧差 **≤1.9 px**、位置配准残差 2.5 px）；截图 `build/v17-evidence/{dv174-visibility,spin-sign,w2-export-parity}/`。**ADR-P11-16**（显示契约变更）写入 `tasks/phases/P11-position-play-composer.md`。**下一步：模拟器/真机点验加塞自转手感；存量出片资产（除 drill_c019 外 125 条）仍是旧口径渲染，需排期批量重出。**
- **批量出片台删除球形 UI ✅（2026-07-25）**：进 drill 选图栅格后，长按已存截图 / `manualNN` / 旧版存档条 → 确认对话框 → `BatchSequenceArchive.deleteArchive` 删内容库序列 JSON（不动源截图与 drill 课程）→ `refreshSaved`。改动：`BatchDrillStudioCore.swift`、`BatchBallExtractionView.swift`。验证：`make build` **BUILD SUCCEEDED**。**下一步：模拟器点验长按删除手感。**
- **批量出片台摆球精调四键 ✅（2026-07-24）**：摆球态 + 选中球（含母球）时球桌左侧竖直四向键；步进 0.5mm；点按/长按连发（复用抽离的 `BTHoldRepeatButton`）；无选中隐藏；仅批量出片台。坐标契约：rotated 顶视 screen↑=+X、screen→=+Z。改动：`BallNudgeMath` / `PositionPlayViewModel.nudgeBall` / `BatchAuthoringView` / `BTHoldRepeatButton`（打点盘改用共享件）。验证：`make build` BUILD SUCCEEDED；`BallNudgeMathTests` **2/0 TEST SUCCEEDED**。**下一步：模拟器点验摆球精调手感。**
- **翻袋解球器线旁文字 ✅（2026-07-21）**：`BankShotViewModel.drawSolution` 轨迹·全档去掉贴地「瞄准线 / 进球线」`addFlatLabel`（线本体与 full 档金点/法线保留）。`make build` BUILD SUCCEEDED。
- **翻袋贴库预反射种子 ⚠️ 返工（见 FL-027）→ ✅ 实测重标重构（2026-07-21）**：首轮（07-20）种子标签直出 + clamp 兜底产生假解/重复解/库数失真，用户打回。返工：删 clamp 兜底；`ShotPrediction` 新增 `objectRailContacts`（引擎事件流主库分类，jaw 不计）+ `objectDepartureDir`；`BankShotCalculator.reconstructedRailContacts` 补回贴库 t≈0 无事件首弹（碰后方向 >sin3° 扎向自库才补）；`BankEngineSolution` 库序/库数/扎库标记全部**实测派生**（实测空=直击淘汰，`seedRails` 单列作微调锚）；K9 去重去掉库数相同前置；文案「扎库(自库)」；去粘性 OR。证据：`BankKickDifficultyTests` 18/0、全量 QiuJiTests **656/0 失败（2 skipped）**（`build/frozen-rail-logs/full-suite3.log`）、诊断矩阵扎库标签一致 17/17、贴库出解 18%（假解淘汰后如实口径）vs 对照 70%。**下一步：用户真机点验贴库翻袋。**
- **翻袋/反射假想球 + 钻石数字 ✅（2026-07-20）**：根因＝两页 VM 未调 `setupVisualizationNodes()` → `ghostBallNode == nil`，`TrajectoryRenderer.showGhost` 静默空转。已对齐补调；长库数字标注去掉。回归曾 16/0，现并入上条 18/0。
- **人工球形设计工作流 ✅ 全部收官（2026-07-20，真源 v1.4）**：B1–B4 均 0 轮返工合入主树。B1 出片台新增球形；B2 基准图集 15 图；B3 变量词典；B4 `verify_manual_formations.py`（主树复跑 c053 金标准 ALL PASS，最大切角误差 0.0033°）。证据 `build/b{1,2,3,4}-*`。留档：A4 nearRail profile=true vs 阈值 0.06 实测 false（未调阈）；`content/…/c006__manual01|02` 未跟踪（仅 cueBall 样例）。**下一步：§五 用户人工设计循环**（对照图集+词典在出片台摆球 → 跑 B4 验算）。
- **问题集合 v16 立项 ⏳（2026-07-20，Orchestrator，纯文档，待用户拍板）**：练习 Tab 产品化——用户认可功能/UI 现状、要求增强产品属性；主控 6 方向分析（状态层/技能分/每日挑战/入口收敛/闭环补边/首次诊断）后按 `/issue-collection-restructure` 落成**《问题集合_v16.md》**（本轮需求真源）。核心诊断：练习 Tab 为静态工具箱、成绩数据不回流首页（违 docs/01 §4「数据回灌」护城河）。批次 **B1 状态层地基 → B2 每日挑战+streak+分享卡 ∥ B5 练→学深链 → B3 技能分 → B4 诊断测试**；P4 入口收敛与解→打存为练习默认缓做。锚点全部读码核实（AngleHomeView/AngleTestResult/AngleHistoryViewModel/AdaptiveQuestionEngine/AngleUsageLimiter/ShareCardImageRenderer/PracticeStorageKey）。**待用户拍板 D-v16-1～7**（范围与优先序/弱项数据源/挑战形态/技能分口径/深链映射/入口收敛/执行模型）。**下一步：用户裁决拍板项后按 B1 开工。**
- **问题集合 v14 全部批次收官 ✅（2026-07-20，plan-delegated-execution：全程 cursor-grok-4.5-high-fast，B1–B3 均 0 轮返工）**：六文档学页壳统一——B1 组件（`LearnDocSectionCard`/`LearnDocText`/`LearnControlStrip`/`LearnDocTextLink`/`LearnDocFormulaNest`）+ B2 交互四页接壳 + B3 原理/球感接壳与公式降级 + 六页巡游取证。**主控验收 B3**：diff 亲读（无越界两沙盘）、落盘 build/单测 4/0/`testAngleLearningPages` 明暗绿亲核、原理/球感/公式截图目视、主树 `make build` SUCCEEDED。真源升 v14.5。证据 `build/v14-b{2,3}-*`。**下一步：无未完成问题集合批次；可选提交 v14 全量改动，或开新议题/动作库 R1 等。**
- **问题集合 v15 全部批次收官 ✅（2026-07-20）**：分离角图谱 W1。真源升 v15.4。
- **问题集合 v13 全部批次收官 ✅（2026-07-20）**：瞄准方法诚实性收口。真源升 v13.3。
- **问题集合 v12 全部批次收官 ✅（2026-07-18，Z4 合并 e071ebf）**：「旋转与加塞」页重做——切角 θ 滑杆联动三路径（示意角教学折线 90/60/120，非半球诚实标注）；打点→旋转 + 吃库顺/逆塞定性示意（T09/Jewett，非引擎实况采样）；Miscue token 化；teaser→真 `PracticeCTA`→`.aimingCorrection`（UI 测断言到达「瞄准修正」导航栏）。**主控验收**：diff 逐文件读（未越界 AimingCorrection/引擎）、几何单测 4/0、Z4 UI 明/暗 TEST EXECUTE SUCCEEDED + 截图 7×2 亲核、主树 build SUCCEEDED。留档：全量学区巡游遇模拟器 signal-kill（环境抖，非页面断言失败）。证据 `build/z4-{evidence,logs,screenshots}/`。真源升 v12.7。**v12 Z1–Z4 全部 ✅。**
- **问题集合 v12 批次 Z3 ✅（2026-07-18，plan-delegated-execution：grok-4.5 worktree〔v12.5 改版〕，0 轮返工）**：「瞄准修正」②③ 插图由裸 Canvas 改为学区统一 `BTTableFigure`/`FigureLine`/`BTFigureTag`；④加塞节（左右塞轴+打滑极限圆盘钳制+俯视挤偏/碰前轨迹）；⑤两档求解对比（A 中杆中速 / B 低杆轻推+左塞）+ 定性速查表；⑥收尾核对；a16 巡游扩帧；SPEC §9.3 契约。**z3 数值补稿**：挤偏无关力度 YES；平杆纯侧旋弧线≈0（正文未写「必弯」）；组合档（左塞+低杆）越慢越大 YES。**主控验收**：diff 逐文件读（引擎零改、无 SpinAndEnglish 越界）、单测 14/0 + 明/暗 UI + a16 巡游日志亲核、默认态截图投掷角≈1.55° 与 z2 CIT 对齐、合并 424b9d2 后主树 `make build` SUCCEEDED。产物 `build/z3-{evidence,logs,screenshots}/`。真源升 v12.6。**下一步：Z4「旋转与加塞」页重做（grok-4.5，串行）。**
- **问题集合 v12 批次 Z2 ✅（2026-07-18，plan-delegated-execution：grok-4.5 worktree 执行〔D-v12-4 拍板路由〕，0 轮返工；中途两次用户误终止后 resume 续做，半成品未损失）**：②投掷效应节——碰撞放大 Canvas 图（接触点/摩擦箭头/理想进球线虚线 vs 实际离开实线，Δ×8 夸大标注）+ 投掷角实时读数；③高低杆有效厚度节——低/中/高三档三联实况对比（`objPostContactDir` 各档一跑，较中杆厚/基准/较中杆薄标签随实况数值不硬编码）；共享控件第一期——力度滑杆 + 高低杆三档 Picker，接 `AimingCorrectionMath` 真源层，`SolveDebounceScheduler` 20ms 去抖，三联按力度缓存减负；左右塞轴留 Z3（`spinX` 锁 0）。**Z2 数值补稿两条首跑证伪记录**：SIT 方向需 cut=0+squirt 补偿隔离（不补偿时挤偏几何效应 −9.6° 反号盖过 SIT +1.0°，正文红线「只说投掷分量向右」）；CIT 非全程单调、半球附近见顶（15° 1.12° < 30° 1.55° ≈ 45° 1.53°），ALL_PASS=true。Z1 留档①节特写帧已补（虚实线近乎重合为引擎真实 Δ=+0.09°，诚实呈现留档）。**主控验收（落盘产物亲验）**：Math/VM/View/双测试 diff 逐文件读（快速模型纪律，只读引擎、手性数值锚定 `testHandednessCalibration`）、build SUCCEEDED + 单测 10/0 + 明/暗 UI 巡游各 TEST SUCCEEDED 日志尾亲核、明暗各 7 帧截图抽验目视（摩擦标签防遮挡修正后复验）、scheme 漂移主控还原后提交、合并 b1807e2 后主控亲跑主树 build SUCCEEDED + Z1/Z2 单测 TEST SUCCEEDED。产物归档主树 `build/z2-{evidence,logs}/`；worktree 已清理。真源升 v12.4。**下一步：v12 Z3 收口（加塞节+求解对比节+巡游扩帧+SPEC 契约，grok-4.5，串行）。**
- **问题集合 v8 波3 X5 ✅ 收官——v8 全部批次完成（2026-07-18，plan-parallel-delegated-execution：grok-4.5 worktree 执行〔用户改派，原 opus-4.8，真源 v8.7〕，0 轮返工）**：K10 翻袋/反射加塞搜索档——`BankKickSolvePipeline.solveBank/solveKick` 外层枚举 spinX∈{−0.3,0,0.3}（引擎零改动），解携带塞 + pill `spinLabel`，`BankKickSolveKey` 扩 `spinXCenti`（全档哨兵 10000）；K11 求解模式打点盘可微调——X6 草稿层同语义移植两 VM（`adjustCurrentSolution` 正向 `ShotPredictor.predict` 重算入 `adjustmentDraft`，catalog `solutions[idx]` 不可变、`nextSolution` 回原解、击打/undo 走草稿含重建），`SolverStageChrome` 求解有解开打点盘 + power/spin binding 走草稿路径。**主控验收（落盘产物亲验）**：4 个业务文件 diff 逐文件读（快速模型纪律）、build/回归 21/0/PERF 4/0/UI 2/0 日志尾亲核（UI 日志内另有一段更早会话 runner bootstrap 环境失败、无用例执行，成功会话 2/2 为准）、解锁盘面 before/after + 微调 6 张截图逐张目视（pill 塞标注「左塞/高杆左塞」上屏、打点盘求解态可开、微调后瞄准线重算）、性能证据亲读（全档 ~0.98s，降档策略留档未启用）、scheme 漂移主控还原后提交、主控亲跑主树 build SUCCEEDED。合并 c777e30；产物 `build/x5-{logs,screenshots,evidence}/`；worktree 已清理。**同波 v12·Z2 子智能体被用户误中止后 resume 续做，已于同日完成合并（见上条）。**

- **跨方案波2（v8 X4 ∥ v8 X6 ∥ v12 Z1）✅ 收官（2026-07-18，plan-parallel-delegated-execution：grok-4.5 ×3 并行 worktree〔用户改派，原 opus-4.8〕，三批均 0 轮返工）**：交集实证零重叠（v8 v8.5/v8.6、v12 v12.2/v12.3）；前置 v11 Y1–Y3 已提交（6ca5e58）；合并顺序 X6→X4→Z1，每批合并后主树 build 亲跑 SUCCEEDED ×3。**X6（5328e0a）**：K12 PlanThree/Snooker 摘「求解范围」恒全能力 + K13 三页微调草稿层（solutions[] 不可变、下一解回原解、击打走草稿，X5 移植真源）；验收=diff 逐文件读 + 单测 22/0 亲核 + 4 张截图目视。**X4（55d1d91）**：K9 展示几何由精修 finalAim 回写（ghost 反推/kick firstContact）+ 去重按精修瞄准/进球首段判同、K14 两页 drawSolution 收敛 `TrajectoryRenderer`（吃库金点+法线保留 full 档）；验收=diff 逐文件读 + 全量 614/0/2 + 钉子 14 条 + PERF 噪声内 + 数值草稿 err~1e-16 + 3 张截图目视；越界外物（Y3 stub/Videos symlink）未合并。**Z1（a83bdce）**：新学页「瞄准修正」基座——`AimingCorrectionMath` 只读真源层 + ①⑥节骨架 + 注册 4 处 + 数值草稿 ALL_PASS（左塞挤右/轻力投掷大/高杆偏薄/低杆偏厚）+ 单测 6/0；pbxproj 冲突 xcodegen 重生成解决。**主树补跑三批单测 24/0（TEST SUCCEEDED，交叉验证 X4 改 ShotPredictor 后 Z1/X6 不受影响）**；产物拷回 `build/{x4,x6,z1}-*`；worktree ×3 已清理；scheme 漂移两树均还原。**下一波：X5 ∥ Z2（X5 opus-4.8 / Z2 grok-4.5，交集已核零重叠），建议新会话执行。**

- **问题集合 v11 批次 Y3「分离角图谱」✅（2026-07-18，plan-delegated-execution：grok-4.5 执行 + 主控独立验收，返工 1 轮）**：学分段新交互页——SceneKit 真台 + 可拖母球/目标球（默认 ~30° 半球）+ spinY∈[±miscueLimit] 均匀 8 档并行 `simulateFree`，切片「首次球-球→其后母球首个 ballCushion，**未吃库降级到停球点（r1）**」；仅两端标注「纯高杆/纯低杆」；右缘 `BTShotInstrumentColumn`（力度可调、打点盘只读 8 色点）；20ms 去抖+单飞+末班车（8 路并行实测 ~66ms 后台队列）；**DR-025** 页内 8 色板豁免线语言 v2；注册 4 处 + Y2 CTA 互链。**返工 r1（主控截图放大核验打回）**：默认力度纯低杆碰后停球无 ballCushion → 第 8 条轨迹与「纯低杆」标注缺失，切片降级修复 + 回归单测。**主控验收（落盘产物亲验）**：diff 逐文件读、r1 build/单测（`SeparationAngleAtlasTests` 4/4）/巡游 a15 明/暗/交互取证（`-y3.uiHooks` 钩子，SceneKit 叠层合成拖力度不可靠）日志尾亲核、默认/高力度/拖球/打点盘截图逐张目视（含裁剪放大验两端标注）、主控亲跑主树 build SUCCEEDED ×2。证据 `build/y3-{logs,screenshots,evidence}/`（r1 产物 `-r1` 后缀）。真源升 v11.6，**v11 Y1–Y3 全部 ✅ 收官**。**下一步：v12 Z1「瞄准修正」基座（opus-4.8）或 v8 波2，串行不并波。**
- **问题集合 v8 波1（X1∥X2∥X3 并行 worktree）✅（2026-07-17，plan-parallel-delegated-execution：grok-4.5 ×3 + 主控落盘产物亲验 + 串行合并逐批 build，X3 返工 1 轮）**：**X1/K1–K4**——首页卡片去 `minimumScaleFactor` 缩排（标题 2 行/副标题单行尾省略）、角度预测参考虚线缩短至球缘前（`rayLen−ballR` 下限 0.72）、3D 角度弧改前向楔形+标签沿角平分线（数值草稿 `build/x1-evidence/k3-*` 全样例过）、**K4 相机忽近忽远修为确定性近景进场（zoom=0，推翻 v5 契约，记 DR-024）**+ onChange 竞态 defer 修复。**X2/K5**——解三页球库 30→36（底栏 Silu/Snooker 94、PlanThree 116→132）、Extraction/Batch 壳对齐 36 档、compact 30 消费点清零（grep 审计留档）、BatchAuthoring 底栏 101→116。**X3/K6–K8**——全 App 开球统一手动交付三态（`autoDeliverOnSettle` 默认 false，Composer 同改 D-v8-3a）+ seed 随机初值、开球打点盘（runner `spinX/spinY`→`BTSpinPadOverlay`→`breakNow` 真传）、瞄准线接 `AimLineGeometry.resolve`（接触→球面交点+红点/未接触→库边，数值草稿留档）；**返工 1 轮**：W9b Composer 旧「自动落座」断言改手动交付语义（实跑 1/0）。**主控验收（落盘产物亲验）**：三树 diff 逐文件读；X1 单测 3/0+UI 5/0、X2 单测 589/0/2+UI 4/4、X3 单测 12/0+UI 2/0 日志尾部亲核；截图逐张目视（K1 字号恒定、K2 三帧不遮线、K4 五帧 MD5 互异、X2 四页底栏同档、X3 三态钮/打点盘/停稳散局）。**合并**：X2→X1→X3 逐批合并，pbxproj 冲突 xcodegen 重生成解决，每批合并后主树 build SUCCEEDED ×3 + 主树补跑 BreakFlowRunnerV6+X1 套 15/0；scheme 漂移与 4 张 DrillThumbnails 污染已 checkout 还原；产物拷回主树 `build/x{1,2,3}-*`；worktree ×3 已清理。真源 v8.4（波次/模型定稿 + X3∥X6 交集勘误）。**下一步：新会话执行波2（X4∥X6，opus-4.8 并行 worktree），完成后波3 X5 收口。**
- **问题集合 v11 批次 Y2「旋转与加塞」学页 ✅（2026-07-17，plan-delegated-execution：grok-4.5 执行 + 主控独立验收，0 轮返工）**：按 v11.3 重定边界交付——旋转四态（滑动/前旋/后旋/自然滚动）→ 分离角联动为主轴：分段选择器高亮三条示意路径（切线 90° / 前旋 60° / 后旋 120°，T01/T02/T03，半球教学球形），几何提为可测真源 `SpinAndEnglishGeometry`（复用 `AimingMethodsGeometry.scene`）+ 不变量单测 2/2 + 数值草稿 `build/y2-evidence/` OVERALL PASS；最小加塞（T09）+ 打滑极限正面打点图（`CuePhysics.miscueLimitFraction` 真源、区内/区外图例）；投掷/挤偏/弧线一句话概念 + 「瞄准修正」**文字预告**（用户指令 Y2 先于 v12 Z 批执行，软依赖降级、Z 批落地后补链）；注册 4 处（琥珀封面色「旋」卡）+ 巡游 `a14-spin-and-english` 扩帧。**主控验收（落盘产物亲验）**：diff 逐文件读（无越界：未做力度滑杆/8 轨迹重交互、未动瞄准原理页结构）、build 日志 + 主控亲跑主树 SUCCEEDED、单测 2/0 + 巡游明/暗 TEST SUCCEEDED 日志尾亲核、明/暗各 5 帧逐张目视（插图无溢出、标签无遮挡、切线⊥进球线与草稿一致）；巡游副作用截图污染（docs/ui-polish 36 张）已清理。词表 4 条（切线 / 旋转四态 / 打滑极限 / 挤偏·弧线·投掷）+ Changelog 入 SPEC §8.8。真源升 v11.4。**下一步：v12 Z1「瞄准修正」基座（opus-4.8）或 v11 Y3「分离角图谱」（拍板项已齐），二者串行不并波。**
- **问题集合 v11 批次 Y1 ✅（2026-07-17，plan-delegated-execution：grok-4.5 执行 + 主控独立验收，返工 1 轮 / FL-026）**：D-v11-1~5 全部拍板（2 新学卡 / 学·分离角图谱 / 打点盘只读 / 双球可拖 / 三批统一 grok-4.5 + 主控逐文件读 diff）；波次定性 Y1→Y2→Y3 全串行（注册文件业务交集）。**Y1 首轮**按 v11.0 调研口径交付后，**用户指正三瞄准法定义与原意不符**（管道=双管道相切、接触点=点对点 Pt/Pc 碰合、平行线=接触点连线过心平行）→ 记 **FL-026**（口语术语单方面映射未回确认；教训回写 `issue-collection-restructure` 技能），真源 §2.1 勘误升 v11.2（核心恒等式 Pc→Pt≡G−C 数值草稿验证）。**返工 r1**：新学页「瞄准方法」交互化落地——共享 θ 滑杆 + 管道试瞄三态徽章（相切吸附亮 Q）+ Pt/Pc 碰合动画（心对点误导线标注 1.8° 误差）+ 平行线 θ 联动（Mosconi 降变体附注）+ 厚薄法补充节（`NamedBallThickness` 真源）；几何提为可测真源 `AimingMethodsGeometry` + 不变量单测 5/5；注册 4 处 + 瞄准原理页 CTA。**主控验收（落盘产物亲验）**：diff 逐文件读、build 日志 + 主控亲跑主树 SUCCEEDED、单测 5/0 + 巡游明/暗 TEST SUCCEEDED + 交互取证 UI 测日志尾亲核、明/暗 5+5 帧与交互 5 帧逐张目视；巡游副作用截图污染已清理。词表增补（接触点 Pt/Pc/Q、管道/试瞄角 φ）+ Changelog 入 SPEC §8.8。**下一步：新会话执行 Y2「旋转与加塞」（注意 v12 D-v12-1 可能重定 Y2 边界，Y2 开工前先拍板 v12）。**
- **问题集合 v11 立项 ⏳（2026-07-17，Orchestrator，纯文档，待用户裁决）**：用户新需求（学区补充管道/接触点重合比例/平行线瞄准法 + 投掷效应/加塞理论/母球旋转状态；新增分离角可视化交互页「力度可调 + spinY 8 档 8 色轨迹碰后→第一库 + 仅两端标注 + 统一仪表柱」）按 `/issue-collection-restructure` 整理为**《问题集合_v11.md》**（本轮需求真源）——N1/N2 需求回显 + §二调研口径（网络调研 + 项目 16 T01/T02/T03/T09）+ §三代码事实（学区=硬编码 SwiftUI 页非 JSON、图文样式真源 `BTTableFigure`+`FigureLine`、`simulateFree`+`events` 切片可得碰后→第一库、纯高/低杆上界=`miscueLimitFraction` 0.5R、仪表柱=`BTShotInstrumentColumn`）+ 批次 **Y1 瞄准方法页 → Y2 旋转与加塞页 → Y3 分离角图谱**（注册文件共享建议串行；不与 v8 X 批同波）。**待用户确认 D-v11-1~5 拍板项**（内容组织 2 新卡 / Y3 命名归属「学·分离角图谱」/ 打点盘只读 8 点高亮 / 双球可拖 / 执行模型）。锚点全部读代码核实。**下一步：用户裁决拍板项后新会话执行 Y1。**
- **问题集合 v10 批次 W1 ✅（2026-07-17，执行智能体 grok-4.5）**：B1 训练/记录 drill 缩略图统一为 `BTBakedDrillTable(drillId:)`。W1-1 `DrillSummary` 补 `drillId`；W1-2 总结页换烘焙图；W1-3 历史详情删私有毡台；W1-4 自定义计划构建器同改。`miniTableThumbnail` 全仓清零。构建：指定 destination `iPhone 16 Pro` 本机不可用 → 改 `iPhone 17 Pro` **BUILD SUCCEEDED**（`build/w10-1-logs/xcodebuild-w1.log` L1767）。截图：UITest 全页导航不稳定，改 ImageRenderer 落盘三处同尺寸接线镜像 + 缺图占位（`build/w10-1-screenshots/01–04`）；渲染测 `W10_1_BakedThumbnailRenderTests` 4/0。未改动作库/v8/v9 分享路径。**主控验收通过（2026-07-17，0 轮返工）**：四文件 diff 亲读（纯数据源替换、布局未动）、`miniTableThumbnail` rg 全仓 0 命中、build 日志 L1767 SUCCEEDED + 主控亲跑主树构建 SUCCEEDED、渲染测 4/0、截图 01–04 逐张目视核验（三处不同 drill 不同图、缺图占位非空白）。留档偏差：destination iPhone 17 Pro（本机无 16 Pro）；截图为 ImageRenderer 同尺寸接线镜像（UITest 全页导航不稳定），真实页面效果留待用户日常使用确认。真源升 v10.1，v10 收官。**下一步：可开始 `问题集合_v8.md`（练习 Tab，X1–X6）。**
- **问题集合 v9 批次 W1 返工第 1 轮 ✅（2026-07-17，同一执行智能体 resume，主控落盘产物亲验）——真根因＝权限文案未进包**：首轮收官后用户真机复测仍秒闪退。主控实测定位：主 target 显式 `INFOPLIST_FILE`（`GENERATE_INFOPLIST_FILE=NO`），pbxproj/project.yml 的 `INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription` **全部被忽略**，构建产物 Info.plist 无该键 → 首次权限弹框被 TCC 强杀（首轮模拟器 `simctl privacy grant` 预授权限掩盖了此路径，v9 §二「权限文案已配置」为误判）。修复：相册添加/相册读取/相机三键直写 `QiuJi/Resources/Info.plist` + `project.yml` info.properties（并留 INFOPLIST_KEY 不生效警告注释防回退）。**主控亲验产物**（`build/w1r1-logs/`）：PlistBuddy 证 iphoneos+sim 两产物三键在包；`PhotoPermissionInfoPlistTests` 2/0 钉死；未预授权限真实弹框冒烟 `TEST SUCCEEDED`（截图 `w1r1-screenshots/w1-07-permission-alert.png` 可见系统授权框 + 文案「将训练分享卡保存到相册」，允许后 toast）；真机 App 已成功安装（`device-install.log`），自动冒烟被用户中止（驱动个人手机）。**用户真机人工点验通过（2026-07-17）：闪退消失，B1 关闭，v9 收官**。真源升 v9.2。**下一步：可开始 `问题集合_v8.md`（练习 Tab，X1–X6）或 `问题集合_v10.md`（drill 缩略图统一）。**
- **问题集合 v9 批次 W1 首轮 ✅（2026-07-17，执行智能体 grok-4.5）**：B1 训练分享「保存相册」。根因假设（无界 Spacer→失控位图/卡死）在模拟器 XCTest **未证实**（width-only ~5ms / 895px vs bounded 960px，证据 `build/w1-evidence/`）；仍落地 `ShareCardImageRenderer` 钉死 Preview 对齐契约 361×480、预览/保存共用、`Task.yield` 后渲染；`ShareCardImageRendererTests` 3/0 + UI 冒烟 `W1_ShareSavePhotosUITests` 1/0（截图可见 toast「已保存到相册」）。真机在线但 install 失败 → **真机完整路径未验**。真源升 v9.1。产物 `build/w1-logs/`、`build/w1-screenshots/`、`build/w1-evidence/`。与 v8 练习 Tab 无关。**下一步：可继续 v8 批次或用户真机冒烟确认原卡死是否消失。**
- **问题集合 v9 立项 ✅（2026-07-17，Orchestrator，纯文档 → W1 已执行）**：真源《问题集合_v9.md》B1 + 单批 W1；与 raw `问题集合_v8.md` 划界。
- **问题集合 v7 收官 ✅（2026-07-16，主控亲做）——W1–W8 + W9a/W9b 全部批次完成（总返工 2 轮：W1 命名撞名 / W9a D15 死控件）**：真源《问题集合_v7.md》升 v7.8（状态改「全部批次 ✅ 收官」）；人工冒烟清单落 `docs/ui-polish/32-v7收官人工冒烟清单.md`（触觉分档/Dark chrome/轨迹三档/Composer 开球全流程/八页菜单/Freemium 限次 + 既有问题留档）；Hub 状态卡已回写。**W1 留档 5 条既有 UI 失败收官复核（主控亲跑，产物 `build/wrap-logs/`）**：DrillTryout×2 直接转绿（环境漂移已被 W9b `launchClean` onboarding 注入根治，非代码问题）；其余 3 条为**测试期望过时**（页面此前改名未同步用例）——`testSegmentCards`「进球点对照表」→「瞄准点对照表」+「走位编排台」→「自由走位」、`testNavigateToFreePlay` 自由击球拆页后标题应为「自由击球」（原断言编排台标题）、`testV02_CategorySidebarVisible` 侧栏标识应为 `sidebar_准度训练` 等 nameZh 全称——修正断言后 5/5 全绿（`legacy5-recheck` → `legacy3-fixed` → `legacy-segcards-fixed` 三轮 xcresult 留档），**确认非 App 回归**。**下一步：用户执行 `32-v7收官人工冒烟清单.md`。**
- **问题集合 v7 批次 W9b（菜单/按钮/开球收尾 C29–C33）✅（2026-07-16，plan-delegated-execution：grok-4.5 执行子智能体 + 主控落盘产物亲验，0 轮返工）——v7 全部批次收官（W1–W9b）**：**C29/G25**——`BTSolverMoreMenu` 泛型扩展（`solveRange`/`pageExtras`/`onClearTable`/`onReset`/`onPrinciple`），三模板收敛八页：Bank/Diamond 解球器模板保持，Silu/PlanThree/Snooker 迁反解模板（求解范围 Section + 清空桌面 + 恢复默认），Composer/FreePlay/ShotSim 迁自由击打模板；「重置默认球形」清零、「恢复默认」统一（Composer「清空并重来」页特有豁免留档）；AngleDynamic 一并迁入（仅显示·网格）。**C31**——AimPointScene 补三点网格入口；AimPointTraining/Geometric 无可配台面项注释留档；Geometric 重置统计独立 trailing 并存策略写入 SPEC §8.9g。**C30**——Geometric/AimPointTraining 主 CTA → `BTTextActionButton`；`NumericKeypadHUD` 契约统一为 **ZStack 底浮层**（与 SceneAiming 同构，safeAreaInset 会压缩画布故弃用，依据留档）。**C32/D12**——Composer `BTBreakSideButton`+`BreakGamePickerSheet` 真接线成为第四可达开球宿主（D14 口径：可点非禁用）；`BreakEntryTile` 死代码删除（grep 亲测 0 引用）。**C33**——Bank/Diamond 文件头、SceneAiming「齿轮」注释修正。**主控验收（落盘产物亲验）**：`build/w9b-logs/` build SUCCEEDED + `QiuJiTests.xcresult` 575/0/2 + `W9b_MenuOnly` 2/0 + 回归 S2+W9a+Tour 10/0（`UITests-rerun.xcresult`）；18 张截图逐张核验（八页菜单展开态符合 G25 模板、Silu 求解范围 Section、ShotSim「恢复默认」、Composer 开球四帧全流程、Geometric 键盘不遮台面）；Composer 开球接线 diff 亲读；「重置默认球形」grep 0 命中。**留档**：BatchAuthoring 私有 Menu（向导页）本批范围外；SPEC §8.9c/f/g + 真源 v7.7 已回写。**收官复核**：W1 留档的 5 条既有 UI 失败（DrillTryout×2/P3×1/P5×2）复跑结果见下一条目。
- **问题集合 v7 批次 W9a（控件布局/文案 C25–C28）✅（2026-07-16，plan-delegated-execution：grok-4.5 执行子智能体 + 主控落盘产物亲验，**返工 1 轮**）**：**C25/G24**——`BTStrikeTitle` 文案字典（击球/击打/打一〔D13〕/击球中/演示中），`strikeTitle` 全消费点扫齐走字典。**C26**——`BTSolverLeftColumn`（求解/下一解竖叠）+ `BTSlotL1Button` 统一外形，Silu/PlanThree/Snooker 接入；SolverStageChrome 角钮统一 48×46、动作列定位 `bottomTrailingFrame`→`actionColumnFrame()`。**C27/D14**——ShotSim/Composer/Snooker/BatchAuthoring 禁用 `BTBreakSideButton` 占位清零（Silu/PlanThree/FreePlay 可达宿主的可点开球保留）。**C28/D15**——SolverStageChrome 补 `BTTrajectoryDetailChip`。**返工 1 轮（主控判定）**：首版 chip 仅文案可切换、`drawSolution` 不读 `TrajectoryDetail`（死控件，违背 D15「与六页对齐」）→ 放开最小 VM 绘制改动：Bank/Diamond `drawSolution` 三档门控线语言（全=现状全部/双=主线+去向隐释义层/瞄准线=仅首段），默认 `.full` 分支与改前逐行等价；UI 断言升级为台面裁剪区 128×128 灰度像素 diff（阈值以实测留档）。**主控验收（落盘产物亲验）**：`QiuJiTests-rework.xcresult` 575/0/2；`W9a_UITests-rework2` 2/0（像素 diff 断言）；`ScreenshotTour_BankDiamond-rework` 2/0（默认档不回退）；三档截图肉眼核验（full 金点/法线/黑进球线/标注 vs minimal 仅白瞄准线）；`strikeTitle` 消费点全表 grep 亲测均在字典内。**下一批：W9b 菜单/按钮/开球收尾（C29–C33），完成后整轮收官。**
- **问题集合 v7 批次 W3（动效与设计 token 收编）✅（2026-07-16，plan-delegated-execution：grok-4.5 执行子智能体 + 主控落盘产物亲验，0 轮返工）**：**C19/G22**——`BTMotion` 新登记 `springLayout`(0.35/0.75)/`easeInOutFast`(0.2)/`easeInOutChrome`(0.25)/`easeInstant`(easeOut 0.12)/`easePress`(0.1)，全部同值替换（DR-023）；`springPanel`/`easeFast`/`easeChrome` 消费点扫齐；5 处低频独特动画留档豁免。**C20**——`AngleCoverPalette` 收编首页 40 处 `Color(red:)` 封面色（主控用 git show 比对迁移前后色值集合逐值一致）+ 水印 56 → `btCoverWatermark`。**C21**——HUD 分隔线统一 12（D5，`BTHudMetricSeparator`）；`BTDailyLimitGate` 字号 token 化；D6 红线「新代码禁止新增字面量字号」写入 SPEC §1.4。**主控验收（落盘产物亲验）**：`qiujitests-full.xcresult` 575/0/2 全绿（首跑 2 条 PocketBehaviorDiag SIGKILL 属模拟器资源争抢，专用机复跑绿）；grep 亲测已登记字面量在 `QiuJi/` 内清零（仅 BTMotion 定义处）；首页 Light/Dark 截图核验封面色不回退。**存疑留档**：simctl 置 dark 后 XCUITest 截图像素仍近似 Light（封面色明暗同值属 C20 有意契约，页面 chrome 的 Dark 呈现列入收官人工冒烟清单人工确认）。**提交边界**：工作区混有另一会话（动作库 B1–B5b）未提交产物，主控先代提交 B4 内容集（含 PD-026 渲染修复）再提交 W3，两批解耦。**下一批：W9a 控件布局/文案（C25–C28）。**
- **动作库球形序列专业化 B1–B5b 全批次收官 ✅（2026-07-16，plan-delegated-execution：5 个执行子智能体 + 主控独立验收，0 轮返工）**：真源 `docs/research/20260716-动作库球形序列专业化方案-v1.md`（已升 v2）。B1 权威球形基准库（BU/PAT/Dr. Dave/新锐/CBSA 坐标级下钻）→ B2 变量覆盖方法论（六要素模板 + 中袋角度 8 球形，`scripts/b2_variable_coverage_midpocket.py`）→ B3 承载 ADR-P12-03（候选 A + sidecar `content/drill_profiles/`）→ B4 c053 替换落地（profile + baked A1 feasible=✅ + 8 条 initial-only 序列 + formations 精讲框架 + 缩略图；验证 20/20、主控亲跑 build 绿；两个既有渲染 bug 修复 PD-026；示范录制挂 **H-21 ⏳ 非阻塞**）→ B5a/B5b 全库 72 条审计（`scripts/b5_audit_drills.py`：保留 11 / 重构 59〔P1×34〕/ 降级 2、8 组坐标重复、缺失 M1–M5、重构批次表 **R1–R11** ≈230 球形/序列段，见审计文档 §十）。**下一步：新会话按 R1（accuracy 切角档重建，c053 为范式）开始批量重构；H-21 可并行。**
- **问题集合 v7 批次 W4（球库组件统一 BTBallPaletteBar）✅（2026-07-16，执行智能体〔grok-4.5〕，主仓库串行）**：**G21**——新建 `Core/Components/BTBallPaletteBar.swift`（`BTBallPaletteMetrics` D7 两档球径 30/36 默认 36、死区统一 10、ghost 描边 success 2.5/idle 1；`BTBallPaletteBar` 交互库 / `BTBallPaletteToken` 单槽 / `BTBallPaletteDragGhost` / `BTDecorativeBallPalette` 只读 / `BTReferenceBallPalette` FreePlay 参考 / `BTBallPaletteDragBack` 拖回删球 hit-test）。**接入 9 页**：Composer / Silu / PlanThree / Snooker / ShotSim / SolverStageChrome（Bank+Diamond）/ BatchAuthoring 走 `BTBallPaletteBar`；Extraction + BatchExtract 保留侧栏 `paletteTwoRows` 壳、token/ghost/拖回走组件（死区 12→10）；FreePlay → Reference（34→36）。**C14**——SceneAiming / AimPointScene 装饰库 → `BTDecorativeBallPalette`（34→36）。**豁免留档**：AngleDynamic 保留 Button+目标描边私有布局（无 drag/ghost），球径改走 metrics。**验证（真实输出）**：`make build` SUCCEEDED；`QiuJiTests` **577 tests / 0 failures / 2 skipped**（`build/w4-logs/qiujitests.xcresult`）；UI：W4_BallPalette 3/3（Composer 拖放+pulse+拖回、Snooker、Extraction 经 `-extract.confirmDemo` 钩子）+ S2 6/6 + S1 1/1 + ScreenshotTour Bank/Diamond 2/2（前段模拟器 signal-kill 风暴 + 新建模拟器 onboarding 首启，最终专用模拟器 + `hasCompletedOnboarding` 注入后全绿；产物 `build/w4-logs/*-final.xcresult`、截图 `build/w4-screenshots/`）；grep 复核私有 `paletteDrag`/`dragGhost` 清零、球径字面量仅组件两档定义。红线执行：`docs/ui-polish`、`DrillThumbnails` 每轮测试后 `git checkout` 恢复。真源 W4 段已回写；SKILL.md 组件 API + Changelog 已登记。**主控验收注记**：执行子智能体在收尾汇报前因上下文耗尽（resource_exhausted）中断，但工作区改动、真源/SKILL 回写与落盘产物均已完成；主控按落盘产物亲验（`qiujitests.xcresult` 575+/0）+ 逐文件 diff 复核 + grep 复核，并对子智能体产物中 signal-kill 的 5 条 UI 失败在专用克隆模拟器 `W4-Verify` 全量复测：S1 1/1、S2 6/6、W4_BallPalette 3/3、ScreenshotTour Bank+Reflection 2/2 全绿，判定失败为模拟器资源争抢环境噪声而非代码回归；提交时剔除同一工作区内其他会话的动作库内容改动（drill_c053 系列，非本批范围）。
- **问题集合 v7 波4：W7（Freemium/haptic）∥ W8（VM 绘制去重）✅（2026-07-16，plan-parallel-delegated-execution：grok-4.5 ×2 并行 worktree + 主控落盘产物亲验 + 串行合并逐批 build，0 轮返工）**：**W7/C22**——新增 `BTFeedback`（度误差 ≤3/≤10、mm 误差 ≤2/≤6 → success/warning/error），四测验 submit 全接线；**W7/C23**——`AngleUsageLimiter.shared` 收口四处独立实例（测试保留 `init(defaults:)` 注入），SceneAiming 补 full 主卡、AimPointScene 全屏遮罩改 scrim+full/compact，「剩」→「剩余」（D9）；**W7/C24**——`PracticeStorageKey` 收口练习域 key（字符串原值冻结）；D10 不做。**W8/C3**——抽 `TrajectoryRenderer`（`.positionPlay`/`.snookerDefense` options，D2 显式参数化）；**W8/C17**——`TableBallPulse`（6 处收编；Bank/Diamond 1.35→1.7 有意对齐已留档）+`SceneStroke`+`SolveConstraintDrawingOverlay`+`BTScenePalette.constraintCyan`；D11 删 `makeExportSequence`；`SiluSpinLabel`→`ShotSpinLabel`。**主控验收（落盘产物亲验，未复跑测试）**：W7 `qiujitests.xcresult` 569/0/2 + W7 UI 3/0 + S3/S5 10/0 + 满额 8 张截图逐张核验（四页 full/compact 形态一致、文案「剩余」）；W8 `full-qiujitests.xcresult` 569/0/2 + focused 35/0 + 轨迹截图核验（Snooker 确无 objectPath、Silu/PlanThree 含约束青环）+ 首轮 Composer UI 失败与复跑通过的 xcresult 前后一致（如实汇报）。合并：业务代码零冲突；`project.pbxproj`/schemes 冲突以 `make xcodegen` 重生成解决；逐批合并后主树 build SUCCEEDED ×2。**过程纠偏**：主树 DrillThumbnails 2 张被污染，`git checkout` 恢复。**下一波：波5 W4 球库组件（串行，最大批）→ 波6 W3 动效 token → 波7/8 W9a/W9b。**
- **问题集合 v7 波3：W5（Bank/Diamond 壳合并）∥ W6（场景几何对齐）✅（2026-07-16，plan-parallel-delegated-execution：grok-4.5 ×2 并行 worktree + 主控串行合并逐批 build + 落盘产物亲验〔本波起测试不复跑，改为亲读子智能体落盘 xcresult/日志/截图〕，0 轮返工）**：**W6/C12**——FreePlay 五处手写贴边 frame/position → `.btStageFrame`/`.btChipBandPlacement`（与 ShotSim 同修饰器）。**W6/C15**——`AngleTrainingScene` 新增 `makeAimPointMarkerNode`+`addAimPointMarker`（0.0065 球，D8），AimPointScene 私有 `addDotNode`（0.007 圆柱片）删除。**W6/C16**——`AimPointTrainingViewModel` 改调 `AimPointGeometry.signedOffset/offsetDistance`（几何草稿：自写公式 vs 真源多样例 Δ≤7.1e-15 mm），`ballRadiusMM` 派生真源，杆速收 `ShotTuning.aimPointVerifyVelocity`，3D 浮动轮走 `ShotStageMetrics`。**W5/C18**——抽 `SolverStageChrome<VM: SolverStageHosting>` + `PrincipleInfoSheet(title:blocks:)`，Bank/Diamond 两页瘦身为配置注入（净删 ~776 行，VM 未改），ADR-P9-04 落 P9。**主控验收（落盘产物亲验）**：W5 `w5-qiujitests.xcresult` 563/0 失败/2 跳过 + `ScreenshotTourUITests` 2/2 + 求解/自由/网格/菜单截图 vs 基线；W6 两个 xcresult 合计 UI 12/0（S3_AimPoint/S5_AimPointSceneV5/S5_TrainingPages/S1_FreePlay/S2_ShotPages）+ `W6-GEOMETRY-DRAFT.md` 数值草稿 + 2D/3D 实操截图；两批文件零交集（git diff 实证）、合并无冲突、逐批合并后主树 build SUCCEEDED；主控主树补跑 `AimPointGeometryTests` 7/0（存疑点：落盘产物未含该 suite 单独记录）。**过程纠偏**：子智能体再次污染 `docs/ui-polish/` 基线 + 3 张缩略图，主控 `git checkout` 恢复。**下一波：波4 并行 ×2（W7 Freemium/haptic ∥ W8 VM 绘制去重，worktree 隔离）。**
- **问题集合 v7 批次 W2（导航 chrome 与页面骨架收口）✅（2026-07-16，plan-delegated-execution：grok-4.5 执行子智能体 + 主控独立验收，0 轮返工）**：**C7**——五暗色测验页（SceneAiming/Geometric/AngleDynamic/AimPointTraining/AimPointScene）补 `toolbarColorScheme(.dark)`+`toolbarBackground(.black)`+principal 品牌绿 `BTSolverNavStatus`。**C8**——七页私有 `navStatus` 清零收编 `BTSolverNavStatus`（DR-022：`statusText` 改 `String?`，nil 时仅标题）；BallExtraction principal 补「第 N 步」副行。**C9**——九套私有 `*FramePreference` → 共享 `BTShotPageFramePreference`（`SolverFramePreference` 留 typealias）。**C10**——五份手写 flash 体收敛 `BTToast.present`（页内一行 facade 保留；TrainingShare 内联属练习 tab 外另案）。**C11**——`ShotStageMetrics.topRowHeight=46` + `BottomBarHeight` 枚举三档（78/94/116）替换全部魔法数（BatchAuthoring 72/101 不在三档内未强改，如实留档）。**主控独立验收（真实输出）**：逐文件读全量 diff（共享件 + 12 页面）；亲跑 `make build` BUILD SUCCEEDED；S2(6)+W2_DarkQuizNav(1) **Executed 7 tests, 0 failures**；grep 四项实测清零（navStatus=0 / FramePreference 仅 1 / flash 体清 / 魔法高度仅定义处）；暗色页截图逐张核验（黑底导航+品牌绿标题）。**过程纠偏**：子智能体覆盖 `docs/ui-polish/screenshots-w2-1/` 基线 6 张 + drill_c050 缩略，主控 `git checkout` 恢复。SPEC §8.3（G20）+ Changelog + DR-022 已回写。**下一波：波3 并行 ×2（W5 Bank/Diamond 壳合并 ∥ W6 场景几何对齐，worktree 隔离）。**
- **问题集合 v7 批次 W1（正确性与数据收敛）✅（2026-07-16，plan-delegated-execution：grok-4.5 执行子智能体〔用户指定全程 grok〕+ 主控独立验收，返工 1 轮）**：**C1**——「3/4 球」收敛到经典球厚度真源 `AngleSceneCalculator.NamedBallThickness`（name↔θ↔overlap↔d/R 四元组，`threeQuarterBall`=14.5°/0.75/0.50，数值草稿 `build/w1-c1-geometry-draft.txt` 确认 D1；旧 48.6° 实为 1/4 球），对照表/球感/瞄准原理/HUD `thicknessName` 四处全接真源。**C2**——历史筛选补 aimPoint/2D/3D 三档（截图取证）。**C4**——Extraction y∈[0.12,0.40]。**C5**——`BTShotActionColumn` 增 `undoTitle` 参数，Composer/FreePlay/ShotSim 三页中钮改「重打」（行为不变）。**C6**——头注释 1.5 秒。**返工 1 轮**：首版 `thicknessName` 留旧偏移口径昵称（7.5°「≈1/4 球」/10°「≈1/3 球」）与经典 48.6°「1/4 球」撞名 → 删偏移昵称 + 正弦标记改真源派生 + 钉子单测。**主控独立验收（真实输出）**：逐文件读全量业务 diff；亲跑 `make build` BUILD SUCCEEDED ×2；BallThicknessNamingTests(4)+AngleQuizTypeFilterTests(1)+AngleTrainingTests(39) 0 failures；grep 复核 48.6/≈1/4/≈1/3 代码零残留。**过程纠偏**：子智能体覆盖 `docs/ui-polish/` 截图基线 + 46 张多余截图，主控 `git checkout`/`git clean` 恢复（返工令已加禁令）。**遗留留档**：全量 `make test` UI 有 5 条既有失败（DrillTryout×2/P3×1/P5×2，与 W1 无关，v5 收官时全绿 → 疑环境漂移，收官波复核）；历史统计瞄准点「平均误差」单位显示 °（应 mm，既有问题未入本轮范围）。**下一批：W2 导航 chrome（波2 串行）。**
- **UI 打磨第 4 波（W2-9 状态呈现/弹层/空态横切，串行收口）完成 ✅（2026-07-14，plan-delegated-execution：grok-4.5 执行子智能体〔用户指定，替代真源原定 opus〕+ 主控独立验收，0 轮返工）——W2-1 ~ W2-10 十批全部收官**：
  - **新共享真源**：`Core/Components/BTToast.swift`（`BTToastTone` success/info/warning/error + `btToast()` modifier，顶栏胶囊 1.6s；收编 FreePlay/PlanThree/Silu/Composer/Snooker/ShotSimulation/BatchAuthoring/BallExtraction×2 共 9 处手写 flash/banner；FreePlay 规则限制提示由错色绿改金 warning；AngleDynamic 底栏常驻教学态特例注释留档）；`Core/Components/BTDailyLimitGate.swift`（角度 4 页日限手写卡三套→一套，皇冠+CTA 统一「解锁 Pro」，含 `BTProBadge` 统一网格/计划卡/列表行/历史行 PRO 角标）。
  - **ST 组**：F-ST-02 训练首页/计划列表用 `BTDrillListSkeleton`、收藏页行骨架；F-ST-03 `BTMotion.easeFast`+`.transition(.opacity)` 全列表接通（含 DrillList 已声明未驱动的过渡）；F-ST-05 `BTEmptyState` 加 `actionStyle`，「创建计划」收进组件；F-ST-06 空态「还没有/暂无」口径拉齐；F-ST-08 `BTPremiumLock.fullMask` title/subtitle 参数化 + `premiumGate` 强制传文案 + progressive/fullMask CTA 统一实心金。
  - **OV 组**：F-OV-01 detent 三档收编（短选择 280/360+把手、设置 medium+large+把手、说明 medium+large；Paywall/登录保持大 sheet）；F-OV-02 清除缓存/注销/删除计划改 confirmationDialog（取消在前；「结束训练」保留 alert）；F-OV-04 「已保存到相册」改非阻断 toast；F-OV-05 休息/场景总结覆层浅 scrim 0.32+opacity；F-OV-07 单按钮 dismiss 全库「确定」（rg「好的」清零）。
  - **PF/CL/TU**：F-PF-03/04 Paywall CTA/定价卡/登录三钮补 `BTPressableStyle`（保 plain 语义与 Apple 反色）；F-PF-06 恢复购买 0.2→0.45；F-PF-11 Onboarding「角度感知训练」→「角度训练」；F-CL-03 骨架 4/3；F-CL-05 死参数接线（**锚点勘误**：在 `BTExerciseRow` 非 BTShareCard，改 `drillId` 接 `BTBakedDrillTable` 烘焙缩略）；F-CL-06 `BTLogoMark` deprecated 归档；F-TU-05 拍板落地：push/sheet 双形态保留，sheet chrome 统一（xmark 关闭钮+medium/large+把手）。
  - **F-CL-04 有意保守（如实留档）**：ActiveTraining 休息覆层保留 W2-6 双环/结束态/numericText 成果，`BTRestTimer` 能力不足不硬换，代码注释说明。
  - **主控独立验收（真实输出）**：逐文件读全量 diff 2148 行（快速模型纪律）；亲跑 `make build` **BUILD SUCCEEDED**；S2(6)+S5(5) 合跑 **Executed 11 tests, 0 failures, TEST SUCCEEDED**；rg 复查三项清零；截图 7 张逐张核验（`docs/ui-polish/screenshots-w2-9/`；paywall-press 实抓 loading 帧，按压 scale 由 BTPressableStyle 代码契约保证，留档）。
  - **收官**：真源 `30-真改清单.md` 升 v6（十批全 ✅）；**人工冒烟清单已汇总 → `docs/ui-polish/31-收官人工冒烟清单.md`（按波列交互点，用户一次性过完，发现问题按批归属返工）。下一步：用户执行人工冒烟。**
- **UI 打磨第 3 波（W2-6 + W2-7 + W2-8 并行 ×3）全部完成 ✅（2026-07-14，plan-parallel-delegated-execution：grok-4.5 执行子智能体 ×3 · worktree 隔离 + 主控独立验收，0 轮返工）**：
  - **交集实证与批次调整（真源 v5 已记）**：① F-TU-11（`DrillRecordView` 精讲入口失败提示）与 W2-6 记分条目同文件 → 由 W2-8 移交 W2-6；② v1 批次表漏排 21-组G（构建器 F-PL-07/08/12/14）→ 主控裁定归 W2-7（`CustomPlanBuilderView`，与 W2-6 无交集）；③ 组D 余项（F-TR-13/14、F-PL-02/05/09/15）按来源组归 W2-6。调整后三批文件集合两两不相交。
  - **W2-6 会话与记分**（23 条）：F-TR-03 休息覆层 token+BTButtonStyle、F-AT-07 僵尸态 800→250ms+0:00 禁 +30S（VM guard）、F-AT-05 结束钮 stopCircle+次级色（品牌绿=主操作语义纠正）、F-AT-06/12 底栏五钮 BTPressableStyle+BTIcon、F-AT-02/11 记分完成 easeFast 动画+轻触觉、F-AT-03/09 chevron 旋转+numericText、F-AT-08 DrillPicker 行内勾+tint（List 内守 .plain 命中）、F-AT-10 顶栏弱页码 pill、F-AT-04 拍板落地：双形态保留+收缩交接（浮标共用「继续训练」+play+计时，含自主修复：自由记录最小化后首页底栏也露继续条）、F-TR-05 排队文案去伪可点、F-TR-13/14 自定义卡对齐 PlanListView（72pt）、F-PL-02 露 planNameZh/weekTheme、F-PL-09「自定义模版」→「我的计划」、F-TU-11 精讲 nil→alert。
  - **W2-7 收尾/总结/心得**（20 条）：F-TS-02 保存「保存中…→已保存 toast→0.7s dismiss」（onSave 改返 Bool，ActiveTrainingView 4 行接线）、F-TS-01 总结仪式 springPanel 成功率条生长+numericText、F-TS-05 心得改真 ScrollView+键盘「完成」、F-TS-11 <60s「不足 1 分钟」（总结+分享卡）、F-TS-03/09 主题 swatch 用 accentColor+「基础绿」→「炭灰」（rawValue 未持久化，主控核实）、F-TS-04 清死 API hideBallTable、F-TS-07/08 分享圆按压+去双返回、F-TS-06 历史详情露 overallRate+逐 drill %、F-PL-04/06/10/11/13 计划详情（padding 60→safeArea、chevron 旋转、行头按压）、组G F-PL-07/08/12/14 构建器（按压、增删 springPanel、BTIcon、BTEmptyState）。F-PL-13 半条留档：首页底栏 xxl 在 TrainingHomeView（W2-6 文件）未对齐。
  - **W2-8 详情转化与精讲**（19 条）：F-DD-01 露 description、F-DD-02 伪百分数→「重点/中等/辅助」+「示意倾向非测评数据」脚注（五条保留）、F-DD-03 空视频单行紧凑空态、F-DD-04/09 锁态 includeTutorialCTA:false+紧凑预览（isPremium 判定 diff 为零，主控核实）、F-DL-01/02 拍板落地：整行移除「要点/历史/图表」伪入口+假备注卡、底栏「上手试打」单钮全宽（原「关闭+加入训练」双钮撤下）、F-DD-06/07/08 缩略按压+收藏 symbolEffect+token、F-DL-03 试打钮补 BTPressableStyle（回放钮 W2-1 已覆盖，如实记录）、F-DL-05 formation 切换 easeFast、F-DL-06 Brief 卡 token、F-TU-04 静态图弱放大角标、F-TU-06「第N杆」btTitle2+序号徽章（含中文数字解析）、F-TU-07 保 n/N 关页点、F-TU-08 字号收编。
  - **主控独立验收（真实输出）**：三批逐文件读 diff（快速模型纪律）；两树 scheme 漂移（xcodegen 重生成「球迹.app→QiuJi.app」等）主控 `git checkout` 还原后才提交；串行合并 W2-8→W2-7→W2-6 每批合并后 `make build` **BUILD SUCCEEDED** ×3（W2-6 合并 pbxproj UITest 注册冲突，主控手解、两侧保留）；主树 S2(6)+S5(5) 合跑 **TEST SUCCEEDED**（iPhone 17 Pro）；截图核验详情页（无伪入口/假备注/幽灵图，单钮全宽）、休息覆层、计划详情、精讲阅读。worktree ×3 与分支已清理。
  - **规划偏差留档（无冲突，不记 FL）**：W2-7 为 F-TS-02 闭环在 `ActiveTrainingView` 做 4 行 onSave 接线（与 W2-6 同文件，auto-merge 无冲突、语义正交）；W2-6 因条目落点改 `BTSetInputGrid`/`BTFloatingIndicator` 共享组件（主控逐行核：仅按压/动画/文案，无 tint/阴影违规）。
  - **留给收官人工冒烟**：保存 toast→dismiss 时序、记分完成触觉/动画手感、最小化收缩交接与他 Tab 浮标态、休息 0:00 禁 +30S、付费锁态详情（子智能体截图未取得）、空视频 drill 紧凑空态、「第N杆」徽章修复后帧、详情底栏撤「关闭」后的返回路径。**下一波：第 4 波 W2-9 状态呈现/弹层/空态横切（串行收口，opus 或主控亲做）。**
- **UI 打磨第 2 波（W2-2 + W2-3 + W2-10 并行 ×3）全部完成 ✅（2026-07-14，plan-parallel-delegated-execution：grok-4.5 执行子智能体 ×3 · worktree 隔离 + 主控独立验收，共返工 1 轮）**：
  - **交集实证与移交**：开工前按裁决文档核对三批文件集合，发现 W2-2 的 F-HI-03 + F-ST-01 统计半条与 W2-10 撞 `HistoryCalendarView`/`StatisticsView`/`HistoryViewModel` → 移交 W2-10 执行（条目语义零变化），调整后三批两两不相交（真源 v4 已记）。
  - **W2-2 诚实反馈**（10.5 条，0 轮返工）：F-PF-01 购买失败 alert 上屏、F-PF-02 登录中 Progress、F-PF-05 注销 overlay+防连点（错误信号与 Auth 解耦为本地 state）、F-PF-07 恢复购买 loading、F-TR-01 相册保存改 `ImageRenderer`+`PHPhotoLibrary` 真实回调（补 `NSPhotoLibraryAddUsageDescription`）、F-TR-02 隐藏死开关（主控核实 `hideNote`/`hideBallTable` 确未消费）、F-PL-01 `@Query UserActivePlan`+onChange 首页刷新、F-PL-03 列表「已激活」徽章、F-TR-04 激活覆盖确认、F-AT-01 记分 `scrollDismissesKeyboard`、F-ST-01(Auth) ProfileView 同步失败 alert。
  - **W2-3 精讲媒体**（4 条，0 轮返工）：F-TU-01 缺图弱占位「配图暂缺」（caption 仍显示）、F-TU-02 `ZoomableContainer` onChanged 走 `Transaction.disablesAnimations` 1:1 跟手（松手/双击保留 interactiveSpring）、F-TU-03 新 `TutorialMediaPage` 让 caption 与媒体共享下滑 offset/opacity、F-TU-10 Viewer 进场 springPanel opacity+scale。
  - **W2-10 场景杂项**（~35 条，**返工 1 轮**）：F-AK-01~06/12（场景胶囊 press、结果卡过渡、球库 Button 化、重置统计 confirmationDialog）、F-SA-01~06（striking pill、去 userPhi 跟手动画、解锁钮场景胶囊化）、F-PP-04/05/07/10（选图 loading、banner 语义分 tone、SpinPad 贴底、拖回库 flash）、F-SC-01~04/06/07/09（回放锁定反馈、powerGradient 同源、预览失败空态、`btTableFelt` 占位/letterbox、ghost 转发 BTGhostCircle）、F-BD-01~07（覆盖确认最低方案、banner 分 tone、求解双轨统一、springPanel 收编）+ Batch 胶囊按压（W2-1 遗留）、F-HI-02/03/04/05/06/08/10 + F-ST-01(统计)（行按压、错误态上屏+重试、ZStack 保活防假闪、锁定行 `btTextTertiary`、环比跌 `btWarning`、「均值线」、详情去重复标题）。**返工点**：① F-SC-01 曾把回放中图标换 `stop.fill` 但按钮 disabled（新假 affordance）→ 改回 play.fill+降透明；② F-HI-05 注释与代码不符 → 修正。均主控实测确认。
  - **主控独立验收（真实输出）**：三批逐文件读 diff（快速模型纪律，共 ~2200 行）；串行合并 W2-3→W2-2→W2-10 每批合并后 `make build` **BUILD SUCCEEDED** ×3；合并完成后主树 `S2_ShotPagesLayoutUITests` **6/6 passed, TEST SUCCEEDED**（iPhone 17 Pro）。worktree ×3 与分支已清理。
  - **留给收官人工冒烟**：购买失败/相册写入/注销/同步失败等运行时态（模拟器难复现）；精讲 Viewer 捏合/下滑 1:1 手感与 caption 跟手中帧；批量台覆盖确认真实点击。**下一波：第 3 波并行 ×3（W2-6 会话记分 + W2-7 收尾总结 + W2-8 详情精讲阅读），开工前须实证三批文件集合不相交（注意 ActiveTrainingView/TrainingSummaryView/DrillDetailView 归属）。**
- **UI 打磨第 1 波（W2-1→W2-4→W2-5 串行地基）全部完成 ✅（2026-07-14，plan-delegated-execution：grok-4.5 执行子智能体 ×3 + 主控独立验收，全程 0 轮返工）**：三个横切真源落地——`BTPressableStyle`（按压）、`BTMotion`（动效）、`HUDStyle.panelBackground` 等 token（见下三条）。主控每批逐文件读 diff + 亲跑 build/UI 回归。
- **UI 打磨第 1 波 · W2-5 Token/Dark/HUD 外壳 ✅（2026-07-14，grok-4.5 执行子智能体 + 主控独立验收，0 轮返工）**：**F-GL-03**——`HUDStyle.panelBackground = Color(white: 0.11)` 场景专用常量唯一真源，19 处场景底栏全替换（rg 复查主控实测清零，仅剩定义处；色值逐位不变，未走 `btBG*` 守 ADR-P11-09）。**F-GL-07**——`BTSceneFAB` 渐变末端→`HUDStyle.fabPrimaryEnd`（RGB 逐位不变，无阴影）。**F-CL-01**——`HUDStyle.rulerCornerRadius = BTRadius.md`，AimWheel 10→12 与力度柱统一。**F-CL-02**——`BTDrillCard` 行卡补 Dark `btSeparator` 0.5pt 描边（只描边无阴影）。**F-CL-07**——组件层 SF Symbol 字面量→`BTIcon`。**F-CL-08**——新增 `BTRadius.xxs=4` 文档化（渲染不变）。**F-CL-09**——PRO 徽标→`btMicro`/`btCaption2`。**F-ST-07**——图表色统一 `btChartSeries=btAccent`（Light 弃球色）；Apple 登录反色豁免。**F-TR-07/F-DL-04**——筛选 chip `#1C1C1E`/`#F2F2F7` 收编 `btChipActiveFillLight/Dark`（色值逐位不变，SPEC §7 锁定）。**F-PF-08/09/12**——Login 26pt→`btChapterNumber`、About 系统蓝灰→token、Settings Toggle 补 `.tint(.btPrimary)`。**F-PP-09**——①②③角色色→`btPlanRole1/2/3` 命名常量。**F-HI-07/09**——历史菜单蓝紫→token、日标记 3→`BTRadius.xs`。**F-AK-08~11**——NumericKeypad 字号间距收编、AngleHome chip/CTA token、对照表主读数→`btLargeTitle`/`btStatNumber`、角袋中袋手写分段→`BTSegmentedTab`。**主控独立验收（真实输出）**：逐文件读 diff（快速模型批纪律）；`make build` BUILD SUCCEEDED；S2(6)+S5(5) 合跑 **TEST SUCCEEDED**（主控复跑，首轮即绿）；明暗截图核验 chip 色值/文字无丢色。**过程纠偏**：子智能体曾误覆盖 `screenshots-20260713/` 基线 6 张 PNG，主控 `git checkout` 恢复。**有意视觉差留档（进收官人工冒烟）**：AimWheel 圆角 10→12、日历标记 3→6、keypad 间距 6→4、图表 Light 球色→accent、角袋/中袋改下划线分段并移标题下方。真源 W2-5 行已标 ✅。
- **UI 打磨第 1 波 · W2-4 动效收编横切 ✅（2026-07-14，plan-delegated-execution：grok-4.5 执行子智能体 + 主控独立验收，0 轮返工）**：新增单一真源 `QiuJi/Core/DesignSystem/BTMotion.swift`（`springPanel`=spring 0.34/0.86、`easeFast`=easeOut 0.2、`easeChrome`=easeOut 0.25）；10 条条目全部收编——F-GL-01 试打进场 `easeIn(0.45)`→easeChrome（仅缓动，V10 宿主时序未碰）、F-GL-04/F-PP-06 PlanThree armed 过弹→springPanel、F-GL-06 `spring(duration:)` 族四处（MainTabView 浮标/Subscription/TrainingHome chip/PlanDetail 手风琴）、F-GL-09 BTOverflowMenu popup scale 0.9→0.95、F-GL-10 BTSegmentedTab 0.25→easeFast、F-TR-09/12 会话面板与手风琴、F-PP-03 拍照建球形步骤 `.id(vm.step)`+opacity 过渡（状态机未动）、F-PF-10 训练目标进度环 0.6s→springPanel、F-HI-01 历史选日/换月补 springPanel。全仓 rg 复查 `spring(duration:`/`response: 0.3, dampingFraction: 0.7`/`easeIn(0.45` **清零（主控实测）**。**主控独立验收（真实输出）**：`make build` BUILD SUCCEEDED；`S2_ShotPagesLayoutUITests` 首轮 4 用例 "Test crashed with signal kill"（陈旧增量构建，V11 已知模式）→ `xcodebuild clean` 后复跑 **TEST SUCCEEDED**，非代码回归。真源 W2-4 行已标 ✅。**下一批：W2-5 Token/Dark/HUD 外壳。**
- **UI 打磨第 1 波 · W2-1 组件按压真源 ✅（2026-07-14，plan-delegated-execution：grok-4.5 执行子智能体 + 主控独立验收，0 轮返工）**：新增单一真源 `QiuJi/Core/Components/BTPressableStyle.swift`（ButtonStyle，纯 scale+easeInOut 0.1，无 tint 无背景守 FL-004；两档 `.row` 0.98 / `.capsule` 0.96）；下沉接入 9 个共享组件——`BTTextActionButton`/`BTAimModeToggleButton`（BTShotPageChrome）、`BTChipRow`、`BTButton` text·destructive（既有 style 内补 scale 0.98）、`BTFloatingIndicator` 浮标、`BTExerciseRow`、`BTTrajectoryDetailChip`、`ShotTryFreePlayButton`、`BTDrillPreviewPlayer` 重播钮 + `BallExtractionView` 手写胶囊族。未碰 Break*（V6）/BankShot·Diamond 重置（V9）/Paywall·登录（W2-9），无越界（主控逐文件读 diff 核实）。**主控独立验收（真实输出）**：`make build` BUILD SUCCEEDED；`S2_ShotPagesLayoutUITests` **Executed 6 tests, 0 failures, TEST SUCCEEDED**（主控复跑）；截图核验击打页 4 个改动组件无丢色（`docs/ui-polish/screenshots-w2-1/`）。存疑留档：`BTExerciseRow` 常规页浅色帧未单独取证（代码契约保证无 tint，并入收官人工冒烟）；`BatchBallExtractionView` 平行胶囊未同步（并入 W2-10）。真源 `30-真改清单.md` W2-1 行已标 ✅。**下一批：W2-4 动效收编横切。**
- **问题集合 v5 批次 V11 总验收 ✅（2026-07-13，主控独立实测 + 文档回写执行智能体）**：对照 `问题集合_v5.md` G13–G19 + Q1–Q19 逐条核验，V1–V10 全部落地（V7 返工 1 轮仅 UI 取证层，其余 0 轮）。**全量单测（主控实测）**：全量 `QiuJiTests` **Executed 560 tests, 2 skipped, 0 failures**（903s）。**关键 UI 套件（主控实测）**：S1 / S2(6) / S5(5) / S5-AimPoint(2) / S6 / S7(5) / S8(4) / ScreenshotTour（bank+reflection）/ DrillTryout(6) **全部 passed**。**clean-rebuild 事件（如实记录）**：期间 3 例（S1/S6/S5-3D）失败为**陈旧增量构建产物**导致的 SwiftUI `_ConditionalContent` 析构 SIGSEGV，`xcodebuild clean` 全量重建后全部通过——**非代码回归**。**文档回写**：`tasks/UI-IMPLEMENTATION-SPEC.md` 新增 §8.9「瞄准与求解交互规范（v5 定稿）」（四契约：G13 拖动=选中+相对调整/G14 0.5s idle 去抖/G18 开球通用/G17 上一杆完整快照 + G15/G16/G19/V8 权重登记）+ §8.8 词表增补页面改名 + Changelog（2026-07-13 行）；`tasks/phases/P18-release-convergence.md` 新增 **§B3.8 问题集合 v5（2026-07-13 收官）**（V1–V11 批次表 + V11 结论 + 遗留清单）；`问题集合_v5.md` 状态行改「全部批次 ✅」+ V11 执行结论段 + 版本表 v5.1。**遗留清单指针**（详见 P18 §B3.8）：① 真机冒烟（G13 增益 0.6 手感/V6 开球拖屏/V7·V8 上一杆动画/V10 序列 0.7s 节奏/Q9 3D 瞄准）；② V8 防守评分权重 0.6/0.4 待实测调优（`AngleSceneCalculator` 常量）；③ Q19.2.5 半句待用户补充（进袋模式默认行为）；④ V6 Composer 开球入口是否放开（产品决策）；⑤ UI 测试对模拟器环境隐式依赖（onboarding 标记 + 无 StoreKit Pro 授权，V10 发现）。**问题集合 v5 全批次收官（V1–V11）。**
- **问题集合 v5 批次 V10（动作库与试打 Q19）✅（2026-07-13，执行智能体，0 轮返工）**：**Q19.1 侧栏回顶**——`DrillListView` 用 `ScrollViewReader`+顶部锚点，侧栏点击/重复点击当前分组自增 `scrollToken`（立即滚 + 去抖 0.36s 补滚）回组顶。**Q19.2① 球数不含白球**——`DrillTryoutFormation.objectBallCount`（滤母球），选球形副标题改用它（单测 c042 球形1=6球→5）。**Q19.2② 标题居中**——Composer principal 本就居中，截图复核（初级蛇彩走位/软打控位）无偏差，未改布局。**Q19.2③ i 并入三点**——删 `briefInfoButton`，说明卡召回入 `moreMenu`「试打说明」（G19 口径）。**Q19.2④ 序列模式（默认）**——`DrillTryoutFormation` 新增 `steps` 补数据通路；`PositionPlayViewModel` 加序列模式状态机（`enterSequenceMode/playSequence/runSequenceStep/…`），复用既有 `PositionPlayShotSolver.solve`+`runCueStroke`+`TrajectoryPlayback` 逐杆演示（杆间 0.7s 停顿、收尾 `playback.duration` 遵 G15），隐藏打点盘/力度条/瞄准条、台面只读、单「击打」列、重摆=重演；Composer 加三模式选择器（序列/进袋/自由）+ 当前杆信息条，有 steps 默认序列、无 steps **降级隐藏序列模式并回自由**（自定夺留档）。**Q19.2.5 明确跳过并留档**——「在进袋模式下，默认使用」半句用户未补，不脑补落码，进袋模式保持现状，转 V11 前置待确认。**验证（真实输出）**：`make build` **BUILD SUCCEEDED**；`DrillTryoutBoardStoreTests` **8/8 0 failures**；`DrillTryoutUITests`（Sidebar回顶/C042序列流/序列模式切换/Premium锁/首次手势/暗色入口）**6/6 0 failures**；回归 `S2 testComposerLayout` passed。存疑：`testTryoutPremiumLocked` 曾因**本机模拟器残留 StoreKit Pro 授权**误判（与本批无关），`simctl erase` 清 StoreKit 后通过（erase 副作用重置 onboarding 标记，已 `simctl spawn defaults write … hasCompletedOnboarding YES` 恢复）；序列终帧取证为逐杆推进帧（蛇彩 5 杆超测试等待窗），完成态由 `finishSequencePlayback` 逻辑保证；真机逐杆衔接手感待冒烟。真源 `问题集合_v5.md` V10 段已回写。**下一批：V11 总验收。**
- **问题集合 v5 批次 V9（翻袋/反射页统一 Q17+Q18+G17）✅（2026-07-13，执行智能体，0 轮返工）**：两页（`BankShotView`/`DiamondSystemView` + 两 VM）同构落地九项 + G17；共享件抽入 `BTShotPageChrome.swift`（`BTSolverNavStatus` + `BTSolverMoreMenu`，避免双真源）。**17.1/17.2/17.7**——删球桌左下 overlay pill，解描述/无解说明/求解中/自由首碰改入 principal 品牌绿 14pt 标题 + 11pt 副标题（VM `statusText` 承载，同三解页）。**17.3 定稿=演示不可中断**——删「演示中」pill/「停止」钮/「重置」钮；演示期动作列禁用（标题「击球中」），重置移入三点菜单「恢复默认」。**17.4**——「下一解」移到球桌左下（自由态「恢复球形」位置带）。**17.5**——求解模式右列收敛为 击打/上一杆/回放；「上一杆」按 G17 全量恢复（页面 `SolveUndoContext` 携带球形+袋口+库数+解集缓存+档位+力度，`restoreSolve` 免重解；`makeSolveUndo`/`restoreSolve` 与 `strike()`/`replaySolveShot()`/单测共用单一真源）。**17.6 定稿=按现 G5 校验（不加瞄准条）**——求解模式瞄准是 `BankKickSolvePipeline` 的**输出**非输入，加可调瞄准条需改求解逻辑（违「明确不做」）；自由模式 `BTAimWheel`+力度柱由 `ShotStageProxy`（G4/G5）已同底，截图核验通过。**17.8**——球库补全 16 球位（`PositionPlayBall.allKeys`，2×8）；母球/黑 8 固定在桌 → 球库位灰显不可拖、点击脉冲（`pulsePaletteBall`）。**17.9（G19）**——`info.circle` 直开 sheet → `ellipsis.circle` 三点菜单（原理说明 + 台面网格 4×8 Toggle + 恢复默认）。**G17 类型取舍（如实）**——翻袋/反射引擎解为 `BankEngineSolution`/`KickEngineSolution`（非 PositionPlay 的 `PositionPlaySolution`），与共享 `SolveShotSnapshot` 类型不兼容，故按方案「用 VM 层已有 solutions 即可」以本页原生类型建 `SolveUndoContext`（`SolveShotSnapshot` 结构未动，符合明确不做）。**验证（真实输出）**：`make build` **BUILD SUCCEEDED**；`PositionPlayUndoSnapshotTests`（新增 `test_bankShot_solveUndo_restoresEveryField` + `test_reflection_solveUndo_restoresEveryField`，真实反解→捕获→抹除→恢复逐字段一致 + `isSolving==false` 证免重解）合计 **Executed 6 tests 0 failures**；回归 `BankKickDifficultyTests` **10/10 0 failures**；UI `ScreenshotTourUITests.testBankShotRealMode` + `testReflectionRealMode` **TEST SUCCEEDED**（`captureSolverStates` 按新 UI 更新：strike 用「击打」文字钮、删「停止」等复位、新增三点菜单+网格 Toggle+求解态上一杆取证）。截图逐张读图核验（`docs/ui-polish/screenshots-latest/light/r0{1,2}-*`）：principal 标题+副标题解读数、16 球库白/黑灰显、下一解左移、演示态无停止、grid-on 网格上屏、solve-undo 全量恢复。存疑：真机手感（拖球/演示细腻度）未上机；`stopStrike()` VM 方法保留但已无 UI 调用（无害，未删以降churn）。真源 V9 段已回写。**下一批：V10 动作库与试打（Q19，需先确认 Q19.2.5 半句）。**
- **问题集合 v5 批次 V8（防守重做 Q16+G17/Q15.3 落地）✅（2026-07-13，plan-delegated-execution：opus-4.8 执行子智能体 + 主控独立验收，0 轮返工）**：**改名**「做斯诺克」→「防守」（导航题/入口卡/测试断言，grep 用户可见字符串清零）。**中八规则重做**——用户只选目标球，`opponentKeys` 推断对方球组（全色↔花色/只剩 8 号防 8 号），`canTarget` 双组在桌禁 8 号拦截（对齐 `legalTargetKeys`）。**求解谱系**——`solveSnooker` 签名 `blockerKey→opponentKeys:[String]`，多球联合可见性（`snookerCoverageMulti`/`defenseCoverage`），完全斯诺克（`satisfiesConstraint`=对方全挡死）最优 → 高难度可行解（诚实标注「未完全斯诺克·挡死 m/n」）→ 诚实无解；评分 v1=maximin 防守（挡死数↑→最弱环难度↑→…→执行难度↓；对手难度 `0.6·切角+0.4·球距`，**权重待实测调优**，常量集中单点可调）。**G17**——`undoLastShot` 复用 `SolveShotSnapshot`+页面 `UndoContext` 全量恢复不重解。**Q15.3**——`BTEraserButton` 在「摆球」右侧。**主控独立验收（真实输出）**：`make build` BUILD SUCCEEDED；`SnookerSolverTests` 重写(6)+回归 solver(13)/undo(4)/sector(6) **Executed 29 tests 0 failures**；`S8_SnookerV8UITests` 4/4 + S2 testSnookerLayout passed；grep 清零核实；`docs/ui-polish/screenshots-20260713/v8/` 四张截图逐张读图核验（完全斯诺克/高难度可行解/诚实无解/清除键）。存疑：评分权重待真机标定；上一杆动画链路待真机冒烟。真源 V8 段已回写。**下一批：V9 翻袋/反射页统一。**
- **App 全页截图基线 ✅（2026-07-13）**：`docs/ui-polish/screenshots-20260713/` 共 43 张命名 PNG（5 Tab + 练习学/练/打/解全入口默认态 + 我的子页 + 订阅），由扩覆盖后的 `testFullScreenshotTour` 生成（iPhone 17 Pro，TEST SUCCEEDED 621s）。索引见该目录 `README.md`；`screenshots-latest` → 本目录软链。未截：自由记录会话（入口未点到）。
- **问题集合 v5 批次 V7（打一走二想三 Q15.1+Q15.2+Q15.3）✅（2026-07-13，plan-delegated-execution：opus-4.8 执行子智能体 + 主控独立验收，返工 1 轮〔仅 UI 测试取证层，产品代码零改动〕；执行会话曾中断，主控恢复后完成验收与返工闭环）**：**Q15.1 扇形默认选区**——`SolveRegion` 新增 `.sector`（顶点=②假想球 + 半径带 [sMin,sMax]米 + 场景 bearing 角区间，无③两侧/有③单侧）+ 精确环形扇区 SDF（点到内外弧+两径向边最近距离带符号，多区间取 min 并集，**禁外接矩形近似**，数值草稿 `/tmp/sector_sdf.py` 对暴力最近点误差 ~1e-8）；`PlanThreeSectorSolver.defaultRegion` 产出（与视觉 `compute` 共用 `preferredSide` 单一真源）；VM `currentConstraint` 状态机：② 就绪且无自选约束 ⇒ 扇形默认落区（高亮、可求解、`.restRegion(sector)`），自选约束 ⇒ 扇形降灰 + 用户约束求解，删除 ⇒ 恢复；`PositionPlaySolver` **零改动**（`signedDistanceMeters` 通用消费）。**Q15.2 <3 球终局**——`canPotOnly`（①+①袋 且目标球 ≤1）走全台面 pot-only（1 球只打进），2 球用扇形（打一走二）；`finishStrike` 检 `objectBallCount==0` ⇒ 清台终局提示。**Q15.3 清除键**——抽共享 `BTEraserButton`（42×32 正常尺寸）放「摆球」右侧，打三+思路两页同改（防守随 V8）。**验证（真实输出）**：`make build` BUILD SUCCEEDED；新增 `PlanThreeSectorSolverTests`(6：SDF 金标准内/外/弧/角边界 + defaultRegion 几何 + 满足解停点在扇形内不变量 + pot-only/终局 + 降灰恢复状态机)；回归 `PositionPlaySolverTests`(13)+`PositionPlayUndoSnapshotTests`(3) 合计 **22 tests 0 failures**；UI 回归 `S2_ShotPagesLayoutUITests` 全 6 用例分两轮复跑全部 passed；新增 `S7_PlanThreeV7UITests`（返工后 5 用例：ClearKey 合并为单用例双页独立深链取证）**全套 passed**；`build/v7-shots/v7-*.png` 六张主控逐张读图核验（扇形默认高亮/降灰/pot-only/清台/两页清除键）。**返工 1 轮**：首版 `testV7ClearKeyLayout` 确定性失败（进求解页后 TabBar 不可达）+ 打三截图泛白过渡帧，返工改独立深链 + snap 前等「摆球」chip 后通过。存疑：击打入袋动画链路（SCNAction 非单测友好）终局状态经 `finishStrike` 逻辑 + 深链截图取证，建议真机冒烟；DoD#2「存在满足解」由深链求解截图取证（单测只钉不变量）。真源 V7 段已回写。**下一批：V8 防守重做。**
- **问题集合 v5 批次 V6（开球通用规范 G18+Q14+Q15.4）✅（2026-07-13，plan-delegated-execution：opus-4.8 执行子智能体 + 主控独立验收，0 轮返工；台账补记于会话中断后恢复时）**：**①随机性只留球堆间距**——删 `BreakFlowRunner.breakJitter` 随机塞（spinX/spinY 恒 0），`RackLayout` 球堆 jitter 保留。**②开放瞄准**——runner 新增 `aimDir`+`nudgeAim`（复用 V1 `aimNudgeDegrees` 公转增益 + `rotatedAim`），拖屏与左侧 `BTAimWheel` 共用入口；未手动调向默认锁顶球，`drawAimLine` 沿可调方向画（淡绿延伸 `rayToInnerRail`）。**③力度条默认 6 m/s**——`runner.velocity`（`defaultBreakVelocity=6.0` 替代固定 7.0），右侧 `BTShotInstrumentColumn` 绑定可调。**④布局**——新增共享 `BreakInstrumentsOverlay`（左瞄准轮 `aimWheelFrame` + 右力度柱 `instrumentFrame`，同底贴边，仅 `.racked` 可调）。**⑤按钮互换+双条收敛**——`FreePlayBreakBar` 删除，四宿主统一 `BreakControlBar`（取消/重开/主按钮，完成移最右主位）；「换一局」与「重开」合并为 `reRack()`（换 seed 重摆）。**⑥四宿主接线**——FreePlay/Silu（Q14）/PlanThree（Q15.4）已接；Composer 按既有设计开球入口禁用（UI 不可达但已接好，是否放开待用户确认）。**主控独立验收（真实输出，恢复会话复跑）**：`make build` BUILD SUCCEEDED；`BreakFlowRunnerV6Tests` Executed 5 tests 0 failures（同 seed 一致/异 seed 有界扰动/无隐藏随机塞确定性/瞄准影响散局/默认 6.0）；执行轮另有 `RackGeneratorTests`(8)+`BreakRackPhysicsTests`(10) 全绿、`S6_BreakUniversalUITests` 四宿主截图核验（`build/v6-shots/`）。存疑：autoDeliver 端到端无专门 UI 用例（既有缺口）；真机拖屏手感待冒烟。真源 V6 段已回写。**下一批：V7 打一走二想三（依赖 V6 已就绪）。**
- **问题集合 v5 批次 V5（场景页相机与瞄准点 Q5+Q7+Q9）✅（2026-07-13，plan-delegated-execution：opus-4.8 执行子智能体 + 主控独立验收，0 轮返工）**：**Q5/Q9 相机**——`CameraRig.enterAiming` 进场目标 pose zoom 0→1（最高机位=竖滑上界，用户可下滑压低；`AimingCameraConfig` zoom 梯与 `applyCameraTransform` 未动；`snapToAimPose` 按范围纪律保持）。**Q7.1**——新增纯几何真源 `Core/Geometry/AimLineGeometry.swift`：垂距 ≥R 白线延伸 `rayToInnerRail` 库边无红点；垂距 <R 白线停射线与球面第一交点 + 双红点（G1 垂足 + 接触点）；删旧 2R 捕捉截停。**Q7.2**——2D 瞄准点训练接 `ShotStageProxy`（顶栏 46/底栏 94、球库 16 球=球桌宽、刻度轮贴边、提交钮齐球桌底线）。**Q7.3**——3s→1.5s + 文案同步。**Q7.4**——`strike()` 走 `runCueStroke` 单一权威（运杆→出杆→触球起播），收尾 `playback.duration`（G15 口径）。**Q9**——3D `interactionMode .tapsOnly→.cameraControl`（横滑 yaw/竖滑 zoom），瞄准=BTAimWheel+点击台面（tap 绝对指向按 G13 保留）；2D 保留 V1 相对拖动。**主控独立验收（真实输出）**：`make build` BUILD SUCCEEDED（亲自复跑）；`AimLineGeometryTests`(5)+`PositionPlayFreeAimTests`(12) 亲自复跑 **17 tests 0 failures**（V1 地基未破坏）；grep 1.5s/文案核实；截图逐张核验（2D 布局贴边、球杆运杆帧、3D 进场最高、竖滑降机位）。偏差：UI 测试机型 iPhone 17 Pro（本机无 16 Pro）。存疑：Q9 3D 点击+刻度轮瞄准手感待真机冒烟。真源 V5 段已回写。**下一批：V6 开球通用规范 → V7 打三。**
- **问题集合 v5 批次 V4（学习/训练页轻修 Q1+Q2+Q3+Q4+Q6）✅（2026-07-13，plan-delegated-execution：opus-4.8 执行子智能体 + 主控独立验收，0 轮返工）**：**Q1**——训练计划卡两处标题硬编码 15pt bold → `.btHeadline`（`TrainingHomeView.planPosterCard`、`PlanListView.PlanCard`），三方（练习页/动作库/计划卡）同 token。**Q2**——「角度与打点」→「角度与瞄准」（navigationTitle + 入口卡 + UI 测试断言 5 处），grep 用户可见字符串清零（仅余代码注释）。**Q3**——对照表标签换位：「瞄准点」左上（`aimPoint−(0.95d,0.40d)`）、「接触点」沿两球连线右法向外移，0–90° 全域数值核验最小间隙 4.2pt@0°/6.4pt@8° 均正，90°（假想球心与瞄准点重合的最坏帧）截图核验不遮挡。**Q4**——角度预测闭式居中：`rayLen=min(w·0.74, h−2·margin−2R)`、`vertex.y=h/2+rayLen/2`（上下留白恒相等），90° 1 号球完整可见（原 y=0 被 clip）。**Q6**——瞄准点训练本页传参收窄（全局真源未动）：线宽 ≈1.76pt（×0.55）、用户红点 d·0.10→0.06、正确红点 d·0.12→0.075。测试基建：`GeometricAngleQuizView` UITest-only 确定性角度钩子（默认不生效）+ `testV4Evidence`。**主控独立验收（真实输出）**：`make build` BUILD SUCCEEDED（亲自复跑）；grep 双清零核实；`testV4Evidence`+S5 两用例 Executed 3 tests 0 failures；`build/v4-shots/` 截图（Q3 0°/45°/90°、Q4 90°、Q6、Q1）逐张核验。真源 `问题集合_v5.md` V4 段已回写。**下一批：V5（场景页相机·瞄准点，依赖 V1 已就绪）。**
- **问题集合 v5 批次 V3（G17 上一杆完整恢复 · 思路训练/打一走二想三先行）✅（2026-07-13，执行智能体，0 轮返工）**：**共享快照**——`QiuJi/Core/PositionPlay/PositionPlaySolverModels.swift` 新增两个纯内存类型：`SolveConstraintDraft`（`region/restPoint/passPoint`，两页 `Draft` 上收单一口径，调用点零改动，保留「落点 vs 落区圆」语义分叉）+ `SolveShotSnapshot`（`before`球形 + `shot` + `prediction` + **`solutions/currentIndex`解缓存回填免重解** + `draft`约束 + `velocity/spinX/spinY` + `allowSideSpin/basicPositionOnly`求解选项）；页面特有选择模型（思路=目标球+袋口，打三=①②③角色）由各 VM `UndoContext` 以本快照为基座另行携带（**V8 防守 / V9 翻袋反射复用本结构，只加一层同款 UndoContext 即可**）。**升级**——`SiluTrainerViewModel.undoLastShot` 与 `PlanThreeViewModel.undoLastShot` 从「仅 `loadBoard(before)` 恢复球位」升级为**全量恢复**（球形 + 目标/袋口或①②③角色 + 约束 draft + 解集 + 打点/力度/瞄准 + 求解选项，**不重求解**）；捕获抽 `makeUndoContext(shot:prediction:)` 由 `play()` 与单测共用（单一真源）。工具态 `activeTool` 复位 `.none`（约束已还原可见，属瞬态 UI 选择非该杆数据，如实标注范围决策）。**明确不做保持**：斯诺克 V8 / 翻袋反射 V9 的 undo、`PositionPlayViewModel` 基准语义未动。**验证（真实输出）**：`make build` BUILD SUCCEEDED；新增 `PositionPlayUndoSnapshotTests`（3 例：手工快照逐字段一致含解计数/档位/选项翻转验证恢复顺序、①②③角色还原、真实反解管线捕获→抹除→恢复往返一致且 `isComputing==false` 证不重解）+ 回归 `PositionPlayFreeAimTests`（12 例）= **Executed 15 tests, 0 failures**；UI 回归 `S2_ShotPagesLayoutUITests`（testSiluLayout+testPlanThreeLayout）**Executed 2 tests, 0 failures**。**存疑**：`play()→undo` 的动画链路非单测友好，建议真机冒烟一次。真源 `问题集合_v5.md` V3 段已回写。**下一批：V4（学习/训练页轻修）/ V5（场景页相机·瞄准点，依赖 V1）可并行；V6 开球 → V7 打三。**
- **问题集合 v5 批次 V2（G15 禁尾速截断 + G16 打点盘白盘增大 + G19 齿轮→三点）✅（2026-07-13，执行智能体，0 轮返工）**：**G15**——删 `TrajectoryPlayback.perceptibleSettleTime()` 及全部 **14 个消费点**（8 VM 击打+回放各 2、AimPointScene 1、BreakFlowRunner 1），统一 `let settle = playback.duration`（播满引擎自然静止；`stateAt` stationary 置零与 `EngineNumerics` 0.001 判定保留）；`SequenceVideoExporter` 本就播到 `pred.duration` 无需改（已 grep 核实同口径）。**G16**——`BTSpinPad` `inset 5→2`（盘区 104/卡片 228 不变；白盘直径 94→100，打滑圈/皮头斑随 ballR 等比放大）。**G19**——`ellipsis.circle` 扫替 AngleDynamicView/SceneAimingView（练习 Tab）+ CustomPlanBuilderView/BTExerciseRow（外围）；**ProfileView「偏好设置」行 `BTIcon.gear` 保留并上报**（iOS 设置风格导航行徽标，换三点语义错误）。grep `gearshape`：练习 Tab 清零，全仓仅余 `IconToken.gear` token。**验证（真实输出）**：`make build` BUILD SUCCEEDED；新增 `TrajectoryPlaybackSettleTests` TEST SUCCEEDED（确定性单测：旧 0.07 截断遗留 12.2mm 瞬移、额外播放 0.35s 到自然静止、尾段速度不置零）；`PositionPlaySequenceExportRunnerTests`、`S5_TrainingPagesLayoutUITests`（testAngleDynamicLayout+testTableGridBrightness，验证三点菜单/网格 Toggle）、`ScreenshotTourUITests`（testBankShotRealMode+testReflectionRealMode，验证 G15 击打回放不卡死）全绿 TEST SUCCEEDED。**副作用如实评估**：击球/开球收尾等到自然静止，比旧感知截断晚 ~0.3–0.5s（被消除的 creep 尾段真实时长），属预期，未新增截断。真源 `问题集合_v5.md` V2 段已回写。**下一批：V3（G17 上一杆完整恢复，思路/打三先行）。**
- **问题集合 v5 批次 V1（G13 瞄准交互重做 + G14 求解去抖）✅（2026-07-13，plan-delegated-execution：opus-4.8 执行子智能体 + 主控独立验收，0 轮返工）**：**G13**——`AngleSceneView.handlePan` 空白拖动改「第一落点只选中瞄准线不转向 + 逐帧相对角位移」；新增 `AngleSceneCalculator.aimNudgeDegrees`（绕母球公转模型：切向才转向、径向不转向，等价增益 (180/π)/r 度/pt 天然随母球距离缩放，近母球封顶 `maxGainDegPerPt=0.6` 度/pt ≈ BTAimWheel 细调 4 倍，🔬 真机手感待定稿）；回调 `onAimDragged→onAimNudged`，7 个接线视图（Composer/FreePlay/翻袋/反射/分离角/批量出片台/2D 瞄准点训练）全部改走各 VM `nudgeFreeAim` 相对入口，旧 `handleAimDrag`「点哪指哪」删净。**G14**——新增 `SolveDebounceScheduler`（交互态 0.5s idle / 离散态 20ms，可注入时钟可单测）；`PositionPlayViewModel.recompute(interactive:)` 拖动中只做纯几何预览（假想球/首碰点 + 闭式瞄准线 `drawFreeAimPreviewLine`，空杆延伸库边走新共享 `rayToInnerRail`）、停 0.5s 才求解；翻袋/反射求解模式 120ms→0.5s idle。**主控独立验收（真实输出）**：`make build` BUILD SUCCEEDED；`PositionPlayFreeAimTests` 12/12 TEST SUCCEEDED（新增 5 条：公转金标准 ±15°/径向 0°/近母球衰减/rotatedAim 端到端/去抖时序拖动 0 触发+idle 单次触发）；`S1_FreePlayLayoutUITests` TEST SUCCEEDED；子智能体轮 S2/S3+翻袋反射+B2 套件 xcresult Passed。真源 `问题集合_v5.md` V1 段已回写执行结论与增益定稿。**遗留**：① 真机手感（0.6 度/pt 与 0.5s idle）待人工；② 力度/打点滑条仍走快速求解路径（非 V1 锚点，如需纳入 idle 由用户裁决）；③ `AngleTrainingScene.swift` L1185 过时注释仍引用旧 `onAimDragged`（纯注释，V11 顺手清）。**下一批：V2（G15+G16+G19 全局小件）。**
- **练习 Tab 问题集合 v5 立项 ⏳（2026-07-13，Orchestrator，纯文档）**：用户提交《问题集合_v4.md》（20 条新一轮反馈），按 `/issue-collection-restructure` 整理为**《问题集合_v5.md》**（仓库根目录，本轮需求真源）——**G13–G19 全局规范**（G13 瞄准交互重做「选中+相对调整」禁点哪指哪、G14 求解去抖拖动不求解停 0.5s 才求解〔用户标注至关重要〕、G15 回放禁 0.07m/s 尾速截断含渲染管线、G16 打点盘白盘增大、G17 上一杆恢复完整快照〔编排台系已是基准，思路/打三/斯诺克/翻袋/反射仅球位需升级〕、G18 开球通用规范〔随机只留球堆间距+开放瞄准+力度条默认 6m/s+完成/重开互换+双开球条收敛单真源〕、G19 齿轮→三点统一）+ **Q 系列页面条目**（含 Q15 打三扇形默认选区进求解〔`SolveRegion.sector` SDF，禁外接矩形〕、Q16 做斯诺克改名「防守」中八规则重做、Q17/Q18 翻袋反射九项 UI 统一、Q19 试打新增**序列模式**为默认）+ **V1–V11 会话批次**（V1 瞄准交互地基 → V2 全局小件 → V3 上一杆 → V4 学习训练轻修 → V5 场景页相机/瞄准点 → V6 开球 → V7 打三 → V8 防守 → V9 翻袋反射 → V10 动作库试打 → V11 总验收；V1–V4 可并行启动）。全部锚点经三路探索子代理读代码核实（含关键事实：拖动瞄准=绝对指向、`recompute` 20ms 去抖、`perceptibleSettleTime 0.07`、扇形不进求解且无外接矩形转换、试打 `DrillTryoutFormation` 缺 steps 通路）。**4 项待用户确认**（v5 §六，含 v4 条 19.2.5 原文未写完）不阻塞开工。**下一步：新会话执行「问题集合 v5 批次 V1」。**
- **图文精讲批量写作（c001–c031 有图 drill）✅（2026-07-10，tutorial-writer × 并行子智能体，按 `.cursor/skills/tutorial-authoring/SKILL.md`）**：对 `build/position_play_export/` 已有出片图（initial+sNN_still）的 28 个 drill / 47 条序列批量写回 `tutorial`（应用课模板，几何事实全部来自 `scripts/tutorial_digest.py` 事实清单，轨迹表述逐张核对出片图）。**已写回 29 个 drill JSON**（含此前散写的 c035/c039/c041）：c001/c002、c005（4 球形）、c010（4 球形）、c011–c018、c019–c021（加塞 formations）、c022（仅球形2）、c023（5 杆）、c024–c029、c030（5 球形）、c031（13 杆）等；全部 `json.load` 校验通过。**红灯未写**：c003/c004/c009（母球 scratch，需编排台重录）、c022 球形4（第 2 杆未进袋）、c010 球形2（低杆切球与「定杆」意图不符，建议重录）。**待办**：① `DrillTutorials/` 配图 0 落位——全部 image 字段已按 `<id>[_fN]_initial/_sNN` 预填，需从 `build/position_play_export/<序列目录>/` 拷贝重命名（约 80+ 张）；② c032+ 无出片图，出片后再写；③ 多个 drill 的 `description`/`animation` 袋口与序列不符（c001/c002/c012/c015/c018/c019 等），待 content-engineer 统一元数据；④ H-11 人工审核精讲文案。**注意**：并行批次间 c005/c015–c017 有重复写入，以后写修正版为准（已含出片核对）。
- **一杆清台求解器方案 v1 定稿 ⏳（2026-07-10，Orchestrator + iOS Architect，纯文档，v1.x 功能）**：用户 idea「中式八球一杆清台求解器——给定球形或开球散局，输出完整清台杆序（目标球/袋口/塞/力度），2 分钟内求解」。三轮设计讨论定稿（beam search 初案 → **(S,x) 状态空间反向价值迭代 DP + 解析势能场 + 残局精算库 + 引擎具现化** → 球形分类学补全：对方 7 球=永久障碍+首触犯规、单球 7 型/球团 C1–C6 量化判据、黑 8 非对称规则、分诊路由 A–F、「不可行证书/最长前缀+命门」为一等输出）+ 文献调研（PickPocket/CueCard/KR-UCT/pix2pockets 2025——启发式+前向模拟优于端到端 RL，基线 30% 整局清台率作参照系）+ 项目 16 理论资产盘点（T05/T06/T07 反向规划与 key ball 先验、`contracts/validation-cases.json` 48 局面为验收集、坐标系已对齐）。锚点全部读代码核实（`PositionPlaySolver.solve` L97 / `SolveRegion` SDF / `BilliardRulesEngine.ChineseEight` 犯规语义 / `AnalyticShotRollout.evaluate` L262 / `predictBankAll/KickAll` / `BreakSimulator` L46；引擎红线：无跳球/扎杆/组合球瞄准搜索）。按 `/issue-collection-restructure` 拆 **W1 Survey+千局普查 → W2 残局精算垂直切片 → W3 解析场+对拍 → W4 DP 核心 → W5 具现化链 → W6 球团分段 → W7 整合+anytime+认证 → W8 UI → W9 总验收** 九批。方案真源 `docs/research/20260710-一杆清台求解器方案.md`（**v1.1**：定稿后自审补 5 洞——G1 场分解〔己方遮挡禁烘静态表，DP 在线修正 + 时序死球复活强制单测〕、G2 开放局选组决策双组 DP、G3 可达性核构建成本入账 3–10s、G4/G5 对方球入袋规则与黑 8 指定袋列 W2 核实项、DP 分数序数口径禁当概率）。**排期：不插 P18 主线（仅剩人工项），W1 随时可启动。**
- **动作库试打模式 D1–D3 全部完成 ✅（2026-07-10，iOS Architect + SwiftUI Developer + Test Engineer，方案真源 `docs/research/20260709-动作库试打模式方案.md` v1.2，用户拍板豁免排期前置一次性执行）**：动作库每个 drill 详情页加「上手试打」——物理引擎自由模式模拟试打，不计分、不新建页、复用 Composer。**D1 数据通路**：新建 `Core/Physics/DrillBoardBuilder.swift`（`DrillContent → BoardSnapshot`：`shots[0].cue→cueBall`、`target→_8`、obstacles 依序编号跳 `_8`；无 shotIntent 从 `animation` 兜底双球局）；`DrillBoardBuilderTests` 单测 + 全库 72 drill smoke（全部含 shotIntent，兜底路径合成数据覆盖）。**D2 试打变体 + 入口**：`PositionPlayComposerView` 加 `sourceDrill` 参数驱动裁剪（标题=drill 名、隐藏开球/重命名/清空、球库与全部球拖动保持原样）；「重摆球形」一等公民按钮（左下角，`loadBoard` 回 drill 初始布局，回放中禁用）；进场说明卡 `DrillTryoutBrief`（三行全部由既有 JSON 生成：局面目标含多杆补充/训练重点=coachingPoints[0]/参考打法=力度档+杆法用语，非 modal 首次交互或点卡淡出、顶栏 info 召回）；`DrillSceneView` bottomTrailing「上手试打」胶囊钮（Premium 锁跟随 `isLocked` 显皇冠、点击弹订阅页）。**D3 润色**：首次手势提示合并为说明卡底部一行（`@AppStorage drillTryout.hasSeenGestureHint` 跨启动，首次交互或点卡关闭即记忆）+ 试打进场台面淡入 0.45s（非试打路径恒 opacity 1 零影响）。**验证（真实输出）**：`make build` BUILD SUCCEEDED；`DrillTryoutUITests` 4 用例全绿 TEST SUCCEEDED（c042 全流程明暗截图：入口两态/布局裁剪/说明卡三行含「共 3 杆」/首次交互淡出+info 召回/击球→重摆闭环/拖球改摆后重摆仍回初始；c050 锁态弹订阅；暗色入口两态；手势提示首启出现+二次启动不出现跨启动断言）；既有 `testPositionPlayComposerOnly` 回归绿。**下一步：回 P18 主线（仅余 S6/S7 人工项）；试打计分统计留二期候选（方案「明确不做」节）。**
- **翻袋反射页重构 W6（自由模式）✅（2026-07-09，SwiftUI Developer + Test Engineer，方案真源 `docs/research/20260709-翻袋反射页重构方案.md` v2 §6 W6，至此 W1–W6 全部完成，方案仅剩可选 W7）**：两页 VM/View 加 `BankKickPageMode` 双模式（solve/free）——切「自由」：清解线、绘**选中解暗虚线参考**（翻袋=目标球全路径、反射=母球至首碰段，hint token），瞄准=台面拖手柄（`AngleSceneView.onAimDragged`）粗调 + 左缘 `BTAimWheel` 细调，预测线/幽灵球/首碰点走 `AngleSceneCalculator.freeAimFirstContact` 纯几何实时预览（空杆射线延至库边）；首碰胶囊（首碰 N 号球+打点缩略+切角）与空杆空态文案；右缘完整仪表柱（`onSpinTap` 开 `BTSpinPadOverlay`，spinX/Y 进击打）；**击球 = `simulateFree` 真物理试手**（复用 W5 播放骨架：出杆+音效+进袋语义，settle 后球停在哪是哪、杆随母球新位重瞄）；动作列=击球/上一杆（undo 回击打前球形）/回放（重播上杆 prediction 后回停点）；左下「恢复球形」=回最近求解快照（翻袋含袋口）并切回求解模式、缓存必命中直显。求解入口/选袋/库数 chips 全 `mode == .solve` guard，自由模式库数 chips 隐藏；`BTAimModeToggleButton` 扩 `solvedLabel/solvedIcon`（两页「求解/自由」，分离角「进袋/自由」不变）；模式过渡 spring 0.35/0.75 + 顶行 easeInOut 0.2。**验证（真实输出）**：`make build` BUILD SUCCEEDED；UI 测试 `captureSolverStates` 扩自由四态（free-default / free-after-shot / free-restored / back-to-solve），`testReflectionRealMode`+`testBankShotRealMode` 全绿 TEST SUCCEEDED；截图核验：模式钮/瞄准轮/仪表柱/暗虚线参考/首碰胶囊（翻袋「首碰 8 号球·14°」、反射空杆空态）/击球后球位真实变化+上一杆回放解锁/恢复球形回快照/切回求解缓存直显解线与 pill；首碰胶囊自由模式抬高避让恢复钮（复验截图确认无遮挡）。纯 View/VM 层改动不触 `ShotPredictor`，回归面沿用 W4 同日全绿结果。**下一步：方案主线收官；W7（c055/c057/c061 翻袋 drill 重烘焙）可选随时；或回 P18 主线。**
- **翻袋反射页重构 W5（求解模式「击打」演示）✅（2026-07-09，SwiftUI Developer + Test Engineer，方案真源 `docs/research/20260709-翻袋反射页重构方案.md` v2 §6 W5）**：两页 VM 加击打演示——击打前快照全部球位（cue/target/障碍球）→ `runCueStroke` 出杆（运杆/出杆单一权威 `CueStroke`，触球瞬间清线起播）→ `TrajectoryPlayback.action(removeOnPocket: false)` 播该解引擎 recorder（画面=物理=回放单一口径；含障碍球被扰动与进袋沉入/停顿/淡出）+ `ShotAudioScheduler` 音效 → `perceptibleSettleTime` 感知静止后自动复位快照并重绘解线，可重复击打/下一解。动作列 = 击打（主）/下一解/重置，回放中切单「停止」（立即复位）；`isPlaying` 全面锁交互（拖球 draggableNodes 清空、顶部 chips、力度柱、球库、求解与摆球入口全 guard），pill 显「演示中…」。**验证（真实输出）**：`make build` BUILD SUCCEEDED；UI 测试 `captureSolverStates` 扩击打 playing/reset 两态，`testReflectionRealMode`+`testBankShotRealMode` 全绿 TEST SUCCEEDED，截图核验：出杆动画帧（球杆可见+演示中 pill+停止钮+chips/球库/力度柱变暗）、复位帧（球位/解线/pill/击打钮全还原，翻袋黑 8 进袋淡出后复原重显）。纯 View/VM 层改动不触 `ShotPredictor`，回归面沿用同日 W4 全绿结果。**下一步：W6 自由模式（模式切换 + 瞄准手柄 + `BTAimWheel` + `simulateFree` + 上一杆/回放/恢复球形）。**
- **翻袋反射页重构 W4（UI 收口 + 球库真实碰撞体）✅（2026-07-09，SwiftUI Developer + Test Engineer，方案真源 `docs/research/20260709-翻袋反射页重构方案.md` v2 §6 W4）**：两页（`BankShotView`/`DiamondSystemView`）重构为「定高顶行 chips + `ShotStageProxy` 场景 + 定高底栏球库带」统一版面；右缘 `BTShotInstrumentColumn` 纯力度柱（onSpinTap=nil，力度=求解输入，拖完才重求解）；FAB 改贴边文字动作列（下一解/重置）；**球库带真实碰撞体接线**——`PositionPlayBall.objectKeys` 去 `_8` 进球库，拖入桌面即 `ObstacleBall` 进 `ShotInput.obstacles` 参与引擎求解（v1 几何避障作废语义落地），`BankKickSolvePipeline.solveBank/solveKick` 加 `obstacles` 参数、缓存 key 扩障碍球位（`BankKickSolveKey.make` 统一构造，毫米量化）；多球钳制 `clampMultiBall` 防重叠、拖回球库带移除、点击落空位；`ReflectionModeControl.swift` 删除（共用 `BTChipRow` 抽出独立文件 `Core/Components/BTChipRow.swift`）、`CushionReflectionSettings.realMode`/`realModeKey` 退役；原理 sheet 文案随新交互更新。**验证（真实输出）**：`make build` 过；`BankKickDifficultyTests` 10/10 全绿；回归面 PhysicsEngine/PhysicsInvariant/ScoringOnly/PositionPlaySolver/SnookerSolver/AnalyticAimParity/AnalyticRolloutParity 全绿 TEST SUCCEEDED；UI 测试重写为 `captureSolverStates` 四态截图（`testReflectionRealMode`+`testBankShotRealMode` 全绿）：反射默认解 pill「1 库·底库·易·容错 100%·1/13」→ 下一解 2/13 → 拖入 1 号障碍球重求解 13→12 解且球库位变暗 → 力度柱拉高难度中 3.6→中大 4.5 幽灵路径联动；翻袋障碍球共存解不变、高力度诚实无解提示（设计内空态）。**下一步：W5（求解模式「击打」：快照 → `runCueStroke` + `TrajectoryPlayback` 回放 + 自动复位）。**
- **翻袋反射页重构 W3（两页 VM 切新管线 + 排序 + 缓存）✅（2026-07-09，SwiftUI Developer + Test Engineer，方案真源 `docs/research/20260709-翻袋反射页重构方案.md` v2 §6 W3）**：新建 `Features/AngleTraining/BankKickDifficulty.swift`（与 `DifficultyModel` 分立不共码）——难度评分（切角引擎口径/库数/路径长/首库角权重集中）+ 档位（易/中/难）+ **E5 式扰动容错**（瞄准 ±0.5° + 力度 ±8% 共 4 次扰动，引擎终验口径评估）+ `BankKickSolvePipeline`（solveBank/solveKick 统一装配 `BankEngineSolution`/`KickEngineSolution`）+ `BankKickSolveCache`（LRU 8 条，key=全部球位毫米量化+袋口 index+力度步进）。翻袋 VM 切 `ShotPredictor.predictBankAll`（本批新增，与 `predictKickAll` 对称的 28 库序并行全枚举）、反射 VM 切 `predictKickAll`；排序=好打分 `w1·难度 − w2·robustness` 升序，tie-break 库数少→路径短；库数 chip 为展示层分桶过滤（不进缓存 key）；**拖球中只清线不重算，`dragEnded`/选袋/换库数/改力度才求解**；pill 上屏难度档（难=btWarning）+「容错 N%」。**口径注记**：pill 库数按种子库序数 `rails.count`（引擎 `objectCushionCount` 含擦 jaw 会虚高，如实分离）；`ReflectionModeControl` 本批先改纯力度形态（理想/真实钮删、`realMode` 停用），组件删除按批次纪律留 W4。**验证（真实输出）**：`BankKickDifficultyTests` 新增（评分单调性/档位边界/缓存逐出与量化 key/solveBank+solveKick 典型盘面集成）全绿；回归面 PhysicsEngine/PhysicsInvariant/ScoringOnlyConsistency/PositionPlaySolver/SnookerSolver/AnalyticAimParity/AnalyticRolloutParity/SolverPerformance 全绿 **TEST SUCCEEDED**；benchmark 同轮噪声内（PERF-W1 典型 0.228s/最坏 0.698s；PERF-W2 典型 0.248s/最坏 0.396s）；UI 截图核验 `testReflectionRealMode` TEST SUCCEEDED——反射 pill「1 库·顶库·易·容错 100%·1/7」+ 解线上屏、翻袋高力度 pill「1 库·右库·切角 14°·易·容错 25%」+ 瞄准线/幽灵球/进袋线齐全、低力度诚实提示「该袋暂无翻袋解」（暗色形态）。过程噪声：全量套件连跑两轮时 13 条重型烘焙/渲染用例被模拟器资源杀（signal kill），单独复跑全绿，与 W3 改动无关。**下一批：W4 UI 收口（去理想行组件删除/力度条右缘化/FAB 贴边动作列/球库带），依赖 W3 已就绪。**
- **动作库试打模式方案 v1 定稿 ⏳（2026-07-09，Orchestrator + iOS Architect，纯文档，发布后 v1.x 功能）**：用户 idea「动作库 drill 球形加入用物理引擎自己练的选项（自由模式试打）」，四轮讨论拍板后按 `/issue-collection-restructure` 拆解落档 `docs/research/20260709-动作库试打模式方案.md`（本轮需求真源）。核心拍板：**不做计分统计**（模拟成绩混入实球训练数据会失真，进袋类自动计分留二期）、**不做预览页**（详情页即预览，价值内化为试打页要点条/首次提示）、**复用 `PositionPlayComposerView(initialBoard:)` 加试打变体**（FreePlay 的开球/规则引擎/禁摆球语义全冲突，不采用；遵循项目「View 层设 VM 属性」裁剪惯例）、**入口=详情页 `DrillSceneView` 演示区覆层「上手试打」胶囊钮**（Premium 锁跟随 `isLocked`，Freemium 钩子；底栏「关闭/加入训练」不动）。代码事实：多球形在数据层不存在（`tutorial.formations` 仅文案且 Bundle 零实例、`shotIntent.shots[]`=多杆序列仅 c042），MVP 只试打根级球局（obstacles 依约定映射球号）；真实缺口仅两块——`DrillContent→BoardSnapshot` 转换器（不存在）+ Composer 变体/入口接线。批次 **D1（转换器+单测）→ D2（变体+入口+明暗截图验收）→ D3 可选润色**；锚点全部读代码核实（含 Composer init L14–23、`loadBoard` L196、`BoardSnapshot` L43、obstacles 无球号 L42–43）。**排期：P18 发布收敛（B3.7 S6、翻袋反射 W3–W6）收尾后启动，不插当前主线。**（2026-07-09，iOS Architect + Test Engineer，方案真源 `docs/research/20260709-翻袋反射页重构方案.md` v2 §6 W2）**：`ShotInput` 增 `kickRails: [DiamondSystemCalculator.Rail]?`（nil = 直击零改动，与 `bankRails` 互斥 bank 优先）；`ShotPrediction` 增 `kickContactMade`（成功判据 = 引擎口径母球**首次球-球碰撞对象 == 目标球**且碰前吃库数 ≥ 库序数，障碍球是真实碰撞体，无进袋语义）；`predict` kick 分支四层管线（`predictKick`）——第 0 层镜像种子（`DiamondSystemCalculator.kickSeedPath`/`candidateRailSequences` 新 API）→ 第 1 层解析目标函数（母球单球 `AnalyticShotRollout.rollout`，目标球+障碍球为静止碰撞体；首碰即定判据，唯一歧义 = rollout 截断）→ 第 2 层歧义限额引擎 scoring-only 回退 → 第 3 层引擎终验物化（解析成功档候选终验与物化**合并为一次全保真模拟**——判定仍以引擎事件流为准；失败自动试备选；全失败如实 `kickContactMade=false`，绝不回退几何解）。评分常量复用 `BankScoring`；`ghost` 复用为接触锚点（目标球心沿种子来向退 2R）。新增**并行全枚举入口 `ShotPredictor.predictKickAll`**（28 库序 `concurrentPerform`，W3 反射页 UI 同口径消费）。**验证（真实输出）**：`PhysicsEngineTests` +5 条 kick 用例（有解/直击不变量/障碍无穿球/低力度诚实未碰/无种子不可行）全绿；parity `test_kickObjective_parityWithEngine` 30 盘面×210 点假阳性 0、漏解 0；benchmark `[PERF-W2]` 典型盘面全枚举 28 库序 **0.263s（≤0.3s ✅，串行 1.094→合并终验 0.713→并行 0.263）**、最坏侧障碍半包围 **0.390s**；回归面 PhysicsEngine/PhysicsInvariant/ScoringOnlyConsistency/AnalyticAimParity/AnalyticRolloutParity/PositionPlaySolver/SnookerSolver 全绿 TEST SUCCEEDED；W1 基准同轮噪声内（典型 0.224s vs 基线 0.222s / 最坏 0.713s vs 0.684s）；`make build` 通过。UI 零改动（按批次纪律）。一次用例返工：障碍穿球断言未先查碰撞事件（障碍被真实撞开后母球可合法通过），与 bank 用例对齐后通过。**下一批：W3 两页 VM 切新管线（依赖 W1+W2，均已就绪）。**
- **翻袋反射页重构 W1（翻袋引擎反解）✅（2026-07-09，iOS Architect + Test Engineer，方案真源 `docs/research/20260709-翻袋反射页重构方案.md` v2 §6 W1，ADR-P18-03）**：`ShotInput` 增 `bankRails: [BankShotCalculator.Rail]?`（nil = 直击零改动）；`predict` bank 分支四层管线——第 0 层镜像种子（`BankShotCalculator.bankSeedPath`/`candidateRailSequences` 新内部 API，`solveSequence` 降级种子生成器）→ 第 1 层解析目标函数（`AnalyticAim` 母球段 + `AnalyticShotRollout.rollout` 目标球段，miss=路径到袋心最近距离+吃库偏离惩罚；目标球撞障碍=候选淘汰）→ 第 2 层歧义限额引擎 scoring-only 回退（粗扫/精修各 8 次预算，耗尽按无效=宁可少解）→ 第 3 层代表解引擎全保真终验物化（失败自动试备选 ≤3；不进=如实 `simObjectPotted=false`，绝不回退几何解）。搜索=种子 ±8° 中心向外早停粗扫 + 黄金分割精修（方案 secant 改黄金分割，语义等价，偏差已回写真源）。母球段吃库==0 硬判据保留。**验证（真实输出）**：`PhysicsEngineTests` +5 条翻袋用例（有解/直击不变量/nil 兼容/障碍无穿球/低力度诚实未进）全绿；parity `test_bankObjective_parityWithEngine` 30 盘面×210 点假阳性 0 漏解 0；benchmark `[PERF-W1]` 典型盘面单袋全枚举 28 库序 **0.222s（≤0.5s ✅）**、最坏侧近库+4 障碍 **0.684s（≤1s ✅）**；回归面 PhysicsEngine/PhysicsInvariant/ScoringOnlyConsistency/PositionPlaySolver/SnookerSolver/AnalyticAimParity/DifficultySolver 全绿，B0 基准同轮噪声内（A 0.15s/B 0.13s/斯诺克 7.16s/批量 0.47s）；改前全量基线 491/491（2 skip）绿，改后全量回归 **499/499（2 skip，0 失败，TEST SUCCEEDED，含新增 8 条）**。UI 零改动（按批次纪律）。**下一批：W2 反射引擎反解（依赖 W1 骨架，已就绪）。**
- **瞄准预测性能优化 P1–P3 全批次 ✅（2026-07-09，iOS Architect + Test Engineer，方案真源 `docs/research/20260709-瞄准预测性能优化方案.md` v1.1，执行结果见其 §5）**：UI 实时瞄准预测提速——**P1**：`PositionPlayShotSolver.solve` 袋口分支由 `predict`（三级网格 ~76 次模拟）切 `predictForPositionSolve(includePresentation: true)`（黄金分割解析评分 ~15 次闭式 + 1 次全保真），一处改动只影响 PositionPlay 三页 UI 管线；**UI 单次进袋预测 173→16.4ms（−91%）**（新增 `PhysicsPerformanceTests.test_perf_positionSolvePredict_record` 同盘面对照）。**P2**：`solveAimOffset.score()` 加 `earlyStop+stopAfterContact` 截断（与 `positionAimScore` 同参数，截断≠降保真、评分逐位一致）+ 改写 `AimScoring` L648 过时注释；**`predict` 单杆 173→60ms（−65%）/ 满台 1027→409ms（−60%）**，惠及 `DrillSceneView`/缩略图烘焙/测试套。**P3**：VM 预测调度改「20ms 去抖 + 单飞 + 末班车」（`predictInFlight`/`predictRerunWanted`，在途跑完只补跑最新意图，`predictGeneration` 代际作废保留——绝不展示旧解）。**验证（真实输出）**：全量 QiuJiTests 三批叠加后 **TEST SUCCEEDED 0 失败**（含 `test_fastPath_matchesPredict_potOutcome`、`ScoringOnlyConsistencyTests`、`AnalyticAimParityTests`、B0 求解器基准同轮无回归 A 0.13–0.16s/B 0.12s/斯诺克 6.94s）；171 杆重放 dump 对拍：漂移 P50 0mm/P95 27mm，**c047/c048 两杆母球洗袋合法分歧**（快路径解目标球同进袋且不洗袋，更优非更劣，落档方案 §5）。过程噪声：全量跑 7 用例「signal kill」（模拟器资源杀），单独复跑全绿。P4 热启动窄括号**未触发**（16.7ms « 20ms 去抖窗）。**⏳ 待人工：模拟器进袋模式快速拖球/切袋口手感走查（自动化已证 16ms/次，体感项 AI 无法代验）。**
- **翻袋/反射解球页重构方案 v2 定稿 ⏳（2026-07-09，iOS Architect + SwiftUI Developer，纯文档）**：用户五轮拍板定稿——两页**不合并**，各自加「求解/自由」双模式。**v2 核心拍板：严格教学 App，求解必须基于真实物理**——WP-B 4-a/4-b（翻袋/kick 引擎反解）由 v1.x **提前并入本方案**：`ShotInput.bankRails` + `solveAimOffset` 放开先吃库限制（母球段仍直击），解=引擎全保真 `ShotPrediction`（squirt/throw/传旋/母球碰后去向全真实）；性能用 **B 系列四层管线**（镜像种子 → `AnalyticAim`+`AnalyticShotRollout` 全解析射击法 → 歧义引擎 scoring-only 回退 → 代表解终验物化），目标翻袋 ≤500ms/反射 ≤300ms 以 benchmark 实测为准；排序=难度+**E5 式扰动容错**合成（借 E2/E4 模式，新建 `BankKickDifficulty` 与 `DifficultyModel` 分立不共码）。求解模式：击打=`TrajectoryPlayback` 播 ShotPrediction 后自动复位、解缓存（全球位+袋口+力度 key，LRU）、力度为求解输入（右缘力度柱）、拖球结束才求解；自由模式：手动瞄准/打点/力度走 `simulateFree` + 上一杆/回放/恢复球形；去理想模式（`ReflectionModeControl` 删除）；球库=真实碰撞体（v1 几何避障作废）；jaw 截断候选淘汰不回退几何解。批次 **W1（翻袋引擎反解+ADR）→ W2（反射）→ W3（VM 切管线+排序+缓存）→ W4（UI 收口+球库）→ W5（击打回放）→ W6（自由模式）→ W7 可选（c055/c057/c061 重烘焙）**。方案真源 `docs/research/20260709-翻袋反射页重构方案.md` v2（锚点行号已核实）；「两页合并为解球计算器」构想作废。**排期：E1–E5 全量回归收尾后启动，从 W1 开始。**
- **可执行性感知求解 E1–E5 全批次 ✅（2026-07-09，iOS Architect + Test Engineer，方案真源 `docs/research/20260709-可执行性感知求解方案.md`，执行记录见其 §6）**：教学产品共识落地——反解不只回答「解是否存在」，还建模「人打起来难不难」。**E1**：新建 `QiuJi/Core/PositionPlay/DifficultyModel.swift`（难度语义单一真源：加权范数 wX:wY=2.5:1 / 力度阈 4 m/s / 塞幅三档 0.15/0.3/0.5R）；5 处排序点（情形 A 桶内 `preferLessSpin`+最终排序、情形 B、斯诺克 `betterCoverage`+最终排序）全部由对称范数 `√(x²+y²)` 换加权范数——修正「spinX=0.3 赢过 spinY=0.4 纯低杆解」的反教学排序；`preferLessSpin` 加 **0 库桶特判**（母球不吃库时横塞零走位收益，同桶存在无塞解则无塞恒优先）。**E2**：`PositionPlaySolution` 增 `difficultyScore`/`difficultyTier`（中杆即可/需高低杆/需横塞/需极限塞），三处装配填充 + summary 追加「档位 · 难度X（薄球）」；三 VM 微调路径同步。**E3**：`SearchParams.maxSpinTier` 三级「优先+兜底」预算 + 预算 ≤`.vertical` 时**前置剪枝快路径**（先只扫 spinX=0 列，无预算内满足解才回退全网格）；思路训练「允许左右塞」升级为预算语义（关塞也绝不「无解」，兜底标「进阶（需横塞）」）。**E4**：切角（60° 起算/75° 标薄球）+ 球距（1m 起算）折算进评分（同次求解内为常量 ⇒ 只影响标注不影响解间排序）。**E5**：`robustnessEnabled`（默认关，思路训练页开）：情形 A 代表解 6 次扰动（瞄准 ±0.5°/力度 ±8%/打点 ±0.05R）解析 rollout（覆盖不了回退引擎），产出 `robustness`∈[0,1] + UI「容错 N%」。**验证（真实输出）**：新增 `DifficultySolverTests` **10/10 绿**（加权范数反转用户核心反例/档位映射/0 库桶代表无横塞/预算兜底/容错开关）；回归 `PositionPlaySolverTests` 13/13 + `SnookerSolverTests` 4/4 绿；perf 基准前后对比（同机同轮）：A 0.15→0.15s / B 0.12→0.12s / 斯诺克 7.12→6.94s / 批量 0.49→0.46s，**差异在噪声内（红线守住）**。范围说明：预算语义只升级思路训练页，三杆/斯诺克/批量出片台保持原硬剪枝（范围纪律）。**下一步：全量 QiuJiTests 回归确认（进行中）；模拟器人工走查思路训练页新文案观感。**
- **反解求解器性能优化 B5 收尾 ◑（2026-07-08，Test Engineer + QA Reviewer，方案真源 `docs/research/20260708-反解求解器性能优化方案.md` §3 B5；仅真机项待 H-20）**：①**批量出片全流程回归 ✅**——新增 `QiuJiTests/BatchSequenceReplayRegressionTests`：重放 `content/position_play/sequences/` 全部 **91 条 drill 序列 / 171 杆**（62 drill 成品出片内容），逐杆走「保存后重进」同链路（`PositionPlayShotSolver.solve`→finalPositions/potted→after 重建）落确定性 dump；**HEAD 基线 worktree（7cc294a）与优化后各跑一次 diff 逐字节一致（IDENTICAL）**，171 杆全可解且 feasible ⇒ B1–B4 对展示物理零改变；测试常驻回归网（TEST SUCCEEDED 43s）。信息项：重放 vs 存档有 1 杆结果差 + 个别停点漂移（基线重放同样如此）=存档早于 07-06 `pocketNoseRestitution` 修订的既有漂移，与本轮无关。②**穿透级联复评（B4 遗留）→ 暂不做**：递归解析多球=重建第二个引擎（红线 2 边界），成本远超收益（仅斯诺克 7.2→~0.5s，低频功能）；未来优先「级联格降均次成本」。③**文档回写 ✅**：方案 §1 终值表（A 0.15s/B 0.12s/批量 0.47s/斯诺克 7.2s）+ §3 B5、`PHYSICS-DEBT.md` §5.9（**D-D1 🟡→🟢◑ 主体偿还、D-D2 分级以「搜索 scoring-only+终验全保真」落地** + `highFidelityBounds` 不可关护栏结论）、P13 卡 §B5、H-20 注记、Hub 状态卡。④**真机 benchmark ⏳**：`devicectl` 实测 iPhone unavailable，不编造数字，H-20 保持 ⏳（连接后 15 分钟补测，B 系列唯一遗留项）。**下一步：B 系列收官；待 H-20 补真机对照表后在方案 §1 填真机列。**
- **反解求解器性能优化 B4 斯诺克分层搜索 ✅（2026-07-08，iOS Architect + Test Engineer，方案真源 `docs/research/20260708-反解求解器性能优化方案.md` §3 B4）**：①`AnalyticShotRollout.evaluateFreeShot` 自由球快评（击杆→母球 rollout 至首触→Han 解算→两球 rollout→kiss 检测；级联/kiss/截断如实 `needsFullSim`）。②`solveSnooker` 三层搜索：全网格 rollout 快评（65% 格就地下结论，几何早筛被吸收）→ 歧义格粗细两阶段引擎评估（aim 步 3×vel 步 2 粗网格 + 可行格邻域加密，必败区整片跳过）→ 代表解引擎**全保真复核**落地（每桶前 3 备选重验硬约束/覆盖/桶，比基线严格）。**实测**：全场模拟 3780→**526 次（−86%）**、墙钟 **36.56→7.0–7.2s（−80%，第一阶段 ≥60% 达标）**，解数 8 持平；**≤500ms 未达（如实记录）**——剩余成本=级联格全场模拟（多球级联只能引擎裁决），穿透级联递归解析留 B5 复评。**解质量对比**：金标准形 8 桶 margin 逐位一致；降级形 3/4 桶一致、2 库桶 21.7°→12.5°（粗细采样已知折衷，落档）。**一次打回**：试关 `simulateFree` scoring-only 的 `highFidelityBounds` 被 `ScoringOnlyConsistencyTests` 打回（多球盘面切步改变轨迹非纯展示量），已还原并注释落档。**验证（真实输出）**：`SnookerSolverTests` 4/4 + 金标准全绿，四条 benchmark：情形 A 0.15s / B 0.12s / 批量 0.47s / 斯诺克 7.2s。ADR-P13-04 + 实施记录落 P13 卡 §B4。**下一步：执行 B5（真机验证 + 批量出片回归 + 文档回写；真机基线依赖 H-20）。**
- **反解求解器性能优化 B3 单球解析 rollout 替换扫描层 + 速度曲线求精 ✅（2026-07-08，iOS Architect + Test Engineer，方案真源 `docs/research/20260708-反解求解器性能优化方案.md` §3 B3）**：①新增 `QiuJi/Core/Physics/AnalyticShotRollout.swift`——碰后母球/目标球单球闭式推演（常加速度段+库边反弹+落袋+状态迁移），引擎组件全复用；库边完整解算抽 `EngineNumerics.resolveCushionImpact` 单一真源（引擎与 rollout 同吃）；级联/kiss/碰前吃库/截断一律 `needsFullSim` 如实回退引擎。②扫描层 `candidateMatrix`→`scanMatrix`（每格 rollout 快评 + 歧义格就地引擎回退 + 漏进重解护栏同语义）；情形 B `passInfo` 增解析版（闭式段弦距剪枝 + dt=0.01 局部加密，撞球/截断未过点=歧义回退，宁可多跑不可漏解）。③删 Hooke-Jeeves `refineCandidate` → `polishVelocityCurve` 速度曲线求精（拓扑锁定两轮 9 点确定性密采）；**每桶代表解经引擎 scoring-only 复核物化（不符自动试备选）** ⇒ 上屏数值全引擎口径，全保真终验链路不动。**对拍**（新增 `AnalyticRolloutParityTests`，120 盘面）：进袋不一致 0、停点偏差按吃库分层——0–1 库 P95 <0.5mm（模型保真）、≥4 库为混沌放大（如实落档不掩盖）。**解质量对比落档**（基线 worktree 同盘面逐解）：A 落点/B 过点/批量 margin 至 1e-4 逐条一致；A 落区 0 库代表低杆右塞→中心球（更贴「少加塞」偏好，不劣）。**实测（`SolverPerformanceTests` 复测）**：情形 A 带精修 **0.15s**、情形 B **0.13s**（vs B0 2.28/1.98s 均 **−93%，达标 ≤300ms**）、批量 **0.48s**（−94%）、斯诺克持平（B4 战场）。**验证（真实输出）**：金标准全绿——`PositionPlaySolverTests` 13/13（`test_refine_notWorse_andDeterministic` 零改动通过）+ `SnookerSolverTests` + `PhysicsBenchmarkTests` + `PhysicsInvariantTests` + `ScoringOnlyConsistencyTests` + `AnalyticAimParityTests` + `AnalyticRolloutParityTests`，解数逐条持平（3/6/8/2）。ADR-P13-03 + 实施记录落 P13 卡 §B3。**下一步：执行 B4（斯诺克分层搜索，方案 §3 B4）。**
- **反解求解器性能优化 B2 解析瞄准层 ✅（2026-07-08，iOS Architect + Test Engineer，方案真源 `docs/research/20260708-反解求解器性能优化方案.md` §3 B2）**：①新增 `QiuJi/Core/Physics/AnalyticAimModel.swift`（`AnalyticAim`）——不进引擎事件循环，闭式推演「击杆→滑动/滚动两段常加速度弹道→首次母-目碰撞→目标球碰后方向」；**引擎组件全部复用零重写**（`CueBallStrike` squirt / `AnalyticalMotion` 闭式解 / `CollisionDetector` 同批四次·二次方程+漏检回退 / `makeBallBallKiss`+`resolveBallBallPure` throw 同源）。②**对拍验证先行**（新增 `AnalyticAimParityTests` 2/2）：评分层 275 点有效性不一致 **0**、角误差偏差 P95 **0.013°**；求解层 40 盘面 Δoffset 全量 **0.000°**、丢解 0、模拟评分交叉裁判真失真 0；唯一红灯（慢速长台丢解）归因=解析吃库支恒值 100 无梯度平台致黄金分割漂移，修复为统一叠加 ghost 距离梯度。③`positionAimOffset` 评分换解析版（搜索器仍黄金分割同括号同容差 ⇒ 对拍纯反映物理保真差；牛顿/割线不采用，理由见 ADR-P13-02），旧路径保留 `positionAimOffsetSimulated` + 编译期开关 `useAnalyticPositionAim` 可回退。④第 0 层几何早筛 `straightFanBlocked`（障碍球挡死 ±12° 瞄准扇区的线段-圆保守预测试，宁可漏筛不误杀）。**实测（`SolverPerformanceTests` 复测）**：瞄准记忆化段情形 A 50→**7.6ms（−85%，vs B0 404ms −98%，达标 ≥70%）**、情形 B 97→**14.6ms**、批量 9.9ms；墙钟情形 A 带精修 1.56→1.48s、情形 B 1.17→1.08s（该段 B1 后占比已小，**单球解析 rollout 组件是 B3 地基**）。**验证（真实输出）**：金标准全绿——`PositionPlaySolverTests` 13/13 + `SnookerSolverTests` 4/4 + `PhysicsBenchmarkTests` 14/14 + `PhysicsInvariantTests` 12/12 + `ScoringOnlyConsistencyTests` 2/2 + `AnalyticAimParityTests` 2/2，解数逐条持平（3/6/8/2）；上屏解全保真终验链路零改动。ADR-P13-02 + 实施记录落 P13 卡 §B2。**下一步：执行 B3（停点曲线求根替换力度网格 + 精修，方案 §3 B3）。**
- **反解求解器性能优化 B1 打分与出片分离 + 引擎早停 ✅（2026-07-08，iOS Architect + Test Engineer，方案真源 `docs/research/20260708-反解求解器性能优化方案.md` §3 B1）**：①scoring-only 模式（`buildPrediction`/`simulateFree`/`predictForPositionSolve` 增 `includePresentation:false`，搜索候选跳 polyline/extraBallPaths/分离角；`ShotPrediction.aimOffsetUsed` 记录实际瞄准）+ 每桶代表解 `finalizeCandidate`/`finalizeSnooker` 用同一 aimOffset 全保真重建后才上屏（画面=物理终验，比基线更严格）；②`EventDrivenEngine` 两种早停——`earlyStopBallNames`（兴趣球停稳 + 能量上界行程判据，兴趣球终态逐位不变）与 `stopAfterContactBetween`（瞄准评分专用：母-目首次碰撞后截断，评分逐位一致，瞄准记忆化 404→50ms）；③`passInfo` 事件段扫描（滚动段弦距精确下界 + 滑动段 aT²/8 曲率松弛剪枝）替换全程 dt=0.01 采样；④精修邻居瞄准复用（±spinY 直用种子 offset、±spinX/±velocity ±2° 窄括号热启动）。**实测降幅（`SolverPerformanceTests` 复测）**：情形 A 带精修 2.28→**1.56s（−32%，达标 ≥30%）**、无精修 1.64→1.24s、情形 B 1.98→**1.17s（−41%）**、批量 8 球盘面 7.42→**4.90s（−34%）**、斯诺克 36.56→36.46s（~0%，预期内，主体收益在 B4）。**验证（真实输出）**：金标准全绿（`PositionPlaySolverTests` 13/13 + `SnookerSolverTests` 4/4 + `PhysicsBenchmarkTests` + `PhysicsInvariantTests`）；新增 `ScoringOnlyConsistencyTests` 2/2（54 组 scoring-only vs 全保真 + 20 组早停 vs 整程，判定量/兴趣球终位/首触**逐条一致**）。实施记录落 P13 卡 §B1。**下一步：执行 B2（解析瞄准层，方案 §3 B2）。**
- **反解求解器性能优化 B0 基线 benchmark 与 profile ✅（2026-07-08，Test Engineer，方案真源 `docs/research/20260708-反解求解器性能优化方案.md`）**：新增 `QiuJiTests/SolverPerformanceTests`（4 条独立墙钟基准：情形 A 带/不带精修、情形 B、斯诺克 `.standard` **补上此前缺失的基线**、批量出片等价 8 球盘面）；`PerformanceProfiler` 增并发安全采样接口（`recordSample`/`measureSample`+`reportText`），插桩求解器分段（瞄准记忆化/候选评估/精修/passInfo/引擎段/后处理）。**实测（4/4 TEST SUCCEEDED）**：情形 A 1.64s 无精修 / 2.28s 带精修、情形 B 1.98s、**斯诺克 36.56s（原推算 2.5–5s 严重低估，全场 simulateFree 均值 94ms/次）**、批量 8 球盘面 7.42s（单次模拟 30ms ≈ 两球 4×）。**xctrace Time Profiler 两轮附着实测**：引擎 simulate 内核占 CPU 97.3–97.5%、后处理 2.0–2.3% ⇒ 主战场=减模拟次数/降单次成本。基线回写方案文件 §1、实施记录落 `tasks/phases/P13-position-play-solver.md` §B0；真机基线无设备连接未测 → 登记 H-20（非阻塞）。**下一步：执行 B1（打分与出片分离 + 引擎早停）。**
- **P18 B3.7 问题集合 v3 批次 S5（训练页布局与渲染 P4–P7 + G2）✅（2026-07-08，SwiftUI Developer + Test Engineer）**：**P4 角度与打点**——P4.1 布局改 `GeometryReader + VStack`（顶栏 46 / 底栏 94 定高，G10 球桌尺寸锁定；去 `ignoresSafeArea(.bottom)` 不再压住球库）、球库 `ShotStageProxy.libraryWidth` 8 列定宽=球桌宽居中（G8）、球库槽位补 AX 标识 `paletteBall_<key>`；P4.2 点击球库上桌/撤下为既有交互（截图核验）；P4.3 `PoolBallFace.potColor` 撤销黑 8 亮灰例外——进球线全号按本色（黑 8=黑虚线），无目标语义仍亮灰兜底。**P6/P7 2D/3D 角度训练**——`SceneAimingView` 同款定高布局（3D 模式底栏 0 不留空条）、2D `autoFitsRotatedTable` 接通、辅助/答题 `BTTextActionButton` 经 `proxy.bottomTrailingFrame` 齐球桌右缘底线（3D 保持浮动右下）、装饰球库同 G8 定宽、`NumericKeypadHUD` 从 `safeAreaInset` 改浮层 overlay（弹出不改球桌高度，G10）；P6.3 假想球心红点为场景层既有 `aimDot`（辅助档截图核验可见）。**P5.1 角度预测**——`NumericKeypadHUD` 新增 `compact` 档（键高 48→36 / 读数 38→24），弹出时「换题 / 显示参考」isHittable 断言通过。**G2 网格提亮**——`setTableGridVisible` alpha 0.28→0.55；**并修一处根因 bug**：偏好已开启时 `AngleSceneView.makeUIView` 先于 VM `setupScene` 懒建网格，彼时 `surfaceY` 还是默认 0.5，网格建成即埋进桌身永不可见 ⇒ 网格 Y 上收到父节点、`setupScene` 末尾按高度失配重建（非调参遮掩，坐标契约核对 `BTTablePhysics.surfaceY=0.8`）。**验证（真实输出）**：`make xcodegen` + build 通过；新增 `S5_TrainingPagesLayoutUITests` **5/5 TEST SUCCEEDED**（8 帧截图逐张核验：P4 布局/黑 8 黑虚线、2D 布局/辅助红点/浮层键盘、3D 无空条、紧凑键盘不遮挡、灰白网格上台呢）。**下一步：新会话执行「问题集合 v3 批次 S6」（补漏项）。**
- **P18 B3.7 问题集合 v3 批次 S4（学习页三项）✅（2026-07-08，SwiftUI Developer）**：**P1 瞄准原理**——P1.1 `AimingFigure` 母球沿瞄准线距假想球 0.24→0.42m、取景 halfHeight 0.30→0.36 / 中心 x 0.50→0.40（数值核验：cue≈(−0.018,−0.084) 全球在框、左缘余量 ≈0.10m，进球线可见段 ≈0.56m——旧取景下基本被裁）、进球线标签 t 0.55→0.35 移入框内、母球标签回球正下方（先试右侧与瞄准线标签相撞，截图打回后修正）；P1.2 G1 口径复核（S3 已接 `AimPointGeometry` 垂足 + 垂线 + 红点，本轮截图确认）；P1.3 名词系统卡末新增金底「常见误区」提示——沿进球线看到的目标球上的点是**接触点**而非瞄准点，瞄着它打会打厚。**P2 浅谈球感**——P2.1 视角差异节改**全宽出血带**（页级水平边距下放各分节自管；2D 图 `aspectRatio(1.4055/0.7995)` 全台恰好占满屏宽，3D 图同宽上下对照；第一版负 padding 方案致溢出，截图打回后改分节边距方案）。**P3 瞄准点对照表**——P3.1 交互卡 220→260 + 图内几何调整（target.y 0.40h→0.44h、进球线延伸 2.4d→1.6d、halfHeight 4.3R→5.0R；数值核验 0° 最坏位标签 y≈19 在框内，旧参数溢出到 −46）；P3.2 实战估角图补**进球线**（目标球→右上角袋绑球色虚线 + 标签，袋口坐标改 `pocketMarkerPositions` 同真源）；P3.3 G1 口径复核（垂线交点=红点、原理说明文案，截图确认）。**测试同步**：`ScreenshotTourUITests` 两处过期卡名「进球点对照表」→「瞄准点对照表」；`testAngleLearningPages` 每页加拍第三帧（页底插图卡）。**验证（真实输出）**：`make build` BUILD SUCCEEDED；`testAngleLearningPages` **TEST SUCCEEDED ×4 轮**（13 帧截图逐张核验：名词图不拥挤/进球线可见/误区提示/2D 图满宽/对照表标签在框内/估角图有进球线）。**下一步：新会话执行「问题集合 v3 批次 S5」（训练页布局与渲染 P4–P7 + G2 网格颜色）。**
- **P18 B3.7 问题集合 v3 批次 S3（瞄准点概念修正 G1 + 瞄准点训练页）✅（2026-07-08，iOS Architect + SwiftUI Developer + Test Engineer）**：**G1 几何唯一真源**——新建 `Core/Geometry/AimPointGeometry.swift`（瞄准点 = 瞄准线与「过目标球心且垂直于瞄准线的直线」交点/垂足；`aimPoint`/`offsetDistance`/`signedOffset` 纯 2D 平面 API，SceneKit XZ 经 `xzPoint` 映射接入）；**文档口径**：`AimingPrincipleView` 名词系统+配图垂线/垂足红点（`AimingFigure` 用 `AimPointGeometry` 算垂足）、`ContactPointTableView` 对照表 G1 标注（水平垂线+红点=交点）。**P8.1–P8.6 瞄准点训练**：`AimPointTrainingView` 加过 1 号球心水平线；假想球去球心红点（`BTGhostCircle(showsAimPoint:false)`）；G1 红色小点 `BTAimPointDot` 标瞄准点；左右切方向修正（向左切=目标球左移⇒母球打右侧，`cutsRight` 口径+提示文案）；特写 `halfHeight 2.7R` 放大目标球；顶栏统计改 `BTReadout` compact 单行（题/均差/历史/剩）。**P9.1 2D/3D 瞄准点训练**：`AimPointSceneQuizViewModel.submit` 误差改 G1 垂足有符号偏移差（非旧假想球心/两交点距离）；辅助线=过目标球心垂直于用户瞄准线白细虚线随瞄准旋转；提交后正确瞄准线红+正确瞄准点红小圆片。**验证（真实输出）**：`make build` BUILD SUCCEEDED；`AimPointGeometryTests` **7/7 TEST SUCCEEDED**（穿球/不穿球交点、d=2R·sinθ、与假想球心差 2R·cosθ、左右切口径、VM 符号约定）；`S3_AimPointUITests` **3/3 TEST SUCCEEDED**（瞄准点训练出题/结果、2D 瞄准点训练瞄准/提交、瞄准原理 G1 配图各 1 帧截图附件）。**遗留（登记 S6/S7）**：3D 瞄准点训练页 G1 垂线/红点截图未单独 UI 测（与 2D 共用 VM，逻辑已对齐）；学习页 P1.1–P1.3/P2/P3 布局项留批次 S4。**下一步：新会话执行「问题集合 v3 批次 S4」（学习页三项）。**
- **P18 B3.7 问题集合 v3 批次 S2（全局规范推广六击打页）✅（2026-07-08，SwiftUI Developer + Test Engineer）**：**六页接入共享布局引擎**——分离角与走位 `ShotSimulationView` / 自由走位 `PositionPlayComposerView` / 批量出片台 `BatchAuthoringView` / 思路训练 `SiluTrainerView` / 打一走二想三 `PlanThreeView` / 做斯诺克 `SnookerTacticsView` 全部改为 `GeometryReader + ShotStageProxy` 定位（G3 轨迹 chip 带区、G4/G5/G7 竖条贴边同底、G6 角落控件齐球桌底线、G8 球库 8 列定宽=球桌宽、G10 顶/底栏定高锁定球桌尺寸、G11 角落/竖条互不遮挡）；`ShotTableLayout.swift` 新增共享修饰器 `btChipBandPlacement` / `btStageFrame` 与通用 `bottomLeadingFrame/bottomTrailingFrame`（避免逐页手调）。**G12 球库瘦身**：思路/三杆/斯诺克三页删除底部 `ShotControlBar` 解摘要行（解读数入口=右柱打点/力度仪表，保留）；三杆保留角色横排。**G9 修订**：思路/三杆内置开球流程（T-P18-47）为既有可用功能，保留**可用**开球按钮不降级禁用（矩阵已改「✓(可用)」并加备注）；开球胶囊图标统一换 `BreakRackGlyph` 同源。**P11.1**：`AngleHomeView.basePlayEntries` 顺序改为分离角与走位→自由走位→**自由击球（第三位）**→拍照建球形（→批量出片台 SIM）。**P12.1 根因修复**：`BatchGuideLine.startPoint/endPoint` 非 `@Published`——选点只改这两属性不改 phase，视图不重渲、「确认」按钮 `hasCurrentPoint` 永远读旧值 ⇒ 补 `@Published`（一行根因修复，非改按钮逻辑）。**验证（真实输出）**：`make build` BUILD SUCCEEDED；`ShotTableLayoutTests` 6/6 TEST SUCCEEDED；`S2_ShotPagesLayoutUITests`（5 页截图 + P11.1 顺序断言）+ `S1_FreePlayLayoutUITests` 回归共 **7/7 TEST SUCCEEDED**；截图逐页目检：chip 贴带区靠最右、双竖条贴边同底、求解/下一解/开球叠左下齐底线、球库与球桌等宽、G12 后底栏无解说明 ✅。**遗留（登记 S6）**：批量出片台为模拟器内容生产工具、需走完整 drill 流程才到编排页，本批仅构建级验证——辅助线「点起点→确认→点终点→确认成线」交互与该页截图核验待 S6 人工/脚本补验。**下一步：新会话执行「问题集合 v3 批次 S3」（瞄准点概念修正 G1 + 瞄准点训练页）。**
- **P18 B3.7 问题集合 v3 批次 S1（全局布局引擎 + 自由击球基准页）✅（2026-07-08，SwiftUI Developer + Test Engineer）**：**共享布局引擎**——新建 `Core/Components/ShotTableLayout.swift`：`ShotTableLayout`（镜像 `CameraRig.fitRotatedTable` 的纯函数，解析求球桌屏幕矩形，可单测）+ `ShotStageMetrics`（控件尺寸常量）+ `ShotStageProxy`（贴边 frame 计算器：刻度轮/仪表柱/开球钮/动作列/轨迹 chip/球库宽，S2 六页直接复用）。**共享组件改造**：`BTBreakSideButton` 三角形内加三圆圈 `BreakRackGlyph`（G9，全局生效）；`BTShotInstrumentColumn` 力度柱移至底部与刻度轮同底对齐（G5）；`BTTextActionButton`/`BTShotActionColumn` 支持宽度参数（46pt 容进右黑边，G6/G11）。**FreePlayView 基准页**：G3 轨迹 chip 下沿贴球桌上沿、靠屏幕最右（用户执行中修订）；G4/G5 双侧竖条贴边+同底对齐；G6 开球钮底边齐球桌底线；G7 竖条 1.2×（用户修订，原方案 1.5× 过长，`maxBarLength=264`）；刻度轮/仪表柱同宽 38pt（用户修订，34/42 取平均，像素核验 116/115px 等宽）；G8 球库排球总宽=球桌宽（8 列定宽居中）；G10 顶栏 46/底栏 94 定高锁定球桌尺寸，stage AX 标识挂 background 层（挂容器会吞子控件 break.entry 可及性——踩坑一次）；P10.1 禁手动摆球（球库只读参考+提示，母球自由球仍可拖）；P10.2 `BilliardRulesEngine.legalTargetKeys` 新协议方法（中八按分组/追分按最小号），非法目标球点击拦截+提示。**验证（真实输出）**：`ShotTableLayoutTests` TEST SUCCEEDED；`S1_FreePlayLayoutUITests` TEST SUCCEEDED（进袋/自由/开球三态截图核验 + G10 三态 frame 零变化断言）；PNG 像素测量：球桌行 span(151,1056) vs 球库 span(154,1051) 等宽 ✅、chip 药丸底 y=534 vs 木框上沿 y=542（间距 ≈2.6pt=预留 padding）贴边 ✅、chip 右缘 x=1181 vs 屏宽 1206（8pt trailing）靠最右 ✅。**下一步：新会话执行「问题集合 v3 批次 S2」（六击打页接入 ShotStageProxy + G12 + P11.1/P12.1）。**
- **P18 B3.7 练习 Tab 问题集合 v3 立项 ⏳（2026-07-08，Orchestrator，纯文档）**：用户提交《问题集合_v2.md》（B3.6 落地后的新一轮反馈，含大量「全局通用」条目）；已整理为**《问题集合_v3.md》**（仓库根目录）作为本轮需求真源——G1–G12 全局规范（瞄准点定义修正、网格颜色、控件贴边/加长/对齐、球桌尺寸锁定、按键互不遮挡、球库瘦身等）+ P1–P14 页面特有项 + G 规范适用矩阵 + **S1–S7 会话批次**（S1 全局布局引擎+自由击球基准 → S2 推广 6 击打页 → S3 瞄准点概念修正 → S4 学习页 → S5 训练页 → S6 补漏 → S7 总验收）。**下一步：新会话执行「问题集合 v3 批次 S1」。**
- **P18 B3.6 练习 Tab 问题集合批（A1–E6 共 24 任务 + 总验收）全收官 ✅（2026-07-06/07，Orchestrator 调度多角色）**：需求真源=用户《问题集合.md》25 条（与 SPEC §9 冲突处以问题集合为准，条 25），计划=`~/.cursor/plans/练习tab问题集合落地_9ed76669.plan.md`，逐任务单独 todo + build + 截图/测试验收。**Batch A 地基**：线语言 v2（进球线/全部击后轨迹改虚线、假想球心红点、去选中圈）+ `BTTrajectoryDetailChip` 三档标注全页接入；控件瘦身（刻度轮纯相对、力度柱 0.5–8.0 非线性 γ=1.8 默认 1.5、打点盘点外关闭）；布局规范 v2（条 18 最高优先：仪表柱齐角袋、右侧 `BTShotActionColumn` 击球/上一杆/回放、左侧 `BTBreakSideButton`）；4×8 网格入设置；地面纯黑+瞄准页发灰白收敛；`pocketNoseRestitution` 0.60→0.70（诊断测试标定）；α→θ 全局。**Batch B/C 学练页**：瞄准原理/浅谈球感/瞄准点对照表（改名）三页生产级重组；角度与打点加球库、角度预测键盘修复、2D/3D 瞄准训练→**角度训练**（随机球号/渲染修正）。**Batch D 新页三张**：瞄准点训练（拖假想球+mm 误差）、2D/3D 瞄准点训练（辅助线交点误差+自动击球）。**Batch E 走位对局**：编排台→**自由走位**（去开球录制、进袋/自由单钮）；**自由击球**新页（开球状态机+中八/追分完整规则引擎 `BilliardRulesEngine`，调研文档入 docs/research，`BilliardRulesEngineTests` 全绿）；分离角与走位换底座限 2 目标球；批量出片台点换+辅助线；思路训练器→**思路训练**+三解页同规范（求解后微调/上一杆/回放/菜单合并）。**总验收（真实输出）**：`QiuJiTests` **454/454（2 skip，0 failures，TEST SUCCEEDED）**；`testB2ShotControls` TEST SUCCEEDED 截图逐张核验；问题集合 1–25 逐条验收清单 ✅ 落 P18 phase 文件 §B3.6（条 24 翻袋/反射未触碰确认）。SPEC 回写：§9.1/9.2/9.3/§8.8 改写 + Changelog 6 条。**下一步：B4 观感反馈或按用户指示；真机手感项挂 H-5。**
- **P18 B3.5 T-P18-52 设计稿定稿入 SPEC §9 ✅ + B3.5 批 12 任务全收官（2026-07-05，Orchestrator）**：SPEC 新增「九、练习体验品牌设计定稿」四小节——§9.1 设计语言（五签名元素+代码唯一真源索引：`TrajectoryStyle`/场景层重叠标注/`BTReadout`/`BTAimWheel`·`BTShotInstrumentColumn`/`HUDStyle`；信号色绿金白红四通道封闭）、§9.2 HUD 七分区+铁律三条、§9.3 逐页契约（16 页现行形态+全局项）、§9.4 指向 §8.7 单点维护；纯文档任务无代码改动。**B3.5 品牌统一设计批（T-P18-41~52）12 任务全部 ✅**：语言地基（41 线语言/45 仪表玻璃/42 重叠标注）→ 交互重做（43 自由瞄准/44 仪表柱）→ 页面重构（46 学练真台化/47 开球内置/48 拆两卡）→ 收口（49 三杆下移/50 翻袋+术语/51 学练导流）→ 52 定稿。**下一步：B4 观感反馈（落袋下沉+音效接线 T-P18-16+放大镜）或按用户指示；真机手感项挂 T-P18-H5。**
- **P18 B3.5 T-P18-51 学→练导流 + 拍照送入三目的地 + 首拖提示 ✅（2026-07-05，SwiftUI Developer）**：学→练 CTA 三条——新增 `PracticeCTA` 共享导流卡（`NavigationLink(value: AngleRoute)`）：瞄准原理页末「去练一练→角度预测」、浅谈球感页末「用真台验证→2D 瞄准训练」、角度预测结果卡常驻「去真台练→2D 瞄准训练」+ 偏差较大时补「回看原理→瞄准原理」；拍照建球形——送入目的地扩三处（编排台/思路/**打一走二想三**，`PlanThreeView(initialBoard:)` 既有 API 直挂），底栏两 primary 钮收成「送入…」菜单，步骤指示补「第 n 步 / 共 4 步 · 步骤名」（选图步也上 stepHeader）；角度与打点——首拖提示（`@AppStorage` 跨启动记忆，`onDragBegan` 置位后让位袋口提示）。验证：build ✅；新增 `testLearnPracticeFlow` TEST SUCCEEDED（6 帧截图核验：CTA 卡/导航到达/结果卡双链/首拖 banner/步骤指示）。**下一任务：T-P18-52（设计稿定稿入 SPEC §9 + Changelog，B3.5 收官）。**
- **P18 B3.5 T-P18-50 翻袋顶部重排 + 下一解 + 术语扫替 + 词表落 SPEC ✅（2026-07-05，SwiftUI Developer）**：翻袋顶部重排——袋口 chip 行删除、袋口改台面直点选定（高亮圈），顶部只留「自动/1库/2库/3库 + 理想/真实〔+力度〕」两行无截断（§8.4）；「下一解」FAB 对齐反射页（`nextSolution` 多解切换）；术语扫替——用户可见字符串统一「切角」（弃切球/切球角，含 `ShotPredictor`/`AngleDynamicViewModel` 不可行文案）、「假想球」（弃幽灵球）、「力度」（弃发力，`ReflectionModeControl` 标签），页名=卡名（「角度预测」「翻袋解球器」导航题与入口卡逐字一致，历史筛选/HistoryViewModel 同步）；词表落 SPEC §8.8（11 条规范术语+禁用别名，新增用户可见字符串必须查表）+ §8.7 翻袋行「切球」笔误修正。验证：build ✅；`testB3PlusGate` + P5 全套 TEST SUCCEEDED（两处过期断言随台词更新：对照表提示语、自由击球默认标题「走位编排台」），翻袋理想/真实两帧截图核验（两行顶栏+解 pill「切角 14°/15°」）。**下一任务：T-P18-51（学→练 CTA 导流 + 答错回原理 + 拍照送入步骤指示）。**
- **P18 B3.5 T-P18-49 三杆角色下移 + 求解三态核验 + 失误去重 + sheet 暗材质 + 统计 chip 加字 ✅（2026-07-05，SwiftUI Developer）**：`PlanThreeView` 右侧竖排角色栏删除、台面全宽，Z6 球库行上方新增横排 `roleRow`（①球→①袋→②球→②袋→③球+清空，VM 提示「右侧」→「下方」）；三解页求解三态核验为 B2 期已具备（就绪 hint/求解中/解 n·N/未找到解），零改动确认与契约一致；编排台 `makeStatus` 删「母球进袋（失误）」重复文案，失误由 Z2 红 pill 唯一承担；翻袋/反射原理 sheet 补 `preferredColorScheme(.dark)`（练习 Tab 浅色 sheet 清零）+ info 钮 AX label；瞄准训练进度 pill SF 图标换单字 label（题/袋/差/剩）。验证：build ✅；`testB2ShotControls` + `testScenePopups`（新增 p03b 原理 sheet 暗材质帧）TEST SUCCEEDED 截图核验。**下一任务：T-P18-50（翻袋顶部重排 + 术语扫替 + 词表落 SPEC）。**
- **P18 B3.5 T-P18-48 2D/3D 拆两卡 + 瞄准训练入口流程 ✅（2026-07-05，SwiftUI Developer）**：拆两卡——`AngleRoute.sceneAiming`→`sceneAiming2D/3D`，`SceneAimingView(initialCameraMode:)` 参数化（视角固定、页内 toggle 删除、标题「2D/3D 瞄准训练」），练分段入口卡×2；成绩分记 `quizTypeLabel` 按 route 固定，历史筛选标签对齐新卡名；入口流程——`setupScene(autoStart:false)` 进页先弹完整训练设置 sheet（`preferredColorScheme(.dark)` 治浮出层浅色违例，「开始训练」金主钮，滑关兜底默认开题），训练中齿轮可换；辅助线接线——`updateVisualization` 拆 `showOverlapMarkers`（接触点+90° 品牌绿短虚线）/`showAngleAnnotations`（数值弧）两开关，辅助档保留 L1 重叠标注仅隐藏数值。验证：build ✅；`testB3PlusGate`（2D/辅助档/3D 三帧）+ `testUnifiedDesignPages`（暗 sheet+2D）均 TEST SUCCEEDED，截图逐张核验。**下一任务：T-P18-49（三杆角色下移 + 求解三态 + sheet 暗材质收尾）。**
- **P18 B3.5 T-P18-47 开球内置三宿主 + 球形生成器页下线 ✅（2026-07-05，SwiftUI Developer）**：新建 `Core/Rack/BreakFlowRunner`（生成器 VM「摆架→开球区拖母球→BreakSimulator 真实散局→就地落座」闭环下沉为可嵌入 runner，seed 换局+塞扰动/刮杆补回/`onSettled` 交付宿主）+ 共享 UI（`BreakGamePickerSheet` 暗材质玩法选择〔中八/9/6/5/4〕、`BreakControlBar`、`BreakEntryTile` 球库行首入口）。编排台/思路/三杆三宿主接入：进开球模式自存快照、挂起求解/约束/角色/可视化，散局落座为新真相，取消恢复进场前球形。生成器页下线：路由/入口卡/页面文件/pbxproj 删净，UI 测试改为内置开球流程截图。验证：build ✅；`testB2ShotControls` TEST SUCCEEDED（sheet/摆架/散局落座三连拍+思路三杆摆架帧+取消恢复）；`RackGeneratorTests`+`BreakRackPhysicsTests`（core 不动）TEST SUCCEEDED。**下一任务：T-P18-48（2D/3D 拆两卡 + 瞄准训练入口流程）。**
- **P18 B3.5 T-P18-46 学练四页真台化 ✅（2026-07-05，SwiftUI Developer）**：新建共享基建 `TableFigureRenderer`（真实 USDZ 空台离屏渲一次缓存底图，全台/特写两种取景，正交映射钉死坐标契约）+ `BTTableFigure`/`TableFigureProjection`（世界米坐标摆球画线，球径/线宽与场景同物理尺度）+ `FigureLine`（SwiftUI 侧从 `TrajectoryStyle` 同源取色）+ `BTFigureBall`（PoolBallFace 真球面）/`BTGhostCircle`/`BTContactDot`/`BTFigureTag`。四页换装：瞄准原理（切角全景半台特写含真实角袋+§1.2 全线语言+术语扫替）、球感（锚点卡+2D 对比真台化）、进球点对照表（抽象圆盘重设计为俯视真台交互图：瞄准点+接触点+横移 d 金标尺同见）、角度预测（题面真台化+补 90° 参考线+修 75° 裁切+确认钮禁用态走 §1.7）。验证：build ✅；`testAngleLearningPages`（扩四页+滚动+参考线帧）TEST SUCCEEDED，10 张截图逐张核验同源。**下一任务：T-P18-47（开球内置 + 生成器页下线）。**
- **P18 B3.5 T-P18-44 ShotControlBar v2 + 仪表柱 ✅（2026-07-05，SwiftUI Developer）**：新建 `BTShotInstrumentColumn`（右缘竖直仪表柱：打点盘置顶 + 力度柱〔三级刻度/暗调渐变水位/金档位线/0.1 detent〕+ BTReadout 金读数）；瞄准轮移左缘两根尺子分居两侧；分离角/编排台/批量出片台/生成器换装，底部控制行删除；`ShotControlBar` 删 editable 形态（B 类三页只留解读数+试打）。验证：build ✅；`testB2ShotControls` 浅色+Dark 各 TEST SUCCEEDED；**43/44 分段验收门 ✅**。**下一任务：T-P18-46（学练四页真台化）。**
- **P18 B3.5 T-P18-43 自由瞄准重做 ✅（2026-07-05，SwiftUI Developer）**：粗调=手指跟随（`AngleSceneView` 删手柄 44pt 命中，pan 起手未命中球即 `onAimDragged` 逐帧指向手指；球命中优先移球不变）；瞄准线手柄圆环删净（场景节点 `setupAimHandle/updateAimHandle` 整体删除、两 VM 删摆位段、死 API `rayDistanceToCushion` 连删）；`BTAimWheel` 去数值只留 1°/5°/10° 三级刻度（`HUDStyle.tickColor` 新助手）+ 金指示线；批量出片台经共享组件自动生效。验证：build ✅；`testB2ShotControls` TEST SUCCEEDED 10 张核验。⚠️ 真机手感挂 T-P18-H5。**下一任务：T-P18-44（ShotControlBar v2）。**
- **P18 B3.5 品牌统一设计 🔄 语言地基三件套齐（41 线语言 ✅ + 45 HUD 仪表玻璃 ✅ + 42 重叠标注 ✅，2026-07-04/05，SwiftUI Developer）**：**T-P18-42**：`ghostBallNode` 场景层单点重建为品牌绿虚线圈（16 段贴台呢，API 不变全页自动换装）+ `updateContactDot` 共享助手，8 处接线补齐「圈+点成对」（含斯诺克解出后首次显示 L0、导出器同源）；配档表+理由落 SPEC §8.7。**过程中揪出 P0（FL-024）**：`addDashedPath` 浮点相位推进死循环（潜伏自 B2）致 `testB2ShotControls` 三连挂，`sample` 采样定位后重写为整数周期索引算法，教训回写 geometry SKILL。验证：build ✅；B3+ 门与 B2 门复跑 Passed，裁剪核验绿虚圈+绿点；两序列重出片核验导出同源。**下一任务：T-P18-43（自由瞄准重做）。**
- **P18 B3.5 前两任务（41 线语言 + 45 HUD 仪表玻璃，2026-07-04 夜，SwiftUI Developer）**：B3.5 批已立卡（12 任务，见 P18 卡）。**T-P18-41**：`TrajectoryStyle` 扩展为线语言唯一真源（线色=球的身份 / 线型=信息性质，金退出轨迹专属方案标记），8 场景页 + 导出器收编，两序列重出片核验。**T-P18-45**：新建 `HUDStyle.swift`（仪表玻璃唯一真源：黑 60% 玻璃+发丝描边**无阴影**、文字三级、value 白测量/金可调/红失误三通道、chip 状态语法、刻度三级语法）+ `View.btHudGlass(in:)` + 新建 `BTReadout` 读数胶囊组件；8 页 HUD 读数换装（弃 ultraThinMaterial+shadow）、`BTChipRow`/`BTSceneFAB`/`BTAimWheel`/`BTSpinPadCard`/`ShotControlBar` 收编、等宽数字扫齐、导出 `ShotHUDView` 金量值同款。**验证（真实输出）**：build ✅；`testB3PlusGate`+`testScenePopups` Passed 18 张截图逐张核验；两序列重出片 TEST SUCCEEDED 静帧核验。SPEC Changelog 已记 3 条。**下一任务：T-P18-42（重叠标注三档组件化）。**
- **球迹练习体验统一设计 v4（品牌设计语言 + 逐页目标形态）✅（2026-07-04 夜，Orchestrator，纯文档待用户过目）**：四轮迭代定稿——v1 契约审计（被否）→ v2 结论式逐页设计 → v3 品牌设计语言「教练仪表盘」→ **v4 并入用户 13 条设计裁决**。`docs/research/20260704-练习Tab功能契约梳理.md`：①线语言 7 token（瞄准线白固定、**进球线绑定目标球本色**〔推翻 v3 橙红方案〕、球迹线金、90° 分离角白短虚线、两档线宽），**App+渲染管线（SequenceVideoExporter 图/视频的打点/力度/轨迹）同源**；②重叠标注三档 L0/L1/L2（L0 假想球圈+接触点全瞄准场景常驻，潜移默化教「什么角度打哪里」；L2 全指标仅角度与打点页）；③自由瞄准重做（手指跟随粗调+无数值刻度轮细调+删手柄圆环）；④ShotControlBar v2（右缘竖直力度柱：离散档+克制渐变色+打点盘置顶）；⑤学练四页真台化（原理/球感/对照表俯视重设计/角度预测补 90° 修 75° 裁切）；⑥**球形生成器页下线**，开球内置编排台/思路/三杆（core 复用，斯诺克不加）；⑦三杆角色下移底部横排；⑧2D/3D 拆两卡+瞄准训练先弹设置再开始；⑨HUD 七分区+Z3a 贴缘仪表柱。任务 T-P18-41~52（M/L 为主，**量级超 P18 追加批，建议独立成批 B3.5 或新 Phase**），排序：语言地基（41/45/42）→交互重做（43/44）→页面重构（46/47/48）→收口（49/50/51）→SPEC 定稿（52）。**下一步：用户过目 §5 十二条结论 + 确认成批方式后开工。**
- **P18 B3 风格收口 + B3+ 截图审查追加项 ✅（2026-07-04，Orchestrator 调度 SwiftUI Developer）**：**B3（T-P18-11~14）**：组件归位（`ReflectionModeControl`→`Core/Components`）；单一真源清理（`DrillSpinIndicator`→`BTSpinMiniIcon(trueScale:)`、删 `StrokePhysics.velocity(forPower:)` 死 API、力度量程收口 `ShotTuning.velocityRange` 六处引用）；明暗三档策略+瞄准辅助显示矩阵写入 SPEC §8.1/8.2，`GeometricAngleQuizView` 按钮对齐场景绿胶囊；`BTIcon` 第一批迁移（5 文件）+ `Image.btSymbol` 封装。**B3+（T-P18-31~39，40 降 v1.x）**：SPEC 新增 §8.3 导航栏规范 / §8.4 顶部区≤2 行硬规范 / §8.5 色彩语义；拍照建球形绿标题、思路/三杆/斯诺克齿轮-省略号菜单拆分、翻袋/反射顶部压 2 行+方案 pill 浮层、入口副标题去歧义、**2D/3D 瞄准合并一卡**（`SceneAimingView` 默认 2D 可切 3D，`Scene2DAimingView` 删除）、球形生成器「送入…」菜单、编排台默认标题「走位编排台」；两个视觉 bug 根治：3D 页黑带（地面渐变纹理融入背景）、轨迹文字倒置（近竖直线 yaw 翻转按 dir.x 判定）。**验证（真实输出）**：`make build` ✅；`testB3StyleGate` 11 张 + `testB3PlusGate` 13 张 + Dark Mode 复跑均 TEST SUCCEEDED，截图逐张核验通过（结论在 P18 卡）。**下一批：B4 观感反馈（落袋下沉+音效接线+放大镜）。**
- **「角度」Tab 改名「练习」+ 首页改动作库式布局 ✅（2026-07-03，SwiftUI Developer，DR-020）**：用户驱动——「角度」已装不下物理沙盘/反解工具/球理知识，拍板改名「练习」；布局对齐动作库 `DrillListView`：左侧 76pt 图标分类侧栏（全部/学/练/打/解）+ 右侧双列分组网格（钉住分组头），卡片改 `BTDrillGridCard` 同款上图下文式 `AngleGridCard`（封面保留渐变大字水印语言）。四分类 IA（ADR-P18-01）不变仅呈现方式变；`AppTab` icon `angle`→`scope`；侧栏沿用 `angleHomeTab_*` AX 标识故 UI 测试选择器零改动，仅同步 Tab 名三处。**验证（真实输出）**：`make build` ✅；`P5_AngleTrainingUITests` 8/8 + `testUnifiedDesignPages` 全绿；明/暗 × 5 分类截图逐张核验（暗色副标题截断当场修复后复验通过）。同日追加动作库同款搜索框（占位「搜索练习」，标题/副标题过滤 + 只留命中分组 + `BTEmptyState` 空态清空动作）——build ✅ + 新增 2 条搜索 UI 测试全绿 + 空闲/命中/空态截图核验。DR-020 已落 IMPLEMENTATION-LOG + SPEC Changelog。
- **P18 B2 统一击球交互（手动瞄准 + 自由击球 + ShotControlBar）✅（2026-07-03，Orchestrator 调度 SwiftUI Developer，ADR-P18-02）**：WP-A 六任务全落地。**T-P18-05 组件下沉**：`BTAimWheel`+`ThicknessOverlapIcon` 从批量出片台（sim-only）抽 `Core/Components/BTAimWheel.swift` 共享。**T-P18-06 瞄准手柄**：`AngleSceneView.onAimHandleDragged` + 场景手柄节点（命中优先于拖球）；纯几何 `AngleSceneCalculator.freeAimFirstContact` 逐帧驱动 ghost 贴目标球滑动 + 首碰读数。**T-P18-07/08 编排台自由模式成完整形态**：贴桌右缘角度齿轮 + 手柄拖动 + 首碰胶囊（厚度重叠图示+切角+厚度名+碰球号）+ `extraBallPaths` 碰后方向预览；「自由击球」入口卡（B1）直达即此形态；**B 类三页（思路/打一走二想三/做斯诺克）加「试打」**——`ShotTryFreePlayButton` 带 `currentSnapshot()` 跳 `PositionPlayComposerView(initialBoard:initialMode:.free)`，反解看思路→带球局亲手试打闭环。**T-P18-09 分离角手动瞄准**：「自动/手动」chip；手动分支 `recomputeManual()`=`simulateFree` 如实模拟（进不进如实展示）+ 自动解按球局缓存画虚线对照；`bearingDeg/rotatedAim` 几何换算收口 `AngleSceneCalculator` 单一真源。**T-P18-10 ShotControlBar**：`Core/Components/ShotControlBar.swift` editable（A 类：分离角/编排台/球形生成器）/readOnly（B 类三页）两形态 + trailing 槽位；Scene3D 自绘 64pt FAB 删除换 `BTSceneFAB`（B3 T-P18-11 对应项提前完成）。**验证（真实输出）**：`make build` ✅；`ScreenshotTourUITests` 10/10（含新增 `testB2ShotControls` 专项巡游）+ `P5_AngleTrainingUITests` 8/8 全绿（19min）；UI 美观性验收 b2-01…07 十张截图逐张核验通过（结论在 P18 卡）；`QiuJiTests` **437/437（2 skip，0 failures，TEST SUCCEEDED）** ✅。ADR-P18-02 落 P18 卡。**下一批：B3 风格收口（T-P18-12 单一真源清理起；T-P18-11 组件归位剩 `BTChipRow`/`ReflectionModeControl` 移 Core）。**
- **P18 B1 角度 Tab IA 四分段重排 + 孤儿页清理 ✅（2026-07-03，Orchestrator 调度 SwiftUI Developer，ADR-P18-01）**：`AngleHomeView` 三分段（学习/训练/工具）→ **四分段（学/练/打/解，按用户目的划分）**——学=球理知识 4 卡、练=直觉测验 3 卡、打=物理沙盘 5 卡（+SIM 批量出片台）、解=引擎反解 5 卡，副标题全部重写为「一句话说清干什么」；新增 `AngleRoute.freePlay`「自由击球」卡直达 `PositionPlayComposerView(initialMode:.free)`（新增 `initialMode` 参数，B2 补手动瞄准后成完整形态）；「球理」入口卡只留代码注释位不渲染（P12 落地后追加）。**孤儿页删除**：`AngleTestView`+`AngleTestViewModel`+`BTAngleTestTable`+`BTBilliardTable`、`SceneAnglePredictionView`+`SceneAngleViewModel`、`AngleTestViewModelTests`、`AngleHistoryView` wrapper（文件改名 `AngleHistorySection.swift`）、死路由 `HistoryRoute.statistics`。**测试同步**：`P5_AngleTrainingUITests` 从旧三卡断言重写为四分段冒烟（8 用例）；`ScreenshotTourUITests` 分段标识全量同步（工具→打/解拆分，u02b/u02c 新增）。**验证（真实输出）**：`make build` ✅；`QiuJiTests` 437/437（2 skip）✅；`P5_AngleTrainingUITests` 8/8 ✅；`ScreenshotTourUITests` 全类 ✅；UI 美观性验收 4 分段 × 明/暗 8 张逐张核验通过（结论在 P18 卡）。ADR-P18-01 落 P18 卡。**下一批：B2（T-P18-05 组件下沉起）。**
- **P18 发布收敛 Phase 立卡 ✅（2026-07-03，Orchestrator）**：`docs/research/20260703-发布前系统优化方案.md`（v2 细化稿）D1–D14 用户全部拍板按建议执行；新建 `tasks/phases/P18-release-convergence.md`——v1.0 发布线收敛为 **7 批**：B0 人工并行（**H-19 App 备案立即启动**/H-18 音效/H-09 隐私政策/TP-P7/ADR-P10-09 手感验收）→ B1 角度 Tab IA 四分段重排+孤儿页清理 → B2 统一击球交互（手动瞄准/自由击球模式/ShotControlBar）→ B3 风格收口 → B4 观感反馈（落袋下沉/音效接线/放大镜）→ B5 合规与产品（占位链接/PIPL/试一杆/通知/埋点/iPhone-only/权益审计）→ B6 内容（标注 v2/精选视频回填）→ B7 发布闸门。**全局硬约束（用户强调）**：所有 UI 修改必须模拟器实测+逐张截图核验美观性，结论记入 P18 卡。v1.x 项（WP-B 翻袋解球引擎化、斯诺克 v2 等）发布后另立卡。新增 `HUMAN-REQUIRED.md` H-19（ICP 备案，lead time 1–4 周）。**下一任务：B1 T-P18-01。**
- **P10 真实袋口重建 — CAD 单一真源 + 纯几何落袋判据 ✅（2026-07-02，iOS Architect，ADR-P10-09）**：承接用户三项反馈「角袋先吃库边再吃 jaw / 中袋瞄准点不对 / 球有被吸进袋的感觉」+ 拍板「球心与袋心距离小于袋口半径即落袋，其余全按真实物理」。**几何**：袋口全面回归 CAD——孔心/孔半径（角 (±1.312,±0.677) r=0.042、中 (0,±0.688) r=0.043）、中袋按双切关系重建（R30 弧心 (±0.073,±0.665)+喉壁 x=±0.043，替换错误的 10mm 窄缝实现）、角袋喉墙改 jaw 共线孪生墙+切线延长衬套（不再越 jaw 平面截击轨迹=「先吃库边」根因）；`pocketPositions` 归 CAD 孔心（物理/瞄准真源）、新增 `pocketMarkerPositions`（USDZ 视觉标记）。**判据**：`resolvePocket` 单判「球心入孔圈」，删 `pocketCoreMissRadius`/`pocketDropSpeed`/`jawSettlePocketSpeed` 全部吸球机制；`enforceTableBounds` 袋口通道无条件放行（挂袋合法）。**根治两个被大捕获圆掩盖的引擎缺陷**：①`QuarticSolver` 近双二次塌缩漏根（浮点大项抵消致 q 极小，Ferrari 预解三次失败→球穿弧）→ 双二次播种+Newton 抛光兜底；②弧 CCD `epsilon=1e-4` 过大（直线库 1e-6），高保真近墙子步逼近后剩余 ~2e-5s 的真实碰撞根被拒→穿弧越界（矩阵 4 例失败根因）→ 统一 1e-6。**测试口径**：65° 大切角 v3.3 实测真实双吻（弹库折返二次接触，v2.0 直进），断言放宽「进袋或≥2次球球接触」；fastPath/predict 停点比对加刀锋景观敏感度自测门（±0.02° 扰动即 >0.1m 移位则跳过）。**新增 3 条不变量**（`PhysicsInvariantTests`）：入圈即袋双向（含反向禁吸入）/挂袋合法/袋口通道轴线无隐形墙。**验证**：`PhysicsMatrixTests`（出界 9→0）/`PhysicsEngineTests`（**含此前 ⚠️ 待排查的 3 个预存失败一并转绿**）/`PhysicsInvariantTests` 12/12/`PhysicsScenarioTests`/`CushionReflectionTests`/`PocketBehaviorDiagTests`/`PositionPlaySolverTests` 全过。改：`BTPhysicsConstants`/`TableGeometry(+QiuJi)`/`EventDrivenEngine`/`EngineNumerics`/`QuarticSolver`/`CollisionDetector`/`AngleSceneCalculator`/`ShotPredictor`/`TrajectoryPlayback`/`AngleTrainingScene`/`AngleSceneView` + 测试 4 文件 + 新诊断 `PocketRefactorDiagTests`。ADR-P10-09 见 `tasks/phases/P10-physics-content-pipeline.md`。**⏳ 待人工**：真机/模拟器实打验证角袋不再先吃库、中袋空心进与 jaw 反弹手感、无吸球感。
- **3D 球桌腿缺失根因修复（USDZ 资产外科手术）✅（2026-07-02，iOS Architect，FL-023）**：承接「3D 导出/App 内 3D 球桌没有腿」——推翻前两轮灯光/相机误诊，坐实双层根因：①2026-02-27 项目 01 Blender 合并重导出把腿子集三角形 dissolve 成巨型 n-gon（最大 256 边）；②SceneKit USDZ 导入器对含 ≥256 顶点面的 GeomSubset **整个静默丢弃**（最小复现：255 边可/256 丢）→ MG_Gold 腿子集 56,944 面不渲染；孤立顶点撑着包围盒到地面 → 误导「几何在」。**修复**：usd-core `Sdf.CopySpec` 把 01_backup 旧版纯三角腿 prim `/root/TaiQiuZhuo_007`（125,984 三角）嫁接进当前模型 + `UsdUtils.CreateNewUsdzPackage` 重打包（**禁走 SceneKit `write(to:)` 往返**——实测纯色被二次伽马漂移 0.801→0.957），新白球贴图/红点 UV 零改动，替换 `QiuJi/Resources/TaiQiuZhuo.usdz`（89.7→98.3MB）。**验证**：`TableLegGeometryDiagTests` 真实管线 TEST SUCCEEDED，侧视三铜腿完整落地、导出相机黑底下铜腿可见；回读材质 rgb 与旧版逐位一致。FL-023 已记 + 回写 `20-swiftui-developer.mdc`。**⏳ 待人工**：真机/模拟器看 3D 模式与导出视频腿部观感；既有已导出视频（drill_c042 等）如需带腿需重新出片。
- **批量出片台（drill 截图 → 走位序列 → 教学素材，仅模拟器内容工具）✅（2026-06-20，iOS Architect + SwiftUI Developer）**：承接「62 个 drill 图片→视频管线，加载/保存自动化」。**(1) 三件套** `QiuJi/Features/BatchDrillStudio/`（全 `#if targetEnvironment(simulator)`）：`BatchDrillStudioCore`（`BatchDrillCatalog` 扫 `15.tutorial_video/shooterpools录制/drill_NNN/` 截图、`drill_NNN→drill_cNNN` 映射、读 `index.json` 分类与内容库已存序列状态、未登记给警告；`BatchSequenceArchive` 按 **drill_cNNN 稳定键**直写 `content/position_play/sequences/`〔区别编排台随机 `seq_<id8>`，文件名 `drill_cNNN-<名称>-<N>杆.json`，便于出片接精讲页〕；`BatchAuthoringContext` 跨步上下文 + `advanceToNextUnsaved`；`UIImage.rotated90Clockwise` 横屏截图旋正）；`BatchBallExtractionView`（复用 `BallExtractionViewModel` 标定/标球/确认全逻辑，仅图源从 `PhotosPicker` 换 drill 文件夹缩略图网格 + 顺时针旋 90°；`onChange(current)` 在「保存并下一个」dismiss 回来时重置到选图步）；`BatchAuthoringView`（**编排+求解二合一**：引擎复用已验证的 `PositionPlayViewModel` 录制状态机；反解层 `BatchShotSolver` 复用 `PositionPlaySolver` 画落区/落点/过点→后台反解→把最优解的塞/力度写回编排参数〔target/pocket 已选中〕，默认「落区求解」、可切「摆球」调整导入球形〔拖球调位 / 球库拖入增球、拖回删球 + 手动设打点力度〕；自动录制、「保存」/「保存并下一个」按 drill_cNNN 落库并迭代）。**(2026-06-21 追加：一个 drill 多球形)** 语义拍板「每张截图 = 一个独立球形：同图重存覆盖、异图并存」。归档键由 drill 级下沉到 **drill × 图片** 级——`BatchSequenceArchive.archive(_:drillId:imageStem:)` 文件名改 `drill_cNNN__<token>-<名>-<N>杆.json`（`token` = 截图名经 `formationToken` 去「-」等分隔符派生，因截图名形如 `Snipaste_2026-05-25_22-28-27` 含大量「-」必须替换才能可靠回解析）；覆盖只删同 `drill_cNNN__<token>-`；**旧版单序列 `drill_cNNN-…`（无 `__`）一律不动**（算该 drill 既有的一个球形，出片正常，淘汰由人工删——去掉了原本写新球形时静默删旧的逻辑以防误删既有成果）。既有 5 个旧文件 c001~c005 因此安全保留。`BatchDrill.hasSavedSequence` 改存 `savedStems:Set` + 计算 `≥1`/`savedFormationCount`；catalog `loadSavedFormationMap` 解析新/旧两式。迭代改两按钮：「保存·选下张图」（存→回本 drill 选图栅格挑下一张，**跳过=不选、零副作用**：出片是 JSON 驱动，没存的图不产 JSON）+「保存·下个drill」（存→跳下一个零球形 drill，靠 `pickerResetToken`/drillId onChange 重置选图页）；选图栅格已存图打绿勾。出片 runner 对非 `seq_` 文件用整段文件名作产物目录 ⇒ 各球形天然分目录不撞，无需改 runner。验证 `make build` BUILD SUCCEEDED、lint 0。改：`BatchDrillStudioCore.swift`/`BatchBallExtractionView.swift`/`BatchAuthoringView.swift`。**⏳ 待人工**：模拟器走查多图 drill 逐图建球形/同图覆盖/异图并存/跳过无影响/绿勾。 ——以下为上一轮记录—— 工具行由「手动/落区/落点/过点」改为对齐思路训练器的「落区/落点/过点/**摆球**」——因导出球形进编排求解台后常需调整：`BatchShotSolver.Tool` 的 `.manual` 改为 `.arrange`（新增 `isSolvingTool` 计算属性收口绘制层覆盖/禁拖/求解控件三处条件），摆球模式关绘制覆盖层 + 启用 `draggableBalls` + 球库增删，控制行常驻故仍可手动设打点力度点「击球」。**(2) 接线**：`AngleRoute.batchDrillStudio`（unconditional 枚举）+ `MainTabView`（`#if simulator` 映射 `BatchDrillStudioView()`、`#else EmptyView()`）+ `AngleHomeView`（`toolEntries` 改计算属性，simulator-only 追加海报卡「批」chip「SIM」）。**(3) 出片分辨率上调**（治放大模糊）：`SequenceVideoExporter` teaching 720×1280→1440×2560、card 640×320→1280×640、gif 480×854→720×1280、3D 高分档→4K 2160×3840。**(4) 4K 内存修复**：`renderFrames` 逐帧 `snapshot()` 包 `autoreleasepool`（治高分档 SIGKILL）。**验证**：`xcodegen generate` 重生 + 模拟器 `xcodebuild ... BUILD SUCCEEDED`（新文件全编译、设备路径 #if 守护、仅 `MainTabView` 一处引用在 simulator 块内）；分辨率轮已 `make position-export` 端到端验证内存修复。改/新增：`QiuJi/Features/BatchDrillStudio/{BatchDrillStudioCore,BatchBallExtractionView,BatchAuthoringView}.swift`(新)、`QiuJi/App/MainTabView.swift`、`QiuJi/Features/AngleTraining/Views/AngleHomeView.swift`、`QiuJi/Core/Media/SequenceVideoExporter.swift`、`QiuJiTests/PositionPlaySequenceExportRunnerTests.swift`、`content/position_play/README.md`、content-engineering SKILL。**⏳ 待人工**：模拟器实跑批量台逐个 drill「选图→旋 90°→标定建球形→编排/求解→保存 drill_cNNN→出片」走查（旋转方向是否正确、标定/求解质量、`drill_cNNN-*.json` 落库后 `make position-export` 出片并接入精讲页）。
- **P12 视频缩放（精讲 clip + 详情页示范视频）✅（2026-06-18，SwiftUI Developer，ADR-P12-02 后续）**：用户走查发现「视频示范点进去不支持放大缩小」。把图片缩放逻辑抽成**通用可复用** `ZoomableContainer<Content>`（捏合/双击放大 + 放大后平移 + 可选下滑关闭，内容类型无关），统一三处：精讲静态图、精讲 clip（`LoopingPlayerView`）、详情页示范视频（`DrillVideoPlayerSheet` 的 `VideoPlayer`，保留系统播放控件、缩放不拦截 scrubber、关闭走 sheet+关闭钮故不开 swipeToDismiss）。`ZoomableContainer` 设 internal 供 `DrillDetailView` 跨文件复用。验证：`make build` BUILD SUCCEEDED、lint 0。改：`QiuJi/Features/DrillLibrary/Views/{DrillTutorialView,DrillDetailView}.swift`、`tasks/phases/P12-content-system-theory.md`。**⏳ 待人工**：真机验证缩放与视频控件/翻页/下滑关闭共存无冲突。
- **P12 多球形精讲隔离 + 精讲全屏看图 + 配图 PNG/动态选用 ✅（2026-06-18，SwiftUI Developer，ADR-P12-02）**：用户反馈精讲页两问题——①一个 drill 可能含**多个球形**，各自精讲文字需隔离（视频按序排/gif 拼接即可，唯精讲不能混在一条滚动里）；②配图嵌卡片太小，要点击全屏看。选项式逐项拍板后落地（全部加法式、向后兼容，70+ 旧 drill 零改动）：①`DrillTutorial` 加可选 `formations:[{id,title,sections}]` 与 `sections` 二选一 + 新 `TutorialFormation`；`TutorialSection` 加 `clip`（mp4 动态片段）；`DrillContentService.tutorialClipURL`。②`DrillTutorialView` 多球形顶部**吸顶分段控件**（`Picker .segmented`+`LazyVStack` pinned header）切换、复用 `sectionCard`；单球形维持平铺。③配图=PNG 海报+可选 clip，有 clip 显**播放角标点击才播**（poster_tap）。④新增内置**全屏看图** `TutorialMediaViewer`（图集翻页 + 捏合/双击缩放 + 下滑关闭 + clip `AVPlayerLooper` 静音循环）——组件内置同文件避免改 pbxproj；`DrillTutorials/` folder ref 自动打包 mp4。⑤详情页球台预览只画第一球形（不改）。⑥**配图选用规则**写入 SOP：讲位置/几何/落点用 PNG，讲运动/走位/杆法效果加 clip（gif 先转静音循环 mp4）。**验证**：`make build` BUILD SUCCEEDED、lint 0。改：`QiuJi/Data/Services/DrillContentService.swift`、`QiuJi/Features/DrillLibrary/Views/DrillTutorialView.swift`、`QiuJi/Resources/Drills/schema.md`、`.cursor/skills/content-engineering/SKILL.md`、`tasks/phases/P12-content-system-theory.md`。**⏳ 待人工**：真机走查多球形切换 + 全屏缩放/翻页/下滑关闭 + clip 循环播放（需先有一条多球形/带 clip 示范内容）；gif→mp4 转码脚本与首条示范内容待排期。
- **出杆跟杆（follow-through）+ 停留收杆 ✅（2026-06-18，SwiftUI Developer）**：用户要更真实的收杆——「击球后球杆超过母球原中心一颗球的距离、减速、停 0.5s 再消失」。在单一权威 `CueStroke` 上加跟杆段：`followThroughPull = −3R`（杆头 `tipOffset=(R+0.001)+pullBack` ⇒ 落在母球原中心 +2R≈一颗球处）、`followThrough(at:)` ease-out 减速（触球瞬间最快→停）、`followThroughDuration=0.2s`、实时停留 `followThroughHold=1.5s`（球仍在滚动停稳）、导出短停 `exportFollowThroughHold=0.2s`。`runCueStroke` 改 `SCNAction.sequence`：出杆到触球→`onContact`（**仅发球**）→减速跟杆→停留→收杆（收杆改由本方法统一接管，**5 个调用点**`PositionPlay/Silu/Snooker/ShotSimulation/RackGenerator`删除触球处 `hideCueStick`，`completion`→`onContact`语义）。**收杆/复位竞态**：`AngleTrainingScene.updateCueStick`/`hideCueStick` 开头 `removeAction(forKey:"strokeAnim")`，显式重新摆杆即取消未结束的延迟收杆，避免击球后复位重瞄的杆被误隐藏；`runCueStroke` 逐帧直接驱动 `CueStick`（仰角恒定）绕开 `updateCueStick` 故不自取消。导出 `renderCueStroke` 同源追加跟杆帧 + 短停帧，并把「触球清预告线/收假想球」改在触球瞬间回调（保 ADR-P11-11 轨迹契约，跟杆期不再显示预告线）。**验证**：`make build` BUILD SUCCEEDED、lint 0；新增 `BallFogDiagTests.test_cue_follow_through_kinematics`（数值：`followThroughPull=−3R`、曲线 0→终点单调向前且每步位移非增=减速、杆头停点=中心+(2R−0.001)）**通过** + `test_render_cue_follow_through` 渲染触球/跟杆两帧（`build/ball_fog/cue_{contact,follow_through}.png`）**通过**，肉眼确认跟杆停点杆头越过母球中心约一颗球。改：`Core/Scene/{CueStroke,AngleTrainingScene}.swift`、`Core/Media/SequenceVideoExporter.swift`、`Features/{PositionPlay/ViewModels/PositionPlayViewModel,PositionPlay/ViewModels/SiluTrainerViewModel,SnookerTactics/ViewModels/SnookerTacticsViewModel,AngleTraining/ViewModels/ShotSimulationViewModel,RackGenerator/ViewModels/RackGeneratorViewModel}.swift`、`QiuJiTests/BallFogDiagTests.swift`。**⏳ 遗留**：已接入 App 的 `drill_c042/full_3d.mp4` 为本次改动前导出，跟杆口径同步需重新出片替换（待用户确认）。
- **出杆动画单一权威收口 + 全场景接入 + 背景统一黑 + 桌底接地 ✅（2026-06-18，SwiftUI Developer，PD-024）**：用户要把「那套严谨的出杆动画」**应用到所有有击球的地方**，并把 3D 背景统一黑、修桌腿。**出杆收口**：新增 `Core/Scene/CueStroke.swift`（单一权威运动学：回杆 `d=a+k·v` → smoothstep 回杆 → 蓄力停顿 → 匀加速出杆，触球杆速=v；`strikePosition` 含加塞偏移；以及 `AngleTrainingScene.runCueStroke(...)` 的 `SCNAction` 实时驱动）。删除 3 处**逐字节相同**的拷贝（`PositionPlayViewModel`/`SiluTrainerViewModel`/`SnookerTacticsViewModel` 各自的 `Stroke` 枚举+`runStrokeAnimation`+`strikePosition`）改调共享；`ShotSimulationViewModel` 把**简易版**（固定 pull/thrust 0.16/0.06s）换成完整 `runCueStroke`；`RackGeneratorViewModel` 开球**原来完全无出杆**，新增运杆→出杆→散开（沿 `aimAtApex` 方向、速度=power）；`SequenceVideoExporter.renderCueStroke` 逐帧改用 `CueStroke.pullBack(at:)` 并删本地常量/公式（产物逐帧等价）。**背景/接地**：`EnhancedEnvironment.apply` 可见背景由冷灰天空盒改 `UIColor.black`（**IBL 不变**，仅 `background.contents` 置黑）⇒ App 内 3D 与 2D 页、导出三者一致；`AngleTrainingScene.setupGround` 地板由近黑暗蓝提亮到中性深灰 (0.066) + 新增烘焙**接地阴影** `setupContactShadow`（径向软暗斑铺桌底）⇒ 黑底下球桌读作落地不悬空。**桌腿诊断（推翻原假设）**：实测 `legBottom(world)≈-0.0005`、地面板 `Y=0` ⇒ 腿**在地面板之上未被遮挡**（占用假设错误）；导出高斜视角看不到桌底腿是相机角度+暗腿压黑底所致，非 bug，故按用户选择走「加地板+接地阴影」让球桌落地。**验证**：`make build` BUILD SUCCEEDED、lint 0；`BallFogDiagTests` 数值诊断 + App 内 3D 渲染腿/袋网正常；`make position-export`（seq_d438b01c）端到端 94s 跑通，抽帧确认出杆杆体渲染正确 + 球桌接地（`build/legdiag/new_{stroke,steady}.png`）。改：`Core/Scene/{CueStroke,AngleTrainingScene,EnhancedEnvironment}.swift`、`Features/{PositionPlay/ViewModels/PositionPlayViewModel,PositionPlay/ViewModels/SiluTrainerViewModel,SnookerTactics/ViewModels/SnookerTacticsViewModel,AngleTraining/ViewModels/ShotSimulationViewModel,RackGenerator/ViewModels/RackGeneratorViewModel}.swift`、`QiuJiTests/BallFogDiagTests.swift`。

- **P11 教学素材 3D 斜视角视频（短边沿长轴 + 透视自动取景 + 双分辨率档）✅（2026-06-18，SwiftUI Developer，ADR-P11-15）**：用户要在现有「2D 视频」（其实是 3D 场景顶视正交拍的）之外**并存**出 3D 斜视角视频供 App 内竖屏播放。四点拍板：①主用途竖屏、默认短边后方沿长轴静态斜视角（+X 端看向 −X，俯角 30°/竖直 FOV 46°）；②2D 与 3D 并存（2D 静帧/cover/preview/gif/full.mp4 全保留，3D 只新增视频）；③双分辨率：手机档 720×1280（OTA）+ 高分档 **1440×2560**（外站备用不进 Bundle）；④高分档仅整段不出 per-shot。**实现**：`SequenceVideoExporter` 加 `CameraMode`/`Perspective3DConfig` + `solvePerspectiveCamera`（固定俯角+FOV，二分推距离使球桌**外框 8 角点**全入框 6% 余量 ⇒「全台可见」不变量，禁 magic number，FOV 锁竖直方向）；`RenderContext` 按模式分叉，3D 走 `enhancedRendering`（studio 光照+IBL+接地阴影，同 `Scene3DAimingView`）；**进袋球到达袋心沿 Y 下沉 7cm 再淡出**（仅导出层，防平面凭空淡掉穿帮）；轨迹线 `lineRadiusScale=1.3` 补远端变细；HUD 条高竖版随宽等比（720→80、1440→160）。Runner 追加 `full_3d.mp4`/`sNN_3d.mp4`/`full_3d@1440.mp4`。**验证**：`make build` BUILD SUCCEEDED；新增 `SequencePerspectiveFitTests` **5/5**（8 角点投影全入框 + 取景最小可行 maxRatio>0.97 + 近库不遮挡近端球 + 端点镜像，5 俯角×3 FOV 批量不变量）；`make position-export` 4 序列端到端跑通；抽帧核验（seq_f4ded688）全台可见/近库不挡球/studio 阴影立体/轨迹线+假想球+球杆+HUD 正常/进袋球沉入侧袋（t=8.7 蓝球入袋、t=9.0 已没）。改：`Core/Media/SequenceVideoExporter.swift`、`QiuJiTests/{PositionPlaySequenceExportRunnerTests,SequencePerspectiveFitTests}.swift`、`content/position_play/README.md`、content-engineering SKILL、`tasks/phases/P11-position-play-composer.md`。**物理边界（诚实标注）**：仍是 2D 平面物理，3D 只换相机——跳球/扎杆腾空出不来。**接入 demo（同日）**：手机档 `full_3d.mp4` 拷入 `Resources/Videos/drill_c042/`，`drill_c042.json` `videos` 追加 `full3d` 与 2D `full` 并存；`make build` ✅ + bundle 含两 mp4；`testDrillC042TutorialDemo` 通过，截图确认详情页「视频示范 2 段」（第1段2D顶视/第2段3D斜视角），3D 缩略图由真实文件 AVAsset 解码 ⇒ 可播。注：`scripts/import-videos-to-app.py` 仅服务项目 15 真机录屏且会重写 videos，不适用合成渲染视频，c042 走手工接入。**遗留**：其余 drill 的 3D 视频接入待批量出片后排期 + OTA 通道（依赖 H-14）；详情页缩略图无 2D/3D 文字标签（视觉可辨）；动态镜头（跟拍/环绕）为后续增强。
- **P17 球形生成器 🔄 阶段 2.2 开球缝隙 1mm→0.2mm（治「球开不开」根因）✅（2026-06-17，iOS Architect）**：用户真机走查满力开球**经常散不开**（球堆只裂几颗、母球带大半能量跑远），疑球间距过大。**测量优先**：新增诊断 `BreakRackPhysicsTests.test_break15Ball_gapSweep`（母球对准顶角全砸 v=7、5 个瞄准偏角取均值，扫 gap 0.1/0.2/0.3/0.5/1/2mm）——均散开>30cm：0.1mm=10.2/15、0.2mm=9.6/15、0.3mm=5.8/15、**1mm（旧）=5.2/15**、2mm=3.6/15，单调且 0.2↔0.3mm 有拐点，全程停稳/不互穿。坐实**1mm 缝隙过大是根因**（动量逐颗传递被缝隙+摩擦耗散、母球留太多能量）。**修复** `RackLayout.gap 0.001→0.0002`（同步 `BreakRackPhysicsTests.gap`；取 0.2mm 留数值余量）。验证 14/14：`diagnoseSpread` v=6 散开>30cm **2/15→12/15**、铺开包围盒 X **108→246cm**（近满台），仍 settled/不互穿/确定性。改：`QiuJi/Core/Rack/RackLayout.swift`、`QiuJiTests/BreakRackPhysicsTests.swift`。**⏳ 待人工**：真机复看满力/中力是否稳定炸开。
- **P17 球形生成器 🔄 阶段 2.1 开球随机性 + 9 球少球摆法 + 力度上调 ✅（2026-06-17，iOS Architect）**：用户走查四点反馈落地。①**随机性根因**：`seed` 仅贴号码、slot 几何写死、引擎里球物理全等 ⇒ 同开球点/力度/打点散开**逐毫米一致**，「换一局」只换号不换形。修复=在 `RackGeneratorViewModel.breakNow` 注入 **seed 驱动的「一点点」扰动**（`breakJitter`，绑定 rack seed ⇒ 同局可复现/换一局即变）：上下塞 ±0.15、左右塞 ±0.10（叠加用户打点后 `clampSpin` 钳 miscue 0.5）。〔注：原撞顶球偏摆 ±1.5° 已于同日移除——用户拍板不要，且阶段 2.2 gap 修复后无需靠偏摆破对称，`breakShot(aimJitter:)` 参数已删〕。②「部分球不动」由阶段 2.2 gap 修复解决。③**力度上调** `powerRange 3–8→4–9`、默认 `6→7`（母球出射≈1.54×杆头）。④**改名+摆法**：UI「追分」→「9 球」；`RackLayout.zhuifen` 改**显式布局+9 球系编号**（删旧三角前缀，新 `makeSlots`）——4 球小菱形 `[1,2,1]`(底=9)、5 球菱形+共线尾球(尾=9)、6 球三角 `[1,2,3]`(底排中点=9)、9 球钻石不动；号码集非连续(4={1,2,3,9}/5={1,2,3,4,9}/6={1,2,3,4,5,9})。**验证**：`make build` BUILD SUCCEEDED、lint 0、`RackGeneratorTests` **8/8**（改断言新编号+9 号锚点；`aimJitter` 默认 0 ⇒ 既有路径不变）。改：`QiuJi/Core/Rack/{RackLayout,BreakSimulator}.swift`、`QiuJi/Features/RackGenerator/{ViewModels/RackGeneratorViewModel,Views/RackGeneratorView}.swift`、`QiuJi/Features/AngleTraining/Views/AngleHomeView.swift`、`QiuJiTests/RackGeneratorTests.swift`。**⏳ 待人工**：真机走查换一局是否真换形/4·5·6 摆法/加力后散开停稳/随机量级是否「一点点」合适。
- **P17 球形生成器（开球/追分开球）🔄 阶段 1 Core ✅ + 阶段 2 UI/接入 ✅（2026-06-17，iOS Architect，ADR-P17-01；阶段 3 真实感对标/分离角适配待续）**：**阶段 2（本次）**：新增 `QiuJi/Features/RackGenerator/`——`RackGeneratorViewModel`（状态机 racked/computing/breaking/settled，复用 `AngleTrainingScene`+`AngleSceneView`；玩法分段 中八/追分[4/5/6/9]、**母球钳开球区+方案 a 锁顶球瞄准**随拖动更新瞄准线、力度滑杆 3–8m/s+`BTSpinPadCard` 打点、后台 `BreakSimulator` 开球→`TrajectoryPlayback` 回放散开→收尾钉终点+刮杆补母球、「换一局」换 seed、废局只提示不筛选、`deliveredBoard()` 交付）+ `RackGeneratorView`（黑底+玩法分段+2D 真台+底部条，停稳后路由 `PositionPlayComposerView/SiluTrainerView(initialBoard:)`）；接线 `AngleRoute.rackGenerator`+海报卡「开」+`MainTabView`。`BreakSimulator.breakShot` 由 `lateral` 改 `cuePosition`+`aimAtApex`（母球位置即开球角度）。验证 `make build` BUILD SUCCEEDED、lint 0、`RackGeneratorTests` **8/8**（签名改后回归仍 settled/合法/确定性）。**⏳ 待人工**：真机走查开球角度感/各玩法散开/废局提示/交付。 |原阶段 1 摘要见下| 用户 idea「给走位编排台/思路训练器加中式八球开球 + 追分开球（4/5/6/9 球）的球形生成入口」。多轮拍板：**livesim 端上真实物理**（非参数化/预烘焙）、玩法全做但**中八 15 球先行**、**WYSIWYG 不筛选**（不满意手动「换一局」）、消费端=走位编排台+思路训练器、**斯诺克不加**（用户复盘排除）、分离角后续适配层。**前置去风险 ✅**：`BreakRackPhysicsTests` 移植 01 三角阵（gap=1mm）跑 3–8m/s 多档，确证 P13 引擎能把 15 球开球跑到**完全停稳/不出界/不互穿/确定性**（事件数仅 ~80–140，maxEvents=8000 头寸充裕），散布非哑火（铺开 ~1m×0.8m，母球行程 ~1–1.5m），真实感对标留后续。**阶段 1（本次）Core 生成器**：新增 `QiuJi/Core/Rack/`——`RackLayout`（`RackGame{中八/9球/追分(N)}`，SceneKit 世界系摆架，中八 8 号居中+底角一花一色、9 球钻石三锚点、追分 1 号最前其余随机；**追分末排取整三角点阵前 N slot 而非重新居中**——等数相邻排会在 z 对齐致排间距退化 0.866·2R 互穿，本轮测试一次抓出修正；`SeededGenerator`/SplitMix64 确定性随机）+ `BreakSimulator`（球架→`EventDrivenEngine` 开球→散开归一化 `BoardSnapshot` + `recorder` 供动画 + `cueScratched`/`eightOnBreak`/`settled` 废局/截断 flag；引擎球名直接用在桌键，回放映射零转换）。**验证**：`RackGeneratorTests` **8/8**（摆架规则/不互穿在界/同 seed 同架/异 seed 异架/中八+9 球开球 settled 合法/同输入两次开球逐球一致 ≤1e-4），`make build` 编译通过、lint 0。详见 `tasks/phases/P17-rack-generator.md`。**下一步（阶段 2）**：生成器入口页（选玩法→摆架预览→开球动画→散开板→路由编排台/思路 + 「换一局」+ 废局兜底）+ 角度 Tab「工具」海报卡入口。**阶段 3（非阻塞）**：开球真实感与 pooltool 跨引擎校准、分离角适配层。
- **P16 做斯诺克战术工具（安全球反解）✅（2026-06-17，iOS Architect，ADR-P16-01）**：用户 idea「做斯诺克的工具——选目标球+障碍球，让母球与目标球落点间被障碍挡住」。多轮澄清独立成页（后续扩展为安全球/战术中心）+ 三项拍板（被困球==首触球 / 纯安全球不进袋 / 手动指定单障碍）。**几何**：新 `AngleSceneCalculator.snookerCoverage`——判「对手能否从母球看到被困球任一点」（球心扫过即可见的张角扇形），闭式 `coverage = β−α−|Δθ|`、完全斯诺克 ⟺ `coverage≥0 ∧ d_障碍<d_被困`；全程 SceneKit 世界系 X–Z、取三球**终位**、规避归一化符号双真源；Python 数值草稿 + XCTest 金标准双验证。**求解器**：新 `PositionPlaySolver.solveSnooker`——与 A/B 关键差异是不进袋无袋口锚定瞄准 ⇒ **瞄准为自由变量**，故走 `simulateFree`（非 candidateMatrix）扫「瞄准偏移×塞×力度」并行求解；硬约束=合法首触目标球∧母球不进袋∧目标不进袋∧母球真停稳∧终位完全挡死；排序库少→覆盖余量大→加塞少；无完全解降级半斯诺克；复用 `applyCushionBudget`/`spinCombos`。`ShotPredictor.simulateFree` 补 `cueFinalSpeed`（与 predict 对称，向后兼容）。**页面**：独立 `Features/SnookerTactics/`，布局 1:1 照搬思路训练器（黑底+三段式+球库+操作列），工具行改「目标球/障碍球/摆球」点选、叠加渲染终位遮挡视线（完全=灰/半=红）+ 角色环；入口角度 Tab「工具」海报卡「斯」+ `AngleRoute.snookerTactics`。**验证**：`make build` BUILD SUCCEEDED、lint 0；`SnookerSolverTests` **4/4**（张角金标准 + 非法输入空 + satisfying 解全复核含必找到完全解 + 降级半斯诺克）；回归 `PositionPlayFreeAimTests` 7/7 + `PositionPlaySolverTests` 13/13；探针 60 布局确证回弹做杆机制（最大余量 71°）。详见 `tasks/phases/P16-snooker-tactics.md`。**遗留（v2）**：被困球≠首触球真安全球、多障碍并集、逃脱难度评分、局部精修、真机走查。
- **P15 拍照建球形（阶段 1 几何竖切）✅（2026-06-16，iOS Architect，ADR-P15-01）**：用户要从任意角度球桌照片提取所有球号+位置。多轮澄清拍板**非 DL 方案 + 人工确认一等公民**（几何用解析单应、感知后续才上 DL 且优先「检测」一环；号码背对相机信息论不可知 → 人工确认既是安全网又是 DL 数据回收口）。阶段 1 零感知竖切：拍照 → 手动拖 4 角标定 → Heckbert square→quad **闭式解单应**（无 SVD，`Homography.swift`）→ 照片上点标每颗球+选号（经 H 映射，球底接触点过 H，禁经验 offset）→ **2D 真台人工确认**（复用 `AngleTrainingScene`/`AngleSceneView`：拖动改位/点选+球库改号/球库拖入增球/拖回球库删球，布局对齐编排台）→ 产出 `BoardSnapshot` → 送编排台（`PositionPlayComposerView(initialBoard:)` + `PositionPlayViewModel.loadBoard(_:)`，默认参数向后兼容）。入口：角度 Tab「工具」加海报卡「拍」(chip「识别」)。**验证**：`make xcodegen`+`make build` BUILD SUCCEEDED、lint 0、`HomographyTests` **5/5**（恒等/四角精确/往返<1e-9/内点在界/退化返回 nil）。**坐标契约**（ADR-P15-01）：图像归一化 uv→归一化台面系（CanvasPoint）。新增 `Core/Geometry/Homography.swift`、`Features/BallExtraction/{ViewModels,Views}`、`HomographyTests.swift`、`tasks/phases/P15-photo-ball-extraction.md`。**⏳ 后续（非阻塞）**：阶段 2 自动球检测（自适应台呢色斑块）、阶段 3 号码归类（中八固定配色+白占比+OCR 兜底+冲突消解）；阶段 2/3 才按需引入 DL。**待人工**：真机/模拟器走查四步流程与几何精度。
- **当前 Phase**：**P10 物理升级**（Track A 内容管线雏形 ✅ 2026-06-04，ADR-P10-01；Track B-1 ✅ 2026-06-04，ADR-P10-02；**Track B-2 漏斗袋口模型 v3 ✅ 2026-06-04，ADR-P10-03**；Track B #3 常量标定需真实视频待办）+ **P9** ✅（2026-06-02）+ **P8** 🔄（仅剩人工：H-17 / TestFlight / App Store）
- **当前激活角色**：iOS Architect（显示闸门下沉 + 纯物理化 ADR-P10-06/07）
- **P14 击球回放音效 + 回放原速化 🔄（2026-06-16，iOS Architect，ADR-P14-01）**：用户要给物理回放加**真实**音效（非游戏/合成）并要求所有回放/视频**原速**。**(1) 原速化** ✅：4 处物理回放（角度 `ShotSimulationViewModel`、Drill `DrillSceneView`、走位编排 `PositionPlayViewModel`、思路 `SiluTrainerViewModel`）`speed 1.4→1.0` + `SequenceVideoExporter.gif` `1.3→1.0`。**(2) 音效基础设施** ✅：新增 `Core/Audio`——`ShotSoundBank`（`AVAudioEngine`+12 player 池，统一画布格式 `AVAudioConverter` 转换，`.ambient+.mixWithOthers` 尊重静音键、与休息计时器后台音频共存，缺资源 no-op）+ `ShotAudioScheduler`（消费 `ShotPrediction.events`，按真实时刻 `DispatchWorkItem` 排程、可取消；力度由 recorder 球速近似；**刻意不变调**，力度差异靠多样本池避免游戏味）；4 条回放路径起播挂调度、复位/结束 `cancel()`；设置加「声音·击球音效」开关（`UserPreferences.soundEffectsEnabled` 默认开）。**(3) 资产管线** ✅：`Resources/Audio/` folder reference（drop-in，放入 `sfx_*.caf` 重新构建即生效）+ `CREDITS.md` 命名约定/转码示例/来源登记。**验证**：`xcodegen`+`make build` BUILD SUCCEEDED、lint 0、缺音频文件回放正常无声不崩。**⏳ 阻塞收尾（H-18，非阻塞）**：真实 CC0 录音需人工下载（Freesound 下载要登录账号；免登录站疑似 AI 合成不满足「真实」）——放入文件后真机听感验收即闭环。详见 `tasks/phases/P14-shot-audio.md`。
- **P13 思路训练器（走位反解器）✅（2026-06-14，iOS Architect，ADR-P13-01）**：把正向瞄准求解升级为**走位反解**——放开塞与力度作自由变量，约束改为母球落点。两情形：A 母球停在手画**可行落区**（rect/circle）；B 母球 v>vMin 时**经过指定点**去 K 球。架构：新增独立 `PositionPlaySolver`（区别于前向 `PositionPlayShotSolver`），外层搜 `(spinX,spinY,velocity)` 网格、内层复用 `ShotPredictor.predict` 解瞄准并以「目标球进选定袋」为硬约束；按吃库数分档枚举、库少优先；多库要求更大 margin（情形 A）。`ShotPredictor` 最小增量：新增 `ShotEvent`/`ShotPrediction.events`（默认空数组，向后兼容，纯只读事件收集）供情形 B 过点后碰撞/到点前吃库分析。新页面 `SiluTrainerView`+`SiluTrainerViewModel` 复用 `AngleTrainingScene`/编排台摆球与回放：塞/力度转**只读指示器**显示当前解、落区/过点台面手势绘制 + SceneKit 叠加渲染、最优解 + 「下一解」翻档、进球线/假想球/母球轨迹 + 文字说明、无解醒目降级、击打回放、导出单步序列经 `PositionPlaySequenceArchive` 送产线（模拟器限定）。入口：角度 Tab「工具」加海报卡「思」+ `AngleRoute.positionPlaySolver`。**验证**：`make xcodegen`+`make build` BUILD SUCCEEDED；`PositionPlaySolverTests` 5/5 全过（SDF 金标准 + 情形 A 可落区/无解降级 + 情形 B 过点分段）；`PositionPlayFreeAimTests` 7/7 回归全过；`PhysicsEngineTests` 既有 3 个失败（largeCutClearShot/objectPath_reachesPocket/withSideSpin_objectPots）经核为此前未提交工作遗留（PHYSICS-DEBT §5.7），本次纯增量无关。关键文件：`PositionPlaySolver(.swift/Models)`、`SiluTrainerView(Model)`、`ShotPredictor.swift`、`AngleHomeView/MainTabView`、`tasks/phases/P13-position-play-solver.md`。**不在本次范围**：多杆连续求解、落区/过点持久化。**后续微调（2026-06-15）**：①内层瞄准提速「共享核心 + 独立编排」——行为不变重构 `predict`→`prepareAim`+`buildPrediction`，新增同文件 `ShotPredictor.predictForPositionSolve`/`positionAimOffset`（黄金分割瞄准 ~15 次模拟 vs 75 次）；求解器拆 spinX/spinY + 跨 spinY 记忆化瞄准（squirt 只随 spinX）+ 漏进重解护栏 + 并行 ⇒ **情形 A 12.65s→1.41s（~9×）、情形 B 11.08s→1.64s（~6.8×）**。②高速假停护栏 `cueFinalSpeed`（情形 A 落区解仅接受末速<0.05m/s 真停点）。③排序「加塞少/吃库少优先」。**零漂移验证**：`test_fastPath_matchesPredict_potOutcome`（28 组进袋判定一致）+ 物理回归 40 测全绿（`PhysicsInvariant` 9/9 含确定性 + `PocketBehaviorDiag` 24 + `PositionPlayFreeAim` 7）+ `PositionPlaySolverTests` 7/7。详见 P13 文件「后续微调（2026-06-15）」。**再后续微调（2026-06-15）**：①**情形 A 局部精修** `refineCandidate`（拓扑锁定 Hooke-Jeeves 模式搜索）——粗网格定位 basin、精修在连续邻域抛光停点，objective 只接受「进袋∧真停稳∧吃库数==种子」样本（永不跨拓扑悬崖），治「basin 对但采样粗」；接入每桶代表解 + 降级解（降级精修后可反升级满足）；`SearchParams` 精修参数全带默认值不破坏旧调用。②**落点约束** `SolveRegion.point`（精确停位 + 容差，复用各向同性圆 SDF）——`solveRestRegion` 按 `isPoint` 分叉「最小化到点距离」装配（所有可进解按桶取最近代表、容差内才判满足）；新增 UI 工具「落点」（琥珀十字 + 容差环），顶部工具行扩为落区/落点/过点/摆球。**验证**：`PositionPlaySolverTests` **10/10**（新增落点 SDF 金标准 + 精修不劣/锁拓扑/确定性 + 落点最近代表分桶），墙钟基本不变（A 2.30s/B 1.68s），`ShotPredictor`/物理本轮未改动无回归风险。**第四轮微调（2026-06-16）— 分水平求解开关**：加两个**可选收窄**开关(默认=完整能力,零回归)。①**是否左右塞**(默认允许,关→`spinXValues=[0]`;横塞是唯一影响瞄准的维度,禁后唯一瞄准 3→1;高低杆始终全开):精修加**锁轴护栏**——`refineCandidate` 只沿网格搜过的轴探测(`spinXValues.count>1` 才探 ±spinX),否则会把被禁横塞引回(关键正确性点)。②**走位复杂度预算** `maxCushions:Int?`(nil 不限默认,基础=1):后处理 `applyCushionBudget` 走「优先+兜底」——优先 ≤cap 解,无解才回退全部解并标 `beyondCushionBudget`(新增字段,UI 加「进阶」前缀;绝不因预算给无解)。UI 在「⋯」菜单加「求解范围」两 Toggle。**验证**:`make build` ✅、`PositionPlaySolverTests` **13/13**(新增禁塞⇒spinX==0含精修不引回 / 基础预算内⇒吃库≤1不标进阶 / 不可满足预算⇒兜底全标进阶不给无解),默认态走原路径墙钟不变。改:`PositionPlaySolver(.swift/Models)`、`SiluTrainerView(Model).swift`、`PositionPlaySolverTests.swift`、P13 文件。
- **P12 内容体系与理论挂接 🔄 规划已立（2026-06-14，ADR-P12-01）**：用户复盘"拖延=对现做法不满意"，根因坐实为 ①生产排在系统定型之前（怕返工）②"更系统更完备"无可证伪终点 ③**理论(项目16)与内容(72 drill)是两座孤岛**（已核实：drill 无 `theoremIds`、工程未 import `contracts/`、16 中枢卡的"≥1 Drill 消费 contracts"闭环至今未达成）。决策：①**完备=课程地图被填满**（新建 [`curriculum-map.md`](curriculum-map.md)：能力树Level×8大类，每格四件套〔挂定理+引擎可达+示范+精讲〕，72 条真实分布=橄榄球形、L1-accuracy 台阶断裂、系统训练模式 0 条）；②理论挂接走**三层渐进式披露**（教练话→one-liner→详情页，禁"根据定理X"教材腔；`theoremIds` 仅作机器标签）；③**理论之家=角度Tab「学习」区升级为"球理"中心**（非第6Tab/非动作库，深链正交+视觉基建都在该Tab）；④理论配图三类（标注图复用 `AngleTrainingScene`/参数扫描待建/战术布局图非引擎）；⑤社媒**拿球形idea不拿片段**（引擎复刻、理论当锚、有界采集）；⑥**分阶段**：阶段1 仅 c042 竖切验证，阶段2 才重构IA+参数扫描，系统训练模式 v1.1。三 SOP 已回写 content-engineering SKILL。详见 `tasks/phases/P12-content-system-theory.md`。**下一步**：用户拍板地图 §6 三参数（每格配额/L4取舍/系统训练模式）+ 执行 c042 竖切（T-P12-02/03）。**示范素材批量录制仍 ⏸ 暂停等编排台录制。**
- **P11 图文精讲结构化渲染升级 ✅（2026-06-13，SwiftUI Developer，DR-019）**：用户反馈精讲文本「太干巴巴」——应用课模板的结构化信息被压扁成字墙。`TutorialSection` 新增可选 `items`（{label,text} 条目行）/`params`（{spinX,spinY,velocity} 击球参数）/`caption`（图注），向后兼容；`DrillTutorialView` 渲染升级：彩色标签胶囊行（为什么=blue/怎么打=btPrimary/自检=orange/其余中性）、参数行复用 `BTSpinMiniIcon`(trueScale)+`SpinDisplay`+`PowerDisplay` 与导出 HUD 同口径图文互证、content 分段+inline markdown、图注。c042 内容已迁移（逐杆节 items+params，常见错误转结构化列表）。**验证**：UI 测试截图 8 张逐张核验 Passed、lint 0。**模板已定稿并回写** `.cursor/skills/content-engineering/SKILL.md` §「图文精讲应用课模板」（接入清单/媒体落位表/红线）。**⏸ 后续暂停（2026-06-13 用户拍板）**：等用户在编排台录完全部示范素材后再继续——按 SKILL 清单逐条接入 + 存量 72 drill「常见错误 1.2.3.」批量转 items + 回滚 c042 临时解锁。
- **P11 序列出片首次接入动作库 demo + 精讲「应用课」模板 ✅（2026-06-13，Content Engineer，ADR-P11-14）**：3 杆序列 `seq_f4ded688` 整链路落进 `drill_c042` 初级蛇彩走位（用户拍板：存量 drill 视频后续整体替换；存量 `shotIntent` 数据精度不可靠，示范击球以编排台**人工录制**为准，不做批量转换）——`shotIntent` 换序列真实 3 杆（含 obstacles）、`videos` 5 条旧 take → `full.mp4`（720p@60 带 HUD）、新增 `DrillTutorials/drill_c042_{initial,s01,s02,s03}.png`。精讲按「应用课」模板重写（多杆走位类适用，技术类四段结构不动）：技术原理 → 开局与击球顺序（布局图）→ 逐杆精讲（为什么/怎么打/自检 + 带 HUD 静帧）→ 常见错误 → 进阶练习。**工程修正**：`DrillTutorials` 改 folder reference（原普通 group，`subdirectory:` 查找解析不到）。**验证**：全量缩略图重烘焙 Passed；UI 测试 `testDrillC042TutorialDemo` 截图核验详情页/视频区/精讲全部正常。**临时项（发布前回滚）**：c042 `isPremium` true→false 供无订阅验收；临时 UI 测试待验收后处理。**下一步**：用户确认模板后回写 content-engineering SKILL SOP + 其余 drill 示范击球录制排期。
- **P11 教学素材击球参数 HUD + 导出暗色背景 ✅（2026-06-13，iOS Architect，ADR-P11-13）**：teaching 档画面底部追加 80px HUD 条（输出 1280×720，16:9）——打点球面（无背景，**真实比例**：`BTSpinMiniIcon` 新增 `trueScale` 模式与打点盘同几何——红斑=皮头中心位置+真实斑径+打滑极限虚线圈，教学素材可照搬到真球；App 内小按钮保持归一化）+ 百分比读数 + 力度条（档名 + m/s），每杆常驻换杆更新；经 `ImageRenderer` 直接复用 App 组件（样式单一真源）；导出场景背景改黑与 App 一致（方案 1，解决白底白字不可读）。瞄准点/接触点用户拍板不做。`Options.showShotHUD`（gif/card 关）；sNN_still 带 HUD、initial/final 纯布局。**验证**：实跑 3 序列 Passed、ffprobe/sips 规格全对、抽帧核验 HUD 数值与 JSON 一致。坑：ImageRenderer 下 HStack 文本竖排折行需 `.fixedSize()`。
- **P11 轨迹线样式真源 + 假想球补全 + 教学视频 60fps ✅（2026-06-12，iOS Architect，ADR-P11-12）**：首版产物评审三项反馈落地——①新增 `TrajectoryStyle` 真源（`PoolBallFace.swift`：aim 0.0025 / pot 0.0030 / compact 0.0045；`potColor(for:)` 进球线随目标球球色、黑 8 例外亮灰），编排台/导出器/分离角/动作库详情/缩略图全部接入（翻袋/颗星/角度测验只统一线宽不跟球色——路线教学色非进球线语义）；②假想球补回编排台预览与导出产物（设置帧/静帧/封面显示、出杆随线清除）；③教学视频卡顿根因=30fps×1.3 倍速帧间步长 43ms（非模拟器性能），preset 改 60fps×原速（GIF 保留 12fps×1.3 控体积）。**验证**：`make position-export` 实跑 3 条序列 xcresult Passed(1/1)、ffprobe 1280×640@60、抽帧核验球色线/假想球/运动帧无线。lint 0。**遗留**：动作库旧烘焙缩略图（橙线）待批量重烘焙统一。
- **P11 教学素材渲染矩阵管线 ✅（2026-06-12，iOS Architect，ADR-P11-11）**：与用户澄清两点契约后落地——①「分辨率」（输出像素尺寸）与「球放大」（`ballScale=1.6`）是两个参数，后者仅限卡片素材，其余产物真实比例；②轨迹线=**击球前预告、出杆即清**（与编排台 App 内一致），非全程挂线。交付：`SequenceVideoExporter` 重写（`Options` preset 三档 teaching 1280×640/card 球放大/gif 480×240 + `showTrajectories` + `RenderContext` 收口 USDZ 装载 + `renderStills`/`renderCover`/`renderPreviewFrames`/`subSequence` 四出口）；runner 按**默认配方**出片到 `build/position_play_export/seq_<id8>/`（cover + 12 预览帧〔卡片风格〕 + initial/final/sNN_still 静帧 + full/sNN.mp4 + full.gif〔真实风格〕）。**验证**：`make build` ✅；`make position-export` 实跑 3 条实录序列全出齐，ffprobe 规格全对，抽帧核验设置帧有预告线/运动帧无线/cover 球径 1.6×。lint 0。**遗留**：一致性校验门 + `engineVersion` 版本戳 + meta sidecar（clean 变体按需）+ 发布回填。ADR-P11-11 见 phase 文件。
- **P11 教学素材管线落点 — 录制直写内容库 + 入口模拟器限定 + 视频 OTA 拍板 ✅（2026-06-12，iOS Architect，ADR-P11-10）**：基于击打序列 JSON 规划图片/GIF/视频生产管线，先确认数据充分性：序列 JSON（意图+前后快照，不存轨迹）几何充分，缺口=引擎版本戳/一致性校验门/教学文案 sidecar/资产命名关联（记入 ADR 遗留）。用户三项拍板落地：①**录制直写**——新建 `content/position_play/sequences/`（真相源进 git，附 README；命名 `seq_<id8>-<名称>-<N>杆.json` 与 `drill_pp_<id8>` 对齐），新增 `PositionPlaySequenceArchive`（整体 `#if targetEnvironment(simulator)`，模拟器进程直写 Mac 文件系统），录制结束 JSON 直落内容库 + banner 提示，删 share sheet 流程与已无使用方的 `ShareSheet.swift`；②**录制入口仅模拟器构建可见**（编译闸即产品开关，真机/发布版无入口）；③**生成视频走 OTA**（ADR-002 通道、被 H-14 阻塞；Bundle 只进静帧/帧序列/封面小资产——`Resources/Videos/` 已 257MB 为前车之鉴）。`make position-export` 渲染前自动同步内容库→收件箱。**验证**：`make xcodegen` + `make build` ✅（BUILD SUCCEEDED）、lint 0；**直写链路同日实录走通**（3 条序列落盘）。**同日补修**：「先录完→再命名」已写盘文件不随重命名更新——`archive` 改同 `seq_<id8>` 旧文件先清再写（一条序列只留一份），重命名保存后已录序列自动重新归档（录制中不处理，结束时按最新名字归档）。**下一步（管线主体）**：渲染矩阵（整段/单杆 × 有/无轨迹 × mp4/帧序列/静帧）、`Options` 分辨率 preset + `showTrajectories`、重模拟 vs `after` 一致性校验门、`engineVersion` 版本戳、`meta/` sidecar schema。ADR-P11-10 见 phase 文件。
- **P10 贴库滑行真根因根治 — 库线吃库×边界安全网浮点竞态 ✅（2026-06-12，iOS Architect，FL-022）**：下一条「幽灵反弹根治」被用户打回（贴库滑行仍在 + 求解轨迹出现「吃库→远端 jaw 弧→假进袋」），属 ⚠️ 返工（见 FL-022）。复盘：下一条只治了袋口弧接触带分支，且把 S2 实测「入29°→反131°」误判为采样误报。**真根因（`test_S4_replicateS2EventChain` 数值确证）**：库线吃库时球心接触位置**恰好等于** safe 边界（零余量），CCD 演进到接触点的浮点噪声偶尔越界 → 零容差硬钳抢在吃库事件前法向减半反向 → Han 解析按已退离速度**自动翻转接触系**把球二次反射**回库内**（vz +3.276→−1.638→+1.101）→ 子步反复钳制成 ~2 折出射角贴库滑出；滑行沿库入角袋 = 假进袋；噪声逐杆不同 = 偶发。**修复（两处物理约束，无 magic offset）**：①`enforceTableBounds` 触发加 `boundsEpsilon=0.5mm`（接触线上=合法吃库态非出界）；②吃库解析「只推不拉」护栏（v·n<0 才施冲量，过时事件跳过不记）。**验证**：S4 引擎出射==手动复算（27°/30°）；S2 全部反射恢复物理（131°→27°、114°→50°）、渲染无贴库段；扫描 1197 次吃库贴库幽灵 0/平行出射 0；`test_solveDrillC005` 117s 无回退；`PhysicsInvariant/Matrix/Scenario/CushionDiagnostics/PositionPlayFreeAim` 全过；3 个 `PhysicsEngineTests` 预存失败与修复前完全同集（⚠️ 待排查）。详见 `PHYSICS-DEBT.md §5.8`、FL-022（已回写 `geometry-spatial-reasoning` SKILL § 经验教训）。
- **P10 幽灵反弹根治 — enforceTableBounds 几何感知豁免 ✅（2026-06-12，iOS Architect；⚠️ 部分结论被 FL-022 推翻，见上条）**：用户截图报告「母球吃左长库后反弹轨迹明显不合理（贴库平行滑出+末端小钩），偶发」，要求先分析测试、确证根因再修。**根因（逐帧确证）**：角袋 jaw 弧合法接触圆（r+R=133.6mm）伸出矩形可玩框 1.3mm~4cm；ADR-P10-07 袋口豁免圈（pocket.radius+2R=127.2mm）差 0.9mm 没罩住接触带 → CCD **已正确检出并调度**的弧碰撞被每子步运行的矩形硬钳抢先（vx 取反减半、vz 保留、无事件、不作废缓存）→ 幽灵反弹 + 陈旧缓存事件接力出钩状伪迹。**修复（`enforceTableBounds` 三处）**：①jaw 弧接触带豁免（角度扇区内、距弧心 ≤ r+R+12mm）+ **径向速度门控 |vr|>0.02**（防切向蹭行研磨——无门控版曾把 `test_solveDrillC005` 拖到 signal kill，门控后 115s≈基线 123s）；②袋嘴圈内带速球无条件放行（删 towardCenter 方向门，rattle 弹出段合法；<0.35m/s 仍被 settle 收袋无研磨风险）；③硬钳后作废该球事件缓存。**验证**：300 杆扫描贴库线幽灵反弹 **4→0**（trial19 现正确产出弧#33 Han 反射 e≈0.68）；S2 疑似 131° 超宽反射排除（实为帧采样误报，真实 29°→27° e≈0.73 正常）；全量单测 372 过，3 个 `PhysicsEngineTests` 失败经基线对照**确认与本修复无关**（此前未提交工作遗留：largeCutClearShot/objectPath_reachesPocket/withSideSpin_objectPots，⚠️ 待排查）。详见 `PHYSICS-DEBT.md §5.7`。新增诊断 `PocketBehaviorDiagTests.test_S_cueRailReboundScan / test_S2 / test_S3`。
- **P10 显示闸门彻底下沉 + 引擎纯物理化 ✅（2026-06-07，iOS Architect，ADR-P10-06/07）**：用户拍板「贴库/进袋判定加了太多非物理规则……符合物理就该让它发生」「offset 全拿掉回归原始物理」「穿库安全网彻底去掉、母球进袋裸取引擎信号」「~1% 球甩出台面几米 → 修引擎让逃逸率≈0」。**(1) ADR-P10-06 移除显示层**：`ShotPredictor.predict` 删全部显示闸门（`clampedRecorder` 穿库安全网 ~100 行 + 母球/目标球进袋一致性闸门 + 整个 `enum DisplayGate`）；`recorder`/`cuePath`/`objectPath`/`cuePocketed`/`objectPocketed` 全裸取引擎信号；进/rattle/小力远jaw→近jaw→袋心进由喉腔几何自然涌现。**(2) ADR-P10-07 引擎根治逃逸**：固定步长上限 `maxEvolveStep=0.05`（dt 超限只推一安全步+记帧+作废缓存重检测，3285→57.5mm）；喉腔侧壁前延 `throatFrontExtend=0.045` 封库段↔jaw 对角接缝（→38.1mm）；近库自适应子步 `adaptiveEvolveCap`（仅逼近边界时收紧到位移级 `nearWallSafeStep=0.35R/速度`，根治高速窄喉隧穿）；`enforceTableBounds` 方向性收容（朝袋心放行/背离硬钳，低速 `jawSettlePocketSpeed=0.35` settle 收袋并补记落袋事件）。**(3) 性能门控**（解全局自适应致 6–10× 回归）：`simulate` 加 `highFidelityBounds`——仅 predict 最终展示模拟启用自适应子步（贴墙密帧、回放不外推穿墙），求解器 ~76 次短模拟完全不切步（恢复 ADR 前速度，结果量由段内采样+enforceTableBounds 兜底保证正确）。**验证**：单杆 predict 中位 **125ms**（曾回归 1114ms，预算 800）、满台 **663ms**（曾 4418ms，预算 3000）；越界不变量展示路径 **0.0mm**；`PhysicsMatrixTests` 矩阵1 求解器 **0 越界/96% 进袋**、矩阵2 裸引擎逃逸 **0.1%**（裸连测试，绕过 predict，用户可见路径恒经高保真 0.0mm）。`clampAwayFromPockets` 按用户决定**保留**（球摆放约束防摆进袋口，非轨迹修饰）。改 `EngineNumerics`/`EventDrivenEngine`/`TableGeometry+QiuJi`/`ShotPredictor`/`PHYSICS-DEBT.md §5.6`。**D-A3「下沉到引擎」终局达成**。
- **P10 求解器评分 — 放宽分支①接受「擦 jaw 再进」+ 否决纯平滑代理目标 ✅（2026-06-07，iOS Architect，ADR-P10-04）**：用户要「求解器满足管道法——可空心进取空心解、不可则擦远端 jaw，0–90°×各贴库距离都找到正确解，快速准确确定」。新增诊断 `PocketBehaviorDiagTests`（A 角度梯度/B 贴库容差/C 力度→rattle/D 目标球旋转撞 jaw/E 求解器vs暴力扫/F 近远 jaw 分类，输出 `build/pocket_diag/*.png`）。**采纳**：`solveAimOffset` 分支① 进袋接受判据由「干净空心进 `objCushionsBeforePocket==0`」放宽为「落袋前撞库点都在喉部 `objMaxPrepocketCushionDist≤0.18m`」→ 接受真实「擦 jaw 再进」解（保留母球碰前 0 吃库反 kick + 0.18m 喉部闸挡远库翻袋）。**否决**：曾试把 hybrid 分支①/② 整体换成不依赖 `pottedSelected` 的纯平滑代理目标（方向项+`objMinDist` 抵达项+远库翻袋平滑惩罚），实测三轮均回归——①纯方向被薄擦退化解骗（A 全未进）；②加抵达项→矩阵1 出 5 例远库翻袋；③加翻袋惩罚→进袋率暴跌 60%、确定性 case0 0/20、选到 kick 解。根因：本引擎实际落袋与几何代理对不齐，代理目标系统性弱于「直接奖励真进袋」。**验证全绿**：E 角度轴 11/11（含旧漏 28°/72°）、贴库轴 100/180/300mm 全进；F 近 jaw 全 rattle 符合管道法；`PhysicsMatrixTests` 矩阵1 进袋 88% + 0 翻袋/0 出界/0 母球碰前翻袋、矩阵3 确定性 20/20。**遗留→Layer C**：① 求解器仍依赖 `pottedSelected`，72° 等 knife-edge 受引擎事件遍历浮点非确定性偶发翻转（引擎层问题）；② drop/jaw 几何与库模型标定（擦远 jaw 更稳、现象5 加塞旋转真实化）；③ `effectivePocketAimPoint` far-jaw 偏置可显式强化。ADR-P10-04 见 `tasks/phases/P10-physics-content-pipeline.md`。
- **物理引擎技术债第三梯队 · 结构性重构 ✅（2026-06-06，iOS Architect）**：有第一梯队测试网（65 方法/~3800 场景）兜底，**全程零行为改动**（仅位置迁移/命名空间化/常量集中/引用收敛/遍历确定性化，数值与逻辑不动）。**引擎遍历确定性化**：`EventDrivenEngine` 加 `ballOrder`（插入有序球名列表），所有 `Dictionary`/`Set` 遍历改走它，清除 FL-020 §5.2 遗留的哈希种子随机化根因。**死代码清理**：移除失效诊断计数器/print/`debugLogPostEvolveOverlaps`。**D-A1** 拆 `EventDrivenEngine.swift` 1539→860 行，抽出 `EngineNumerics`（纯数值/运动学，可脱离引擎单测）+ `PhysicsEvent`（事件类型+`BallState`）+ `EventCache` + `SceneKitBridge` 四独立文件，16 处调用点改 `EngineNumerics.*`。**D-A3 ◑** `ShotPredictor` 三处显示闸门散落阈值（0.004/0.006/0.12/0.06/0.14/0.07/120Hz/0.1/0.2/0.05）抽入新 `enum DisplayGate` 逐条注明量纲（数值零改；中期「下沉到引擎」留待专门做）。**D-A4 ✅** 几何双真源收敛：内框尺寸/球半径以 `BTPhysicsConstants` 为唯一真源，`AngleSceneCalculator` 改引用（值 2.54≡2.540 等同，零改）；USDZ 袋口几何仍以 `AngleSceneCalculator` 为单一真源（ADR 见 PHYSICS-DEBT §5.5）。`xcodegen` 重生、lint 0、**全物理网 65/65 全过 0 失败**（含确定性 <1e-5m、矩阵母球绕库 0/远处翻袋 0/确定性跨度 0.00°）。改 `PHYSICS-DEBT.md` §5.5 + 标 D-A1/A4 ✅、D-A3 ◑。**剩第四梯队**（B1/B2/B3 依赖真实俯拍视频/USDZ 重导，人工 backlog）。
- **物理引擎技术债第二梯队 · 低风险清理 ✅（2026-06-05，iOS Architect）**：有第一梯队测试网（22 方法/~3800 场景）兜底，安全清理。**D-A2** 删 `CushionCollisionModel.swift`（636 行 Mathavan 2010 死代码，生产只用 Han2005）+ 移除唯一消费它的 print 对照测试 `test_cushion_HanVsMathavan`（零断言）+ Han2005 头注更新；**D-A5** 删死函数 `CollisionResolver.vector4`；**D-B4** `solveAimOffset` 评分魔数（−10 基线/0.3/0.05 scratch/1e-3 正则/100 miss/0.3 kick 惩罚/三级网格半幅步长）全抽入新 `enum AimScoring` 逐条注明量纲（数值零改动，评分序行为由矩阵 1/3 覆盖故未抽 score 纯函数单测）；**D-D2** 搜索保真 500/15 抽具名常量（降保真未做——行为/性能改动有"双景观错位"回归风险，留待专门优化）。`xcodegen` 重生（pbxproj 0 残留）、`PhysicsEngineTests` 23/23、`CushionDiagnosticsTests` 4/4（原 5）、`Benchmark` 14/14、`Invariant` 9/9、`Scenario` 7/7、`Matrix` 3/3（母球绕库 0/远处翻袋 0/确定性跨度 0.00°）全过、lint 0、**零行为回归**。改 `PHYSICS-DEBT.md` §5.4 + 标记 D-A2/A5/B4 ✅、D-D2 ◑。**剩第三梯队**（拆 EventDrivenEngine/下沉显示闸门/几何双真源/引擎遍历确定性化，结构性重构有网兜底）+ 第四梯队（B1/B2/B3 依赖真实视频，人工 backlog）。
- **物理引擎技术债审计 + 第一梯队测试网 ✅（2026-06-05，iOS Architect + QA Reviewer）**：用户要「全面审查物理引擎、避免技术债」，澄清为**只读审计**→产出 `tasks/qa-reports/PHYSICS-DEBT.md`（21 文件 ~6315 行，17 项债务分 架构/正确性/测试/性能 四类 + 测试覆盖缺口矩阵 + 四梯队偿还建议；基线 42 项物理测试全绿）。**核心判断**：内核忠实移植 pooltool、探针绿灯、无阻塞 bug；债集中在 ①移植期死代码（D-A2 `CushionCollisionModel` 636 行 Mathavan 仅测试调用、生产用 Han2005）②`ShotPredictor` 显示闸门 patch-on-patch 耦合（D-A3）③测试以事后诊断为主、缺不变量护栏（D-C1/C3/C4）④常量未真实标定（D-B1）。随后按用户「1和2一起做」+ 两轮「考虑真实台球复杂度、用例量级要到几百上千」执行**第一梯队（第 1+2 项）全扩面**（零生产代码改动）：新建 4 套护栏 `PhysicsInvariantTests`(9)+`PhysicsScenarioTests`(7)+`PhysicsMatrixTests`(2)+`PhysicsPerformanceTests`(3)，全绿 **21 方法 / 约 3800+ 个被断言场景**（XCTest 方法数≠被断言场景数，后者才反映问题）。(a) **属性化随机不变量 ~1700 场景**（种子化 SplitMix64，trial 提到几百）：能量耗散单调（最坏 E/E0=1.0000）、无重叠（0mm）、生产轨迹不出界（0mm，守 FL-018）、自由球减速单调（0 回升）、模拟收敛无失控（终速 0）、**球-球碰撞动量守恒 600 次（误差 0.0000）+ 能量耗散（≤0.99）**、静止球被触碰前不动（0 漂移，重写为「触碰前不动」真不变量——高速母球会绕库飞回撞身后球，旧「身后=永不碰」假设错误）、确定性（引擎逐帧一致/predict 位置 <1e-5m）；(b) **系统化组合矩阵 2054 场景**（确定性网格，逐例断言 + 失败打印复现 ID）：矩阵 1 求解器进袋合约 338 可行 predict（0 出界、0 画面≠物理、进袋率 85%、52 组沿正确线停袋前）；矩阵 2 裸引擎出射方向 1716 组（**最坏方向误差仅 1.9°=零翻袋坏解**、穿库逃逸率 1.1–1.3% 优于审计估计 2.7%）；(c) **真实球理场景 ~50**：跟/定/缩杆落点次序、力度→走位路程单调（5.41→12.52m）、加塞改目标球方向、带塞吃库变线、连续多库 9 库不出界、组合球串二库 1125mm、清晰球进袋矩阵 32/32；(d) **性能门槛**：单杆中位 101ms（预算 800）、满台+8 障碍 514ms（预算 3000）+ measure 基线。**测试期发现（强化 D-C3）**：predict 分离角等派生标量带 ~6.6e-5° 运行间微抖动，定位为求解器 ~75 次短模拟评分择优时浮点求和顺序（引擎字典/Set 遍历）所致——非混沌发散，护栏锁 0.02°；根治＝引擎遍历改确定性有序容器（归第三梯队，不阻塞）。`xcodegen` 收纳 4 文件、lint 0。**第一梯队完成**：D-C2 已由矩阵 2054 场景实质收口（无翻袋坏解/方向连续/不穿库均转机器断言），`ShotScenarioRenderTests` PNG 出图保留为人工复核用途。**进一步按用户「直球求解必须保证母球碰目标球前不吃库、目标球进袋前不吃库」加体检 → 矩阵 1 抓出真 bug FL-020**：3/285 进袋解母球先吃 4 库，聚焦 `t3p5c10s+v2.8` 同输入连跑 30 次 `cueCushionsBeforeContact` 在 0/4 随机翻转、进袋 28/30、分离角取 3 值（求解器约 40% 概率选「母球绕 4 库歪打正着」kick 退化解 + 引擎无序遍历致宏观翻转）。**修复**：`ShotPrediction` 加权威字段 `cueCushionsBeforeContact`，`solveAimOffset` 最优区追加 `&& cueCushionsBeforeContact==0`（钉死直击解唯一占 −10）+ 方向解支轻惩罚。修复后矩阵 3（t3p5/t3p4/t4p4 各 20 次重跑）cuePreBank 恒 0、进袋 20/20、分离角跨度 0.00°（连旧记的 1e-4° 微抖动一并消除——那其实是评分并列的尾巴）；矩阵 1 进袋率维持 85%、母球绕库 0、远处翻袋 0；`PhysicsEngineTests` 23/23、Invariant 9/9、Scenario 7/7、Reconstruction 2/2 全过、lint 0。新增 FL-020 + 回写 `10-ios-architect.mdc` v0.6。本轮**含 1 处生产求解器修复**（非零生产改动）。
- **P11 走位编排器与击打序列 ✅（2026-06-05，iOS Architect + SwiftUI/Data，ADR-P11-01）**：用户要「类似分离角的页面，桌上自由摆母球 + 任意目标球，逐杆选目标+袋口、调连续力度/打点控制母球落点，串成击打序列」，用途＝教学视频制作 + 走位训练。复用 N 球物理引擎（`EventDrivenEngine`）+ USDZ 现成 16 颗球（`AngleTrainingScene.allBallNodes`）+ 单杆交互（`AngleSceneView`），改动集中在新模型/新页面 + `ShotInput` 小扩展。**交付**：①`ShotInput.obstacles`+`ShotPrediction.finalPositions/pocketedBalls`（每杆只对「选中目标+指定袋口」求解，其余在桌球作障碍真实碰撞，瞄准评分零改动）；②内容模型 `PositionPlayModels`（BoardSnapshot/PlannedShot/SequenceStep/PositionPlaySequence，进袋离场回库 + 中途改摆截断重录 + 球号无规则语义）；③`PositionPlayViewModel` + `PositionPlayComposerView`（球库面板/拖球/点选目标/选袋/连续力度滑杆+打点盘/序列时间轴）；④SwiftData `PositionPlaySequenceEntity`（JSON blob）+ `PositionPlaySequenceStore` + 注册 `ModelContainerFactory`；⑤`PositionPlayDrillExporter`（序列→符合 schema 的 Drill JSON，`shotIntent.shots[]` 逐 Step）；⑥`Core/Media`：`SequenceVideoExporter`（SceneKit 真台逐帧 `SCNRenderer.snapshot`）+ `VideoWriter`(AVAssetWriter mp4) + `GIFEncoder`(CGImageDestination) + `ShareSheet`；⑦多杆 `PositionPlaySequencePlayer` + `PositionPlaySequencePlayerView` + `PositionPlayTrainingView`（@Query 列已存序列）。**IA**：编排台→角度 Tab 进阶、走位训练→角度 Tab 训练（仅扩 `AngleRoute`+`angleDestination`，不进球库、不动 5 Tab）。`xcodegen` 收纳 14 个新文件、`make build` ✅、`PhysicsEngineTests` **23/23**（含 2 条多球障碍用例：远处障碍不挡仍进 / 进球线被挡不进）、lint 0。新增 `tasks/phases/P11-position-play-composer.md`（ADR-P11-01）。**遗留非阻塞**：满台单杆求解 ~数百 ms（编辑模式可接受，跟手度后续优化）；序列云端 OTA；训练形态评分细化。
- **P11 编排台交互/布局重构 ✅（2026-06-05，SwiftUI Developer，ADR-P11-02）**：用户逐条反馈首版编排台问题（击打后自动复位、重置全清、球库单行数字、顶部状态栏冗余、球库压桌等）。**交付**：①**击球状态机（保留击球/记录两键）**——`play` 改 `finishStrike`：击球后球停在 `finalPositions` 终局、进袋离场，不再复位；编辑入口（拖球/选目标/换袋/调参）若 `hasStruck` 先 `restorePendingBefore()` 软回退再算；「记录」以 `pendingBefore` 为 before；「重打」`replayCurrent` 只退本杆不动已记录的杆；「清空并重来」`resetAll` 移入更多菜单。②**完整拖拽**——球库球拖到桌面任意点落位（新 `TableProjector.unproject` 反投影 + `clampMultiBall`），桌上球拖回球库区松手即移除（新 `AngleSceneView.onDragEndedAt` 带结束坐标；旧 `onDragEnded` 签名不变，其它页零改动）。③**真实球面球库**——新 `Core/Scene/BallFaceRenderer` 进编排台离屏烘焙 USDZ 球面圆图缓存，双行 `LazyHGrid` 替代数字 token。④**夹角浮标贴目标球**——移除顶部状态栏，夹角+选中环用 SwiftUI overlay 经 `TableProjector.project` 跟随目标球屏幕投影；可行性=环色、母球进袋=红点（去整条文字）。⑤**布局不挡桌**——球库实心底栏在 `VStack` 流内、球桌独占上方铺满。⑥按钮：播放→击球（自绘 `CueStickShape` 图标）、重置→重打。`make build` ✅、lint 0。改 `AngleSceneView.swift`/`PositionPlayViewModel.swift`/`PositionPlayComposerView.swift`，新增 `BallFaceRenderer.swift`；ADR-P11-02 见 `tasks/phases/P11-position-play-composer.md`。
- **P11 分离角页底部控制条对齐编排台 ✅（2026-06-12，SwiftUI Developer，ADR-P11-09）**：用户三点要求（分离角打点/速度位置样式参考编排台；打点弹层底材用分离角的半透明材质更舒服；功能按键全移最下方与编排台同款）。①分离角页删右侧 FAB 列 + 「击球设置」HUD（连 `HUDHeightKey` 机制），改编排台同款底部条：`BTSpinMiniIcon` 打点入口 + 连续力度滑条(0.5–6.0) + 档名读数 + 重置圆钮 + 击球胶囊；`speedLevel` 5 档枚举→连续 `velocity`（默认 3.3）。②打点盘改共享浮层卡片 `BTSpinPadCard`（「打点」标题+✕+`BTSpinPad`+读数+回中，`ultraThinMaterial` 圆角卡浮在球桌底缘）——**两轮坑**：系统 sheet 方案 a) `environment(\.colorScheme,.dark)` 不影响 presentationBackground 材质解析（白底）；b) `preferredColorScheme(.dark)` 后 sheet 底下纯黑+压暗层使材质过深被用户打回——半透明材质要透出球桌绿色必须浮在球桌上方（与旧「击球设置」HUD 同位）。③编排台私有组件下沉共享：`BTSpinPadCard`/`BTSpinMiniIcon`/`CueStickShape`/`PowerDisplay.name`/`SpinDisplay.readout` 迁入 `Core/Components/BTSpinPad.swift`，删两处重复实现与 spinPadSheet。**验证**：`make build` ✅、`testScenePopups` 三轮截图核验 ✅（底部条与编排台一致、两页浮层卡透出桌面绿色）、lint 0。ADR-P11-09 见 phase 文件。
- **P11 2D 球桌统一自适应取景 + 角度首页海报墙 ✅（2026-06-12，SwiftUI Developer + UI Reviewer，ADR-P11-08）**：用户两点打回（角度首页仍简陋需一直下滑；2D 球桌页位置/占比/居中/按钮风格不统一）。①**统一取景根因修复**：装桌后实测球桌世界包围盒回填 `CameraRig`，新 `fitRotatedTable(viewSize:)` 按视口宽高比算正交 scale（双轴约束取大 + 1.2% 余量），`AngleSceneView.autoFitsRotatedTable` 渲染循环逐帧适配；分离角/反射/翻袋/2D瞄准/角度与打点/编排台 6 页启用，删 `composerTopDownScale` 等全部硬编码 scale——任何视口球桌完整可见、双轴居中。②编排台去 60pt 左栏：信息上移顶部单行（模式 chips+切角胶囊+警示/录制 pill），球桌占满全宽真居中。③新 `BTSceneFAB`（56pt 圆形 FAB，primary 绿渐变/neutral 半透明）替换 4 页自绘按钮。④角度首页重写为 `BTSegmentedTab`（学习/训练/工具）+ 双列 `AnglePosterCard` 海报墙（渐变封面+大字水印+chip，与训练页 `BTPlanCover` 同语言），每分段单屏放完。⑤打点盘 sheet 加右上 ✕ 关闭钮（原仅可下滑且手势被打点盘吞掉）。**验证**：`testScenePopups` ✅（4 弹层截图核验）+ `testUnifiedDesignPages` ✅（8 帧逐张核验）+ `PositionPlayFreeAimTests` 7/7 ✅、lint 0。**测试基建教训**：UI 测试反复假失败根因=同机另一会话并行对同名模拟器跑 xcodebuild（重装杀进程），UI 截图测试改独立设备（iPhone 17）隔离 + `launchClean` 改进程态校验。ADR-P11-08 见 phase 文件。
- **P11 进袋入洞段 v2 + 场景页统一设计语言 ✅（2026-06-12，SwiftUI Developer + UI Reviewer，ADR-P11-07）**：①进袋消失重做（用户两次打回后根因坐实）：引擎深入/jaw-settle 落袋**不吸心**（pocketed 帧=捕获点，实测距袋心 0.069–0.079m），旧沉入 move 目标取 pocketed 帧位置 ⇒ 零位移原地淡出；新 `solvePocketEntry`——以进袋时真实速度**匀速**冲洞（不提前减速）→ 速度射线与袋口圆求远交点=**远端袋弧碰撞** → 0.12s 回落袋心 → 停顿淡出；袋心按 `nearestPocket` 查找不信任帧位置；回放 SCNAction 与导出器逐帧求值同源（30fps 抽帧核验）。②统一设计语言（黑底+品牌绿标题+指标胶囊+胶囊分段 `BTChipRow`+右下 FAB）：反射/翻袋系统 segmented→胶囊分段、分离角大结果卡→单行指标胶囊、编排台标题改绿、**角度预测页整页暗色重做**（统计下沉顶部胶囊+换题/参考胶囊行）、**角度Tab首页卡片化**（学习/工具整行渐变图标卡、训练/进阶双列彩色封面卡，对齐训练/动作库语言）。`make build` ✅、入洞数值探针 ✅、`make position-export` ✅、新增 `testUnifiedDesignPages` 截图回归 6 帧全核验、lint 0。ADR-P11-07 见 phase 文件。
- **P11 零遮挡布局 + 重打全量恢复 + 进袋沉入 ✅（2026-06-11，SwiftUI Developer，ADR-P11-06）**：用户 5 项体验问题。①重打恢复目标球/袋口/速度/打点/瞄准模式（`lastShot:(before,shot)` 上下文）；②删全部球桌叠层：左侧 56pt 信息栏（进袋/自由+角度厚薄+母球进袋+录制指示）、状态行上移导航栏 principal、击球/录制/重打移右下操作列；③实测 USDZ 外框半长 1.4055（新增实测契约测试），`composerTopDownScale=1.416` 上下木框完整可见、球桌像素尺寸基本不变；④启用 `pocketSinkDuration` 0.22s：进袋改「easeOut 沉入袋心→停顿 0.35→淡出 0.25」，回放/离线导出同源（抽帧核验球肉眼进入袋口圆）；⑤击球后打点自动回中、打点盘 sheet 紧凑化（detent 290→204）。`make build` ✅、FreeAim 7/7、`make position-export` ✅、新增 `testPositionPlayComposerOnly` UI 截图三连核验（布局/击球后/重打恢复与击打前一致）、lint 0。恢复被 xcodegen 删的 `QiuJiUITour.xcscheme`。
- **P11 孤岛移除 + 自动选袋遮挡闸 ✅（2026-06-11，iOS Architect，ADR-P11-05）**：收口 ADR-P11-04 两项遗留（用户拍板「移除，加上障碍球遮挡判定」）。①删「走位训练/我的序列」整条孤岛链路 5 文件（TrainingView/SequencePlayerView/SequencePlayer/SequenceStore/`PositionPlaySequenceEntity`），SwiftData schema 纯减实体（无需 MigrationPlan）、`AngleRoute` 删 `.positionPlayTraining`；序列消费唯一路径=录制分享 JSON→`make position-export`。②`AngleSceneCalculator.isPathBlocked`（X–Z 点到线段距离<2R，投影钳 [0,1]）接入 `selectBestPocket`：障碍球挡「母球→假想球」或「目标球→进球点」即不可行，全不可行退最近袋（可手选）。`make build` ✅、`PositionPlayFreeAimTests` 6/6（含金标准 5 例）、lint 0。ADR-P11-05 见 phase 文件。
- **P11 编排台操作简化轮 ✅（2026-06-11，SwiftUI Developer + iOS Architect，ADR-P11-04）**：用户 11 项需求 + 3 分叉拍板（录制只分享 JSON / 离线脚本本轮做 / 时间轴拿掉）。**(1) 连续击打状态机**：废除 `hasStruck`/`pendingBefore` 软回退与只读预览，击球后桌面前进为新真相（进袋回库、母球停走位终点），自动选下一杆=距母球最近目标球+距目标球最近可进袋袋口（由最小切角改最近距离），「重打」才回退上一杆击打前。**(2) 录制开关**：删逐杆记录/时间轴/保存/MP4/GIF/Drill 导出菜单/退出草稿；「录制」开→每次击球自动记一杆（重打撤回）、停→分享序列 JSON（iso8601）；离线复现=新 `PositionPlaySequenceExportRunnerTests` + `make position-export`（读 `build/position_play_sequences/*.json`→物理引擎复现→`build/position_play_export/*.mp4+.gif`，端到端 ✅ 抽帧核验）。**(3) 球库微边框**：固定两行序（母+1–7 / 8–15）、只显在库球按序补位、32pt 球、删说明文案，栏高减半以上。**(4) 控制行**：力度滑条移球桌/球库之间，左侧 `SpinMiniIcon` 点击弹打点盘；删「设置」「随机」按钮与随机球形。**(5) 进球观感**：`extendPathToPocketRim` 进球线延伸到袋口圆边缘（jaw 碰撞由真实轨迹自带）；`TrajectoryPlayback` 进袋停顿 0.35s 再淡出 0.25s（编排台/导出同源）。**(6) 运杆动画**：回杆 `d=a+k·v`（0.05+0.035v）smoothstep 0.5s→停顿→匀加速出杆（`a=v²/2d`，触球杆速=v），`CueStick.update(pullBack:)` 逐帧驱动。`make build` ✅、`PositionPlayFreeAimTests` 5/5、runner 1/1；`PhysicsEngineTests` 20/23（3 失败与 ADR-P11-03 记录的在途库边物理整合同组既有失败，本轮未触引擎求解路径）。ADR-P11-04 见 `tasks/phases/P11-position-play-composer.md`。遗留：「我的序列」页成孤岛待定夺；自动选袋未含障碍遮挡。
- **P11 编排台系统性整改 ✅（2026-06-10，iOS Architect + SwiftUI Developer，ADR-P11-03）**：系统性审查发现 15 项问题（P0 状态机/数据完整性 ×2、P1 链一致性/竞态/退出丢失、P2 反馈缺失、能力边界 #11/#12、球面图 #15），用户确认后一次性整改。**交付**：①**求解上下文打包** `SolvedShot(before+shot+prediction)`——「击球/记录」只消费它，修「目标球进袋后记录静默失效」（P0）与记录竞态（+`!isComputing` 闸、`invalidatePendingPredict` generation 作废）；②**只读预览模式**——时间轴点按预览（黄横幅+返回编排，编辑入口全 guard），长按=回退（确认对话框）/逐杆备注（播放器展示）；③**自由瞄准模式（#11）**——`AimMode.pocket/.free` 切换，自由杆不选目标/袋口、点桌面/球/袋设方向（`AngleSceneView.onTableTapped`），引擎新增 `ShotPredictor.simulateFree`（直瞄真实模拟+`extraBallPaths`），模型 `PlannedShot.freeAim`（旧数据兼容），新增 `PositionPlayShotSolver` 统一编排台/播放器/视频导出三处求解，自由杆禁导出 Drill JSON（`canExport`）；④**球面图真根因修复（#15）**——USDZ pivot≠球心 → 按 `boundingSphere` 取景 + 无光照直出贴图（修 PBR 过曝白月牙）+ 姿态矩阵出图选 `(π,0,π/2)` + 号码徽标兜底；⑤**状态反馈**——`statusText` 上屏状态行（求解中菊花）、已击球=「记录」绿描边高亮、障碍挡线启发式提示（球心距线<2R）；⑥**破坏性纪律**——随机/清空在有步时=新序列（新 id）+确认、工具栏分享/更多菜单分离、退出自动存草稿；⑦**力度（#12）**——0.5–6.0 m/s（支持轻推）+ 场景区常驻内联滑条 + 档名（轻推/轻/中/中大/大力）。**验证**：`make build` ✅；新增 `PositionPlayFreeAimTests` 5/5（坐标契约金标准+直瞄不变量+Codable 兼容）、`BallFaceRenderDiagTests` ✅（PNG 出图+覆盖率断言）；`PhysicsEngineTests` 20/23——3 失败经 git stash 在 HEAD 复跑全过，**确认源自工作区在途库边物理整合**（与本轮无关，本轮对 ShotPredictor 纯新增）。⚠️ 发现 `table-geometry.md` canvasY↔sceneZ 符号与代码真源相反（双真源矛盾，待文档修正）。ADR-P11-03 见 `tasks/phases/P11-position-play-composer.md`。
- **P10 Track B-2.5 — 打点盘真实化（DR-018）✅（2026-06-05，iOS Architect）**：用户要「按真实母球/皮头尺寸/皮头弧度同比例，不同加塞对应真实加塞点」。①**统一参数源** `CuePhysics`：`tipDiameter=11mm`(中八)、`tipContactRadius`、`tipCurvatureRadius=10.5mm`(nickel)、`miscueLimitFraction=0.5`（打滑极限，满塞≈半个半径），删旧无用且撞名的 `tipRadius`，`CueStick.tipRadius` 改引用单一来源。②**SpinPadView 真实比例重做**：盘=母球正面，加**打滑极限虚线圈(0.5R)**，红色皮头接触斑直径=皮头/母球真实比例(11/57.15≈0.19)取代固定 18pt，拖动**钳到 0.5R**（之前到球边缘 1.0R，物理打不出必 miscue）；读数改「占满塞百分比」。③语义不变（spinX/spinY=接触点偏移/R）但收到真实区间——满塞 squirt≈1.9°（旧版到 1.0R 偏大不可达）。④**皮头曲率精确换算**（用户「两个都做更严谨」）：两球相切几何 `tipContactPullFactor=R/(R+ρ)≈0.731`，打点盘以「皮头中心摆放」为操作量、真实接触点=中心偏移×系数（曲率拉向球心），红点画皮头中心、打滑圈在中心可达边界 0.5R/系数≈0.684R。⑤**shotIntent miscue 体检+守门**：扫全部 Drill，4 条 |spin|幅值>0.5R（c004/c017/c020/c021）按方向钳回 0.5R 改 JSON + 在内容入口 `shotInput()` 加 `clampToMiscueLimit` 守门，4 条缩略图重烘焙。`make build` ✅、`PhysicsEngineTests` **21/21**、lint 0。改 `BTPhysicsConstants.swift`/`CueStick.swift`/`ShotSimulationView.swift`/`ShotIntent.swift`/`PhysicsEngineTests.swift`/`Drills/cueAction/c004|c017|c020|c021.json`/对应缩略图。〔`CueBallStrike` 保持 pooltool 忠实不钳制；钳制只在意图层。〕
- **P10 Track B-2.4 — 母球进袋（失误）误判修复（FL-019）✅（2026-06-05，iOS Architect）**：用户反馈「分离角与走位」页 cut9° 带塞母球**走位弧线擦过中袋嘴后继续走到台面中下部、根本没进袋**，状态栏却报「母球进袋（失误）」。根因＝目标球进袋判定早已用「显示用钳制轨迹最近点 ≤ 捕获窗」做一致性闸门，但**母球 `cuePocketed` 直接取裸引擎信号、无同源闸门**；带塞母球是曲线(squirt+swerve)，袋口 CCD 用直线/抛物线模型排程进袋事件、`resolvePocket` 接受阈值又宽(`pocket.radius+R*1.5`≈中袋117.8mm) → 预测会进漏斗、实际只擦到 54–65mm 却被裸引擎判落袋。**修复**：给 `cuePocketed` 加与目标球**同源的显示一致性闸门**（要求母球钳制轨迹真正落入某袋捕获窗、落袋吸球心后最近点≈0）。诊断 `test_diag_cueScratch`（180 含塞场景）：假阳性 4→**0**（上报进袋 11→6，余 6 为白线真到袋心的诚实 scratch）；`PhysicsEngineTests` 18/18、lint 0。引擎宽松接受阈值留物理标定 backlog。改 `QiuJi/Core/Physics/ShotPredictor.swift` + `QiuJiTests/ShotScenarioRenderTests.swift`（诊断/扫描）。
- **P10 Track B-2.3 — 吃库后立即停住 bug 修复（FL-018）✅（2026-06-05，iOS Architect）**：用户反馈母球**吃库瞬间速度还很快却定格在库边**。根因＝`ShotPredictor.clampedRecorder` 的「穿库安全网」按**回放重采样的解析外推位置**（容差 6mm）判飞出，而 `TrajectoryPlayback` 在「撞库事件帧→下一帧」间用事件帧速度外推、撞库帧仍带朝库入射速度 → 采样位置被外推到库线内侧 7~9mm（吃库越快冲越远，v5.8>6cm）瞬时越界 → 被误判穿库冻结 + `allDone` 截断后续全部轨迹（原始物理其实仍在多次吃库走位）。**修复**：飞出判定改为**基于引擎记录帧（物理真值）**——正常反弹记录帧绝不越线、只有真飞出才会记录帧越界≥6cm 且远离袋口嘴 14cm，预扫每球记录帧求首次飞出时刻才冻结+截断；事件帧间瞬时外推过冲改为**仅化妆性钳位、保持运动不截断**。回归 `test_diag_cushionFreeze` 误冻结 **0/60**、`PhysicsEngineTests` 18/18、`make build` 通过、lint 0。改 `QiuJi/Core/Physics/ShotPredictor.swift` + `QiuJiTests/ShotScenarioRenderTests.swift`（回归守卫）。
- **P10 Track B-2.2 — 目标球"消失"bug 修复 + 360 组合矩阵验证 ✅（2026-06-05，iOS Architect/SwiftUI）**：①**修复目标球消失**：用户反馈「击球后重置/移动目标球时目标球消失」。根因＝`TrajectoryPlayback.action` 在球进袋后追加 `.removeFromParentNode()`，与 `finishPlayback` 复位**竞态**把目标球移出父节点；且 `applyBallLayout`（reset 调用）只设 `isHidden=false`/`position` **不重新挂回父节点** → 一旦被移除，reset/拖动都救不回。修复：(a) `action` 加 `removeOnPocket` 参数，分离角页传 `false`（只淡出、保留节点）；(b) `applyBallLayout` 防御性 `if node.parent==nil { rootNode.addChildNode(node) }` + 复位 opacity。②**360 组合矩阵**（`test_diag_combinatorialMatrix`：目标球开阔/近长库/近短库/近角袋/近中袋/半台 × 多袋口 × 切角 20/45/65 × 力度 1.6/3.3/5.8 × 塞 无/高/低/左/右）：**进袋221 / 方向对未进139 / 刮杆5 / ⚠️方向错翻袋 0**——零翻袋坏解，每个可行球都「进袋或沿正确进球线未进」，坐实 v3.1 修复。新增渲染 `06_cushion_pocket_proximity`/`07_spin_near_cushion`（贴库/近袋/塞×力度接触表，肉眼确认进球线方向 + 杆法走位 follow/draw/squirt 正确）。`make build` ✅、PhysicsEngine 18 全绿、lint 0。③**回放复位迟滞修复**：用户反馈「母球停下后迟迟不复位、像在等目标球」。根因＝之前为修「缓行入袋被截断」把 `clampedRecorder` 的有效时长 `lastActive` 改成「任何运动态都延长」（去掉速度阈值）→ 低滚动摩擦下球会以肉眼几乎静止的速度蠕行很久才转 stationary，有效时长被拖长、复位迟迟不触发。改为：`lastActive` 用 `stopSpeed=0.07m/s` 阈值（蠕停不计），**但单独记录进袋时刻 `lastPocket`** 保证缓行入袋的落袋帧仍被纳入（不回退 cut15/v3.3 截断 bug）。有效时长 = max(可感知运动末+0.1, 落袋+0.2)。验证：cut15 全力度仍 om0 进袋、cut 扫描不变、PhysicsEngine 18 全绿。
- **P10 Track B-2.1 — 求解器目标改为「进球线方向正确」而非强制进袋 ✅（2026-06-04，iOS Architect，ADR-P10-03 v3.1）**：用户洞察「大切角+小力度目标球动能不足进不去可接受，但进球线方向一定要对」。截图诊断坐实旧 bug：求解器**未进时退化为最小化到袋心最近距离**→挑到**绕库擦袋的多库翻袋解**（碰后初始方向错），正是用户看到的"大角度变翻袋"。改 `solveAimOffset` 为 **hybrid 评分**：①能直接进袋(0 撞库)=−10 基线；②否则按**目标球碰后方向 vs 进球线(target→袋心)夹角误差**（平滑单峰、自动补偿 squirt+swerve+throw、天然排斥 banking）。效果：可进的球干净直进；进不去的薄/弱球**沿正确进球线停袋前**（如实未进、不绕库）；左上角袋(idx0)对称恢复；用户截图球形不再翻袋。方向景观平滑 ⇒ 粗扫 0.2°→**0.5°**（~75 次短模拟，predict 提速）。新增 `PhysicsEngineTests.test_predictor_largeCutClearShot_directPotBothCorners`（守护大切角清晰球直接进、不退化翻袋）。物理套件全绿（PhysicsEngine 18、Benchmark 14 E-solver 5/5、DrillBake 5/5）。注：本轮顺带 `xcodegen` 收纳用户新增的 `DrillShotResolver.swift`（缩略图物理烘焙，原未在 target 致构建失败）。
- **P10 Track B-2 — 截图诊断驱动的漏斗袋口模型 v3 ✅（2026-06-04，iOS Architect，ADR-P10-03，PD-010）**：用户"对分离角与走位页生成多袋口/角度/塞/力度的击球，分析轨迹与进袋，用**截图**看真实情况后优化物理引擎及袋口进袋"。新建 `ShotScenarioRenderTests`——纯 CoreGraphics **2D 顶视接触表 PNG** 渲染器（库边/jaw/落袋孔/标记/幽灵球/瞄准点/母球+目标球真实轨迹/进袋判定一并可见，输出 `build/shot_probe/*.png`），对多袋口×切角×塞×力度矩阵出图肉眼复盘。**截图证伪 ADR-P10-02「喉腔模型」已解决**：进袋呈**斑点状非单调闪烁**（角袋 cut15 v3.3 偏不进、瞄准偏移每 0.1° 翻转）。**根因坐实**：①喉腔「弹珠箱」高恢复系数侧/后壁把对准球反复弹射致进袋带碎裂；②求解短模拟与上报全模拟进袋带错位；③粗扫 0.5° 跨过 0.2–0.4° 窄带漏检；④缓行入袋被显示钳制器 0.02m/s 阈值截断；⑤求解 scratch 避让把直接进球推到绕库别扭解。**修复（漏斗袋口模型 = 真实袋口几何）**：(a) 去喉腔弹珠箱、保留 jaw 闸口；(b) 落袋捕获覆盖 jaw mouth（角 0.052→0.070、中→0.075，干净进开口必落袋、jaw 仍拦坏球）；(c) 落袋吸心（`resolvePocket` 球心吸到袋心，画面=物理）；(d) 求解 sim 改 500/15 与上报同保真度 + 粗扫 0.2°；(e) 直接进袋优先（`objCushionsBeforePocket`，评分 −10+撞库数×1.0+scratch×0.3，近全直球如实报母球进袋）；(f) 慢进袋不截断（钳制器按运动态判定）。**结果**：4 张接触表肉眼全部物理自洽——干净直进、~90° 切线走位、follow/draw/squirt 杆法可见、无穿库/碎裂/判进画面不进；仅近全直球如实报 scratch。`QiuJiTests` 物理套件全绿（PhysicsEngine 含新增非单调回归断言、Benchmark 14/14 E-solver 5/5 E-geom 3→4/5、穿库扫描、DrillBake 5/5）、`make build` 通过、lint 0。新增 `QiuJiTests/ShotScenarioRenderTests.swift`。**性能权衡**：单次 predict ~150–200ms（求解提保真度+0.2° 粗扫），后台线程+防抖+取消下可用，取正确性优先。**遗留非阻塞**：薄切 55° 窄口个别力度敏感（真实）；中袋 mouth ±0.035→±0.046；Track B #3 常量标定需真实俯拍视频；predict 跟手度优化。
- **P10 Track B-1 — 物理保真进球管线 ✅（2026-06-04，iOS Architect，ADR-P10-02）**：聚焦用户要的"完整、物理保真的进球点/进球判定算法"。用**程序化 USDZ 网格实测**（`TableGeometryProbeTests`，用户选定测量法）**证伪探针的「jaw 放错 17mm」预设**——库边（±0.635/±1.27）、袋心、jaw（与库边精确相切）实测皆自洽，几何无需重导。真正问题＝旧引擎**袋口只是袋心一个判定圆**（13.4mm 甜点 / 后改 0.055 仍是放大的圈），无真实袋口内部结构，球要么命中甜点要么穿库飞出；闭环求解又在窄口偶落坏局部最优。**用户复评后明确反对"放宽捕获半径"为偷懒、要真实袋口物理。修复**：(a) **真实袋口物理 = 喉腔模型**（`TableGeometry.chineseEightBallQiuJi` + `throatCushions`）：每袋口 = jaw 库（偏转切球）+ **喉腔**（实测 jaw 尖端 `pocketJaws` 沿喉轴挤出的两条侧壁 + 一道后壁，可反弹）+ 喉腔内**物理落袋孔** P（`pocketDropRadius`，球心进孔即落袋）；rattle 由几何自然涌现（穿库飞出 8%→2.7%），落袋孔半径仅表物理洞口、与视觉标记半径解耦。(b) **两逻辑分清**：(A) 袋口判定＝球来时由喉腔真实几何决定进/rattle；(B) 瞄准求解 `solveAimOffset` 固定力度+塞采样寻优最优接触点、在 A 下让目标球落袋（直接/借 jaw），打不进如实报。(c) 稳健化求解（进选定袋优先 −10 基线 + scratch 仅 mm 量级轻罚〔禁 1.0 大值〕 + 粗扫加密 ±16°/0.5° 两级细化）。(d) **画面=物理**：`ShotPredictor.objectPath` 取真实模拟折线（含穿喉腔反弹）、`objectPocketed/simObjectPotted` 改**轨迹基**判定（消除穿库假阳性），`ShotSimulationViewModel` 诚实显示进/未进。**结果**：E-solver 角袋 cut0–45 全力度进、cut55（薄）个别力度敏感、中袋全力度进、c002 由 ⚠️ 转 ✅、5 条试点 Drill 真实模拟 5/5。`QiuJiTests` **291/291**、`make build` 通过、lint 0。新增 `QiuJiTests/TableGeometryProbeTests.swift`（USDZ 实测+进球覆盖诊断）+ `PhysicsEngineTests` 3 条保真断言；报告 `PHYSICS-PROBE.md` §USDZ 实测标定、`DRILL-BAKE-REPORT.md`（c002→✅）。**遗留非阻塞**：中袋 jaw mouth ±0.035→实测 ±0.046；朴素瞄准 E-geom 3/5（窄喉口掠角 rattle 真实物理，求解器规避）；Track B #3 常量标定需真实俯拍视频。
- **动作库轨迹/走位改由物理引擎真算 — ✅（2026-06-04，iOS Architect，DR-017）**：用户"现在球的运动轨迹，看起来不是通过物理引擎计算出来的，而是画出来的线，请修正"。根因：DR-016 渲染层消费 `DrillAnimation.path`（72 条里仅 5 条试点物理烘焙，其余 67 条仍是手画贝塞尔）。修复：轨迹来源前移到 `ShotPredictor`——新增 `DrillShotResolver`（Drill→`ShotInput`，优先 `shotIntent` 否则按摆球+选袋反推中等力度 3.3m/s 无塞）；`DrillThumbnailRenderer.render(drill:)` 画 `prediction.cuePath/objectPath` 真实折线（不可行退回手画）、**72/72 物理重烘焙**；详情页 `DrillSceneController` 后台 `predict` + `TrajectoryPlayback` 按真实模拟逐帧回放（与分离角页同源，含减速/吃库/走位）。`make build` ✅、lint 0、烘焙 72/72 ✅（c001 直线进底中袋、c040 切球分离+目标进右底袋几何自洽）。遗留：67 条无 `shotIntent` 用默认力度反推＝真物理但非原走位意图，补 `shotIntent` 即精确还原。
- **动作库 2D 球桌 → USDZ 真台 2D 顶视那套 — ✅（2026-06-04，DR-016，取代 DR-015）**：用户"不要用这种，要用角度页面里的 2D 视角的 usdz 球桌那一套"。改用 `AngleTrainingScene`（`TaiQiuZhuo.usdz` 真台 + plain 光照）正交顶视真渲染。**缩略图离线烘焙 PNG**：新增 `DrillThumbnailRenderer`（`SCNRenderer` 离屏快照）+ `DrillThumbnailBakeRunnerTests` 烘焙 **72/72** → `Resources/DrillThumbnails/<id>.png`；运行时 `BTBakedDrillTable`+`DrillThumbnailStore`(NSCache) 秒加载、零 SceneKit 成本（不能把 N 个 USDZ 场景塞进滚动网格）。**详情页 live 场景**：`DrillSceneView`+`DrillSceneController` 复用 `AngleSceneView` 顶视 + 摆球 + 烘焙轨迹 + 回放走位。记录页改轻量烘焙图。删除 `BTDrillTableView.swift`、移除 `BTBilliardTable` 视图（仅留 `TableRender`）。`patch-pbxproj-folder-refs.py` 加 `DrillThumbnails` folder ref。`make build` ✅、lint 0、`testDrillLibraryOnly` 截图确认（网格烘焙 PNG + 详情 live USDZ 2D 顶视）。
- **动作库 2D 球桌渲染统一 — ✅（2026-06-04，SwiftUI Developer，DR-015）**：用户反馈"我没看到动作库里改了哪里 / 废弃 BTMiniTable，用现在真实的 2D 球桌"。新建统一拟真渲染器 `BTDrillTableView`（`BTAimTableView` feltOnly 拟真台呢 + `BTRealisticBall` 高光球 + 烘焙/手画轨迹虚线 + 袋口标记 + 目标袋光环），**双模式**：`animationProgress=nil` 静态缩略图（球停起点+全画轨迹）/ `!=nil` 动画回放（轨迹逐段+球随相位移动）。删除 `BTMiniTable.swift`；`BTBilliardTable` 退化为薄封装委托新组件（保留 `animationProgress` API + `TableRender` 常量供 `BTAngleTestTable`）。替换网格卡/`BTDrillThumbnail`/计划迷你台/详情页/记录页共 5 处；去掉 `BTDrillCard` 的 `BTDrillPreviewPlayer` PNG 帧短路（此前 c005 烘焙轨迹被旧 PNG 盖住）。`make build` ✅、lint 0、新增 `testDrillLibraryOnly` UI 截图测试通过（网格+详情拟真渲染确认）。
- **P10 Track A — 动作库内容管线 + 击球意图 schema 雏形 ✅（2026-06-04，iOS Architect，ADR-P10-01）**：探针绿灯后把引擎"用起来"的第一步。设计 Drill「击球意图」schema（归一化摆球+选袋+**连续 velocity(m/s)**+塞，用户改连续值以支持精准走位）→ 离线烘焙（`ShotBaker` 调 `ShotPredictor`+USDZ 对齐球桌 `chineseEightBallQiuJi`，把精确轨迹**回填**现有 `DrillAnimation`，渲染层零改动）→ 物理可达校验报告对接 H-11。新增 `ShotIntent.swift`/`ShotBaker.swift`/`DrillBakeRunnerTests.swift`；`DrillContent.shotIntent?` + `DrillAnimation.source/generator` 均为**可选**（旧 72 条零回归）。5 条多类别试点（c001 直线 / c002 斜角 / c005 一库走位 / c014 定杆 / c024 分离角）烘焙 **5/5 feasible** 并 round-trip 回填（`source:"baked"`）；`QiuJiTests` **203/203**、lint 0、JSON 校验通过。命中 ADR 触发（内容/数据策略 + 引擎抽成离线管线=跨模块边界）→ ADR-P10-01 已采纳。报告 `tasks/qa-reports/DRILL-BAKE-REPORT.md`；schema.md + content-engineering SKILL 同步（PD-008）。遗留：c002 真实模拟未落袋（4.2° 贴 jaw，属 P10 jaw↔洞标定，不阻断显示）。
- **2D 物理引擎探针 — 🟢 绿灯（2026-06-04）**：为"物理升级（动作库 GIF/精讲/视频统一由引擎驱动 + 暑假 Tier 1 上线）"做 go/no-go 探针。新建 `QiuJiTests/PhysicsBenchmarkTests.swift`（14 项体检：A 击打/B 球-球/C 库边/D 走位/E 进袋，带 Dr.Dave/Alciatore 参考 band）。**核心物理 A/B/C/D 全健康**；**根因坐实在几何**：双真源（CAD 模拟 vs USDZ 标记差 ~17mm）靠 `ShotPredictor` 60mm 容差糊合。修复：抽共享 `TableGeometry.chineseEightBallCushions(y:)`，给 `chineseEightBallQiuJi` 补齐 jaw 圆弧（v1 无→v2 全），`ShotPredictor` 改用它 + **删 60mm 容差改按 pocketId 精确判进袋** + 加只读 `simObjectPotted`。**产品路径 E-solver 修后 5/5 诚实进袋**；回归 `QiuJiTests` **278/278**。遗留 1 项 **P10 标定**：jaw 圆弧仍取 CAD 坐标、与 USDZ 洞心残留 ~17mm 错位 → 朴素瞄准贴角球 rattle（产品用求解器规避，不阻断）。报告 `tasks/qa-reports/PHYSICS-PROBE.md`。**结论**：探针绿灯，可推进物理升级；P10（jaw↔洞对齐 / 目标球真实轨迹 / 真实视频标定常量）排在绿灯之后。
- **整体进度**：9 / 11 Phases 完成（R0 ✅ | P2 ✅ 附条件 | P4 ✅ 附条件 | P5 ✅ | P6 ✅ | P7 ✅ | R-UI ✅ 附条件 | R1 ✅ | P9 ✅）+ P8 🔄
- **UI 设计交付物**：44/44 任务完成，40 帧 Light + 5 帧 Dark + 3 份标注已就绪（见 `ui_design/final-report.md`）
- **最近 UI 审查 + 修复**：2026-05-29 全 App 浅色巡游审查（`tasks/ui-reviews/UR-20260529-FullApp-Light.md`）发现 9 项（P1×4 / P2×5），**同日已修复并回归 U-01~U-07**（7 项代码问题，FL-011~014 标 ✅），剩 U-08（视频/数据导出占位）/U-09（图标）为内容/设计轨道。关键修复：角度台呢荧光绿（根因＝`enhanceClothMaterials` 的 multiply 守卫漏掉 USDZ NSURL 贴图，改为无条件着色 + plain 专用暗化 tint + 光照/曝光下调）、计划详情期号标签与状态栏重叠、日历空状态被 Tab 栏遮挡、Paywall 无限 loading（加 8s 超时兜底）、错别字「浅淡球感」、动作详情顶栏穿透。**下一步**：角度交互态（`现有问题.md`：小角度文字挤压/左下 HUD 上移常驻/自动选袋 85°）仍需动态复检。
  - 新增巡游工具：`QiuJiUITests/ScreenshotTourUITests.swift` + 专用 scheme `QiuJiUITour`（仅构建 App+UITests，绕开 QiuJiTests 的 TEST_HOST 误配 QiuJi.app↔球迹.app）。
- **QA-P9 收尾 + 测试基建修复（2026-06-02，QA Reviewer）**：用户确认 P9 人工功能验收通过，遂闭环 AI 侧验收。逐条代码复核发现 2 项缺口并当场修复：① **FL-016** 几何角度训练 `GeometricAngleQuizView` 只计数不阻断 Freemium（Scene 页已生效、几何页漏），按既有范式补「今日剩余 N + 限额卡 + 解锁入口 + 按钮禁用」；② 补 `AngleSceneCalculator` 往返 XCTest（T-P9-02 DoD，精度<0.001，6 条全过）。另发现对照表「球种切换」与 **P9-05 设计 APPROVED（移除球种切换、固定中八）冲突** → 撤销我误加的切换、回归设计（仅保留曲线标记点 `.red→.btAccent` token 合规）。**PD-007**：修复让整套 `QiuJiTests` 命令行从未跑通的根因——`TEST_HOST` 指向 `QiuJi.app` 而产物是 `球迹.app` + 缺 `PRODUCT_MODULE_NAME` 致 `@testable import QiuJi` 失败；在 `project.yml`+`pbxproj` 双向钉死后 **241/241 全通过**。交付物：`tasks/qa-reports/QA-P9.md`、`ui_design/tasks/P9-REVIEW/consistency-review.md`、`09-UI设计交付文档.md` §3.3 补齐。`make build` 通过、lint 0。**P9 归档，整体 9/11 Phase；主线仅剩 P8 人工事项（H-17 / TestFlight / App Store）。**
- **图标系统优化（2026-06-01，UI Reviewer + SwiftUI Developer）**：用户反馈"图标乱/丑、Logo 不动"。专项审查 `tasks/ui-reviews/UR-20260601-IconSystem.md`（6 项：P1×3/P2×3），分两阶段修复并截图回归。**阶段 A**：新增统一 `BTIconBadge`（淡色圆底+单色图形），Profile 列表彩虹圆底（红/蓝/紫/灰）→ 收口品牌绿（仅订阅留金）；空状态举杠铃健身小人→品牌 `BTLogoMark`、锤子→训练计划语义图标。**阶段 B**：`BTDrillCategoryIcon` 整体重写为统一系统（双线宽+标准球半径+单一金色强调，8 分类构图统一），`BTTrainingIcon` 加重对齐 SF Symbol。**关键 bug**：drawFundamentals `r = env.ballRadius * s * 1.4` 重复乘 scale（env.ballRadius 已含 s）→「基础功」爆框成橙方块，登记 **FL-015**。`make build` 通过、巡游 0 失败、`screenshot-v4/` 26 帧刷新、lint 0。剩余 backlog（非阻塞）：~180 处裸 `systemName` 增量迁移到 `BTIcon`（U-I05/06）。
- **翻袋/反射解球器「真实反射模式」（2026-06-03，iOS Architect + SwiftUI Developer，ADR-P9-02）**：用户要求在原「入射角=反射角」理想模型外，增加真实模式（反射角相对法线略小于入射角）+ 两个控件（理想/真实开关 + 缩小因子滑块 0.50–1.00），让用户几次试打拟合自己的球台/发力。**物理模型**：碰库时法向分量翻转、切向分量×factor（`tan θ_out = factor·tan θ_in`，factor=1 即理想）。**算法**：镜像展开仅 factor=1 成立 → 新建共享 `Features/AngleTraining/ReflectionSolverCore.swift`（`CushionReflectionSolver`：正向射线追迹 + 射击法扫描发射角→变号区间→二分求根 + 每次反弹校验命中库），并提供 `CushionReflectionSettings`（UserDefaults，两页共享 factor/模式）。**混合策略**：`factor=1` 两个求解器沿用原镜像展开（零回归）；`factor<1` 对每条理想解库序射击得真实解，理想解作蓝色虚线对照。改动：`BankShotCalculator`/`DiamondSystemCalculator.solveAll` 加 `factor` 参数 + `Solution` 加理想对照路径；两页 VM/View 接入 `ReflectionModeControl`（分段开关+滑块）+ 双路线绘制；`AngleTrainingScene` 加 `addDashedLine`。**测试**：新增 `CushionReflectionTests`（反射缩放/单库等价镜像/真实模式发散随因子单调），`make build` 通过、`QiuJiTests` **256/256** 全通、lint 0。
- **「分离角与走位」进袋几何切换（2026-06-03）**：用户反馈"目标球还是进不了袋"并指向项目一的进袋判定/袋口参数。诊断：进袋判定（EventDrivenEngine + enforceTableBounds）本就是项目一移植件、与项目一一致；差距在**球台几何**——我初版用简化"6 直库+6 圆袋无 jaw"，球易擦过窄捕获窗(13mm)进不去。改为 `ShotPredictor` 直接用项目一完整 `TableGeometry.chineseEightBall()`（**角袋 jaw 圆弧 + jaw 直线段 + 中袋圆角 + CAD 袋口**，喇叭口导球入袋）。进选定袋判定：以 `AngleSceneCalculator` 袋口中心（=屏幕标记）为目标 + 最近距离 60mm 阈值甄别（两套中心差 ~17mm）。新增单测 `easyCornerPot`/`defaultLayoutPots` 验证近距小角度 & 默认球形开箱即进，`PhysicsEngineTests` **12/12**。**仍存**：极薄+远袋因能量不足到不了袋（物理事实，提示"此角度/力度难进袋"）；落袋点在 CAD 中心、与标记盘差 ~17mm（在盘内，可接受）。
- **「分离角与走位」页 UX/性能修复轮（2026-06-03，SwiftUI Developer）**：用户真机反馈四点并修复——①**播放卡顿**根因＝`recompute()` 把闭环求解(~26 次模拟)放主线程同步跑，遇加塞乱飞球单次模拟可达数百 ms → 累积卡死；改为**后台串行队列预测 + 代次(generation)守卫丢弃过期结果**，主线程只画线，UI 不再卡。②**播放结束复位**：`finishPlayback` 把两球复位到击球前位置（进袋球重新加回场景+恢复 opacity）并**瞬时重画原轨迹**（不重新求解）。③**"力度不足"误导**（力度 90 仍提示不足）：实为薄球+远袋能量不足，文案改为"此角度/力度难进袋，试试加大力度或换袋口"。④**UI 重做**：顶部结果卡（分离角大字+状态+后台计算转圈）、底部控制卡（打点盘带"击球点"标签+径向高光、力度滑杆+高低/左右塞量读数、重置+播放双按钮）。另默认摆一个清晰可进的中等角度球。播放速度 1.4×。`make build` 通过、`PhysicsEngineTests` 10/10。**注**：仍未本地真机复测此轮 UI（用户侧验证）。
- **「分离角与走位」物理演示页 + pooltool 物理引擎移植（2026-06-03，iOS Architect + SwiftUI Developer，ADR-P9-03）**：用户要求角度页新增「分离角 + 母球/目标球轨迹」动态演示，可调击打袋口/力度/上下左右塞——现有都是纯几何模型（无旋转/力度/库边/throw），故**完整移植项目一(`01.billiard_app`)的事件驱动物理引擎**（即 **pooltool** 的 Swift 移植）到 `QiuJi/Core/Physics/`（15 文件：`EventDrivenEngine`/`AnalyticalMotion`/`CollisionResolver`/`CushionCollisionModel`/`CueBallStrike`/`CollisionDetector`/`QuarticSolver`/`TrajectoryRecorder`/`TrajectoryPlayback`/`SimulationWorker`/`TableGeometry`/`BallMotionState`/`BTPhysicsConstants`/`SCNVector3+Physics`/`PerformanceProfiler`）。**以 pooltool 为准校正**（用户明确要求）：①`CueBallStrike` 修复角速度偏大 ~35×（缺 R 因子）的 bug + 帧/符号，重写为 pooltool z-up 求解后映射到 SceneKit y-up；②`resolveBallBallPure` 弃用自写冲量、改用 pooltool `_resolve_ball_ball`（滑移 `u_b·|Δvₙ|·(−v̂)` + 无滑移 1/7、5/14 + Alciatore 摩擦曲线）；③Mathavan 库边逐式核对一致；④常量全对齐 pooltool 默认值。新建 `TableGeometry.chineseEightBallQiuJi`（袋口/库段取自 `AngleSceneCalculator`，对齐 USDZ，v1 仅 6 直库+6 圆袋，暂无角袋 jaw 圆弧）；门面 `ShotPredictor`（摆球+袋口+力度+塞→两球轨迹/分离角/切线/进袋，轨迹用 `TrajectoryPlayback` 解析采样捕捉塞曲线）；**进袋逻辑**：选定袋口先过可行性闸门（切球角≥89°/母球挡路→「当前角度无法进袋」不画轨迹），几何可进则用**闭环瞄准求解**（粗扫±8°+细化±1°，每候选短时模拟评分）找到真正落袋的发射方向——一次性纳入 squirt+碰撞 throw+swerve（开环只补 squirt 在加塞时仍漏袋），单次 predict ~30–40ms；`ShotSimulationViewModel`/`ShotSimulationView`（复用 `AngleSceneView` 拖球+点袋、力度滑杆+打点盘，实时画轨迹，「播放」用 `recorder.action` 让球沿轨迹跑+进袋淡出+复位）。接线 `AngleRoute.shotSimulation`（角度页「进阶」）。去重 `CameraRig` 的 file-private `SCNVector3` 扩展避免歧义。新增 `QiuJiTests/PhysicsEngineTests.swift`（90°法则/高低杆/squirt/端到端，8 例全过）。`make build` 通过。**后续 TODO**：角袋 jaw 圆弧、力度标定、真机 UI 截图验证（本轮仅编译+单测）。
- **角度训练视觉拟真化（2026-06-01，SwiftUI Developer）**：新增可复用 `Core/Components/BTRealisticBall.swift`（拟真球：球面明暗+高光+接触阴影，矢量无版权风险）与 `BTAimTableView.swift`（拟真 2D 台面插图：木纹库边/皮革袋口/颗星/台呢光影，暴露台呢矩形供叠加）。改造 `AimingPrincipleView` / `BallFeelView`（学习文档插图）与 `GeometricAngleQuizView`（几何角度训练页）。**修正**：用户反馈拟真"球桌"装饰（木纹库边/袋口/颗星）显廉价且抢焦点、不如之前简洁——已改为干净路线：`BTAimTableView` 统一 `feltOnly`（近平台呢）+ 新增 `BTPocketMark`（干净袋口点）+ 加粗瞄准线/角度弧 + 几何页黄色扇形高亮夹角，让"角度/线/假想球"成为主角，保留有质感的球。新增轻量 `testAngleLearningPages` 仅截角度三页（完整巡游在本机偶发模拟器 server died，已规避）。`make build` 通过，回归截图 build/v2-09/11/12。注：`make xcodegen` 会清掉自建 `QiuJiUITour.xcscheme`，已用确定性 blueprint id 原样重建。

---

## R0 Design System Upgrade — ✅ 已完成

> **前置**：UI 设计全部完成。P4 暂停于 T-P4-04。详见 `tasks/phases/R0-design-system.md`。

| 任务 | 状态 |
|------|------|
| T-R0-01 创建 UI-IMPLEMENTATION-SPEC.md | ✅ 已完成（2026-04-05）|
| T-R0-02 Token 值审计 | ✅ 已完成（2026-04-05）|
| T-R0-03 BTButton 补全 7 种样式 | ✅ 已完成（2026-04-05）|
| T-R0-04 新建组件 Batch 1（导航/布局） | ✅ 已完成（2026-04-05）|
| T-R0-05 新建组件 Batch 2（训练） | ✅ 已完成（2026-04-05）|
| T-R0-06 新建组件 Batch 3（反馈/分享） | ✅ 已完成（2026-04-05）|
| T-R0-07 校验与更新已有组件 | ✅ 已完成（2026-04-05）|
| QA-R0 Phase R0 验收 | ✅ 附条件通过（2026-04-05）— 3 项 P2 改进记入下一迭代 |

---

## P1 Foundation — 部分完成（阻塞项已推迟）

| 任务 | 状态 |
|------|------|
| T-P1-01 Xcode 项目初始化 | ✅ 已完成 |
| T-P1-02 SPM 依赖初始配置 | ✅ 已完成（ADR-001）|
| T-P1-03 Design System Token | ✅ 已完成 |
| T-P1-04 5 Tab 导航骨架 | ✅ 已完成 |
| T-P1-05 登录流程 UI | ✅ 已完成 |
| T-P1-06 Sign in with Apple | ✅ 已完成 |
| T-P1-07 REST API + 手机验证码登录 | ⏳ 待开始（H-15 推迟） |
| T-P1-08 微信登录集成 | ⏳ 待开始（H-05 推迟） |
| T-P1-09 AppConfig + .gitignore | ✅ 已完成 |
| QA-P1 P1 验收 | ⏳ 待开始 |

---

## P2 Data Layer — 功能完成，待人工验收

| 任务 | 状态 |
|------|------|
| T-P2-01 SwiftData Schema | ✅ 已完成（2026-03-29）|
| T-P2-02 Local Repository | ✅ 已完成（自动化测试 42/42）|
| T-P2-03 ~~CloudKit~~ | ✅ 已取消（ADR-002）|
| T-P2-04 Bundle Fallback JSON | ✅ 已完成（2026-03-29）|
| T-P2-05 后端用户数据同步 | ✅ 已完成（2026-03-29）|
| T-P2-06 匿名用户本地模式 | ✅ 已完成（2026-03-29）|
| T-P2-07 SyncQueue | ✅ 已完成（2026-03-29）|
| QA-P2 验收 | ✅ 附条件通过（2026-04-10）— 235/235 自动化 + 31/31 人工测试；3 issue（FL-001/FL-002/B-03）已修复 + Code Review 确认；条件：用户重建后确认修复生效 |

---

## P3 Drill Library — ✅ 附条件通过

| 任务 | 状态 |
|------|------|
| T-P3-01 ~ T-P3-11 | ✅ 全部已完成（2026-03-29，自动化测试 47/47）|
| QA-P3 验收 | ✅ 附条件通过（2026-04-11）— 自动化 47/47；人工 TP-P3 50/53 执行，3 项失败（FL-003/FL-004/FL-005）已修复并验证；设备矩阵/可访问性/性能待补测 |

---

## P4 Training Log — ✅ 附条件通过

| 任务 | 状态 |
|------|------|
| T-P4-01 官方训练计划 JSON | ✅ 已完成（2026-03-29）|
| T-P4-02 训练 Tab 今日计划视图 | ✅ 已完成（2026-03-29）|
| T-P4-03 官方计划列表与详情页 | ✅ 已完成（2026-03-29）|
| T-P4-04 开始训练流程 | ✅ 已完成（2026-03-29）|
| T-P4-05 训练中 Drill 记录界面 | ✅ 已完成（2026-04-05，使用 BTSetInputGrid + BTExerciseRow）|
| T-P4-06 心得备注输入 | ✅ 已完成（2026-04-05，匹配 code.html 设计，DR-004）|
| T-P4-07 训练完成总结页 | ✅ 已完成（2026-04-05，匹配 code.html 设计，使用 BTLevelBadge 等 R0 组件）|
| T-P4-08 TrainingSession 持久化 | ✅ 已完成（2026-04-05，saveTraining 已在 T-P4-04 实现并测试通过 30/30）|
| T-P4-09 自定义训练计划 | ✅ 已完成（2026-04-05，匹配 code.html 设计，DR-007）|
| T-P4-10 TrainingShareView（新增） | ✅ 已完成（2026-04-05，BTShareCard 升级匹配 code.html + 定制面板 + 分享入口）|
| QA-P4 验收 | ✅ 附条件通过（2026-04-11）— 自动化 235/235 + 人工 TP-P4 92/98；FL-006/FL-007/FL-008 已修复，FL-009 P3 延后 |

---

## P5 Angle Training — ✅ 已完成

| Phase | 状态 | 备注 |
|-------|------|------|
| P5 Angle Training | ✅ 已完成（2026-04-05） | 代码审查 + 设计对齐 + 22 测试通过 |

---

## P6 History + Statistics — ✅ 已完成

| 任务 | 状态 |
|------|------|
| T-P6-01 历史 Tab 日历视图 | ✅ 已完成（2026-04-05）— BTSegmentedTab + 6 行日历 + 训练分类标签 + 设计对齐 |
| T-P6-02 训练详情页 | ✅ 已完成（2026-04-05）— Sheet 模态 + 统计横滚 + Drill 组明细 + 底栏操作 |
| T-P6-03 统计视图 | ✅ 已完成（2026-04-05）— BTTogglePillGroup + 三张统计卡片 + 左侧绿线装饰 |
| T-P6-04 训练频率柱状图 + 趋势线 | ✅ 已完成（2026-04-05）— Swift Charts BarMark + RuleMark，琥珀+品牌绿双色 |
| T-P6-05 各类别成功率对比 | ✅ 已完成（2026-04-05）— 2 列网格替代雷达图，环比变化 + 迷你柱状图 |
| T-P6-06 Freemium 历史查看限制 | ✅ 已完成（2026-04-05）— HistoryAccessController 60 天限制 + 锁定提示 |
| QA-P6 验收 | ✅ 附条件通过（2026-04-12）— 人工 TP-P6 日历/详情/动画/边界/性能全通过；统计 Pro paywall 正确生效（符合规格）；Pro 统计 UI + 60 天限制 e2e 待 TestFlight 补测 |

---

## P7 Subscription — ✅ 已完成

| 任务 | 状态 |
|------|------|
| T-P7-01 StoreKit 2 集成 | ✅ 已完成 — StoreKitService + Products.storekit 3 个 IAP |
| T-P7-02 订阅状态管理 | ✅ 已完成 — SubscriptionManager isPremium + Transaction.updates 监听 |
| T-P7-03 订阅页 UI | ✅ 已完成（2026-04-05）— 深色 #111111 全屏 + 金色编号功能列表 + 3 列方案卡 + 年订绿框推荐 |
| T-P7-04 恢复购买 | ✅ 已完成 — AppStore.sync() + 成功/失败 Alert |
| T-P7-05 Freemium 边界整合 | ✅ 已完成（2026-04-05）— 修复 AngleTestView limiter isPremium 同步 bug |
| QA-P7 验收 | ✅ 通过（2026-04-05）— 代码审查 + 234/234 自动化测试通过 |

---

## R-UI Existing Page Alignment — ✅ 附条件通过

> 详见 `tasks/phases/R-UI-alignment.md`

| 任务 | 状态 |
|------|------|
| T-RUI-01 TrainingHomeView 对齐 | ✅ 已完成（2026-04-05）— 今日安排卡片 + BTSegmentedTab 计划浏览 + 筛选 Chip + 固定底部按钮 + 空状态 |
| T-RUI-02 DrillListView + DrillDetailView 对齐 | ✅ 已完成（2026-04-05）— 灰色操作图标行 + 标签行 + darkPill/primary 固定底栏 + Pro 金色底栏 |
| T-RUI-03 ActiveTrainingView 对齐 | ✅ 已完成（2026-04-05）— 毛玻璃顶栏 4 图标 + 计划名进度条 + 5 键底栏带文字标签 + 橙色热身标记 |
| T-RUI-04 ProfileView + LoginView 对齐 | ✅ 已完成（2026-04-05）— 彩色圆底图标菜单 + 月度概览 + 游客警告/Pro 推广卡 + 三按钮登录 + 药丸验证码输入 |
| T-RUI-05 OnboardingView 对齐 | ✅ 已完成（2026-04-05）— 品牌绿圆底图标 + QJ Logo + 强制浅色 + 3 FeatureRow |
| QA-RUI 验收 | ✅ 附条件通过（2026-04-05）— D-1 已修复；8 项 P2 改进记入 P8 |

---

## P8 Polish & Release — 🔄 进行中

| 任务 | 状态 |
|------|------|
| T-P8-01 Privacy Manifest | ✅ 已完成（2026-04-05）— PrivacyInfo.xcprivacy 创建 + Xcode Target 添加 |
| T-P8-02 性能优化 | ✅ 代码审计通过（2026-04-06）— LazyVStack/Canvas/debounce 等已优化；4 项 Instruments 指标待人工验证 |
| T-P8-03 空状态与加载态全覆盖 | ✅ 已完成（2026-04-05）— BTShimmer 骨架屏 + 6 场景空状态/加载态全覆盖 |
| T-P8-04 首次引导流程完整版 | ✅ 已完成（2026-04-06）— 3 页 TabView + Capsule 页指示器 + 跳过/登录分页按钮 |
| T-P8-05 个人设置页 | ✅ 已完成（2026-04-06）— SettingsView（球种+周目标）+ 账号注销 + 隐私政策链接 |
| T-P8-06 账号注销与数据删除 | ✅ 已完成（2026-04-06）— 在 T-P8-05 中一并实现（二次确认 + DELETE API + 失败重试）|
| T-P8-07 XCTest 核心流程测试 | ✅ 已完成（2026-04-06）— 235/235 通过（+1 CRUD update 测试）|
| T-P8-08 TestFlight 内部测试 | ⏳ 待开始 |
| T-P8-09 App Store 资产准备 | ⏳ 待开始 |
| T-P8-10 App Store 提交审核 | ⏳ 待开始 |
| T-P8-11 Dark Mode 全面通刷 | ✅ 已完成（2026-04-05）— 21 Token 双值验证 + 14 文件修复 + D-1~D-7 全部确认 |
| T-P8-12 人工测试计划更新与执行 | ✅ 已完成（2026-04-06）— TP-P2/P3/P4 更新 + TP-P5/P6/P7 新建 + H-17 人工执行项 |
| T-P8-13 R-UI QA P2 改进项 | ✅ 已完成（2026-04-05）— 8 项全部处理（P8-A~H，详见下方） |
| QA-P8 最终验收 | ⏳ 待开始 |

---

## 阻塞项

| 阻塞 ID | 影响任务 | 描述 | 负责方 |
|---------|---------|------|--------|
| H-05 | T-P1-08 | 微信开放平台资质 — 🔜 推迟至 App 主体开发完成后 | 人工 |
| H-15 | T-P1-07 | 腾讯云短信服务 — 🔜 推迟至 App 主体开发完成后 | 人工 |

---

## Phase 完成记录

| Phase | 完成日期 | 备注 |
|-------|---------|------|
| R0 Design System | 2026-04-05 | 附条件通过（3 项 P2 改进记入 P8 Polish）|
| P1 Foundation | — | 部分阻塞（H-05, H-15 推迟）|
| P2 Data Layer | 2026-04-10 | 附条件通过（FL-001/FL-002/B-03 已修复，待用户重建确认）|
| P3 Drill Library | 2026-04-11 | 附条件通过（FL-003/FL-004/FL-005 已修复；设备矩阵/可访问性/性能待补测）|
| P4 Training Log | 2026-04-11 | 附条件通过（人工 92/98 + FL-006/007/008 已修复；FL-009 P3 延后）|
| P5 Angle Training | 2026-04-05 | 代码审查 + 设计对齐 + 22 测试通过 |
| P6 History | 2026-04-12 | ✅ 附条件通过（人工 TP-P6 + 234/234 自动化；Pro 统计 UI + 60 天限制 e2e 待 TestFlight 补测）|
| P7 Subscription | 2026-04-05 | 5 任务完成 + SubscriptionView 设计对齐 + Freemium 全整合 + 234/234 测试 |
| R-UI Alignment | 2026-04-05 | 附条件通过（D-1 已修复；8 项 P2 改进记入 P8-13）|
| R1 UI 逐页审查 | 2026-04-06 | 11 份报告完成，145 项偏差（P0:0 / P1:33 / P2:112）|
| P9 Aiming Expansion | 2026-06-02 | QA-P9 通过；241/241 自动化 + 人工功能验收；FL-016 + PD-007 修复；T-P9-D-REVIEW/T-P9-00 收尾 |
| P8 Polish & Release | — | 仅剩人工：H-17 人工测试 / TestFlight / App Store 资产与提交 |

---

## R1 UI 逐页审查 — ✅ 已完成

> 详见 `tasks/phases/R1-ui-review.md` + `tasks/ui-reviews/UR-20260406-*.md`（11 份）

| 任务 | 状态 |
|------|------|
| T-R1-01 TrainingHomeView 审查 | ✅ 已完成（2026-04-06）— 10 项（P1:3 / P2:7）|
| T-R1-02 ActiveTrainingView 审查 | ✅ 已完成（2026-04-06）— 16 项（P1:3 / P2:13）|
| T-R1-03 TrainingSummary + ShareView 审查 | ✅ 已完成（2026-04-06）— 17 项（P1:3 / P2:14）|
| T-R1-04 Plans（List+Detail+Builder）审查 | ✅ 已完成（2026-04-06）— 18 项（P1:7 / P2:11）|
| T-R1-05 DrillLibrary 审查 | ✅ 已完成（2026-04-06）— 13 项（P1:6 / P2:7）|
| T-R1-06 AngleTraining 审查 | ✅ 已完成（2026-04-06）— 16 项（P1:1 / P2:15）|
| T-R1-07 History + Statistics 审查 | ✅ 已完成（2026-04-06）— 13 项（P1:2 / P2:11）|
| T-R1-08 Profile + Settings 审查 | ✅ 已完成（2026-04-06）— 13 项（P1:4 / P2:9）|
| T-R1-09 Onboarding + Login 审查 | ✅ 已完成（2026-04-06）— 7 项（P1:1 / P2:6）|
| T-R1-10 SubscriptionView 审查 | ✅ 已完成（2026-04-06）— 11 项（P2:11）|
| T-R1-11 全局 + 组件审查 | ✅ 已完成（2026-04-06）— 11 项（P1:3 / P2:8）|

**汇总**：全部 11 个审查任务完成，共发现 **145 项偏差**（P0: 0 / P1: 33 / P2: 112）。

---

## P9 Aiming Feature Expansion — ✅ 已完成（QA-P9 通过 2026-06-02）

> 详见 `tasks/phases/P9-aiming.md`

| 任务 | 状态 |
|------|------|
| T-P9-00 UI 设计交付文档更新 | ✅ 已完成（2026-06-02）— `09-UI设计交付文档.md` §3.3 补 5 页 + AngleHome 分组 + 对照表增强 + 导航树 + §7.5 |
| T-P9-D-01~06 UI 设计出图 | ✅ 已完成（2026-04-14，6/7 APPROVED） |
| T-P9-D-REVIEW 设计一致性审查 | ✅ 已完成（2026-06-02）— `ui_design/tasks/P9-REVIEW/consistency-review.md`，无 P1 偏差 |
| T-P9-01 SceneKit 场景基础设施 | ✅ 已完成（2026-04-14）— ADR-P9-01 |
| T-P9-02 数据层扩展 | ✅ 已完成（2026-04-14） |
| T-P9-03 AngleHomeView 导航重构 | ✅ 已完成（2026-04-14） |
| T-P9-04 瞄准原理页 | ✅ 已完成（2026-04-14） |
| T-P9-05 角度与打点动态关系页 | ✅ 已完成（2026-04-14） |
| T-P9-06 几何角度预测训练 | ✅ 已完成（2026-04-14） |
| T-P9-07 SceneKit 角度预测页（2D/3D） | ✅ 已完成（2026-04-14） |
| T-P9-08 SceneKit 角度预测增强 | ✅ 已完成（2026-04-14） |
| T-P9-09 进球点对照表增强 | ✅ 已完成（2026-04-14） |
| T-P9-10 浅淡球感页 | ✅ 已完成（2026-04-14） |
| T-P9-11 AngleHistoryView 增强 | ✅ 已完成（2026-04-14） |
| QA-P9 验收 | ✅ 通过（2026-06-02）— `tasks/qa-reports/QA-P9.md`；241/241 自动化 + 人工功能验收（用户确认）；修复 FL-016（几何训练 Freemium 闸门）+ PD-007（测试宿主/模块名，恢复命令行测试） |

---

## 执行顺序

```
R0 ✅ → P4 ✅ → P5 ✅ → P6 ✅ → P7 ✅ → R-UI ✅ → R1 ✅ → P9 ✅ → P8 🔄（仅人工）
```

---

## 下一步

- **【P18 发布收敛 — 当前主线】（2026-07-03 立卡）**：按 `tasks/phases/P18-release-convergence.md` 七批执行，**B1 ✅（2026-07-03）**，当前批 **B2**（T-P18-05 组件下沉 → T-P18-10 ShotControlBar，预估 2–3 会话）。人工并行项：**H-19 App 备案今天启动**、H-18 音效素材、H-09 隐私政策、TP-P7、ADR-P10-09 手感验收。
- **【P12 内容体系与理论挂接 — 规划已立，待执行】（2026-06-14，ADR-P12-01）**：单一真源 [`curriculum-map.md`](curriculum-map.md) + phase 卡 `tasks/phases/P12-content-system-theory.md`。**待用户拍板**：地图 §6 三参数（每格配额 / L4 是否进 v1.0 / 系统训练模式定位）。**第一刀（建议新会话）**：c042 竖切——扩 `DrillContent.theoremIds/moduleIds?` + `TutorialSection.theoremRefs?`（可选向后兼容）、vendor `16/contracts/*.json` 进 `Resources/Theory/`、c042 精讲三层披露 + 建 T01/T03 理论详情页（复用 `AngleTrainingScene` 标注图）+ 学习区"球理"入口卡、建 `THEORY-CONSUMPTION-LOG.md` 翻 16 中枢卡 v1.0 final（达成 16↔13 闭环）。
- **✅ 动作库内容管线 + 击球意图 schema 雏形（2026-06-04 完成）**：见上方 P10 Track A 条目 + `tasks/phases/P10-physics-content-pipeline.md`（ADR-P10-01）。**下一步**：~~① 把 `shotIntent` 推广到全量 72 条~~ ✅（见下）；② 废弃 `BTDrillPreviewPlayer` 的 PNG 帧序列、动画统一由烘焙轨迹驱动；③ 展示三件套（GIF 烘焙轨迹 / 精讲参数化对错对比 / 视频降级为真人身体动作）统一重构；④ 多杆球（`obstacles`/多 shot）+ **翻袋/吃库瞄准**烘焙支持（当前 v1 直瞄无法表达 c055/c057 等特殊球路）。
- **✅ shotIntent 全量补齐（2026-06-04，iOS Architect 调度 8×content-engineer 并行，DR-017 后续）**：为剩余 67 条 Drill 并行补 `shotIntent`（8 子智能体各管一 category，按描述/杆法推断 velocity+spin），+5 试点 = **72/72 全有 shotIntent**、JSON 全合法。新增可行性扫描 `test_scanFeasibility`：**67/72 引擎干净落袋**；修 2 条几何颠倒（c039/c062）+ 6 条 follow 误推乱弹（改中心球）。**5 条特殊球路**（c055 翻袋/c057 K球吃库/c058 贴库/c061 解球/c066 开球）单杆直瞄无法干净进 → c055 退回手画、余渲染真实物理近失（v1 烘焙器不支持翻袋/吃库瞄准，记 H-11）。72 缩略图全量重烘焙。详见 H-11 § shotIntent 全量补齐 待物理核查。
- **✅ P10 Track B-1 物理保真进球管线（2026-06-04 完成，ADR-P10-02）**：见上方 Track B-1 条目。USDZ 实测证伪「jaw 放错 17mm」预设（几何自洽）。用户复评后拒绝"放宽捕获半径"偷懒做法，改建**真实袋口物理（喉腔模型）**：jaw 库 + 实测 jaw 尖端挤出的喉腔侧壁/后壁（可反弹）+ 物理落袋孔，rattle 由几何涌现；配套稳健化闭环求解（采样寻优最优接触点）+ 画面=物理（objectPath 真实模拟、轨迹基进袋判定）。E-solver/中袋/c002 全绿，291/291。详见 `tasks/qa-reports/PHYSICS-PROBE.md` §USDZ 实测标定。
- **【P10 物理标定 — 剩余】**：① 中袋 jaw mouth ±0.035→对齐实测 ±0.046（非阻塞微调）；② **常量标定**（e_b/台呢库边摩擦/恢复系数，**需真实球俯拍视频**，用 `PhysicsBenchmarkTests` 钉死）；③ 朴素瞄准 E-geom 3/5 属窄喉口掠角真实物理（产品用求解器规避，非闸门）。

0. **全局字体密度优化已完成**（2026-05-26，DR-014 / PD-006）：
   - Typography Token 全局下调（btDisplay 48→44 / btDisplaySmall 36→30 / btLargeTitle 34→32 / btChapterNumber 32→26 / btTitle 22→20 / btTitle2 20→18 / btTitleMedium 19→17 / btStatNumber 28→24）
   - 页面级局部修正：TrainingHomeView 今日 Drill 卡标题降级 + 序号轻量化 + issueThumbnail 硬编码改 Token；PlanDetailView statCell 数字 + 描述 lead 句降权
   - SKILL.md 与 UI-IMPLEMENTATION-SPEC.md 字体规范同步更新，新增「使用原则」四条避坑指引
   - 实施日志新增 DR-014 + PD-006（双层修法模式）
   - 构建验证：`make build` 通过；ReadLints 无错误
   - **待人工复核截图**：训练首页、动作库、计划列表、计划详情、角度首页、我的、训练总结

1. **P9 实现任务全部完成**（2026-04-14）：
   - Wave 1：SceneKit 基础设施 + 数据层 quizType + 导航重构（7 功能分组）
   - Wave 2：5 独立页面（瞄准原理 / 角度与打点 / 几何训练 / 对照表增强 / 浅淡球感）
   - Wave 3-4：SceneKit 2D/3D 角度预测 + 增强（训练类型/自由练习/幽灵球/瞄准线）
   - Wave 5：AngleHistoryView quizType 筛选增强
   - **待人工验收**：模拟器运行验证 SceneKit 加载 / 2D↔3D 切换 / 角度计算 / Dark Mode
   - **ADR-P9-01**：SceneKit 引入决策已记录
2. **R1 审查 + 修复 + DrillLibrary 改造已完成**（2026-04-06）：
   - 11 份审查报告 → 145 项偏差 → 10 组并行修复 → 235/235 测试通过
   - **DrillLibrary 参照训记全面改造**（DR-011）：
     - 新建 `BTMiniTable.swift`（缩略图 Canvas：球径 3x + 路径 2x + 袋口高亮 + 无库边）
     - `BTDrillGridCard` 使用 BTMiniTable + 等级徽章/PRO/收藏叠加层 + 底部渐变
     - `DrillListView` 改为训记风格：左侧分类侧边栏（72pt）+ 右侧 2 列网格
     - `DrillDetailView` 新增：备注输入卡、训练维度 5 进度条、查看精讲按钮、真人示范占位
     - `BTDrillListSkeleton` 更新为 2 列网格骨架
   - **延后项**：TrainingHome「即将到来」Section、DrillRecordView 休息设置行、BTShareCard 备注 toggle、History 新增功能按钮
   - **下一步**：人工测试（H-17）→ TestFlight
2. **P8 待执行**：
   - **H-17 人工测试执行**：🔄 5/6 已执行（TP-P2/P3/P4/P5/P6 ✅），**仅剩 TP-P7 订阅**（需 StoreKit sandbox/真账号 — [HUMAN]，约 30 分钟）
   - T-P8-08（TestFlight 发布 — [HUMAN]）
   - T-P8-09（App Store 资产准备 — [HUMAN]）
   - T-P8-10（App Store 提交 — [HUMAN]）
   - QA-P8 最终验收
3. **人工测试**：6 份测试计划已就绪（TP-P2~P7），待人工在模拟器/真机上执行（见 H-17）。
4. **后端部署** ✅（2026-03-29）：已部署至 106.54.3.210:3000，72 条 Drill 已 seed。
5. **知识累积机制**：`tasks/IMPLEMENTATION-LOG.md`（FL/DR/PD 三类条目）+ `UI-IMPLEMENTATION-SPEC.md` Changelog 节跨会话保持实施知识。

---

## 已完成 Phase 归档

当某一 Phase **全部任务**均为 ✅ 后：

1. 将任务明细表剪切至 `tasks/archive/Pn-completed.md`。
2. 在「Phase 完成记录」表中填写完成日期。
3. 从下一会话起仅读当前 Phase 任务卡。
