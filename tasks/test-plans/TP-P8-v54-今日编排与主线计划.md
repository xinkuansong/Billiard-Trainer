# 人工测试计划 — P8 / v54 今日编排与主线计划

> **使用方式**：以非隐私测试数据在真机上逐项执行；通过后勾选 `[x]`，失败则记录设备、系统、时区、操作步骤和截图/录屏。
> **边界**：自动化已覆盖业务规则和四组模拟器页面；本单只验收真实触控、系统辅助功能、跨日/时区及真实 owner 迁移。

---

## 前置条件

- [ ] 已安装 v54 最新 Debug/TestFlight 构建，记录 commit 或构建号
- [ ] 准备 guest、账号 A、账号 B 三个可隔离的 owner
- [ ] 准备一个官方计划、一个自定义模版和两个可训练动作
- [ ] 确认系统可切换 Light/Dark、最大辅助字号、VoiceOver 与减弱动态效果
- [ ] 屏幕录制中不出现真实账号、token 或其他隐私信息

## 自动化基线（2026-09-03）

- 内容：12/12 官方计划，43 阶段 / 129 课程；I10 88、I11 365、I13 915，FAIL 0。
- 后端：`backend` 15/15 通过；来源 DTO 新旧字段兼容。
- UI：iPhone / iPad × Light / Dark 四组 `V54ScheduleUITests` 均 3/3 通过，共 52 张截图；路径 `build/ui-reviews/v54/`。
- 领域与迁移：参见 `V54PlanContentModelTests`、`V54DataMigrationTests`、`V54ScheduleDomainTests`、`V54TrainingTransactionTests`、`V54PresentationStateTests`。
- 总门禁：`make -f scripts/Makefile verify-gate` 通过；v47 route/write-surface 分别为 70 / 131。

---

## 一、主线与今日编排

| # | 操作 | 预期结果 | 通过 |
|---|---|---|---|
| F-01 | 激活一个官方计划，进入计划详情 | 只有一条官方主线；页面按阶段/课程展示，周数是结合“每周 X 练”的动态预计 | [ ] |
| F-02 | 打开“编排今天” | 默认选中当前课；能多选已完成、当前和未来课程 | [ ] |
| F-03 | 同时选“已完成课 + 当前课”并保存 | 结果摘要说明复练与可推进项；今日队列同时保留两项 | [ ] |
| F-04 | 选“当前课 + 下一课”，逐块完成 | 每完成一块回到今日队列，不自动开始下一块；两课均完成后主线连续推进两课 | [ ] |
| F-05 | 选“当前课 + 隔一课的未来课”并完成 | 只推进当前课；跨缺口的课只记为提前练习 | [ ] |
| F-06 | 计划完成后再选任一已学课 | 允许加入并生成复练记录；已完成计划不倒退 | [ ] |

## 二、混合来源与触控

| # | 操作 | 预期结果 | 通过 |
|---|---|---|---|
| T-01 | 从模版页加入今日，再从动作详情加入今日 | 两项都追加到当日队列；官方主线与游标不变，没有“替换当日计划” | [ ] |
| T-02 | 对 pending 项目长按拖动排序 | 顺序即时保存；已完成项不被拖入 pending 区；排序不改变课程的推进资格 | [ ] |
| T-03 | 删除一个 pending 项 | 只删所选项，其他来源、已完成项和官方主线不变 | [ ] |
| T-04 | 启动一块后退出，再点首页唯一 CTA | 显示“继续”并回到正确 in-progress 块；不误启动后续项 | [ ] |
| T-05 | 完成一块后在历史详情查看来源 | 存在的官方课/模版/动作可跳转；删除源后仍显示冻结面包屑，但不伪造可跳转 | [ ] |

## 三、动态字体、VoiceOver 与减弱动态效果

| # | 环境 | 检查项 | 通过 |
|---|---|---|---|
| A-01 | iPhone 最大辅助字号 | 阶段/课程标题、结果摘要、跨缺口提示和底部 CTA 无重叠、遮挡或无法到达 | [ ] |
| A-02 | iPad 最大辅助字号 | 编排 sheet 可完整滚动和保存，长标题可读 | [ ] |
| A-03 | VoiceOver | 每课读出已完成/当前/未来、选中状态和完成影响；队列读出顺序、pending/in-progress/completed | [ ] |
| A-04 | VoiceOver | 编排结果摘要可整体理解；历史来源可跳转与已删除状态可区分 | [ ] |
| A-05 | 减弱动态效果 | 排序、加入、删除、完成后状态不依赖动画才能理解 | [ ] |

## 四、跨日、时区与同步

| # | 操作 | 预期结果 | 通过 |
|---|---|---|---|
| D-01 | 23:59 启动，00:10 完成 | session 按真实完成时间进入统计；item 仍属创建日，次日不重复生成 | [ ] |
| D-02 | 今日留一个 pending，次日打开首页 | 只显示“昨日未完成”入口，不自动顺延 | [ ] |
| D-03 | 把昨日项复制到今日两次 | 首次按当前游标重新分类；第二次不产生未完成重复项 | [ ] |
| D-04 | 跨时区后创建新日安排 | 旧 schedule 保留原 `localDayKey + timeZoneIdentifier`；新日按当前时区创建，无重复容器 | [ ] |
| D-05 | 断网完成混合块，重启后联网同步，再在第二设备恢复 | 本地记录/item/游标一致；重试不重复推进；第二设备历史来源完整 | [ ] |

## 五、owner 隔离与迁移

| # | 操作 | 预期结果 | 通过 |
|---|---|---|---|
| O-01 | guest 编排混合队列，登录 A 并选择迁移 | 主线、schedule/item 和历史归 A，同日重复项按稳定身份合并 | [ ] |
| O-02 | 退出 A，登录 B | B 看不到 A 的主线、今日队列和历史；A 再登录后数据完整 | [ ] |
| O-03 | guest 登录时选择“暂不迁移” | 账号与 guest 数据分离，未同意前 guest 安排不上传 | [ ] |

## 六、验收记录

- 设备 / iOS：
- App 构建号：
- 测试人 / 日期：
- 证据目录：`build/manual/v54/`
- 失败项与问题单：
- 结论：[ ] 通过　[ ] 有条件通过　[ ] 不通过
