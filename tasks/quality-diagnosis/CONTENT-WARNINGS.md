# 内容门禁提示审计（run-001）

日期：2026-09-05。输入：`build/quality-diagnosis/run-001/content-gate.log`，本子任务只读日志、脚本、契约和资源目录；未重跑门禁、未生成/同步/修复资产。下列结论是提示分类与证据能力审查，不是 App 视觉验收。

## 结论

| 提示 | 分类 | 能确认的事实 | 不能据此确认 |
|---|---|---|---|
| C1：95 提示，0 通过，0 失败 | **新鲜度证据缺失，存在过时产物风险但未证实过时** | 当前 `build/position_play_export` 整目录不存在，所有被 C1 检查的非零杆序列均无 initial.png 标记 | 不能说 95 张 App 图片丢失；不能说出片/回填与当前序列一致 |
| I7：11 提示 | **预期退役分支** | 11 个 profile 均 `retired: true`；不是待修复的 11 个活跃 profile | 不能证明历史 profile 与现行序列匹配，也无需强制恢复匹配 |
| I9：3 已知豁免 | **现行明确豁免** | c065/c068/c070 在 index、无序列、基线登记一致 | 不能证明无序列课的教学完整、入口体验正常 |
| I9：5 提示 | **上游保留符合下架决定；下游盘面残留待核** | 5 个 ID 已从 index/Drills 移除；上游保留有意为之；DrillBoards 仍有对应 6 份 JSON | 不能说它们已重新进入动作库；不能仅凭 I8 通过证明“下架资源不进包” |

总门禁 `FAIL: 0` 的正确表述是“本次脚本定义的阻塞条件未触发”。日志结尾“全链路同步”不能用于证明 C1/C2 覆盖缺失的环节。

## CW-01：C1 缺失的是本机中间出片目录

证据：日志 `:2`–`:97` 共 95 条“尚无出片产物”；脚本 `scripts/verify_tutorial_sync.py:210`–`:226`：逐序列查 `build/position_play_export/<stem>/initial.png`，0 杆跳过，marker 不存在进入 warn，marker 比序列 mtime 旧才 fail。路径定义在 `:78`；`FAIL_KEYS` 在 `:65`–`:66` 明确 warn 不阻塞。

本次只读目录检查：`build/position_play_export.exists() == False`、`*/initial.png` 数量 0。因此不是发现了 95 个“时间戳已过时”的产物，而是没有可比较对象。95 是序列项数，不是 drill 数，也不是最终 HEIC 数；包含已经下架但上游仍保留的非零杆序列。

关联证据缺口：C2 也显示 0 通过/0 不符（日志 `:99`）。脚本 `:236`–`:239` 在 export 目录不存在时返回全零；`:249` 还要求子目录有 full.mp4 才参与回填比较。故 C2 的零失败不证明回填一致。

C3 的 704 引用命中（日志 `:101`）主要是在 `QiuJi/Resources/DrillTutorials` 查名字/旧图时间戳（脚本 `:314` 起）。实际打包资源为 `TutorialFigures`（`project.yml:83`–`:91`），母版目录明确不进包。这是不同证据层，不能相互替代。

正式测试建议：

1. 先复用现有 TutorialFigures 清单/构建配置检查，验证当前精讲引用在实际 App 包里全部可加载；不运行出片或回填来消除提示。
2. 选单球形、多球形、手动 token 和旧时间戳 token 的代表课，核对序列事实—正文—最终展示图，保留对应输入哈希和截图。
3. 若已有其他目录的可信出片 manifest，定向查找并核验哈希；找不到则保留“新鲜度未验证”，不要根据 mtime 或图片存在推断已一致。

## CW-02：I7 的 11 个 profile 已按裁定退役

日志 `:111`–`:122`：c016/c018/c020/c021/c053/c073/c074/c075/c076/c077/c078，全部“已标记退役，跳过一致性判定”。只读解析 `content/drill_profiles/*.profile.json`：文件总数 11，`retired: true` 数量 11。

依据：契约 `.kiro/steering/content-data-contract.md:46`–`:61` 规定序列为真源，profile 仅设计档案，不得据此断言 App 球形；`:766`–`:770` 记录 v26 W0 退役。`scripts/content_invariant_baselines.json:11`–`:12` 已清空旧豁免并解释退役分支。脚本 `:429`–`:460` 允许 retired/deprecated，输出 warn 而非 fail。

处置：不列 App 缺陷，不重建 profile，不把设计档案当几何预期。后续静态消费者审查可确认生产路径不重新读取 profile 为真源；若发现此类消费者，再另立问题。当前只证明标记与政策相符。

## CW-03：I9 的 3 个无序列课是明确例外

日志 `:126`–`:129` 与 `scripts/content_invariant_baselines.json:13`–`:18` 一致：

- c065 Ghost Game。
- c068 五球连打走位。
- c070 全台清台挑战。

契约 `:801`–`:810` 解释随机/对抗盘面不适合固定序列，需独立通道；当前文件索引核对三者各 1 份正式 drill JSON、均无上游序列与 Bundle board。不能为使序列门禁全绿编造球形。

正式测试建议：实际打开三课，检查无图/无回放时是否仍有明确教学内容、可开始记录，试打/预览入口是否正确禁用或提供独立行为，且不空白/崩溃。记录随机盘面课程的真实产品体验，不以“豁免”豁免全部功能质量。

另有文档陈旧计数：契约 `:753` 仍写“I9 的 8 条”，但 `:801` 及 `:964` 已更新为 3，基线也为 3。分类为文档一致性问题；本轮未修改。

## CW-04：I9 五个上游保留项与下架盘面残留

日志 `:130`–`:134`：c002/c006/c007/c062/c066 有序列但未登记 index。`content/retired-drills/README.md:3`–`:5` 明确用户裁定十课从 App 完全消失（动作库 + 官方计划不出现），上游序列保留；`:13`–`:21` 含这五课。故“上游有、index 无”本身符合决定。

本次只读目录与 index 核对：

| ID | index 登记 | 正式 Drills JSON | 上游序列 | 当前 DrillBoards JSON |
|---|---:|---:|---:|---:|
| c002 | 否 | 0 | 1 | 1 |
| c006 | 否 | 0 | 2 | 2 |
| c007 | 否 | 0 | 1 | 1 |
| c062 | 否 | 0 | 1 | 1 |
| c066 | 否 | 0 | 1 | 1 |

`project.yml:102`–`:104` 将 DrillBoards 整目录作为 resources folder；因此存在**下架内容的派生盘面仍可进入包**的配置风险。尚未检查最终 `.app`，也未追踪是否存在可绕过 index 的入口，不能宣称用户已看见被下架课程。

为什么 I8 未报错：脚本 `:469`–`:493` 只验证 Bundle board 是上游序列子集且字节相等；没有要求它属于当前 index。因此全部源/派生字节一致仍可能保留已下架 ID。这是门禁覆盖范围，不是 I8 自己算错。

正式测试建议：

1. 在当前构建的 `.app/DrillBoards` 检查这 6 份文件是否实际进包，记录资源清单与构建指纹。
2. 核对全部十个下架 ID 在动作库索引、官方计划引用、搜索及公开路由是否不可达；不只检查本次有 I9 提示的五个。
3. 若确实入包但不可达，分类为下架资产残留/包配置问题；如有可达入口，再按用户影响升级为功能问题。最终“不进 Bundle”是否包括全部保留派生资产，结合退役目录恢复步骤（README `:24`–`:30`）进一步确认，不擅自删除资源。

## 建议纳入主报告的表述

“内容门禁未触发阻塞，但出片新鲜度与回填比较没有可用中间产物，不能证明图像随当前序列更新；11 个退役 profile 与 3 个无序列课符合现行规则。另有 5 个下架 ID 的 6 份盘面仍存在于配置为打包的资源目录，需核验实际安装包和用户可达性。”
