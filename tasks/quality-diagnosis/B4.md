# B4账号与权益执行卡

2026-09-05。用户真实条件询问已发出，本机隔离检查不依赖其回答。

## FORMAL-B4-001（执行中）

- 创建全新专用iPhone17Pro/iOS26.2：3E8EDBAD-12BB-4AB9-9956-9112B8D83223，没有之前安装/真实登录，不读取或输出凭证。B1原设备与样本保留。
- 冻结snapshot002，formal-DerivedData。41个白名单逐项校验存在；api10/auth6/owner9/profile16分进程串行，组失败就停，成功后仅终止该新设备宿主。
- 主句柄25875；实际完成组看formal-b4-001各子目录exit.json/xcode-test.log，勿重复运行已经完成组。
- 每组argv写command.json。Mock与宿主通知安全前提见B4-PREPARATION；本批不执行StoreKit购买，不触碰用户真实账号或服务。
- SC26第二小批仍由独立子任务准备；当前41项不覆盖整个B4。

## 已完成批次与正式路由边界

- 001四组api10/auth6/owner9/profile16均make0，共41/41通过；各组独立进程结束。
- 002 delete8/restore11/ownerQueue1均make0，共20/20通过；exec42786已结束。仅隔离替身层，不能外推真实服务。
- FORMAL-B4-ROUTES：Node exit1；17个叶子用例10通过7失败（另2父容器失败不重复计数）。临时副本路径environment.json，源文件哈希逐一匹配snapshot002；依赖APFS复制且core版本与冻结lock一致，运行前解析路径强制落临时树，无.env/无Mongo，connect/createConnection禁止；只监听127.0.0.1:0。
- QD007正式复现PUT owner修改被路由接受200。QD008：训练与角度两个端点，499/500样本完整；501及1000 aligned、501同updatedAt均截断500且after为0；反向date对照二次after能补1。手动after请求不是实际客户端（现有客户端更只请求一次），更不是Mongo持久化证据。
- 输出formal-b4-routes/test.log/result.json，保留所有失败，不修复后端。继续本地StoreKit与真实条件缺口，不把61个单测通过当B4完成。

## FORMAL-B4-003本地StoreKit已完成
3/3、make0、5.386秒；商品月/年/终身类型、月购买与到期、年购买、终身恢复通过。SKTestSession本地Transactions，不是Apple真实Sandbox购买，也没有覆盖pending/取消/断网真实商店。

## 真实Mongo补验FORMAL-B4-REAL-MONGO-001

本机原无mongod，已从官方取得8.0.29临时运行时并核对sha256，不改依赖/系统服务。18份冻结src原样与真实Mongoose装配，新dbpath/loopback/PID/空库核验后五项2pass3fail、Node1；QD007真实owner转B，两个501缺1，500对照完整。自有实例正常关闭且保留库/log。独立审核见REAL-MONGO-RESULT.md；仍非App/真实身份/部署端到端。
