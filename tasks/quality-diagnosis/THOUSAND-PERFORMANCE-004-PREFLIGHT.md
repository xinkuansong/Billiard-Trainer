# 千条记录性能补验前置核对

2026-09-08。当前只读核对完成；尚未建立新千条库或运行性能测试。

## 旧数据能否复用

THOUSAND-RESUME-RESULT.md 保存 snapshot003 的历史结果：1000场/entry/set、队列0；正常UI抽样999和978及统计总值已验证。旧首轮439.125秒、补验121.968秒包含大量AX查询，不能解释为产品操作耗时。

本次重新读取实际simctl设备清单：历史设备CE76026D前缀无匹配；旧 `resume-20260907/thousand-container-chain.json` 不存在；归档runs中没有thousand原件。检查结果位于 `archive/quality-diagnosis/observations/thousand-reuse-check004.json`。因此不能声称现在正在复用或检查原库，也不能将历史功能结果抹为未执行。

## 下一步的有限执行方式

剩余的是明确口径的一次千条历史/统计交互观测，并非重新遍历千条功能验收。因原库不可用，需按既有隔离流程重建可追溯合成前置：全新专用设备 → 内存宿主中的空默认磁盘probe → 按实测owner/相对路径/当天日期运行ThousandHistoryFixtureTests → 终止宿主 → 独立确认manifest和1000行 → 正常磁盘UI。

旧播种类已有严格路径/owner/文件不存在条件，不能跳过或用任意磁盘目标替代。首次登记时仍须核当前API与环境前缀兼容，保留旧源码与历史结果。测试数据只存在新专用沙盒，不搬入用户设备，不清理任何旧库。

仅新增一个有限UI测量方法：正常记录入口就绪 → 一次短滚动后实际行可用 → 正常统计入口到预先指定摘要可用 → 返回历史。记录各次用户操作到对应可访问性断言成功的单调时钟时间、持续录像和实际App PID/RSS；校验1000组/8000到10000球等真实摘要作为数据前置。指标明确含XCTest/AX成本，不能拿该值直接判普通用户延迟、帧率或2秒启动SLA。不要再次运行999/978两个详情或整个长滚动方法。

若执行出现渲染/查询超时，保留有限失败、录像及未完成阶段，不添加无限等待或改业务。真机性能与长期内存/热耗电仍是外部补测。此文件只是将“原库可复用”的过时前提修正为当前可执行前置，不意味着性能覆盖已完成。


2026-09-08执行更新：新专用设备CCA94E7E-981D-4F16-A115-40677FC3097C，probe001实际1/1通过0.013秒/make0，51文件归档。主控解析原始探针JSON：内存启动标记存在、无禁止参数，默认store/WAL/SHM均不存在，owner与相对路径已核。种库单独注册ThousandPerformanceFixtureTests，保留旧类原件，仅新增设备一致性守卫、TEST_RUNNER环境回退、授权缺失硬失败；1000实体生成及路径/owner/day/不覆盖守卫未改。seed001开始，尚不是性能结果。

2026-09-08 千条性能前置：新设备CCA94E7E-981D-4F16-A115-40677FC3097C，probe0011/1通过0.013秒/51文件，seed0011/1通过0.425秒/49文件，均make0归档。宿主终态后独立SQLite备份核1000场/entry/set、队列0、唯一owner、2000分钟、8000/10000球、1000唯一note；容器重定位前后manifest记录保持，见archive/quality-diagnosis/observations/thousand-perf-seed001。当前只是数据前置，性能UI尚未执行。
