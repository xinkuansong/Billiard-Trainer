# B5 M3 Dark 独立图像审核

2026-09-06。确认 `formal-b5-m3-dark/exit.json` 已存在后完成报告：make_exit=0，结束03:28:53+08；完成日志记4 tests、0 failures、122.107秒。只读逐张打开 screenshots 下 **全部13张PNG、4份frames.json**；图为1488×2266竖屏iPad，坐标约744×1133pt。没有设备交互、业务修改或读取进程环境。

## 实际外观与配对

五根、动作详情/返回页、训练与休息弹层均实际呈黑底、深灰卡片、白色主文字、绿色强调；与此前实看的M3 Light白卡/浅灰背景明显不同，**确认真实Dark渲染，不依靠simctl设置或目录名推断**。根页同为游客、空训练/历史、9月6日，卡片标题顺序与Light一致。详情同为中袋直线出杆，返回搜索内容一致；时间不同不要求截图逐像素相同。工具在Light也黑底，因此工具背景不独立证明外观切换成功。

## 13张逐图观察

| PNG | 实际观察及Light对照 |
|---|---|
| root-training | 空安排、周0/3、每日清台、全部等级与两列首排计划完整，顶部五Tab可辨；Dark卡背景与文字配套变化。浮动自由训练仍覆盖下排右侧封面局部，延续Light观察，不新增永久不可达结论 |
| root-library | 两列动作卡和左分类、搜索/等级/顶部Tab清晰；白色标题配深卡，球桌缩图正常。最下排超视口仍需滚动，不当作裁切缺陷 |
| root-practice | 两列学卡、左学理练打解完整；照片保留原色，标题/摘要改浅色系，卡底区无白色残留 |
| root-history | 同月空日历、选中6日、空态文字和CTA都在屏内，非当前月灰字较暗；未测量对比度，不能称WCAG达标 |
| root-profile | 游客绿头、橙提示、深色Pro卡和菜单可读；偏好/关于完整，底部本机说明与版本灰字较暗；菜单无明显重叠，不代表所有项目已点击 |
| library-detail | 球桌预览、说明、要点、要求和双CTA完整；背景及卡片都为Dark，没有Light白卡残留 |
| library-detail-after-3s | 同页内容稳定可辨；相近画面不证明动画播放或性能 |
| library-tryout | 正常详情进入试打：第1/8杆、提示条、桌面及两行球库可见；与Light相同布局，黑底是工具设计。没有实际击打八杆结果证据 |
| library-return | 搜索仍为中袋直线出杆，单匹配卡及动作库Tab选中；实际Dark，支持返回保留搜索上下文 |
| active-training-rest-overlay | 半台直线球5组fixture上显示深灰休息卡、0:58、双环、最小化、+30S、完成休息，背景内容变暗；弹层按钮在屏内，未见文字推挤 |
| active-training-rest-minimized | 弹层消失，顶部56s和右下橙色0:56浮标可见；底部五操作文字不被浮标遮住，浮标接近底栏上缘。不是跨Tab训练浮标测试 |
| compact-shot-stage | 自由击球深链：完整桌面、左右轨道、打点入口、击球/重打/回放、两行球库；与Light同构，无新增遮挡 |
| compact-shot-stage-spin-pad | 打点盘四向/中心球/回中完整、位于桌内、未盖球库；对照Light布局相同。盘内遮挡桌面是覆盖层表现 |

## frame核对

球桌JSON与Light同值：aim高度264，instrument含入口预留348，二者底缘870；被测1/2/8/15球均44×44，末排底缘1106.5，距1133屏底26.5pt。图中两行球面完整，但不是16球全命中验收。closeSpin仍为`(0,132,744,882)`大关闭区域，不是可见按钮或盘面frame。

休息minimizeRest为`(642,384,70,44)`，overlayTitle为`(32,395.5,67.5,20.5)`，横向相隔充足，支持标题与最小化不重叠。expandRest为`(580,44,44,44)`，是**顶部56s入口**，不能拿它证明右下橙色浮标自身触达尺寸。当前资料没有M3 Light同名休息图的本次独立配对，休息只作Dark实际布局审核，不虚称完成该状态浅深配对。

## 范围结论

本组13图支持M3这些代表页真实深色呈现、正常详情试打返回和fixture休息显示，以及工具当前布局。未发现需新增编号的确定裁切/遮挡缺陷；低对比灰字仅目视记录，未计算合规阈值。五根与搜索详情属于正常入口/内存状态；休息为训练fixture；自由击球为深链。没有Dark真实训练输入/键盘/保存重启三图、长计时三态、认知历史、全页滚动、所有菜单触摸、真实VoiceOver、分屏或旋转证据；不能把4/4或13图完整扩大成全App暗色/可访问性通过。
