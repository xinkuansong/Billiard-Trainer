# c042 tutorial003 滚动失败只读审计

2026-09-08。读取当前草稿、C042-TUTORIAL-004-RESULT、003归档实际tested-sources、日志、终态AX及PNG；只读查询xcresult活动元数据。没有UI操作、重跑、构建或修改任何测试/产品。当前草稿与003归档源码SHA256均为 `b9904c569cedc46fa37da333c5a5e2b49f62245e5694e64fb6f49c549e05e869`。

## 有证据的原因

**16次预算确实耗尽；没有方向振荡证据。最后一次上滚过冲后已经没有校正机会。** 003不是没找到第八杆按钮，也不是图片高于视口而数学上无法完整展示。

原件：`archive/quality-diagnosis/runs/formal-004-c042-tutorial-003/xcode-test.log`。该reveal的16次drag依次发生在29.58、32.50、35.44、38.34、41.30、44.35、47.36、50.32、53.24、56.19、59.16、62.12、65.11、68.11、71.15、74.22秒。全部从y696.2向上拖；前15次终点约340.8（一次340.7），第16次终点385.3。因此这是15次粗定位+末次缩短上滚，**不是上下交替振荡**。77.05秒后报告Bounded bidirectional reveal failed，79.015秒方法失败；没有第17次反向拖动。

最后一次实际拖动约310.9pt，已比355.5pt标准距离短，证明003“按溢出距离缩短末次”的代码分支确实起作用。但手指距离不等于ScrollView最终内容位移：XCTest日志注明500px/s、结束hold0，可能伴随松手后的惯性。不能只用 overflow+12 假设1:1定位。

终态AX（screenshots内teardown txt）：ScrollView `(0,163,402,711)`，范围y163…874；第八杆Button `(31.8,63.8,338.5,638.8)`，范围y63.8…702.6；caption y706.4。图片高度638.8<711，有72.2pt可用余量，所以可以完整进入该视口。终态缺的是图顶99.2pt，不是底部。下一轮若还有校正机会，当前代码会判down分支；本次不能声称它已验证反向校正成功。

已实际view_image查看完整终态PNG：球形1选中，第八杆海报上沿被固定切换栏截住，海报下方图注与“常见错误与纠正”露出；与AX一致。完整可见断言失败合理，不能改为部分相交/hittable就算通过。ScrollView顶163在导航栏底116、picker底155之后，视口定位有真实来源，不是把导航栏底误用成内容顶。图宽也完整位于window内。

由末次距离公式可作**推断而非实测帧轨迹**：distance310.9意味着末次前下溢约298.9，因此图片top约534.1（若高度不变）；到终态63.8位移约470.3，大于手指310.9。缺逐步frame采样，无法把比例精确归因为惯性而排除延迟布局。最稳妥结论是“最后上滚后的终态过冲+预算耗尽”；不直接判产品滚动故障。

## 视频原件状态

归档xcresult为 `Test-QiuJi-2026.09.08_15-38-35-+0800.xcresult`，`xcresulttool get test-results activities` 可读，实际含 `Screen Recording 2026-09-08 at 03.38.47 下午.mp4`，UUID `D01B98FC-B826-45E9-8E45-D95B5A607F83`，timestamp1788853127.878。当前run目录没有已导出的mp4/mov。本次未导出额外文件或解码视频帧；不能把元数据读取称为录像目视审阅。现有日志与终态足以否定“已发生方向振荡”并确认16次耗尽；若主控需要精确惯性判别，可只导出此现有附件，核74.22秒前后实际图顶随时间的位移，不操作设备。

## 最小观察适配建议（未实施）

1. 保留所有原业务oracle、精确第八杆图片/图注身份、完整image frame包含于有效视口。把“搜索远处目标”和“对齐已接近的目标”分成有界两个阶段：原16次粗查保持；一旦目标接近/与视口有交集进入最多4次精调。不是单纯把全局16改成更大的盲滚次数。
2. 每次滚前后取同一目标frame、window/有效scrollframe、方向、命令距离、单调时间，写一份事件JSON；进入精调/末次失败先PNG再AX。连续两次无进展或方向来回但overflow不减少就中止，不无限尝试。每次必须等frame稳定（有界两次相同/小容差采样），不能只把tap返回当几何稳定。
3. 精调优先把图中心对齐视口中心：本终态需要内容向下约135.3pt，可容许范围下移99.2…171.4pt。这是由实际frame算出的目标，不是硬编码坐标；手势先用部分纠偏量、减速或末端短hold抑制惯性，再以真实frame读回校正。具体手势速度/hold是待验证测试参数，不宣称已解决。出现图片实际高度超过有效视口时直接报不可完整展示，不能缩小验收范围。
4. 使用唯一当前活跃ScrollView同一effective rect检查和操作。现visible中tutorialSection采用scrollViews.firstMatch，而reveal选唯一可交互scroll；F2访问后有多个隐藏scroll实例，未来可能二者取不同对象。003只有F1，不是本次失败根因，但可以同一次纯观察器适配统一查找，避免F2后再引入假失败。caption也应以实际内容视口判可见，避免只在window内却被picker遮住。
5. **回F1仍绝对不滚动**：先前展示F1第八杆图片/图注后记可见frame作为证据，切F2再回F1只读检查，不调用reveal/精调。仍要求原图注可见、另一球形未串入；不能用“校正位置”掩盖阅读位置丢失。

这是最小诊断观察器建议，不需要改生产SwiftUI滚动行为、不需要生成图片，也不需要先跑更多课程。F2与位置保持在003尚未执行，原失败不覆盖；004后续是否通过只能依据新的实际终态与原图，不能由此审计预先宣布修好。
