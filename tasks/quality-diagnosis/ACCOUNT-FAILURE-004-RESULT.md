# 固定离线账号UI004结果

最终formal-004-account-failure-004实际1/1通过102.984秒，make/xcode0；xcresult14-41-17及123文件已归档。专用设备A24DACBB-4130-4D1B-85EC-DE02DCEEEDB7，iPhone17Pro/iOS26.2/light/large。

正常游客→现有合成账号fixture：合法旧名中插入一次短标记后保存，真实失败alert -1009，重新进入仍旧名。注销确认外侧关闭后身份保持；再次确认显示注销失败，点重试仍失败，取消后身份保持。正常退出登录→同进程游客；停止PID53485，移除身份fixture重启PID53802后仍游客。主控已看最终run七张关键全屏图：资料失败/重开旧名、注销失败/重试、身份保持、退出游客、重启游客。

前置失败原件保留：001 36.212秒/1415文件（键盘引导与中心光标导致替换不完整）；002 32.636秒/1337文件（末端点击仍未清空，未点保存）；003 72.304秒/1373文件（资料链已验，确认实际Popover无取消按钮）。004采用已观察PopoverDismissRegion且点击中心需在popover外。不改业务、不放宽保存失败后旧名、注销失败后身份和退出游客的断言。

边界：fixture在APIClient.requestData中构造请求前throw，协调器也提前停止push/pull。验证正常界面在固定离线失败下的状态，不是真实Apple认证/服务端注销/联网恢复；没有删除真实账号，没有验证成功注销后的本地训练数据迁移。重启时移除fixture是必要条件，否则引导会主动注入合成身份；不能冒称真实凭据冷启动。失败重试有tap与结果证据，未加请求计数器，不声称精确两次请求到服务端。
