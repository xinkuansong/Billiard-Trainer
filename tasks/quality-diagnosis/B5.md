# B5 代表设备与产物诊断执行卡

2026-09-06。依据PLAN-v2/B5-PREPARATION，冻结snapshot-002，只诊断。

- 系统清单重新读取：simulator-inventory-20260906.json；M2 SE3/iOS17.0 `DACB4EED-91D5-403A-A7C5-10DA70779987`，M3 mini A17Pro/iOS26.2 `5F88194A-942A-4A20-A5C7-976DDFBF5D07`，均本轮新建。M1沿用439DA53C，真实账号未登录。
- 14个待运行单元在build/quality-diagnosis/matrix-configs；每次先设置并读回系统外观/字号/对比度。R/N/D在所有launch贯穿既有followSystemAppearance参数，V51方法本来已有；仍须目视确认真实渲染。
- M1 Light/large五方法RTFSO已结束make0、5/5、113.024秒（旧exec11367已关闭），formal-b5-m1-light。运行器真实传V50截图环境（V51实际读取V50变量），证据不静默丢失。
- iPad旋转测试按仅竖屏规格解释，见DISCUSSION；L/P旧方法会清其独立空叶目录，专用/private/tmp/qiuji-v50/quality-b5路径，不覆盖专题证据根。
- Release独立入口diagnostic-release.mk已dry-run审核，仅Release simulator build禁签名，尚未执行；不能用Debug测试当Release结果。
- 外部真机/账号/声音实听等尚未取得条件，不混入本机通过。

- 当前M2 Light/large根入口1方法执行中exec33817，formal-b5-m2-light-root；boot/status与系统三项readback已核实。

- M2-light-root最终1/1、23.690秒、make0；旧exec33817关闭。当前M2-light-core三方法exec44174。M1闲置设备已shutdown，不影响保留数据；4841文件复核无业务漂移，见root/baseline-observation.json。

## M2 Light core原失败与复验

core最终make2/xcode65，3方法中训练旅程失败36.091秒；计时/球桌2方法通过62.010秒，总98.101秒。失败在Quality:137 note.isHittable，未保存。真实AX附件93391917…txt第164行是TextView且placeholderValue为记录本项心得，冻结DrillRecordView:205为axis.vertical TextField；failure-frame-30.png显示输入框可见。判为诊断定位类型失配，不是已证实输入不可操作或数据丢失。

overlay007只让独立诊断helper兼容TextField/TextView，仍要求可点击、实际输入唯一值、保存、进程重启读回。原failure/xcresult/录屏不覆盖；M2 persistence002单方法exec37307复验中。两项新独立ProfileNavigation/DetailJourney注册随overlay007审计，无业务/方案配置改动，scheme字节相同。

M2 persistence002完成1/1 make0，42.314秒，QD-92DCA1BC保存并真实重启读回；原失配保留。M2最大辅助字号三设置读回完成，root执行中exec65838。

M2 ax5-root 1/1 make0、25.119秒；当前exec88209串行core四方法→仅成功才unit20，失败停队列。字号图审单列B5-M2-AX5-REVIEW。

M2 AX5 core 4/4 make0、140.764秒，unit20/20 make0、0.042秒；88209队列关闭。随后独立RootReachability两方法补验筛选/历史空态真实点击，overlay008仅新增两独立测试注册（另音效草稿仍未跑）。

RootReachability AX5 2/2 make0、32.444秒，真实筛选换卡及历史空态CTA返回训练已执行，图审另记ROOT-REACHABILITY-RESULT。昵称/模板AX5两方法exec92578运行中。

M2 AX5 INPUT001 1/2、make2：昵称30.296秒通过，模板12.095秒在more.isHittable失败；002追加前置PNG/AX原样17.324秒复现。frame在屏内、menu可见，未判根因；后续独立MenuHit与货架入口验证。M3专用device启动→Light/large→root执行队列exec13665，M2已shutdown保留全部证据。

M3 root1/1 make0 37.528秒，五图独立审阅；当前61110串行core→theory→rotation，任一失败停止后续；L/P保持/private/tmp独立叶目录，结束要单独收manifest。

M3 core3/3 make0、138.746秒（详情44.098/保存67.688/球桌26.959），61110已进入theory；其后rotation仅前项成功才执行。M3 root五图已逐张审：实际Light、两列与顶部Tab完整；core另追加审核。

M3 theory1/1 make0 30.009秒；rotation1/1 make0 26.404秒。主控实看两图，理论正文非空，旋转图为横向系统中竖向App居中黑边。只断言竖向App frame，不证明旋转状态全控件交互；orientation已恢复portrait。L/P各单图manifest单列external-screenshot-manifest。61110关闭。Release独立build开始exec97725。

M3 Dark16624已结束4/4、122.107秒make0，13PNG+4frames独立审核，见B5-M3-DARK-REVIEW.md。Release97725已结束xcode0/make0，实际包审计见build/quality-diagnosis/formal-b5-release-001/RELEASE-RESULT.md；QD021为空API配置的冻结产物事实，根因未确认。当前12583为M3 AX5三单元串行队列。

## 用户暂停（2026-09-06，最新状态）

M1Dark10661按用户要求中断，make2/xcode75非产品失败；不计整组通过。此前运行中描述仅作历史，后续全部停止，见PAUSE-SUMMARY.md。
