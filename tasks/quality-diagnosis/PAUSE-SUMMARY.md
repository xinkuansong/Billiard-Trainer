# 质量诊断暂停记录与阶段结果

2026-09-06。**按用户明确要求暂停，余下工作以后再议；不自动恢复、不预约上线后任务。** 本文是阶段归档，不是最终全量诊断报告或上线验收。

## 暂停原因与边界

用户认为工作量过大，要求“先暂停下吧，记录下进展……后期上线后再说吧”。立即停止新增测试、补测准备与子任务；只做停止进程和保存进展。原“持续执行至交付”安排由本次暂停决定覆盖。没有修复业务代码、替换正式资源、提交或发布；本次暂停也没有关闭既有问题。

## 已经取得的主要证据

- 预期梳理、38场景覆盖方案、正式冻结基线及记录流程已建立。对象是snapshot-002（含当时未提交变更），不能外推后续主工作区版本。
- 正常训练保存并杀进程重启读回；自身历史样本编辑、取消、非法值拒绝、删除与旧样本保护；训练编排、统计独立账本等已有正式结果。
- 内容全量引用/解码检查，12课分层教学抽样，115杆/130图引用，其中20张教学图实际审核；代表工具已有正常操作、击打/重打/返回等证据，非全部物理/内容状态穷举。
- 账号隔离/恢复/队列替身测试、本地StoreKit三项已执行。新增真实MongoDB隔离实例五项2通过3失败，强化两项数据问题证据；实例已正常关闭，数据与日志保留。
- 小屏iOS17与iPad默认/最大辅助字号、iPad深色、竖屏规格下的旋转请求、昵称/模板等已有执行及图审。小屏更多/保存按钮AX不可命中与实际中心触摸响应分别保留，不将前者改写成原断言通过。
- Release优化模拟器构建通过，实际包资源/配置/隐私清单/扩展/体积已检查；非签名分发或真机验收。
- 最新完整原输入核对4841项：仅独立诊断Quality文件与工程注册两项变化，业务/资源漂移0。之后overlay012仅修改独立MenuHit诊断文件。

## 已发现且尚未修复的重点

| 事项 | 当前证据与用户影响 |
|---|---|
| QD007 数据归属 | 真实Mongo复验：A修改自己记录请求体后owner变为B，A不可见/B可见；不是已证明接管B原有记录 |
| QD008 恢复完整性 | 训练及认知两端点500条对照完整，501条各漏最旧1条；原服务端数据并未被删除 |
| QD012 提前结束 | 未操作组被保存并计入结果/分母，可能误导训练完成与统计 |
| QD013–018 内容 | 存在课程描述、方向、力度、几何等冲突；其中剂量/术语口径仍待裁定，不一概认定全部为程序bug |
| QD019 大字体 | 小屏与iPad搜索图标放大后被裁切；不代表已完成全App可访问性验收 |
| QD020 音效 | 四类资源在冻结源码及实际Debug/Release包中均缺；首发必须交付与否尚待裁定 |
| QD021 Release配置 | 冻结Release包API地址为空；B0未见有意脱敏，具体配置来源根因未确认，不外推当前工作区 |

完整台账见[ISSUES.md](ISSUES.md)，其中也包含测试失配与资源残留，编号数不能当程序缺陷总数。这里只保留后续处理优先级，不执行修复。

## 停在何处

- 当前M1 Dark运行FORMAL-B5-M1-DARK在用户暂停后被中断：exec10661已结束，make2/xcode75；保留pause-request.json及原始日志。**此非零退出属于用户中止，不记产品失败，不计整组通过。** 中断前截图/单项输出亦未经最终整组审核。
- 本轮专用M1已shutdown保存数据；M2/M3此前已shutdown。本轮没有继续UI队列或数据库实例。其他用户/并行任务设备未动。
- 子智能体无继续执行任务；已完成报告保留，tool_ui被中断的收尾稿不能凭自报当已验收。
- 四个新建专用设备（Thousand/Data1/NotificationAllow/NotificationDeny）只创建未启动，没有播种/登录/通知授权。UDID见build/quality-diagnosis/isolated-device-plans.json。
- Mongo官方运行时、临时数据库和构建/截图证据保留。没有做磁盘清理或删除，避免丢失复现依据。

## 明确未完成

- M1深色收口；剩余学习/理论、五类认知保存历史、多球形详情、求解边界、正常训练与模板/每日清台等补充旅程。
- 千条真实磁盘历史UI、DATA1跨页面精确账本；相关独立scheme/测试已注册，probe/seed/UI均未执行，不得当作证据。
- 冷启动及重复工具操作性能；通知真实首次允许/拒绝；尚未运行的资源诊断与其他配置列于additional-configs，草稿不等于覆盖。
- 真实Apple登录、真实购买恢复、真机声音/触感/后台/通知/VoiceOver等外部条件验证。
- 全部38场景最终复核与正式总报告。COVERAGE-STATUS中的partial保持局部完成，不升级为通过。

## 将来如何恢复

只在用户明确要求恢复后执行；“上线后再说”不是自动启动日期。先读本记录、README和COVERAGE-STATUS，再核对当前版本与冻结基线差异。**不要直接重跑全部旧计划**：优先重新约定较小范围，例如先处理数据可靠性问题，再按当时版本挑少量关键回归。

保留旧证据，恢复时用新的运行号，禁止覆盖中断批次或把新旧版本通过结果混合。旧冻结资源在build下，部分截图在/private/tmp下；如后续被手动/系统清理，须如实记证据缺失，不能凭本文复建“已通过”结论。未答的产品事项见DECISIONS-NEEDED.md，当前不催答。
