# 问题集合 v24 — 动作库缩略图：裁桌修复 + 列表大球

> **真源声明**：本文件是「动作库列表裁桌 + 列表/计划缩略图球放大（详情顶栏 live 保持真尺寸）」这一轮需求的**真源**，供 `plan-batch-execution` / `plan-delegated-execution` 执行。高版本覆盖低版本。
>
> - **版本**：v24.2
> - **日期**：2026-07-29
> - **状态**：✅ 全部收官（W1+W2，主控亲做，0 返工；D-v24-1=C · D-v24-2=1.8 · D-v24-3=主控亲做）
> - **来源**：2026-07-29 会话——DR-039 静帧对齐落地后用户反馈：(1) 刚进动作库列表卡片看不到完整球桌；(2) 希望列表/计划用放大球、详情顶栏「动图」保持真尺寸；主控澄清裁切根因与资产分层后，指令 `/issue-collection-restructure`。
> - **排期前置**：
>   - `tasks/HUMAN-REQUIRED.md` 无 ⏳ `[BLOCKING]` 项。
>   - 依赖 **DR-039** 已落地（`DrillStaticPreview` + 代表性选源 + 杆/线 v2/ghost + 77 张 PNG）；本轮**不重做**静帧契约，只改显示槽比例与列表 `ballScale`。
>   - 与 v23 点验 / v16 拍板 / H-22 **互不阻塞**。
> - **与既有方案关系**：
>   - 契约底稿：`docs/research/20260729-drill缩略图静帧契约.md`（本轮结束后同步修订 ballScale / 显示槽）。
>   - 导出管线已有「卡片档」`SequenceVideoExporter.Options.card()`（`ballScale=1.6`，cover/Previews 帧）——本轮**不强制**与之数值对齐，见 D-v24-2；App 详情顶栏是 live `DrillSceneView`，**不是**烘焙动图。
>   - **不做**：第二套 `DrillThumbnails` PNG（详情真尺寸不靠烘焙）；不改 Angle 首页 `AngleGridCard` 的 4:3（那是海报封面，非球桌缩略图）。
> - **锚点核实日期**：2026-07-29（§五逐条读代码）
> - **编号约定**：条目 **E1–E3**；批次 **W1–W2**；拍板项 **D-v24-N**

---

## 一、需求回显（语义基准，不增删）

### 1.1 用户原始诉求

1. 刚进入动作库列表时，卡片里的缩略图**看不到完整球桌**。
2. 缩略图用在卡片、计划、以及「详细内容首页的动图」等多处——想法是：**卡片和计划用 1.8 倍球大小**；另一档保持真尺寸观感。
3. 经澄清：「详细内容首页的动图」= **Drill 详情顶栏可播台面**（live），不是精讲页、也不是再烘一套详情 PNG。

### 1.2 验收语义（条目）

| # | 条目 | 验收语义 |
|---|---|---|
| **E1** | 网格卡完整桌 | 动作库双列网格卡封面区比例与烘焙图一致（**2:1**），`.fill` 后**左右库边/外框可见**，不再被 4:3 槽裁掉；骨架屏同步，避免加载跳变 |
| **E2** | 列表大球 | 所有进 Bundle 的 `DrillThumbnails/<id>.png` 按列表可读性以 **`ballScale=1.8`** 重烘（数值以 D-v24-2 为准）；`DrillStaticPreview.Options.thumbnail` 与烘焙入口一致 |
| **E3** | 详情顶栏真尺寸 | `DrillSceneView` / `Options.detail` 保持 **`ballScale=1.0`**；**不**为详情另存第二套缩略图 PNG |

### 1.3 主控理解 / 假设 / 不确定点

**理解的需求**：列表小槽位要「一眼认出台型 + 球够大」；详情顶栏要「球形真实、可点播放」。裁桌是显示层比例错误，不是烘焙漏画。

**假设**（不符请在 D-v24-* 纠正）：

| ID | 假设 | 依据 |
|---|---|---|
| A1 | 裁桌根因 = 烘焙 **2:1** + 网格槽 **4:3** + `BTBakedDrillTable` **`.fill`** → 左右被裁 | 读码核实 §五 |
| A2 | 网格卡改 **2:1** 是主修复；不重烘成 4:3、不为裁切再加第二套图 | 会话共识 |
| A3 | 详情顶栏继续 live，列表只需**一套**放大球 PNG | 用户澄清「不是详情页再烘图」 |
| A4 | 计划详情行已是 **40×20（2:1）**，裁桌不严重；主要痛点在动作库网格 4:3 | `PlanDetailView` L442–443 |
| A5 | 方槽行卡（64²/56²/48²/40²）物理上无法「铺满又看全 2:1」——需单独拍板策略 | D-v24-1 |

**不确定点 → 拍板项**：见 §四。

---

## 二、代码事实（读码核实）

### 2.1 裁切链

| 层 | 现状 | 路径 |
|---|---|---|
| 烘焙像素 | 320×160@2x → **640×320，2:1** | `DrillThumbnailRenderer.render` 默认 size |
| 静帧选项 | `Options.thumbnail.ballScale = **1.0**`（热修后）；`detail = 1.0` | `DrillStaticPreview.swift` L49–56 |
| 列表组件 | `aspectRatio(2.0, contentMode: **.fill**)` + `.clipped()` | `BTBakedDrillTable.swift` L28–35 |
| 网格卡槽 | `aspectRatio(**4.0/3.0**)` + overlay 缩略图 + `.clipped()` | `BTDrillCard.swift` `BTDrillGridCard` L113–118 |
| 骨架 | 同 **4/3**（F-CL-03 对齐真卡） | `BTShimmer.swift` `BTDrillGridCardSkeleton` L78 |
| 消费入口 | `DrillListView` → `BTDrillGridCard` | `DrillListView.swift` |

### 2.2 缩略图消费点（均 `BTBakedDrillTable`）

| 位置 | 槽尺寸 / 比例 | 备注 |
|---|---|---|
| `BTDrillGridCard` | 宽满列 × **4:3** | **主裁桌点** |
| `BTDrillCard` / `BTDrillThumbnail` | **64×64** 方 | 行卡；完整桌本就不可能 |
| `BTExerciseRow` | **56×56** | 方 |
| `TrainingSummaryView` | **48×48** | 方 |
| `TrainingDetailView` | **40×40** | 方 |
| `CustomPlanBuilderView` | 计划构建行（同组件） | 方/小 |
| `PlanDetailView.drillThumbnail` | **40×20（2:1）** | 已与烘焙比匹配 |
| `DrillRecordView` | 记录行 | 小槽 |

### 2.3 详情顶栏「动图」

| 事实 | 路径 |
|---|---|
| Drill 详情顶栏 = **`DrillSceneView` live**（可点播放回放），非 `DrillThumbnails` PNG | `DrillDetailView.tableSection` L138–149 |
| live 静帧/回放共用 `DrillStaticPreview.Options.detail`（`ballScale=1`） | `DrillSceneView.swift` |
| 位图帧动画 SPIKE：`BTDrillPreviewPlayer` → `Resources/Previews/<id>/`；导出 card 档 `ballScale=1.6` | `BTDrillPreviewPlayer.swift`；`SequenceVideoExporter.Options.card()` L136–142 |

### 2.4 明确划界（本轮不做）

- `AngleHomeView.AngleGridCard` 的 4:3：角度练习**海报封面**，不是球桌缩略图。
- 为详情再烘一套 1.0 PNG；双目录 `DrillThumbnails` / `DrillThumbnailsCard`。
- 改导出 `card()` 1.6 或 Previews 量产（除非 D-v24-2 要求数值对齐且另开批）。

---

## 三、批次表

| 批次 | 范围 | 量级 | 依赖 | 建议模型 |
|---|---|---|---|---|
| **W1** ✅ 2026-07-29 | E1：网格卡（+骨架）封面 **4:3→2:1**；`BTBakedDrillTable` 可选 `contentMode`（默认 fill，供方槽日后 fit）；SPEC/契约一句显示槽说明 | S | 无 | 主控亲做 |
| **W2** ✅ 2026-07-29 | E2+E3：`Options.thumbnail.ballScale`→**1.8**；全量 `test_bakeAllThumbnails` 重烘；抽查网格完整桌+大球；`detail` 仍 1.0 的断言/注释；回写契约文档 + DR + Changelog | S–M | W1 ✅ | 主控亲做 |

**依赖图**：

```text
W1（显示槽 2:1）──► W2（列表 1.8 重烘；详情 live 1.0 不动）
```

### W1 完成标准

- [x] `BTDrillGridCard` 封面 `aspectRatio` 为 **2.0**（或等价 2/1）
- [x] `BTDrillGridCardSkeleton` 同步 2:1（F-CL-03 不回退）
- [x] `make build` SUCCEEDED（`build/v24-w1-build.log`）
- [x] 截图或 ImageRenderer：动作库网格卡可见左右外框/袋口（`build/v24-w1-evidence/01-grid-cards-2to1.png`；单测 1/0）
- [x] **未**改 `AngleGridCard` 4:3（`AngleHomeView` L466 仍 4/3）
- [x] 方槽策略按 D-v24-1=**C** 留档：方槽维持 `.fill`；`BTBakedDrillTable` 增可选 `contentMode` 默认 fill

### W2 完成标准

- [x] `DrillStaticPreview.Options.thumbnail.ballScale` = **1.8**；`detail.ballScale` 仍为 **1.0**（单测 `test_options_listBallScale_vs_detailTrueSize`）
- [x] `test_bakeAllThumbnails` **77/77**；日志 `build/v24-w2-rebake.log`
- [x] 抽查 ≥3 张（c001/c053/c010）：`build/v24-w2-evidence/png-*.png` + 网格 `01-grid-cards-ballscale18.png`；完整桌仍在
- [x] 契约文档 + SPEC Changelog + **DR-040**；`PROGRESS` / Hub 回写
- [x] 代码/注释标明：详情顶栏不消费列表 PNG 球放大（`DrillStaticPreview.Options`）

---

## 四、拍板项（开工前）

| ID | 问题 | 决议 | 备选（未选） |
|---|---|---|---|
| **D-v24-1** | 方槽行卡（64² 等）策略？ | ✅ **C** — 本轮只改网格+骨架；方槽维持 `.fill` 中心裁，契约/PROGRESS 留档（不假装完整桌） | A 全站 fill 声明；B `.fit`+letterbox |
| **D-v24-2** | 列表 `ballScale`？ | ✅ **1.8**（用户原话） | 1.6（与导出 `card()` 对齐） |
| **D-v24-3** | 执行模型？ | ✅ **主控亲做** | 子智能体 grok 串行 W1→W2 |

> 2026-07-29 用户确认「按推荐」→ 升 **v24.1**，开工 W1。

---

## 五、锚点索引（2026-07-29 核实）

| 锚点 | 路径 | 行号/符号（约） |
|---|---|---|
| 网格卡 4:3 槽 | `QiuJi/Core/Components/BTDrillCard.swift` | `BTDrillGridCard` L113–118 |
| 骨架 4:3 | `QiuJi/Core/Components/BTShimmer.swift` | `BTDrillGridCardSkeleton` L78 |
| 列表组件 fill | `QiuJi/Core/Components/BTBakedDrillTable.swift` | L28–35 |
| 静帧 ballScale | `QiuJi/Core/Scene/DrillStaticPreview.swift` | `Options.thumbnail/detail` L49–56 |
| 烘焙入口 | `QiuJi/Core/Scene/DrillThumbnailRenderer.swift` | `options: .thumbnail` |
| 烘焙跑测 | `QiuJiTests/DrillThumbnailBakeRunnerTests.swift` | `test_bakeAllThumbnails` |
| 列表入口 | `QiuJi/Features/DrillLibrary/Views/DrillListView.swift` | `BTDrillGridCard` |
| 详情顶栏 live | `QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift` | `tableSection` → `DrillSceneView` L138–149 |
| 计划行 2:1 | `QiuJi/Features/Training/Views/PlanDetailView.swift` | L442–443 `40×20` |
| 导出卡片档 | `QiuJi/Core/Media/SequenceVideoExporter.swift` | `Options.card()` L136–142 |
| 既有契约 | `docs/research/20260729-drill缩略图静帧契约.md` | 全文 |
| DR-039 | `tasks/IMPLEMENTATION-LOG.md` | DR-039 |
| 划界：角度海报 | `QiuJi/Features/AngleTraining/Views/AngleHomeView.swift` | `AngleGridCard` L466（**本轮不改**） |

---

## 六、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| **v24.2** | 2026-07-29 | W1+W2 收官：网格 2:1 + 列表 ballScale=1.8 重烘 77；DR-040；证据 `build/v24-w{1,2}-*` |
| **v24.1** | 2026-07-29 | 拍板齐：D-v24-1=C / D-v24-2=1.8 / D-v24-3=主控亲做；开工 W1 |
| **v24.0** | 2026-07-29 | 立项：E1 网格 2:1 裁桌修复；E2 列表 ballScale（推荐 1.8）重烘；E3 详情 live 保持 1.0、不双套 PNG；批次 W1→W2；拍板 D-v24-1～3 |

---

## 七、推荐默认（拍板前可先按此理解）

若用户回复「按推荐」：

- D-v24-1 = **C**（本轮只改网格+骨架；方槽维持 fill，PROGRESS/契约留档）
- D-v24-2 = **1.8**
- D-v24-3 = **主控亲做**

然后直接开 W1。
