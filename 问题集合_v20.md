# 问题集合 v20 — 加塞吃库图谱（中杆 × 左右塞 × 碰后首库出射）

> **本文件是本轮需求真源。** 高版本覆盖低版本；与 v18（⏸ 暂缓）、v19（✅ 收官）文件域近乎零交集（本轮新增学区沙盘页，不改杆显隐 / 可行打点 / 引擎常量）。
>
> - **版本**：v20.5
> - **日期**：2026-07-28
> - **状态**：✅ **全部批次收官**（W1+W2；2026-07-28 修订 D-v20-5：碰前 8 线实况）
> - **来源**：2026-07-27 会话——既有「分离角图谱」（高低杆）后，用户要姊妹页：中杆、扫左右塞、重点展示吃一库后变化；拍板 ①碰目标球后吃一库 ②纯看库边效应（补偿挤偏）③独立学卡；指令 `/issue-collection-restructure`；同日「开始吧，使用 cursor grok 4.5」→ 委派执行
> - **排期前置**：无硬前置。`HUMAN-REQUIRED` 现存 ⏳ 均为发布类，不阻塞。建议与 v16 拍板、v18 暂缓互不抢波。
> - **锚点核实日期**：2026-07-27（§五全部逐条读代码确认）
> - **编号约定**：条目 **E1–E4**；批次 **W1–W2**；拍板项 **D-v20-N**

---

## 一、需求回显（语义基准，不增删）

### 1.1 用户原始诉求

1. 已有「分离角图谱」（高低杆扇形），要再做一个**加塞量 → 吃库后分离/反弹变化**的图谱。
2. **中杆**（`spinY=0`），扫不同**左右塞**量。
3. **主要展示吃一库后的变化**。

### 1.2 已拍板（2026-07-27）

| # | 结论 | 原话/选项 |
|---|---|---|
| **D-v20-A** | 场景 = **先碰目标球 → 再吃一库**（非直吃库） | 「1. 碰目标球后吃一库」 |
| **D-v20-B** | 瞄准隔离 = **纯看库边效应**（补偿挤偏，使各档切角/接触几何对齐） | 「2. 纯看库边效应」 |
| **D-v20-C** | 产品形态 = **独立学卡**（不并入分离角图谱分段切换） | 「3. 独立学卡」 |
| **D-v20-1** | 学卡名「**加塞吃库图谱**」；副标题「中杆 · 左右塞 · 吃库后出射」；glyph「塞」；封面青绿偏蓝 | 用户开工「按推荐」隐含（2026-07-27 开始委派） |
| **D-v20-2** | 切片终点：首库 → 下一 `ballCushion`；无二库则固定弧长 **0.40 m**（或至停点取短者） | 同上 |
| **D-v20-3** | 默认教学盘面：**专用盘面**（默认力度下 8 档皆碰后吃到长库） | 同上 |
| **D-v20-4** | 学卡序：紧挨「分离角图谱」之后 | 同上（W2） |
| **D-v20-5** | 碰前/库后均按 8 档实况上色（2026-07-28 用户改口：按实际画碰前 8 线；原「淡灰单条」废止） | 同上（W2；后修订） |
| **D-v20-6** | 执行：两批串行 `cursor-grok-4.5-high-fast` + 主控逐文件读 diff | 用户显式指定 cursor grok 4.5 |

### 1.3 验收语义（条目）

| # | 条目 | 验收语义 |
|---|---|---|
| **E1** | 中杆 × 左右塞 8 档实况扇形 | `spinY≡0`；`spinX` 在 `[+miscueLimit, −miscueLimit]` 均匀 8 档；每档一次全保真 `simulateFree`；页内 8 色轨迹对比；力度可调；打点为只读图例（左缘 8 迷你盘，横轴左右塞） |
| **E2** | 切片 = 碰后**首库出射段** | 母球路径主展示：`首次 ballCushion（碰球之后）→ 其后一段`（见 D-v20-2）；**不是** Y3 的「球-球→到库」；无首库时诚实降级（停点/短弧）并保证 8 档仍有线可画 |
| **E3** | 挤偏补偿（纯库边） | 各档在调用 `simulateFree` 前对几何瞄准做挤偏逆补偿，使**实际出发方向对齐同一几何切角**（同接触几何意图）；UI 脚注诚实写清：已补偿挤偏；球-球投掷（SIT）与碰前侧旋衰减仍随塞变化（不假装「只剩库边一项」） |
| **E4** | 独立学卡 + 导流 | 新 `AngleRoute` + `learnEntries` 卡 + 封面色 + `MainTabView` destination；「旋转与加塞」吃库节从「定性示意」链到本页（遵守页末 `PracticeCTA`≤2，优先 `LearnDocTextLink` / 脚注改链）；SPEC §9.3 新行 + Changelog；巡游帧 |

### 1.4 主控理解 / 假设 / 不确定点

**理解的需求**：做「分离角图谱」的**加塞姊妹页**——变量从高低杆换成左右塞，观察窗口从「碰后到库」换成「吃一库后出射」，用引擎实况替换 SpinAndEnglish 里的顺/逆塞示意折线。

**假设**（不符请在 D-v20-* 纠正）：

- A1：产品名推荐「**加塞吃库图谱**」（见 D-v20-1）；口语「分离角」在吃库语境下实为**反弹角/出射方向**，正文避免与 Y3「分离角」混用。
- A2：「纯看库边」= **只补偿挤偏（squirt）**，使各档几何切角对齐；**不**消除 SIT/传旋/侧旋衰减——那些是「碰球后再吃库」的真实物理，脚注交代即可。
- A3：壳复用 Y3/v15 范式（真台、去抖并行、左缘 8 盘、右缘力度、球库），**不**改 `SeparationAngleAtlas*` 源文件（克隆新类型，防双页缠死）。
- A4：引擎零改动；库边侧旋已由 `Han2005`/`resolveCushionCollisionPure` 消费 `wUp`。

**不确定点 → §四拍板项**。

### 1.5 明确不做

| 不做项 | 依据 |
|---|---|
| 直吃库模式 / 双模式切换 | D-v20-A 已定碰后再吃库 |
| 并入「分离角图谱」用 Picker 切换轴 | D-v20-C 独立卡 |
| 改引擎 / 库边常数标定 | 教学可视化，非 Layer C |
| 深讲挤偏/弧线/投掷 | 归「瞄准修正」；本页只隔离+展示库后扇形 |
| 可编辑打点盘（开放任意 spin） | 与 Y3 一致：8 档扫一轴，盘只读 |
| 改 v14 学文档壳契约主体 | 本页是暗色沙盘，对齐 Y3 豁免（SPEC 已排除图谱类沙盘） |

---

## 二、调研 / 教学口径（写作事实来源）

| 主题 | 口径 | 既有资产 |
|---|---|---|
| 顺塞 / 逆塞 | 无塞基线「入≈反」；侧旋打破基线——**顺塞更开、逆塞更闭** | `SpinAndEnglishView` cushion 节（定性）；项目 16 T09 |
| 与高低杆图谱分工 | 高低杆改**碰后分离方向**（Y3）；左右塞主要改**吃库反弹**（本页） | SPEC §9.3 旋转与加塞 / 分离角图谱行 |
| 挤偏 | `spinX` 正=左塞；`squirtAngle` 负=向右；`actualDirection = aim.rotatedY(−squirt)` | `CueBallStrike.swift:92–143`；AimingCorrection Z3 |
| 平杆纯侧旋弧线 | ≈0（Z3 草稿）；故中杆隔离下补偿挤偏后，碰前路径弯折不是主混淆源 | `问题集合_v12` Z3 数值补稿 |

**红线**：轨迹/角度上屏必须来自 `simulateFree` 实测切片；禁止手动画顺/逆塞折线冒充图谱（SpinAndEnglish 示意可保留，并改链到本页）。

---

## 三、代码现状（事实，已核实 2026-07-27）

### 3.1 姊妹页「分离角图谱」（克隆模板）

| 项 | 锚点 | 现状 |
|---|---|---|
| Geometry | `QiuJi/Features/AngleTraining/SeparationAngleAtlasGeometry.swift`（170 行） | `spinYLevels()` 8 档；`trackColors`；切片 `pathAfterContactToFirstCueCushion`（球-球→首库，无库降级停点）；默认半球场景 |
| VM | `…/ViewModels/SeparationAngleAtlasViewModel.swift`（442 行） | 20ms 去抖+单飞+末班车；`concurrentPerform`×8 `simulateFree(spinX:0, spinY:)`；球库语义 v15 |
| View | `…/Views/SeparationAngleAtlasView.swift`（325 行） | 暗色沙盘；左缘 8 只读盘（纵轴高→低）；右缘力度 `onSpinTap:nil`；底栏 `BTBallPaletteBar` |
| 单测 | `QiuJiTests/SeparationAngleAtlasTests.swift` | 档位端点、切片、90°、低力度降级 |
| 巡游 | `ScreenshotTourUITests` a15 + `testSeparationAngleAtlasInteractions` | 按卡名点击 |
| DR | DR-025 | 页内 8 色豁免线语言 v2 |

### 3.2 物理 / 挤偏（本页关键）

| 项 | 锚点 | 用途 |
|---|---|---|
| `simulateFree` | `ShotPredictor.swift:500+` | 全保真；`events` 含 `.ballBall` / `.ballCushion` |
| `squirtAngle` / `actualDirection` | `CueBallStrike.swift:92–143` | 补偿：令 `actualDirection(aim',spinX)≈geometricAim` ⇒ `aim' = geometricAim.rotatedY(+squirtAngle(spinX))`（与 `actualDirection` 取 `−squirt` 互逆；W1 数值草稿必须实证） |
| 库边侧旋 | `CollisionResolver.resolveCushionCollisionPure` → `Han2005CushionModel`（`wUp`） | 引擎已吃侧旋，本轮零改 |
| 打滑极限 | `CuePhysics.miscueLimitFraction`（0.5） | 8 档端点 |
| 力度 | `ShotTuning.velocityRange` / `defaultVelocity` | 与 Y3 同柱 |

### 3.3 学卡注册（E4 四处 + 导流）

| 项 | 锚点 |
|---|---|
| `AngleRoute` | `AngleHomeView.swift:5–41`（现无本页 case） |
| `learnEntries` | 同文件 L113–148：…旋 → 角度与瞄准 → **分离角图谱** → 浅谈球感… |
| 封面色 | `AngleCoverPalette.swift`（学区段 L13–48） |
| destination | `MainTabView.swift` `angleDestination` L110–123 |
| 吃库定性节 | `SpinAndEnglishView.swift` L183–201；脚注指向「分离角图谱或走位求解」（L198）——**应改指本页** |
| CTA 密度 | 同文件已有 `PracticeCTA`×2（瞄准修正 L210 + 分离角图谱 L227）；D-v14-6 **页末大卡 ≤2** ⇒ 本页入口用 `LearnDocTextLink` 或放进吃库节正文链，**勿再加第三张 PracticeCTA** |

### 3.4 与「分离角」命名的关系（防 FL-026）

用户口语「吃库后分离角」在台球教学里通常指**反弹/出射方向相对无塞基线的变化**，不是球-球 90° 分离角。真源采用推荐名「加塞吃库图谱」；若用户改名见 D-v20-1。委派提示词必须附对照：

| 用户原话 | 采用定义 |
|---|---|
| 加塞量的吃库后分离角变化图谱 | 中杆下扫 `spinX`，展示母球**碰目标球后首个吃库之后**的出射轨迹扇形（引擎实况） |
| 纯看库边效应 | 各档补偿挤偏使几何切角对齐；主对比量=库后出射方向差 |

---

## 四、拍板项（开工前确认；带推荐）

| # | 问题 | 推荐 | 状态 |
|---|---|---|---|
| **D-v20-1** | 学卡命名 + glyph | 标题「**加塞吃库图谱**」；副标题「中杆 · 左右塞 · 吃库后出射」；glyph「塞」；封面色青绿偏蓝 | ✅ 2026-07-27 按推荐 |
| **D-v20-2** | 库后切片终点 | **首库 → 下一 `ballCushion`；若无二库则固定弧长 0.40 m（或至停点取短者）**；金点可标首库触点 | ✅ 2026-07-27 按推荐 |
| **D-v20-3** | 默认教学盘面 | **专用盘面**（保证默认力度下 8 档皆「碰球后吃到长库」且库后扇形可辨），不必复用 Y3 半球 | ✅ 2026-07-27 按推荐 |
| **D-v20-4** | 学卡插入位置 | `learnEntries`：**紧挨「分离角图谱」之后** | ✅ 2026-07-27 按推荐（W2） |
| **D-v20-5** | 碰前轨迹是否上屏 | ~~淡灰单条~~ → **8 档实况同色**（碰前 hint / 库后 main） | ✅ 2026-07-27 原推荐；**2026-07-28 用户改口「按实际画碰前 8 线」** |
| **D-v20-6** | 执行模型 | 两批串行 `cursor-grok-4.5-high-fast`；主控逐文件读 diff | ✅ 用户指定 cursor grok 4.5 |

> **已拍齐**：D-v20-A/B/C + D-v20-1～6。

---

## 五、锚点索引（读代码核实，2026-07-27）

| 锚点 | 位置 | 用途 |
|---|---|---|
| Y3 Geometry / 切片 | `SeparationAngleAtlasGeometry.swift` 全文 | 克隆对照；本页新切片函数 |
| Y3 VM 并行模拟 | `SeparationAngleAtlasViewModel.swift` ~L300–358（`spinYLevels`→改 `spinX` + 补偿 aim） | 性能契约模板 |
| Y3 View 布局 | `SeparationAngleAtlasView.swift` L1–180 | 左缘图例/力度/球库 |
| `simulateFree` | `ShotPredictor.swift:500+` | 轨迹真源 |
| 挤偏互逆 | `CueBallStrike.swift:92–143` | E3 补偿公式 |
| 库边 `wUp` | `CollisionResolver.swift:129–172` | 证明引擎已支持，勿改 |
| 学卡注册 | `AngleHomeView.swift:5–41, 113–148`；`MainTabView.swift:110–123`；`AngleCoverPalette.swift:13–48` | E4 |
| 吃库示意脚注 | `SpinAndEnglishView.swift:183–201, 221–240` | 改链 + CTA 密度 |
| SPEC 学表 | `tasks/UI-IMPLEMENTATION-SPEC.md` §9.3 ~L929–930 | 新行 + Changelog |
| 巡游 | `ScreenshotTourUITests.swift` ~L187（a15） | 增 a17（或下一空号）+ 交互测 |
| DR-025 先例 | `tasks/IMPLEMENTATION-LOG.md` DR-025 | 本页另记 DR（左右塞色板） |

---

## 六、批次表

量级估计：W1 几何+单测+草稿 ≈ Y3 切片返工量；W2 整页克隆+注册 ≈ Y3/v15 单批。合并成一批易在「盘面保吃库 + 补偿手性 + UI」三处同时翻车 → **拆两批**。

```
W1（几何真源 + 补偿 + 切片 + 单测/草稿）
 └── W2（独立学卡页 + 注册/导流 + 巡游 + SPEC）
```

| 批次 | 范围 | 量级 | 依赖 | 模型 | 状态 |
|---|---|---|---|---|---|
| **W1** | 新建 `CushionEnglishAtlasGeometry`：`spinXLevels()`；`aimDirCompensatingSquirt(geometric:spinX:)`；`pathAfterFirstCueCushion(…)`（E2/D-v20-2）；`defaultTeachingScene()`（D-v20-3）；页内色板；`CushionEnglishAtlasTests` + `build/v20-evidence/` 草稿 | M | D-v20-1～3、6 ✅ | `cursor-grok-4.5-high-fast` | ✅ 2026-07-27（0 轮返工；证据 `build/v20-{logs,evidence}/`） |
| **W2** | `CushionEnglishAtlasView`/`ViewModel`（克隆 Y3/v15 壳：去抖并行、左缘 8 **横向**迷你盘、右缘力度、球库）；注册 4 处；SpinAndEnglish 脚注/文字链；SPEC §9.3+Changelog；DR-029；巡游+交互 UI 测；明/暗默认态截图 | L | W1 ✅；D-v20-4/5 ✅ | `cursor-grok-4.5-high-fast` | ✅ 2026-07-27（0 轮返工；证据 `build/v20-{logs,screenshots}/`） |

**波次**：全串行 W1→W2；不与改 `AngleHomeView`/`ScreenshotTourUITests` 的其它批同波硬并行（合并易撞）。

### W1 完成标准

1. 坐标契约回显（SceneKit X–Z、Y 上；`spinX` 正=左塞）写入 Geometry 文件头。
2. 单测：`spinXLevels` 端点=±miscue；补偿后 `CueBallStrike.actualDirection(aim',spinX)` 与 geometric 夹角 **P95 < 0.05°**（或草稿等价阈值）；默认盘面+默认力度下 8 档均存在「球-球后首个 ballCushion」；切片起点贴首库、终点符合 D-v20-2。
3. 数值草稿落盘 `build/v20-evidence/`（含手性：左塞/右塞相对中杆开闭方向与 SpinAndEnglish 文案一致或如实记录引擎符号）。
4. `make build` SUCCEEDED；本批单测全绿；**不改**引擎与 Y3 源文件。

### W1 主控验收纪要（2026-07-27）

- 亲读：`w1-build.log` **BUILD SUCCEEDED**；`w1-unit-tests.log` **5/0 TEST SUCCEEDED**；草稿补偿 P95=0.0198°、8/8 长库、手性同号如实未喂绿；Geometry 文件头契约齐；`git` 未改 Y3/引擎；主树复跑 `make build` **SUCCEEDED**。
- **留给 W2 的诚实脚注**：本默认盘面极端左右塞库后 Δyaw 相对中杆**同号**（+40°/+26°），中间档有负值——UI **禁止**硬写「本图证明顺开逆闭」；可写「侧旋改变吃库出射；顺/逆塞定性见旋转与加塞；本页为引擎实况扇形」。

### W2 完成标准

1. 学卡可点达；导航标题=「加塞吃库图谱」；HUD 文案标明「8 档左右塞 · 吃库后」。
2. 默认态截图：8 色库后扇形可辨；左缘 8 盘为左右塞点位（非高低杆竖点）；碰前亦为同序 8 色实况线（D-v20-5 修订）。
3. SpinAndEnglish 吃库节精确轨迹改链本页；`PracticeCTA` 仍 ≤2（本页用 `LearnDocTextLink` 或吃库节文字链）。
4. SPEC §9.3 新行 + Changelog；IMPLEMENTATION-LOG DR（页内色板豁免，仿 DR-025）。
5. `make build` SUCCEEDED；交互/巡游相关 UI 测绿；明/暗截图归档 `build/v20-screenshots/`（或 `build/v20-evidence/screenshots/`）。
6. 脚注含 W1 手性诚实口径（见上纪要），禁止喂绿文案。

### W2 主控验收纪要（2026-07-27）

- 亲读：`w2-build.log` **BUILD SUCCEEDED**；`w2-unit-tests.log` **5/0**；`w2-ui-tests.log` 明/暗 `testCushionEnglishAtlasInteractions` 各 **1/0 TEST SUCCEEDED**；主树复跑 `make build` **SUCCEEDED**。
- 截图目视：`a17-*-default.png` 库后 8 色扇形可辨、左缘横向左右塞点、HUD「8 档左右塞 · 吃库后」、底栏诚实脚注（无「证明顺开逆闭」）；`-w2.uiHooks` 角标仅测钩子（对齐 Y3）。
- 注册：学卡紧挨分离角图谱后；`PracticeCTA` 在 SpinAndEnglish 仍为 2；吃库节 `LearnDocTextLink`→本页；SPEC §9.3 + DR-029。
- **留档**：完整 `testAngleLearningPages`（含 a17）未整段重跑——a17 已注册，交互测已覆盖本页；非阻塞。

---

## 七、坐标契约（执行批强制回显）

- SceneKit：水平 **X–Z**，**Y 朝上**；+X 右端，+Z 顶视图上方；单位米（`.kiro/steering/table-geometry.md`）。
- `spinX` 正 = **左塞**；`spinY` 本页锁 **0**（中杆）。
- 挤偏：`α = CueBallStrike.squirtAngle(a: spinX)`；补偿瞄准 `aim' = geometricAim.rotatedY(+α)`，使得 `actualDirection(aim', spinX) ≈ geometricAim`（W1 实证，禁止凭记忆改号）。
- 切片时间轴：以 `ShotEvent.time` 与 `recorder.stateAt` 为准，与 Y3 同族 helper。

---

## 八、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| **v20.5** | 2026-07-28 | D-v20-5 修订：碰前按 8 档实况同色上屏（废止淡灰单条） |
| **v20.4** | 2026-07-27 | W2 ✅ 主控验收 0 返工 → **v20 全部收官** |
| **v20.3** | 2026-07-27 | W2 执行交付：View/VM/注册/导流/SPEC/DR-029/巡游 a17+交互测/明暗截图；证据 `build/v20-{logs,screenshots}/`；待主控验收 |
| **v20.2** | 2026-07-27 | W1 ✅ 主控验收 0 返工；开工 W2；记手性同号脚注约束 |
| **v20.1** | 2026-07-27 | D-v20-1～6 按推荐齐；模型写入批次表；开工 W1（委派 grok-4.5） |
| **v20.0** | 2026-07-27 | 初版立项：拍板 D-v20-A/B/C；批次 W1→W2；待 D-v20-1～6 |
