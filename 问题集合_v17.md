# 问题集合 v17 — 加塞旋转不上屏（球体自转渲染层根治）

> **本文件是本轮需求的真源。** 日期：2026-07-25　状态：✅ **全部完成**（W1 ✅ / W2 ✅，2026-07-25）　版本：v17.3
> 来源：2026-07-25 会话——用户报告「球加塞后效果上会带有旋转，但现在看起来好像并没有生效（看不到球的旋转状态）」，要求检查是否确有此问题；主控读码确证后，用户指令 `/issue-collection-restructure` 整理成可执行方案。
> 编号约定：条目 **S1–S5**；批次 **W1–W2**；拍板项 **D-v17-N**。
> 排期前置：与 **v16**（练习 Tab 产品化，⏳ 待拍板 D-v16-1～7）**文件域零交集**（v16 动 `Features/AngleTraining` 首页与数据层，v17 动 `Core/Physics/TrajectoryPlayback` + `Core/Media`），可独立推进或并行。`tasks/HUMAN-REQUIRED.md` 现存 ⏳ 项均为发布/外部流程类（H-19 ICP 备案等），不阻塞本轮渲染层改动，与历史批次口径一致。
> 与既有方案关系：**不推翻任何物理契约**。本轮只改「引擎结果 → 球体节点姿态」的渲染消费口径，不动引擎、不动轨迹、不动落袋判定。与 ADR-P10-04 Layer C ②「现象5 加塞旋转真实化」**是两件事**（那条是物理层 drop/jaw 标定，见 D-v17-5 划界）。

---

## 〇、问题陈述（事实 / 推测分开）

### 核心诊断

**用户看到的现象是真的，且根因单一：物理层完整算出了角速度 ω 并写入了每一帧，但渲染层从未消费它。** 球体节点的朝向由「本帧走了多远」反推纯滚动近似驱动，旋转轴被写死为「垂直于运动方向的水平轴」。加塞对应的竖轴分量 ω_y 在这条通路上不存在——顶视相机下它恰恰是最容易看清的那种转动（球像陀螺一样原地打转），却是唯一被完全丢弃的分量。

### 事实（已读代码逐处核实，2026-07-25）

**数据流断点**

```
spinX/spinY（BTSpinPad）
  → CueBallStrike.executeStrike        ✅ 产出 (velocity, angularVelocity, squirtAngle)
  → EventDrivenEngine + AnalyticalMotion ✅ 逐段演进 ω（sliding/rolling/spinning 三态齐全）
  → TrajectoryRecorder.BallFrame        ✅ 每帧存了 angularVelocity: SCNVector4
  → TrajectoryPlayback.stateAt          ⚠️ ω 只用来求 position/velocity，不透出
  → PlaybackBallState                   ❌ struct 里根本没有 angularVelocity 字段
  → TrajectoryPlayback.action           ❌ 姿态 = 位移弧长 / R，轴 = ŷ × v̂
```

- `PlaybackBallState`（`TrajectoryPlayback.swift` L12–20）：`accumulatedRotation` 注释写「用于视觉旋转」，实际口径是 `Σ 位移 / R`（L184–201 前缀和、L301–305 段内增量），与 ω 无关。
- 上屏处（L385–396）：`if dRot > 1e-5, s.moveDirection.length() > 0.001` 双门槛 + `axis = ŷ × v̂`。**没有平移就不转**。
- `stateAt` 的 `.spinning` 分支（L290–296）调 `evolveSpinning` 拿到衰减后的 ω_y，随后直接丢弃；该状态 `velocity` 与 `segmentDistance` 均为 0 ⇒ `moveDirection = 0` ⇒ 姿态完全不变。`.stationary` 分支（L245–253）同理返回零方向。
- `SequenceVideoExporter.swift` L428–438 是同一段逻辑的拷贝（注释自称「与 App `TrajectoryPlayback.action` 逻辑一致」），导出视频有同样缺陷。
- **单一收口点**：全仓库 12 处回放调用（9 个 ViewModel/View + 导出器）全部走 `TrajectoryPlayback.action(for:ballName:...)`，无第二条通路。修在这一个方法内即全域生效。
- **球节点可安全原地自转**：`TableModelLoader.extractBallNodes`（L239–261）已把 mesh 包围盒中心偏移到包装节点原点（`anchorNode.position = -centerInAnchor`），故 `node.simdOrientation` 是绕球心自转，不会变成绕偏心点公转。
- **球面有参照物**：`AngleTrainingScene.enhanceBallMaterials`（L256–264）明确保留 USDZ 烘焙 diffuse，注释注明白球带「红色打点贴纸（intentional spin / aim references）」；`MaterialFactory.applyBallMaterial` 的 `diffuseOverride` 参数**无任何调用方传值**，即当前所有球都保留原贴图。
- **纯色 fallback 球只在 USDZ 缺失时出现**：`AngleTrainingScene.applyBallLayout` L179–185 / L194–203，正常路径（模型缓存 + 启动预热已落地，2026-07-20）不触发。
- 姿态从不复位：`DrillSceneView.resetBalls`（L208–220）与 `PositionPlayViewModel.applyBoard`（L1328）等复位路径只写 `position`，不碰 `simdOrientation`。
- 死代码（可一并清理）：`SceneKitBridge.swift` 全仓库零调用方；它是 `TrajectoryRecorder.action`（L78–94，旧版位移滚动实现）的唯一调用方。
- 现有测试基线：`QiuJiTests/TrajectoryPlaybackSettleTests.swift`（G15 尾段不截断）已提供手工构造 `TrajectoryRecorder` 的范式，可直接复用于本轮单测；其 `BallFrame` 构造已带 `angularVelocity` 参数位。
- 相关常量：`BallPhysics.radius = 0.028575 m`；`SpinPhysics.maxSideSpin = 100.0`。

**改成 ω 驱动后必然暴露的新风险（已算数值，非推测）**

纯滚动时 ω = v/R。v = 2 m/s ⇒ ω ≈ 70 rad/s ≈ 11.1 转/秒；60fps 下每帧转 0.19 转。单个可辨识特征（白球红点）的频闪反转阈值是 0.5 转/帧 ⇒ 60fps 对应 v ≈ 5.4 m/s、120fps 对应 v ≈ 10.9 m/s。加塞侧：V0 = 3 m/s、a = 0.5 时 ω_y ≈ 131 rad/s ≈ 20.9 转/秒，60fps 下 0.35 转/帧（未过 Nyquist 但已明显频闪）。**结论：中低速真实、高速段会看到频闪甚至视觉反转。** 这是任何忠实 ω 渲染的固有性质（真实台球高速时人眼同样看不清），需拍板取舍，见 D-v17-3。

### 推测（明确标注，未验证）

- **白球红点在上半球的分布未知**，故「顶视 2D 下 ω_y 自转肉眼可辨」目前是推测。3D 透视下可辨的把握较大。此项必须在 W1 开工第一步做实证验证（D-v17-4），验证不通过则本轮量级上升。
- 用户所说「球的旋转状态」指的是**球面真实自转**，属主控映射，非用户原话定义——也可能指「一个显示旋转的指示 UI」。见 D-v17-1（守 FL-026）。

---

## 一、需求回显（语义基准，不增删）

### 用户原话诉求

| # | 用户原话 | 验收语义 |
|---|---|---|
| **S1** | 「球加塞后效果上会带有旋转，但是现在看起来好像并没有生效（看不到球的旋转状态）」 | 施加左/右塞后，回放中母球呈现可肉眼分辨的竖轴自转；左塞与右塞旋向相反；不加塞时无竖轴自转 |

### 主控诊断派生条目（同根因 / 同一处改动，纳入与否见 D-v17-2）

| # | 条目 | 与 S1 的关系 | 验收语义 |
|---|---|---|---|
| **S2** | `.spinning` 状态（球已停止平移、只剩竖轴自转）画面完全静止 | 同一双门槛条件所致；不修则 S1 在「球停下前的最后一段」仍然看不到塞 | 该状态下位置不变而球面持续自转，直至 ω_y 衰减为零 |
| **S3** | sliding 段转/滑不匹配被一律画成纯滚动：低杆倒旋（球边前进边倒转）、定杆（滑而不转）都画成正滚 | 同一行 `axis = ŷ × v̂` 所致 | 低杆段球面倒转、定杆段球面近乎不转，与引擎 ω 一致 |
| **S4** | 导出教学视频有同样缺陷 | `SequenceVideoExporter` 是同段逻辑拷贝 | App 内与导出视频同一杆旋向一致 |
| **S5** | 回放结束/复位不重置球体姿态，反复播放同一杆姿态累积漂移 | 次生问题；不修则 S1 的截图取证无法复现 | 同一杆连播两次，起始姿态一致 |

**明确不做（本轮，除非拍板扩大）**

| 不做项 | 依据 |
|---|---|
| 物理层 ω 的计算、系数标定、加塞与 jaw 交互真实化 | 属 ADR-P10-04 **Layer C ②**，依赖 D-B1 真实俯拍视频标定（🧑 人工 backlog）。本轮是渲染层，两者不可混谈，见 D-v17-5 |
| 引擎 / 轨迹 / 落袋判定的任何改动 | 本轮只改渲染消费侧；物理测试网（65 方法 / ~3800 场景）必须零回归 |
| 2D 矢量球加旋转动画（`BTRealisticBall` / `PoolBallFace` / `BTTableFigure`） | 这些是静态示意图元，不承载回放语义 |
| 球面贴图 / 材质改动 | 除非 D-v17-4 可见性验证不通过，届时另行拍板 |
| 新增旋转指示器 UI（箭头 / 尾迹 / 数值） | 若 D-v17-1 选方案①则不做；作为教学增强可另立项 |

---

## 二、代码现状与锚点索引（事实，已核实 2026-07-25）

### 2.1 主改动面

| 项 | 锚点 | 现状 / 改动性质 |
|---|---|---|
| 回放状态结构 | `QiuJi/Core/Physics/TrajectoryPlayback.swift` L12–20 `PlaybackBallState` | 需新增 `angularVelocity` 字段 |
| 状态求值 | 同文件 L204–323 `stateAt` | `evolved.angularVelocity`（L273、L296、L298）已算出，只需透出 |
| 姿态上屏 | 同文件 L358–397 `action` 的 `customAction` 闭包 | 核心改动点：L388–396 换成 ω 逐帧四元数积分 |
| 跨帧游标 | 同文件 L410–416 `PlaybackCursor` | 需增 `lastSimTime`（积分需 Δt）；`lastRotation` 随旧口径退役 |
| 旧口径前缀和 | 同文件 L30–31 `rotationPrefixSums`、L183–201 `precomputeRotationPrefixSums`、L240/L250/L302 引用点 | ω 驱动落地后成死代码，同批清理 |
| 导出器同源段 | `QiuJi/Core/Media/SequenceVideoExporter.swift` L388–389（`lastRot`）、L428–438（上屏） | W2：改为与 W1 同口径 |

### 2.2 姿态复位点（S5）

| 项 | 锚点 | 现状 |
|---|---|---|
| Drill 详情复位 | `QiuJi/Core/Scene/DrillSceneView.swift` L208–220 `resetBalls` | 只重置 position / opacity / removeAllActions |
| 走位编排台复位 | `QiuJi/Features/PositionPlay/ViewModels/PositionPlayViewModel.swift` L1328 `applyBoard` | 同上 |
| 翻袋 / 钻石复位 | `BankShotViewModel.swift` L504 `applyBoard`、`DiamondSystemViewModel.swift` L483 `applyBoard` | 同上 |
| 场景摆球（共享） | `QiuJi/Core/Scene/AngleTrainingScene.swift` L161–204 `applyBallLayout`、L225–234 `showBall` | 共享入口，是「统一复位姿态」的候选收口点（优先在此收口，避免逐页散落） |

### 2.3 回放调用方全集（改动波及面，均无需逐个改，仅回归验证）

`DrillSceneView` L131–139 ｜ `PositionPlayViewModel` L956/L1214/L1495 ｜ `SiluTrainerViewModel` L901/L952 ｜ `PlanThreeViewModel` L1226/L1275 ｜ `SnookerTacticsViewModel` L770/L818 ｜ `BankShotViewModel` L438/L731 ｜ `DiamondSystemViewModel` L417/L704 ｜ `AimPointSceneTrainingView` L220–228 ｜ `BreakFlowRunner` L272–276 ｜ `SequenceVideoExporter` L366

### 2.4 死代码（W2 清理）

| 项 | 锚点 | 证据 |
|---|---|---|
| `SceneKitBridge` | `QiuJi/Core/Physics/SceneKitBridge.swift` 全文 | 全仓库 rg 仅自身文件 + pbxproj + 文档提及，零调用方 |
| 旧回放动作 | `QiuJi/Core/Physics/TrajectoryRecorder.swift` L78–94 `action(for:...)` | 唯一调用方是上面这个死类 |

### 2.5 可复用件

| 项 | 锚点 | 用途 |
|---|---|---|
| 手工 recorder 构造范式 | `QiuJiTests/TrajectoryPlaybackSettleTests.swift` L27–55 | W1 单测直接复用（确定性、不依赖物理系数） |
| 物理常量 | `BTPhysicsConstants.swift`：`BallPhysics.radius` L26、`SpinPhysics.maxSideSpin` L116 | 单测口径与频闪估算 |
| 加塞入口 | `CueBallStrike.executeStrike` L111–133（含 pooltool z-up → SceneKit y-up 换轴） | 旋向符号正确性的溯源点；截图验证左右塞反向即验此处 |

### 2.6 待定位（写明，不臆测）

| 项 | 说明 |
|---|---|
| 白球 USDZ 贴图上半球特征分布 | 无法静态读出，须渲染实证（D-v17-4 前置验证） |
| 姿态复位的最佳收口层 | 倾向 `AngleTrainingScene.applyBallLayout` / `showBall`，但需确认是否所有复位路径都经过它们（`DrillSceneView.resetBalls` 直接操作 `cueBallNode` 未走共享入口）；W1 开工时读码定位 |

---

## 三、拍板项

> 全部拍板完成（2026-07-25，用户当场裁决）。

| # | 问题 | 裁决 | 状态 |
|---|---|---|---|
| **D-v17-1** | **术语映射确认（守 FL-026）**：「看不到球的旋转状态」= ①球面真实自转？②旋转指示 UI？③两者？ | **①球面真实自转**（用户确认）——球体按物理 ω 转，靠白球红点/号码辨识。旋转指示器 UI 本轮**不做** | ✅ |
| **D-v17-2** | 范围：S2/S3/S5 是否纳入本轮？ | **全纳**（用户确认）。S2/S3 与 S1 同一行代码所致；S5 不修则截图取证无法稳定复现 | ✅ |
| **D-v17-3** | 高速段频闪取舍（数值见 §〇：60fps 下 v>5.4 m/s 会视觉反转） | **不做视觉缩放，如实渲染**（用户确认）。禁止给 ω 乘视觉系数；需要看清用既有 `action(speed:)` 慢放倍率 | ✅ |
| **D-v17-4** | 顶视 2D 可见性前置验证：白球上半球是否有足以辨识 ω_y 自转的特征？ | 按推荐执行：**W1 第一步先出实证截图**（纯 ω_y 姿态序列，顶视 2D + 3D 各一组，落 `build/v17-evidence/`），可辨则继续；**不可辨则停批回报**，是否追加「教学用参照标记」（材质/贴图改动，量级 M→L）另行拍板，不得在批内擅自扩范围 | ✅ |
| **D-v17-5** | 与 ADR-P10-04 **Layer C ②**「drop/jaw 标定 + 现象5 加塞旋转真实化」的划界 | 按推荐执行：**两件事，本轮不碰 Layer C**。Layer C ② 是物理层（加塞如何改变目标球撞 jaw 后走向，依赖 D-B1 真实俯拍视频标定，🧑 人工 backlog）；本轮是渲染层。本轮完成后 Layer C ② 仍未做 | ✅ |
| **D-v17-6** | 执行模型（per `.cursor/rules/01-subagent-model-selection.mdc`） | **不派执行子智能体，主控亲做**（用户裁决）。W1/W2 均由主控在主会话执行，验收标准不降级——完成标准仍需逐条附真实 build/test/截图产物 | ✅ |

---

## 四、批次表

依赖图：

```
W1（回放层 ω 驱动 + 姿态复位 + 单测 + 截图取证）──> W2（导出器同源 + 死代码清理 + 出片验证）
```

严格串行：W2 的导出器改动必须复用 W1 落定的 ω 积分口径，口径未定不得开工。

| 批次 | 范围 | 量级 | 依赖 | 执行 |
|---|---|---|---|---|
| **W1** ✅ | S1+S2+S3+S5：①D-v17-4 前置可见性验证；②`PlaybackBallState` 透出 `angularVelocity`；③`action` 闭包改逐帧四元数积分（`Δq = quat(angle: \|ω\|·Δt, axis: ω̂)`，`node.simdOrientation = Δq * node.simdOrientation`），去掉「有位移才转」双门槛；④`PlaybackCursor` 增 `lastSimTime`；⑤回放起点 / 复位路径统一重置姿态（优先收口到 `AngleTrainingScene` 共享入口）；⑥清理同文件旧口径死代码（`rotationPrefixSums` 一族）；⑦单测 + 截图取证 | M | D-v17-1/2/3/4 ✅ | 主控亲做（2026-07-25 完成） |
| **W2** ✅ | S4 + 清理：①~~`SequenceVideoExporter` L428–438 改用 W1 同口径~~（**已随 W1 落地**，见 §五 v17.2 偏差记录）；②删 `SceneKitBridge.swift` + `TrajectoryRecorder.action`，`make xcodegen` 重生工程；③离线出片验证 App 内与视频旋向一致 | S | W1（口径）✅ | 主控亲做 |

**波次建议**：不并行。两批文件域相邻且 W2 完全依赖 W1 口径，串行成本低于合并后在验证阶段（模拟器截图 + 离线出片两套链路）爆上下文的风险。

### W1 完成标准

1. **前置可见性实证（D-v17-4）**：顶视 2D + 3D 透视各一组「纯 ω_y 自转」渲染序列截图，落 `build/v17-evidence/`，肉眼可辨则在批内说明「已确认可辨」；不可辨则**停批回报**，不得自行扩范围改材质。
2. **纯滚动不回归（关键锚）**：单测断言在 `.rolling` 恒定纯滚动段，新 ω 积分产出的姿态角度与旧「位移弧长 / R」口径在同一时间窗内一致（相对误差 < 1%）——证明本轮不是把已经对的东西改坏。
3. **ω 积分口径单测**：给定恒定 ω 与 dt，姿态增量角 = |ω|·Δt（误差 < 1e-4 rad）、轴 = ω̂；`.spinning` 段位置不变而姿态持续变化且随 ω_y 衰减而变慢；`.stationary` 段姿态不变。
4. **旋向符号实证（防坐标系搞反）**：左塞 / 右塞 / 低杆各一组回放截图序列（3D 透视 + 顶视 2D），左塞与右塞竖轴旋向**相反**、低杆段球面**倒转**；截图落 `build/v17-evidence/`。批内须按 `geometry-spatial-reasoning` 回显坐标契约（SceneKit y-up 与 pooltool z-up 的换轴，交叉核对 `CueBallStrike.executeStrike` 与 `.kiro/steering/table-geometry.md`），禁止脑算断言旋向。
5. **姿态复位（S5）**：同一杆连续回放两次，第二次起始 `simdOrientation` 与第一次一致（单测断言四元数分量，容差 1e-5）。
6. **零回归**：`make build` BUILD SUCCEEDED；`QiuJiTests` 全量 0 failures（**`TrajectoryPlaybackSettleTests` 必须仍绿**）；物理测试网（`PhysicsInvariant/Matrix/Scenario/Benchmark`）0 failures——本轮不应触碰物理，任何物理测试变化都要当作误改追查。
7. **死代码清零**：`rg 'rotationPrefixSums|precomputeRotationPrefixSums'` 无残留。

### W2 完成标准

1. **同源一致性**：取一段含明显加塞的走位序列，`make position-export` 出片；同一杆的 App 内截图与视频帧对照，旋向一致、无「App 内转、视频不转」；产物落 `build/v17-evidence/`。
2. **死代码删除**：`rg 'SceneKitBridge|recorder\.action'` 仅剩历史文档提及（`tasks/PROGRESS.md` / `PHYSICS-DEBT.md` 记录不改）；`make xcodegen` 后 pbxproj 无残留引用。
3. **零回归**：`make build` BUILD SUCCEEDED；`QiuJiTests` 全量 0 failures。

### 收尾（W2 后，主控执行）

- `tasks/PROGRESS.md` 记录本轮结论与产物路径；`/Users/song/projects/project-hub/projects/13.billiard_trainer.md`「最近完成 / 下一步」同步。
- 命中「跨模块边界 / 渲染契约变更」时在 `tasks/phases/` 对应卡追加 ADR（回放姿态口径由位移反推改为 ω 积分，属显示契约变更，建议记 ADR）。
- 若过程中出现返工，按 `00-orchestrator.mdc` 追加 `FL-NNN` 并回写规则。

---

## 五、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v17.3 | 2026-07-25 | **W2 收官 ✅（主控亲做）＝本轮全部完成**。①**死代码清零**：删 `Core/Physics/SceneKitBridge.swift` + `TrajectoryRecorder.action`，`make xcodegen` 重生工程，`rg 'SceneKitBridge\|recorder\.action'` 仅剩历史文档与本文件提及、pbxproj 零残留。②**离线出片验证**：取加塞最强单杆 `drill_c019 右塞一库变线·球形6`（spinX=−0.405、v=3.3），清 `.resume` 强制重出（否则命中续跑跳过、验的是改动前旧产物），`s01.mp4`/`s01_3d.mp4` 正常产出。③**同源一致性实证**（新增 `QiuJiTests/SpinExportParityTests` 2 例）：<br>　• **真实 App 路径 vs 导出器口径**——把线上 `TrajectoryPlayback.action` 挂到节点上、由 `SCNRenderer.snapshot(atTime:)` 推进场景时钟逐帧求值（跑的是闭包本体，不是测试手抄的等价实现），与导出器固定步长循环比对 121 帧，**最大姿态偏差 6.91e-04 rad**；<br>　• **视频帧 vs App 帧同相位对照**——App 侧用导出同一套取景（`fitRotatedTable`+`applyTopDown2DRotated`，1440×2560），以视频实测球心**反解**对应模拟时刻（最大位置残差 **2.5 px**、相邻帧严格 1/60 s 递进 ⇒ 两条管线摆位同源），再出同相位裁剪图；`ffmpeg` 抽 `s01.mp4` n250–261（撞球后慢速段）与之逐帧比对，红点相对球心偏移逐帧大幅变化（dx −5→−45 px ⇒ **视频里球确实在转**）、两侧每帧差 **≤1.9 px**（6× 放大图上，等价原始尺度 <0.4 px）。产物 `build/v17-evidence/w2-export-parity/`（`compare_video_vs_app.png` 为并排对照图）。<br>**零回归**：`make build` BUILD SUCCEEDED；`QiuJiTests` 全量 **672 tests / 0 failures**（2 skipped，日志 `build/v17-evidence/logs/w2-full-unit-tests.log`）。**ADR**：`tasks/phases/P11-position-play-composer.md` § **ADR-P11-16**（显示契约由位移反推改为 ω 积分）。<br>**证据链口径（诚实标注）**：视频 ⟵ 导出器二进制产物；导出器循环 ≡ 测试内同口径循环（像素级 ≤1.9px）；该循环 ≡ 真实 `action` 闭包（≤6.9e-4 rad）——三段相接构成「App 内与视频同源」。<br>**遗留（不在本轮范围）**：存量出片资产除 drill_c019 外的 125 条仍是旧口径渲染，需批量重出。 |
| v17.0 | 2026-07-25 | 立项：用户报「加塞看不到旋转」→ 主控读码确证根因（ω 算了但渲染层从不消费，姿态由位移弧长反推、轴写死 ŷ × v̂）；条目 S1（用户原话）+ S2–S5（诊断派生）；锚点全量读码核实（含球节点已中心化、白球保留贴图、12 处调用方单一收口、两处死代码）；频闪风险已算数值；W1–W2 批次表与完成标准；D-v17-1～6 待拍板 |
| v17.2 | 2026-07-25 | **W1 收官 ✅（主控亲做）**。落地：`PlaybackBallState.accumulatedRotation/moveDirection` 口径 → 新增 `angularVelocity`（世界系 rad/s）；新增 `BallSpinIntegrator`（`delta` = `quat(\|ω\|·Δt, ω̂)`、`advance` 取前后帧 ω **梯形均值** + 每帧四元数归一化、`resetPose`）；`action` 闭包改按**模拟时间** Δt 积分并删掉「有位移才转」双门槛；`PlaybackCursor` 换 `lastSimTime`/`lastAngularVelocity`；姿态复位收口 `AngleTrainingScene.showBall`/`applyBallLayout` + `DrillSceneView.resetBalls` + Bank/Diamond `restoreBall`；删 `rotationPrefixSums` 一族（rg 清零）。<br>**D-v17-4 闸门判定：可辨 → 继续**。证据 `build/v17-evidence/dv174-visibility/`：3D 斜视明确可辨；顶视（含 App 真实 2D 尺寸，球 ≈14pt）在红点离极点时明确可辨（`tilt45_*`），红点恰好朝正上方时纯 ω_y 近乎不可辨（麻点球贴图固有，非渲染缺陷，且一杆中途姿态持续翻滚）。未改材质、未加指示器。<br>**验收**：`make build` BUILD SUCCEEDED；`QiuJiTests` 全量 **670 tests / 0 failures**（2 skipped，日志 `build/v17-evidence/logs/w1-full-unit-tests.log`），含物理网 `PhysicsInvariant/Matrix/Scenario/Benchmark/Engine` 与 `TrajectoryPlaybackSettleTests` 全绿；新增单测 `TrajectoryPlaybackSpinTests` 8/0（纯滚动新旧口径偏差 <1%、\|ω\|·Δt 与 ω̂、`.spinning` 转且随衰减变慢、`.stationary` 不转、复位后重播姿态逐分量一致 1e-5）；旋向实证 `build/v17-evidence/spin-sign/`（左塞 ω_y=−76.36 / 右塞 +76.36 反号且量级相当、纯低杆 ω_y≈0 而 ω_z>0 倒转、滚动段 ω_z=−v/R±2%），出 1× 与 0.25× 慢放两档（1× 下 80°/帧，静图判方向须用慢放档——即 §〇 预告的频闪，符合 D-v17-3 如实渲染）。<br>**偏差记录（小，未改需求语义）**：`SequenceVideoExporter` 的同源三行（原 W2 ①）随 W1 一并改为 `BallSpinIntegrator` 口径——删除 `accumulatedRotation` 后不改则主工程编译不过；W2 剩余 = 死代码删除 + 离线出片旋向一致性验证。 |
| v17.1 | 2026-07-25 | **D-v17-1～6 全部拍板**：①=球面真实自转（不做指示器 UI）；②=S2/S3/S5 全纳；③=不做视觉缩放、如实渲染（禁止给 ω 乘系数）；④/⑤按推荐（可见性前置验证为停批闸门；不碰 Layer C）；⑥=**不派子智能体，主控亲做**。批次表「模型」列改「执行」列并填入。**状态：可开工 W1** |
