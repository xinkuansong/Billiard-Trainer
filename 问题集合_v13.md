# 问题集合 v13 — 「瞄准方法」学页内容诚实性与可读性收口

> **本文件是本轮需求的真源。** 日期：2026-07-18　状态：⏳ **待拍板后开工**（D-v13-1～4）
> 来源：2026-07-18 会话对「瞄准方法」页的内容审阅 + 用户反馈（试瞄管不可见 / 文字样式丑）的剩余项。
> 编号约定：条目 **A1–A10**；批次 **B1–B2**；拍板项 **D-v13-N**。
> 与 v11 Y1（页已建）、v12（瞄准修正）**无业务文件冲突**；本轮只动瞄准方法页及其几何/测试。

---

## 〇、已收基线（本轮**不做**，避免返工）

| 项 | 状态 | 证据 |
|---|---|---|
| 试瞄管道绿-on-绿不可见 | ✅ 已修 | 试瞄管改 `FigureLine.aim`；UI 测 `testAimingMethodsInteractions` 绿；截图 `build/aiming-pipe-screenshots/y1r1-pipe-tangent.png` |
| 长段墙文初改 | ✅ 部分已做 | 开篇三行摘要 + 各节 `aimingStep` 编号 + caption 脚注（`AimingMethodsView`） |
| 加塞交叉引用过期（指向「旋转与加塞」） | ✅ 已改 | 管道脚注 →「瞄准修正」；相关页已加 `PracticeCTA` → `.aimingCorrection` |

> 本轮真源**不重做**上述三项；若验收时发现回归再开缺陷条。

---

## 一、需求回显（语义基准，不增删）

来自审阅结论与用户原话，合并去重后的**剩余诉求**：

| # | 诉求 | 验收语义 |
|---|---|---|
| A1 | **θ 契约诚实**：页顶文案写「三节主图同步变化」，但厚薄图、Mosconi 变体写死 30°；调 θ 时用户以为坏了 | 凡宣称跟 θ 的图必须真跟；若不跟须显式标注「示意固定半球」且改掉过度承诺文案 |
| A2 | **厚薄节图与表对齐**：图只画半球，表有 3/4·半球·1/4 | 图能表达当前厚度（跟 θ 或可切换三档）；表行对应当前态有高亮 |
| A3 | **Mosconi 变体跟 θ** | 变体图随页顶 θ 变，不再锁 `halfBall` |
| A4 | **试瞄角 φ 与 θ 协同**：改 θ 后 φ 仍停在旧值，易出现「默认不相切」困惑 | 改 θ 时 φ 跟随到新 θ（或吸附区内自动对齐），默认进页仍相切 |
| A5 | **相关页补环**：已有瞄准原理 / 对照表 / 瞄准修正；缺「旋转与加塞」 | 相关页增加 CTA → `.spinAndEnglish` |
| A6 | **符号可扫读**：θ/φ/Q/Pt/Pc/G/C 同页出场无图例 | 开篇或首节前有一行符号图例（或词条行），与 §8.8 词表一致 |
| A7 | **接触点节误导角更醒目**：未播放时灰线与真瞄准线夹角小，难看出「心对点」害处 | 默认可见误差（终帧或常显误差角徽章）；播放键仍可重播 |
| A8 | **平行线节补几何标注**：文称点对称 / Q，图上未标 Q | 主图标出 Q（或对称提示），与正文一致 |
| A9 | **推演去重**：三节 caption 仍各复述一次「与 d=2R·sinθ 等价」 | 等价说明收拢到开篇或页末一处；各节只留操作步骤 |
| A10 | **可读性再收一刀**（用户：「文字样式感觉也很丑」） | 在已有 step 形态上：统一 caption/body 层级、去掉重复公式句、徽章/脚注不挤字；**不引入新字体/新装饰风格**（守设计系统） |

**明确不做（本轮）**

- 不重做管道管色 / 不重写三法几何定义（v11.2 口径冻结）。
- 不做粘性顶栏 θ（除非 D-v13-3 选做）；默认用节内 θ 读数解决「滑杆滚出视野」。
- 不改「瞄准原理 / 瞄准修正 / 旋转与加塞」他页结构（只加本页 CTA）。
- 不新增第四种瞄准法。

---

## 二、代码现状（事实，已核实 2026-07-18）

### 2.1 页面与几何

| 事实 | 锚点 |
|---|---|
| 单页 `AimingMethodsView`（~701 行）+ 私有 Figure/Section | `QiuJi/Features/AngleTraining/Views/AimingMethodsView.swift` |
| 共享 θ `@State cutAngleDeg`，传给管道/接触点/平行线三节 | L14, L20–22 |
| 厚薄节 `OverlapAimingFigure()` **无参数**，内部 `thickness = halfBall` 写死 | L97, L634–635 |
| Mosconi `MosconiVariantFigure()` **无参数**，`scene(cutAngleDeg: halfBall)` 写死 | L506, L579–582 |
| 开篇 caption：「拖动切角，**三节主图**同步变化」——未覆盖厚薄/Mosconi | L53–55 |
| 管道 `trialAngleDeg` 本地 `@State`，**不**随父级 `cutAngleDeg` 重置 | L170–176 |
| 相关页 CTA：原理 / 对照表 / 瞄准修正；**无**旋转与加塞 | L144–161 |
| 几何真源 `AimingMethodsGeometry`（恒等式/管道/碰合） | `AimingMethodsGeometry.swift`（173 行） |
| 厚薄数值真源 `NamedBallThickness`（overlap ↔ θ ↔ d/R） | `AngleSceneCalculator.swift:1149–1201`；已有 `namedBallThickness(cutAngleDegrees:tolerance:)` |
| 单测 `AimingMethodsGeometryTests`（5 例不变量） | `QiuJiTests/AimingMethodsGeometryTests.swift` |
| UI：巡游 `a13-aiming-methods` + 交互 `testAimingMethodsInteractions` | `ScreenshotTourUITests.swift:177, 212–218, 308–355` |
| 线语言：试瞄管必须用 `FigureLine.aim`（绿禁令已写注释） | `AimingMethodsView.swift:251–252` |

### 2.2 改动波及面（估）

- **几乎全集中在两文件**：`AimingMethodsView.swift` + `AimingMethodsGeometry.swift`（若抽 overlap 辅助函数）。
- 测试：扩 `AimingMethodsGeometryTests`（θ↔overlap 派生若上提几何层）+ UI 交互取证增帧（厚薄/Mosconi 跟 θ）。
- 注册/封面/路由：**零改动**（页已存在）。

---

## 三、拍板项（开工前需确认；带推荐）

| # | 问题 | 推荐 | 状态 |
|---|---|---|---|
| D-v13-1 | 厚薄节交互形态 | **连续跟页顶 θ**：重合示意用 `overlap = 1 − sin(θ°)`（与 `NamedBallThickness` 经典定义一致）；表三行高亮「当前 θ 最近命名厚度」（`namedBallThickness(cutAngleDegrees:tolerance: 2.5)`）。不做独立三档 Picker（避免与页顶 θ 双真源） | ⏳ 待拍 |
| D-v13-2 | 接触点节默认态 | **进入即 `mergeProgress = 1`（碰合终帧）**，按钮文案「重播碰合过程」；误差角用徽章常显（不依赖播放） | ⏳ 待拍 |
| D-v13-3 | θ 滚出视野 | **节内小读数**（管道/平行线/厚薄卡头显示「当前 θ = N°」），**不做** sticky 顶栏（改动面大、收益有限） | ⏳ 待拍 |
| D-v13-4 | 执行模型 | 两批均 `cursor-grok-4.5-high-fast` + 主控逐文件读 diff；几何批强制 `geometry-spatial-reasoning` 数值草稿 | ⏳ 待拍 |

---

## 四、批次表

两批、同文件簇、强相关 ⇒ **全串行**（B2 消费 B1 的 θ 契约与图 API）。量级均在单会话可验收。

```
B1（θ 契约诚实）──► B2（教学标注 + 文案收口 + 测）
```

| 批次 | 范围 | 量级 | 依赖 | 模型 |
|---|---|---|---|---|
| **B1** | A1–A5：厚薄图跟 θ + 表行高亮；Mosconi 接 θ；φ 随 θ 对齐；文案去掉过度承诺；相关页 + 旋转与加塞 CTA；几何辅助（若需）+ 单测钉子 | M | — | `cursor-grok-4.5-high-fast`（D-v13-4） |
| **B2** | A6–A10：符号图例；接触点默认终帧+误差徽章；平行线标 Q/对称；推演去重与 caption 收口；节内 θ 读数（D-v13-3）；巡游/交互 UI 测扩帧；必要时 SPEC §8.8 补「隧道」括注 | M | B1 | 同上 |

### B1 完成标准

1. 拖页顶 θ 至非 30°（如 45°）：厚薄图重合比例与脚注角度同步变化；Mosconi 图随 θ 变；管道默认仍相切（φ 已跟到新 θ）。
2. 厚薄表三行中，当前 θ 最近命名厚度行有可见高亮（或等价指示）。
3. 开篇不再写「只有三节同步」却隐含厚薄也变——文案与真实行为一致（或明确列出跟 θ 的图清单）。
4. 相关页可见「旋转与加塞」CTA，导航可达。
5. `make build` SUCCEEDED；`AimingMethodsGeometryTests` 全绿；若新增 overlap 派生函数须有单测 + 数值草稿（`build/v13-evidence/`，geometry-spatial-reasoning）。
6. 明/暗至少各 1 帧厚薄非 30° 截图目视（或复用交互测 attachment，拷到 `build/v13-screenshots/`）。

### B2 完成标准

1. 开篇或首卡可见符号图例（θ/φ/Q/Pt/Pc/G 等本页用到的子集）。
2. 进入接触点节无需先点播放即可看出误导关系（终帧或误差角徽章）。
3. 平行线主图可见 Q（或等价对称标注），与正文「点对称」不矛盾。
4. 各节不再各写一整段「殊途同归 d=2R·sinθ」；等价说明 ≤ 1 处集中。
5. `testAimingMethodsInteractions` 仍绿；巡游 a13 或交互测增厚薄/Mosconi 跟 θ 取证帧；`make build` SUCCEEDED。
6. 主控逐文件读 diff：无越界改瞄准修正/旋转页；试瞄管色未回退为 `btPrimary`。

---

## 五、锚点索引（读代码核实，2026-07-18）

| 锚点 | 位置 | 用途 |
|---|---|---|
| 页骨架 / θ 状态 | `AimingMethodsView.swift:12–33, 37–75` | A1 文案、节传参 |
| 厚薄节 + 表 | `AimingMethodsView.swift:91–140` | A1/A2 |
| `OverlapAimingFigure` | `AimingMethodsView.swift:634–665` | A2：现写死 halfBall |
| `MosconiVariantFigure` | `AimingMethodsView.swift:579–632` | A3：现写死 30° |
| 管道 φ 状态 | `AimingMethodsView.swift:167–176, 204–206` | A4 |
| 相关页 CTA | `AimingMethodsView.swift:144–161` | A5；`.spinAndEnglish` 见 `AngleHomeView.swift:13` |
| 接触点节 / 播放 | `AimingMethodsView.swift:340–392` | A7 |
| 平行线主图 | `AimingMethodsView.swift:476–517` + `ParallelContactFigure` L522+ | A8 |
| `aimingStep` 助手 | `AimingMethodsView.swift:671–683` | A9/A10 文案形态 |
| 几何真源 | `AimingMethodsGeometry.swift` 全文件 | B1 可增 overlap 辅助 |
| 命名厚度 API | `AngleSceneCalculator.swift:1149–1201` | A2 高亮真源 |
| 线语言白管禁令 | `AimingMethodsView.swift:251–252`；`BTTableFigure.swift` `FigureLine` | 回归红线 |
| 单测 | `AimingMethodsGeometryTests.swift` | B1 钉子 |
| UI 测 | `ScreenshotTourUITests.swift:177, 212–218, 308–355` | B2 扩帧 |
| 词表 | `tasks/UI-IMPLEMENTATION-SPEC.md` §8.8（管道/试瞄角 φ 已有） | A6/隧道括注 |

---

## 六、依赖与排期

- **前置**：无代码前置；**拍板 D-v13-1～4** 后开 B1。
- **不与**未完成的他方案抢注册文件（本页已注册）。
- 建议执行：`plan-delegated-execution` 串行 B1→B2；或主控亲做（文件面小）。

---

## 七、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v13.0 | 2026-07-18 | 初版：从「瞄准方法」审阅剩余项 + 用户管色/文案反馈重组；已修三项列入 §〇 基线；批次 B1–B2；拍板 D-v13-1～4 待确认 |
