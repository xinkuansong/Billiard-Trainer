# 问题集合 v21 — 加塞训练线补齐（动作库）

> **真源声明**：本文件是「动作库加塞训练线」这一轮需求的**真源**，供 `plan-batch-execution` / `plan-delegated-execution` 执行。
> **日期**：2026-07-28 **版本**：v21.8 **状态**：拍板齐 ✅；**W1–W5 全部 ✅**（各 0 返工）；计数 **77**（退役 c019）；**v21 收官**。
> **排期前置**：H-21（`drill_c053` 8 球形示范录制）仍 ⏳；录制策略已拍（D-v21-6）= 内容先行 initial-only，示范异步补。
> **模型（D-v21-1 用户拍板）**：全批次统一 `cursor-grok-4.5-high-fast` + 主控独立验收（用户指令覆盖原「W1 用强推理」推荐）。

> **[v22 修订指针 · 2026-07-28 · 不改写本文件已完成历史]**：`问题集合_v22` **D-v22-2=A** 修订本轮 W5「综合计划写完整加塞周次」策略为「**轻量钩子已落 + 主路径加塞专项 `plan_english`**」。**不回滚**本文件 W5 已落地的 `plan_cueball` W6 / `plan_intermediate` W1 轻量接入；后续旧计划最多再加 description 导流句。详见 `问题集合_v22.md` §2.4。

---

## 一、需求回显（用户原话与拍板结论，语义基准）

1. **原始诉求（用户原话）**：「我们现在的动作库里缺少了加塞的练习」。
2. **语义澄清（用户第二轮原话，⛔ 关键，不得再偏）**：「**练习如何加塞，相当于加塞后如何还能让母球击打到目标球正确的进球点**」。
   - 即：本轮主题 = **带塞仍能打准（挤偏/让点补偿）**；
   - ⛔ **不是**「塞如何改变碰库出射角 / 加塞走位变线」——那条线属审计 R4 与学区「加塞吃库图谱」，本轮只在 W4 顺带按新口径重构，不作为主题。
3. **形态选择（用户拍板）**：优先 **A = 动作库跟打 drill**（非「交互式带塞试瞄课」）。交互课形态留待后续议题。
4. **规模（用户拍板）**：「多增加几个 drill，参考基础训练和准度训练中的规划」→ 采纳建议 **新增 6 条**。
5. **分类归属（用户采纳建议）**：**拆开放**——基础加塞 3 条进 `cueAction`，加塞准度 3 条进 `accuracy`；不新开第 9 分类。
6. **与 R4 关系（用户采纳建议）**：**合并为一条加塞总线**——新增 6 条与 R4（`c016`/`c018`–`c021` 重构）在同一轮方案内完成，避免 `cueAction` 同时躺新旧两套口径。

### 采纳的设计判断（由主控提出、用户「按你的建议」概括拍板）

- **左右塞不拆成两条 drill**：`side` 是 drill 内的目标变量两档（`c053` 范式），不是两个 drill。2026-03 分类表把「左塞基础／右塞基础」列成两项属体系层粗粒度，`c018`/`c019` 的镜像对正是该做法的产物且已被审计判重构。
- **基础加塞用直球（切角 0°）**：脱靶只剩挤偏一个来源，是隔离校准的正确设计。
- **准度条的真变量是「塞方向相对切向同侧/异侧」**（顺塞/反塞），不是绝对左右——它决定投掷让球变厚还是变薄。

---

## 二、代码现状关键事实（2026-07-28 读码核实，全部附锚点）

### 2.1 现库结构与规划粒度

- 全库 **72 条 / 8 分类**：`fundamentals` 8、**`accuracy` 12**、**`cueAction` 10**、`separation` 8、`positioning` 10、`forceControl` 8、`specialShots` 8、`combined` 8（`QiuJi/Resources/Drills/index.json`）。
- **`accuracy` 的规划体例**（本轮准度 3 条对标）：按「球形几何 + 距离」切子类，一主题多条、每条一子类——`straight`×3（c001/c011/c012）、`angle`×3（c002/c013/c032）、`longAngle`、`threePoint`、`sidePocket`、`sidePocketLong`、`thinCut`、`extremeLong`。
- **`cueAction` 的规划体例**（本轮基础 3 条对标）：按杆法切，基础杆法「基础版 + 远台版」成对——`follow`×2、`draw`×2、`stun`×2、`sideSpin`×2、`followSpin`、`drawSpin`。
- **`cueAction` 现有 10 条全部被审计判「重构」**（审计 §三 小结：P1×6 = c003/c004/c014/c015/c016/c017；P2×4 = c018/c019/c020/c021）。在其上直接堆新条目会造成新旧口径混杂 → 故 D-v21-3 合并 R4。

### 2.2 本轮主题在现库的承载 = 0（缺口坐实）

- `drill_c018` 的 `coachingPoints[2]` 写了「注意让点：加塞时瞄准点需略向右补偿」（`QiuJi/Resources/Drills/cueAction/drill_c018.json:17`），但 `standardCriteria` 是「10球中5球进袋**且母球碰库后走到左半台**」（`:19`）——判定混入走位，练不出让点。
- 全库无任何以「带塞进袋率」为唯一判定的条目 `[事实：8 分类子类清单逐条核对]`。
- 2026-03 体系表 §3.2「横向旋转（加塞）」五项中，「加塞让点补偿（中级→高级）」在现库**无对应承载**（`docs/research/20260323-训练内容体系-动作库分类.md:67-75`）。

### 2.3 ⚠️ 新发现 A：现有加塞 drill 是「计划孤儿」

在 `QiuJi/Resources/Plans/` 全目录 grep `drill_c016|drill_c018|drill_c019|drill_c020|drill_c021` → **0 命中** `[事实]`。

即 6 套计划（`plan_beginner` / `plan_intermediate` / `plan_advanced` / `plan_cueball` / `plan_positioning` / `plan_fullskill`）**没有任何一套引用过加塞或斯登 drill**；`plan_cueball`（"基础杆法专项"）的周次全部由 `c001`–`c013` 一带构成。

**推论**：即使新增 6 条，若不同步接入计划，用户「没有加塞课」的感知不会消失——新条目同样成为孤儿。故 W5 把计划接入列为**必做项而非可选项**。

### 2.4 ⚠️ 新发现 B：profile schema 没有 spin 字段，且 initial-only 序列丢失塞信息

- sidecar profile 的 `FormationProfile` 字段为 `id` / `difficultyRank` / `cue` / `target` / `pocket` / `cutAngleDeg?` / `variables` / `representative?` / `sequenceToken?`（`QiuJi/Resources/Drills/schema.md:203-214`），**无结构化 spin 字段**；`variables` 是 `{String: String}` 自由字典，只能存"档位标签"字符串。
- 试打序列在未录制时为 initial-only：实测 `content/position_play/sequences/drill_c053__A1-中袋角度球 · 球形1-0杆.json` 只有 `initial.onTable` 的球位与 `steps: []`，**不含 spin/velocity** `[事实：全文 19 行已读]`。
- **后果**：`c053` 那类几何主题 drill，摆位本身即承载了训练变量，initial-only 也能练；而**加塞 drill 的目标变量是 spin，摆位看不出来**——不做 schema 扩展或不录示范，用户在试打页拿到的只是一个普通球形，不知道该加多少塞。
- 这是本轮**与 c053 范式的实质差异**，必须在 W1 决策（D-v21-4）。

### 2.5 数值口径（项目内已实证，内容文案红线）

- `CuePhysics.miscueLimitFraction = 0.5`（`QiuJi/Core/Physics/BTPhysicsConstants.swift:233`）；内容→引擎单一入口 `ShotIntent.clampToMiscueLimit`（`QiuJi/Core/Physics/ShotIntent.swift:58-64`，由 `shotInput()` 第 110 行调用）会把 |spin| 钳到 0.5R。⇒ **"满塞"= 0.5R，不是球边缘**。
- v12 Z3 数值补稿已实证：**挤偏与力度无关**；**平杆纯侧旋弧线 ≈ 0**；**投掷越慢越大**（`tasks/PROGRESS.md` Z3 条目）。⇒ 文案禁止写「大力挤偏更大」。
- 抬杆引起的弧线（swerve）属 **v18 高度维度，已暂缓** ⇒ 本轮文案不得提前引入仰角结论。
- 可用真源层：`AimingCorrectionMath.squirtDegrees(spinX:)`（`QiuJi/Features/AngleTraining/AimingCorrectionMath.swift:202-204`，**负值 = 向右**，包装 `CueBallStrike.squirtAngle`）、`throwAngleDegrees(potDir:objPost:)`（`:186`）、`positionAimOffset` 口径 Δ（`:16`）。

### 2.6 ⚠️ 存疑 S1：现有让点方向表述可能与引擎口径矛盾（W1 必须核验）

- `c018` 写「加**左**塞时瞄准点需略向**右**补偿」（`drill_c018.json:17`）。
- 项目 Z1 数值草稿结论是「**左塞挤右**」（母球出射被推向右，`tasks/PROGRESS.md` Z1 条目 ALL_PASS）。
- 二者不必然矛盾（取决于描述的是"瞄准线偏移"还是"后手加塞的杆头摆放"），但**方向性文案是本轮 6 条 drill 的核心教学内容**，不能沿用未经核验的旧文案。
- **强制**：W1 用 `AimingCorrectionMath` 跑数值草稿定死方向与量级，产出 `[事实]` 结论后才允许写入任何 `coachingPoints`。⛔ 禁止脑算断言（`geometry-spatial-reasoning`）。

### 2.7 ⚠️ 存疑 S2：直球加塞的洗袋（scratch）风险

直球定义为母-目-袋共线，母球天然朝袋口方向行进，加塞后仍可能跟进落袋。审计 §十一 存疑 3 已就 c018 系列点过该风险（当时为 `[推测·未经引擎验证]`）。

**强制**：本轮每个球形烘焙时必须实测 `feasible` 且核 `cuePocketed == false`，不达标就改摆位/杆法（如目标球远离袋 + 轻力 + 微低杆压跟进），⛔ 禁止靠手画 animation 掩盖。

### 2.8 会被新增 drill 打断的硬约束

| 约束 | 锚点 | 影响 |
|---|---|---|
| 索引条数写死 72 | `QiuJiTests/DrillContentValidationTests.swift:25-27` | 6 条新增后须改 78 |
| 加载条数写死 72 | 同上 `:50-52` | 同上 |
| 分类数写死 8 | 同上 `:21-23` | 本轮**不新开分类**，此断言不动（若改 D-v21-2 走新分类方案则须动） |
| 分类枚举 8 case + nameZh + icon | `QiuJi/Data/Services/DrillContentService.swift:210-247` | 本轮不动（同上） |
| 索引数组 | `QiuJi/Resources/Drills/index.json` | `cueAction` 10→13、`accuracy` 12→15 |
| 示范录制红线 | `QiuJi/Resources/Drills/schema.md:169`（⛔ 禁止从存量 shotIntent 反推） | 新增 ≈30 + R4 ≈18 球形的示范均需人工录制 |
| 试打同步 | `scripts/Makefile:162-169`（`make tryout-sync`） | 每批落地后须跑 |
| 现存人工欠账 | `tasks/HUMAN-REQUIRED.md:247-258`（H-21 ⏳，c053 8 球形） | 录制积压叠加，见 D-v21-6 |

---

## 三、内容设计骨架（待 W1 用脚本推导坐标，本节只定变量与归属）

### 3.1 新增 6 条（id 顺延 `c073`–`c078`；现库最大 id = `c072`）

| id | 建议名 | 分类 / 子类 | 目标变量（全覆盖轴） | 条件变量（散布） | 判定 | 球形数 |
|---|---|---|---|---|---|---|
| c073 | 挤偏认知·直球近台 | `cueAction` / `sideSpinSquirt` | 塞侧 L/R（塞量固定中档） | 距离（近段）、力度 | 仅目标球进袋 | 4 |
| c074 | 挤偏放大·直球长台 | `cueAction` / `sideSpinSquirtLong` | 塞侧 L/R × 距离档 | 力度 | 仅目标球进袋 | 4 |
| c075 | 塞量阶梯 | `cueAction` / `sideSpinLevels` | 塞量 轻/中/满（满 = 0.5R） | 距离、塞侧 | 仅目标球进袋 | 6 |
| c076 | 小角度带塞进袋 | `accuracy` / `spinAngle` | 切角 15/30° × 顺塞/反塞 | 距离、塞量（中档固定） | 仅目标球进袋 | 6 |
| c077 | 中大角度带塞进袋 | `accuracy` / `spinWideAngle` | 切角 45° × 顺/反塞 + 无塞对照 | 距离、目标球距袋 | 仅目标球进袋 | 6 |
| c078 | 远台带塞准度 | `accuracy` / `spinLong` | 距离档 × 塞侧 | 塞量、入袋角 | 仅目标球进袋 | 4 |

合计 **≈30 球形**（量级对标审计 R1 的「5 drills ≈35 球形」）。

**固定变量（全 6 条通用）**：中杆（`spinY = 0`，把纵向杆法排除在外，避免与 `cueAction` 的高低杆家族混淆）；⛔ **判定只验目标球进袋，不计母球落点**——一旦引入落点判定就退化成走位 drill，主题失守。

**不做的**：贴库带塞（绕不开抬杆，v18 已暂缓，做了只能手画糊弄）；带塞走位落点（属 R6/positioning）。

**level / difficulty / isPremium（D-v21-5 ✅）**：c073/c074/c075 = L2，c076 = L2、c077/c078 = L3；**c073 `isPremium: false`（免费钩子）**，其余 true。

### 3.2 R4 重构 5 条（并入本轮，按审计 §十 R4 口径 + 本轮新口径）

| id | 现状问题（审计结论） | 本轮动作 |
|---|---|---|
| c016 斯登角度停球 | P1：切角 1.8°，斯登切线滑行物理不可见 | 重摆为 20°–45° 有效切角（审计既定） |
| c018 左塞一库变线 | P2：塞量单档、切角 1.1° 自称角度球、洗袋风险 | **D-v21-7 ✅ 归并**：保留 c018，扩 `side` 两档 + 塞量档；主题保持「变线/碰库」 |
| c019 右塞一库变线 | P2：同上（c018 严格镜像） | **退役/降级为 c018 镜像档**（W4 落地时从 index 移除或改文案指向 c018）；计数目标 **72+6−1=77** |
| c020 高杆加塞走位 | P2：组合比例单档、落点区单一 | 按「组合比例 × 切角」双档 |
| c021 低杆加塞回位 | P2：同 c020 | 与 c020 捆绑重构 |

⚠️ **边界红线**：R4 五条的主题是**碰库变线与走位**，新增六条的主题是**带塞进袋准度**。两组的 `standardCriteria` 必须可区分——前者含母球落点，后者只有进袋率。⛔ 禁止在 R4 里塞进让点训练，也禁止在新 6 条里加落点要求。

---

## 四、批次表

> 每批 = 一次子智能体会话可完成并验收的粒度。模型列已按 D-v21-1 用户拍板写入，后续会话视为已指定，不再询问。

| 批次 | 范围 | 量级 | 依赖 | 模型 |
|---|---|---|---|---|
| **W1** 口径与承载基座 ✅ 2026-07-28（0 轮返工） | ①数值核验三件事；②schema 扩 spin；③6 条定稿表；④变量档案骨架。证据 `build/v21-evidence/`；备忘 `docs/research/20260728-v21-W1口径备忘.md`。**S1**：左塞挤右→瞄准向左让；c018 旧文案列入 W4。洗袋 dtp∈[0.12,0.25] | 纯文档 + 验算，已完成 | 无 | `cursor-grok-4.5-high-fast` |
| **W2** 基础加塞 3 条落地 ✅ 2026-07-28（主控验收 0 返工） | c073/c074/c075：14 球形全 bake `feasible∧¬cuePocketed`；计数 75；文案引 W1 让点方向。证据 `build/v21-w2-{logs,screenshots,evidence}/` | 3 drills / 14 球形 | W1 | `cursor-grok-4.5-high-fast` |
| **W3** 加塞准度 3 条落地 ✅ 2026-07-28（主控验收 0 返工） | c076/c077/c078：16 球形全 bake；计数 78；顺塞=切向同侧塞（主控脚本复核一致）。证据 `build/v21-w3-*` | 3 drills / 16 球形 | W1；排 W2 后 | `cursor-grok-4.5-high-fast` |
| **W4** R4 重构（并入） ✅ 2026-07-28（主控验收 0 返工） | c016 切角 20/30/40/45°；c018 吸收 c019（side×塞量 6 球形）并**删文件退役**；c020/c021 组合×切角各 4；bake 18/18 ALL_OK；计数 **77**。证据 `build/v21-w4-*` | ≈4 drills 有效（−1 归并）≈18 球形 | W1；排 W2/W3 后 | `cursor-grok-4.5-high-fast` |
| **W5** 收口与接线 ✅ 2026-07-28（主控验收 0 返工） | ①`plan_cueball` W6 / `plan_intermediate` W1 加塞 drill；②`AngleRoute.drillDetail` + 两学页 CTA→c073（DR-030）；③审计 M6+R4；④SPEC+DR；⑤H-22 录制非阻塞。证据 `build/v21-w5-*` | 跨文件小改多处，1 会话 | W2+W3+W4 | `cursor-grok-4.5-high-fast` |

### 依赖图

```
W1 ──┬─→ W2 ──→ W3 ──→ W4 ──→ W5
     └─（口径供 W2/W3/W4 共用）
```

W2/W3/W4 理论上域不相交（不同 drill 文件），但**三者都改 `index.json` 与 `DrillContentValidationTests` 计数断言**，并行会撞车 → **建议全串行**（与 v11 Y 批同理）。

### 各批完成标准

**W1**
- 数值草稿脚本落盘 + 输出留档（`build/v21-evidence/`），三件事各有 `[事实]` 结论行：
  - S1：给定 spinX 档，母球出射偏向（左/右、度数）与"瞄准应如何补偿"的方向，附 `AimingCorrectionMath` 实测；若与 `c018:17` 现有文案矛盾，明确写出「旧文案需修正」并列入 W4。
  - 挤偏与力度关系复核（复现 Z3 结论，不复现即如实标注差异）。
  - 直球 + 三档塞量的洗袋扫描：给出可用摆位区间（目标球距袋 / 力度上界）。
- `schema.md` **已拍板扩结构化 spin**（D-v21-4）：落地字段定义（推荐 `spin: { "x": number, "y": number }?` 于 `FormationProfile`，并说明与 `variables` 档位标签的关系）；写清消费方（审计/脚本/人工，不进 App Bundle）。
- 6 条 drill 的 id/子类/level/difficulty/isPremium 表定稿（对齐 §3.1 + D-v21-5）。
- ⛔ 本批不产出任何 drill JSON / 序列文件。

**W2 / W3**（各自 3 条）
- 每条：profile 落 `content/drill_profiles/`，坐标由脚本推导（脚本 + 输出留档，⛔ 禁脑算）；变量覆盖矩阵无目标变量空档。
- 每球形一条 initial-only 序列文件（token = profile 球形 id，⛔ 不含 `-`），`make tryout-sync` 后 `Resources/DrillBoards/` 出现对应文件（贴 `ls` 输出）。
- 代表性球形烘焙：`===BAKE===` 输出 `feasible=✅` **且 `cuePocketed=false`**（洗袋核验，逐条贴）。
- `index.json` 更新 + `DrillContentValidationTests` 计数 72→75→78 同步，全量该测试文件 **0 失败**（贴日志尾）。
- `make build` **BUILD SUCCEEDED**（贴日志尾）。
- 模拟器截图核验：动作库分类列表出现新条目 + 详情页 + 精讲分段（每批 ≥3 张）。
- 精讲 `coachingPoints` / `formations` 文案中，方向性表述必须引用 W1 的 `[事实]` 结论；⛔ 不得出现「大力挤偏更大」「加塞必弯」「抬杆…」等未证/暂缓域表述。

**W4**
- c016/c018/c020/c021 按新口径重摆并烘焙（同上 feasible + 洗袋核验）；**c019 从 index 退役**（或文案降级指向 c018，批内二选一并记录）。
- `standardCriteria` 与新 6 条可区分（含母球落点 vs 仅进袋），逐条贴对照表。
- 计数断言终值 **77**（72+6−1），全量 `DrillContentValidationTests` 0 失败 + `make build` SUCCEEDED。

**W5**
- 计划 JSON 改动后：`make build` + 计划相关测试 0 失败；截图核验计划详情页出现加塞周次。
- 学区两页 CTA 可达新 drill（UI 测断言到达目标页导航栏，沿用 v12 Z4 范式）。
- 审计文档 §九/§十、`UI-IMPLEMENTATION-SPEC` Changelog、`tasks/PROGRESS.md`、project-hub 状态卡同步更新。
- 录制项写入 `tasks/HUMAN-REQUIRED.md`（H-xx，**非阻塞**——D-v21-6：内容先行；优先挂 c073/c076）。

---

## 五、拍板项（D-v21-N）— 全部 ✅（2026-07-28 用户：「按你的建议」+ 模型统一 cursor-grok-4.5）

| # | 事项 | 决议 | 备注 |
|---|---|---|---|
| **D-v21-1** | 执行模型 | ✅ 全批统一 `cursor-grok-4.5-high-fast` + 主控独立验收 | 用户覆盖原「W1 强推理」推荐；已写入批次表「模型」列 |
| **D-v21-2** | 分类归属 | ✅ 拆开放：cueAction +3 / accuracy +3；不新开第 9 分类 | 不动 `DrillCategory` / 8 分类断言 |
| **D-v21-3** | R4 并入 | ✅ 并入本轮 W4 | 避免 cueAction 新旧口径共存 |
| **D-v21-4** | profile 扩 spin | ✅ 扩结构化 `spin` 字段（W1 落 `schema.md`） | 解决 initial-only 丢塞信息（§2.4） |
| **D-v21-5** | Freemium | ✅ c073 免费钩子；c074–c078 premium | |
| **D-v21-6** | 录制策略 | ✅ 内容先行 initial-only；录制分 2–3 人工子批异步补；优先 c073/c076 | 不阻塞 W2–W5 收官 |
| **D-v21-7** | c018/c019 归并 | ✅ 归并为一条（保留 c018，退役 c019）；终计数 **77** | W4 落地 |

---

## 六、红线（全批次通用，违反即返工）

1. ⛔ **主题不得漂移**：新 6 条只练「带塞进袋准度」，判定只有进袋率，不得引入母球落点。
2. ⛔ **禁反推**：示范击球以编排台人工录制为准，禁止从存量 `shotIntent` 批量反推生成序列（`schema.md:169`）。
3. ⛔ **禁脑算几何**：切角/坐标/让点方向一律脚本验算并留档，动代码前读 `geometry-spatial-reasoning`。
4. ⛔ **直球设定不得被"近直线=缺陷"口径改坏**：审计判 `c016`/`c024` 近直线为 P1 缺陷，是因为它们的目标变量是切线滑行/分离角；**本轮基础 3 条的目标变量是挤偏，直球是正确设计**，不得改成角度球。
5. ⛔ **文案不得越界**：不写「大力挤偏更大」（与 Z3 实证相反）、不写抬杆/弧线结论（v18 暂缓域）、方向性表述须引 W1 `[事实]`。
6. ⛔ **烘焙不得放水**：`feasible=✅` 且 `cuePocketed=false` 才算过，不达标改摆位而非改判定或手画 animation。
7. ⛔ **诚实交付**：声称通过必须附真实 build/test 输出，未验证如实说未验证。

---

## 七、锚点索引（2026-07-28 读码核实）

| 锚点 | 路径 | 用途 |
|---|---|---|
| 索引与分类数组 | `QiuJi/Resources/Drills/index.json` | W2/W3/W4 |
| 计数/分类断言 | `QiuJiTests/DrillContentValidationTests.swift:21-23`（8 分类）、`:25-27`（索引 72）、`:50-52`（加载 72） | W2/W3/W4 |
| 分类枚举 | `QiuJi/Data/Services/DrillContentService.swift:210-247`（8 case + nameZh + icon） | D-v21-2 备用 |
| Drill schema / 三层承载 | `QiuJi/Resources/Drills/schema.md:166-171` | 全批 |
| profile sidecar schema | 同上 `:172-215`；`FormationProfile` 字段 `:203-214`（基线无 spin；**D-v21-4 ✅ W1 须扩**） | W1 |
| profile 唯一现例 | `content/drill_profiles/drill_c053.profile.json`（8 球形 A1–A8） | W2/W3 范式 |
| initial-only 序列实例 | `content/position_play/sequences/drill_c053__A1-中袋角度球 · 球形1-0杆.json`（`steps: []`，无 spin） | W1（2.4） |
| 试打同步 | `scripts/Makefile:162-169`（`tryout-sync`）；`QiuJi/Core/PositionPlay/DrillTryoutBoardStore.swift` | W2/W3/W4 |
| miscue 常量 | `QiuJi/Core/Physics/BTPhysicsConstants.swift:233`（`miscueLimitFraction = 0.5`） | 塞量档定义 |
| miscue 守门 | `QiuJi/Core/Physics/ShotIntent.swift:58-64`，调用点 `:110` | 塞量档定义 |
| 挤偏/投掷真源层 | `QiuJi/Features/AngleTraining/AimingCorrectionMath.swift:16`（Δ 口径）、`:118`、`:186`（throw）、`:202-204`（`squirtDegrees`，负=向右） | W1 数值核验 |
| 现有让点文案（存疑 S1） | `QiuJi/Resources/Drills/cueAction/drill_c018.json:17`（教学点）、`:19`（判定含走位） | W1 / W4 |
| 计划目录（零引用实测） | `QiuJi/Resources/Plans/*.json`；grep `drill_c016\|c018\|c019\|c020\|c021` → 0 命中 | W5 |
| 审计缺失清单 §九 | `docs/research/20260716-动作库全库审计.md:763-775`（M1–M5） | W5 补 M6 |
| 审计批次表 R4 | 同上 `:806` | W4 |
| 体系表加塞五项 | `docs/research/20260323-训练内容体系-动作库分类.md:67-75` | 内容对标 |
| 变量覆盖方法论 | `docs/research/20260716-drill变量覆盖设计方法论.md` | W1/W2/W3 |
| 现存录制欠账 | `tasks/HUMAN-REQUIRED.md:247-258`（H-21 ⏳） | D-v21-6 |

---

## 八、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v21.8 | 2026-07-28 | **W5 主控验收 ✅**（0 返工）：亲读计划 grep+截图 W6「加塞与挤偏」、CTA→c073 详情 05/08、审计 M6/R4、DR-030、H-22 非阻塞；落盘 plan 3/0 + CTA UI 3/0 + 主树 `make build` SUCCEEDED。**v21 全部批次收官** |
| （指针） | 2026-07-28 | 文首增加 **[v22 修订指针]**：W5 计划接入策略被 `问题集合_v22` D-v22-2=A 修订；不改写本版已完成事实与产物 |
| v21.7 | 2026-07-28 | **W4 主控验收 ✅**（0 返工）：亲读 bake 18/18 ALL_OK、index 77/无 c019、criteria 对照、截图（列表有斯登/搜「加塞一库」仅一条/详情让点左塞→向左）、validation 20/0、主树 `make build` SUCCEEDED。开工 W5 |
| v21.6 | 2026-07-28 | **W3 主控验收 ✅**（0 返工）：亲读 bake 16/16、validation 20/0、截图列表/详情、spinKind 一致性脚本 True、主树 build 绿。建议新会话 W4（本会话已连续 W1–W3） |
| v21.5 | 2026-07-28 | **W3 ✅ 执行交卷**（grok-4.5）：c076/c077/c078 落地（16 球形全 bake）；计数 78；待主控验收 |
| v21.4 | 2026-07-28 | **W2 主控验收 ✅**（0 返工）：亲读 bake-all 14/14 ALL_OK、validation 20/0、截图三帧（列表/详情/精讲 A2）、profile spin、仅进袋判定、c073 免费；主树 `make build` SUCCEEDED。开工 W3 |
| v21.3 | 2026-07-28 | **W2 ✅ 执行交卷**（grok-4.5）：c073/c074/c075 落地（14 球形全 bake）；计数 75；待主控验收 |
| v21.2 | 2026-07-28 | **W1 ✅**（plan-delegated-execution，grok-4.5，0 返工）：主控亲读 s1/squirt-vs-power/scratch-scan + schema spin + 备忘定稿表；日志 `Executed 3 tests, 0 failures` + `TEST SUCCEEDED`；主树 `make build` SUCCEEDED。开工 W2 |
| v21.1 | 2026-07-28 | **D-v21-1～7 全部拍板**：模型全批 `cursor-grok-4.5-high-fast`；拆开放；R4 并入；profile 扩 spin；c073 免费；录制内容先行；c018 吸收 c019、终计数 77。批次表模型列与 W1/W4/W5 完成标准同步。状态→可开工 W1 |
| v21.0 | 2026-07-28 | 立项。需求回显（含第二轮语义澄清：主题=带塞进袋准度，非碰库变线）；新增 6 条 + R4 并入的设计骨架；批次 W1–W5 + 依赖图 + 完成标准；D-v21-1～7 待拍；锚点全部读码核实；两项新发现（计划零引用加塞 drill、profile 无 spin 字段致 initial-only 丢塞信息）与两项存疑（S1 让点方向、S2 洗袋风险）入库 |
