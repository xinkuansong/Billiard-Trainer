# 问题集合 v56 — 全 App 色彩语义与 Light / Dark 一致性收口

> **本文件是本轮需求真源**（v56.8，制定日期：2026-09-03，状态：**W0–W7 自动化范围已完成；OLED / Reduce Transparency 人工复验待 H-28**）。
> **用户目标**：基于当前所有页面的 Light / Dark 截图，把全 App 的颜色整体整理干净；Dark 也要全量复验，不能只验证 Light。
> **执行边界**：v56.0–v56.6 先用 `issue-collection-restructure` 完成问题、依赖、批次与验收门禁；用户于 2026-09-04 明确授权后，已在当前共享工作区按 W0–W7 实施。未创建 worktree、未改品牌绿、未换图标轮廓，也未把随机封面试装写成正式选图。
> **证据基线**：`tasks/ui-reviews/UR-20260903-full-app-color-consistency.md`；Light 与 Dark 使用同一源码指纹 `5c84e0beabb481d48e0d482fa9f3bc7fdee1fa54bd2c61cc17cba3a66f5a5aef`。
> **排期前置**：v55 统计页会触及统计视觉与可能的图表颜色；v56 开始生产代码前，须先完成 v55 W1–W5，或明确冻结 `StatisticsView*` / `AngleHistorySection.swift` / `btChartSeries` 的归属，禁止两个方案同时改同一视觉契约。
> **工作区边界**：当前主工作区有 v50–v55、引导页 D 系列、封面候选等未提交并行工作。执行时仅修改本批清单内路径，不覆盖、回退、格式化或顺带提交其他改动。

---

## 一、需求回显与不可弱化口径

| ID | 用户原意 / 已有结论 | 本方案的不可弱化口径 |
|---|---|---|
| R1 | “整体 UI 的颜色上有点乱” | 处理的是跨页颜色语法，不是把所有页面机械改成同一种颜色 |
| R2 | “看看当前所有页面的截图” | 以 66 页主巡游为路由基线；文件数量、页面身份、截图内容三者都要验证 |
| R3 | “DARK 也全量复验” | 最终验收必须同时包含 Light / Dark 全量巡游，不能用少量 Dark 抽样替代 |
| R4 | “整体整理下” | 先治理共享 Token / 组件，再处理单页；禁止每页各写一套近似颜色 |
| R5 | 保留成熟暗场工作区 | 黑底球桌、彩球、轨迹、瞄准点、力度与警示色属于操作/物理信息，不做去色或浅色化 |
| R6 | 当前 Dark 明显比 Light 统一 | Dark 不重做背景体系；只修状态语义、个人页强调与共享组件一致性 |
| R7 | 当前封面是随机候选试装，尚未视觉批准 | 封面批次先锁定素材集合，再统一调色/叠层；不得把随机抽中当成最终选图 |
| R8 | 本轮要写方案 | 本轮产物只有问题集合、批次与进度记录；构建、测试和截图均属于后续执行证据 |
| R9 | “还是用之前的样式，只换颜色；想用更有质感的、生成的” | 保留现有五角星/皇冠轮廓，不开发自定义 Pro Mark；生成只负责金色材质，文字仍用可读纯色 |

### 1.1 一句话设计目标

> **背景与品牌绿的现有 Light / Dark 色相保持不变；品牌绿按导航、筛选、主操作分强度，Pro 使用克制的炭黑 + 分层香槟金，警告与物理信息各走自己的语义色，同类选中态在全 App 只说同一种语言。**

### 1.2 已有截图结论

- Light 色彩一致性约 `7/10`：基础背景和文字稳定，主要乱在黑白/绿色/灰色选中态并存，以及浅色内容页进入黑底工具页时翻转过多。
- Dark 色彩一致性约 `8/10`：深灰卡、白字、亮绿交互连续性良好，不存在“整体失控”；剩余问题集中在状态语义、金色过载和个人页首屏焦点竞争。
- 历史 / 统计是当前最克制的页面族；黑底球桌工具是成熟资产，二者都应作为保护样板，而不是重构对象。
- 摄影封面的白平衡、曝光和黑位差异，比基础 Token 更容易制造杂乱感。

### 1.3 不在本轮改变的产品语义

1. 不改五 Tab、页面导航、登录方式、Pro 权益、训练数据或物理计算。
2. 不改球号本色、球桌台呢、母球/目标球轨迹含义和红绿接触点语义。
3. 不把暗场工具强制跟随系统 Light 变成浅色；它仍是固定暗场工作区。
4. Apple 登录按钮保留 Apple 黑/白品牌处理，它是明确例外，不纳入品牌绿按钮统一。
5. 手机号登录仍是测试专用深链覆盖，不重新开放生产入口。
6. Onboarding D 系列尚未获视觉批准；v56 只约束当前生产引导页的颜色角色，不接入 D 图、不改叙事内容。
7. 不通过“全量换封面图”绕过调色问题；素材替换需另有用户选择证据。

---

## 二、当前实现事实与问题清单（2026-09-03 实读）

### 2.1 当前颜色链路

```text
Assets.xcassets semantic colors
    └─ Colors.swift
        ├─ 页面背景 / 文字 / 品牌 / 状态 Token
        ├─ BTFilterChip：Light 黑、Dark 白的反相选中态
        ├─ BTTogglePillGroup：Light 绿、Dark 白的跨模式分叉
        ├─ BTSegmentedTab / sidebar：绿色文字、下划线或侧轨
        ├─ btAccent：Pro + 警告 + 收藏 + 图表 + 教学图解 + HUD 数值
        └─ BTAtmosphereLayer：60 张摄影封面 + 固定 12% 黑色中性叠层
```

### 2.2 问题总表

| ID | 优先级 | 问题 / 风险 | 归属批次 |
|---|---:|---|---|
| P56-01 | P1 | `66/66` 可以在页面尚未切换完成时仍通过；Dark `70-login.png` 实际是个人页 | W1、W7 |
| P56-02 | P1 | Light `00-launch.png` 与 `01-training-home.png` 内容重复，文件数被误当成独立视觉状态数 | W1、W7 |
| P56-03 | P1 | `testV47BrandSample` 仍要求已下线的微信/手机号入口及不一定存在的法律链接，当前失败不能证明 UI 回归 | W1 |
| P56-04 | P2 | 筛选、切换、顶部 Tab、侧栏、日期分别用黑白反相、绿底、绿线和灰底表达“选中” | W2 |
| P56-05 | P2 | `btAccent` 同时承担 Pro、警告、收藏、图表、教学提示、轨迹和装饰，语义彼此竞争 | W3 |
| P56-06 | P2 | 品牌绿、交互绿、成功绿、球桌绿、照片绿、薄荷表面缺少可审查的角色边界 | W0、W3 |
| P56-07 | P2 | 访客个人页同时展示登录绿、警告金、Pro 黑金和多色菜单图标，第一行动不清楚 | W4 |
| P56-08 | P2 | Light 内容页进入固定黑底工具页时，背景、导航、控件与强调色同时翻转 | W5 |
| P56-09 | P2 | 60 张摄影封面的曝光、黑位、色温和绿桌色相差异大，12% 固定叠层无法稳定归一 | W6 |
| P56-10 | P2 | 旧 DR-043 明确锁定筛选 Chip 为 Light 黑 / Dark 白，与本轮统一绿色交互态冲突 | W0、W2 |
| P56-11 | P2 | v55 正在重构统计页；若同时改 `btChartSeries` 或统计截图，会出现方案互相覆盖 | W0、W3、W7 |
| P56-12 | P2 | 当前 Pro 金是单一橙黄纯色；星形/皇冠保留，但需要可控的生成式材质层，且不能牺牲小尺寸清晰度或文字对比 | G0、W0、W3、W4 |

### 2.3 根因判断

1. 不是 `btBG`、`btText` 或 `btPrimary` 的基础色值选错；`btPrimary` 当前 Light `#1A6B3C`、Dark `#25A25A`，继续作为品牌绿，不在 v56 改色相。
2. 主要根因是颜色“用途”没有收紧：相同状态用了不同颜色，相同颜色又承担了不同含义。
3. 其次是照片本身的色调波动；它不能只靠改一个全局 Token 解决。
4. 截图门禁当前只验证“文件生成”，没有在关键页验证“页面身份”，导致视觉审查会建立在错误画面上。

---

## 三、目标色彩语法

### 3.1 角色表

| 角色 | 唯一用途 | 允许位置 | 禁止用途 |
|---|---|---|---|
| `btPrimary` | 品牌、主 CTA、当前导航/筛选 | 主按钮、选中筛选文字/边框、Tab 指示、侧栏轨、可点击主标题 | 警告、成功、Pro、纯装饰大面积底色 |
| `btPrimaryMuted` | 品牌绿的低强度表面 | 激活筛选背景、品牌信息弱底、轻量 hover/selection | 第二主 CTA、成功状态 |
| `btPremiumForeground`（由 `btAccent` 拆出） | 可读的 Pro 前景 | Pro 标题、图标、会员状态；Light 用深金、Dark 用浅香槟金 | warning、普通选中、大面积背景 |
| `btPremiumSurface` / `btPremiumBorder`（由 `btAccent` 拆出） | Pro 弱表面、描边与少量高光 | Pro badge、订阅卡局部、CTA 层级 | 正文前景、默认图表系列、教程误区 |
| 稀疏品牌签名金 | 成就、关键值或品牌识别点 | 经台账确认的单个小面积识别点 | 访客警告、普通收藏、装饰性分隔 |
| `btWarning` | 需注意但非错误 | 访客同步提醒、风险提示、警告 Toast | Pro、普通强调、物理力度 |
| `btSuccess` | 已完成 / 成功 | 完成态、成功反馈 | 当前选中、品牌装饰 |
| `btDestructive` | 删除 / 失败 / 危险 | 错误、破坏性操作 | 警告、当前态 |
| `btPathCue` / `btPathTarget` / 专用物理 Token | 球路与物理标注 | 球桌、教学图解、HUD 可调量值 | 页面导航、会员、普通卡片强调 |
| `btTableFelt` / 真实彩球 | 台球内容身份 | 球桌、球、封面中的真实内容 | UI 按钮或选中态 |
| 中性色 | 信息层级与结构 | 背景、卡片、分隔、普通图标、非主提示 | 需要明确操作反馈的主 CTA |

### 3.2 同一品牌语义分三级强度

```text
跨内容导航（Tab / 分段标题 / 左侧栏目）
    = btPrimary 文字或图标 + 2–3pt 指示线/侧轨

高频局部筛选（等级 Chip / 多项筛选）
    = btPrimaryMuted 弱底 + btPrimary 字/边框

主操作或关键二元切换
    = btPrimary 实底 + 白字
```

- 删除 `Light 黑底 / Dark 白底` 这套反相语法；Light / Dark 的语义不再翻转。
- 不把同屏所有“当前”都做成高饱和绿块；层级通过强度区分，不通过换色相区分。
- 同一组件不得因 Dark Mode 改变“选中角色”，只允许背景/边框对比度做动态适配。
- 系统日期选择若受系统控件限制，可保留系统形状，但选中颜色仍须映射到品牌主色。
- 暗场工具内的 HUD chip 使用既有暗场规则，不强行套内容页组件；它的“绿管选择、金管数值”属于受保护的操作语义。

### 3.3 金色收口规则

- 当前 `btAccent` 是 Light `#D4941A`、Dark `#F0AD30`，偏橙黄且过亮；它在大面积卡片/按钮上更像促销或警告，不能只做饱和度微调后继续一色多用。
- 主方向采用“炭黑 + 分层香槟金”：候选基准金 `#C6A15B` 只用于边框/图形/装饰，Light 可读前景候选 `#8C6B2F`（对白 `4.93:1`），Dark 可读前景候选 `#E7D3A0`（对 `#1C1C1E` 为 `11.52:1`）。实际动态色仍须在实现期按真实背景复验。
- 冷淡沙金仅作为弱表面/高光参考；古铜金因棕调明显不进入主方案。
- 保留：Pro badge、订阅 CTA、会员状态；经 W0 台账确认的极少量品牌签名、成就或关键值。每个局部层级最多一个小面积金色焦点。
- 迁出：访客同步提醒 → `btWarning`；普通收藏 → `btPrimary` 或中性激活；统计系列 → 待 v55 完成后按数据域语义决定；教程误区 → `btWarning`；纯装饰短线 → 中性或品牌弱色。
- 物理/教学中的黄橙色不得直接全局替换：先改为专用语义 Token 或既有 `btPathTarget` / `HUDStyle.valueAdjustable`，保持原含义与可辨识度。
- Pro CTA 禁止使用“中亮金底 + 白字”的低对比组合；Light 可用深金底 + 白字或香槟金底 + 炭黑字，Dark 优先浅香槟金底 + 炭黑字。

### 3.4 页面焦点规则

- 普通内容页一屏最多一个高面积主行动色。
- Pro 金可与主行动同屏，但只能以 badge、小图标或单一卡片局部出现，不与警告共用相同金色。
- 个人页访客态第一行动为“登录”；同步提醒降为信息层，Pro 卡降低面积权重，普通菜单图标统一为品牌绿或中性灰。
- 黑底工具页不改变工作区，只统一返回/标题/更多、状态胶囊和主 CTA 的形状与品牌绿角色，建立 Light 页面进入暗场时的连续性。

### 3.4a Pro 生成式金色材质

- 保留现有 `star.fill` 与 `crown` 轮廓、位置和语义，不新增自定义 Pro Mark，不替换 `BTBrandLogo`。
- 暂推荐“细拉丝香槟金”：低饱和金、宽柔光、极轻斜向拉丝、边缘压暗；禁止镜面高光、闪粉、珠宝切面、厚浮雕和金币感。
- 生成材质只填充星形、皇冠及其徽章表面；`了解更多`、会员状态、CTA 文案继续使用满足对比度的纯色 `btPremiumForeground`。
- 生产实现优先保留确定性的 SF Symbol mask，并以生成材质图作为内部 fill；小于 24pt 时允许降低或关闭纹理，以清晰轮廓优先。
- 生成材质需提供可平铺/可裁切母版及 Light/Dark 适配，不直接把整张页面 mockup 或带背景图标 PNG 放入 Asset Catalog。

### 3.5 封面调色目标

- 先对最终 60 张素材计算并记录亮度中位数、黑位、色温倾向、绿桌区域饱和度与文字安全区；不以主观“差不多”批处理。
- 官方计划、练习、模版各自允许一套轻微不同的裁切/叠层，但同一货架内必须统一。
- 白棚拍只作为明确系列出现，不与深绿摄影随机交错；若最终素材集合无法通过叠层收口，返回选图，不继续加重黑罩掩盖问题。
- 编号、Pro badge 和标题安全区必须在 Light / Dark 卡片表面保持相同位置与对比度。

### 3.6 代码前文生图校准 G0

已用当前真实截图生成/编辑 20 张色彩、图标与材质目标态图，见：

- `docs/design/v56-color-calibration/MANIFEST.md`
- `tasks/ui-reviews/UR-20260903-v56-color-target-calibration.md`

校准发现并修正了 v56.0 的过度简化，并补入 Pro 身份图形要求：

1. 多项筛选不宜统一绿实底，默认应为弱绿表面；Dark 需比生成图更强的表面差/边框。
2. 金色不应机械清零，允许极少量品牌签名/成就/关键值，但不得再承担 warning 或普通 selection。
3. 封面不能只调共享黑罩，还需摄影系列分组、相邻排序与 outlier 重选。
4. 当前品牌绿不需要换色相；需要调整的是使用强度与面积。
5. 当前橙黄金不具备稳定的付费质感；三版比较后建议香槟金为主、冷淡沙金为弱层，且先拆语义再换色值。
6. 自定义 Pro Mark 探索已完成但不采用；用户明确保留原星形/皇冠。
7. 三种生成式材质中细拉丝香槟金最平衡；哑光版近金币，象牙金版近奶油塑料。材质只进图标表面，文字不能使用纹理。
8. Dark 生成中先后出现镂空金币与切面宝石漂移；收紧稿可用作方向参考，但生产端必须用 SF Symbol mask 锁定实心轮廓，并限制高光/描边强度。

**G0 已确认（2026-09-04）**：用户授权开始执行全部批次；颜色方向固定为“品牌绿不改色相 + 筛选弱绿 + Pro 分层香槟金”，Pro 保留原星形/皇冠。Light 以 `18`、Dark 以 `20` 为页面参考，独立材质母版为 `21` / `22`。生产仍必须由 SF Symbol mask 锁形。

---

## 四、执行批次、依赖与完成标准

### 4.1 批次总表

| 批次 | 范围 | 量级 | 依赖 | 主要交付 |
|---|---|---:|---|---|
| W0 | 冻结经 G0 确认的语义契约、冲突台账与基线 | 小 | G0、v55 归属确认 | DR / Token 使用台账 / 基线清单 |
| W1 | 修复截图页面身份与登录测试契约 | 小 | W0 | 可信的 66 页证据链 |
| W2 | 统一内容页选中态共享组件 | 中 | W0、W1 | Chip / Toggle / Tab / Sidebar 统一 |
| W3 | 收紧金色、绿色与教学/物理语义 | 大，拆两段验收 | W2、v55 完成 | 通用语义迁移 + 教学/物理专用语义 |
| W4 | 个人页首屏与登录入口颜色层级 | 中 | W3 | Guest / Login / Pro 层级收口 |
| W5 | Light 内容页 → 暗场工具页共享 Chrome | 中 | W2、W3 | 导航与状态控件连续性 |
| W6 | 摄影封面调色与叠层归一 | 中 | 用户锁定最终封面集 | 60 张量化台账 + 三货架复验 |
| W7 | Light / Dark 全量回归与文档收口 | 大 | W1–W6 | 396 张主矩阵 + AX/高对比抽查 + 报告 |

### 4.2 依赖图

```text
G0 用户确认 ─────────────┐
v55 视觉归属完成或冻结 ─┤
                         ├─ W0 ─ W1 ─ W2 ─ W3 ─┬─ W4 ─┐
                         │                       └─ W5 ─┼─ W7
最终 60 张封面获用户确认 ───────────────────────── W6 ─┘
```

### W0 — 语义契约、冲突台账与基线冻结

**范围**

1. 将用户确认后的 §3 写入 `tasks/UI-IMPLEMENTATION-SPEC.md` 新 DR；明确它 supersede DR-043 中“筛选 Chip 不得用品牌绿”的旧决定，但不改暗场 HUD 规则。
2. 生成 `btAccent`、硬编码 `Color(red:)`、选中态、封面叠层的文件级使用台账，逐项标注 `迁入 premium 三层 / 迁出 warning-selection-chart / 待 v55 / 受保护物理语义`。
3. 盘点 `star.fill`、`crown`、`BTProBadge`、`BTPremiumLock`、订阅页 Hero 等 Pro 入口，定义统一 Premium 材质填充、SF Symbol mask、小尺寸纯色回退和 accessibility 契约；不改变图标轮廓。
4. 冻结本轮基线源码指纹、Light/Dark 截图目录、66 页 manifest 与审核报告；后续截图必须记录新指纹。
5. 建立 v55 冲突台账：`StatisticsView*`、`AngleHistorySection.swift`、`Colors.swift:btChartSeries` 同一时刻只归一个方案修改。

**完成标准**

- [x] 新 DR-083 明确色彩角色、选中态三级强度、Apple/暗场/彩球例外和旧 DR 覆盖关系。
- [x] 42 个文件 / 114 个 `btAccent` 命中完成分类；Premium 前景/表面/边框分别登记，未做全局替换。
- [x] Pro 图标消费点完成盘点；独立材质母版 `21` / `22` 已生成，mask、24pt 以下回退和 `BTBrandLogo` 不变边界明确。
- [x] v55 重叠文件唯一 owner 已锁为 v55；v56 W3 跳过冻结项，W7 等 v55/v51 最终源码。
- [x] 本批仅文档与设计证据改动；`git diff --check` 通过。

**W0 执行证据（2026-09-04）**

- 语义 / 冲突 / 基线台账：`docs/research/20260904-v56-color-semantic-ledger.md`。
- 规范与调整记录：`tasks/UI-IMPLEMENTATION-SPEC.md` DR-083、`tasks/IMPLEMENTATION-LOG.md` DR-083。
- W0 源码指纹：`d7e9b5108153f2c82aaab0c15464ba5d7d453a82bee513d534c5ed3747068dc9`；文档与 `docs/design` 证据不进入该指纹。
- 材质母版：`21-premium-material-master-light.png` / `22-premium-material-master-dark.png`，SHA-256 已写入 manifest。
- `StatisticsView*`、`AngleHistorySection.swift`、`btChartSeries` 冻结给 v55；60 张 cover 仍受 G1 约束。

### W1 — 截图证据与登录断言修复

**范围**

1. `openProfileLogin()` 点击后必须等待登录页身份锚点（建议 `login.apple` 或“看懂球路，练出结果”）出现，等待成功才返回 `true`；不得点击即宣告成功。
2. `tourLogin()` 在 `snap("70-login")` 前再次断言页面身份；`71-phone-login` 明确标记为 test-only retained screen，不计入生产可达登录方式。
3. 为 66 页 manifest 增加关键页面身份断言；至少覆盖根 Tab、计划详情、理论页、暗场工具、个人页、登录、手机号测试页和 Onboarding。
4. 对 `00-launch` 与 `01-training-home`：要么稳定捕获真实 Launch Screen，要么把 `00` 标为启动过程证据、不计入独立页面总数；禁止继续宣称 66 个不同视觉状态。
5. 更新 `testV47BrandSample` 为当前 Apple-only 契约，保留真实登录能力断言，不“删断言求绿”。

**既有断言与失败机理（PD-027）**

| 既有断言 | 当前失败机理 | 正确迁移 |
|---|---|---|
| `for label in ["通过 Apple 登录", "微信登录", "手机号登录", "暂不登录，匿名使用"] { XCTAssertTrue(app.buttons[label].exists) }` | `LoginView` 仅保留 Apple 与匿名按钮；微信/手机号已变为“暂未开放”的静态说明，生产按钮不存在 | 断言 Apple 与匿名按钮存在；断言“微信与手机号登录暂未开放”说明存在；手机号表单由测试专用深链单独覆盖 |
| `XCTAssertTrue(app.staticTexts["用户协议"].exists)` / `隐私政策` | URL 存在时两项是 `Link`，URL 缺失时页面显示合并的 fallback 文本；用 `staticTexts` 且假设 URL 必有，元素类型和配置前提都不成立 | 分支验证：有 URL 时查找 Link；无 URL 时断言 fallback 文本，不能静默跳过法律区 |
| `candidate.tap(); return true` | Sheet 转场尚未完成即返回，紧接的 `snap("70-login")` 可能截到个人页 | 点击后等待登录身份锚点；超时返回 false 并使巡游失败 |

**写盘边界**

- 本批不使用“跑完全部测试后整个仓库零 diff”作为门禁；仓库内有大量截图、bake、export 与诊断测试会写 `build/`、`/tmp` 或显式证据目录。
- 只运行登录/巡游定向用例；执行前记录 `git status --short`，执行后仅检查本批声明的跟踪文件 diff，并确认未改写 Asset Catalog / 内容真源。
- 若实现期新增全量“零 diff”门禁，必须先以 `rg -l '\.write\(|FileManager.*create' QiuJiTests QiuJiUITests` 加 scheme 默认执行集完成写盘测试全集盘点，不能沿用本批的定向清单。

**完成标准**

- [x] `testV47BrandSample` Light/Dark 均通过，且截图确认为真实登录页。
- [x] 定向 `tourLogin` 的 `70` / `71` 页面身份断言通过。
- [x] Light/Dark 各跑一次主巡游，关键页面 identity 全部通过。
- [x] 报告明确“文件数 / 生产页面数 / 测试专用页面数 / 独立视觉状态数”，不再混写为一个 66。

### W2 — 内容页选中态共享组件统一

**范围**

1. `BTFilterChip` 改为跨模式弱绿表面 + 品牌绿文字/边框，移除 Dark 白底与 Light 黑底反相；`BTTogglePillGroup` 是否实绿按其是否属于关键二元切换判断，不与高频筛选一刀切。
2. `BTSegmentedTab` 保留绿色文字 + 指示线；侧栏保留绿色文字/图标 + 侧轨，作为“导航选中”样板。
3. 审核并收口 TrainingHome、Drill Library、AngleHome、History、Settings 的消费点；不在页面内复制私有色值。
4. Dark 下核对选中绿填充与白字对比；Light 下核对未选中边框、触控面积、Dynamic Type 和 Reduce Transparency。

**完成标准**

- [x] 共享组件 Preview 覆盖 Light/Dark、selected/unselected/disabled（如适用）。
- [x] 训练筛选、动作库等级筛选、设置外观切换、记录分段、练习侧栏截图语法一致。
- [x] Accessibility 保留 `.isSelected`，颜色不是唯一状态编码。
- [x] 组件定向单测/渲染测试通过；`make -f scripts/Makefile build` 通过。
- [x] iPhone SE、标准 iPhone、iPad mini 的 Light/Dark 聚焦截图通过。

### W3 — 语义色收紧（两段验收）

#### W3a — 通用 UI 语义迁移

**范围**

- 访客警告、warning Toast、误区提示使用 `btWarning`。
- Pro / 订阅从 `btAccent` 迁入 premium 前景/表面/边框三层；普通收藏、编辑、进度、统计系列根据其业务语义迁到 primary / success / neutral / chart 专用角色。
- 新增统一 Premium 材质渲染：继续使用原 star/crown mask，大尺寸显示细拉丝材质，小尺寸按门禁降低纹理或回退纯色；不扩大到普通成就。
- `BTIconBadge.accent` 继续只服务商业化/成就；普通 Profile 菜单不得为装饰使用 accent。
- `btChartSeries` 等 v55 重叠项在 v55 完成后迁移，且需复用 v55 的图表层级，禁止本批重排统计页。

#### W3b — 教学图解与暗场物理颜色

**范围**

- 按 `btPathCue`、`btPathTarget`、`HUDStyle.valueAdjustable`、警示/错误角色分类现有黄橙绿用法。
- 只改 Token 归属和注释，不改几何、轨迹、球号、坐标、动画或求解逻辑；若触及任何几何计算，实施前必须另行加载 `geometry-spatial-reasoning`。
- 保留“绿管选择、金/橙管数值、红管错误”和白色瞄准线等暗场成熟规则，避免为了表面统一损失信息编码。

**完成标准**

- [x] `btAccent` 剩余使用均能解释为 v55 冻结的 `AngleHistorySection` / `btChartSeries` 或历史兼容别名；生产新消费点已迁出。
- [x] Pro 推广卡、菜单行、badge 与订阅页保留既有 star/crown 语义并共享同一材质家族；20pt 清楚，Dark/Light 均有 `PRO` 或文字第二编码。
- [x] warning、success、destructive、physics 不再借用同一个 accent 角色。
- [x] 教学页 Light/Dark 的误区、图解、标签可读；暗场工具关键轨迹辨识度不低于基线。
- [x] 颜色外仍有图标、文字、线型或位置编码，满足色觉缺陷用户识别。
- [x] W3a 与 W3b 各自完成聚焦与全量截图复验，没有把编译通过当视觉验收。

### W4 — 个人页首屏与登录入口层级

**范围**

1. 访客首屏只保留登录为主行动；同步提醒改低强度 warning / neutral 信息条。
2. Pro 卡保留黑金身份，但降低面积与饱和度竞争，不与普通菜单入口同层抢焦点。
3. 普通菜单 `BTIconBadge` 统一 primary / neutral；accent 只给订阅或成就。
4. 去除访客态重复登录 CTA，或将底部入口降为明确次级文本动作；登录路径和 accessibility identifier 保持兼容。
5. Login / Onboarding 只做颜色契约复验；Apple 黑白按钮与当前暗色品牌 Hero 不改为绿底。

**完成标准**

- [x] Guest、已登录 Free、Pro 三态在 Light/Dark 都只有一个明确首要行动。
- [x] 个人页 warning 与 Pro 不再共用金色。
- [x] 登录、订阅、设置、收藏等入口可达性与原业务逻辑不变。
- [x] iPhone SE 首屏不挤压主行动；AX XXXL 下不靠颜色区分入口。
- [x] Profile 定向 UI 测试和 `testV47BrandSample` Light/Dark 回归通过。

### W5 — Light 内容页到固定暗场工具页的桥接

**范围**

1. 盘点 `BTShotPageChrome`、`SolverStageChrome`、理论页共享 Chrome 及其页面宿主。
2. 保留暗场 `Color.black`、`.toolbarColorScheme(.dark)` 和 HUD；统一导航标题、返回/更多按钮、状态胶囊、主 CTA 的形状与品牌绿角色。
3. 只改善 Light 的跨页连续性；Dark 已稳定，改动后不得出现退步。
4. 按“内容入口 → 工具首屏 → 返回内容页”成对截图，不用孤立工具截图判断桥接。

**完成标准**

- [x] 理论末页→测验、练习大厅→2D/3D/自由打/解球器的 Light 成对路径通过。
- [x] Dark 同路径与基线等价或更统一；暗场工具内部对比、轨迹、仪表不退化。
- [x] 导航返回、更多菜单、模式切换和击球主按钮行为不变。
- [x] 相关共享页面 UI 测试、构建和聚焦截图通过；全仓仅保留一个集中式 `.toolbarColorScheme(.dark)` 消费点。

### W6 — 摄影封面归一

**开始门槛 G1**

当前 60 张封面是固定随机种子的试装结果，随机命中不等于视觉批准。W6 开始前，用户需选择：保留当前 60 张、从四选一候选中重选，或恢复上一套正式资产。未过 G1 时只可做只读度量，不写 Asset Catalog。

**范围**

1. 对锁定素材生成 60 行图像台账和官方/练习/模版三张联系表。
2. 优先通过 `BTAtmosphereLayer` 的语义化 neutral/bottom scrim、裁切和安全区统一；避免每张卡私写 opacity。
3. 建立摄影系列分组和相邻排序规则；白棚拍、绿桌近景、暗场静物分别成组，不随机交错。
4. 仅当共享叠层与分组不能修复个别极端素材时，返回素材选择；不靠加重全局黑罩牺牲全部图片。
5. 保留 `showsColorWash: false` 和 DR-080/081 的去彩色罩、去主题叠字要求。

**完成标准**

- [x] 60/60 试装素材有 hash、亮度/黑位/色温倾向与归属记录。
- [x] 在不重选素材的授权边界内，共享中性遮罩已按亮度分档；六张最终联系表未见 UI 叠层制造的新跳变。
- [x] Light/Dark 编号、Pro badge、标题安全区对比稳定。
- [x] 不新增几何伪信息、额外球杆、错误球号或构图漂移。
- [ ] 当前 60 张仍是可恢复的随机试装，未获素材级 G1 批准；v56 未替换其源图，也不把本轮 UI 归一写成正式选图结论。

### W7 — 全量 Light / Dark 复验与收口

**自动矩阵**

| 设备 | 模式 | 范围 | 目的 |
|---|---|---|---|
| iPhone SE（第 3 代） | Light + Dark | 66 页主巡游 | 紧凑宽高、卡片/筛选拥挤与 Dark 对比 |
| 标准 Pro iPhone | Light + Dark | 66 页主巡游 | 主视觉基线与联系表审查 |
| iPad mini | Light + Dark | 66 页主巡游 | 双栏、宽卡、工具栏和封面裁切 |

共 `3 × 2 × 66 = 396` 张文件证据；另做 AX Large、High Contrast、Reduce Transparency 的重点页面抽查，不把它们伪称为 66 页全矩阵。

**每个矩阵的自动门禁**

- 源码指纹一致；manifest 数量完整；文件可解码、尺寸正确、无近纯色、无重复内容。
- 关键页面 identity 断言通过；`70-login` 必须是登录页，`71-phone-login` 必须标为测试专用。
- 对比度检查覆盖正文、次级文字、选中/未选中、warning、Pro、暗场 HUD；颜色外必须有第二编码。
- 生成逐页联系表和差异清单；自动通过不等于目视通过。

**目视门禁**

1. 按页面族检查：训练/计划、动作库、练习大厅、理论、暗场工具、历史/统计、个人/订阅/设置、登录/Onboarding。
2. 同时检查“同一页面 Light↔Dark”和“相邻页面前↔后”，避免只看单张。
3. P0/P1 为 0 才能收口；P2 必须修复或由用户明确接受并登记。
4. 更新 UI 审阅报告、`tasks/UI-IMPLEMENTATION-SPEC.md` Changelog、`tasks/PROGRESS.md`；有真实返工才写 `tasks/FAILURE-LOG.md`。

**完成标准**

- [x] 6/6 全量矩阵成功，396/396 文件及页面身份审计通过。
- [x] 6 张联系表逐页目视完成，Light/Dark 均有结论。
- [x] 选中态、Pro、warning、success、物理颜色在目标页面族内语义一致。
- [x] 标准 build、相关单测/UI 测试和文档门禁通过。
- [ ] 真机至少复验 1 台 OLED iPhone 的 Dark 黑位、低亮度文字和封面暗部；若未执行，报告必须保留人工边界，不得写“全量验收完成”。

---

## 五、验收与保护矩阵

### 5.1 状态矩阵

| 维度 | 必测状态 |
|---|---|
| 外观 | Light、Dark |
| 设备 | 紧凑 iPhone、标准 iPhone、iPad mini |
| 选中控件 | selected、unselected、disabled、pressed（可稳定捕获时） |
| 账号/权益 | Guest、已登录 Free、Pro |
| 页面模式 | 内容页、固定暗场工具、内容→工具→返回 |
| 内容 | 空态、有数据、长标题、Pro 锁定 |
| 可访问性 | AX Large、High Contrast、Reduce Transparency、颜色外第二编码 |
| 封面 | 官方 12、练习 36、模版 12 |

### 5.2 保护清单

- 彩球与球号颜色。
- 黑底工具工作区、白色瞄准线、目标球本色路径、红/绿接触点。
- `HUDStyle` 的测量/可调/错误三通道，除非专项验证证明语义错误。
- Apple 登录黑/白品牌按钮。
- 历史/统计当前克制的中性层级；v55 完成前不抢改统计结构。
- DR-080/081 已确认的封面“无彩色罩、无主题叠字”规则。
- 页面功能、导航、数据、权益、登录与测试专用深链边界。

### 5.3 明确不采用的做法

1. 不全局搜索替换 `btAccent → btPrimary`。
2. 不重做 `btBG` / `btText` / `btPrimary` 基础资产色值来掩盖语义问题。
3. 不把所有绿色降饱和；球桌绿和照片绿是内容，不等于 UI 交互色。
4. 不把 Dark 改成 Light 的反色版本；Dark 已有更强连续性。
5. 不以“测试通过、截图有 66 张”代替页面身份和目视检查。
6. 不在封面素材未拍板时批量写正式 Asset Catalog。

---

## 六、并行改动冲突台账

| 路径 / 领域 | 当前并行来源 | v56 规则 |
|---|---|---|
| `StatisticsView*`、`AngleHistorySection.swift`、`btChartSeries` | v55 W1–W5 待执行 | v55 先行；v56 W3/W7 后接，不重排其信息架构 |
| `OnboardingView.swift`、`LoginView.swift` 品牌 Hero | D 聚焦系列待用户反馈 | v56 不接 D 图；W1 只修测试，W4 只做当前生产颜色复验 |
| 60 张 `cover*` Asset Catalog | 随机四选一试装，未视觉批准 | W6 必须先过 G1；禁止覆盖备份或把随机选择写成正式裁定 |
| `ProfileView.swift` | 本月概览真实统计已并行接入 | W4 只改视觉层级，保留自然月数据逻辑和 Query |
| `Colors.swift` / shared components | 多方案共同依赖 | 一批一个 owner；W0 台账后才允许改，禁止跨批顺带清理 |
| `ScreenshotTourUITests.swift` | v51 W5 仍在收口全量巡游 | W1 先同步当前 v51 最终版本，再修身份断言，避免覆盖巡游修复 |

---

## 七、代码与证据锚点索引（2026-09-03 实读）

| 领域 | 已核实锚点 | 当前事实 |
|---|---|---|
| 基础 Token | `QiuJi/Core/DesignSystem/Colors.swift:5–41, 46–74` | 语义色齐全；Chip 仍硬编码 Light 黑 / Dark 白；`btChartSeries = btAccent` |
| 筛选 Chip | `QiuJi/Core/Components/BTFilterChip.swift:5–49` | 选中态按模式反相，正是 C-01 共享根因 |
| Toggle pill | `QiuJi/Core/Components/BTTogglePillGroup.swift:3–58` | Light 绿、Dark 近白，第二个跨模式分叉 |
| 分段 Tab | `QiuJi/Core/Components/BTSegmentedTab.swift:3–48` | 已使用绿色文字 + 2pt 下划线，可作导航选中样板 |
| 内容页消费点 | `TrainingHomeView.swift:1107–1151`；`DrillListView.swift:146–214, 292–317`；`AngleHomeView.swift:282–418`；`SettingsView.swift:97–111` | 同屏组合了多套选中语法 |
| 图标徽标 | `QiuJi/Core/DesignSystem/IconToken.swift:149–200` | 注释已规定 accent 仅商业化/强调、全屏最多一处；Profile 仍需按此收口 |
| 通用按钮 | `QiuJi/Core/Components/BTButton.swift:5–35, 175–190` | `goldFilled` 明确是 Pro CTA，应保留为商业化语义 |
| 个人页 | `QiuJi/Features/Profile/Views/ProfileView.swift:26–50, 178–264, 318–391, 460–491` | Guest 登录、accent 警告、黑金 Pro、菜单徽标和底部第二登录动作并置 |
| 登录页 | `QiuJi/Features/Profile/Views/LoginView.swift:11–80, 85–115, 141–190` | 当前为 Apple-only；手机号/微信仅说明；法律区按 URL 有链接或 fallback |
| 暗场共享件 | `QiuJi/Core/Components/BTShotPageChrome.swift:74–148, 152–237` | 暗场按钮已用白/绿/红角色，是受保护基线 |
| Solver Chrome | `QiuJi/Features/AngleTraining/Views/SolverStageChrome.swift:123–205, 207–240` | 固定黑底、Dark toolbar、品牌绿标题与暗场 chip |
| 理论共享件 | `QiuJi/Features/AngleTraining/Theory/TheoryPageChrome.swift:13–54, 56–100, 104–146` | 标题/编号稳定；误区卡仍以 accent 充当 warning |
| 封面共享层 | `QiuJi/Core/Components/BTAtmosphereLayer.swift:4–17, 83–124` | 照片默认可叠 12% 黑罩；底部渐变固定 45% |
| 官方计划封面 | `QiuJi/Core/Components/BTPlanCover.swift:3–55` | 已关闭 color wash、开启 neutral scrim，保留编号 |
| 自定义模版 | `QiuJi/Features/Training/Views/PlanListView.swift:400–455` | UUID 稳定映射 12 图池，封面同用 neutral scrim |
| 练习封面 | `QiuJi/Features/AngleTraining/Views/AngleHomeView.swift:488–535` | 36 个入口同用无彩罩照片、neutral scrim、编号与 Pro badge |
| 登录巡游入口 | `QiuJiUITests/ScreenshotTourUITests.swift:255–276, 2554–2580` | 点击个人页登录后立即返回并截图，缺少 Sheet 身份等待 |
| 旧登录断言 | `QiuJiUITests/ScreenshotTourUITests.swift:647–684` | 仍断言微信/手机号按钮和固定 staticText 法律链接 |
| 手机测试深链 | `QiuJi/App/RootView.swift:75–81` | `-v51.phoneLoginPreview` 仅测试可达，生产入口关闭 |
| 审阅真源 | `tasks/ui-reviews/UR-20260903-full-app-color-consistency.md:1–118` | Light 7/10、Dark 8/10；C-01–C-06 与证据边界已登记 |
| 旧设计决定 | `tasks/UI-IMPLEMENTATION-SPEC.md:302–317, 1088–1089, 1138` | DR-043 锁反相 Chip；DR-080/081 锁封面无彩罩/无叠字 |

> 行号用于 v56.0 初次制定时定位；执行每批前须重新 `rg` / 实读。代码漂移后以符号名与当前行号为准，不以本文旧行号强行套改。

---

## 八、排期前置与停止条件

1. **先 v55、后 v56 生产代码**：若 v55 仍在改统计视觉，v56 只可执行 W1 中与登录/截图身份相关且不冲突的部分。
2. **W1 先于视觉修改**：截图身份仍可能假通过时，不得进入 W2–W7 并宣称视觉复验有效。
3. **W6 单独受 G1 阻塞**：其他批次可继续；封面未拍板时不得改正式图片资产。
4. 任一批发现现有 DR 与本方案冲突，先在 W0 台账写明 supersede 关系，不静默改变历史规范。
5. 任一批需要改几何、轨迹计算、数据模型、登录产品能力或 Pro 权益，立即停止并另立问题集合。
6. 构建受无关并行改动阻塞时，记录具体文件/错误和本批定向测试结果；不得回退用户或其他会话的改动求绿。
7. 最终没有真机 Dark 证据时，只能写“模拟器全量通过，真机待验”，不得写“全量验收完成”。

---

## 九、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v56.8 | 2026-09-04 | W1–W7 自动化范围收口：保留原布局和 SF Symbol 轮廓，品牌绿不改；新增语义 token 与 Premium 拉丝材质 mask、弱绿筛选、Guest/Pro 层级和暗场共享 Chrome；6/6 矩阵 396/396、六张联系表、Light/Dark 品牌登录、高对比 Dark、AX XXXL Light 通过。Reduce Transparency 因 `simctl` 无受支持开关且候选偏好经 UIKit 验证未生效，与 OLED 真机一并转 H-28；60 张封面仍是未批准随机试装。v56 截图冻结指纹 `20f603c8dd6b6dbc83dc37821821550dd9cc8bd9e437793f6b919db43ed4b25f`；截图后并行 v54 仅改其独立 UI test，详见实施报告。 |
| v56.7 | 2026-09-04 | W1–W6 生产实现：拆分 Premium / warning / teaching / physics / data 语义，加入 `BTPremiumMaterialSymbol` 和 `btPremiumOnDark`，共享组件与 Profile/Subscription 收口；封面只做中性遮罩和 60/60 只读审计。 |
| v56.6 | 2026-09-04 | 用户确认 G0 并授权执行；W0 完成 DR-083、42 文件/114 命中的 accent 分类、24 文件/155 命中的硬编码颜色分类、v55/v51 owner 冻结、源码/截图基线及 Light/Dark 独立拉丝材质母版；未改生产 SwiftUI/Token/Asset Catalog |
| v56.5 | 2026-09-03 | 补 Dark 细拉丝香槟金目标图；两次失败稿暴露镂空金币/切面宝石漂移，最终方向明确为“生成材质参考 + SF Symbol mask 锁形”，仍未改代码 |
| v56.4 | 2026-09-03 | 用户裁定保留原星形/皇冠，只换更有质感的生成式金色；新增哑光、细拉丝、象牙金 3 张 Profile 对比，暂推荐细拉丝香槟金；自定义 Mark 退出实施，仍未改代码 |
| v56.3 | 2026-09-03 | 响应“换色仍不够”：新增 5 枚 Pro 自定义图标探索与 2 张 Profile 回填图；确认皇冠/星星模板感是独立问题，暂定“克制钻石轮廓 + 单球识别”继续迭代；生产必须确定性矢量重绘，仍未实施 |
| v56.2 | 2026-09-03 | 增加 3 张 Pro 金色目标图与对比度复核：品牌绿动态色保持不变；Pro 推荐炭黑 + 分层香槟金，冷淡沙金只作弱层，古铜金不进入实现；明确先拆 premium 语义再换色值，仍未实施 |
| v56.1 | 2026-09-03 | 基于 6 张真实截图目标态编辑图完成代码前校准：筛选改弱绿默认、金色允许稀疏身份用法、封面加入摄影系列分组/outlier 门槛；新增 G0 用户确认，仍未实施 |
| v56.0 | 2026-09-03 | 基于同源码 Light/Dark 66 页截图审阅，建立颜色角色、两类选中态、保护清单、截图身份修复、封面 G1 和 W0–W7 执行批次；仅方案，未实施 |
