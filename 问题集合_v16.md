# 问题集合 v16 — 练习 Tab 产品化（状态层 / 每日挑战 / 技能分 / 诊断 / 闭环补边）

> **本文件是本轮需求的真源。** 日期：2026-07-20　状态：⏳ 待拍板（D-v16-1～7）
> 来源：2026-07-20 会话——用户认可练习 Tab 功能/UI 现状，要求「产品性优化，让产品属性更强」；主控产出 6 个方向分析（P1–P6）后，用户指令 `/issue-collection-restructure` 整理成详细方案。
> 编号约定：条目 **P1–P6**；批次 **B1–B6**；拍板项 **D-v16-N**。
> 排期前置：**无**——v14 / v15 全部批次已收官（2026-07-20），`tasks/HUMAN-REQUIRED.md` 无 ⏳ BLOCKING 项。
> 与既有方案关系：不推翻 v7–v15 任何契约；新首页状态层落在 `AngleHomeView`（v14/v15 只碰过其 `learnEntries` 卡序，无结构冲突）。

---

## 〇、问题陈述（事实 / 推测分开）

### 核心诊断

练习 Tab 目前是「工具箱」而非「产品」：4 分类 × 23 入口的**静态目录**，首页不承载任何用户状态（无继续上次 / 无推荐 / 无成长感）。数据流单向：练习成绩流向记录 Tab 后不回流练习 Tab。`docs/01-产品愿景与定位.md` §4 明确「学练记闭环 + 数据驱动个性化」为护城河，当前练习 Tab 未兑现「数据回灌」半环。

### 事实（已读代码，2026-07-20）

- 首页 `AngleHomeView`：大标题 + 搜索 + 侧栏（全部/学/练/打/解）+ 双列网格；`AngleEntry` 为纯静态结构（glyph/title/subtitle/封面色/chip），无任何用户数据字段。
- 成绩真源 = SwiftData `AngleTestResult`（`date/actualAngle/userAngle/pocketType/quizType/errorMM`），quizType 7 种：`table2D/geometric/scene2D/scene3D/aimPoint/aimPoint2D/aimPoint3D`。写入方 4 个：`AimingQuizViewModel`（SceneAiming 2D/3D 共用）、`GeometricAngleViewModel`、`AimPointTrainingView`、`AimPointSceneTrainingView`；仓库 `LocalAngleTestRepository`。
- 聚合层已存在：`AngleHistoryViewModel`（时间/题型双筛、5 题分组趋势、**六档角度区间正确率 `rangeStats`**、`overallAverageError/bestScore/accurateRate`）+ `AngleHistorySection`（仅挂在记录 Tab `StatisticsView` 与历史日历）。
- 弱区自适应 `AdaptiveQuestionEngine`：UserDefaults 存 corner/side 各角度档近 10 次误差、按平均误差加权出题；**仅 `AimingQuizViewModel`（SceneAiming 家族）实例化**，Geometric / AimPoint 族不喂不用。
- 免费限次 `AngleUsageLimiter.shared`：20 题/天，跨测验页共享计数。
- 学→练导流 `PracticeCTA` 已接 5 个学页（AimingPrinciple/BallFeel/SpinAndEnglish/AimingCorrection/AimingMethods）；**无反向**（错题→学页）。
- 分享卡渲染器 `ShareCardImageRenderer`（v9 产物，361×480 契约 + 相册保存权限链路已通）可复用。
- 编程式导航可用：`AppRouter.anglePath: NavigationPath`，push `AngleRoute` 即达任意练习页。
- **不存在**：最近使用/继续上次的持久化、streak、技能分、每日挑战、诊断测试。`PracticeStorageKey` 为练习域 UserDefaults key 收口点（字符串原值冻结，新 key 在此登记）。
- 无后端排名数据源（自建 REST API 未做排行/分位接口）——分享卡**不可**出现「全国分位」类虚构数据。

### 推测（标注，非事实）

- 多数用户进 Tab 后只固定使用 2–3 个模式，目录长尾触达低——**无埋点验证**，故 P4（入口收敛）默认缓做（见 D-v16-6）。
- 技能分映射公式（误差→分）无现成口径，B3 需先数值草稿定标再实现（见 D-v16-4）。

---

## 一、需求回显（语义基准，不增删）

> 说明：P1–P6 为主控 2026-07-20 分析产出、用户以「整理成详细方案」指令承接；**逐条是否全做 + 优先序**列入 D-v16-1 拍板，未确认前本表为「候选全集」。

| # | 方向（原分析意） | 验收语义 |
|---|---|---|
| P1 | 首页加「我的状态层」：继续上次、弱项驱动的今日推荐、卡片轻量状态（最近练过/历史最佳） | 首页网格上方出现动态区（有数据时）：①最近一次练习卡（模式名+上次成绩+一键进入）；②弱项推荐卡（引用真实区间误差数据+目标页深链）；网格卡片带最近使用/最佳成绩角标；全空数据时不出占位噪声 |
| P2 | 技能度量：把测验产出归一为 2–3 个技能维度（角度直觉/瞄准点精度），可增长的分数+趋势 | 存在单一真源的技能分计算层（输入 `AngleTestResult` 全量，输出维度分+分段位/掌握度）；首页状态层与测验结算页展示分数与趋势；口径经 D-v16-4 拍板并有数值草稿留档 |
| P3 | 每日挑战：固定入口、混合 5 题、2 分钟完成、streak、可分享结算卡 | 首页有「今日挑战」常驻入口；进入后混合题型串打（复用既有出题/判分逻辑）；完成出结算页（得分+自我对比+连续天数）；分享卡走 `ShareCardImageRenderer` 契约保存相册；streak 断/续规则明确；**不出现虚构全国数据** |
| P4 | 入口收敛：2D/3D 合并为模式内切换或由推荐层降低目录决策压力 | 【缓做，见 D-v16-6】若做：同名 2D/3D 卡合并为单卡+页内视角切换，成绩仍按原 quizType 分记不迁移 |
| P5 | 闭环补边：练→学（错题结算挂理论深链）；解→打（解球结果存为可复练局面） | 练→学：测验结算/错题反馈处出现「为什么错了」入口，深链到对应学页（映射表拍板 D-v16-5a）；解→打：范围大，是否本轮做见 D-v16-5b |
| P6 | 首次诊断测试：60 秒 8 题混合，产出基线分+弱项报告，作为激活钩子与个性化种子 | 首次进练习 Tab（可跳过）触发诊断流程；完成后生成基线报告页（技能分口径复用 P2）；结果写入 `AngleTestResult`（专用 quizType）；跳过后有低打扰召回入口 |

**明确不做（本轮，除非拍板扩大）**

| 不做项 | 依据 |
|---|---|
| 任何需要后端新接口的功能（排行、全国分位、云端 streak） | 无排名 API；诚实红线：不虚构对比数据 |
| 改动既有测验的出题/判分/物理逻辑 | 本轮只做消费侧聚合与包装 |
| 改 `AngleTestResult` 既有字段语义或迁移历史数据 | 新增 quizType 枚举值走既有字符串扩展模式（含默认值兼容） |
| 训练/动作库/记录 Tab 的产品化 | 范围=练习 Tab；`AngleHistorySection` 在记录 Tab 的既有挂载不动 |
| P4 入口收敛（默认） | 推测未验证，先靠 P1 推荐层降决策压力；见 D-v16-6 |

---

## 二、代码现状与锚点索引（事实，已核实 2026-07-20）

### 2.1 首页与导航

| 项 | 锚点 | 现状 |
|---|---|---|
| 首页结构 | `QiuJi/Features/AngleTraining/Views/AngleHomeView.swift` | `pageHeader`+`searchBar`+`mainContent`（侧栏 76pt + `LazyVStack` 钉头网格）；L263–270 body；状态层插入点=`mainContent` 上方或网格首 Section 前 |
| 入口模型 | 同文件 `AngleEntry` L45–55 | 纯静态；加角标需扩展或旁挂状态查询 |
| 分类 | `PracticeSection` L59–93 | 学 8 / 练 6 / 打 4(+SIM 1) / 解 5 |
| 路由 | `AppRouter.anglePath`（`QiuJi/App/AppRouter.swift` L38）+ `MainTabView.angleDestination` | push `AngleRoute` 可编程直达；新页需在 `AngleRoute` + `angleDestination` 注册 |
| 导流组件 | `PracticeCTA`（AngleHomeView L532–571） | 学→练已通；可复用样式做反向深链卡 |

### 2.2 成绩与聚合数据

| 项 | 锚点 | 现状 |
|---|---|---|
| 成绩模型 | `QiuJi/Data/Models/AngleTestResult.swift` | quizType 字符串 + `errorMM`（瞄准点族），带默认值轻量迁移模式可循 |
| 仓库 | `QiuJi/Data/Repositories/Local/LocalAngleTestRepository.swift` + Protocol | `fetchAll()` 全量取 |
| 聚合 VM | `QiuJi/Features/AngleTraining/ViewModels/AngleHistoryViewModel.swift` | `rangeStats` 六档区间正确率（L157–177）、趋势分组（L142–153）——B1 弱项推荐与 B3 技能分的现成输入 |
| 弱区引擎 | `QiuJi/Features/AngleTraining/AdaptiveQuestionEngine.swift` | 仅 SceneAiming 喂数据（`AimingQuizViewModel` L113 独占实例化）——**不宜作为全局弱项真源**，见 D-v16-2 |
| 写入方 ×4 | `AimingQuizViewModel` / `GeometricAngleViewModel` / `AimPointTrainingView` / `AimPointSceneTrainingView` | 结算处=练→学深链与技能分展示的挂载点 |
| 限次 | `QiuJi/Features/AngleTraining/AngleUsageLimiter.swift` | 20 题/天；每日挑战是否计入限次见 D-v16-3 |
| key 收口 | `QiuJi/Core/PracticeStorageKey.swift` | 新增最近使用/streak/诊断标记 key 在此登记 |

### 2.3 可复用件

| 项 | 锚点 | 用途 |
|---|---|---|
| 分享卡渲染 | `QiuJi/Core/Components/ShareCardImageRenderer.swift`（361×480 契约、相册权限链路 v9 已通） | B2 挑战结算分享卡 |
| 空态/卡片/按压 | `BTEmptyState` / `BTDrillGridCard` 样式语言 / `BTPressableStyle` | 状态层卡片沿用设计系统，禁新字面量字号（SPEC §1.4 D6 红线） |
| 首启标记先例 | `hasCompletedOnboarding`（`RootView` / `AuthState`） | B4 诊断首次触发的实现参照 |

### 2.4 待定位（写明，不臆测）

| 项 | 说明 |
|---|---|
| 解→打「存为练习」承载 | `BankShotViewModel` 等 solver 有 `solutions` 但无局面序列化/收藏机制；若 D-v16-5b 拍板做，B5 开工前需先读 solver VM + `PositionPlayModels` 定持久化方案（可能触发 ADR） |
| 测验结算 UI 逐页差异 | 四写入方结算形态不同（HUD/卡片），B3/B5 挂载点需逐页确认，批内执行时定位 |

---

## 三、拍板项

| # | 问题 | 推荐 | 状态 |
|---|---|---|---|
| D-v16-1 | P1–P6 是否全做？优先序？ | 全做除 P4；顺序 P1→P3→P2→P6→P5（B1→B2→B3→B4→B5），P4 挂起待埋点/使用验证 | ⏳ |
| D-v16-2 | 弱项推荐数据源 | **用 SwiftData `AngleTestResult` 全量算区间弱项**（复用 `AngleHistoryViewModel.rangeStats` 口径，覆盖全部 7 种 quizType），不扩 `AdaptiveQuestionEngine`（其仅 SceneAiming 域，且 UserDefaults 非真源） | ⏳ |
| D-v16-3 | 每日挑战形态 | 5 题 = 角度类 3（geometric/scene2D 池）+ 瞄准点类 2（aimPoint 池）；成绩写 `AngleTestResult` 新 quizType `"dailyChallenge"`；**不计入** `AngleUsageLimiter` 免费限次（挑战是留存钩子，免费全量开放）；streak 本地存（`PracticeStorageKey` 新 key），断签即清零、无补签 v1 | ⏳ |
| D-v16-4 | 技能分口径 | 维度 2 个 v1：角度直觉（度误差族）/ 瞄准点精度（mm 误差族）；分数=近 N 题加权误差映射 0–100（公式实现前**先数值草稿定标**并留档 `build/v16-evidence/`，样例含空数据/单题/满数据）；显示带「基于最近 N 题」诚实标注 | ⏳ |
| D-v16-5a | 练→学深链映射表 | 角度类错题→「瞄准原理/瞄准方法」；瞄准点类→「瞄准点对照表/角度与瞄准」；映射硬编码 v1（枚举级，非配置化） | ⏳ |
| D-v16-5b | 解→打「存为练习」是否本轮做 | **缓做**（承载待定位、量级 L、可能触发 ADR）；B5 v1 只做练→学深链，解→打立项到下轮 | ⏳ |
| D-v16-6 | P4 入口收敛是否做 | **本轮不做**；若 B1 上线后仍要做，2D/3D 合并单卡+页内切换、成绩分记不迁移 | ⏳ |
| D-v16-7 | 执行模型（per `01-subagent-model-selection`） | 建议：B1/B2/B4/B5（页面域、条目明确）→ `cursor-grok-4.5-high-fast`；B3（技能分口径、横切定标）→ `claude-opus-4-8-thinking-medium`；委派时再按本规则向用户确认 | ⏳ |

---

## 四、批次表

依赖图：

```
B1（状态层+最近使用地基）──┬──> B2（每日挑战+streak+分享卡）
                          └──> B3（技能分模型）──> B4（诊断测试）
B5（练→学深链）独立，可与 B2–B4 任意并行（文件域=四测验结算页+学页 route，与 B1 首页无交集需合并时手核 AngleHomeView）
```

| 批次 | 范围 | 量级 | 依赖 | 模型 |
|---|---|---|---|---|
| **B1** | P1：新建 `PracticeActivityStore`（最近使用条目+时间+末次成绩摘要，UserDefaults 经 `PracticeStorageKey`）；23 入口进入时记账；`AngleHomeView` 状态层（继续上次卡 + 弱项推荐卡〔D-v16-2 口径，读 SwiftData〕）；网格卡角标（最近练过/最佳）；空数据零噪声 | M | D-v16-1/2 | 待 D-v16-7 |
| **B2** | P3：新 `AngleRoute.dailyChallenge` + 挑战页（复用既有出题/判分，D-v16-3 组题）；结算页（得分+自我对比+streak）；streak 存取；分享卡（`ShareCardImageRenderer` 契约）；首页状态层挂「今日挑战」入口 | L | B1（状态层挂载+记账复用）；D-v16-3 | 待 D-v16-7 |
| **B3** | P2：`PracticeSkillScore` 计算层（单一真源，输入 `AngleTestResult`，D-v16-4 口径+数值草稿先行）；首页状态层展示维度分+趋势；四测验结算页轻量展示（本局对维度分影响）；单测钉口径 | M | B1（展示位）；D-v16-4 | 待 D-v16-7 |
| **B4** | P6：首次诊断流程（8 题混合、可跳过、`PracticeStorageKey` 首启标记）；基线报告页（复用 B3 技能分）；结果写 `AngleTestResult`（quizType `"diagnostic"`）；跳过召回入口（状态层弱化卡） | M | B3（分数口径） | 待 D-v16-7 |
| **B5** | P5 v1：练→学错题深链（D-v16-5a 映射表；四测验结算处挂 `PracticeCTA` 同语言入口，push 学页 route）；解→打不做（D-v16-5b） | S–M | D-v16-5a；与 B2–B4 可并行 | 待 D-v16-7 |
| ~~B6~~ | P4 入口收敛 | — | 本轮不做（D-v16-6） | — |

**波次建议**：B1 串行先行（地基）；之后 B2 ∥ B5 可并行（B2 動首页状态层、B5 动测验结算页+学页，交集仅路由注册，合并手核）；B3→B4 串行。单批均控制在一次会话可执行+验收。

### B1 完成标准

1. 冷启动无任何成绩数据：首页与现状一致（无状态层噪声），截图对照。
2. 完成一次任意测验后回首页：出现「继续上次」卡（模式名+成绩摘要），点击直达该页；截图取证。
3. 弱项推荐卡引用真实区间数据（文案含区间与误差/正确率数字，与 `AngleHistorySection` 区间分析一致），点击直达推荐页；截图+口径交叉核对。
4. 记账 key 全部走 `PracticeStorageKey`；`make build` SUCCEEDED；新增 store 单测（记账/取回/空态）0 failures；既有练习 Tab UI 回归（首页巡游用例）通过。

### B2 完成标准

1. 首页「今日挑战」入口常驻；完成 5 题全流程截图（题型混合可见）。
2. 结算页：得分+与个人历史对比+连续天数；**全文案无全国/排名类虚构对比**（rg 审计）。
3. 分享卡保存相册走真实权限链路（模拟器冒烟通过）；渲染尺寸符合 361×480 契约。
4. streak 规则单测：同日重复不加、隔日 +1、断日清零（注入时钟测试）。
5. 挑战题不消耗 `AngleUsageLimiter` 限次（单测或日志证明）；`make build` + 既有测验回归单测 0 failures。

### B3 完成标准

1. 数值草稿先行留档 `build/v16-evidence/`（空数据/单题/大样本/极端误差样例，公式输出符合 D-v16-4 定标），实现与草稿一致。
2. 技能分单一真源层 + 单测钉死（口径样例断言）；四结算页与首页展示接通，截图取证。
3. 展示处含「基于最近 N 题」标注；`make build` SUCCEEDED；全量相关单测 0 failures。

### B4 完成标准

1. 清空首启标记后首次进 Tab 触发诊断（可跳过），完整 8 题流程截图。
2. 报告页展示基线技能分+弱项区间，口径与 B3 一致；结果以 `"diagnostic"` quizType 落库（单测断言）。
3. 跳过后不再强弹，状态层出现低打扰召回入口；`make build` + 回归通过。

### B5 完成标准

1. 四测验页错题/结算处出现深链入口，逐页截图；点击到达 D-v16-5a 映射学页（UI 测试断言导航标题）。
2. 映射表单测（题型→route 枚举全覆盖）；`make build` + 既有结算流回归 0 failures。

---

## 五、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v16.0 | 2026-07-20 | 立项：6 方向分析→P1–P6 回显、锚点全量读码核实、B1–B5 批次表与完成标准、D-v16-1～7 待拍板 |
