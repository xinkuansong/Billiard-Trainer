# 问题集合 v36 — 用户数据同步链路收口（上行字段 / 删除同步 / 下行恢复）

> **真源声明**：本文件是本轮需求真源（2026-08-11 立档，v36.1，状态：**拍板全清，可开工**）。
> 来源：用户问「用户数据存到服务器有什么变化吗」→ Orchestrator/Data Engineer 全链路只读走查（SwiftData 模型 → DTO → 同步队列 → 后端路由/schema）。
> **用户已拍板前提（2026-08-11）**：App **尚未上线**，现有服务器 MongoDB 中的数据**可以全部删掉** ⇒ 后端 schema 可做破坏性修改、无存量数据迁移/重推负担、无线上旧客户端兼容压力。
> **排期前置**：与 v35 相同——工作区停有大量未提交改动（v34 全案等），用户已选定「自行提交后再开工」路径。本轮改动面（`BackendSyncService` / `SyncQueueManager` / `backend/`）与未提交文件基本不重叠，仅 `TrainingDataEditorView.swift` 有未提交修改且 W2 可能触及，开工时以提交后的基线为准。

---

## 一、需求回显（语义基准）

用户诉求：后端代码长期未动，怀疑「要改的东西挺多」，要求把用户数据服务器存储的缺口全面收口。走查确认的问题全集：

| # | 问题 | 定性 |
|---|------|------|
| Q1 | **上行字段缺口**：客户端 SwiftData V2 起新增的 9 个成绩字段（`DrillSet.formationToken/formationName/unitLabel/passMade/passTotal/durationSeconds`、`DrillEntry.orderIndex/note/criteriaText`）从未进 `TrainingSessionDTO` 与后端 Mongoose schema ⇒ 服务器副本有损。`unitLabel` 丢失会让恢复数据**语义错误**（「局/次」被当「球」）。契约 §4.1 目标 schema 本地已实现，传输层脱节 | 数据完整性（最核心） |
| Q2 | **删除不同步**：本地删除训练（两处入口）只删 SwiftData 不通知后端；即使入队也没用——`SyncQueueManager.uploadItem` 忽略 `operation` 字段，且实体查不到时 `return true` 直接出队；后端 `DELETE /training-sessions/:id` 用 Mongo `_id`，客户端只有 `clientId` 根本调不了。删除确认弹窗写「删除后无法恢复」，实际服务器上永远活着 | 正确性 bug + 隐私预期 |
| Q3 | **下行恢复缺失**：`fetchSessionsAfter` 定义后全仓库零调用方；服务器目前是纯写入端备份，换机/重装无任何代码把数据拉回 SwiftData。DTO 自定义解码器、后端 GET 路由都已备好，缺 DTO→实体重建、触发时机、与本地合并去重 | 功能半成品 |
| Q4 | **队列健壮性**：`SyncPendingItem` 无重试计数/失败分类，永久失败项（服务端 4xx）每次 app 激活无限重试；`processQueue` 里 `try? context.save()` 吞错误（撞 FL-029） | 健壮性 |
| Q5 | **双端对齐无门禁**：DTO CodingKeys ↔ Mongoose schema 字段清单没有任何机器校验，本次脱节全靠人肉发现，V4 时代必重演 | 流程护栏 |

**明确不在本轮范围**：
- `AngleTest` 链路——v29 W5 已双端对齐（`quizType`/`errorMM`/`sessionId`），零缺口，不动。
- `CustomPlan` / `UserActivePlan` / `DrillFavorite`——纯本地实体，从不上传，是否云同步属新需求，本轮不做。
- 手机验证码登录 / 微信 OAuth（H-15 推迟项）、Drill 内容 OTA——与用户数据存储无关。

**范围红线**：不做 SwiftData Schema V4（本地模型字段已齐，本轮只动传输层与后端）；不动 v34/v35 已定稿的剂量与计划语义。

---

## 二、拍板项

**裁定结果（2026-08-11，用户逐条拍板）**：

| # | 问题 | 裁定 | 状态 |
|---|------|------|------|
| D-v36-1 | 下行恢复（W3）是否本轮做 | **A. 本轮做**——未上线是零包袱窗口，登录功能隐含「换机不丢数据」承诺 | ✅ |
| D-v36-2 | 删除同步方案 | **A. 硬删**——后端加按 clientId 删除的端点；多设备墓碑是未来需求届时再升级 | ✅ |
| D-v36-3 | 双端对齐门禁（Q5）是否做 | **A. 做**——脚本比对 DTO CodingKeys ↔ Mongoose schema paths，进 `make verify-gate` | ✅ |
| D-v36-4 | 执行方式与模型 | **`plan-delegated-execution` 委派，全批 `claude-opus-5-thinking-medium`**（已写入批次表，后续会话不再询问）；主控独立验收 | ✅ |

---

## 三、批次表

> 模型已按 D-v36-4 裁定写入（全批 `claude-opus-5-thinking-medium`，委派执行 + 主控独立验收），后续会话不再询问。

| 批次 | 范围 | 量级 | 依赖 | 模型 |
|------|------|------|------|------|
| W1 上行字段对齐 | Q1：`TrainingSessionDTO`（`DrillEntryDTO`/`DrillSetDTO`）补 9 字段（编码直加；解码按既有约定 `decodeIfPresent`+默认值）；后端 `drillEntrySchema`/`drillSetSchema` 同步登记（给默认值、不加 required，⛔ Mongoose `strict:true` 不登记即静默丢）；扩展 `V29W5CognitiveToolSessionTests` DTO 编解码测试；ADR（命中「数据同步策略变更」强制清单）落 `tasks/phases/P2-data-layer.md`；契约 §4.1「待实现」状态更新、§8.13 处置闭环 | 小-中 | 无 | `claude-opus-5-thinking-medium` |
| W2 删除同步 + 队列健壮性 | Q2+Q4：后端删除路由（D-v36-2=A 硬删，`DELETE /training-sessions/by-client/:clientId`）；`SyncQueueManager.uploadItem` 真正分发 `operation`（delete 项不回查实体，payload 只需 clientId）；两处删除入口入队 `"delete"`；失败分类（网络/5xx 保留重试、4xx 记失败出队并打印可定位信息）；`processQueue` 的 `try?` save 改 `do/catch`（FL-029）；核实 `TrainingDataEditorView` 保存路径是否已被 `TrainingDetailView` L331/L350 的 update 入队覆盖（走查标记「待执行批确认」） | 中 | W1（同文件串行，避免冲突） | `claude-opus-5-thinking-medium` |
| W3 下行恢复链路 | Q3（D-v36-1=A，本轮做）：DTO→SwiftData 实体重建（含 9 个新字段还原）；按 `clientId` 幂等合并（重复拉取不重复建、本地较新不被覆盖——冲突策略执行批给提案）；触发时机 = 登录成功后全量 + 前台激活增量（`after=lastSyncDate`，锚点存 UserDefaults 或新增轻量记录）；`AngleTest` 下行一并接（GET 路由已备好）；配套单测 | 大 | W1、W2 | `claude-opus-5-thinking-medium` |
| W4 收尾 | Q5（D-v36-3=A）：对齐门禁脚本进 `make verify-gate`；服务器部署新后端 + **清空现有 MongoDB 用户数据集合**（用户已授权，H-14/H-16 服务器与 DB 均已就绪，部署步骤若需人工登录服务器则写入 `HUMAN-REQUIRED.md`）；PROGRESS/hub 回写 | 小 | W1–W3 全部 | `claude-opus-5-thinking-medium` |

依赖图：`W1 → W2 → W3 → W4`（纯串行；W1/W2 动同一批文件，W3 依赖字段与删除语义齐备，W4 是部署收口）。

### 各批完成标准

**W1**：
- `make build` SUCCEEDED；`V29W5CognitiveToolSessionTests`（扩展后）全过，至少含：① 编码后 JSON keys 含全部 9 个新字段；② 模拟旧后端回包（缺新字段）可解码且取默认值——防「解码失败→队列卡死」回归。
- 后端：`node --check` 全部改动 js 文件通过；node 一行脚本 require 模型打印 `drillSetSchema`/`drillEntrySchema` 的 `paths`，9 个新字段全部在列（后端无测试框架，此为最低机器验证）。
- ADR 已落 P2 文件；契约 §4.1 状态更新。

**W2**：
- 删除一条训练后：入队 `"delete"` 项、队列处理时调用删除端点、成功出队（单测覆盖，网络层用 stub/协议注入——现有 `APIClient` 是否可注入待执行批定，禁止真打服务器）。
- 4xx 出队不再重试、网络错误保留重试的分类逻辑有单测。
- `make build` SUCCEEDED；聚焦单测（本批新增 + `V29W5CognitiveToolSessionTests`）0 新增失败。

**W3**：
- 单测：同一批 DTO 拉取两次，实体数不翻倍（clientId 幂等）；`unitLabel="局"` 等字段经上行→下行往返后语义无损；已删除项不复活（配合 W2 方案）。
- 模拟器实跑：登录 → 造数据 → 删 App 重装（或清库重登）→ 数据回来，截图留证。
- `make build` SUCCEEDED；聚焦测试 0 新增失败。

**W4**：
- `make verify-gate` 含对齐检查且 FAIL 0；人为在 DTO 注入一个 schema 未登记的字段，门禁能 FAIL（对照实证，FL-029 第 3 条）。
- 服务器上新后端运行、旧集合已清空（人工步骤有记录）。
- 契约 / PROGRESS / hub 状态卡回写完成。

---

## 四、锚点索引（全部读码核实，2026-08-11）

| 锚点 | 位置 | 说明 |
|------|------|------|
| 上行 DTO | `QiuJi/Data/Services/BackendSyncService.swift` L6–63（`TrainingSessionDTO`，L48–51 有前向兼容解码惯例注释）、L104–120（User/Auth/Batch DTO） | W1 主战场 |
| 本地模型（字段已齐） | `QiuJi/Data/Models/DrillSet.swift` L10–21、`DrillEntry.swift` L9–14、`TrainingSession.swift`（DTO 已全覆盖）、`AngleTestResult.swift`（双端已对齐） | 本轮不动模型 |
| 后端 schema | `backend/src/models/TrainingSession.js` L3–19（set/entry 子 schema 只有 3+3 字段）、L21–37（kind 已登记）；`AngleTest.js`（已对齐） | W1 |
| 后端路由 | `backend/src/routes/trainingSession.js` L26–40（POST upsert by clientId）、L43–61（batch bulkWrite 幂等）、L63–90（PUT/DELETE 用 `_id`，客户端不可用）；`angleTest.js` L9–23（GET 已备好） | W2/W3 |
| 同步队列 | `QiuJi/Data/Services/SyncQueueManager.swift` L45–54（`uploadItem` 忽略 operation）、L56–68（实体缺失 return true）、L27–43（`try?` save、无失败分类）；`QiuJi/Data/Models/SyncPendingItem.swift`（无重试计数字段） | W2 |
| 入队调用点 | `LocalTrainingSessionRepository.swift` L16/L40（create/update 已入队）、L43–46（**delete 不入队**）；`TrainingDetailView.swift` L331/L350（update）、L363–370（**deleteSession 只删本地**）；`ToolUsageTracker.swift` L72、`LocalAngleTestRepository.swift` L33–36、`ActiveTrainingViewModel.swift` L889 | W2 |
| 下行半成品 | `BackendSyncService.swift` L154–160（`fetchSessionsAfter` 全仓库零调用方）；`QiuJiApp.swift` L80–86（前台激活处理队列，可挂增量拉取）、L94–108（`migrateAnonymousData` 上行迁移） | W3 |
| 现有 DTO 测试 | `QiuJiTests/V29W5CognitiveToolSessionTests.swift` L334–368（编码 keys 断言 + 旧回包解码），W1 直接在此扩展 | W1 |
| 后端无测试框架 | `backend/package.json`（scripts 无 test；deps 仅 express/mongoose/jwt 等） | 验收口径依据 |
| 契约 | `.kiro/steering/content-data-contract.md` §4.1（目标 schema「待实现」L235–258）、§8.13（「改 DTO 须同步后端 schema」处置备注 L619–620）、L43（数据流表） | W1/W4 回写 |
| 待定位 | `TrainingDataEditorView.swift` L142 注释「调用方负责 save」——其保存是否经 `TrainingDetailView` 的 update 入队覆盖，W2 执行时确认 | W2 |

---

## 五、批次验收记录

（执行时逐批填写）

---

## 版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v36.0 | 2026-08-11 | 立档：全链路走查定性 Q1–Q5，批次 W1–W4，拍板项 D-v36-1…4 待裁定。前提：未上线、现有库可清空（用户拍板）。 |
| v36.1 | 2026-08-11 | 拍板全清：D-v36-1=A（W3 本轮做）、D-v36-2=A（硬删 by clientId）、D-v36-3=A（对齐门禁）、D-v36-4=委派执行全批 `claude-opus-5-thinking-medium`。状态改「可开工」，仍受排期前置约束（等用户提交现有改动）。 |
