# 问题集合 v35 — 自定义训练计划全链路体验修复

> **真源声明**：本文件曾是自定义计划体验轮需求真源（2026-08-11 立档，v35.1，状态：**未执行，冻结**）。2026-08-26 起，入口/周次/用于今日语义改由 [`问题集合_v43.md`](问题集合_v43.md) 接管（覆盖本文件 Q1/Q2/Q3 与 W1/W3/W4）。本文件历史裁定保留备查，不再加批次、不再开工。
> 来源：用户实机走查「新建训练计划」反馈 + Orchestrator 全链路代码走查（入口 → 编辑器 → 选动作 → 保存/激活 → 今日安排）。
> **排期前置**：工作区停有大量未提交改动（v34 全案 + T01 返工 + 练习页三小修等，见 git status），且本轮涉及文件中 `ActiveTrainingView.swift` / `TrainingHomeViewModelTests.swift` 已有未提交修改 ⇒ **开工前先把现有改动 commit**（沿用 v34 分组提交惯例），或各批以含现状的 worktree 为基。

---

## 一、需求回显（语义基准）

用户原话两条 + 走查确认的问题清单：

1. **「现在新建训练计划的逻辑感觉有点问题」**——经全链路走查，确认问题集中在流程语义，非编辑器本身（动作增删/排序/遍数派生/编辑回填实现是扎实的）。
2. **「我的计划好像只能添加一个」**——代码无数量限制（`CustomPlan` 可无限建、两处列表渲染全部）。真因是**入口设计**：建完第一个计划后「创建计划」按钮（仅空态显示）消失，唯一可点的是计划卡片，而卡片直达**编辑器**（与新建页几乎同貌）⇒ 用户改了旧计划还以为在建新的。若用户实际另有症状（如建了第二个但看不到），拍板时补充，见 D-v35-5。

走查确认的问题全集（按严重度）：

| # | 问题 | 定性 |
|---|------|------|
| Q1 | 「每周训练天数」是假配置：自定义计划每天同一张完整动作表，`sessionsPerWeek` 只影响「第N周第M天」进位计数，不影响内容/排程/提醒 | 界面承诺未兑现（最核心） |
| Q2 | 非空列表无「新建计划」入口（首页「我的计划」tab 空态才有按钮），第二个计划只能从隐蔽的溢出菜单/列表页 toolbar 进 | 入口发现性（=用户反馈 2） |
| Q3 | 首页「我的计划」卡片点击直达编辑器，且**无激活入口**（激活只能去「训练计划」列表页） | 入口发现性 |
| Q4 | 编辑器返回即丢失全部编辑，无「未保存更改」拦截 | 数据损失风险 |
| Q5 | 保存按钮置灰不解释原因；名称 placeholder「我的训练计划」像默认名但实为必填 | 交互缺口 |
| Q6 | 「仅保存」成功后静默 dismiss，无任何反馈；用户不知道计划存哪了、也不知道**没被激活** | 交互缺口 |
| Q7 | 两条新建路径规则分裂：动作详情 sheet 名称可空有兜底、激活默认开**无确认**（静默替换当前激活计划）、`sessionsPerWeek` 固定 3 不可见；编辑器名称必填、激活走确认弹窗 | 语义不一致 |
| Q8 | `DrillPickerSheet` 无 Premium 标识与拦截：免费用户可把 premium drill 加进自定义计划（官方计划侧有完整 Pro 锁） | 商业化口径待拍板 |
| Q9 | `DrillPickerSheet` 只有搜索无分类筛选，全库平铺 | 挑选成本 |
| Q10 | 小项打包：允许重名计划无区分；保存失败错误文案硬编码「存储空间不足」；信息卡「N 组 M 动作」口径与设置 sheet「遍数/每轮球数」不同体系；同一动作重复添加被静默拒绝无反馈 | 打磨 |
| Q11 | 遍数最小粒度 = 完整剂量 ×1，多球形 drill 想「少练点」无出口 | v34 R9 有意设计，本轮默认不动（D-v35-4） |

**范围红线**：不动 v34 定稿的剂量语义（`roundsPerFormation` 倍数、球数由内容派生）；不做 Schema 迁移（Q1 若选轻方案不改模型，`sessionsPerWeek` 字段保留）。

---

## 二、拍板项（2026-08-11 用户已全部裁定）

**裁定结果**：
- **D-v35-1 = A**：保留控件，文案改真实语义「练满 N 次记为一周」+ 说明行；三处透出同步。
- **D-v35-2 = A**：Picker 加 Pro 徽章 + 免费用户选中拦截提示，与官方计划锁一致。
- **D-v35-3 = A + 语义确认**：用户原话「可以都练吧，相当于是加练的逻辑」，经回确认映射为——**不动单激活模型**；从动作库加练走现有「加入今日训练」（追加进当前激活计划，已支持）；「新建计划 + 设为今日」仍是替换制，但已有激活计划时**必须弹替换确认**（与编辑器一致）。⛔ 多计划叠加合并今日清单**不做**（用户已明确选否）。
- **D-v35-4**：遍数最小粒度本轮**不做**，挂遗留。
- **D-v35-5 = 确认**：「只能添加一个」= 入口发现性问题（建完第一个后找不到再建入口），W1 修。
- **执行方式**：委派子智能体逐批执行（`plan-delegated-execution`），**全部批次模型 = `claude-opus-5-thinking-medium`**（用户拍板，已写入批次表，后续会话不再重复询问）。

---

<details>
<summary>拍板前原始选项（存档）</summary>

- **D-v35-1（Q1，阻塞 W4）**：「每周训练天数」语义怎么收敛？
  - **A（推荐，小改）**：保留字段与控件，文案改为真实语义（如「练满 N 次记为一周」/「每周目标次数」），并在编辑器加一行说明「自定义计划每天显示同一张动作表」；三处透出文案（首页卡片「N 次/周」、列表卡片、动作详情 sheet「每周 N 练」）同步。
  - **B（大工程，本轮不建议）**：做真排程——把动作分配到周内各天/支持休息日。需要新数据结构 + 今日安排逻辑重写，建议另立专案。
  - **C**：从编辑器移除该控件（字段保留默认 3，仅作周进位）。
- **D-v35-2（Q8，阻塞 W5）**：premium drill 进自定义计划的口径？
  - **A（推荐）**：Picker 内加 Pro 徽章，免费用户选中时弹 paywall/提示（与官方计划锁一致）。
  - **B**：明确允许（记入决策，官方计划锁的旁路成为特性）。
- **D-v35-3（Q7，影响 W3）**：动作详情 sheet「设为今日训练」默认开、创建即静默替换当前激活计划——
  - **A（推荐）**：默认开保留，但**已有激活计划时**弹与编辑器一致的替换确认。
  - **B**：默认改关。
- **D-v35-4（Q11）**：遍数最小粒度问题本轮是否处理？**推荐：不做**，挂遗留观察用户反馈。
- **D-v35-5（Q2 解读确认）**：「只能添加一个」= 入口发现性问题（W1 修）。若实际症状不同（如建了第二个但列表/今日不显示），请补充描述，届时增补批次。
- **执行方式与模型**：本方案可由用户逐批自跑，或按 `plan-delegated-execution` 委派子智能体执行。若委派，按 01-subagent-model-selection 规则确认模型后填入批次表「模型」列。**风险推荐**：W1–W3 为页面域小批可用 `cursor-grok-4.5-high-fast`；W4/W5 含商业化与文案口径判断，建议 `claude-opus-5-thinking-medium` 或主控亲做。

</details>

---

## 三、批次表

> 依赖图：`W1 ∥ W2`（可并行/换序）→ `W3`（依赖 W2 定稿的保存/激活口径 + D-v35-3）；`W4` 仅依赖 D-v35-1；`W5` 仅依赖 D-v35-2。W4/W5 与 W1–W3 无代码冲突面（W4 触碰文案行、W5 只动 Picker），但涉及同文件（`TrainingHomeView` / `DrillDetailView`）建议串行合并。

| 批 | 范围 | 主要改动面 | 量级 | 依赖 | 模型 |
|----|------|-----------|------|------|------|
| **W1 入口与发现性** | Q2 + Q3：①「我的计划」列表非空时保留「+ 新建计划」入口（首页 tab 列表尾部一行，样式复用 `addDrillButton` 模式）；② 首页自定义计划卡片补激活能力（推荐：卡片加 context menu「设为当前计划/编辑/删除」或卡片内激活按钮，复用 `PlanListView` 的激活确认弹窗语义）；③ 编辑器标题差异保持（已有「新建计划/编辑计划」），卡片上加「编辑」暗示可选 | `TrainingHomeView.swift`（L515–582） | 小～中 | 无 | `claude-opus-5-thinking-medium` |
| **W2 编辑器保存/离开语义** | Q4 + Q5 + Q6 + Q10：① 未保存更改返回拦截（自定义返回按钮 + `confirmationDialog`「放弃更改/继续编辑」；需 `navigationBarBackButtonHidden` + 处理侧滑返回，注意 iOS 17 无官方 API 拦截侧滑，允许方案为「有未保存更改时禁用侧滑」）；② 名称空时以「我的训练计划」（重名时追加序号）兜底，`canSave` 只剩「至少一个动作」，置灰时改为可点并 toast/提示缺什么；③ 保存成功给出明确反馈——「仅保存」后 toast「已保存到我的计划」（实现允许：保存后短暂展示再 dismiss，或经 Notification 在上级页出 toast），「保存并激活」后 toast「已设为当前计划」；④ 保存失败文案改为透传错误类别，不硬编「存储空间」；⑤ 重复添加同一动作时给 toast 反馈（不改去重行为）；⑥ 信息卡「N 组 M 动作」改与设置 sheet 同体系口径（如「M 个动作 · 共 N 球」，具体以 `ResolvedDose` 既有文案函数为准，禁自造第三种口径） | `CustomPlanBuilderView.swift` / `CustomPlanBuilderViewModel.swift` + `CustomPlanBuilderViewModelTests.swift` | 中 | 无 | `claude-opus-5-thinking-medium` |
| **W3 两路径语义统一** | Q7：① 动作详情 sheet 激活语义按 D-v35-3 落地（推荐 A：替换已有激活计划时弹确认）；② 新建计划的默认名兜底规则与 W2 定稿一致；③ sheet 创建的计划 `sessionsPerWeek` 默认值与编辑器一致（均 3，确认后写注释钉死）；④ sheet 文案「每周 N 练」随 D-v35-1 口径同步 | `DrillDetailView.swift`（L670–788）/ `DrillTrainingPlanService.swift` + `DrillTrainingPlanServiceTests.swift` | 小 | W2、D-v35-3 | `claude-opus-5-thinking-medium` |
| **W4 sessionsPerWeek 语义收敛** | Q1 按 D-v35-1 落地（若 A）：编辑器控件文案 + 说明行；首页卡片 L559「N 次/周」、`PlanListView` 卡片、动作详情 sheet L720「每周 N 练」四处透出同步为新口径；`PlanProgressService` 注释补一句「仅进位计数」现状（代码不动） | `CustomPlanBuilderView.swift` / `TrainingHomeView.swift` / `PlanListView.swift` / `DrillDetailView.swift` | 小 | D-v35-1 | `claude-opus-5-thinking-medium` |
| **W5 Picker 改进** | Q8 + Q9：① premium 按 D-v35-2 落地（若 A：行内 Pro 徽章复用 `BTProBadge`，免费用户点选时出 paywall 提示，已在计划里的 premium drill 不追溯）；② 加分类筛选（复用动作库既有分类体系/`DrillCategory`，形式可为顶部分段或筛选菜单，与 `BTLibrarySearchBar` 风格一致） | `ActiveTrainingView.swift`（DrillPickerSheet L726–837）+ 相关 UI 测试 | 中 | D-v35-2 | `claude-opus-5-thinking-medium` |

### 各批完成标准（验收含可验证项）

**通用**：`make build` SUCCEEDED；`ReadLints` 0 新增；聚焦单测（`CustomPlanBuilderViewModelTests` / `DrillTrainingPlanServiceTests` / `TrainingHomeViewModelTests`）0 新增失败（既有基线失败原样保留，⛔ FL-027 不修绿）；改动文件范围与批次声明一致（git diff 核对）。

- **W1**：模拟器截图证明——已有 ≥1 个计划时首页「我的计划」tab 可见新建入口，点击进入「新建计划」页；卡片激活路径可用且弹确认后 `UserActivePlan` 正确替换（sqlite 或 UI 断言）；新增/更新 UI 测试覆盖「有计划时仍能新建第二个计划」全流程。
- **W2**：① 编辑器加动作后直接返回 → 弹拦截对话框（截图/UI 测试）；② 不填名称仅加动作可保存，落库名称为兜底名（单测）；③ 仅保存后有可见反馈（截图）；④ 重复添加同一动作出 toast（截图或 UI 测试）；⑤ 单测覆盖默认名兜底 + 重名序号。
- **W3**：已有激活计划时从动作详情新建并勾选「设为今日」→ 出确认弹窗（UI 测试/截图）；`DrillTrainingPlanServiceTests` 更新后全过。
- **W4**：四处透出文案 grep 无旧口径残留；截图走查编辑器 + 首页 + 列表 + 详情 sheet 四处新文案。
- **W5**：免费态（`-forceNonPremium`）Picker 中 premium drill 带徽章且选中出提示（UI 测试）；付费态（`-forcePremium`）可正常添加；分类筛选切换后列表正确过滤（UI 测试或单测过滤逻辑）。

---

## 四、锚点索引（2026-08-11 全部读码核实）

| 锚点 | 位置 | 说明 |
|------|------|------|
| 编辑器视图 | `QiuJi/Features/Training/Views/CustomPlanBuilderView.swift` | saveMenu L305–325；weeklyConfigCard L138–191；planInfoCard L110–134（「N 组 M 动作」L126）；无返回拦截；DrillSettingsSheet L339–438 |
| 编辑器 VM | `QiuJi/Features/Training/ViewModels/CustomPlanBuilderViewModel.swift` | canSave L58–60；save L174–208（错误文案 L205）；addDrill 去重 L96–105；activate L210–218 |
| 首页我的计划 | `QiuJi/Features/Training/Views/TrainingHomeView.swift` | customPlanBrowsing L515–539（空态才有创建按钮 L517–527；卡片→`customPlanEdit` L530）；溢出菜单新建 L99–116；customPlanCard L541–582（「N 次/周」L559，无激活入口） |
| 计划列表页 | `QiuJi/Features/Training/Views/PlanListView.swift` | toolbar「新建」L86–90；激活确认 L106–120；activateCustomPlan L296–304；deleteCustomPlan L306+ |
| 动作 Picker | `QiuJi/Features/Training/Views/ActiveTrainingView.swift` | DrillPickerSheet L726–837（无 premium 标识/gate、无分类筛选；`loadFallbackDrills` 全库平铺 L833） |
| 动作详情加入训练 | `QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift` | AddDrillToTrainingSheet L670–788（默认名兜底 L737–739；Toggle 默认开 L679；「每周 N 练」L720） |
| 计划服务 | `QiuJi/Data/Services/DrillTrainingPlanService.swift` | createPlan L79–113（`sessionsPerWeek` 固定 3 L88；activateAsToday 无确认 L99–101） |
| 今日安排解析 | `QiuJi/Features/Training/ViewModels/TrainingHomeViewModel.swift` | loadCustomPlan L226–275（L265 注释「每天同一张动作表，周/天只作推进计数」） |
| 周进位 | `QiuJi/Data/Services/PlanProgressService.swift` | `PlanSchedule.custom` L43–45（`sessionsPerWeek` 仅决定进位，无限循环） |
| 数据模型 | `QiuJi/Data/Models/CustomPlan.swift` | `CustomPlan.sessionsPerWeek` L8；`CustomPlanDrill.roundsPerFormation` L29（v34 倍数语义，⛔ 不动） |
| 既有测试 | `QiuJiTests/CustomPlanBuilderViewModelTests.swift`、`QiuJiTests/DrillTrainingPlanServiceTests.swift`、`QiuJiTests/TrainingHomeViewModelTests.swift`（后者有未提交改动） | 验收基线 |
| 订阅注入 | `SubscriptionManager` 的 `-forcePremium` / `-forceNonPremium`（X-v31-3 落地） | W5 验收用 |

---

## 五、批次验收记录

（执行时逐批填写：日期、执行者/模型、证据路径、返工轮次）

---

## 版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v35.0 | 2026-08-11 | 立档：自定义计划全链路走查问题 Q1–Q11 → 批次 W1–W5；待拍板 D-v35-1～D-v35-5 |
| v35.1 | 2026-08-11 | 拍板全清：D-v35-1=A / D-v35-2=A / D-v35-3=A（「加练」经回确认 = 追加进当前激活计划，⛔ 不做多计划叠加）/ D-v35-4 不做 / D-v35-5 确认入口发现性；模型全批 `claude-opus-5-thinking-medium`（用户拍板） |
