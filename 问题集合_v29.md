# 问题集合 v29 — 训练数据结构与计分口径改造

> **真源声明**：本文件是「训练数据结构、计分口径、记录链路」这一轮需求的**真源**，
> 供 `plan-batch-execution` / `plan-delegated-execution` 执行。
> **日期**：2026-08-06 **状态**：待执行（v29.1 审核修订完毕；原标「W1 执行中」经核无任何执行痕迹，回 ⏳）
> **模型路由定稿（2026-08-06，用户指定「都用 opus 5 medium」）**：**全批次 = `claude-opus-5-thinking-medium`**。
> 按 `01-subagent-model-selection` 视为已指定，后续会话不再重复询问。
> **契约真源**：`.kiro/steering/content-data-contract.md` v1.0（本轮所有裁定已回写其中，
> 二者冲突时以 steering 契约为准；本文件只负责「怎么分批做」）
> **排期前置**：~~W4/W6/W7 各自有一个待拍板项（见 §三），拍板前不得开工~~
> ✅ **2026-08-06 晚：D-v29-1～5 全部已裁定（均按推荐），无任何批次再被拍板卡住。**
> **与既有线的关系**：**精讲重写不在本轮**——23 条 C4 不一致与 11 条 profile 孤儿
> 划归 `问题集合_v25` W8 收尾后的 v26 全量迁移（见 §六）。

---

## 一 需求回显（用户拍板结论，语义基准）

本轮源于一次从 UI 细节出发、逐层下探到数据结构的排查。用户在过程中的裁定：

1. **录入原语只有计数型**（2026-08-06）。用户原话要点：走位不要去量化距离，
   「一是用户没办法去量，二是不好记录，还是最简单的比如用户练了 10 次成功了 5 次，
   或者说一组里有 5 次成功」。
   ⇒ 砍掉逐次三态判定与走位距离量化；「10 局赢 3 局」= made 3 / target 10 同构。
2. **历史记录存快照**（2026-08-06，原 D6）。内容改版不回溯改写历史记录。
3. **角度训练并入 `TrainingSession`**（2026-08-06，原 D5），加 `kind` + `sessionId`。
4. **工具活跃度要记**（2026-08-06）。用户原话：「记录工具的活跃度是可以的，比如时长这种，
   这样可以方便后期运维及产品优化」。⇒ `kind="tool"`，只记时长，不计分、不进准确率。
5. **球形几何真源 = 批量出片台录制序列**（2026-08-06）。精讲以序列为准重写。
6. **原始 UI 诉求**（本轮起点，尚未处置）：训练心得页有两个「完成」按钮，删下面那个并把
   跳过/完成整体下移；历史记录详情页那些按钮点了没有实际用途。

---

## 二 现状关键事实（2026-08-06 读码 + 实跑核实）

- **`TrainingSession.planId` 是死字段**：生产代码中**从未赋值**，永远 nil；
  `SwiftDataModelTests.swift:31` 还有断言 `XCTAssertNil(session.planId)` 固化了这个状态；
  `BackendSyncService.swift:33` 照常把 nil 同步上去。
  ⇒ **计划训练与自由训练在记录里无法区分**，这是「按完成推进计划」的硬前置。
- **计划推进完全没有实现**：`UserActivePlan.currentWeek/currentDay` 仅在 `init`（L18–19）
  赋值为 1；`TrainingHomeViewModel` L132–133/L172–173、`PlanDetailView` L255/L483 只读；
  全仓库零处写入。激活 12 周计划后用户永远停在第 1 周第 1 天。
- **SwiftData 无版本化 schema**：`ModelContainerFactory` L18 直接 `Schema(allModels)`，
  无 `VersionedSchema`、无 `MigrationPlan`。本轮加字段须同时建立迁移基建。
- **采集了但丢弃的字段**：`DrillSetData.duration`（`BTSetInputGrid.swift` L5–11 有该字段，
  `DrillRecordView` 实测每组用时）与 `ActiveTrainingViewModel.drillNotes`（L109 声明、
  L278/L291/L310 维护数组长度）——`saveTraining`（L562–599）两者都不写。
- **4 处 `try?` 吞错**：`GeometricAngleViewModel` L62、`AimingQuizViewModel` L203、
  `AimPointTrainingView` L89、`AimPointSceneTrainingView` L238，保存失败静默丢题
  （行号经 2026-08-06 复核；全仓无第 5 处 repository 吞错保存）。
- **缩略图烘焙污染仓库**：`DrillThumbnailBakeRunnerTests.swift` L24 硬编码绝对路径
  `/Users/song/projects/13.billiard_trainer/QiuJi/Resources/DrillThumbnails`，
  L28 `test_bakeAllThumbnails` 无任何开关，跑全量测试即重写全部 PNG。
  `make position-export`（`Makefile` L137–153）已是「make 目标驱动 test runner」的现成范式。
- **练习 Tab 计分现状**（清点 4 分区 25+ 入口）：「练」分区 6 页**全部已写** `AngleTestResult`
  （6 页收敛到 4 个写入点：角度预测→`GeometricAngleViewModel`；2D/3D 角度→`AimingQuizViewModel`；
  瞄准点→`AimPointTrainingView`；2D/3D 瞄准点→`AimPointSceneTrainingView`）；
  「学」9 页均为阅读类，不应计分；「打/解」四个**产品入口**均有进袋判定但未持久化——
  判定真源两处：`PositionPlayViewModel` L952–982（`objectPocketed`；编排台/自由走位/动作库试打
  共用此 VM）与 `PlanThreeViewModel` L1357–1387；`FreePlayView` L222–235 走
  `ShotFacts.pocketedKeys` → `rules.judge`（v29.1 更正：非 `objectPocketed` 字段；
  原引 `PositionPlayComposerView` L524–538 实为试打模式切换，非判定点）。
- **`verify-tutorials` 实跑（2026-08-06）**：总计 **211 FAIL**——
  C1 通过 22 / 失败 65（产物早于序列）、C2 大量回填不一致、C3 通过 69 / 失效 34、
  C4 通过 2 / 不一致 23 / 待迁移 52。**故「接门禁」不能先做，须先重出片。**
- **历史详情页 7 个按钮全为空实现**：`TrainingDetailView.swift` L197（编辑数据）、
  L206（复制到今天）、L216–220（生成分享图 / 移动到某天 / 编辑心得 / 导入为模版 / 删除）
  的 action 闭包均为 `{}`。`BTOverflowMenu` 本身转发正常，问题在调用方。
- **训练心得第二个「完成」**：`TrainingNoteView.swift` L45–53 的
  `ToolbarItemGroup(placement: .keyboard)`；L39–41 是 `bottomBar`；L54 `onAppear` 自动聚焦。

---

## 三 待拍板项

> 每项标注它卡住哪一批。**未拍板不得开工对应批次。**

| 编号 | 事项 | 卡住 | 推荐 |
|---|---|---|---|
| ~~**D-v29-1**~~（原 D2） | ~~达标线是否机读化、挂 drill 级还是球形级~~ | W4 | ✅ **已裁定 2026-08-06：按推荐**——机读，挂球形级，drill 级兜底；**内容侧 72 条暂不补**，W4 一律写 0 表示「未设定」，内容补齐另立批次。 |
| ~~**D-v29-2**~~（原 D1） | ~~跨 drill 聚合口径~~ | W6 | ✅ **已裁定 2026-08-06：按推荐**——删掉全局单一准确率，改按 category 分组。 |
| ~~**D-v29-3**~~（原 D7） | ~~计划推进规则~~ | W7 | ✅ **已裁定 2026-08-06：按推荐**——按完成推进，不按自然日；允许手动跳过/回退某天。 |
| ~~**D-v29-4**~~（新 D9） | ~~`tool` 时长是否经 `BackendSyncService` 上传~~ | W5 | ✅ **已裁定 2026-08-06：上传**（按推荐）。合规义务已转记 H-09 / H-12：隐私政策与问卷须声明「使用数据/产品交互」。 |
| ~~**D-v29-5**~~ | ~~历史详情页 7 个按钮各自去留~~ | W2 | ✅ **已裁定 2026-08-06：按建议**——实现「删除 / 生成分享图 / 编辑心得 / 编辑数据」四个；删除「复制到今天 / 导入为模版 / 移动到某天」三个按钮。 |

---

## 四 批次表

> 每批 = 一次子智能体会话可完成并验收的粒度。模型路由已定稿（见文件头：全批 `claude-opus-5-thinking-medium`）。

### W1 — 无风险缺陷修复（`try?` 吞错 + 缩略图烘焙脱离测试）

- **范围**：
  1. 4 处 `try? await repository?.save(result)` 改为真实错误处理：失败时置可见错误态并保留用户答案，
     禁止静默丢题（违反 `00-orchestrator.mdc` 工程底线第 3 条）。
     ⚠️ 范围钉死：仅这 4 处 repository 保存；仓内另有多处 `try? modelContext.save()`
     （CustomPlanBuilder / PlanDetail / PlanList / SyncQueue）**不在本批**，勿扩扫。
  2. 烘焙/出片类 runner 测试脱离常规测试。**范围经 2026-08-06 审核扩大——只 gate 缩略图一处
     无法满足完成标准 2**（scheme 无 testplan、无 skipped 列表，以下全部随全量测试跑）：
     - `DrillThumbnailBakeRunnerTests`：去掉 L24 硬编码绝对路径改环境变量注入；
       `test_bakeAllThumbnails` 在变量未设置时 `XCTSkip`；
     - **`V21W2BakeTests` / `V21W3BakeTests` / `V21W4BakeTests`**（审核新发现，最大遗漏）：
       无任何 gate，不仅写 `Resources/DrillThumbnails`，`patchAnimation` 还会改写 git 跟踪的
       `Resources/Drills/accuracy|cueAction/*.json`（W3 L199/L233、W4 L212/L246）——同款 `XCTSkip` gate；
     - `PositionPlaySequenceExportRunnerTests`：现仅软 gate（收件箱空则 skip；写 `build/` 不进 git），
       收件箱有残留时全量测试会再次全量出片长跑——加同款环境变量硬 gate；
     - `test_scanFeasibility` 与 `DrillBakeRunnerTests` 只读不写盘，但跑全量物理，一并 gate。
  3. 新增 `make thumbnails` 目标：仿 `Makefile` L137–152 `position-export` 范式——
     该范式的本体是 **`-only-testing` 限定 runner 类**（不是「设环境变量驱动」；
     env 只作测试内开关），make 目标内设置 gate 变量后调用。
- **红线**：不得为了让测试变绿而删断言；缩略图 PNG 内容本身不得变化。
- **依赖**：无。可立即开工。
- **完成标准**：
  1. `make build` SUCCEEDED（附输出）；
  2. **关键实证**：跑一次全量 `QiuJiTests` 后 `git status` 中 `QiuJi/Resources/`（含
     `DrillThumbnails/` 与 `Drills/*/*.json`）**零 diff**（附输出）——这是本批是否成立的唯一硬证据；
  3. `make thumbnails` 能正常产出：附脚本日志 + 产物 mtime 更新证明烘焙确实执行。
     （v29.1 更正：若渲染确定性使字节相同，`git status` 无 diff 属正常，**不得**以「PNG 出现 diff」
     为验收条件——原写法与红线「PNG 内容不得变化」自相矛盾。）
  4. 4 处保存失败路径各有一条单测覆盖（注入失败 repository，断言错误态被设置）。

### W2 — 历史详情页按钮 + 训练心得键盘完成键（用户原始诉求）

- **范围**：
  1. `TrainingNoteView.swift` L45–53：删除键盘工具栏的「完成」；`bottomBar`（L39–41）整体下移。
     ⚠️ 删除后用户失去显式收起键盘的入口——L29 已有 `.scrollDismissesKeyboard(.interactively)`
     可下滑收起，须在模拟器实测确认可用后再删，否则改为保留但调整视觉。
  2. `TrainingDetailView.swift` L197–221 七个空按钮，按 **D-v29-5 已裁定结果**处置：
     - ✅ **实现 · 删除**（L220）
     - ✅ **实现 · 生成分享图**（L216）—— `TrainingShareView` 已存在（`ActiveTrainingView.swift` L138–139 已在用）
     - ✅ **实现 · 编辑心得**（L218）—— 复用 `TrainingNoteView`
     - ✅ **实现 · 编辑数据**（L197）—— **须排在 W4 之后**（新增字段会改变可编辑面），
       故 W2 拆为 **W2a**（前三个 + 训练心得，不依赖 W4）与 **W2b**（编辑数据，依赖 W4）
     - ❌ **删除按钮 · 复制到今天**（L206）
     - ❌ **删除按钮 · 导入为模版**（L219）
     - ❌ **删除按钮 · 移动到某天**（L217）
- **红线**：不得保留任何空 action 闭包——要么实现，要么删按钮。
- **依赖**：W2a 无依赖；W2b 依赖 W4。
- **完成标准**：`make build` SUCCEEDED；模拟器逐个点击录屏/截图证明每个保留下来的按钮都有实际行为；
  全文件 grep 确认无 `Button {} label:` 残留；训练心得页截图确认只剩一个「完成」且位置符合预期。

### W3 — SwiftData 版本化 schema + 字段扩展 + ADR

- **范围**：
  1. `ModelContainerFactory` 引入 `VersionedSchema` + `SchemaMigrationPlan`（当前 L18 是裸 `Schema`）。
  2. 按 `.kiro/steering/content-data-contract.md` §4.1 加字段：
     `TrainingSession.kind`；`DrillEntry.orderIndex/note/criteriaText`；
     `DrillSet.formationToken/formationName/unitLabel/passMade/passTotal/durationSeconds`；
     `AngleTestResult.sessionId`。全部给默认值以走轻量迁移。
  3. ~~修正 `SwiftDataModelTests.swift:31` 的 `XCTAssertNil(session.planId)`~~
     **v29.1 审核撤销**：该断言属 `test_TrainingSession_defaultValues`（裸构造断言默认值为 nil），
     是合法的默认值测试；W4 只给计划训练的 session 赋 planId、不改默认构造，断言**不会失败**。
     保留不动，本子项作废。
  4. **写 ADR**（`00-orchestrator.mdc` ADR 强制触发清单：SwiftData Schema 结构性变更 + 新增 MigrationPlan）。
- **红线**：⛔ 迁移必须保证既有本地数据零丢失；⛔ 不得用「删库重建」绕过迁移；
  ⛔ 本批只动数据层，不碰任何 View / ViewModel 的业务逻辑。
- **依赖**：无（字段设计已在契约 §4.1 定稿）。
- **完成标准**：`make build` SUCCEEDED；**迁移测试**：构造旧版本 store（含 session/entry/set/angle 各若干条）
  → 迁移到新版本 → 断言全部旧数据仍在且新字段为默认值（附测试输出）；
  `SwiftDataModelTests` 全绿；ADR 落 `tasks/phases/` 对应 Phase 文件末尾。

### W4 — 训练录入链路接上新字段

- **范围**（`ActiveTrainingViewModel.saveTraining` L562–599 为核心改点）：
  1. 写 `session.kind = "drill"`；
  2. **补 `session.planId`**——计划训练时写入当前计划 id（修死字段，W7 的硬前置）；
  3. 落 `DrillSetData.duration` → `DrillSet.durationSeconds`（`BTSetInputGrid.swift` L5–11 已采集）；
  4. 落 `drillNotes` → `DrillEntry.note`，并**接上 UI**（当前 L109/L278/L291/L310 只维护数组长度，无绑定）；
  5. 写 `DrillEntry.orderIndex`（当前依赖数组下标，脆）与 `criteriaText` 快照；
  6. **球形维度**：多球形 drill 在录入时选择本组打的是哪个球形，写 `DrillSet.formationToken/formationName`。
     球形列表来自 `DrillTryoutBoardStore.formations(for:)`；单球形 drill 不出选择 UI。
  7. `unitLabel` 按 drill 语义写「球」/「局」/「次」；`passMade/passTotal` 本轮**一律写 0
     表示「未设定」**（✅ D-v29-1 已裁定：机读挂球形级，但内容侧 72 条暂不补达标线）。
- **红线**：⛔ 快照字段写入即冻结，**展示层禁止回查当前内容**（契约 §6.5 推论 2）；
  ⛔ 不引入 metric 类型枚举（契约 §5.1 推论 3）；⛔ 不做逐次三态判定与走位距离量化。
- **依赖**：W3。（D-v29-1 已裁定，无待拍板项。）
- **完成标准**：`make build` SUCCEEDED；模拟器完整走一遍训练（含一个多球形 drill 与一个计划训练）
  并截图；落库后用测试或调试输出证明 `planId`/`kind`/`durationSeconds`/`note`/`formationToken`
  **确有非默认值**（这是本批唯一硬证据，不得只看 UI）；`git grep` 确认无残留的 `duration` 丢弃路径。

### W5 — 练习 cognitive session + tool 活跃度

- **范围**：
  1. 「练」分区 6 页答题时创建/复用 `kind="cognitive"` 的 `TrainingSession`，
     并给 `AngleTestResult.sessionId` 赋值（写入点仍是既有 4 处 VM/View）。
  2. 历史无 `sessionId` 的 `AngleTestResult` 一次性回填——复用现有
     `AngleTrainingSession` 的「同 `quizType` 且间隔 < 30 分钟」推断逻辑。
  3. `kind="tool"` 活跃度：4 个**产品入口**（编排台自由走位 / `FreePlayView` /
     打一走二想三 / 动作库试打）记录日期与停留时长，**不产生 `DrillEntry`、不记任何成败**。
     ⚠️ v29.1 埋点口径：按**页面入口**计时埋点，勿在进袋判定点动刀——编排台/自由走位/试打
     共用 `PositionPlayViewModel`（判定在其 L952 一带），按判定点计会重复计数；
     `PositionPlayComposerView` L524–538 是试打模式切换，**不是**判定点。
  4. tool session 进 `SyncQueueManager` 上传（✅ D-v29-4 已裁定 2026-08-06：上传）。
  5. 顺带登记（v29.1 审核发现）：`AngleTestDTO`（`BackendSyncService` L46–59）未同步
     `quizType`/`errorMM`——cognitive 归并后若继续上传角度成绩，须补这两个字段或明确留档不同步。
- **红线**：⛔ `tool` 类严禁写入任何 made/target 或进袋结果（契约 §5.3）——
  引擎判定的进袋不是真实球技，混入统计即污染。
- **依赖**：W3。（D-v29-4 已裁定「上传」，本批无待拍板项。）
- **完成标准**：`make build` SUCCEEDED；答 3 题后落库查得 1 条 cognitive session 且 3 条
  `AngleTestResult.sessionId` 指向它（附输出）；回填脚本/逻辑对既有数据跑通并给出条数统计；
  用一个 tool 入口停留后落库查得 1 条 tool session 且 `drillEntries` 为空。

### W6 — 记录 / 统计页按 `kind` 读取改造

- **范围**：
  1. 历史页去掉 `AngleTrainingSession` 内存投影，改为直接读 `kind="cognitive"` 的真 session；
  2. 统计页（`StatisticsViewModel` L153–158 `overallSuccessRate` 为核心改点）按 `kind` 过滤，
     `tool` 一律排除；`drill` 与 `cognitive` 分开展示；
  3. 聚合口径按 ✅ D-v29-2 裁定实现：删全局单一准确率，按 category 分组；
  4. 日历上 `tool` 显示淡色活跃标记，周目标只算 `drill` + `cognitive`。
- **红线**：⛔ 展示历史记录一律用快照字段，禁止用 `drillId`/`token` 回查当前内容取名称或达标线。
- **依赖**：W4、W5。（D-v29-2 已裁定。）
- **完成标准**：`make build` SUCCEEDED；构造含三种 kind 的数据集，截图证明统计页数字与手算一致
  （附手算过程）；证明删除某 drill 的某球形后历史记录仍正常显示（快照生效的实证）。

### W7 — 计划推进

- **范围**：按 ✅ D-v29-3 裁定实现 `UserActivePlan.currentWeek/currentDay` 推进——
  **按完成推进**（不按自然日）：完成计划训练后进位，跨周进位；支持手动跳过/回退某天。
- **红线**：⛔ 依赖 W4 落地的 `session.planId`——在 planId 仍为 nil 时**不得**用「当天有训练」
  之类的启发式代替判定（那是脑补，会把自由训练误算成计划完成）。
- **依赖**：W4。（D-v29-3 已裁定。）
- **完成标准**：`make build` SUCCEEDED；模拟器完成一次计划训练后截图证明周/日推进；
  单测覆盖跨周进位与计划末尾边界。（原「删除 `SwiftDataModelTests.swift:31` 断言」
  经 v29.1 审核撤销——那是合法默认值测试，删它反而是「删断言求绿」，见 W3 注。）

### W8 — 重出片，让 C1 / C2 / C3 归零

- **范围**：`make position-export` → `import-engine-export-to-app.py`（**必须带 `--skip-json`**）
  → `make tryout-sync`，消化 C1 的 65 条「产物早于序列」、C2 回填不一致与 C3 的 34 条失效引用。
  顺带处理契约 §8.12 记录的 `drill_c073` 上游 7 杆 / Bundle 6 杆陈旧。
- **红线**：⛔ 不改任何序列文件（球形真源，人工录制）；⛔ 不手改产物目录；
  ⛔ **回填必须 `--skip-json`**（v29.1 审核新增）：该开关**并非默认开启**（脚本 L203–207
  `store_true`，帮助文案写「默认应开」但代码没开），漏传会重写 drill JSON 的 `videos[]`，
  把 v25 W1 已清空的视频引用全部写回、静默回退该拍板。建议本批顺手把脚本默认值改为 skip。
- **依赖**：无。但产物体量大（上次全量出片 ≈11G 视频 + 2.1G 静帧、76 条序列），需预留时间。
- **完成标准**：`make verify-tutorials` 的 **C1 / C2 / C3 全绿**（附完整输出）。
  C4 仍会 FAIL（属精讲，见 §六），本批不负责。

### W9 — 不变量补全与门禁

- **范围**：
  1. 新增契约 §7 的 I5（精讲 token ⊆ 序列 token）、I7（profile 与序列一致或标记退役）、
     I8（Bundle DrillBoards ⊆ 上游序列）、I9（每个登记 drill 至少 1 条序列，含 §8.5 十条豁免名单）；
  2. 把 I1–I4 中**已归零的** C1/C2/C3 接成阻塞门禁（pre-push 或 CI）；
  3. C4 因 23 条不一致未收敛，**登记为已知豁免并写明解除条件**（v26 完成后转阻塞）。
- **依赖**：W8。
- **完成标准**：新增检查项各有一条构造性用例证明能真正报错（不是永远 PASS 的空壳）；
  门禁在故意制造的不一致上确实拦截（附输出）；豁免清单写入契约 §7 与 §8。

### 依赖图

```
W1（无风险修复）──── 独立，可立即开工
W2a（UI 诉求）  ──── 独立
W8（重出片）    ──── 独立 ──→ W9（门禁）

W3（schema+ADR）──┬─→ W4（训练录入）──┬─→ W6（统计读取）
                  │                    ├─→ W7（计划推进）
                  │                    └─→ W2b（编辑数据）
                  └─→ W5（练习+tool）──────↑
```

### 批次一览

| 批次 | 依赖 | 待拍板 | 模型 | 状态 |
|---|---|---|---|---|
| W1 无风险修复 | — | — | `claude-opus-5-thinking-medium` | ✅ 2026-08-06（0 返工，合并 bf83be1；BakeRunnerGate 11 处 gate + make thumbnails 77/77 + 全量 634 后 Resources 零 diff；全量中 3 条失败经 main baseline 复现为既有问题） |
| W2a UI 诉求 | — | ✅ 已裁定 | `claude-opus-5-thinking-medium` | ✅ 2026-08-06（0 返工，合并 ddd9f59；三实现三删 + 心得单「完成」；范围外必要改动：HistoryCalendarView onDismiss 刷新） |
| W2b 编辑数据 | W4 | ✅ 已裁定 | `claude-opus-5-thinking-medium` | ✅ 2026-08-06（0 返工，波3 首合；TrainingDataEditorView 编辑 made/target/duration/formationToken，复用 BTSetInputGrid（抽出 BTFormationMenu，W4 UI 测试复跑无回归）；红线 3 达标线渲染兜底） |
| W3 schema + ADR | — | — | `claude-opus-5-thinking-medium` | ✅ 2026-08-06（0 返工，合并 6a58978；V1/V2 + 迁移计划 + sqlite table_info 反证删库重建；ADR-003 落 P2） |
| W4 训练录入 | W3 | ✅ 已裁定（pass 写 0） | `claude-opus-5-thinking-medium` | ✅ 2026-08-06（0 返工，合并 `00f7443`；sqlite 直查非默认值全齐 planId/kind/duration/note/formation/orderIndex；§六.4 A1 死分支已清 + 2 条测试前提更新为 manual 语义；unitLabel 逐条读 78 drill 量词派生） |
| W5 练习 + tool | W3 | ✅ 已裁定（上传） | `claude-opus-5-thinking-medium` | ✅ 2026-08-06（0 返工，合并 `9658e55`；cognitive 会话边界=同 quizType ≤30min 与历史推断同源；tool 4 入口页面级埋点、<5s 不落、note 存工具名；回填幂等；backend 2 schema 增量补 kind/quizType/errorMM/sessionId 防 mongoose strict 吞字段；AngleTestDTO 已补齐） |
| W6 统计读取 | W4、W5 | ✅ 已裁定（按 category 分组） | `claude-opus-5-thinking-medium` | ✅ 2026-08-06（0 返工，波3 末合 `1a32147`；历史页下线 inferAngleSessions 投影改读 kind=cognitive 真 session；统计删全局率改 category 分组 Σmade/Σtarget；tool 全排除（硬塞脏组仍 18/25 实证按 kind）；周目标只算 drill+cognitive；**留档**：drill category 仍靠 drillId 回查内容库，下架降级「综合」，彻底快照化需后续批 `DrillEntry.categorySnapshot`） |
| W7 计划推进 | W4 | ✅ 已裁定（按完成推进） | `claude-opus-5-thinking-medium` | ✅ 2026-08-06（0 返工，波3 次合；PlanProgressService 按完成推进 currentWeek/currentDay，末日收官不越界；手动跳过/回退菜单；planId 匹配当前计划才推进） |
| W8 重出片 | — | — | `claude-opus-5-thinking-medium` | ✅ 2026-08-07（0 返工，主树串行 `239de4c`+`0c9069a`；93/95 全量重渲（2 条 0 杆按 runner 设计跳过），C1 65→0、C2 137→0；C3 的 FAIL（stale）本就 0，「失效 34」系 WARN——legacy `_fN_` 引用须 v26 迁移消化，重出片物理上不可能生成；`--skip-json` 改默认开启（BooleanOptionalAction），带 `--no-skip-json` 实测会写回 66 个 drill 证明风险真实；序列真源零 diff、videos[] 未写回；c073 Bundle 更新为上游 7 杆；契约 §8.12/§8.14 已消化升 v1.4。**留给 W9**：C3 dead 计数纳入不变量、5 个 legacy drill（c005/c010/c012/c014/c030）列 v26 豁免） |
| W9 门禁 | W8 | — | `claude-opus-5-thinking-medium` | ⏳ |

**可并行**：W1、W2、W8 三者互不相交（Swift 缺陷层 / UI 层 / 内容产物层），可同波并行。
**必须串行**：W3 → W4/W5 → W6/W7（同一批数据模型与录入链路）。
**执行基线（v29.2 定稿）**：**各批基线 = `main`（`8cf4fbc`）**。v28 表层语法与 DR-061
序列演示均已包含在 main 中；`feat/v28-sequence-playback-controls` worktree 与分支已清理
（2026-08-06）。无悬挂代码分支阻碍任何批次。

---

## 五 锚点索引（2026-08-06 读码核实）

| 锚点 | 路径 | 批次 |
|---|---|---|
| 4 处吞错保存 | `AngleTraining/ViewModels/GeometricAngleViewModel.swift` L62；`AimingQuizViewModel.swift` L203；`AngleTraining/Views/AimPointTrainingView.swift` L89；`AimPointSceneTrainingView.swift` L238 | W1 |
| 无 gate 的烘焙测试（v29.1 新增） | `QiuJiTests/V21W2BakeTests.swift`（thumbnails L172–185 / patch JSON L197–210）；`V21W3BakeTests.swift` L199/L208/L233；`V21W4BakeTests.swift` L212/L221/L246；`PositionPlaySequenceExportRunnerTests.swift`（软 gate）；`DrillBakeRunnerTests.swift`（只读全量物理） | W1 |
| 缩略图烘焙 | `QiuJiTests/DrillThumbnailBakeRunnerTests.swift` L24（硬编码路径）、L28（全量烘焙）、L76（可行性扫描） | W1 |
| make 目标范式 | `scripts/Makefile` L137–153（`position-export`） | W1 / W8 |
| 训练心得键盘完成 | `Training/Views/TrainingNoteView.swift` L45–53；`bottomBar` L39–41；`onAppear` L54；滑动收起 L29 | W2 |
| 历史详情空按钮 | `History/Views/TrainingDetailView.swift` L195–221（L197 / L206 / L216–220） | W2 |
| 菜单组件（本身正常） | `Core/Components/BTOverflowMenu.swift` L43–45 | W2 |
| 分享视图先例 | `Training/Views/ActiveTrainingView.swift` L138–139（`TrainingShareView`） | W2 |
| SwiftData 容器 | `Data/Models/ModelContainerFactory.swift` L5–15（`allModels`）、L17–25（裸 `Schema`，无迁移） | W3 |
| 数据模型 | `Data/Models/{TrainingSession,DrillEntry,DrillSet,AngleTestResult,UserActivePlan}.swift` | W3 |
| 过时断言 | `QiuJiTests/SwiftDataModelTests.swift` L31（`XCTAssertNil(session.planId)`） | W3 / W7 |
| 训练保存 | `Training/ViewModels/ActiveTrainingViewModel.swift` L562–599（`saveTraining`）；`drillNotes` L109/L278/L291/L310 | W4 |
| 每组用时采集 | `Core/Components/BTSetInputGrid.swift` L5–11（`DrillSetData.duration`） | W4 |
| 球形来源 | `Core/PositionPlay/DrillTryoutBoardStore.swift` L60–89（`formations(for:)`；L103 为 `representative` 的 A1 死分支）、L138–142（token 解析） | W4 |
| tool 入口（v29.1 更正：按页面入口埋点，非判定点） | 判定真源：`PositionPlay/ViewModels/PositionPlayViewModel.swift` L952–982（三入口共用）；`PlanThreeViewModel.swift` L1357–1387；`PositionPlay/Views/FreePlayView.swift` L222–235（`pocketedKeys`） | W5 |
| 练习页路由 | `App/MainTabView.swift` L117–178；`AngleTraining/Views/AngleHomeView.swift` L137–246（练区入口表 L207–214） | W5 |
| 30 分钟会话推断（回填复用） | `History/ViewModels/HistoryViewModel.swift` L21（`AngleTrainingSession`）、L125（`gap = 30*60`）、L395–424（`inferAngleSessions`） | W5 / W6 |
| 统计口径 | `History/ViewModels/StatisticsViewModel.swift` L153–158（`overallSuccessRate`） | W6 |
| 计划读取点 | `Training/ViewModels/TrainingHomeViewModel.swift` L132–133/L172–173；`Training/Views/PlanDetailView.swift` L255/L483/L552；`Data/Models/UserActivePlan.swift` L10–11/L18–19 | W7 |
| 同步链路 | `Data/Services/SyncQueueManager.swift` L18；`BackendSyncService.swift` L33/L105/L127 | W5 / W7 |
| 内容校验脚本 | `scripts/verify_tutorial_sync.py`（C1–C4）；`scripts/verify_manual_formations.py` | W8 / W9 |
| 契约真源 | `.kiro/steering/content-data-contract.md`（§4.1 schema、§5 口径、§6.5 快照、§7 不变量、§8 偏差） | 全批 |

---

## 六 范围边界（本轮不做）

1. ⛔ **精讲重写不在本轮**。C4 的 23 条不一致与 11 条 profile 孤儿（契约 §8.1）需要
   `tutorial-authoring` / `tutorial-migration` 技能，与 `问题集合_v25` W8（模板基建 + 3 条试点，
   当前 `[ ]` 未完成）及其产出的 **v26 全量迁移方案**高度重合。本轮只把偏差清单登记清楚作为其输入。
2. ⛔ **52 条 legacy 精讲全量迁移**同上，归 v26。
3. ⛔ **profile 孤儿的最终处置**（标注退役还是删除）随精讲重写一并做，本轮不动
   `content/drill_profiles/`。
4. ⛔ **球形 token 规范不重做**（原 D8；✅ 用户已确认「不做」2026-08-06，契约 §9 已同步销掉）：token 已编码进 418 个
   `DrillTutorials` 文件名、`Previews` 目录名与 `Videos` 产物名，重做需重命名千级文件 + 重跑全量出片，
   收益仅为命名美观。唯一顺手项是清理 `DrillTryoutBoardStore.representative` 里已死的 `A1` 分支
   （全库已无 `__A[0-9]` 序列）——可并入 W4。
5. ⛔ 不碰动作库以外 Tab 的视觉层；不重做 v24 静帧契约；不动 v28 表层语法。

---

## 七 红线汇总（每批执行前回显）

1. ⛔ **诚实交付**：声称「通过 / 完成」必须附真实 build / test / 脚本输出。
   W1 的缩略图零 diff、W4 的落库非默认值、W8 的 C1–C3 全绿——这三项是本轮最容易被
   「看起来对了」蒙混的地方，必须给硬证据。
2. ⛔ **禁止喂绿测试**（FL-027）：断言不过先验证断言前提，不得加特例分支或删断言。
   ~~W3 的 `XCTAssertNil(session.planId)` 是该改的过时断言~~ **v29.1 更正**：经核该断言是
   合法默认值测试（裸构造断言默认 nil），W4 落地后不会失败——**保留不动**，
   改/删它反而违反本条红线。原定性本身就是「未验证断言前提」的反面案例。
3. ⛔ **数据零丢失**：W3 迁移必须有实证测试，不得以删库重建绕过。
4. ⛔ **快照不可回查**：展示历史记录禁止用 id 回查当前内容（契约 §6.5）。
5. ⛔ **tool 不计分**：引擎判定的进袋严禁进入任何准确率聚合（契约 §5.3）。
6. ⛔ **不动球形真源**：W8 只重出片，不改 `content/position_play/sequences/` 任何文件。
7. ⛔ **范围纪律**：§六列出的边界不得越界；范围要变先说再做。

---

## 八 版本记录

| 版本 | 日期 | 变更 | 来源 |
|---|---|---|---|
| v29.0 | 2026-08-06 | 首版：W1–W9 批次化；锚点全量读码核实；待拍板 D-v29-1~5；精讲重写明确划归 v25 W8 / v26 | 用户指令 `/issue-collection-restructure`，综合 2026-08-06 会话从 UI 细节下探至数据结构的全部讨论与裁定 |
| v29.2 | 2026-08-06 | 拍板全清：D-v29-1（机读+球形级，内容暂不补、W4 写 0）、D-v29-2（删全局准确率、按 category 分组）、D-v29-3（按完成推进+手动跳过/回退）、D-v29-4（tool 时长上传，合规义务转 H-09/H-12）均按推荐落定；D8 确认不做（契约销）；H-21 关闭；DR-061 分支合入 main。W1–W9 全部批次可按依赖图放行。 | 用户逐项拍板（2026-08-06 晚） |
| v29.1 | 2026-08-06 | 主控审核修订：**W1 范围扩大**（新发现 `V21W2/3/4BakeTests` 无 gate 写 PNG + 改写 git 跟踪 drill JSON，为最大遗漏；ExportRunner 补硬 gate；完成标准 2 扩到 `Resources/` 全域、标准 3 与红线矛盾修正；范式更正为 `-only-testing`）；**撤销 W3/W7 对 `SwiftDataModelTests:31` 的错误定性**（属默认值测试，W4 后不会失败，保留不动）；**W8 补 `--skip-json` 红线**（防静默回退 v25 W1 清空的 `videos[]`）；W5 锚点更正（Composer 判定在共享 VM、FreePlay 走 `pocketedKeys`）+ D-v29-4 降为仅阻塞同步项 + 登记 `AngleTestDTO` 缺 `quizType`/`errorMM`；W1 状态回 ⏳；补执行基线声明；§六.4 D8 与契约不一致提示 | 本会话审核：3 个 grok-4.5 探查子智能体核 24 条锚点（约 21 条属实）+ 主控亲验 P1 三处 + `make verify-tutorials` 实跑 211 FAIL 与 §二逐项吻合 |
