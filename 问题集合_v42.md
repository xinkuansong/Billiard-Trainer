# 问题集合 v42 — 按训练意图整课重写描述 / 要点 / 达标 / 精讲

> **文件墙（2026-08-24）**：**本文件是本轮需求真源**（v42.1，状态：**立档完成，W0 ✅，待开 W1**）。`问题集合_v41.md` 只保留 W0–W12 收官记录，**不再改语义、不再加批次**。四字段成色以本文件为准重做。
> 来源：用户看完 v41 描述/要点后认为改得不够彻底；又以五分点（c023）为样张指出「意图点名的杆法必须上屏」，并裁定：**全库**把 `description` / `coachingPoints` / `standardCriteria` / `tutorial` 按意图**整课重写**（精讲含开头和逐杆），写精讲必须看渲染图。立档示范已用现网 c023（达标仍锁中杆，W6 必须改掉），**不再单开试点批**。
> **排期前置**：意图表「完善」列已过目（v41 W3）；空意图课与 c062 / c059 仍整课跳过。序列 / 出片 / `shotIntent` / **建议训练量（剂量）** 本轮不动。达标**正文**按意图改；组数/次数等计数量词不新编。
> **模型**：W0 = 主控本线（本会话已完成）。W1–W7 派发时按 `.cursor/rules/01-subagent-model-selection.mdc` 确认；批次表预填为立档默认——**W1 推荐** `claude-opus-5-thinking-medium`（准度阶梯 + v40 W1 四条解冻，判断密）；**W2–W6 推荐** `cursor-grok-4.6-high-fast`；**W8 回归 = 主控**。首次派发若用户未另指定，即按此列，写入后后续会话不再问。

---

## 一、需求回显（语义基准）

用户原话与已拍板结论（不增删语义）：

1. **v41 不够彻底**——描述/要点去剂量之后，仍不像按意图写的课；不要再给 v41 打补丁。
2. **全库四字段整课重写**——`description`、`coachingPoints`、`standardCriteria`（详情页「训练要求 → 达标目标」）、`tutorial`（开头 **和** 逐杆）。中间档仍可留空。
3. **写精讲必须看图**——图 = JSON `image` 同名的 PNG 母版。digest 取值，看图证伪，禁止从图估角度/距离。
4. **五分点是质量锚，不是「已做完可跳过」**——立档示范用现网 c023 的描述/要点/精讲；**达标现网仍写锁中杆，是反例，W6 必须按意图改掉**。
5. **意图点名的杆法术语必须上屏**——去剂量 ≠ 去术语（D-v41-8）。达标里的杆法锁死若与意图打架，跟意图改达标，不上报了事。
6. **主题跟意图，数字跟序列**——切角/距离/杆速跟 digest；冲突上报，禁止硬圆。

| # | 需求 | 细化口径 | 状态 |
|---|------|----------|------|
| R1 | **整课重写四字段** | 描述 / 要点 / 达标 / 精讲按意图表「完善」列重写；精讲含开篇与「第N杆」。推翻 D-v41-6 与「精讲另批」 | ⏳ |
| R2 | **看 PNG 写精讲** | 路径 `QiuJi/Resources/DrillTutorials/<image>.png`，`<image>` 取该节 JSON 字段。HEIC 不是读图主路径。孤儿帧（如 `Snipaste_*`）禁止当本课图 | ⏳ |
| R3 | **质量锚 = c023** | 描述写主练+形差+意图杆法名；要点第 2 条只写「只改什么」；精讲按实测杆法写。达标不得再锁中杆。见 §四 | ⏳ |
| R4 | **v40 W1 四条解冻** | c013 / c032 / c053 / c063 的 `tutorial` **这次要改** | ⏳ |
| R5 | **跳过课不动** | 空意图 + c062 + c059，四字段本轮不改 | ✅ 名单锁定 |
| R6 | **训练要求一起改** | 改的是卡内「达标目标」=`standardCriteria`。卡内「建议训练量」不动 | ⏳ |

**术语对照（FL-026）**

| 你的原话 | 采用定义 | 状态 |
|----------|----------|------|
| 「训练意图」 | 研究表「完善」列；一行一个球形。不是 `shotIntent` | ✅ |
| 「动作描述 / 训练要点 / 精讲」 | `description` / `coachingPoints[]` / `tutorial` | ✅ |
| 「训练要求」 | 详情页卡标题。卡内两块：① **达标目标** = `standardCriteria`（本轮要改）；② **建议训练量** = 剂量解析（本轮不改） | ✅（D-v42-6） |
| 「渲染图 / 配图」 | `DrillTutorials/<image>.png` 母版，与 JSON `image` 同名 | ✅（D-v42-2） |
| 「彻底重写」 | 四字段整课按意图重写，不是只改开头或只去剂量 | ✅（D-v42-1） |
| 「角袋就是底袋」 | 袋口只分中袋 / 底袋（=角袋）。不做全表换词 | ✅（继承 D-v41-1） |
| 「立档用了五分点」 | c023 描述/要点/精讲 = 成色样张；达标现网是反例 | ✅ |

**本轮继承的拍板（不重辩）**

| 编号 | 裁定 |
|------|------|
| D-v41-1 | 袋口只分中袋 / 底袋（=角袋） |
| D-v41-2 | c062「删除」跳过，不删 JSON |
| D-v41-3 | 空意图行不 AI 补写；对应课下游整课跳过 |
| D-v41-5 | **部分被 D-v42-6 覆盖**：不再「达标整段不动」。组数/次数等计数量词仍不新编 |
| D-v41-7 | `description` 几乎只写主练+形差；档数、切角度数、杆速 m/s、台面「颗球」距退出描述和要点（达标里的次数/组数保留，见 D-v42-6） |
| D-v41-8 | 意图点名的杆法术语必须进描述/要点/**达标**；与示范冲突时跟意图，上报不硬圆 |

**本轮新裁定**

| 编号 | 裁定 |
|------|------|
| D-v42-1 | 精讲整课重写（开篇 + 逐杆）。中间档可留空。推翻 D-v41-6 |
| D-v42-2 | 写精讲必须读 JSON `image` 同名 PNG；digest 取值、看图证伪 |
| D-v42-3 | v40 W1 四条（c013 / c032 / c053 / c063）`tutorial` 解冻，本轮要改 |
| D-v42-4 | 跳过课名单不变（见下） |
| D-v42-5 | 质量锚 = 现网 c023 的描述/要点/精讲；达标现网锁中杆是反例，W6 改 |
| D-v42-6 | 「训练要求」一起改 = 只改 `standardCriteria` 正文（杆法/合格判据跟意图）。「建议训练量」与 `sets` / 计划剂量不动。组数、次数、「10 次中至少 N 次」不新编及格线；与意图/序列事实矛盾则上报 |

**跳过课（11，四字段不动）**

空意图：c006、c007、c008、c043、c002、c055、c061、c066、c067。  
删除标记：c062。未开放：c059。

**本轮要改的 72 课**（83 − 11）

**明确不在本轮范围**

- 改序列 / 出片 / 配图文件 / `shotIntent` / `animation` / 计划 JSON / I13 / `nameZh` / 「建议训练量」剂量。
- 改意图表「训练意图」原文列（AI 只动「完善」列；本轮默认连完善列也不改，除非发现与序列事实矛盾须上报）。
- 用脚本生成或回写 `tutorial` 正文（FL-032）。
- 重写定杆五问（引入课仍是 c010；后续课一句前置 + 新层）。
- 把 digest 的形态判定 / `initial` 占位直接写进正文（PD-029）。

---

## 二、批次表

| 批次 | 范围 | 课数 | 量级 | 依赖 | 模型（立档默认） |
|------|------|------|------|------|------------------|
| **W0** | 立本文件；v41 移交；进度/Hub 回写 | — | 小 | — | 主控本线 ✅ |
| **W1** | 准度：c001、c011、c012、c013、c032、c033、c052、c053、c063、c072、c076、c077、c078 | 13 | 大 | W0 | `claude-opus-5-thinking-medium` |
| **W2** | 杆法：c003、c004、c014、c015、c016、c017、c018、c020、c021、c073、c074、c075 | 12 | 大 | W0（可与 W1 并行） | `cursor-grok-4.6-high-fast` |
| **W3** | 分离：c024–c031、c083、c084 | 10 | 大 | W0（可与 W1 并行） | 同上 |
| **W4** | 走位前半：c005、c034–c040 | 8 | 大 | W0 | 同上 |
| **W5** | 走位后半：c041、c042、c079–c082 | 6 | 中 | W0 | 同上 |
| **W6** | 基本功 + 力度：c009、c010、c022、**c023（核回退）**、c044–c051 | 12 | 大 | W0 | 同上 |
| **W7** | 特殊 + 综合：c054、c056、c057、c058、c060、c064、c065、c068–c071 | 11 | 大 | W0 | 同上 |
| **W8** | 回归：gate + 抽图 + 抽意图术语 + 抽检对照 c023 | 全库抽 | 中 | W1–W7 | 主控 |

**依赖图**

```
W0 ✅
 ├─ W1 准度
 ├─ W2 杆法     ┐
 ├─ W3 分离     │ 目录批互不改同一 JSON，可并行
 ├─ W4 走位前   │
 ├─ W5 走位后   │
 ├─ W6 基本功+力度（含 c023 核）
 └─ W7 特殊+综合
         └─ W8 回归
```

横切（意图表、规范、所有权表、门禁脚本）W0 已锁，目录批不要回头改共享技能语义。

### 每批完成标准（W1–W7 共用）

1. **逐课打开 JSON 手改**四字段（`description` / `coachingPoints` / `standardCriteria` / `tutorial`）。禁止脚本灌正文。
2. 每课：读意图表该课各行「完善」列 → 跑 `scripts/tutorial_digest.py` 对应序列 → **Read 该课每节 `image` 同名 PNG** → 再写。缺 PNG 标「待定位」并上报，不改用 HEIC、不从图估几何。
3. 描述 / 要点符合 D-v41-7 + D-v41-8；达标符合 D-v42-6（杆法/判据跟意图，计数量词不新编）；精讲符合 v40 规范（`docs/research/20260818-v40-精讲内容规范.md`）+ 所有权表引入/后续课纪律。角度只在 caption，正文不报度数。
4. 意图与**示范/序列**冲突：**上报，不硬圆**。达标正文与意图打架时**改达标**，不要再当「上报即可」。已知清单见 §五。
5. 本批课 `make -f scripts/Makefile verify-gate` **FAIL 0**。⛔ 完成标准**不写**「全量测试后工作区零 diff」（PD-027）。
6. 交付列出：改了哪些 id、缺图、冲突上报、未改的跳过课（确认未误碰）。

### W8 完成标准

1. `make -f scripts/Makefile verify-gate` FAIL 0。
2. 抽检 ≥6 课：c023（锚）+ c013（解冻）+ 每已完成目录至少 1 课。对照 §四：意图术语在描述/要点/**达标**；c023 达标不得再写锁中杆；精讲逐杆与 digest/图不打架。
3. 确认 11 堂跳过课四字段相对本轮基线无改（可用 `git diff -- QiuJi/Resources/Drills/**/drill_c00{2,6,7,8}.json` 等核对名单）。
4. `make build` 能跑则跑；跑不了如实写「未验证」。

---

## 三、锚点索引（2026-08-24 读码核实）

| 锚点 | 路径 / 位置 | 核实 |
|------|-------------|------|
| 意图表 | `docs/research/20260819-动作库球形训练意图.md`（原文列 / 完善列） | 真 |
| 精讲规范 | `docs/research/20260818-v40-精讲内容规范.md` | 真 |
| 概念所有权 | `docs/research/20260818-v40-概念所有权表.md` | 真 |
| 课清单 | `QiuJi/Resources/Drills/index.json`（8 目录，83 id） | 真 |
| 四字段模型 | `QiuJi/Data/Services/DrillContentService.swift` L80–85（`description` / `coachingPoints` / `standardCriteria` / `tutorial`） | 真 |
| 描述上屏 | `QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift` L72 | 真 |
| 要点卡 | 同文件 L315「训练要点」 | 真 |
| 训练要求卡 | 同文件 L370–390：标题「训练要求」；「达标目标」绑 `standardCriteria`；其下「建议训练量」绑剂量，本轮不改 | 真 |
| 质量锚 | `QiuJi/Resources/Drills/fundamentals/drill_c023.json`：描述 L13、要点 L14–18、精讲已按意图；达标 L19「杆法锁中杆」是反例，W6 改 | 真 |
| PNG 母版 | `QiuJi/Resources/DrillTutorials/<image>.png`（gitignore，不进包）。c023 例：`drill_c023_manual01_initial.png` | 真 |
| 发布图 | `QiuJi/Resources/TutorialFigures/*.heic`（进包；**不是**读图主路径） | 真 |
| 出片备用 | `build/position_play_export/` 下部分 `sNN_still.png`（仅当母版缺且文件名能对上时备用） | 真 |
| 序列 | `content/position_play/sequences/drill_cNNN__*.json` | 真 |
| digest | `scripts/tutorial_digest.py`（几何取值唯一脚本入口） | 真 |
| 门禁 | `make -f scripts/Makefile verify-gate`（I10 等） | 真 |
| 母版 vs 发布 | `.kiro/steering/content-data-contract.md` §6.4 D-v25-14（约 L607–612） | 真 |
| 技能 | `formation-training-intent`（描述禁剂量）；`tutorial-authoring`（已写「看 PNG 证伪」） | 真 |

**目录批文件墙（执行者只改本批 JSON）**

| 批 | 目录 |
|----|------|
| W1 | `QiuJi/Resources/Drills/accuracy/drill_c{001,011,012,013,032,033,052,053,063,072,076,077,078}.json` |
| W2 | `…/cueAction/drill_c{003,004,014,015,016,017,018,020,021,073,074,075}.json` |
| W3 | `…/separation/drill_c{024,025,026,027,028,029,030,031,083,084}.json` |
| W4 | `…/positioning/drill_c{005,034,035,036,037,038,039,040}.json` |
| W5 | `…/positioning/drill_c{041,042,079,080,081,082}.json` |
| W6 | `…/fundamentals/drill_c{009,010,022,023}.json` + `…/forceControl/drill_c{044–051}.json` |
| W7 | `…/specialShots/drill_c{054,056,057,058,060}.json` + `…/combined/drill_c{064,065,068,069,070,071}.json` |

---

## 四、执行口径（给目录批，不是填空说明书）

写作语义真源仍是 v40 精讲规范 + 所有权表。派发词只给本批 id 清单 + 下列红线，**禁止**把规范改写成「锚点档 / 五问排版」步骤（FL-032）。

**质量锚（打开 c023 对照，不要抄句子到别的课）**

- 描述：主练 + 这一课和邻课差在哪 + **意图点名的杆法名**（定杆、高杆双下、低杆双下、前/后斯登）。没有档数、切角度数、m/s、颗球距。
- 要点：约 3 条；第 2 条只写「只改打点和力度」（或该课真正的自变量）。
- 精讲：开篇主题跟意图；逐杆跟 digest/图（c023 第 2 杆低杆双下短停、第 3 杆高杆双下、第 4 杆低杆双下、第 5 杆中大力定杆、第 6/8 杆前后斯登、第 7 杆中间档空）。机制不抢 c010 / c016。
- 达标：跟意图写合格判据（c023 不得再写锁中杆）。保留「八档各多次」这类计数量词，不新编及格线。建议训练量一行不要动。

**每课最小动作**

1. 意图表该课各球形「完善」列：自变量 / 目标 / 主练 / 本形差。
2. `python scripts/tutorial_digest.py --sequence <对应序列>`（或项目惯用入口）。数字只从这里抄。
3. 对该课 `tutorial` 里每一处 `"image"`：`Read QiuJi/Resources/DrillTutorials/<image>.png`。图上没有的对象、空台、错序列，禁止按旧图注写。
4. 手改 JSON 四字段。中间档无新信息可留空节。
5. 冲突进交付清单：示范/序列与意图打架上报；达标正文与意图打架则改达标。不改序列、不改建议训练量、不编一套「两边都对」的词。

**定杆 / 后续课**：c010 仍是定杆引入。别的课一句「定杆见 c010」+ 本课新层，禁止重写定杆五问。

---

## 五、已知冲突（上报，禁止硬圆）

立档时仍有效（v41 执行期留下，未裁定）：

| 课 | 冲突摘要 |
|----|----------|
| c023 | 达标现网锁中杆 → **W6 改达标**（不再上报了事）。第 1 杆实测往前约 4.7 颗、不是贴死定杆：序列事实，上报，不改序列 |
| c010 ↔ c014 | 盘面几乎同构 |
| c034 | 意图高杆 vs 示范偏只低杆 |
| c077 | 意图左右塞 vs 示范偏只右塞 |
| 另有 | c012 / c009 / c072 / c020 / c021 / c074 形2 / c075 / c018 / c056 / c057 / c045 / c049 / c046 / c039 / c042 / c070 / c024 / c031 / c084 等（详见 v41 各批交付） |

新发现一律追加，不在正文里圆过去。

---

## 六、与邻方案边界

| 方案 | 本轮可碰 | 本轮不碰 |
|------|----------|----------|
| **v42（本文件）** | 72 课四字段整课重写（含达标正文） | 跳过课；序列；建议训练量；意图原文列 |
| **v41** | 只读：意图完善列、跳过名单、冲突清单 | 不再加批次、不改四字段语义 |
| **v40** | 规范 / 所有权表只读 | 不重开 v40 精讲迁移批 |

---

## 七、版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v42.1 | 2026-08-24 | 用户补口：训练要求一起改。范围扩为四字段；只改 `standardCriteria` 正文，建议训练量不动。D-v42-6；部分覆盖 D-v41-5。 |
| v42.0 | 2026-08-24 | 立档。推翻「只改开头 / 精讲另批 / W1 四条保护」。72 课整课重写；看 PNG；质量锚 c023；W0–W8。 |

---

## Changelog

| 日期 | 条目 |
|------|------|
| 2026-08-24 | v42.1：训练要求（达标目标）纳入本轮。 |
| 2026-08-24 | v42.0 立档。本轮真源。 |
