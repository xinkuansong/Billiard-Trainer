# 练习 Tab 问题集合 v7.2（代码复用与一致性收敛，可多会话执行版）

> **本文件是本轮需求真源**。来源：2026-07-16 用户发起的「练习 tab 卡片页复用与一致性」审计，共六路（组A 角度/瞄准基础页、组B SceneKit 场景训练页、组C 走位/战术/解球页、组D 横切维度；v7.1 补充：组E 沙盘/求解页按键布局盘点、组F 测验页控件与开球流程），主会话已对关键锚点逐一读代码复核。与 `tasks/UI-IMPLEMENTATION-SPEC.md` 等既有规范冲突时以本文件为准，落地后回写规范。
> 状态：**执行中**（W1 ✅；W2 ✅；W5 ✅；W6 ✅；W3–W4、W7–W9 ⏳）；**D1–D15 已全部拍板**（2026-07-16 用户裁决：全按主控建议，见 §四）。
> 排期前置：无（v5 全部批次已于 2026-07-13 收官；v6 为空壳草稿，由本文件取代）。
> 全部锚点已读代码核实（2026-07-16，六路审计子代理 + 主会话抽查复核，见 §五 锚点索引）。
> 编号约定：全局规范延续 v5 的 G 系列（G20 起）；问题条目用 C 系列（C1–C33）。

---

## 一、需求回显（用户诉求 + 审计结论合并去重，语义不增删）

用户原始诉求：练习 tab 各卡片页很多代码未复用，导致相同的东西在不同页面不一致，要求综合全面分析并收敛；v7.1 追加诉求：**各页面的按键布局、开球等控件层面也要纳入**。以下为六路审计合并去重后的问题清单（语义基准）：

### 正确性 / 数据（P0）

| 条 | 问题 | 来源 |
|---|---|---|
| C1 | 「3/4 球」语义分裂：瞄准原理页 14.5°/overlap 0.75，对照表/球感/运行时命名 48.6°（球感 overlap 还写 0.25），教学内容自相矛盾 | 组A P0-1 |
| C2 | 瞄准点成绩已写入 SwiftData（`aimPoint`/`aimPoint2D`/`aimPoint3D`）但 `AngleQuizTypeFilter` 无对应筛选项，历史页筛不到 | 组B P1-7 / 组D P0 |
| C3 | Snooker 防守页轨迹绘制与 Composer/Silu/PlanThree 分叉：不画 objectPath、无 extendPathToPocketRim、ghost 取 `firstContact` 而非 `ghost` | 组C P0-1 |
| C4 | BallExtraction `freeNormalizedSlot` y 扫描区间 `0.10…0.42` 与其余四 VM `0.12…0.40` 不一致 | 组C P0-3 |
| C5 | 「上一杆」同名三套语义：Composer/FreePlay/ShotSimulation = `replayCurrent()`（重打＝删末杆退回）；Silu/PlanThree/Snooker/Bank·自由 = `undoLastShot()`（全量恢复）；Bank/Diamond 求解 = `undoSolveShot()`（解集级恢复）。共享 `BTShotActionColumn` 中钮文案统一为「上一杆」掩盖差异 | 组C P0-2 / 组E P0-1 |
| C6 | AimPointScene 文件头注释「3 秒」与实现/HUD「1.5 秒」矛盾 | 组B P1-6 |

### 导航 / 页面骨架（P1）

| 条 | 问题 | 来源 |
|---|---|---|
| C7 | 暗色测验页（SceneAiming/Geometric/AngleDynamic/AimPointTraining/AimPointScene）缺 `toolbarColorScheme(.dark)` + `toolbarBackground(.black)`，与沙盘页导航亮暗跳变，违反 SPEC §8.3 | 组A P1-1 / 组D P0 |
| C8 | `BTSolverNavStatus` 公共组件已存在，但 7 页仍复制私有 `navStatus`（FreePlay/Composer/Snooker/ShotSim/Silu/PlanThree/BatchAuthoring）；BallExtraction principal 缺副行 | 组C P1-2 / 组D P1 |
| C9 | `FramePreference` PreferenceKey 共 9 套结构完全相同的私有拷贝 | 组C P1-6（主会话核实为 9 处） |
| C10 | `flash` toast 同构实现 5 份（Composer/Silu/PlanThree/Snooker/Extraction VM） | 组C P1-7 |
| C11 | G10 常量 `topRowHeight=46` / `bottomBarHeight`（78/94/116）散落魔法数，未收进 `ShotStageMetrics` | 组B P1-9 / 组C P2-5 |
| C12 | FreePlay 未用 `.btStageFrame` / `.btChipBandPlacement` 共享修饰器，手写等价 frame/position，与 ShotSimulation 分叉 | 组B P1-3 |

### 球库 / 场景层（P1）

| 条 | 问题 | 来源 |
|---|---|---|
| C13 | 球库+拖拽幽灵+拖回删球实现复制 5+ 份，球径 30/32/34/36、token frame、行高、拖拽死区 10/12、行间距各不相同 | 组B P1-2、P2-2 / 组C P1-1 |
| C14 | SceneAiming ↔ AimPointScene 装饰球库几乎逐行复制 | 组B P1-2 |
| C15 | AimPointScene 自建瞄准可视化（`addDotNode` 半径 0.007）与 `AngleTrainingScene.updateVisualization`（aimDot 0.0065）平行两套 | 组B P1-1 |
| C16 | AimPointTraining 未调用 `AimPointGeometry` API，自写等价公式（双真源风险）；AimPointScene 验证击球速度 `1.5` 魔法数、3D 刻度轮 44×170 偏离 `ShotStageMetrics.aimWheelWidth=38` | 组B P1-8、P1-5、P1-4 |
| C17 | VM 层绘制代码复制：`pulseTableBall`（4 处同参）、`strokeCircle/strokeRect`（3 处）、约束绘制 overlay + 反投影（Silu/PlanThree 两份）、约束色 `UIColor(red: 0.2, green: 0.85, blue: 0.95…)` 硬编码 3 处 | 组C P1-4、P1-5 |
| C18 | BankShotView ≈ DiamondSystemView 整页复制约 350+ 行（仅 VM/文案/coordinateSpace/onPocketTapped 不同），InfoSheet 也是同构两份 | 组A P2-1、P2-6 |

### 设计 token / 动效 / 反馈（P1–P2）

| 条 | 问题 | 来源 |
|---|---|---|
| C19 | `spring(response: 0.34, dampingFraction: 0.86)` 字面量 8 处未用 `BTMotion.springPanel`；`easeInOut(0.2/0.25)`、`easeOut(0.12)`、`spring(0.35,0.75)` 等散落自定义 | 组A P1-3 / 组B P2-1 / 组C P1-3 |
| C20 | 首页 `AngleHomeView` 40 处 `Color(red:)` 硬编码封面色；水印字号 `size: 56` 硬编码 | 组D P0/P1 |
| C21 | 全 tab `.font(.system(size:))` 100+ 处（BallExtraction 20、Composer 18、FreePlay 12…）；HUD 分隔线高度 12/14 不一 | 组A P1-2、P1-7 / 组B P2-3 / 组D §4 |
| C22 | 练习测验正误判定无任何 haptic/音效（仅键盘/瞄准轮有轻震），无 `BTFeedback` 统一封装 | 组A P2-9 / 组D §5 |
| C23 | Freemium 满额 UI 三种形态（Geometric full card / SceneAiming 仅总结 compact / AimPointScene 全屏遮罩）；「剩余」vs「剩」文案不一；`AngleUsageLimiter()` 四处独立实例（key 共享但内存分离）；首页有日限的卡不标 Pro | 组D §3 |
| C24 | 持久化 key 四种命名风格并存（`AngleUsage_count` / `AdaptiveQuestionEngine_v1` / `cushionReflectionPower` / `angleDynamic.hasDraggedOnce`），无统一常量门面 | 组A P2-8 / 组D §2 |

### 控件布局 / 按键 / 开球（P0–P2，v7.1 组E/组F 补充）

| 条 | 问题 | 来源 |
|---|---|---|
| C25 | 主击按钮文案「击球 / 击打 / 打一」跨页混用：多数页「击球」，Bank/Diamond 求解态与 Composer 序列模式「击打」，PlanThree「打一」；共享列默认 `strikeTitle = "击球"`（`BTShotPageChrome.swift:112`）被页级随意覆盖 | 组E P0-2 |
| C26 | 左下角控件家族四种形态：A 族（Silu/PlanThree/Snooker）求解→下一解→开球竖叠 48×122；B 族（Composer/ShotSim）单独禁用开球 48×46；C 族（FreePlay）单独可点开球；D 族（Bank/Diamond）无开球、放「下一解/恢复球形」52×46 且形态与 A 族「下一解」不同。跨页左下角色完全换型 | 组E P1-3/P1-4 |
| C27 | 「无开球页」占位策略分裂：ShotSim/Composer/Snooker 显示禁用 `BTBreakSideButton`，Bank/Diamond 不显示——同为无开球场景两种做法 | 组E P1-5 |
| C28 | `BTTrajectoryDetailChip` 轨迹档位 chip 六页有、Bank/Diamond/Extraction 无（Bank/Diamond 有轨迹可视化却无档位入口） | 组E P1-6 |
| C29 | 三点菜单内容层未收敛（G19 只统一了图标）：仅 Bank/Diamond 用标准件 `BTSolverMoreMenu`（原理→显示→恢复默认）；反解三页私有 Menu 多「求解范围」且「清空桌面」顺序不同；Composer/FreePlay/ShotSim 各写各的，ShotSim 用「重置默认球形」文案而非「恢复默认」 | 组E P1-9 / 组C P1-2 |
| C30 | 测验页主操作按钮双真源：SceneAiming/AimPointScene 用共享 `BTTextActionButton`，Geometric/AimPointTraining 用页内私有绿胶囊（`GeometricAngleQuizView.swift:188–221`、`AimPointTrainingView.swift:294–305`）；`NumericKeypadHUD` 呈现契约也分两种（safeAreaInset vs 全屏 ZStack 浮层） | 组F 任务一 P1-1/P1-5 |
| C31 | 测验页顶栏 trailing 与设置能力不对齐：Geometric=重置统计、SceneAiming=三点+完整设置 sheet、AngleDynamic=三点仅网格 Menu、AimPoint 两页=无任何设置入口（网格/模式不可配）；成绩条词表不一（「次数/正确率/平均」vs「题/袋/差」vs「题/均差」，「剩余」vs「剩」并入 C23/D9） | 组F 任务一 P1-2/P1-3、P2-2 |
| C32 | G18 开球规范未闭环两处：① Composer 开球入口仍禁用（`PositionPlayComposerView.swift:254–261`）但 runner/开球条/仪表已全接线，死路径挂页；② `BreakEntryTile`（`BreakFlowRunner.swift:441–463`）零消费死代码。其余 G18 ①–⑤ 已核实落地（四宿主共享 `BreakFlowRunner`+`BreakControlBar`+`BreakInstrumentsOverlay`，无私有开球条） | 组F 任务二 P1-1/P1-2 / 组E P2-14 |
| C33 | 过时注释三处：Bank/Diamond 文件头写动作列「下一解/重置」与实现不符（`BankShotView.swift:7–9`）；SceneAiming 注释仍写「齿轮」（文件头 L8、L363） | 组E P2-13 / 组F P2-1 |

### 审计确认为「不建议动 / 不纳入本轮」的部分

- 教学页（浅色 Scroll）vs 场景页（黑底）两套骨架：定位不同，**保留两套**，只在各自体系内收敛。
- SceneKit 侧 `UIColor` 调色板与 SwiftUI token 体系隔离：建议独立「Scene palette」，本轮仅收敛 C17 的 3 处复制色，不做全量迁移。
- Freemium **扩围**（沙盘/解球是否也要门控）与首页 Pro 角标：属产品商业化决策，列入 §四 待拍板，不自行扩围。
- `ShotTryFreePlayButton` 死代码、`SiluSpinLabel` 死代码：随所在批次顺手处理（删除或恢复由拍板决定）。
- Silu/Snooker `makeExportSequence()` 悬空 API：列入待拍板（接 UI 或删除）。
- 仪表柱能力差异（组E P1-8）：求解页「无解时禁用/打点仅自由模式」vs 自由击打页「始终可调」属**产品语义**（力度在求解页是求解输入），不强行统一；但 Bank/Diamond 力度量程用 `CushionReflectionSettings` 而非 `ShotTuning` 的差异保留现状并在规范中注明。
- Q15.3 清除键（`BTEraserButton`）只在有「摆球+约束」的三页（Silu/PlanThree/Snooker）：符合规范本意，Bank/Diamond/Composer 无对应语义，不补。
- FreePlay 独有「完成→规则对局」、各宿主开球落座后的业务差异（清角色/自动选目标）：产品差异，非 chrome 分叉。
- 教学三页浅底无 trailing 与场景测验页语言分离：有意设计，保留。

---

## 二、G 系列全局规范（G20–G23，延续 v5）

### G20 场景页 chrome 单一真源

所有黑底练习页（测验 + 沙盘 + 向导确认步）统一：`toolbarColorScheme(.dark, for: .navigationBar)` + `toolbarBackground(Color.black, for: .navigationBar)` + principal 用 `BTSolverNavStatus`（无状态副行的页面允许简化变体，但组件同源）。禁止页面再自写 `navStatus`。`FramePreference` 收敛为共享 `BTShotPageFramePreference`（保留 `SolverFramePreference` 作为迁移别名或直接替换）。

### G21 球库单一组件

抽 `BTBallPaletteBar`（含拖拽幽灵、拖回删球、pulse/place 回调、`coordinateSpace`/`ballDiameter`/`isPlaying` 参数化）。球径、行高、拖拽死区（统一 `minimumDistance: 10`）、ghost 描边（success 2.5 / idle 1）全部收进组件默认值；页面只传差异参数。装饰（只读）球库为同组件只读模式或姊妹组件 `BTDecorativeBallPalette`。

### G22 动效一律走 `BTMotion`

页面禁止书写 spring/ease 字面量：`spring(0.34, 0.86)` → `BTMotion.springPanel`；`easeOut(0.25)` → `BTMotion.easeChrome`；其余高频参数（如 `easeInOut(0.2)`、`spring(0.35, 0.75)`）经核对后登记为新 token（如 `easeFast` 对齐、`springLayout`）或改用既有 token。滑条即时反馈类超短动画（0.12s）登记 `easeInstant` 类 token 后使用。

### G23 测验正误统一反馈

新增 `BTFeedback`（Core 层）：`quiz(correct/close/off)` → `UINotificationFeedbackGenerator` 档位映射（可选轻音效开关预留）。四测验（Geometric / SceneAiming / AimPointTraining / AimPointScene）submit 判题路径统一调用。键区/瞄准轮既有 light impact 保留。

### G24 动作列文案字典 + 左下插槽协议（v7.1）

**文案字典（强制）**：主击钮——自由试打统一「击球」，解演示统一「击打」，PlanThree 是否保留「打一」由 D13 拍板；中钮——行为为 `replayCurrent`（重打）的页面文案改「重打」，真 `undoLastShot` 的页面保留「上一杆」（对齐 v5 G17 与 C5）。
**左下插槽协议**：左下角 Slot L1（贴底）只允许五种角色之一——开球 | 禁用开球 | 下一解 | 恢复球形 | 重摆球形，统一一种外形尺寸；反解页的「求解/下一解」竖叠抽共享 `BTSolverLeftColumn`，消灭 Bank/Diamond「下一解独立角钮」与 Silu 族「竖叠文字钮」双形态。无开球页占位策略按 D14 拍板后全局二选一。

### G25 三点菜单内容模板（G19 内容层收敛，v7.1）

G19 已统一图标为 `ellipsis.circle`，本条收敛**菜单内容与顺序**：解球器 = `BTSolverMoreMenu`（原理说明 → 显示·网格 → 恢复默认）；反解训练 = 扩展模板 + Section「求解范围」+「清空桌面」；自由击打 = 显示·网格 + 页特有项。「恢复默认 / 重置默认球形 / 恢复球形」文案统一为「恢复默认」（页特有语义除外）。测验页有可配项必须提供三点入口（AimPoint 两页补齐或明确声明无可配项）。

---

## 三、批次表（W1–W9）

依赖图：

```
W1（正确性/决策落地）──→ W8（VM 绘制去重，含 Snooker 轨迹统一实施）
W2（导航 chrome）────→ W5（Bank/Diamond 合并，复用 W2 收敛后的 chrome）
              └────→ W9（控件布局/文案/菜单统一，依赖 W2 + D12–D14 拍板）
W3（动效/token 收编）  独立
W4（球库组件）        独立（建议在 W2 后，减少同文件冲突）
W6（场景/几何对齐）    独立（几何部分强制 geometry-spatial-reasoning）
W7（Freemium/haptic）  独立
```

并行提示：W3 / W6 / W7 相互独立且与 W1 独立，可并行或换序；W4 与 W2、W8 与 W1、W9 与 W2/W5 建议串行（W9 与 W5 同触碰 Bank/Diamond，W5 先做可减小 W9 面积）。

---

### W1 正确性与数据收敛（P0 优先，改动小） — ✅ 2026-07-16

**范围**：C1、C2、C4、C5（改名部分）、C6，以及 C3 的**语义确认**（实施在 W8）。

1. C1：拍板「3/4 球」唯一定义（§四 D1），以 `AngleSceneCalculator` 层新增/修订单一真源函数（名称 ↔ 切角 ↔ overlap ↔ d/R 四元组），同步改 `ContactPointTableView.swift:28,519`、`BallFeelView.swift:99`、`AimingPrincipleView.swift:299`、`AngleSceneCalculator.swift:1144`。**强制**：动手前按 `geometry-spatial-reasoning` 技能执行坐标/角度契约回显，用数值草稿验证 sinθ 与 overlap 的换算，禁止脑算。
2. C2：`AngleQuizTypeFilter`（`AngleHistoryViewModel.swift:10–27`）补 `aimPoint` / `aimPoint2D` / `aimPoint3D` 三档（文案对齐 `HistoryViewModel.AngleQuizType` 的「瞄准点训练/2D 瞄准点训练/3D 瞄准点训练」）。
3. C4：确认 Extraction 区间是否有意放宽（§四 D3）；默认统一为 `0.12…0.40`（改 `BallExtractionViewModel.swift:428–429`），若拍板保留则加注释说明豁免原因。
4. C5：Composer/FreePlay/ShotSimulation 三页动作列中钮文案改为区分语义（如「重打」vs「上一杆」，§四 D4 拍板措辞），不改行为（v5 G17 已定 `replayCurrent` 为基准语义）；主击文案字典全量统一放 W9（G24）。
5. C6：修正 `AimPointSceneTrainingView.swift` 头注释（12 行「3 秒」→「1.5 秒」）。
6. C3 语义确认：与用户确认 Snooker 防守页不画 objectPath / ghost 取 firstContact 是否为防守语义的有意设计（§四 D2）；确认结论写回本文件，统一实施放 W8。

**量级**：约 6–8 文件，多为点改。
**完成标准**：`make build` + `make test` 通过；「3/4 球」四处引用值一致且与真源函数一致（grep 复核）；历史页筛选出 aimPoint 记录的模拟器截图；D1–D4 拍板结论回写本文件 §四。
**落地摘要（2026-07-16）**：C1 真源 `AngleSceneCalculator.NamedBallThickness` / `threeQuarterBall`（14.5°/0.75/0.50）；C2 历史筛补 aimPoint 三档；C4 Extraction y∈[0.12,0.40]；C5 Composer/FreePlay/ShotSim 中钮「重打」；C6 头注释 1.5 秒；C3 不实施（D2→W8）。验证：`make build` SUCCEEDED；`QiuJiTests` 564/0 全绿；全量 `make test` UI 有 5 条既有失败（DrillTryout/P3/P5，与本批无关）；截图 `build/w1-screenshots/w1-03-aimpoint-filter-selected.png`。**返工 1 轮**：首版 `thicknessName` 保留旧偏移口径「≈1/4 球」(7.5°)/「≈1/3 球」(10°) 与经典命名表（48.6°=「1/4 球」）同函数撞名 → 删除偏移口径昵称、正弦曲线标记改从 `namedBallThicknesses` 派生、新增钉子单测；主控复跑 build + BallThicknessNamingTests(4)/AngleQuizTypeFilterTests(1) 全绿。

---

### W2 导航 chrome 与页面骨架收口 — ✅ 2026-07-16

**范围**：C7、C8、C9、C10、C11。

1. C7：五个暗色测验页补 `toolbarColorScheme(.dark)` + `toolbarBackground(Color.black)`（`SceneAimingView` / `GeometricAngleQuizView` / `AngleDynamicView` / `AimPointTrainingView` / `AimPointSceneTrainingView`），principal 按 SPEC §8.3 用品牌绿标题（接 `BTSolverNavStatus` 或其无副行简化形态）。
2. C8：7 处私有 `navStatus` 替换为 `BTSolverNavStatus`（`FreePlayView.swift:275` / `PositionPlayComposerView.swift:380` / `SnookerTacticsView.swift:92` / `ShotSimulationView.swift:174` / `SiluTrainerView.swift:92` / `PlanThreeView.swift:105` / `BatchAuthoringView.swift:383`）；如个别页副行内容超出现有 API，先扩展 `BTSolverNavStatus` 参数再接入（记 DR）。`BallExtractionView.swift:64–68` principal 补齐同款结构。
3. C9：9 套 `*FramePreference` 收敛为共享 `BTShotPageFramePreference`（落 `BTShotPageChrome.swift` 或 `ShotTableLayout.swift`）。
4. C10：5 份 `flash` 收敛为共享实现（如 `View.btFlashToast(message:)` modifier 或 `BTToast` 静态入口），行为保持 `BTMotion.easeChrome` + `BTToast.defaultDuration`。
5. C11：`topRowHeight` / `bottomBarHeight` 收进 `ShotStageMetrics`（底栏高度用枚举档：`.paletteOnly(78)` / `.composer(94)` / `.planThree(116)`），替换各页魔法数。

**量级**：约 14–16 文件（多为替换删减）。
**完成标准**：`make build` + `make test` 通过；每个暗色页导航栏截图（导航亮暗不再跳变、标题品牌绿）；grep 复核 `private var navStatus` = 0、`FramePreference: PreferenceKey` 仅剩共享 1 处；`tasks/UI-IMPLEMENTATION-SPEC.md` §8.3 回写。

**落地摘要（2026-07-16）**：C7 五暗色测验页 dark toolbar + `BTSolverNavStatus`（`statusText` 可选）；C8 七页私有 `navStatus` 清零 + BallExtraction principal 副行；C9 → `BTShotPageFramePreference`（`SolverFramePreference` 别名）；C10 flash 体收敛 `BTToast.present`（页内留一行 facade）；C11 `ShotStageMetrics.topRowHeight` + `BottomBarHeight` 三档。验证：`make build` SUCCEEDED；`QiuJiTests` **565/2 skipped/0 failures**；`S2_ShotPagesLayoutUITests`+`W2_DarkQuizNavChromeUITests` **7/0**；截图 `build/w2-screenshots/w2-c7-0{1..5}-*.png`。SPEC §8.3 + Changelog 已回写。

---

### W3 动效与设计 token 收编

**范围**：C19、C20、C21（分隔线与公共组件部分）。

1. C19：8 处 spring 字面量 → `BTMotion.springPanel`（`BankShotView.swift:184` / `DiamondSystemView.swift:185` / `ShotSimulationView.swift:63` / `SiluTrainerView.swift:59` / `PlanThreeView.swift:62` / `SnookerTacticsView.swift:59` / `PositionPlayComposerView.swift:100` / `BatchAuthoringView.swift:322`）；`SceneAimingView` overlay `spring(0.35, 0.75)`、各页 `easeInOut(0.2/0.25)`、`easeOut(0.12)` 按 G22 登记 token 后替换（新 token 记 DR 并回写 `BTMotion.swift` 注释）。
2. C20：`AngleHomeView.swift` 40 处 `Color(red:)` 封面色迁 `AngleCoverPalette`（Asset Catalog 或 DesignSystem 常量组，Dark 模式各自校验）；水印 `size: 56` → token。
3. C21（本批只做公共件与统一值）：HUD 竖分隔线高度统一（12 或 14 拍板 §四 D5）并落共享小组件/常量；`BTDailyLimitGate.swift:26,40,62` 内部 `.system(size:)` token 化。**全量页面字号迁移不纳入本批**（见 §四 D6，按页渐进）。

**量级**：约 12–14 文件（多为一行替换）+ Asset。
**完成标准**：`make build` 通过；grep 复核 `spring(response: 0.34` 在 `QiuJi/` 内仅剩 `BTMotion.swift` 定义 1 处；首页 Light/Dark 截图对比（封面色迁移后视觉不回退）；新 token 记 DR 并回写 SPEC Changelog。

---

### W4 球库组件统一（BTBallPaletteBar）

**范围**：C13、C14。依赖：无硬依赖，建议 W2 之后执行。

1. 按 G21 新建 `BTBallPaletteBar`（Core/Components），吸收：两行 8 列布局、`PoolBallFace` 尺寸、拖拽幽灵、拖回删球、pulse/place、opacity 规则。
2. 交互球库接入：`PositionPlayComposerView.swift:635–724` / `SiluTrainerView.swift:356–432` / `PlanThreeView.swift:456–531` / `SnookerTacticsView.swift:270–346` / `BallExtractionView.swift:461–512`（含 BatchDrillStudio 两页若结构一致则一并接入，不一致则记「待定位」延后）。
3. 球径统一值拍板 §四 D7（现状 30/32/34/36），死区统一 10。
4. C14：`SceneAimingView.swift:163–186` 与 `AimPointSceneTrainingView.swift:509–532` 装饰球库接入只读模式。

**量级**：新组件 1 + 改 7–9 文件，**本轮最大批**。
**完成标准**：`make build` + `make test` 通过；每个接入页拖球放置/拖回删球/pulse 的模拟器实操验证（至少 Composer、Snooker、Extraction 三页录屏或逐步截图）；grep 复核各页私有 `paletteDrag`/`dragGhost` 实现清零；组件 API 记入 `swiftui-design-system` SKILL.md。

---

### W5 BankShot / Diamond 壳合并 — ✅ 2026-07-16

**范围**：C18。依赖：W2（chrome 已收敛，合并面更小）。

1. 抽 `SolverStageChrome`（或泛型容器 View + VM 协议）：topRow、stage 仪表柱/瞄准轮/动作列、球库拖拽（若 W4 已完成则直接消费 `BTBallPaletteBar`）、恢复/下一解按钮、`SolverFramePreference` 消费。
2. 差异注入点：VM、标题、coordinateSpace 名、`onPocketTapped?`（仅 BankShot）、InfoSheet 内容。
3. 两份 InfoSheet 合并为 `PrincipleInfoSheet(title:blocks:)` 模板。

**量级**：2 大 View + 1 新容器 + InfoSheet 模板，净删约 300+ 行。
**完成标准**：`make build` + `make test` 通过；翻袋/反射两页全流程模拟器实操对照（求解、拖球、袋口点选仅翻袋有、恢复默认、下一解、InfoSheet），与改前行为一致；ADR 追加至对应 Phase 文件（跨模块公共容器属架构变更）。

**落地摘要（2026-07-16）**：抽 `SolverStageChrome<VM: SolverStageHosting>`（泛型壳 + VM 协议 extension 适配）+ `PrincipleInfoSheet(title:blocks:)` 模板；差异注入：标题/coordinateSpace/`onPocketTapped?`（仅 BankShot）/InfoSheet 内容；Bank/Diamond 两页瘦身为配置装配，合计净删 ~776 行，VM 未改。验证（worktree 落盘产物，主控亲验）：`QiuJiTests` 563/0 失败/2 跳过（`w5-qiujitests.xcresult`）；`ScreenshotTourUITests` 翻袋/反射真实模式 2/2；两页求解/自由/网格/菜单/下一解截图 vs 基线对照（`build/w5-screenshots/`）；合并主树 build SUCCEEDED。ADR-P9-04 已落 `tasks/phases/P9-aiming.md`。与 W6 并行（worktree），文件零交集，合并无冲突。

---

### W6 场景/几何单一真源对齐 — ✅ 2026-07-16

**范围**：C12、C15、C16。**几何部分强制先读 `geometry-spatial-reasoning` 并执行坐标契约回显。**

1. C12：FreePlay 贴边布局改用 `.btStageFrame` / `.btChipBandPlacement`（`FreePlayView.swift:115–163`，对照 `ShotTableLayout.swift:163–178`），改后与 ShotSimulation 逐项对齐截图。
2. C15：AimPointScene 瞄准可视化并入 `AngleTrainingScene`（扩展 `updateVisualization` 或新增 `AimPointOverlay` API），消灭私有 `addDotNode`（`AimPointSceneTrainingView.swift:242–316`）；瞄准点标记几何统一（0.0065 球 vs 0.007 圆柱片，拍板 §四 D8）。
3. C16：`AimPointTrainingViewModel` 改调 `AimPointGeometry`，`ballRadiusMM` 派生自 `AngleSceneCalculator.ballRadius * 1000`；验证击球 `velocity 1.5` 收进命名常量（`ShotTuning` 子集）；3D 刻度轮尺寸走 `ShotStageMetrics`。

**量级**：约 6–8 文件。
**完成标准**：`make build` + `make test`（含 `AimPointGeometryTests`）通过；瞄准点训练 2D/3D 各出一题实操验证（瞄准线/红点/验证击球行为不回退）；FreePlay 与 ShotSimulation 布局对照截图；几何改动附数值草稿验证记录。

**落地摘要（2026-07-16）**：C12 FreePlay 五处贴边手写 frame/position → `.btStageFrame` / `.btChipBandPlacement`（与 ShotSim 同修饰器）；C15 `AngleTrainingScene` 新增 `makeAimPointMarkerNode` 工厂 + `addAimPointMarker` API（0.0065 球，D8），ghost aimDot 同源，AimPointScene 私有 `addDotNode`（0.007 圆柱片）删除；C16 `AimPointTrainingViewModel` 偏移改调 `AimPointGeometry.signedOffset/offsetDistance`，`ballRadiusMM = AngleSceneCalculator.ballRadius*1000`，验证杆速收进 `ShotTuning.aimPointVerifyVelocity`（=defaultVelocity 1.5），3D 浮动轮 44×170 → `ShotStageMetrics.aimWheelWidth(38)×aimWheelFloatingHeight(170，新增常量；44 无有意偏离证据，git 溯源早于 38 真源引入）。验证：`make build` SUCCEEDED；`QiuJiTests` 0 failures（`AimPointGeometryTests` 12/12 绿；专用模拟器复跑排除并行批资源争抢）；`S5_TrainingPagesLayoutUITests`+`S5_AimPointSceneV5UITests`+`S3_AimPointUITests` TEST SUCCEEDED；数值草稿：自写公式 vs `AimPointGeometry` 多样例逐点一致（含 φ 正负/0/极值）；取证 `build/w6-screenshots/`（2D/3D 实操 + FreePlay/ShotSim 对照）。

---

### W7 Freemium 形态与测验反馈统一

**范围**：C22、C23、C24。

1. C22：按 G23 新建 `BTFeedback`，四测验 submit 判题路径接入（`GeometricAngleViewModel` / `AimingQuizViewModel` / `AimPointTrainingViewModel` / `AimPointSceneQuizViewModel`）。
2. C23：满额 UI 统一为「`BTDailyLimitGate` full 主卡 + 结果区 compact」双形态（SceneAiming 补 full 形态、AimPointScene 全屏遮罩改造对齐）；「剩余/剩」文案统一（拍板 §四 D9）；`AngleUsageLimiter` 改共享实例（`.shared` 或环境注入，四处 `AngleUsageLimiter()` 收口，注意 `QiuJiTests/AngleTrainingTests.swift` 的构造用例同步）。
3. C24：新增 `PracticeStorageKey` 常量枚举收口现有 UserDefaults key（**只收口引用，不迁移已有 key 值**，避免用户数据丢失；新 key 一律走枚举）。
4. 首页 Pro 角标、Freemium 扩围：**仅在 D10 拍板后实施**，默认本批不做。

**量级**：约 8–10 文件。
**完成标准**：`make build` + `make test` 通过（限次用例仍绿）；四测验答题触觉实机/模拟器验证说明；免费账号刷满日限后四页满额形态截图一致；key 收口 grep 复核（裸字符串 key 仅剩枚举定义处）。

---

### W8 VM 层绘制去重（含 Snooker 轨迹统一实施）

**范围**：C3（实施）、C17。依赖：W1（D2 拍板结论）。

1. C3：按 D2 结论统一或显式参数化 Snooker 轨迹绘制——若拍板「防守页有意不同」，则抽 `TrajectoryRenderer.draw(prediction:options:)` 用 `includeObjectPath` / `ghostSource: .ghost|.firstContact` 显式表达差异；若拍板「应一致」，Snooker 对齐另三页（`SnookerTacticsViewModel.swift:453–471`）。
2. C17：抽共享 helper：`pulseTableBall`（4 处同参：scale 1.7/0.18s + 1.0/0.24s）、`strokeCircle/strokeRect`（segments 36、线半径 0.0022，消掉 Snooker 的 surfaceY 写死差异或显式参数）、Silu/PlanThree `drawingOverlay` + 反投影合并为 `SolveConstraintDrawingOverlay`（coordinateSpace 参数化）；约束 cyan 色收进 Scene palette 常量（单点定义）。
3. 顺手项：`SiluSpinLabel` 死代码删除（`SiluTrainerView.swift:478–488`）。

**量级**：约 6–8 文件（VM 为主）。
**完成标准**：`make build` + `make test` 通过；Silu/PlanThree/Snooker/Composer 各打一杆的轨迹渲染实操对照（含约束绘制），与拍板语义一致；grep 复核 `pulseTableBall` / `strokeCircle` 私有拷贝清零。

---

### W9 控件布局 / 文案 / 菜单统一（v7.1 新增）

**范围**：C25、C26、C27、C28、C29、C30、C31、C32、C33。依赖：W2（chrome 收敛后再动布局）；建议 W5 之后（同触碰 Bank/Diamond）；D12–D14 须先拍板。

1. C25 + C5 收尾：按 G24 文案字典统一主击/中钮文案（`BTShotActionColumn` 消费点全扫，`strikeTitle` 覆盖仅允许字典内取值；PlanThree「打一」按 D13 结论处理）。
2. C26：左下插槽按 G24 协议统一——抽 `BTSolverLeftColumn`（求解/下一解竖叠，Silu/PlanThree/Snooker 接入），Bank/Diamond「下一解/恢复球形」角钮统一为同一外形尺寸；Bank/Diamond 动作列定位从 `bottomTrailingFrame` 改 `actionColumnFrame()`（组E P2-12）。
3. C27：无开球页占位策略按 D14 结论全局二选一（涉及 ShotSim/Composer/Snooker/Bank/Diamond/BatchAuthoring 的禁用 `BTBreakSideButton` 增删）。
4. C28：Bank/Diamond 补 `BTTrajectoryDetailChip`（若拍板「解球器免轨迹档位」则在 SPEC 写明豁免，二选一）。
5. C29：三点菜单按 G25 模板收敛——反解三页/自由击打三页的私有 `Menu` 迁移到扩展后的 `BTSolverMoreMenu`（加 Section 支持）；「恢复默认」文案统一；AimPoint 两页设置入口按 G25 处理（C31 测验页 trailing 对齐一并做：Geometric 重置入口保留但形态与三点并存策略写进 SPEC）。
6. C30：Geometric/AimPointTraining 私有绿胶囊按钮换 `BTTextActionButton`；`NumericKeypadHUD` 呈现契约统一（safeAreaInset vs ZStack 二选一，以不遮挡球桌为准）。
7. C32：Composer 开球入口按 D12 结论处理（放开 或 删除页内开球 UI 死分支）；删除 `BreakEntryTile` 死代码（`BreakFlowRunner.swift:441–463`）。
8. C33 顺手项：修正 Bank/Diamond 文件头注释、SceneAiming「齿轮」注释。

**量级**：约 15–18 文件（多为文案/组件替换，逻辑改动集中在 Composer 开球与菜单迁移），**建议拆两个会话执行**（W9a：C25/C26/C27/C28 布局与文案；W9b：C29/C30/C31/C32/C33 菜单/按钮组件/开球收尾）——执行时按实际负载定。
**完成标准**：`make build` + `make test` 通过；九个沙盘/求解页左下角与动作列逐页截图对照（插槽协议五选一无第六形态）；三点菜单逐页截图（内容顺序符合 G25）；grep 复核 `strikeTitle` 取值均在字典内、`BreakEntryTile` 已删；SPEC §8 回写 G24/G25；若 D12 = 放开 Composer 开球，补开球全流程实操验证。

---

## 四、决策项（2026-07-16 用户拍板：**全部按主控建议执行**）

| 项 | 问题 | 拍板结论 ✅ | 影响批次 |
|---|---|---|---|
| D1 | 「3/4 球」唯一定义：经典球厚度（sinθ=0.25 → 14.5°）还是偏移分数（48.6°） | **14.5°（经典球厚度）**；W1 实施时仍须按 geometry-spatial-reasoning 做几何草稿验证，若数值验证推翻则上报再议 | W1 |
| D2 | Snooker 防守页轨迹省略 objectPath / ghost 取 firstContact 是否有意（防守不进球语义） | **视为有意，但必须显式参数化**（`TrajectoryRenderer` options 表达差异，禁止隐式分叉） | W8 |
| D3 | Extraction 空位网格 `0.10…0.42` 是否有意放宽 | **统一为 `0.12…0.40`** | W1 |
| D4 | Composer 族「上一杆」改名措辞 | **改名「重打」**（Composer/FreePlay/ShotSim 三页），不改行为 | W1 |
| D5 | HUD 分隔线高度统一 12 还是 14 | **12**（与击打页一致），登记共享常量 | W3 |
| D6 | 100+ 处 `.system(size:)` 全量迁移是否立项 | **不入本轮**：按页渐进迁移 + 新代码红线（禁止新增字面量字号），写进 SPEC | —（记录） |
| D7 | 球库球径统一值（现状 30/32/34/36） | **两档参数化：紧凑 30 / 常规 36，组件默认 36**；FreePlay 34 与 Extraction 32 归入最近档 | W4 |
| D8 | 瞄准点标记几何统一 | **0.0065 球**（随 `updateVisualization` 现状） | W6 |
| D9 | 限次文案「剩余」vs「剩」 | **「剩余」** | W7 |
| D10 | 首页 Pro 角标 + Freemium 扩围到沙盘/解球 | **本轮不做**（商业化决策留档，后续单独立项） | —（记录） |
| D11 | Silu/Snooker `makeExportSequence()` 悬空 API | **删除**（需要时从 git 恢复） | W8（顺手） |
| D12 | Composer 开球入口 | **放开**（G18-6 闭环，成为第四可达宿主；接线已就绪，补全流程验证） | W9 |
| D13 | 主击文案字典：PlanThree「打一」 | **保留「打一」**，登记为字典合法值 | W9 |
| D14 | 无开球页占位 | **全局不显示**（删 ShotSim/Composer〔非开球宿主态〕/Snooker/BatchAuthoring 的禁用占位钮；注意 D12 放开后 Composer 是可达宿主、显示可点开球） | W9 |
| D15 | Bank/Diamond 是否补轨迹档位 chip | **补 `BTTrajectoryDetailChip`**，与其余六页对齐 | W9 |

---

## 五、锚点索引（2026-07-16 读代码核实）

| 锚点 | 位置 | 核实方式 |
|---|---|---|
| 「3/4 球」四处冲突 | `AimingPrincipleView.swift:299`（14.5°）；`ContactPointTableView.swift:28,519`、`BallFeelView.swift:99`、`AngleSceneCalculator.swift:1144`（48.6°） | 主会话 grep 复核 ✅ |
| `AngleQuizTypeFilter` 缺 aimPoint | `AngleHistoryViewModel.swift:10–27` 仅 all/geometric/table2D/scene2D/scene3D | 主会话读文件 ✅ |
| aimPoint 写入 | `AimPointTrainingView.swift:85`；`AimPointSceneTrainingView.swift:33`（2D/3D 标注）；`HistoryViewModel.swift:69–115` 已支持 | 主会话 grep ✅ |
| `BTSolverNavStatus` / `BTSolverMoreMenu` | `BTShotPageChrome.swift:174,198` | 主会话 grep ✅ |
| 私有 `navStatus` 7 处 | FreePlay:275 / Composer:380 / Snooker:92 / ShotSim:174 / BatchAuthoring:383 / Silu:92 / PlanThree:105 | 主会话 grep ✅ |
| `FramePreference` 9 套 | PlanThree:575 / Composer:786 / Silu:493 / BatchAuthoring:1149 / BatchExtract:691 / Extract:738 / BankShot:387（非 private）/ Snooker:392 / Simulation:405 | 主会话 grep ✅ |
| 暗色页 `toolbarColorScheme` 缺失 | 已配置 11 页均为沙盘/向导；SceneAiming/Geometric/AngleDynamic/AimPointTraining/AimPointScene 无匹配 | 主会话 grep ✅ |
| spring 字面量 8 处 | BankShot:184 / Diamond:185 / ShotSim:63 / Silu:59 / PlanThree:62 / Snooker:59 / Composer:100 / BatchAuthoring:322；token 定义 `BTMotion.swift:10` | 主会话 grep ✅（`docs/ui-polish/31` 已有相同记录） |
| 首页硬编码色 40 处 | `AngleHomeView.swift`（`Color(red:` 计数 40） | 主会话 grep ✅ |
| 球径 30/32/34/36 | Silu:384 / PlanThree:483 / Snooker:298（30）；Extraction:356,464 / Batch 两页（32）；SceneAiming:173 / AimPointScene:519 / FreePlay:471（34）；Composer:670 / ShotSim:313 / BankShot:321 / Diamond:321 / AngleDynamic:238（36） | 主会话 grep ✅ |
| `AngleUsageLimiter` key 与 4 实例 | `AngleUsageLimiter.swift:16–17`；实例 Geometric:16 / SceneAiming:20 / AimPointTraining:137 / AimPointScene:375；测试 `AngleTrainingTests.swift:200–265` | 主会话 grep ✅ |
| Snooker 轨迹分叉 | `SnookerTacticsViewModel.swift:453–471` vs `PositionPlayViewModel.swift:837–861` 等 | 组C 审计（行号引自审计，实施前复读） |
| 球库拖拽 5 份 / 装饰库复制 / flash 5 份 / strokeCircle 3 份 / drawingOverlay 2 份 | 见 §一 C13/C14/C10/C17 所列行号 | 组C/组B 审计（实施前复读对应段落） |
| G10 常量 / `ShotStageMetrics.aimWheelWidth=38` / `.btStageFrame` 定义 | `ShotTableLayout.swift:57,163–178`；各页 topRowHeight=46 | 组B/组C 审计交叉一致 |
| Freemium 三形态 / `BTDailyLimitGate` | `BTDailyLimitGate.swift:5–88`；Geometric L33–35 / SceneAiming L428–430 / AimPointScene L424 | 组D 审计（实施前复读） |
| `BTShotActionColumn` 顺序与 `strikeTitle` 默认 | `BTShotPageChrome.swift:111–131`（击球→上一杆→回放；`strikeTitle = "击球"` L112） | 组E 审计（实施前复读） |
| 「上一杆」三套语义 | Composer:275–285 / FreePlay:153–160 / ShotSim:124–131（`replayCurrent`，注释见 `PositionPlayViewModel.swift:1144–1149`）；反解三页 `undoLastShot`；Bank/Diamond 求解 `undoSolveShot` | 组E 审计 + 组C 交叉一致 |
| 开球链路单一真源 | `BreakFlowRunner.swift`：Runner L17–319 / `BreakControlBar` L332–399 / `BreakInstrumentsOverlay` L407–437 / `BreakEntryTile` L441–463（零消费死代码）；`FreePlayBreakBar` 已不存在（仅注释提及）；消费点 FreePlay/Silu/PlanThree（可达）+ Composer（禁用，`PositionPlayComposerView.swift:254–261`） | 组F 审计（含 G18 ①–⑤ 逐条核对落地） |
| 三点菜单分布 | 标准件仅 Bank/Diamond；反解三页/自由三页私有 Menu；Extraction 无 | 组E 审计（实施前复读） |
| 测验页私有按钮 | `GeometricAngleQuizView.swift:188–221` / `AimPointTrainingView.swift:294–305`（私有胶囊）；SceneAiming:231–247 / AimPointScene:610–627（`BTTextActionButton`） | 组F 审计（实施前复读） |
| 底栏高度/左下插槽形态 | 94（Composer/FreePlay/ShotSim/Bank/Diamond）/ 78（Silu/Snooker）/ 116（PlanThree）；左下四族形态见 §一 C26 | 组E 审计 + 组C 交叉一致 |
| 完整六份审计报告 | 会话 2026-07-16「练习tab代码复用分析」六子智能体最终回复（组A–F） | 原始证据 |

> 标注「审计」的行号来自六路子智能体实读报告且互相交叉印证；对应批次执行时须先复读所列文件段落再动手（写入各批红线）。

---

## 六、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v7.2 | 2026-07-16 | 用户拍板 D1–D15 **全部按主控建议**（§四改为决策结论表）：D1=14.5° / D2=有意但显式参数化 / D3=统一 0.12…0.40 / D4=「重打」 / D5=12 / D6 不入本轮 / D7=30·36 两档默认 36 / D8=0.0065 球 / D9=「剩余」 / D10 本轮不做 / D11 删除 / D12=放开 Composer 开球 / D13 保留「打一」 / D14=全局不显示禁用占位 / D15=补轨迹 chip。无阻塞，可开工 W1。 |
| v7.1 | 2026-07-16 | 用户指出控件层面（按键布局、开球等）未覆盖 → 补组E（沙盘页八维控件盘点）/组F（测验页控件 + G18 开球核对）两路审计：新增 C25–C33、G24/G25、批次 W9、拍板项 D12–D15；C5 扩展为三套语义全景；「不建议动」补仪表柱能力差异等 4 条。开球链路核实结论：G18 ①–⑤ 已落地单一真源，残留 Composer 入口禁用与 `BreakEntryTile` 死代码。 |
| v7 | 2026-07-16 | 新建：练习 tab 复用与一致性收敛（四路审计 → C1–C24 → W1–W8）。取代空壳草稿 v6。 |
| v6 | — | 空壳草稿（仅「练习页面 1.」占位），未启用。 |
| v5 | 2026-07-13 | 上一轮（用户 v4 20 条），全部批次 ✅ 收官。 |
