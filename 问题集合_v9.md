# 训练分享「保存相册」卡死/闪退 — 问题集合 v9.0

> **本文件是本轮需求真源**。来源：2026-07-17 用户反馈「训练完成后，保存到相册会直接卡死或者闪退」，按 `/issue-collection-restructure` 对照代码拆解。
> 状态：✅ W1 完成（2026-07-17，返工 1 轮）——首轮后用户真机复测仍秒闪退；主控定位**真根因**：主 target `GENERATE_INFOPLIST_FILE = NO`，pbxproj/project.yml 的 `INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription` 全部被忽略，构建产物 Info.plist 无该键 → 首次权限弹框被 TCC 强杀（§二/§五「权限文案已配置」为**误判**；首轮模拟器用 `simctl privacy grant` 预授权限掩盖了此路径）。返工修复：三个权限键（相册添加/相册读取/相机）直写 `QiuJi/Resources/Info.plist` + project.yml 防 xcodegen 回退；PlistBuddy 实证 iphoneos+sim 两产物键在包内；未预授权限的真实弹框冒烟通过（弹框截图 + 点允许 + TEST SUCCEEDED）；`PhotoPermissionInfoPlistTests` 2/0 钉死。产物 `build/w1r1-logs/`、`build/w1r1-screenshots/`。真机自动冒烟被用户中止（测试驱动个人手机），App 已成功安装。**用户真机人工点验通过（2026-07-17）：保存相册不再闪退——B1 关闭，v9 收官。**
> 排期前置：无（P0 稳定性缺陷，可插队；与未整理的 `问题集合_v8.md` 练习 Tab 反馈**无关**，互不覆盖）
> 全部锚点已读代码核实（2026-07-17，见 §五）
> 编号约定：本轮单条缺陷用 **B1**；批次用 **W1**。

---

## 一、需求回显（语义基准，不增删）

| # | 用户诉求（原意） | 验收语义 |
|---|---|---|
| B1 | 训练完成后，保存到相册会直接**卡死**或**闪退** | 在合法权限下点「保存相册」必须：① UI 不永久冻结；② 不崩溃/不 jetsam；③ 成功则非阻断 toast「已保存到相册」，失败则 alert（诚实反馈，沿用 F-TR-01 / F-OV-04） |

### 本轮假设（待执行时若不符则改方案）

| 类型 | 内容 |
|---|---|
| **假设 A（复现路径）** | 训练结束 → 总结页「生成分享图」→ `TrainingShareView` sheet → 点「保存相册」。**不是**总结页「保存训练」（SwiftData 持久化）。 |
| **假设 B（根因优先序）** | 卡死/闪退发生在 **生成分享位图**（`ImageRenderer.uiImage`）阶段，而非 `PHPhotoLibrary.performChanges` 本身。 |
| **不确定点** | 是否仅真机复现、是否与 drill 数量/主题字体相关——执行批须先最小复现再改码。 |

若实际路径是「保存训练」卡死，本文件不适用，须另立条目，禁止把本方案硬套到 SwiftData 保存。

---

## 二、代码现状与根因假设（事实 / 推测分栏）

### 事实（已读代码）

1. 入口：`ActiveTrainingView` summary 态 → `onGenerateShareImage` → `.sheet` 呈现 `TrainingShareView(session: buildShareSession())`（`ActiveTrainingView.swift` L43–45 / L136–138）。
2. 保存实现：`TrainingShareView.saveCardToPhotos()`（`TrainingShareView.swift` L262–305）：
   - `@MainActor` 同步取 `ImageRenderer(...).uiImage`
   - 仅约束 `.frame(width: 361)`，**无高度**
   - `renderer.scale = UIScreen.main.scale`
   - 再 `PHPhotoLibrary.requestAuthorization(for: .addOnly)` + `performChanges`
3. 卡片：`BTShareCard` body 含 `Spacer(minLength: 0)`（`BTShareCard.swift` L100）；Preview 一律 `.frame(height: 480)`（L299 / L309），与保存路径不一致。
4. 权限文案已在工程配置：`INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription`（`project.yml` / pbxproj）。
5. **无**针对相册保存的单元测试或 UI 测试（`QiuJiTests` / `QiuJiUITests` grep 零命中）。

### 根因假设（推测，执行时验证）

> **现象「卡死/闪退」是因为 `ImageRenderer` 在无界高度下布局 `BTShareCard`（含 `Spacer`）导致主线程长时间卡住或产出超大位图触发内存 jetsam。**

验证方式（执行批必做，禁止未验证就调阈值糊弄）：

1. 调试器 / Instruments：卡死时栈是否停在 `ImageRenderer` / SwiftUI layout，而非 Photos。
2. 复现对比：给渲染内容补**有界高度**（或去掉离屏路径上的 `Spacer`）后，卡死是否消失。
3. 失败对照：权限拒绝时应走 alert，不应崩（权限路径回归）。

### 次优先风险（同一文件可一并加固，不另开批）

| 风险 | 说明 |
|---|---|
| 主线程长阻塞 | 即使有界，`uiImage` 同步渲染大卡仍可能短卡；可接受上限约数百 ms，超时须失败而非无限等 |
| `UIImage` 跨 `await` 进 `performChanges` | 一般安全；若验证到生命周期问题，对 `cgImage` 做本地拷贝 |
| 连点 | 已有 `isSavingToPhotos` guard，保留 |

---

## 三、批次表

| 批次 | 范围 | 量级 | 依赖 | 建议角色 | 模型 | 状态 |
|---|---|---|---|---|---|---|
| **W1** | B1：修好训练分享「保存相册」卡死/闪退，并留下可回归验证 | S（约 1 文件主改 + 可选抽渲染助手 + 1 单测） | 无 | SwiftUI Developer + Test Engineer | `cursor-grok-4.5-high-fast`（2026-07-17 用户确认） | ✅ 2026-07-17（返工 1 轮：真根因＝权限文案未进包，已修） |

### 依赖图

```
W1（唯一批）
```

---

## 四、W1 完成标准（可验证）

### 范围（必须）

1. **钉死离屏渲染尺寸契约**：`ImageRenderer` 的 content 必须同时具备有界宽与有界高（或等价：`proposedSize` / 固定 frame 且卡片离屏变体**不含**会吃无限高度的 `Spacer`）。预览路径与保存路径尺寸语义对齐，禁止「Preview 有高、保存无高」。
2. **主线程不永久卡死**：点「保存相册」后要么在合理时间内出现「已保存到相册」toast /「保存失败」alert，要么显示「保存中」后以失败收口；禁止无反馈永久冻结。
3. **不崩溃**：模拟器至少跑通一次完整路径；若本机有真机则补一次真机（无真机则如实写「真机未验」）。
4. **诚实反馈不回退**：成功仍用 `BTToast`；失败仍用 alert（F-TR-01 / F-OV-04）。
5. **回归测试**：新增可自动化断言——至少覆盖「给定 `TrainingSessionSummary`，离屏渲染产出非 nil、像素尺寸在合理上界内」（例如宽=361×scale，高有限且远小于「失控 Spacer」量级）。UI 全流程相册权限若模拟器难稳，单测钉渲染契约即可，UI 冒烟列入完成说明。

### 明确不做

- 不做微信好友/朋友圈（H-05 仍推迟）。
- 不改总结页「保存训练」SwiftData 逻辑（除非复现证明同根因）。
- 不借机重做分享卡视觉设计。
- 不扩大到 `问题集合_v8.md` 练习 Tab 条目。

### 红线

- 根因未用复现/栈证据确认前，禁止用「加 delay / 吞异常 / 假成功」压症状。
- 禁止删除 `Spacer` 却不核对预览布局是否塌缩（若删 Spacer，须保证屏上预览与导出图高度一致策略写清）。

### 建议改法方向（供执行参考，非强制选型）

优先序：

1. **首选**：保存路径对 `BTShareCard` 使用与 Preview 同级的有界 frame（宽 361 + 显式高度，或按内容 `fixedSize` + 去掉离屏专用变体中的 `Spacer`）。
2. **更稳**：抽 `ShareCardImageRenderer.render(session:theme:… ) -> UIImage?` 单一真源，View 与测试共用；高度策略集中一处。
3. 若验证显示 `ImageRenderer` 仍不稳：降级为 `UIGraphicsImageRenderer` + 简化绘制——仅在首选失败后启用，并写 FL/DR。

---

## 五、锚点索引（2026-07-17 核实）

| 锚点 | 路径 | 行号/符号 | 备注 |
|---|---|---|---|
| 分享 sheet 入口 | `QiuJi/Features/Training/Views/ActiveTrainingView.swift` | L43–45 `onGenerateShareImage`；L136–138 `.sheet`；L173–190 `buildShareSession()` | 总结 → 分享 |
| 保存相册 | `QiuJi/Features/Training/Views/TrainingShareView.swift` | L213–220 按钮；L262–305 `saveCardToPhotos` | `ImageRenderer` 仅 width 361 |
| 分享卡布局 | `QiuJi/Core/Components/BTShareCard.swift` | L86–110 body（含 L100 `Spacer`）；Preview L294–309 固定 height 480 | 预览有界、保存无界 |
| 品牌标 | `QiuJi/Core/DesignSystem/BTBrandLogo.swift` | 全文 | 资产 `BrandLogoMark`，离屏一般可用 |
| 权限 Info | `project.yml` / `QiuJi.xcodeproj/project.pbxproj` | `NSPhotoLibraryAddUsageDescription` | 已配置 |
| 历史相关 | F-TR-01（W2-2）、F-OV-04（W2-9） | `tasks/PROGRESS.md` / ui-polish 文档 | 真写入 + toast，未覆盖崩溃 |

---

## 六、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v9.0 | 2026-07-17 | 立项：B1 训练分享保存相册卡死/闪退；单批 W1；锚点核实；与 raw `问题集合_v8.md` 划界 |
| v9.1 | 2026-07-17 | **W1 ✅**：根因假设（无界 Spacer→失控位图/主线程挂死）在模拟器 XCTest **未证实**（width-only 完成于 ~5ms、895px 高，bounded 960px）；仍钉死 Preview 对齐契约 `ShareCardImageRenderer` 361×480；`Task.yield` 让「保存中」可上屏；回归单测 + UI 冒烟 toast「已保存到相册」。产物 `build/w1-*`。真机：设备在线但 install 失败 → **真机完整路径未验**。 |
| v9.2 | 2026-07-17 | **W1 返工第 1 轮 ✅（真根因）**：用户真机复测仍秒闪退 → 主控实测：主 target `GENERATE_INFOPLIST_FILE=NO`，`INFOPLIST_KEY_*` 被忽略，产物 Info.plist 无 `NSPhotoLibraryAddUsageDescription` → 首次权限弹框 TCC 强杀。修复：三权限键直写 `QiuJi/Resources/Info.plist` + `project.yml` info.properties（留防回退警告注释）；PlistBuddy 证 iphoneos+sim 产物三键在包；`PhotoPermissionInfoPlistTests` 2/0；未预授权限真实弹框冒烟通过（弹框截图+允许+toast）。产物 `build/w1r1-*`。真机 App 已装上、自动冒烟被用户中止，待人工点验。 |

---

## 八、W1 执行结论（2026-07-17）

### 根因验证

| 项 | 结果 |
|---|---|
| 假设：无界高度 + Spacer → 卡死/超大图 jetsam | **模拟器未证实**（见 `build/w1-evidence/`） |
| 事实：Preview 有高、保存无高 | **确认**；已对齐 |
| 次要：同步 `uiImage` 阻塞导致「保存中」不刷 | **已加固**（`await Task.yield()`） |

### 改动文件

- `QiuJi/Core/Components/ShareCardImageRenderer.swift`（新建）
- `QiuJi/Features/Training/Views/TrainingShareView.swift`
- `QiuJiTests/ShareCardImageRendererTests.swift`（新建）
- `QiuJiTests/ShareCardImageRendererRootCauseDiagTests.swift`（新建，诊断留档）
- `QiuJiUITests/W1_ShareSavePhotosUITests.swift`（新建）
- `QiuJi.xcodeproj`（xcodegen）

### 验证命令与结果

```bash
xcodebuild … build → BUILD SUCCEEDED（build/w1-logs/build.log L321）
xcodebuild … -only-testing:QiuJiTests/ShareCardImageRendererTests test → TEST SUCCEEDED（unit-regression.log）
xcodebuild … -only-testing:QiuJiUITests/W1_ShareSavePhotosUITests test → TEST SUCCEEDED；截图含 toast「已保存到相册」
```

---

## 七、执行入口

新会话说：**「执行问题集合 v9 批次 W1」**（或「修训练分享保存相册崩溃」）。

执行前按 `plan-batch-execution`：锁本文件为真源 → 回显锚点与红线 → 先复现/验证根因假设 → 再改码 → 按 §四 完成标准实证验收 → 回写本文件状态与 `tasks/PROGRESS.md`。
