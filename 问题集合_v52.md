# 问题集合 v52 — 训练首页「每日清台」

> **本文件是本轮需求真源**（v52.1，制定日期：2026-09-03，状态：**W0–W4 已完成**）。
> **用户目标**：在训练首页「本周训练」卡片标题右侧增加「每日清台」入口；进入后复用自由击球页，按「我的 → 偏好设置」的默认玩法自动开好球，用户直接开始单人清台，并可主动重新开球。
> **语义边界**：v52.0 源于用户「分析得很好，写一个方案」的规划授权；用户随后明确要求开始执行并完成全部任务，v52.1 据此记录 W0–W4 的实现与验收终态。
> **方案方法**：按 `issue-collection-restructure` 的需求回显、实码锚点、依赖拆批、可验证完成标准与版本记录契约编写；执行阶段按 `plan-batch-execution` 串行完成，并以真实测试退出码和非跟踪证据目录收口。

---

## 一、需求回显与不可弱化口径

| ID | 用户需求 | 本方案的不可弱化口径 |
|---|---|---|
| R1 | 在截图中的训练首页加「每日清台」按钮 | 入口放在「本周训练」卡片标题行右侧，是独立可点的次要胶囊按钮，不吞并「本周训练」与「今日安排」两张兄弟卡片 |
| R2 | 它对应自由击球练习页 | 复用 `FreePlayView` 的球桌、击球、回放、开球与物理链路；通过明确的入口模式分支「普通自由击球」与「每日清台」，不复制第二张球桌页 |
| R3 | 进入时默认开球 | 当天没有未完成草稿时，页面建好场景后立即按默认玩法自动执行开球，播放开球动画，球停稳后自动交付单人清台阶段，不再要求用户点「完成」 |
| R4 | 每次进入会自动开好球 | 「每次」按日常任务语义解释为：当天首次、没有草稿时自动新开；误触返回、切后台或中途退出后再进入必须恢复同一局，禁止悄然换盘 |
| R5 | 默认开球模式可在「我的，设置」中设定 | 在「我的 → 偏好设置」新增「每日清台默认玩法」，与个人信息中可为「两者」的「主打球种」分开；新设置只影响下一个新局，不改变正在进行的草稿 |
| R6 | 默认玩法包括中八、九球等 | 直接对齐现有开球能力：中式八球、9 球、6 球、5 球、4 球；设置用单选列表/菜单，不把 5 个选项硬塞进一行胶囊 |
| R7 | 用户可以自己重新开球 | 「重新开球」回到摆好球的 `.racked` 阶段，让用户自己调开球点、方向、力度和打点后点「开球」；已打过清台杆时先确认会放弃本局进度 |

### 1.1 本方案默认产品决策

1. **每日清台是单人挑战，不是玩家 A/B 对局**。页面复用自由击球的物理与交互壳，但不直接复用其「一人扮两方」的记分牌和轮转文案。
2. **屏内引擎进球不代表真实球技**。每日清台只记独立的「今日未开始 / 进行中 / 已完成」，保持 `kind="tool"` 边界，不计入训练周目标、真实准确率或今日安排完成数。
3. **未完成就恢复，不自动换局**。当天首次新建时自动开球；已有草稿时恢复球形、规则状态、杆数、犯规数与有效用时。
4. **当天只授予一次「已清台」状态**。完成后仍可再玩，但不重复累加当日成就；本版不做排行榜、奖励、打卡补签或跨设备同步。
5. **系统自动开球是出题过程，不计入用户杆数或犯规**。系统开球若白球落袋，沿用现有补回母球的逻辑并继续；若 8 号/9 号等终局球直接落袋，不允许零操作完成，改用下一 seed 自动再开，最多 3 次；仍失败则停在摆架态并提示用户手动开球，禁止无界重试。
6. **普通自由击球保持原样**。「练习 → 打 → 自由击球」仍是玩家 A/B 对局，手动选玩法、手动开球、停稳后手动点「完成」；每日清台不得偷改这条入口。

### 1.2 状态、文案与完成口径

#### 首页入口三态

| 当日状态 | 按钮文案 | 点击行为 |
|---|---|---|
| 无草稿、未完成 | 每日清台 | 创建当日挑战，按默认玩法自动开球 |
| 有未完成草稿 | 继续清台 | 恢复同一盘球形与全部挑战状态 |
| 已完成 | 今日已清台 ✓ | 进入完成态/再来一局，不重复记当日完成 |

按钮使用现有 `BreakRackGlyph` 的球架语义，不固定显示 8 号球而在 9 球/少球玩法下产生误导。无障碍标签须包含状态和当前玩法，例如「继续清台，中式八球」。

#### 单人规则

- **中式八球**：系统开球后保持开放局；用户第一杆合法进袋的首颗全色/花色球决定本局球组；清空本组后合法打进 8 号球才完成。
- **9 球与 4/5/6 球**：首碰必须是桌面最低号球；允许合法组合进 9 号；合法打进 9 号球时完成。
- **普通犯规**：累加犯规数，母球可自由摆放，用户继续清台；不切换到「玩家 B」。
- **致命犯规**：未清本组时打进 8 号，或犯规同时打进该玩法的终局球，本次尝试结束但不授予「今日已清台」；页面提供「重新开球」。
- **杆数口径**：只计系统已交付散局后的用户击球；系统自动开球和用户重新开球的开球杆均是「出题/换盘」，不计入清台杆数，保持两种入局方式可比。

#### 挑战页 HUD

球桌与现有控件位置不重排，只把顶部双人记分信息换成单人信息：

- 页名：「每日清台」。
- 状态胶囊：玩法 + 未完成/清台中/本次失败/已完成。
- 精简指标：剩余目标球、杆数、犯规数、有效用时。
- 完成时：给出玩法、杆数、犯规数、用时，以及「再来一局」；不显示虚构的真实球技准确率。

---

## 二、现状盘点（2026-09-03 实读）

| 范围 | 当前代码事实 | 对 v52 的影响 |
|---|---|---|
| 训练首页 | `TrainingHomeView.weeklyProgressCard` 当前标题行只有「本周训练」；卡片末尾用 `.accessibilityElement(children: .combine)` 合并全部子项 | 按钮必须改成独立可聚焦元素；不能直接塞进当前的合并无障碍容器 |
| 无计划首页 | `emptyStateContent` 只在当日有补充训练记录时才显示本周卡 | 每日入口不得因用户没有激活计划而消失；本周卡要在首页稳定可见 |
| 训练导航 | `TrainingRoute` 只有计划列表/详情/模版编辑；自由击球只注册在 `AngleRoute.freePlay` | 新增训练栈自有的 `TrainingRoute.dailyClearance`，禁止为打开页面先偷切到「练习」Tab |
| 自由击球入场 | `FreePlayView` 只有默认初始化；用户点开球入口、选玩法后才调 `startBreakFlow(game:manualDeliver: true)` | 需增加可测试的 `entryMode`/configuration；每日模式在 `setupScene` 后自动起流 |
| 现有开球 | `BreakFlowRunner` 已支持中八、9/6/5/4 球，可调矄准/力度/打点，开球后可重开；`PositionPlayViewModel.startBreakFlow` 已有自动交付能力 | 复用共享 runner；只做可选 seed、结果元数据与每日编排缝，不再造开球引擎 |
| 规则引擎 | `ChineseEightBallRules` / `ZhuifenRules` 明确是「单机双玩家」，内部保存当前球员、双方球组/得分和轮转 | 每日清台新建纯单人规则状态机，复用 `ShotFacts`/`BallGroup` 与合法首碰语义，禁止在 View 里散落规则 if/else |
| 草稿货币 | `BoardSnapshot` 已是 `Codable`，可保存当前在桌球坐标 | 当日草稿不需为球形新造数据格式 |
| 个人偏好 | `UserPreferences` 只有 `PreferredSport` 的中八/9球/两者，`SettingsView` 当前只有外观、声音、教学辅助等节 | 新增独立 `DailyClearanceGame`；当前玩法不能从「两者」直接推导 |
| 数据口径 | `FreePlayView` 使用 `.toolUsageSession(.freePlay)`；`TrainingSessionKind.tool` 明确不计周目标与准确率 | 新增 `ToolUsageEntry.dailyClearance` 区分入口，但仍是 `kind="tool"`；当日完成状态走本地轻量 store，不污染真实训练统计 |
| 现有测试 | 已有 `BilliardRulesEngineTests`、`BreakFlowRunnerV6Tests`、`S1_FreePlayLayoutUITests`、`S6_BreakUniversalUITests`、`X3_BreakFlowUITests` 和训练首页状态测试 | 新测试在现有 seam 上扩展；普通自由击球回归必须继续通过 |

### 2.1 当前工作区边界

- 方案落笔时工作区已有大量未提交改动，且 `TrainingHomeView.swift`、`QiuJiApp.swift`、`UserPreferences` 相关调用面与部分测试属于高重叠区。
- v52 执行前必须先保存定向路径的基线 status/diff；不清理、回退、覆盖或提交其他任务的修改。
- 不以「整个工作区 clean」作为 v52 完成标准；只要求基线改动原样保留、v52 预期路径发生可解释差异、测试没有新增意外写盘。

---

## 三、范围、冻结项与交付物

### 3.1 本轮纳入

1. 训练首页每日清台入口、三态文案、无计划可见性与独立无障碍语义。
2. 训练导航栈直达每日清台，不改当前 Tab。
3. 每日默认玩法设置与首次默认值。
4. `FreePlayView` 的入口模式化，以及每日模式的自动开球、单人 HUD、完成/失败和重新开球。
5. 当日挑战草稿与完成状态的本地持久化，包括后台/退出恢复和日期边界。
6. 每日清台工具使用时长的独立标识，并继续排除准确率与周目标。
7. 规则、开球、恢复、设置、首页、路由、Light/Dark、紧凑屏、iPad 与无障碍验收。

### 3.2 本轮冻结

- 不改当前「本周训练」天数、连续天数、周一至周日圆点的真实训练口径。
- 不把每日清台加进「今日安排」Drill 列表，不改「今日完成 X/Y」。
- 不改普通自由击球的双人规则、手动选玩法、手动完成开球流程。
- 不新增 SwiftData Schema、迁移计划或后端 DTO；当日草稿/完成态暂为本地设备数据。
- 不做跨设备同步、历史趋势、分享卡、排行榜、勋章、提醒推送、补签、连续清台日历或付费墙。
- 不新增第三方依赖，不修改球桌物理常量或摆架坐标。
- 不生成新位图素材；入口图形复用现有球架图标。

### 3.3 计划交付物

| 交付物 | 计划路径/形态 |
|---|---|
| 本轮真源 | `问题集合_v52.md` |
| 单人挑战模型/规则 | 新增 `QiuJi/Core/Rules/DailyClearanceRulesEngine.swift` |
| 当日草稿/完成状态 | 新增 `QiuJi/Data/Services/DailyClearanceStore.swift`（`Codable` + `UserDefaults`） |
| 挑战编排 | 新增 `QiuJi/Features/PositionPlay/ViewModels/DailyClearanceController.swift` |
| 共享开球增量 | `BreakFlowRunner.swift`、`PositionPlayViewModel.swift` |
| 复用页入口模式 | `FreePlayView.swift` |
| 首页入口 | `TrainingHomeView.swift` |
| 导航 | `PlanListView.swift` 中 `TrainingRoute`、`MainTabView.swift` |
| 偏好 | `UserPreferences.swift`、`SettingsView.swift` |
| 使用时长区分 | `ToolUsageTracker.swift` |
| 单元测试 | 新增 `DailyClearanceRulesTests.swift`、`DailyClearanceStoreTests.swift`；扩展 `BreakFlowRunnerV6Tests.swift` |
| UI 测试 | 新增 `DailyClearanceUITests.swift`；回归现有自由击球/首页套件 |
| 规格同步 | `docs/04-功能规划.md`、`docs/05-信息架构与交互设计.md`、`.kiro/steering/content-data-contract.md`、`tasks/UI-IMPLEMENTATION-SPEC.md`、`docs/00-讨论记录.md` |
| 路由/写盘台账 | `docs/design/v47/route-coverage.csv`、`route-surface-signatures.sha256`、`write-surface-files.txt`、`write-surface-audit.md` |

> 新文件路径是方案规划位，不冒充现有代码锚点。执行时如发现现有文件更适合承载，可在不改需求语义的前提下调整落点，并先升版本文件。

---

## 四、状态与数据契约

### 4.1 可持久草稿

`DailyClearanceDraft: Codable` 至少保存：

- `challengeDay`：按当前 `Calendar` 规整的日期标识，禁止只比较 24 小时时差。
- `game`：可持久的 `DailyClearanceGame`，映射现有 `RackGame`。
- `seed`：首次自动开球前即写入；若 App 在物理求解/回放中退出，再进入用同 seed 重放，禁止换盘。
- `phase`：至少区分 `autoBreaking`、`playing`、`failed`；完成后转独立 completion，不继续保留 active draft。
- `board`：已交付散局后的 `BoardSnapshot`；开球未停稳时可为 nil。
- `ruleState`：中八的开放/已定组，或 9 球系的最低号要求与失败/完成状态。
- `shotCount`、`foulCount`、`activeDurationSeconds`、`startedAt`、`updatedAt`。

保存时机：创建挑战与确定 seed 后、开球停稳交付后、每杆停稳裁决后、手动重开后、App 进入非 active 态与页面消失前。

### 4.2 完成记录

`DailyClearanceCompletion: Codable` 仅保存当前/最近必要状态：`challengeDay`、`game`、`shotCount`、`foulCount`、`activeDurationSeconds`、`completedAt`。它服务首页「今日已清台」与完成总结，不是训练准确率样本。

日期边界：

1. 挑战在创建时绑定 `challengeDay`。
2. 页面前台持续跨零点时，允许完成正在进行的挑战，完成归属于其创建日。
3. 新一天重新从首页进入时，不恢复昨日未完成草稿；上一日草稿作废，创建新挑战。

### 4.3 存储边界

- 草稿与 completion 使用集中的 `DailyClearanceStore`，不允许 View 直接散落 `UserDefaults.standard.set` 键。
- 键名一旦发布即冻结；解码失败必须输出可定位信息并安全清理该草稿，禁止 `try?` 静默吞掉错误。
- 首版本地保存、不跨设备同步；若执行期想改为 SwiftData/后端历史，已超出本方案并命中 ADR/数据契约变更，必须暂停升版本文件。

---

## 五、交互与共享层契约

### 5.1 `FreePlayView` 入口模式

规划增加可显式测试的入口模式（命名可在实施时依现有风格微调）：

```swift
enum FreePlayEntryMode: Equatable {
    case standard
    case dailyClearance
}
```

- `.standard`：保持当前手动选玩法、玩家 A/B 规则、手动确认散局、`.toolUsageSession(.freePlay)`。
- `.dailyClearance`：标题改为「每日清台」，由 `DailyClearanceController` 决定创建/恢复，使用单人规则和 `.toolUsageSession(.dailyClearance)`。
- 两者共用 `PositionPlayViewModel`、Scene、球桌布局、球库、击球、回放、打点/力度和 `BreakFlowRunner`。

### 5.2 自动开球编排

1. `setupScene()` 只成功一次后才能恢复草稿或起开球流，禁止 `body` 重算多次触发开球。
2. 无草稿：先持久化 game + seed + `autoBreaking`，再调 `startBreakFlow(game:seed:manualDeliver: false)` 与 `breakNow()`。
3. 计算/回放中退出：再进入使用同 seed 重放同一次开球。
4. 停稳后 runner 向每日控制器交付 `BreakOutcome`（至少含 board、seed、pocketed、cueScratched、terminalBallPocketed、settled）。
5. 只有「系统出题时终局球直接落袋」触发有界再开；不对一般散局做「可玩性筛选」，普通自由击球继续保持 WYSIWYG。

`BreakOutcome` 应以增量方式加到共享层：既有三个开球宿主仍可只消费 `BoardSnapshot`，不迫使全部页面重写回调。

### 5.3 重新开球与换玩法

- 清台阶段尚未击球：「重新开球」直接放弃当前系统散局、换 seed 并停在摆架态。
- 已有用户击球：先弹确认「重新开球会放弃本局进度」；确认后清空当前草稿的球形/规则/计数，换 seed 并进入手动开球。
- 开球的矄准、力度、打点与母球位置沿用现有 `BreakInstrumentsOverlay`/`BreakControlBar`。
- 页内「切换玩法」可复用 `BreakGamePickerSheet`，但属于当局临时选择，不悄然改写「偏好设置」默认值；已有进度时同样需要确认。

### 5.4 时间与生命周期

- 用时只累加页面可见且 App active 的时间；后台、来电中断或页面消失不计入。
- 实现使用 Date 差值 + 已累加秒数，不用可在后台暂停的「每秒 +1」当真源。
- 页面反复 appear/disappear 不得重复累加同一时段，也不得重复创建工具使用会话。

---

## 六、批次表与依赖

| 批次 | 范围 | 量级 | 依赖 | 主要交付 |
|---|---|---:|---|---|
| W0 | 单人规则、持久化模型、日期口径与数据边界 | M | — | 纯规则引擎、草稿/completion store、单元测试 |
| W1 | 开球结果增量、确定 seed、自动开球编排和恢复 | L | W0 | `BreakOutcome`、`DailyClearanceController`、自动/恢复/重开闭环 |
| W2 | `FreePlayView` 每日模式、单人 HUD、完成/失败与页内操作 | L | W1 | 可交互的每日清台页，普通自由击球无回归 |
| W3 | 训练首页入口、无计划可见、导航、偏好设置、工具记录区分 | M | W0 + W2 | 首页三态、偏好持久化、训练栈直达、`kind="tool"` 边界 |
| W4 | 系统回归、多尺寸/Light-Dark/无障碍验收、规格与台账同步 | L | W0–W3 | 完整测试证据、截图、文档、路由/写盘门禁收口 |

### 6.1 依赖图

```text
W0 规则 + 草稿真源
 └── W1 开球增量 + 每日编排
      └── W2 自由击球每日模式
           └── W3 首页 + 导航 + 偏好 + 记录边界
                └── W4 系统验收与规格收口
```

W0–W4 共享 `FreePlayView` / `PositionPlayViewModel` / 每日 store 的语义上下文，且后批依赖前批 API，按一条主链串行最稳妥；不为形式并行而重复读取和修改同一组文件。

### 6.2 执行状态

| 批次 | 状态 | 备注 |
|---|---|---|
| W0 | ✅ 已完成 | 单人规则、草稿/完成记录、默认玩法迁移已实现；16 个定向测试通过 |
| W1 | ✅ 已完成 | 固定 seed、增量 `BreakOutcome`、每日控制器与有界重开已实现；47 个定向/回归测试通过 |
| W2 | ✅ 已完成 | `FreePlayEntryMode`、单人 HUD、恢复/重摆/换玩法/完成再玩已接入；7 条 controller 测试、4 条每日 UI 场景与普通页 S1 回归通过 |
| W3 | ✅ 已完成 | 首页三态、空态稳定入口、训练栈导航、五玩法设置持久化与 `tool` 统计边界已接入；10 条控制器测试、23 条工具记录测试及 3 条首页/设置 UI 主链通过 |
| W4 | ✅ 已完成 | 单元/共享回归、完整 UI 套件、三设备 Light/Dark/AX 矩阵、规格与路由/写盘门禁均已收口 |

---

## 七、各批详细范围与完成标准

### W0 — 单人规则与当日状态真源

**范围**

1. 定义 `DailyClearanceGame`，与中八、9/6/5/4 球 `RackGame` 一一映射，支持 Codable/CaseIterable/展示名。
2. 定义纯 `DailyClearanceRulesEngine`，消费现有 `ShotFacts`，输出合法目标、单杆裁决、犯规、失败、完成和可持久规则状态。
3. 定义 `DailyClearanceDraft` / `DailyClearanceCompletion` 与 `DailyClearanceStore`；所有日期判断可注入 Calendar/now，测试不依赖真实零点。
4. 新局默认值：已有用户首次没有独立键时，主打球种=中八 → 中八，9 球 → 9 球，两者 → 中八；一旦用户修改新键，与主打球种解耦。

**完成标准**

- 中八：开放局定组、合法首碰、普通犯规不轮转、清组后进 8 完成、早进/犯规进 8 失败全有精确单元测试。
- 9 球系：最低号首碰、合法组合进 9 完成、错首碰/白球落袋犯规、犯规进终局球失败全有测试。
- 草稿可编码/解码且 `BoardSnapshot` 往返无损；今天/昨天、完成后再玩、新日作废旧草稿、损坏数据显式处理全有测试。
- 不新增 SwiftData 模型/迁移，`TrainingSessionKind.goalCounting` 与 `countsTowardAccuracy` 无变化。
- 定向新测试全部通过，`git diff --check -- <W0 预期路径>` 通过。

### W1 — 自动开球、结果交付与可恢复编排

**范围**

1. 为 `BreakFlowRunner` 增加可选固定 seed 的宿主通路，不改普通初值仍随机的语义。
2. 增量交付 `BreakOutcome`，保留既有只消费 `BoardSnapshot` 的宿主 API；表达终局球落袋、白球落袋、未停稳等事实。
3. 建立 `DailyClearanceController`：首次自动开球、有界重开、散局交付、规则裁决、每杆保存、完成/失败、后台用时结算、草稿恢复和手动重开。
4. 增加测试 seam，允许 UI 测试绕过真实物理等待直接注入已停稳盘面；生产入口仍必须走真实 `BreakSimulator`。

**完成标准**

- 同 game + seed 两次自动开球交付的桌面逐球一致；中途退出再进入不换 seed。
- 系统开球不增 shot/foul；白球落袋可继续；终局球落袋最多自动再开 3 次，第 4 次不得继续递归/排队。
- 手动重开在未击球时直接进入 `.racked`，已击球时返回「需确认」决策；确认后全部旧挑战计数清零。
- 页面消失/后台时长不累加；重复 flush 幂等。
- 既有 `BreakFlowRunnerV6Tests`、`BilliardRulesEngineTests` 与三个现有开球宿主的编译/定向测试无回归。

### W2 — 每日清台页面模式

**范围**

1. 给 `FreePlayView` 注入 `FreePlayEntryMode`，默认 `.standard`，保持所有既有调用编译且行为不变。
2. 每日模式初次只起一次创建/恢复；自动开球时显示玩法与「正在开球」，停稳后自动切为清台 HUD。
3. 用单人状态胶囊替换玩家 A/B 记分牌，显示剩余目标球/杆/犯规/用时；合法目标球拦截走新单人规则。
4. 接入失败与完成反馈、重新开球确认、页内临时换玩法；完成后「再来一局」不擦除当日 completion。
5. 保存时机紧跟真实停稳/裁决，不在击球动画中保存半帧桌面。

**完成标准**

- 首次进入从默认玩法自动开球，无玩法选择 sheet，无额外「完成」点击；开球停稳后可立即击球。
- 已保存草稿进入后桌面、定组/最低号要求、杆数、犯规数和用时均恢复。
- 重新开球、取消确认、确认放弃、临时换玩法与完成后再玩全部有 UI 自动化覆盖。
- 普通自由击球的页名、玩家 A/B HUD、手动选玩法、开球停稳「完成」和现有 AX identifier 保持。
- `freeplay.stage` 在自动开球、清台与手动重开三态之间 frame 不抖动；普通页 `S1_FreePlayLayoutUITests` 继续通过。

### W3 — 首页入口、偏好、导航与统计边界

**范围**

1. `weeklyProgressCard` 标题行改为 HStack：左为标题，右为三态每日入口；球架图标、文案、箭头与截图层级一致。
2. 将卡片「统计摘要」与「每日按钮」分成独立无障碍元素；按钮命中区不小于 44pt，默认字号、最大可用字号和 VoiceOver 顺序可理解。
3. `emptyStateContent` 始终显示本周卡；有当日补充 Drill 时再显示今日安排，无计划无记录也保留每日入口。
4. 新增 `TrainingRoute.dailyClearance`，导航到 `FreePlayView(entryMode: .dailyClearance)`；返回后首页自动刷新按钮状态。
5. 在 `SettingsView` 增加「每日清台」节与单选玩法，说明「只影响下一个新局」；加入 `UserPreferences` 持久化。
6. 新增 `ToolUsageEntry.dailyClearance`，存储名称「每日清台」；继续使用 `TrainingSessionKind.tool`。

**完成标准**

- 有计划、无计划无记录、无计划但有自由训练记录三种首页状态都可见并可点每日入口。
- 首页按钮在未开始/草稿/完成三态分别显示准确文案，重启 App 后仍正确。
- 从训练首页进入/返回后仍留在训练 Tab，训练导航栈可返回，不污染练习栈。
- 设置支持五种玩法并可持久化；正在进行的草稿不因设置变更被换局。
- 每日使用会话在历史中显示「每日清台」，但 `goalCounting`/`countsTowardAccuracy` 断言明确为 false；本周天数、连续天数、今日 X/Y 不变。
- iPhone SE 宽度与最大可用字号下按钮不覆盖标题，不裁切关键状态；按钮保持独立 hittable。

### W4 — 系统验收与真源收口

**自动化覆盖**

1. 全部 W0–W3 新单元测试。
2. `BilliardRulesEngineTests`、`BreakFlowRunnerV6Tests`、现有开球编排定向测试。
3. `S1_FreePlayLayoutUITests`、`S6_BreakUniversalUITests`、`X3_BreakFlowUITests`、`P5_AngleTrainingUITests.testNavigateToFreePlay`。
4. 首页无计划/有计划状态测试、偏好持久化测试、工具记录与周目标边界测试。
5. `make -f scripts/Makefile build` 与适用的总门禁；任何命令必须保留真实退出码，不用 `|| true` 伪造通过。

**UI 验收矩阵**

| 页面/状态 | iPhone SE | iPhone 17 Pro | iPad mini | Light | Dark |
|---|---:|---:|---:|---:|---:|
| 训练首页：有计划 + 三态按钮 | ● | ● | ● | ● | ● |
| 训练首页：无计划无记录 | ● | ● | ○ | ● | ● |
| 偏好设置：五玩法 + 大字号 | ● | ● | ● | ● | ● |
| 每日页：自动开球/清台/重开/完成 | ● | ● | ● | 黑底恒定 | 黑底恒定 |
| 普通自由击球回归 | ● | ● | ○ | 黑底恒定 | 黑底恒定 |

注：「黑底恒定」是现有场景页设计常量，不等于跳过 Light/Dark 启动、导航栏、sheet 与系统控件检查。

**文档与台账收口**

- 在 `docs/04`、`docs/05`、UI SPEC 和讨论记录写明入口、状态、默认玩法与单人规则。
- 在内容/数据契约写明每日清台仍是 `kind="tool"`，独立 completion 不参与准确率/周目标，当前不同步。
- 每日页是同一 `FreePlayView` 的状态变体，在 route coverage 中按可见状态登记，并更新 route surface 签名。
- 新 UI 测试如有截图/文件输出，先盘点其写盘路径并更新 `write-surface-files.txt` / `write-surface-audit.md`；验收是基线差集，不是整仓粗暴求 clean。

**W4 完成标准**

- 五种玩法至少都完成「默认值 → 自动开球 → 可击球」的规则/编排自动测试；中八和 9 球各完成一条 UI 端到端主链。
- 断点恢复覆盖：自动开球中、已交付未击球、已打若干杆、失败、已完成后再进入。
- 首页三态、无计划可见、设置影响新局但不影响草稿、普通自由击球无回归均有可追溯自动证据。
- 首页/设置的 Light/Dark、紧凑屏、iPad mini 和最大可用字号不裁切、不覆盖、按钮可点；无障碍标签和顺序正确。
- build、定向单测/UI 测试与适用门禁真实通过；截图、xcresult 与日志路径全部在非跟踪证据目录。
- 路由/写盘台账与实际编译、测试行为一致；基线已有改动无新增意外覆盖。

---

## 八、实码锚点索引（核实于 2026-09-03）

| 锚点 | 实读位置 | 用途/影响 |
|---|---|---|
| 训练首页结构 | `QiuJi/Features/Training/Views/TrainingHomeView.swift:251–273` | 有计划时的本周/今日兄弟卡布局 |
| 本周卡标题与 AX | `TrainingHomeView.swift:370–451` | 入口目标行；当前卡片整体 children combine |
| 无计划状态 | `TrainingHomeView.swift:990–1020` 附近 `emptyStateContent` | 当前本周卡是条件渲染，需改为稳定可见 |
| 首页周目标计算 | `TrainingHomeView.swift:564–597` 附近 | 保持只消费 `TrainingGoalMetrics.goalCounting` |
| 训练路由 | `QiuJi/Features/Training/Views/PlanListView.swift:4–11` | `TrainingRoute` 当前无每日页 |
| 训练路由注册 | `QiuJi/App/MainTabView.swift:9–14,145–157` | 训练栈的 destination switch |
| 练习栈自由击球 | `MainTabView.swift:203–208` | 既有 `AngleRoute.freePlay` 必须保持 standard |
| 自由击球初始状态 | `QiuJi/Features/PositionPlay/Views/FreePlayView.swift:12–108` | 现为固定 VM、手动选玩法、玩家 A/B 规则和 tool 埋点 |
| 自由击球菜单 | `FreePlayView.swift:458–479` | 每日模式增重开/换玩法操作 |
| 玩法列表 | `QiuJi/Core/Rack/BreakFlowRunner.swift:30–49` | 现有中八、9/6/5/4 球与显示名 |
| 开球状态/交付 | `BreakFlowRunner.swift:20–75,313–367` | racked/computing/breaking/settled、自动交付、重开与确认 |
| 宿主开球编排 | `QiuJi/Features/PositionPlay/ViewModels/PositionPlayViewModel.swift:1961–2007` | 已有 `manualDeliver` 切换；待增 seed/outcome seam |
| 盘面快照 | `QiuJi/Core/PositionPlay/PositionPlayModels.swift:40–68` | `BoardSnapshot: Codable` |
| 玩法类型 | `QiuJi/Core/Rack/RackLayout.swift:19–40` | `RackGame` 映射与球数真源 |
| 双玩家规则 | `QiuJi/Core/Rules/BilliardRulesEngine.swift:7–71,98–135,230–327` | 已确认为 A/B 轮转，每日不可直接套用 |
| 用户偏好 | `QiuJi/Data/Services/UserPreferences.swift:5–18,75–186` | 主打球种含「两者」；新建独立每日默认值 |
| 偏好设置页 | `QiuJi/Features/Profile/Views/SettingsView.swift:3–39,90–155` | 现有节点次序与卡片风格 |
| 工具时长 | `QiuJi/Data/Services/ToolUsageTracker.swift:5–75,80–117` | 增每日入口标识，保持 tool 口径 |
| 统计边界 | `QiuJi/Data/Models/TrainingSessionKind.swift:3–26` | tool 不计周目标/准确率 |
| 规则测试 | `QiuJiTests/BilliardRulesEngineTests.swift` | 现有双人规则回归基线 |
| 开球测试 | `QiuJiTests/BreakFlowRunnerV6Tests.swift` | 现有默认力度、seed、重开/完成语义基线 |
| 自由击球 UI | `QiuJiUITests/S1_FreePlayLayoutUITests.swift`、`S6_BreakUniversalUITests.swift`、`X3_BreakFlowUITests.swift` | 页面布局、三宿主开球、散局确认回归 |
| 首页状态 UI | `QiuJiUITests/V50StateMatrixUITests.swift` | 无计划/长数据/菜单等现有 fixture 基础 |

---

## 九、风险、冲突与回滚单位

| 风险 | 处置 |
|---|---|
| 每日模式深改普通自由击球 | `FreePlayEntryMode.standard` 是默认；现有页名、手动开球、A/B HUD、AX id 与定向 UI 测试全部作回归红线 |
| 一个 View 继续堆积规则和持久化 if/else | 规则收口到 `DailyClearanceRulesEngine`，编排收口到 controller，store 不进 View |
| 首页入口放在本周卡导致用户以为计周目标 | 按钮只改自身三态，不点亮周圆点；数据契约、测试与必要说明同时锁定 |
| 设置改动悄然替换未完成草稿 | 草稿内冻结 game；设置只在无草稿创建新局时读取 |
| 自动开球在 onAppear/body 重复启动 | controller 以 draft phase + 单次 setup gate 幂等；需求 UI 测试反复前后台/导航 |
| 自动开球终局球落袋造成零操作完成或无界重试 | 终局球仅重开 3 次；还失败就进手动摆架态，单测证明不有第 4 次自动队列 |
| 系统开球结果与用户重开的计数不可比 | 两种开球都属出题阶段，不计清台 shot/foul；清台计时从散局交付开始 |
| 跨零点和时区变化产生重复挑战 | 日期通过注入 Calendar 规整；测试 DST/时区改变后的日标识，不用纯秒数判断 |
| UserDefaults 数据损坏后静默消失 | 显式 do/catch + 可定位日志 + 只清该草稿；禁止 `try?` |
| 新路由/UI 测试漏更台账 | W4 同步 route coverage/signature 与全部写盘文件，然后跑总门禁 |
| 方案与当前 dirty 改动互相覆盖 | 每批只检查相关路径基线差集，禁止全仓 restore/reset/clean |

回滚单位按语义分为：W0 规则/store、W1 开球增量/编排、W2 页面模式、W3 首页/设置/导航。某一批失败只回滚本批明确新增的 v52 内容，不还原其他任务的同文件改动。

---

## 十、跨项目与 ADR 边界

- 本轮只修改 `13.billiard_trainer`，不需要 14 / 15 / 16 / 18 项目提供新设计、视频、理论断言或图标资产，因此不触发 Project Hub 跨项目交付。
- 方案默认使用 Codable + UserDefaults，不新增 Schema/同步/第三方依赖，因此不触发 ADR。
- 若执行期要求将清台结果进入真实训练统计、跨设备同步、新增 SwiftData 模型或改后端 DTO，必须视为新的产品/数据决策，先升版 v52 并按项目规则记 ADR，禁止在实施中顺手扩范围。

---

## 十一、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v52.0 | 2026-09-03 | 按用户「每日清台」需求与对上一轮分析的认可立档；实读首页、自由击球、开球、规则、偏好、记录与测试锚点；确立单人挑战、首次自动开球、未完成恢复、手动重开、独立完成状态、`kind="tool"` 不计周目标、W0–W4 串行依赖与全量验收口径。仅方案，未改生产代码，未运行 v52 测试。 |
| v52.1 | 2026-09-03 | 用户授权执行后完成 W0–W4：规则/store/编排、复用页每日模式、首页三态、训练栈路由、五玩法设置、工具记录边界、恢复/失败/完成闭环全部落地；单元/共享回归 90/90、v52 UI 9/9、旧流程 UI 5/5、三设备 Light/Dark/AX 聚焦矩阵通过，Debug 构建与总门禁 FAIL 0。 |

---

## Changelog

| 日期 | 条目 |
|---|---|
| 2026-09-03 | v52.0 立档：每日清台方案完成，W0–W4 均待执行。 |
| 2026-09-03 | W0–W2 完成：单人规则/store、开球编排及 `FreePlayView` 每日模式落地；普通自由击球默认模式与 `freeplay.stage` 尺寸回归通过。 |
| 2026-09-03 | W3 完成：首页卡片加入未开始/进行中/已完成三态入口，无计划空态稳定可见；训练栈路由、设置五玩法与重启持久化、草稿玩法隔离、每日 `kind=tool` 且不计周目标/准确率均有自动证据。 |
| 2026-09-03 | W4 完成：五玩法规则/编排、自动/交付/已打/失败/完成恢复、完整 v52 UI 9/9 与旧自由击球/开球 5/5 回归通过；SE/17 Pro/iPad mini 的 Light/Dark/最大字号截图目检通过；规格、数据契约、route coverage/signature、128 项写盘清单及总门禁全部同步。 |
