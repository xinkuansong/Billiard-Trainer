# v41 W5 交付说明（accuracy 12 条）

worktree：`/Users/song/projects/13.billiard_trainer-w5`  
分支：`v41-w5-accuracy`  
HEAD 基线：`bdf448b`  
意图语义：主仓 `docs/research/20260819-动作库球形训练意图.md`「完善」列；空则用「训练意图」。本批未改意图表。

## 改动清单

| id | 改 description | 改 coachingPoints | 改 tutorial 开头 | 逐杆 |
|----|----------------|-------------------|------------------|------|
| c001 | 是 | 是 | 是（技术原理） | 未动 |
| c011 | 是 | 是 | 是（技术原理，引入课五问补袋口容差） | 未动 |
| c012 | 是 | 是 | 是（技术原理） | 未动 |
| c032 | 是 | 是 | ⛔ 未动（v40 W1 定稿） | 未动 |
| c033 | 是 | 是 | 是（技术原理） | 未动 |
| c052 | 是 | 是 | 是（技术原理） | 未动 |
| c053 | 是 | 是（4→3 条） | ⛔ 未动（v40 W1 定稿） | 未动 |
| c063 | 是 | 是 | ⛔ 未动（v40 W1 定稿） | 未动 |
| c072 | 是 | 是 | 是（技术原理） | 未动 |
| c076 | 是 | 是（4→3 条） | 是（两形技术原理，分得开） | 未动 |
| c077 | 是 | 是（4→3 条） | 是（技术原理；塞向跟序列） | 未动 |
| c078 | 是 | 是（4→3 条） | 是（技术原理） | 未动 |

没有「已合格不改」的课：12 条 description / coachingPoints 都按 §四重写过。

`c013` / `c002` / `c062` 整课未碰。其他目录、序列、`standardCriteria`、意图表、方案、PROGRESS、技能、`project.yml` 未碰。

## 逐杆动过的节

无。扫过 9 条全改课的「第N杆」：与意图/实测无新矛盾、无 D-v40-8/9 新违规、两形技术原理不靠逐杆区分。按 D-v41-6 三种情况才动，故整批逐杆零改。

## 冲突清单（未硬圆）

1. **c077（已知）**  
   - 意图/完善：底袋中大角度，**左右塞都练**，主练加塞准度。  
   - 序列/现网：五档全部 **半颗皮头右塞**（digest `spinX=-0.2515…`，打点读数右 50%），切角约 46°→72°。  
   - 本批：数字/塞向跟序列；主练写成「中大角度加塞准度」；正文不写左右塞对打。待主控/用户裁定意图要不要改成只右塞。

2. **c012**  
   - 意图：「中力、**从不同角度**打进中袋」。  
   - 序列：右侧标记列 **只改距离** 的母球进左侧中袋（digest 各杆自由球/scratch，无切角扫）。  
   - 本批：自变量按序列写「只改距离」；不把意图的「换角度」写进三字段。

3. **c072**  
   - 意图：「目标球到角袋约 30–40 公分」。  
   - digest：1 号距右下角袋约 4.6 颗球、2 号距左下角袋约 4.4 颗球（约 25 公分量级）。  
   - 本批：不采用意图的 30–40 公分；描述不新写该距离。

## c076 两形开头对照（标准 3）

- 形1：中袋 + 小角度加塞准度 + 左右塞吃库后线路。  
- 形2：袋口换成底袋、瞄准点跟着换 + 底袋小角度加塞准度。  
互换即违和。

## digest × params（标准 6：c033 + c076）

digest 落盘：`build/w5-logs/digests/c033.txt`、`c076_f1.txt`、`c076_f2.txt`。  
本批 **未改任何 params**（`git diff` 不含 `spinX`/`spinY`/`velocity`）。

| 课 | 杆数对齐 | 数值 | 逐字符 |
|----|----------|------|--------|
| c033 | 8=8 | 全等（中杆 2.5，spin 0） | digest 打印 `spinX=0`；现网 JSON 写作 `0.0`（既有序列化，未改） |
| c076 形1 | 14=14 | 全等 | 长小数逐字符一致；部分 `velocity` digest 打印 `2`、JSON 为 `2.0`（既有） |
| c076 形2 | 14=14 | 全等 | 同上 `2` vs `2.0` |

未为「逐字符」去改既有 `0.0`/`2.0`——那会动 params，超出「只改开头」口径。

## 完成标准自测

1. 12 条 description/coachingPoints 都改过。  
2. 全改课开头指回该行主练 + 所有权表角色/新层；c032/c053/c063 `tutorial` 与 HEAD 全等。  
3. 两形技术原理不可互换；c076 两形已分。  
4. `python3 -c "import json; json.load(open(path))"` 12 条全过。  
5. `make -f scripts/Makefile verify-gate`：**整闸 FAIL 0**（内容 C1–C4/I5–I13 FAIL 0；发布图 FAIL 0；双端对齐 FAIL 0 / WARN 0）。**C4 本批：通过 73、不一致 0**。完整日志：`build/w5-logs/verify-gate.log`。首次在空 worktree 跑时 C3 因 gitignore 母版 PNG 未拷入报 710 失效；从主仓 rsync `DrillTutorials/` 后复跑。DTO 检查需 `backend/node_modules`（本树软链自主仓，未提交）。  
6. digest 对照见上。  
7. 本文件 + verify-gate.log 落盘。

## 未完成 / 存疑

- `make build` 本批未跑（方案写可选）。  
- c063 现网精讲仍是走位链（所有权表要求保持）；description 仍写「独立击打」（与现网剂量/达标一致，未改 tutorial）。
