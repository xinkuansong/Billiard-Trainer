> **状态**：草稿 | **版本**：0.4 | **最后更新**：2026-03-29 | **变更**：ADR-001 — 以自建 REST API 替代 LeanCloud（见 tasks/phases/P1-foundation.md ADR-001）

# 技术架构

## 1. 架构概览

```
┌─────────────────────────────────────────┐
│           iOS App（SwiftUI）             │
│                                         │
│   训练 │ 动作库 │ 角度 │ 历史 │ 我的    │
│                                         │
│   本地层（SwiftData）                    │
│   ├── 训练记录（离线可用，后台同步）      │
│   └── 角度测试历史（离线可用，后台同步）  │
└─────────────────────────────────────────┘
          │                    │
    用户数据层              内容数据层
          │                    │
┌─────────────────┐   ┌────────────────────┐
│  自建 REST API  │   │  CloudKit（公开库） │
│  (腾讯云 Node)  │   │                    │
│                 │   │ Drill 内容         │
│ 用户认证：       │   │ 官方训练计划        │
│  微信 OAuth     │   │ （只读，热更新）    │
│  手机验证码      │   │                    │
│  Sign in Apple  │   └────────────────────┘
│                 │
│ 用户私有数据：   │
│  训练记录同步    │
│  角度测试历史    │
│  用户设置偏好    │
│                 │
│ 基础设施：       │
│  腾讯云短信      │
│  MongoDB        │
│  JWT 认证        │
└─────────────────┘
         ↑
    同一套 API
    Android 版本复用
```

**核心原则**：
- **离线优先**：训练记录、角度测试、动作库浏览均可在无网络（球馆）下完整使用
- **无自建复杂后端**：自建极简 REST API（~20 接口）+ CloudKit（内容数据），均无状态/易维护
- **内容热更新**：Drill 内容存于 CloudKit 公开库，无需发版即可修改/新增
- **跨平台就绪**：iOS 和 Android 共用同一套 REST API，认证逻辑一次实现

---

## 2. 客户端技术选型

| 技术 | 选型 | 说明 |
|---|---|---|
| UI 框架 | **SwiftUI** | 现代声明式 UI |
| 最低系统版本 | **iOS 17** | 覆盖约 85% 以上活跃 iPhone |
| 本地数据库 | **SwiftData** | iOS 17 原生 ORM，本地离线存储 |
| 用户认证 + 私有数据 | **自建 REST API**（腾讯云 Node.js + MongoDB） | iOS/Android 共用，中国合规 |
| 公开内容库 | **CloudKit（公开数据库）** | Drill 内容 + 官方计划，热更新 |
| 球台动画 | **SwiftUI Canvas + Animation** | 绘制球台顶视图，路径动画 |
| 角度计算 | **纯 Swift 数学** | sin(α) × R，零依赖 |

---

## 3. 用户认证方案（自建 REST API）

### 支持三种登录方式

| 登录方式 | 适用场景 | 实现 |
|---|---|---|
| **微信登录** | 主流，一键登录，用户体验最好 | 客户端传 code，服务端用 AppSecret 换 openid/unionid，返回 JWT |
| **手机验证码** | 托底方案，不用微信的用户 | 腾讯云短信服务发送验证码，服务端验证后返回 JWT |
| **Sign in with Apple** | App Store 合规要求 | 服务端验证 Apple identity token，返回 JWT |

> **App Store 规则**：提供任何第三方登录时，必须同时提供 Sign in with Apple，已满足。

### 认证架构

```
客户端 (iOS/Android)
  │
  ├─ POST /auth/login-wechat  { code }
  │    └─ 服务端用 AppSecret 换 unionid → 生成 JWT
  │
  ├─ POST /auth/login-sms     { phone, code }
  │    └─ 腾讯云短信验证 → 生成 JWT
  │
  └─ POST /auth/login-apple   { identityToken }
       └─ Apple 公钥验证 → 生成 JWT

JWT: Access Token (1h) + Refresh Token (30d, 存 Keychain)
```

---

## 4. 数据架构

### 4.1 本地数据（SwiftData，离线主存储）

```
TrainingSession（训练记录）
├── id: UUID
├── date: Date
├── ballType: BallType（中式台球 / 9球）
├── duration: Int（分钟）
├── notes: String（心得备注）
├── sets: [DrillSet]
└── syncStatus: SyncStatus（本地 / 已同步 / 待同步）

DrillSet（每个 Drill 的练习记录）
├── drillId: String
└── groups: [DrillGroup]

DrillGroup（每一组）
├── targetBalls: Int（如 15）
└── madeBalls: Int（实际进球数）

AngleTestSession（角度测试历史）
├── id: UUID
├── date: Date
└── questions: [AngleQuestion]

AngleQuestion
├── actualAngle: Int
├── answeredAngle: Int
├── pocketType: PocketType（角袋 / 中袋）
└── isCorrect: Bool（±3° 容差）
```

### 4.2 后端私有数据（用户登录后同步）

- 自建 REST API 存储于腾讯云 MongoDB
- 与本地 SwiftData 结构一一对应
- 同步时机：每次保存训练记录后后台异步同步
- 冲突策略：以最新 updatedAt 时间戳为准
- 离线写入：本地先保存（SyncQueue），网络恢复后自动补同步

### 4.3 CloudKit 公开数据库（Drill 内容）

```
DrillContent（训练项目，约 72 条）
├── id: String
├── name: String
├── category: DrillCategory（8 大类）
├── subCategory: String
├── ballTypes: [BallType]
├── level: TrainingLevel（L0–L4）
├── diagramData: JSON（球台动画路径数据）
├── videoURL: String?（v1.0 暂空，v1.1 填入）
├── description: String
├── tips: String
├── targetBalls: Int
├── passCriteria: [LevelCriteria]
└── relatedDrillIds: [String]

OfficialPlan（官方训练计划，6 套）
├── id: String
├── name: String
├── targetLevel: TrainingLevel
├── durationWeeks: Int
├── sessionsPerWeek: Int
└── weeks: [PlanWeek]
```

**内容更新流程**（无需发版）：
1. 在 CloudKit Dashboard 修改 DrillContent 记录
2. App 启动时检查内容版本号，有更新则后台拉取
3. 新内容写入本地缓存，离线可用

---

## 5. 球台动画技术方案

**实现**：SwiftUI Canvas + withAnimation，路径数据存于 CloudKit DrillContent.diagramData

**动画数据格式（JSON）**：

```json
{
  "table": { "width": 1, "height": 2 },
  "balls": [
    { "id": "cue", "x": 0.5, "y": 0.8, "color": "white" },
    { "id": "obj1", "x": 0.3, "y": 0.3, "color": "red" }
  ],
  "paths": [
    { "ballId": "cue",  "phase": 1, "points": [{"x":0.5,"y":0.8}, {"x":0.3,"y":0.35}] },
    { "ballId": "cue",  "phase": 2, "points": [{"x":0.3,"y":0.35}, {"x":0.5,"y":0.6}] },
    { "ballId": "obj1", "phase": 2, "points": [{"x":0.3,"y":0.3}, {"x":0.3,"y":0.05}] }
  ],
  "annotations": [{ "type": "pocket_target", "pocketId": "tm" }]
}
```

**动画阶段**：
1. 静态展示初始球位 + 目标袋口高亮
2. 母球运动路径动画（击球到接触）
3. 母球走位 + 目标球进袋同时播放
4. 循环播放 / 用户可手动暂停

---

## 6. 角度感知训练技术方案

纯本地计算，零网络依赖：

```swift
// 核心公式
func contactPointOffset(cutAngle: Double) -> Double {
    return sin(cutAngle * .pi / 180)  // 返回 0–1，1 = 球半径
}

// 自适应权重出题（30% 基础 + 70% 误差加权）
func nextQuestion(history: [AngleRecord]) -> AngleQuestion {
    let weights = computeWeights(history: history)   // 各 5° 区间误差加权
    let angle = weightedRandom(weights: weights)      // 按权重抽角度
    let pocketType = choosePocketType()               // 60% 角袋 / 40% 中袋
    return AngleQuestion(angle: angle, pocketType: pocketType)
}

// 专业容差 ±3°
func isCorrect(actual: Int, answered: Int) -> Bool {
    return abs(actual - answered) <= 3
}
```

---

## 7. 第三方服务汇总

| 服务 | 用途 | 费用 |
|---|---|---|
| **腾讯云轻量服务器** | 运行 Node.js REST API | ~¥50/月 |
| **腾讯云 TencentDB for MongoDB** | 用户数据持久化 | ~¥50/月（入门规格） |
| **腾讯云短信** | 手机验证码发送 | ~¥0.045/条 |
| **微信开放平台** | 微信 OAuth 登录 | 免费（需企业或个人开发者认证） |
| **Apple CloudKit** | Drill 公开内容库（热更新） | 免费 |
| **Sign in with Apple** | App Store 合规登录 | 免费 |
| **视频 CDN**（v1.1） | 教学视频托管播放 | 届时评估（推荐七牛云/阿里 OSS） |

**刻意不引入**：
- Firebase / 任何 Google SDK（国内访问不稳定）
- LeanCloud / TDS（停止国内新注册）
- 广告 SDK
- 第三方崩溃上报（v1.0 用 Apple 原生 MetricKit，后续按需加）

---

## 8. 安全与隐私

- **用户训练数据**：存于自建后端 MongoDB，用户私有，需登录（JWT）才可访问
- **匿名用户数据**：本地 SwiftData 存储，不上传云端
- **微信登录**：仅获取微信 OpenID + 昵称/头像，不获取通讯录/好友
- **短信验证码**：手机号经后端加密存储（bcrypt/hash），不以明文保存
- **App Store 隐私清单**：需申报「账号信息（手机号）」「使用数据（训练记录）」

---

## 9. 非功能需求

| 指标 | 目标 |
|---|---|
| App 冷启动 | < 1.5 秒 |
| 训练记录保存 | 本地即时（< 0.1 秒），后台同步云端 |
| 动作库加载 | 首次从 CloudKit 拉取后缓存本地，后续离线可用 |
| 球台动画帧率 | 60fps |
| 角度测试响应 | < 10ms（纯本地计算） |
| 包体积 | < 50MB（不含视频） |

---

## 10. 开放问题与技术风险

| 问题 | 风险 | 说明 |
|---|---|---|
| 微信开放平台审核 | 中 | 需提前申请，审核周期 1–3 天，需 App 已基本可用 |
| 球台动画路径数据制作 | 中 | 72 个 Drill 需逐一设计路径 JSON，工作量较大 |
| 自建后端运维 | 低 | Node.js + MongoDB 成熟稳定，每季度一次更新维护 |
| CloudKit 内容首次加载 | 低 | 首次需网络；已缓存后离线可用 |
| 视频 CDN 选型 | 低（v1.1） | v1.0 不涉及，视频上线时再决定 |
