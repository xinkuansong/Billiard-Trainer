# v54 W2 官方计划内容迁移复核

日期：2026-09-03

## 范围与机器核对

| 计划 | 阶段 | 课程 | phase | drill 引用 | 核对结果 |
|---|---:|---:|---:|---:|---|
| `plan_beginner` | 3 | 9 | 24 | 18 | 前后一致 |
| `plan_accuracy` | 3 | 9 | 35 | 26 | 前后一致 |
| `plan_accuracy3` | 2 | 6 | 23 | 17 | 前后一致 |
| `plan_cueball` | 3 | 9 | 33 | 24 | 前后一致 |
| `plan_force` | 3 | 9 | 35 | 26 | 前后一致 |
| `plan_separation` | 4 | 12 | 48 | 37 | 前后一致 |
| 合计 | 18 | 54 | 198 | 148 | 前后一致 |

完整机器报告：`build/v54/w2-content-migration.json`。该报告同时核对每个计划的 drill 引用多重集合，避免只有总数相同但引用发生替换。

## 人工文案复核

- 阶段标题沿用原内容主题，并确认均表达训练能力，例如“直线推白球与半台直线”“近中距跟进与缩杆”“不吃库与高低杆调制”；没有使用“阶段 1”占位标题。
- 课程标题从该课主练动作提炼；重复主课使用“入门 / 巩固 / 检验”区分训练意图，例如“近台小角度进球入门—巩固—检验”。
- 抽查跨动作课程：“中袋直线出杆与底袋直线出杆”“分离角90度规则与厚球分离角控制”，标题与 focused 引用一致。
- 每节课保留原 phases、时长、drill 引用、dose、decay 与 reviewFrom；迁移只改变课程容器和显示元数据。
- stable ID 使用 `<planId>.stageNN.lessonNN`，不依赖标题，后续改文案不会改变身份。

## 验证证据

- `verify_v54_w0_contract.py`：12 计划课程基线仍为 129，PASS。
- `verify_tutorial_sync.py --only I10 I11 I13`：FAIL 0。
- `V21W5PlanContentTests`：14/14 PASS，证明真实 bundle 与旧消费兼容视图可读取。
