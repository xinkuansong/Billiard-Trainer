# B4 落地指引（B3 产出，2026-07-16）

> 依据：ADR-P12-03（`tasks/phases/P12-content-system-theory.md`）+ `QiuJi/Resources/Drills/schema.md` §「Multi-Formation Drills & Variable-Coverage Profile」+ B2 样板（`docs/research/20260716-drill变量覆盖设计方法论.md` § 2.3 球形表 A1–A8）。
> B4 执行者按本清单操作即可；角色 content-engineer，几何红线与录制红线见方案 v1 § 五。

## 0. 批内先决决策（B4 开工第一步）

- 定夺「新建 drill vs 替换现有中袋角度 drill」：候选替换对象 `drill_c053`（中袋角度精准，accuracy 分类）——读其 JSON 现状后批内拍板并记录；替换则沿用 `drill_c053` id，新建则走 `index.json` 增条目。下文以 `<drillId>` 指代。

## 1. 变量档案 sidecar（新目录，机器可读真源）

- 新建 `content/drill_profiles/<drillId>.profile.json`，格式照 schema.md § Variable Profile（version=1）。
- 内容：B2 § 2.3 表 A1–A8 逐条转入 `formations[]`（cue/target/pocket/cutAngleDeg/variables{cutAngle,side,dtp,d,nearRail}/difficultyRank），`representative: true` 标在 A1。
- 坐标直接取 B2 表值（已脚本验算，4 位小数），**不得手工重算**；如需复核跑 `python3 scripts/b2_variable_coverage_midpocket.py`。

## 2. drill JSON（代表性球形）

- `<drillId>.json` 的 `shotIntent` 写 A1 单杆（cue/target/pocket=bottomCenter，velocity 批内定，spin 缺省）；`shots[]` 长度 1。
- 烘焙：把 `<drillId>` 加入 `DrillBakeRunnerTests` pilot 列表跑测，拷回 `===BAKE===` 输出的 `animation`（`source:"baked"`），确认 `feasible=✅`（附输出）。
- `standardCriteria` 按 B2 § 2.1 达标线（10 打 6，A7/A8 球形 10 打 5——文案层描述，App 不逐球形计分）。

## 3. 每球形序列文件（试打 + 出片摆球）

- 每球形一条 `PositionPlaySequence` JSON 落 `content/position_play/sequences/`，命名 `<drillId>__A<N>-中袋角度球 · 球形N-0杆.json`（token=A1..A8，不含「-」）。
- **initial-only 合法**：`initial.onTable` 摆 `cueBall` + `_8`（取 profile 坐标），`steps: []`——试打序列模式自动降级、进袋/自由模式可用。
- ⛔ 红线：steps（示范击打）不得由 shotIntent 反推或脚本伪造；示范录制写入 `tasks/HUMAN-REQUIRED.md` 新 H-xx（非阻塞）。
- 同步：`cd scripts && make tryout-sync`，确认 `QiuJi/Resources/DrillBoards/` 出现 8 个文件。

## 4. 多球形精讲

- `<drillId>.json` `tutorial` 改用 `formations[]`（id=A1..A8 与 profile 对齐，title 如「A3 · 30° 左切」），每球形 sections 按应用课模板；写作可派 tutorial-writer 技能。

## 5. 缩略图与索引

- 缩略图重烘焙（代表性球形 A1）；替换现有 drill 则覆盖 `Resources/DrillThumbnails/<drillId>.png`。
- 新建 drill 时更新 `index.json`（accuracy 分类），并同步 `DrillContentValidationTests` 的 72 计数断言（新建=73 需改 3 处断言；替换则免）。

## 6. 验证闸门（全部附真实输出）

1. `DrillContentValidationTests` 全绿；
2. bake `feasible=✅`（`===BAKE===` 输出）；
3. 模拟器截图：详情页演示、精讲 formations 分段切换、试打选球形列表（8 项）；
4. H-xx 已挂且状态标注清楚。
