# UI 打磨收官人工冒烟清单（W2-1 ~ W2-10 全批次）

> 日期：2026-07-14。用途：按 `30-真改清单.md` §5.1 约定，全程收官后**一次性人工验收**。
> 建议环境：iPhone 16/17 Pro 模拟器（明暗各过一遍高频页）；标 📱 的项建议真机。
> 发现问题按条目所属批次归属返工（resume 对应执行记录）；通过则在行首勾选。

## 一、第 1 波留档（组件真源 + 有意视觉差确认）

- [ ] W2-1 `BTExerciseRow` 常规页浅色（Light）下按压无 tint 丢色（训练会话/记分页行按压）。
- [ ] W2-5 有意视觉差 5 项是否接受：① AimWheel 圆角 10→12；② 历史日历日标记圆角 3→6；③ NumericKeypad 间距 6→4；④ 统计图表 Light 由球色→accent 绿；⑤ 角袋/中袋对照表改下划线分段并移标题下方。

## 二、第 2 波留档（运行时态，模拟器难复现）

- [ ] 订阅购买失败 alert 上屏（断网/沙盒拒绝时不再静默）。
- [ ] 训练分享「保存到相册」真实权限回调：授权成功 → 顶部 toast「已保存到相册」（W2-9 已改非阻断）；拒绝授权 → 「保存失败」alert。
- [ ] 注销进行中 overlay + 防连点；Profile 同步失败 alert（文案「确定」）。
- [ ] 📱 精讲 Viewer 捏合缩放/下滑关闭 1:1 跟手，caption 与媒体一体下滑。
- [ ] 批量台保存遇同图存档 → 覆盖 confirmationDialog（取消在前）。

## 三、第 3 波留档（训练流交互）

- [ ] 训练保存：「保存中…」→「已保存」toast → 0.7s 自动 dismiss 时序流畅。
- [ ] 记分完成一组：动画 + 轻触觉（📱 触觉需真机）。
- [ ] 「继续训练」最小化：收缩向浮标/条过渡动效；切他 Tab 浮标态正确；自由记录最小化后首页底栏露继续条。
- [ ] 休息倒计时 0:00 时 +30S 禁用；覆层浅 scrim（0.32）下台面仍可见（W2-9 调整）。
- [ ] 付费锁态 drill 详情：紧凑预览 + 无精讲 CTA（子智能体截图未取得，需人工看一眼）。
- [ ] 空视频 drill 的单行紧凑空态；精讲「第N杆」序号徽章帧正常。
- [ ] 详情底栏撤「关闭」后（只剩「上手试打」全宽），返回路径不迷路。

## 四、第 4 波（W2-9 本批新增交互点）

- [ ] 共享 toast 真实触发：自由击球「按当前规则不能打 N 号球」= 金色 warning（不再是绿）；「已移回球库」= 绿 success；拍照建球形读图失败 = 红 error。
- [ ] 破坏性确认新形态（底部 confirmationDialog，取消在前）：设置-清除缓存、设置-注销账号、计划列表-删除计划、角度测验-重置统计；「结束训练」仍为居中 alert（有说明文案，属有意保留）。
- [ ] sheet 把手/detent：休息时长（280 + 把手）、开球选择（360 + 把手）、场景训练设置（medium/large + 把手）、drill 设置（medium/large）、精讲 sheet（medium/large + xmark 关闭钮）。
- [ ] Paywall：主 CTA 与定价卡按压 scale；「恢复购买/条款/隐私」对比度上调后可读且不抢主 CTA；Paywall 近黑背景未变。
- [ ] 登录页三路按钮按压 scale；Apple 按钮明暗反色保留。
- [ ] 角度训练 4 页日限卡统一（皇冠 +「解锁 Pro」）；免费次数门槛行为无变化（只换视觉）。
- [ ] 列表加载骨架：训练首页/计划列表/收藏页骨架 → 内容 opacity 过渡无硬切；动作库骨架过渡接通。
- [ ] 训练首页自定义计划空态：「创建计划」按钮收进空态组件（secondary 描边），点击进构建器。
- [ ] 场景总结卡（2D/3D 角度训练结束）加浅 scrim 后台面仍可见、进出 opacity 过渡。
- [ ] 训练会话行缩略图：接线烘焙台面图（原为占位图标），滚动性能无感知劣化。

## 五、跨波遗留（非本轮阻塞，留档）

- [ ] §4-5 存疑高频项待议：总结页双分享入口；分享卡假二维码；「3/4 球」跨页教学口径（14.5° vs 48.6°）；微信登录/分享空实现呈现（H-05）；退出登录是否加确认。
- [ ] 场景页 4 处 `.spring(response: 0.34, dampingFraction: 0.86)` 字面量与 `BTMotion.springPanel` 数值等价未替换（纯写法不一致，渲染零差异），可在后续顺手收编。
