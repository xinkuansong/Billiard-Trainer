# 问题集合 v53 —「我的」Tab 账号资料、偏好与数据隔离真实性闭环

> **本文件是本轮需求真源**（v53.9，制定日期：2026-09-03，状态：**代码、模拟器自动化与头像后端部署完成，其余真实环境待验收**）。
> **用户目标**：解决头像不能修改、昵称修改后退出再登录不保留、偏好设置中的外观不生效，并系统盘点与修复「我的」Tab 中同类的“看起来可用、实际没有闭环”问题。
> **执行边界**：v53.0/v53.1 先按 `issue-collection-restructure` 完成方案；用户随后明确要求持续执行，v53.2 实施 W0–W6 及 W7 本地部分，v53.3 完成契约复审，v53.4 补齐 H-09 本地可部署包与隐私声明，v53.5 修复服务端注销遗留头像文件，v53.6 完成客户端删号缓存、跨账号异步竞态和本地迁移补偿审计，v53.7 复核生产域名与真机门禁，v53.8 在清理磁盘后恢复独立模拟器验收，补齐真实系统相册/通知授权与本地 StoreKit 配置证据，v53.9 将账号资料/头像服务部署到 `106.54.3.210:3000`，以合成账号验证资料五字段与头像上传/读取/删除真实往返。真实 Apple、用户相片选择与重登、真机通知、StoreKit sandbox 与公开法律 URL 仍不得由合成探针或本地测试冒充。
> **版本说明**：仓库已有其他主题的 `问题集合_v52.md`，本轮新建 v53，不覆盖 v50–v52 及当前工作区其他未提交改动。

---

## 一、需求回显与不可弱化口径

| ID | 用户原始诉求 | 本方案的不可弱化口径 |
|---|---|---|
| R1 | 当前用户头像不支持修改 | 头像必须可从相册选择、裁切/压缩、上传、失败重试、删除恢复默认；成功后「我的」页与个人信息页立即一致，重启、退出再登录仍显示服务端真实头像 |
| R2 | 修改用户名后，退出再登录没有切换成真实的 | 登录账号的昵称以服务端资料为真源；保存必须收到服务端成功响应，重新启动、退出再登录、换设备拉取后均一致，失败不得显示“假成功” |
| R3 | 偏好里的外观设置不起作用 | “跟随系统 / 浅色 / 深色”必须作用于 App 根视图并立即切换，重启后保持；测试要验证真实界面颜色方案变化，不只验证选项存在 |
| R4 | 分析「我的」里面还有哪些问题 | 纳入账号恢复/退出、跨账号数据隔离、匿名迁移、训练提醒、假登录入口、法律链接、会员状态、版本号、游客空操作、无障碍标识和测试空通过等同类缺口 |
| R5 | 写详细方案 | 给出事实盘点、优先级、数据/接口契约、依赖批次、完成标准、测试矩阵、人工前置、回滚边界与锚点索引；本轮不实施 |
| R6 | 训练时长目标拿掉 | 从训练目标页和偏好模型移除“每次训练时长目标”，不进入后端资料、不在训练页增加目标进度；只保留训练产生的真实时长记录与统计 |

### 1.1 方案默认产品决策

1. **登录账号资料以服务端为真源**。`displayName`、头像和账号型个人资料在登录态从 `/user/profile` 获取；本地只做当前会话缓存，不再靠修改 `AuthState.currentUser` 假装保存成功。
2. **游客资料与账号资料严格分开**。游客可以在本机使用独立昵称/偏好；登录后不自动把游客昵称或头像覆盖账号资料。游客训练数据是否归入账号仍必须由用户明确确认。
3. **账号数据先隔离，再恢复自动同步**。训练记录、认知成绩、收藏、计划和同步队列必须带 owner；账号 A 的本地数据/队列绝不能在账号 B 的 JWT 下展示或上传。
4. **登录成功必须代表后端会话成功**。Apple 身份验证成功但后端登录失败时，不得把本地 Apple 用户伪装成“可云同步的登录账号”；可以明确提示并允许用户选择继续游客模式。
5. **外观、音效、教学辅助属于设备偏好**，不因退出或换账号而重置；昵称、球种、水平、球龄和周目标属于当前 owner 的资料/目标。
6. **训练提醒属于设备能力**。开关必须申请通知权限并真实增删系统通知；权限拒绝时 UI 不得保持一个无效的“已开启”状态。
7. **未取得前置资质的登录方式不冒充可用**。H-05/H-15 未完成前，微信/手机号入口显示“暂未开放”或不展示，禁止保留点击无反应、延时后匿名登录的假流程。
8. **会员与法律信息只展示可证明事实**。终身买断不显示“自动续费中”；订阅到期日来自 StoreKit 交易；条款和隐私链接必须是已核验可访问的真实 URL。

### 1.2 用户可见状态契约

#### 个人资料保存

| 场景 | 必须行为 |
|---|---|
| 登录用户修改昵称 | 校验后发起服务端更新；保存中禁重复提交；2xx 后统一更新 `AuthState` 与页面；失败保留输入并显示可重试错误 |
| 登录用户修改头像 | 本地预览仅标“正在上传”；服务端成功并返回新 revision 后才成为正式头像；失败回退旧头像 |
| 游客修改个人资料 | 明示“仅保存在此设备”；写入游客 owner 的本地资料，不触发上传 |
| 退出再登录 | 启动/登录完成后重新 GET profile，昵称与头像不得回落为首次 Apple 名称或“球迹用户” |
| 账号 A → 退出 → 账号 B | 只展示 B 的资料、数据、收藏、计划和队列；退出后回到本机游客空间，不暴露 A 的内容 |

#### 外观切换

- `.system`：根视图不强制 `preferredColorScheme`，跟随系统实时变化。
- `.light`：整个 App（含引导、Tab、导航、sheet、alert）强制浅色；球桌场景自身的黑色舞台不被错误改成白色。
- `.dark`：整个 App 强制深色；重启后仍保持。
- UI 测试专用强制参数优先于用户偏好，且只在显式测试启动参数存在时生效，避免破坏现有截图矩阵。

---

## 二、现状盘点与问题清单（2026-09-03 实读）

### 2.1 问题总表

| ID | 优先级 | 当前事实 | 根因/风险 | 归属批次 |
|---|---:|---|---|---|
| P53-01 | P1 | `PersonalInfoView` 头像固定为 `person.fill`，没有选择、上传、缓存或删除动作 | 头像功能只有占位 UI；后端 `User` 也没有头像字段/文件接口 | W0、W4 |
| P53-02 | P0 | `saveName()` 只改 `authState.currentUser?.displayName` | 没有 PUT profile；退出/重启后内存值消失，用户看到假成功 | W0、W4 |
| P53-03 | P0 | 后端已有 GET/PUT `/user/profile`，客户端没有调用；GET/PUT 返回原始 Mongoose `_id` | 响应形状与客户端 `UserDTO.id` 不一致，即使接上也会解码失败 | W0 |
| P53-04 | P0 | Apple 首次授权的 `fullName` 只存在本次凭证；后端建用户时只收 `identityToken` | Apple 后续通常不再返回姓名，服务端又没存首次姓名，重新登录会回落 | W0、W1 |
| P53-05 | P0 | App 启动只恢复 `hasCompletedOnboarding`，不从 refresh token 恢复当前用户 | 仍有有效 Keychain token 时 UI 也表现为未登录；会话真源与凭证真源断裂 | W1 |
| P53-06 | P0 | `ProfileView` 退出只调用 `AuthState.logout()`，没有走 `AuthService.logout()` | 后端 refresh token 和 Keychain 可能仍在；“退出”只清 UI 内存 | W1 |
| P53-07 | P0 | Apple 后端登录失败时仍返回 `.apple` 本地用户 | UI 宣称可同步，但没有有效后端会话；后续资料和同步必然失败 | W1 |
| P53-08 | P0 | `TrainingSession`、`AngleTestResult`、`DrillFavorite`、`UserActivePlan`、`CustomPlan`、`SyncPendingItem` 没有 owner | 本地查询跨账号共用；队列可能把 A 的数据用 B 的 JWT 上传，是最高风险数据边界缺口 | W2、W3 |
| P53-09 | P0 | 登录通知立即执行 `syncPushThenPull(.full)`，匿名迁移确认在其后 | 用户尚未同意迁移，guest 队列就可能先上传；“暂不同步”不能兑现 | W3 |
| P53-10 | P1 | `appearanceMode` 只写 UserDefaults，`RootView` 生产路径始终跟随系统 | 设置有选中态但没有消费者 | W5 |
| P53-11 | P1 | 训练提醒只有开关和时间，没有 `UNUserNotificationCenter` 调用 | 不申请权限、不排期、不取消，功能完全不生效 | W5 |
| P53-12 | P1 | `targetSessionMinutes` 和“每次训练时长目标”当前只有存值/展示，没有真实用途；用户已决定拿掉 | 删除设置 UI、属性、摘要和旧键，不扩展到训练页；真实 `TrainingSession.totalDurationMinutes` 统计保持 | W5 |
| P53-13 | P1 | 手机验证码用 `sleep` 模拟，最后调用匿名登录；微信按钮无动作 | 把未完成功能表现为可用，账号类型和用户预期均错误 | W6 |
| P53-14 | P1 | 登录页“用户协议/隐私政策”是普通文字；付费墙链接指向 `example.com` | 法律入口不可用或错误；发布合规风险 | W6 |
| P53-15 | P2 | Profile 对所有 Pro 显示“自动续费中”；状态页无真实到期日 | 终身买断和订阅被混淆；用户看不到可信权益状态 | W6 |
| P53-16 | P2 | Profile 版本固定 `1.0.0`，About 页才读 Bundle | 同一 App 两处版本可能不一致 | W6 |
| P53-17 | P2 | 游客页底部“跳过，以游客身份继续”为空 action | 可见按钮点击无反应；而用户已经处于游客态，语义重复 | W6 |
| P53-18 | P2 | 登录用户头卡仍使用 `profile.login` identifier 和“点击登录”AX label | VoiceOver 与 UI 自动化把已登录资料卡误认成登录入口 | W4、W6 |
| P53-19 | P0 | `P8_ProfileSettingsUITests` 多处 `guard ... else { return }`，提醒用例即使找不到开关也通过 | 缺控件、导航失败、外观未变化都可能“绿灯” | W7 |
| P53-20 | P2 | 付费墙写死“72 个训练动作”，项目现有内容数量已变化 | 静态数量会持续漂移；营销文案与内容真源不一致 | W6 |
| P53-21 | P1 | 规格/任务卡把账号、头像、通知、隐私链接标为已完成 | 文档完成态与实际闭环不一致，后续发布检查会漏项 | W7 |
| P53-22 | P1 | iOS 昵称校验允许 40 字符，服务端只允许 20 字符 | 21–40 字符在客户端看似可提交，最终被服务端拒绝；前后端契约不一致 | W0、W4、W7 |
| P53-23 | P0 | iOS 球龄枚举为 `lessThan1/oneToThree/threeToFive/fivePlus`，服务端却使用 `oneTo3/threeTo5/moreThan5` | 合法资料更新会被 400 拒绝；旧值恢复时客户端回落默认值，造成资料失真 | W0、W7 |
| P53-24 | P0 | H-09 没有任何可部署法律页面；Privacy Manifest 仍声明未开放手机号，却没有头像、Apple 姓名/邮箱和工具使用数据 | 即使域名修好也没有内容可发布，App Store 隐私标签与真实资料/头像链路不一致 | W6、W7 |
| P53-25 | P0 | `DELETE /user/account` 删除用户、训练和角度数据，但没有删除头像文件 | 注销后服务器继续遗留可关联账号的头像，违反“账号及所有数据已删除”和隐私政策 | W0、W6、W7 |
| P53-26 | P0 | 服务端注销成功后，iOS 未清账号级资料 UserDefaults 与全部头像 revision 缓存 | 删除账号后本机仍残留昵称、偏好和图片；同 ID 重建或缓存扫描可能看到旧资料 | W3、W4、W7 |
| P53-27 | P0 | A 的资料/头像异步请求返回时没有再次核对当前身份，头像旧请求的 `defer` 还会全局改写 phase | 快速退出或切到 B 后，A 的迟到响应、失败回滚或 loading 结束可能覆盖 B 的资料、头像或交互状态 | W1、W4、W7 |
| P53-28 | P0 | 云端删号先于本地 account→guest 迁移；迁移保存失败时统一 catch 会保留一个已不存在的登录态，且旧 account 同步队列也会被搬到 guest | 重试注销已无服务端账号，本地训练数据仍挂在旧 owner 下不可见；以后 guest 迁移还可能把旧账号更新/删除操作带入新账号 | W3、W7 |
| P53-29 | P2 | “清除所有缓存数据”和缓存体积只处理 `URLCache`，不处理 App 自建头像 JPEG 缓存 | 文案与实际清理范围不一致，用户无法从设置释放头像缓存 | W4、W6、W7 |

### 2.2 已确认真实生效、无需推倒重做的设置

| 设置 | 当前消费者 | v53 处理 |
|---|---|---|
| 击球音效 | `ShotSoundBank`、`ShotAudioScheduler`、App 启动预热 | 保留为设备偏好，只补回归测试 |
| 90° 分离角辅助线 | `TrajectoryRenderer` | 保留，只验证立即生效和持久化 |
| 台面网格 | `BTShotPageChrome`、`AngleSceneView` | 保留，只纳入设备偏好分层和回归 |
| 瞄准特写 | `BTAimCloseupHUD`、`BTShotPageChrome` | 保留，只纳入设备偏好分层和回归 |
| 每周目标天数 | `TrainingGoalMetrics` / 首页与个人页统计 | 保留计算语义，但改为 owner 资料，验证账号切换不串值 |
| 本月概览 | 当前 `ProfileView` 已查询 `TrainingSession` 并走 `TrainingGoalMetrics` | 不在 v53 重做；只在 owner 隔离后补跨账号回归 |

### 2.3 当前工作区边界

- 当前工作区已有大量未提交/未跟踪改动，含 v50–v52、Profile、训练首页、截图与内容资产工作。
- v53 每批开始前记录**定向路径基线**；禁止 `reset --hard`、`checkout --`、`clean` 或整仓覆盖。
- 不用“整个 worktree clean”作为完成标准。完成证据按 v53 预期路径、基线差集、测试退出码和截图/日志路径判断。
- v53 方案完成不等于任何问题已修复；当前未运行模拟器、真实 Apple 登录、真实后端、相册、通知或 StoreKit 验收。

---

## 三、范围、冻结项与数据分类

### 3.1 本轮实施范围

1. 账号 profile DTO 与接口规范化，昵称/头像/个人资料真实读写。
2. App 启动会话恢复、有效退出、失效 token 归位、Apple 后端失败的诚实状态。
3. SwiftData V4 owner 隔离、guest → account 明示迁移、账号切换与同步队列隔离。
4. 个人信息页头像/昵称交互、加载/失败/游客状态，以及 Profile 头卡一致性。
5. 设备偏好与账号资料拆分；外观根级消费；提醒真实排期；移除无实际用途的训练时长目标设置。
6. 微信/手机未开放状态、游客空按钮、法律链接、会员状态、版本号、内容数量和无障碍修正。
7. 客户端/后端自动测试、迁移测试、真实环境验收矩阵和规格/任务状态收口。

### 3.2 本轮冻结

- 不在 H-05/H-13 完成前接微信 SDK，不在 H-15 完成前接真实短信服务。
- 不把头像原图或 base64 放进 MongoDB；服务端只存头像 revision/元数据，图片进入可备份的持久文件存储。
- 不把 App 外观、音效、教学辅助同步到云端；它们保持设备级。
- 不做公开用户主页、社交头像浏览、好友系统、头像审核系统或多头像历史。
- 不改变训练统计口径、付费权益范围、商品价格或训练内容本身。
- 不在本方案中承诺服务器、域名、隐私政策、真实 Apple/StoreKit 已可用；这些必须在 W7 用真实环境验证。

### 3.3 偏好与资料归属

| 类别 | 字段 | 真源 | 退出/换账号行为 |
|---|---|---|---|
| 账号身份 | `id`、provider、email（若有） | 后端 `/user/profile` | 当前账号切换时整体切换 |
| 账号资料 | `displayName`、`avatarRevision`、`preferredSport`、`skillLevel`、`yearsPlaying` | 登录态后端；游客态本地 owner profile | A/B 不共享；guest 不自动覆盖账号 |
| 训练目标 | `weeklyGoalDays` | 登录态后端；游客态本地 owner profile | 跟 owner 切换 |
| 设备偏好 | `appearanceMode`、`soundEffectsEnabled`、`showSeparationAngle`、`trajectoryDetail`、`showTableGrid`、`showAimCloseup` | 本机 UserDefaults | 退出/换账号不改变 |
| 设备提醒 | `reminderEnabled`、`reminderTime`、系统通知授权/请求 | 本机 UserDefaults + `UNUserNotificationCenter` | 设备级保留；明确可在本机关闭 |
| 用户数据 | 训练、认知成绩、收藏、计划、同步队列 | SwiftData + 当前 `ownerKey` | 只显示/同步当前 owner |

---

## 四、核心数据与接口契约

### 4.1 标准化 `UserDTO`

所有认证和 profile 接口统一返回显式 DTO，禁止直接 `res.json(mongooseDocument)`：

```text
UserDTO
├── id: String                 // Mongo _id 字符串
├── provider: apple|phone|wechat
├── displayName: String?
├── email: String?
├── avatarRevision: Int?
├── preferredSport: String?
├── skillLevel: String?
├── yearsPlaying: String?
└── weeklyGoalDays: Int?
```

约束：

- 昵称 trim 后 1–20 个可见字符；空串不能悄然替换默认名；客户端和服务端同规则，服务端为最终裁决。
- 枚举值必须白名单校验，目标天数 1–7，单次时长使用产品现有选项集合。
- PUT `/user/profile` 使用 partial update，只接受白名单字段；未知字段返回 400，不写入。
- GET、PUT、Apple 登录三个响应复用同一 `serializeUser()`，保证 `id`/nullable 字段形状一致。
- Apple 首次登录请求在有效 identity token 之外可附带一次性 `displayName`；服务端只在当前用户没有昵称时采用，防止后续登录覆盖用户手改昵称。

### 4.2 头像契约

MVP 采用“客户端规范化 + 后端持久文件 + revision”方案：

1. 客户端通过 `PhotosPicker` 选择图片，在本机完成方向纠正、正方形裁切、512×512 JPEG、质量压缩，最终不超过 1 MiB。
2. PUT `/user/avatar` 使用 `Content-Type: image/jpeg` 原始 body，后端限制 body 大小、校验 JPEG magic bytes，以当前 `userId` 生成文件名并原子替换。
3. 后端图片目录由 `AVATAR_STORAGE_DIR` 指向部署机持久卷，必须纳入备份；临时文件和测试文件只能写系统临时目录。
4. 成功后递增 `avatarRevision` 并返回标准 `UserDTO`。GET `/user/avatar?v=<revision>` 只返回当前认证用户头像；DELETE `/user/avatar` 删除文件、清 revision。
5. iOS 新增带 Authorization 的头像加载/缓存服务；缓存键为 `userId + avatarRevision`。revision 变化必须绕过旧缓存，退出/换账号不串图。
6. UI 在服务端 2xx 前只显示上传中预览；失败恢复旧头像并提供重试。删除失败同样保留旧头像。

> `AVATAR_STORAGE_DIR`、生产目录权限、HTTPS 和备份恢复属于 W0/W7 的人工部署前置。若项目改用腾讯 COS，必须保持以上 API/DTO 不变，并另记架构决策；不得在 View 层直连供应商 SDK。

### 4.3 认证状态机

规划用显式状态替代“`currentUser != nil` 就算登录”：

```text
launching
  ├── 无 refresh token ──> guest / signedOut
  └── 有 refresh token ──> restoring
        ├── refresh + GET profile 成功 ──> authenticated(UserDTO)
        ├── 网络暂时失败 ──> signedOutWithRecoverableSession（不冒充已登录）
        └── 401/refresh 失效 ──> 清凭证 ──> signedOut
```

- Root 启动恢复完成前显示稳定的 launch state，禁止闪一下游客页再切账号。
- 所有 Auth UI 只消费同一个 `AuthState`；API 401 导致凭证失效时必须通知/驱动它归位。
- 正常退出：尽力 DELETE `/auth/logout`，随后无条件清 Keychain 和当前 account UI；网络失败不能阻止本机退出。
- 账号注销：服务端成功后把该账号本地数据按现有承诺移交到 guest owner，再清凭证/锚点/账号缓存；服务端失败不提前改变本地状态。

### 4.4 Owner 契约与 V3 → V4 迁移

统一字符串 `ownerKey`：

- 游客：`guest:<deviceGuestUUID>`，UUID 持久保存且不随每次启动重建。
- 登录账号：`account:<serverUserId>`。

V4 至少为下列顶层模型新增非空 `ownerKey`：

- `TrainingSession`
- `AngleTestResult`
- `DrillFavorite`
- `UserActivePlan`
- `CustomPlan`
- `SyncPendingItem`

`DrillEntry`、`DrillSet`、`CustomPlanDrill` 通过父关系继承归属，不单独制造可能不一致的 owner 字段。所有创建、读取、更新、删除、统计和同步必须经过当前 owner predicate。

迁移规则：

1. V3 ownerless 记录迁移到稳定的 `guest:<deviceGuestUUID>`，**不得猜测它属于当前/最近 Apple 账号**。
2. 旧 `SyncPendingItem` 同样归 guest，登录后未确认迁移前禁止处理。
3. 登录完成先 GET profile，再检查 guest 是否存在可迁移数据；没有数据时直接拉取账号数据。
4. 用户确认迁移：在一个可回滚事务内把 guest 顶层记录改为 account owner、为可同步实体建立 account 队列，再执行 push → pull。
5. 用户选择暂不迁移：guest 数据原样保留且对账号不可见；只 pull 当前账号，禁止 push guest 队列。
6. 正常退出不改 owner；账号 A 数据继续留在本机但对 guest/B 隐藏。账号注销成功则按“本地数据保留”承诺将 A 的本地记录降级到 guest，并清除其待同步队列。

---

## 五、批次表与依赖

| 批次 | 范围 | 量级 | 依赖 | 主要交付 |
|---|---|---:|---|---|
| W0 | Profile/Avatar API、DTO 规范、后端测试基线与部署前置 | L | — | 标准 `UserDTO`、GET/PUT profile、头像 PUT/GET/DELETE、后端测试 |
| W1 | Auth 状态机、启动恢复、有效退出、Apple 登录语义 | L | W0 | 单一会话真源、refresh 恢复、失效归位、无假登录 |
| W2 | SwiftData V4 owner 模型、迁移与 owner-aware repository/queue | L | W1 稳定 user id | V4、guest/account owner、队列隔离、迁移测试 |
| W3 | 全部用户数据消费者 owner 化、登录迁移时序与账号切换 | L | W2 | 查询/统计/计划/收藏隔离，先确认后上传 |
| W4 | 头像/昵称/个人资料 UI 闭环与 Profile 头卡 | L | W0 + W1 + W3 | 可编辑资料、上传状态、重登一致、正确 AX |
| W5 | 偏好分层、根级外观、真实提醒与时长目标移除 | L | W1 + W3 | DevicePreferences、OwnerProfileStore、外观/通知生效、无效设置下线 |
| W6 | 登录/Profile/订阅/法律/版本等诚实性收口 | M | W1 + W4 + W5；法律 URL 前置 | 移除假入口与空操作，真实会员/法律/版本文案 |
| W7 | 全链路自动化、真实环境矩阵、规格/任务状态与台账收口 | L | W0–W6 | 可靠测试、截图/日志、文档与完成态一致 |

### 5.1 依赖图

```text
W0 API / DTO / Avatar contract
 └── W1 Auth session truth
      └── W2 Owner schema + migration + queue
           └── W3 Owner-aware consumers + consent ordering
                ├── W4 Profile editing UI
                └── W5 Preferences / appearance / reminder / goal
                     └── W6 Truthful shell / subscription / legal
                          └── W7 System acceptance + docs
```

拆批依据：W2/W3 涉及约 40 处生产查询/FetchDescriptor 锚点，若与资料 UI 混成一个会话，迁移、查询隔离、同步时序和视觉验收会同时挤压上下文；因此先收口数据边界，再做用户可见功能。W4 与 W5 可在 W3 完成后换序，但二者都通过后才能进入统一 Profile 收口。

### 5.2 执行状态

| 批次 | 状态 | 本轮事实 |
|---|---|---|
| W0 | ✅ 已完成 | Profile/Avatar API、统一 DTO、头像安全目录、客户端二进制请求与后端测试已落地；昵称统一为 1–20 字符，球龄写入统一为 canonical 枚举并兼容读取旧别名；账号注销先清头像再删归属数据，存储失败时保留账号以便重试；相关服务已部署生产后端，头像写入使用应用目录外的 `/var/lib/qiuji/avatars`；当前 backend 全套 15/15 |
| W1 | ✅ 已完成 | 认证状态机、冷启动 refresh/profile 恢复、集中失效归位、统一异步退出与服务端 Apple 语义已落地；真实 Apple 授权留 H-26 |
| W2 | ✅ 已完成 | Schema V4、稳定 guest/account owner、V1–V4 迁移、owner-aware repository/queue/restore、冲突去重与失败回滚已落地 |
| W3 | ✅ 已完成 | 生产查询/统计/计划/收藏全面 owner 化；登录先检查并征得 guest 迁移同意，确认/拒绝均幂等；账号注销时数据降级 guest 并丢弃旧 account 同步队列；云端删号后先记补偿标记，本地迁移失败也退出，下一次启动幂等恢复，避免已删除账号假登录和 owner 孤儿 |
| W4 | ✅ 已完成 | 昵称/资料以服务端响应提交；头像支持相册、裁切/压缩、上传/删除、revision 缓存、失败回退及游客隔离；A 的迟到资料/头像响应不能覆盖 B，删号清理账号资料与全部头像 revision；生产后端合成账号上传/读取/删除与文件清理已通过，真实用户相册和退出重登仍留 H-26 |
| W5 | ✅ 已完成 | 设备偏好与 owner profile 拆分；外观根级即时生效；通知真实申请/排期/取消并显示权限状态；训练时长目标 UI、属性、摘要和旧键已删除，真实训练时长保留 |
| W6 | 🟡 代码完成 | 假微信/手机路径、游客空按钮、测试 URL、硬编码版本/动作数和不实会员文案已清理；transaction 到期日/终身状态已接。中英双语法律模板、默认 503 防误发布、生产配置门禁及 App 三处动态链接已落地；运营主体事实、部署与正式 HTTPS URL 仍被 H-09 阻塞，StoreKit sandbox 留 H-26 |
| W7 | 🟡 自动化/文档完成 | backend 13/13、v53 iOS unit 46/46、P8 Profile/Settings 基础 UI 17/17，并在两台独立全新 iOS 26.2 模拟器上补齐相册与通知系统授权允许/拒绝路径；本地 StoreKit 月/年/终身商品、失效与恢复 3/3；法律 HTTP 路由 3/3、标准 Debug build、v47 路由/写盘门禁通过。H-09/H-26 的真实环境证据未完成，因此 v53 不标全量验收完成 |

### 5.3 v53.9 当前执行证据

| 门禁 | 结果 | 证据/边界 |
|---|---|---|
| 后端自动测试 | ✅ 13/13 | `cd backend && npm test`，覆盖 profile/avatar、账号注销头像清理与失败重试顺序、球龄 canonical/旧别名以及法律模板配置门禁/安全渲染 |
| 生产资料/头像服务 | ✅ 合成账号全链路 | 先备份旧版至 `/root/qiuji-backend-backup-20260903-avatar-predeploy.tar.gz`（SHA-256 `e41e76da6188ef4039963d2282a1b672534002abebf2c309ef3ff9337ea87e2c`），再定向部署 v53 认证/资料/头像文件并重启 PM2。外部 `/health` 200；临时合成账号实测资料五字段 PUT 200、头像 PUT/GET/DELETE 均 200，revision、JPEG 字节、持久文件与删除均一致；探针用户 0 遗留、探针文件 0 遗留 |
| iOS v53 定向单测 | ✅ 46/46 | `/tmp/qiuji-v53-resume-unit-final-20260903.xcresult`；原 43 项继续覆盖昵称/球龄、owner/认证/资料偏好、删号缓存、迁移补偿及跨账号竞态；新增 3 项 `SKTestSession` 覆盖月/年/终身商品加载、购买、订阅失效和恢复。这是本地 StoreKit Configuration 证据，不是 App Store sandbox 证据 |
| P8 Profile/Settings 基础 UI | ✅ 17/17 | `/tmp/qiuji-v53-resume-p8-20260903.xcresult`；独立 iPhone 17 Pro Max / iOS 26.2 上全跑，断言头像入口、服务端身份冷启动、外观即时/冷启动、提醒权限状态、无时长目标与法律未发布诚实状态 |
| 相册系统权限 | ✅ 允许 1/1、拒绝 1/1 | `/tmp/qiuji-v53-resume-photo-allowed-20260903.xcresult`、`/tmp/qiuji-v53-resume-photo-denied-20260903.xcresult`；每次重置真实 `photos-add` 权限并操作 SpringBoard 弹窗。该证据只证明系统授权状态，不冒充 PhotosPicker 选图、裁切或生产头像上传 |
| 通知系统权限 | ✅ 允许 1/1、拒绝 1/1 | `/tmp/qiuji-v53-resume-notification-allowed-20260903.xcresult`、`/tmp/qiuji-v53-resume-notification-denied-20260903.xcresult`；允许用例在 iPhone 17 Pro Max 验证状态与开关持续，拒绝用例在全新 iPhone 16e 验证错误提示与开关回退；均操作真实 SpringBoard 授权框，仍不代表真机到点展示 |
| 法律页面本地 HTTP | ✅ 3/3 | 以测试运营事实启动 `createApp()`，`/privacy`、`/terms`、`/legal.css` 均返回 200 与正确 Content-Type；默认/缺字段状态由自动测试证明返回 503，不会误发布模板 |
| 标准构建 | ✅ | `make -f scripts/Makefile build`；`xcpretty` 缺失后脚本按既定 fallback 运行 `xcodebuild`，最终 `BUILD SUCCEEDED` |
| v47 UI 基线门禁 | ✅ FAIL=0 | `screenshots=66 routes=70 write-surfaces=129`；新增头像缓存测试只写 UUID 系统临时目录并由 `defer` 清理；本批未覆盖的历史截图不据此冒充 v53 真机视觉验收 |
| 真实环境 | ⏳ | H-09 二次实测：`qiuji.app` TLS 主机名不匹配；绕过校验后 `/privacy`、`/terms` 被 Varnish 301 到 `www` 根页，`www` 对三条请求均返回 Cloudflare 521，须修 DNS/源站/路由/证书并发布正式法律页。H-26 的真实 Apple 首次/再次登录、用户本人 PhotosPicker 选图裁切与退出重登、头像备份恢复演练、真机到点通知、App Store sandbox 仍待人工证据 |

---

## 六、各批详细范围与完成标准

### W0 — Profile / Avatar API 与契约基线

**范围**

1. 抽出后端 `serializeUser()` 与 profile 白名单 validator，Apple login、GET/PUT profile 共用。
2. 扩展 `User` schema 与客户端 `UserDTO`，字段、nullability、枚举和数值范围一致。
3. Apple 登录请求允许带首次 `displayName`，仅在服务端当前为空时落库。
4. 实现头像 PUT/GET/DELETE 与 `AVATAR_STORAGE_DIR`；原子写、大小/MIME 校验、旧文件清理、revision 更新和无头像 404 语义明确。
5. 为 backend 增加可重复运行的测试入口；纯 validator/serializer 测试不依赖生产数据库，头像文件测试只写 `mkdtemp` 临时目录并在结束时清理。
6. 客户端 API 层增加 GET/PUT profile、原始图片 body 请求与 Data 响应能力；不得把网络逻辑写进 SwiftUI View。

**完成标准**

- Apple 登录、GET profile、PUT profile 三个 JSON 的 `id/displayName/avatarRevision/provider` 形状一致，客户端 fixture 全部可解码。
- 昵称空白、超长、非法枚举、越界目标值、未知字段均返回可定位 400；合法 partial update 不清空未提交字段。
- 头像超过 1 MiB、非 JPEG、无 token、写盘失败均不更新 revision；成功写盘可 GET，相同用户替换后只有新 revision 生效；DELETE 幂等。
- 后端测试不连接生产 Mongo、不写仓库路径；真实退出码为 0。
- iOS API 层 mock 测试覆盖 2xx、400、401、5xx、解码失败、头像 Data 上传/读取和错误保留。
- 增量依赖和环境变量同步到 `.env.example`/技术文档，但不提交真实密钥、服务器绝对路径或用户图片。

### W1 — 认证会话真源与生命周期

**范围**

1. 将 `AuthState` 改为显式启动/恢复/游客/已登录/错误状态；`AppUser` 从标准 `UserDTO` 构建。
2. App 启动检测 refresh token，refresh 后 GET profile；完成前 Root 显示稳定启动态。
3. API token refresh 失败通过单一认证协调器使 UI 归位；避免 APIClient 私自清 Keychain、AuthState 仍显示登录。
4. Apple 登录只有后端认证成功才进入 account 状态；后端不可用时显示错误与“继续游客使用”，不伪装云账号。
5. 所有退出入口统一调用一条 async logout：服务端撤销尽力而为，本机凭证与账号状态必清。
6. 账号注销复用一致的凭证清理，但须等待 W3 的本地数据转 guest 事务完成后再最终退出。

**完成标准**

- 有效 refresh token 冷启动恢复同一用户，不闪游客/登录卡；无 token 进入正确 guest/signedOut。
- access 过期 + refresh 成功恢复；refresh 401 清凭证并显示登录态；暂时网络失败不显示“已同步账号”。
- Apple 后端失败不会产生 `.apple` 已登录用户，不触发同步通知。
- Profile 和 Settings 的退出都撤销/清理同一组 token；网络断开时本机仍能退出，下一次启动不自动回到旧账号。
- 单测覆盖状态迁移和并发幂等：重复 bootstrap/login/logout 不产生双回调、双同步或状态倒退。
- 真实 Apple 登录仍留到 W7；W1 的自动测试使用 mock，不宣称第三方授权已验收。

### W2 — SwiftData V4 Owner 与同步队列隔离

**范围**

1. 新建 `QiuJiSchemaV4` 与 V3→V4 迁移阶段；为六个顶层模型增加非空 `ownerKey`。
2. 建立稳定 `deviceGuestUUID` 和 `CurrentOwnerContext`；所有新建实体在构造/Repository 边界写 owner。
3. 迁移全部 V3 记录到 guest owner；关系树数量、id、日期、kind、组/杆数据和自定义计划剂量必须无损。
4. `SyncPendingItem.enqueue/process/pendingCount` 接收 owner；只处理 `account:<currentId>`，guest/其他账号项保持不动。
5. `SyncRestoreService` 插入记录时写当前 account owner；查重、待删保护和锚点都同时带 owner。
6. 定义 owner transfer 事务：guest→account、account deletion→guest；失败回滚，不出现一半改 owner、一半留旧队列。

**完成标准**

- V1→V2→V3→V4、V2→V3→V4、V3→V4 和全新 V4 库均能打开；迁移前后每类实体数量/关键字段一致。
- 迁移后所有顶层记录 owner 非空且为同一稳定 guest key；重启不生成新 guest UUID。
- A/B/guest 各有 queue item 时，A 登录只发送 A；B/guest 项数量与内容不变。
- restore A/B 使用相同 clientId fixture 时也按 owner 分开查重，不互相吞记录。
- guest→A 转移中模拟 save 失败时数据和队列都保持转移前状态；成功时不产生重复实体/重复队列。
- `ModelContainerFactory` 的磁盘、指定 URL、内存容器均使用 V4 migration plan；现有 V2→V3 剂量折算测试继续通过。

### W3 — 全部消费者 Owner 化与迁移同意时序

**范围**

1. 盘点并修改生产代码中所有用户数据 `@Query` / `FetchDescriptor` / repository 调用；范围覆盖训练首页、历史、个人统计、收藏、动作详情、计划列表/详情/编辑、认知记录、同步与 backfill。
2. 动态 owner 切换时让页面查询重建/刷新，禁止继续显示前一个 owner 的数组缓存。
3. 重写登录后顺序：profile 成功 → 切换 account owner → 检查 guest 数据 → 有数据则提示 → 确认后 transfer/push/pull，拒绝则只 pull account。
4. 没有 guest 数据时不弹空提示，直接 pull account；同步并发期间切账号必须取消或丢弃旧 owner 结果。
5. 正常退出切回 guest view；不转移 account 数据。账号注销成功则把本地账号数据转 guest 并清该账号队列/恢复锚点。
6. 为跨账号统计/收藏/计划/同步建立集成 fixture，不以单个页面“看起来为空”代替数据层断言。

**完成标准**

- 账号 A/B/guest 的训练历史、本月概览、周目标、收藏、激活计划、自定义计划、认知成绩互不可见。
- 登录前 guest 有待同步数据时，在用户确认前网络 mock 收到 0 次 guest upload；“暂不同步”后仍为 0，guest 数据可在退出后恢复看到。
- 确认迁移只执行一次；重复通知/前后台不重复转移或上传。
- 同步 A 途中切到 B 时，A 的响应不能插入 B owner；B 页面不能短暂显示 A 内容。
- 账号注销后返回 guest 可见被保留的本地数据，且旧 account 队列不会再发送。
- 所有 40+ 生产查询锚点完成清单式复核；新增模型后没有裸 `FetchDescriptor<UserOwnedType>()` 遗漏，允许的迁移/维护扫描须有明确注释。

### W4 — 头像、昵称与个人资料 UI 闭环

**范围**

1. `PersonalInfoView` 接入 owner profile store，区分 loading/saving/uploading/error/guestLocal 状态。
2. 头像卡支持选择、系统权限、正方形裁切/缩放、上传、重试、删除；Profile 头卡和个人信息页使用同一头像组件/缓存。
3. 昵称编辑加入字符计数、trim/长度校验、保存中状态、失败保留和成功后的统一更新；不再直接突变 `currentUser`。
4. 登录态 profile 进入时必要时 refresh；游客态明确“仅本机保存”。主打球种/水平/球龄走当前 owner profile，不再使用全局键。
5. 已登录头卡改用独立 `profile.accountCard` identifier 和正确 AX label；guest 才保留 `profile.login`。
6. 对 56/80pt 头像、占位、加载、错误、最大字号和 Reduce Motion 做布局/无障碍处理。

**完成标准**

- 昵称成功链：编辑 → PUT 2xx → 两页同步 → terminate/relaunch → logout/login → 仍为新昵称。
- 昵称失败链：400 显示具体校验错误；离线/5xx 保留输入和旧正式昵称；重试成功后才更新。
- 头像成功链：相册选择 → 裁切 → 上传 → 两页更新 → 冷启动/重登仍显示；新 revision 不读旧缓存。
- 头像取消选择不改状态；拒绝相册权限有说明；上传失败回退旧图；删除成功后两页恢复默认头像。
- A/B 头像缓存不串；guest 头像只在 guest 可见且不自动上传。
- VoiceOver 分别朗读“账号资料，昵称…”和“点击登录”，登录用户不再被读成登录按钮。

### W5 — 偏好、外观、提醒生效与时长目标移除

**范围**

1. 将现有 `UserPreferences` 拆为设备偏好与 owner profile/goal store，提供一次性旧 UserDefaults 键迁移；迁移不得把旧全局资料悄然写入任一账号，应先落 guest。
2. Root 按 `appearanceMode` 映射 `ColorScheme?`；明确 UI 测试参数的覆盖优先级，生产无参数时才读用户偏好。
3. 建立 `TrainingReminderScheduler` 包装 `UNUserNotificationCenter`：请求权限、创建唯一日历通知、改时间重排、关闭取消、权限拒绝/受限状态回写 UI。
4. `weeklyGoalDays` 通过当前 owner profile 读写；登录用户服务端保存失败不得显示假成功。
5. 从 `TrainingGoalView` 删除“每次训练时长目标”整节，从偏好模型删除 `targetSessionMinutes` / `sessionDurationSummary`，并在旧偏好迁移时清理 `targetSessionMinutes` UserDefaults 键；不向 UserDTO/后端 User schema 增加该字段。
6. 保留 `TrainingSession.totalDurationMinutes`、历史/本月概览中的真实训练时长和现有计时器；“删除目标设置”不得误删实际时长数据或统计。
7. 对音效、分离角、轨迹层级、网格、瞄准特写做消费回归，避免拆 store 时让已生效设置失效。

**完成标准**

- App 内点浅色/深色立即改变 `colorScheme`；`.system` 随系统切换；三种模式冷启动持久；sheet/导航/alert 与根视图一致。
- 测试强制 Light/Dark 参数仍能稳定覆盖偏好，现有 v49/v50/v51 截图矩阵不被用户默认值污染。
- 首次开提醒触发系统权限；允许后 pending request 只有一个正确 hour/minute；改时间仍只有一个；关闭后为 0。
- 拒绝权限后开关回到关闭并显示“前往系统设置”；不得显示假的开启状态。
- 账号 A/B/guest 的球种、水平、球龄、周目标不串；设备外观/音效在切账号后保持。
- 训练目标页不再出现 15/30/45/60 分钟或“不限”选项；新安装和 V3/V4 升级后偏好模型都不再暴露 `targetSessionMinutes`，旧键被安全清理。
- 完成真实训练后，历史详情、本月概览和训练总结中的实际训练时长仍与移除前一致。

### W6 — 「我的」其余诚实性收口

**范围**

1. H-05/H-15 未满足时，微信和手机入口标“暂未开放”并不可进入假流程；删除 `sleep → loginAnonymously()` 与无动作按钮。
2. 登录页“用户协议/隐私政策”改为可点击真实链接；Profile 与 Paywall 复用统一 `LegalLinks` 配置。
3. 核验 H-09 状态与真实 URL。若 URL 不存在/不可公开访问，W6 保持阻塞，禁止用 `example.com` 或空 action 充数。
4. StoreKit 层暴露经验证的 entitlement snapshot（商品类型、expirationDate、revocation/billing retry 必要状态）；Profile/状态页按事实显示“终身有效 / 有效至… / 订阅状态待刷新”。
5. Profile 版本号复用 About 的 Bundle 版本格式；付费墙“72 个”改为不易漂移的“完整动作库”或直接消费内容真源计数。
6. 删除游客页重复无动作的“跳过，以游客身份继续”，或改为真正关闭登录 sheet 的位置；Profile 已在游客态时不展示无意义按钮。
7. 统一错误、加载、禁用、AX identifier 与中文文案，不把占位能力标成已完成。

**完成标准**

- 微信/手机号未开放时无任何路径能产生 phone/wechat/anonymous 假账号；满足人工前置后的真实接入另立实施版本。
- 三处法律入口都能打开同一已核验 HTTPS 页面；离线时由系统浏览器行为处理，不崩溃；链接有明确 AX label。
- lifetime 从不显示自动续费；月/年订阅显示真实 expirationDate；无可验证 snapshot 时用诚实降级文案。
- Profile/About 版本完全一致且随 Bundle 配置变化；营销动作数不再手工漂移。
- 「我的」页所有可见 Button/Link 都有实际动作或明确 disabled 状态，不存在点击无反应。
- 登录/游客/Free Pro/订阅/月年/终身/权益失效七类状态有 UI fixture 和断言。

### W7 — 系统验收、真实环境与文档收口

**自动化范围**

1. Backend：serializer/validator、Apple 首次名字、profile partial update、头像安全/原子写/删除与认证测试。
2. iOS unit：Auth 状态机、Profile API、avatar cache revision、V1–V4 迁移、owner transfer、queue/restore 隔离、偏好迁移、外观映射、通知 scheduler、entitlement snapshot。
3. iOS UI：昵称/头像成功失败、外观即时与冷启动、通知允许拒绝、A/B/guest 切换、迁移确认/拒绝、退出/注销、法律/会员/版本与 AX。
4. 修复 `P8_ProfileSettingsUITests`：导航/控件缺失必须 `XCTFail`，不得 `guard ... else { return }`；外观测试必须断言实际 scheme/可视 token 变化，提醒测试必须断言请求状态。
5. 现有同步、迁移、Profile、订阅、截图与响应式矩阵定向回归；最后运行 `make -f scripts/Makefile build` 和适用总门禁，保留真实退出码。

**真实环境矩阵**

| 场景 | 模拟器/mock | 真机/真实服务 | 完成证据 |
|---|---:|---:|---|
| Apple 首次/再次登录、冷启动恢复、退出重登 | ○ | ● | 屏录/截图 + 服务端 profile 响应脱敏日志 |
| 头像相册选择/拒绝、上传、删除、重登 | ● | ● | 前后截图 + avatar revision/文件元数据，不保存真实照片进 Git |
| 账号 A/B/guest 数据隔离与迁移同意 | ● | ● | 三身份数据表 + 网络请求计数/脱敏日志 |
| 外观三态即时/冷启动 | ● | ○ | Light/Dark/system 截图与 scheme 断言 |
| 通知允许/拒绝/改时/取消/实际到点 | ● | ● | pending request dump + 真机通知截图 |
| StoreKit 月/年/终身/失效/恢复 | StoreKit config ● | sandbox ● | entitlement snapshot + 各状态截图 |
| 法律链接 | ● | ● | URL/HTTP 状态 + 页面截图 |

**视觉与无障碍矩阵**

| 页面/状态 | iPhone SE | iPhone 17 Pro | iPad mini | Light | Dark | AX5/VoiceOver |
|---|---:|---:|---:|---:|---:|---:|
| Profile guest/account + 长昵称 | ● | ● | ● | ● | ● | ● |
| PersonalInfo 默认/编辑/上传/错误 | ● | ● | ● | ● | ● | ● |
| Settings 外观三态 | ● | ● | ● | ● | ● | ● |
| TrainingGoal 通知/周目标（无时长目标） | ● | ● | ○ | ● | ● | ● |
| Login 未开放/错误/法律链接 | ● | ● | ● | ● | ● | ● |
| Subscription 各权益状态 | ● | ● | ● | ● | ● | ● |

**写盘与证据纪律**

- 在声称“测试后无跟踪污染”前，先用 `rg -l "\.write\(|FileManager.*create" QiuJiTests QiuJiUITests` 盘点默认 scheme/testplan 中全部写盘测试，并同步 write-surface 台账。
- 截图、xcresult、头像临时文件、通知/网络脱敏日志进入 `build/`、系统临时目录或既定非跟踪证据目录，不写进资源目录。
- 完成判断使用 v53 开始时记录的定向 baseline；不得要求或制造整仓 clean，不触碰其他任务已有 diff。

**文档/任务收口**

- 更新 `docs/04-功能规划.md`：账号/个人设置只在真实会话、资料、通知和 owner 隔离验收后恢复为 ✅。
- 更新 `docs/05-信息架构与交互设计.md`：头像/昵称状态、游客/账号资料、外观/提醒行为。
- 更新 `docs/06-技术架构.md` 和数据契约：标准 UserDTO、头像存储、OwnerScope、同步同意时序与设备/账号偏好边界。
- 新增 V4/Owner 隔离 ADR；记录为什么不能根据最近登录账号猜 owner，以及账号删除后本地保留的降级规则。
- 校正 `tasks/phases/P8-polish-release.md` 与 `tasks/HUMAN-REQUIRED.md` 中 H-09/头像/通知/订阅状态的事实冲突。
- 更新 route/write-surface 台账与 `tasks/PROGRESS.md`，只有 W0–W7 全部完成才把 v53 标为已实施。

**W7 完成标准**

- 自动测试无 silent return；所有关键状态都有明确断言，真实退出码为 0。
- A/B/guest 隔离、用户未同意前 0 次 guest upload、昵称/头像重登一致、外观三态、通知真实排期为硬门禁。
- build、定向单测/UI、backend tests 与适用总门禁通过；失败必须记录真实根因，不用 `|| true` 或仅重跑隐藏失败。
- 真机 Apple/相册/通知、真实后端头像目录/备份恢复、StoreKit sandbox、法律 URL 分别有证据；未做的项继续标未验收。
- 文档“已完成”与代码/自动/人工证据一致，不再以控件存在或本地存值代替功能完成。

---

## 七、实码锚点索引（核实于 2026-09-03）

| 范围 | 实读锚点 | 事实/影响 |
|---|---|---|
| 个人头像/昵称 | `QiuJi/Features/Profile/Views/PersonalInfoView.swift:38–94,251–258` | 固定 SF Symbol；昵称只改内存 |
| Profile 头卡/版本/退出 | `QiuJi/Features/Profile/Views/ProfileView.swift:17–45,101–155,381–405` | 版本写死；已登录卡 AX 错；退出没清凭证；游客有空 action |
| 偏好页面 | `QiuJi/Features/Profile/Views/SettingsView.swift:14–39,90–155,292–307` | 外观只绑定 prefs；注销账号调用后端后只清 AuthState |
| 训练目标/提醒 | `QiuJi/Features/Profile/Views/TrainingGoalView.swift:204–293` | 时长目标仅有 UI/存值且将移除；提醒开关/时间仍需接系统通知 |
| 偏好存储 | `QiuJi/Data/Services/UserPreferences.swift:75–200` | 所有账号/设备字段共用全局 UserDefaults |
| 根外观 | `QiuJi/App/RootView.swift:4–31` | 生产未消费 `appearanceMode`；仅测试参数可强制 scheme |
| Auth 内存状态 | `QiuJi/Data/Services/AuthState.swift:17–69` | init 只恢复 onboarding；logout 只 nil currentUser |
| Apple 登录 | `QiuJi/Data/Services/AuthService.swift:12–31,49–89` | fullName 一次性；后端失败降级成本地 `.apple` |
| API token | `QiuJi/Data/Services/APIClient.swift:84–108,156–173` | refresh 失败私自清 Keychain，未同步 AuthState |
| Keychain | `QiuJi/Data/Services/KeychainService.swift:4–50` | 有 token/Apple id 存储，但无用户 profile 恢复 |
| 客户端 UserDTO | `QiuJi/Data/Services/BackendSyncService.swift:187–221,280–285` | 有 login/delete，无 GET/PUT profile/avatar |
| 后端 User | `backend/src/models/User.js:3–11` | 只有 Apple id、昵称、email、provider、refresh token |
| 后端 profile | `backend/src/routes/user.js:10–33` | 已有 GET/PUT，但返回 raw `_id` 且只允许 displayName |
| 后端 Apple login | `backend/src/routes/auth.js:9–48` | 请求只收 token，新用户不落首次 Apple displayName |
| App 同步时序 | `QiuJi/App/QiuJiApp.swift:85–128` | 登录先 push/pull；迁移确认另一路；迁移抓全部 session |
| 同步队列 | `QiuJi/Data/Services/SyncQueueManager.swift:42–107,126–168` | enqueue/process 无 owner，登录后遍历全部队列 |
| 下行恢复 | `QiuJi/Data/Services/SyncRestoreService.swift:99–210` | 锚点按 userId，但插入实体/查重无 owner |
| 用户数据模型 | `TrainingSession.swift:4–27`、`AngleTestResult.swift:4–32`、`DrillFavorite.swift:4–12`、`UserActivePlan.swift:4–20`、`CustomPlan.swift:4–39`、`SyncPendingItem.swift:17–31` | 顶层实体均无 owner |
| 当前 Schema | `QiuJi/Data/Models/QiuJiSchemaV3.swift:5–38`、`ModelContainerFactory.swift:4–55` | 当前 V3；owner 需要 V4 + 迁移/ADR |
| 登录占位 | `QiuJi/Features/Profile/Views/LoginView.swift:53–79,92–149`、`PhoneLoginView.swift:184–212` | 法律文字不可点；微信空 action；手机号假延时匿名登录 |
| 会员/法律 | `SubscriptionView.swift:379–403`、`SubscriptionStatusView.swift:169–180`、`SubscriptionManager.swift:169–180`、`StoreKitService.swift:58–69` | example.com；只保存 product IDs，无到期日 snapshot |
| Profile 测试 | `QiuJiUITests/P8_ProfileSettingsUITests.swift:16–150` | 多处 silent return；外观/提醒只测控件存在 |
| 键盘测试 | `QiuJiUITests/V50StateMatrixUITests.swift:110–125` | 只验证输入框/完成按钮可达，不验证保存/重登 |
| Profile 规格 | `docs/05-信息架构与交互设计.md:276–296` | 明确要求头像/昵称/账号 ID、通知和偏好 |
| 技术/隐私规格 | `docs/06-技术架构.md:146–167` | 自建 Node/Mongo；匿名数据不上传；微信昵称/头像承诺 |
| 完成态冲突 | `docs/04-功能规划.md:20–30,54–55`、`tasks/phases/P8-polish-release.md:77–103` | 账号/个人设置/头像/通知/隐私已标完成，与实码不符 |

> 上表只把已存在且实读过的路径称为“锚点”。`QiuJiSchemaV4`、Owner store、Avatar service、Reminder scheduler 等尚不存在的落点是计划交付物，不冒充现有代码。

---

## 八、计划交付物

| 类型 | 计划路径/形态 |
|---|---|
| 本轮真源 | `问题集合_v53.md` |
| 认证/profile DTO | 扩展 `AuthState.swift`、`AuthService.swift`、`BackendSyncService.swift`、`APIClient.swift` |
| 后端 profile/avatar | 扩展 `backend/src/models/User.js`、`routes/auth.js`、`routes/user.js`；新增 serializer/validator/storage 辅助文件与 tests |
| Owner/Schema | 新增 `QiuJiSchemaV4.swift`、owner context/transfer service；扩展六个顶层模型、repositories、sync services |
| Profile UI | `PersonalInfoView.swift`、`ProfileView.swift`；新增共享 avatar view/loader/crop flow |
| 偏好/提醒 | 重构 `UserPreferences.swift` 或拆为 DevicePreferences + OwnerProfileStore；新增 `TrainingReminderScheduler` |
| 训练目标收口 | `TrainingGoalView.swift`、`UserPreferences.swift`：移除时长目标 UI/属性/摘要/旧键；保留真实训练时长统计 |
| 诚实性收口 | `LoginView.swift`、`PhoneLoginView.swift`、`SubscriptionView.swift`、`SubscriptionStatusView.swift`、About/version helper |
| 自动测试 | 新增 Auth/Profile/Owner/V4/Reminder/Entitlement 测试；扩展 P8/V50/同步/迁移/Profile/订阅 UI 测试 |
| 决策与规格 | Owner V4 ADR、`docs/04`–`06`、数据契约、P8 任务卡、HUMAN-REQUIRED、route/write-surface 台账、PROGRESS |

新文件的具体目录命名可在对应批次执行时按仓库现有组织微调；若改动会改变接口、owner 语义、头像隐私或批次依赖，必须先升版 v53，不得静默偏离。

---

## 九、风险、人工前置与回滚单位

### 9.1 主要风险

| 风险 | 处置 |
|---|---|
| 先做 UI 后补 owner，导致资料/偏好再重构 | 严格按 W0→W3 先收口会话与数据归属，W4/W5 后置 |
| V4 把历史记录猜到错误账号 | 所有 ownerless 数据一律迁到稳定 guest；只经用户确认转账号 |
| 登录通知在确认前上传 guest 数据 | 删除当前“登录即 push”时序；用单一 migration coordinator 决定何时可 push |
| 动态账号切换时旧异步响应污染新账号 | 每个请求/恢复带 captured owner，提交前核对仍是 active owner；切换时取消旧 task |
| 头像泄露/磁盘爆炸 | 认证 GET、大小/MIME 限制、固定尺寸、用户 ID 文件名、原子替换、备份与清理策略 |
| SwiftUI `AsyncImage` 无法附 auth header | 使用专用 Avatar loader，不在 URL 中放 token，不直接复用公开图片 URL 假设 |
| 外观修复破坏截图测试 | 明确 test launch args > user preference > system 的优先级，并回归 v49–v51 |
| 通知开关与系统权限不同步 | scheduler 返回实际授权/排期结果，UI 由结果回写，不先乐观置 true |
| 账号注销后“本地保留”却看不到数据 | 注销成功事务把 account 数据转 guest，失败则不动本地 |
| 法律状态文档互相矛盾 | W6/W7 以真实可访问 URL 为门禁，未核验就保持阻塞 |
| 既有 UI 测试静默通过 | 移除 silent return，关键查找统一断言失败；缺页/缺按钮必须红 |

### 9.2 人工前置

1. 确认生产头像持久目录/备份策略，配置 `AVATAR_STORAGE_DIR` 权限；若改用 COS，补对应账号/密钥/域名但不写入仓库。
2. 使用真实 Apple 账号完成首次/再次授权与退出重登；自动化不能替代系统授权 UI。
3. 核验 H-09 对应的用户协议/隐私政策公开 HTTPS URL。当前任务文档对 H-09 完成状态存在冲突，执行前必须以实际页面可访问性为准。
4. 真机允许/拒绝通知并等待一次实际到点通知；模拟器 pending request 只能证明排期，不证明系统最终展示。
5. StoreKit sandbox 覆盖月/年/终身/失效与恢复购买。
6. H-05/H-13/H-15 未完成不阻塞 v53 核心修复，但阻塞微信/手机号真正开放；v53 只负责不再假装可用。

### 9.3 回滚单位

- W0：后端 DTO/profile/avatar contract；数据库新增字段必须兼容旧用户，图片文件与 revision 可独立回滚。
- W1：Auth 状态机与启动恢复；保留明确迁移适配层，避免 View 同时依赖新旧状态。
- W2：Schema V4/owner/queue 是一个原子回滚单位；不得只退模型不退 migration plan。
- W3：消费者 predicate + migration coordinator；回滚时不能重新启用“登录即上传 guest”。
- W4：Profile UI/avatar cache；不回滚 W0–W3 的数据安全修复。
- W5：设备/owner preferences 和 reminder scheduler；外观映射可独立关闭但不能恢复全局账号偏好串用。
- W6：诚实性 UI 与法律/会员文案；未开放入口宁可保持 disabled，不回退到假流程。

任何回滚只处理本批明确变更，不还原当前工作区其他任务在同文件中的既有修改。

---

## 十、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v53.9 | 2026-09-03 | 修复真实手机上传头像 404：确认生产服务器仍运行 2026-08-12 旧版，缺 `/user/avatar` 与新 Profile 字段；建立可恢复备份后，定向部署 v53 认证/资料/头像服务，配置 `/var/lib/qiuji/avatars`，PM2 重启后 online、外部健康检查 200。合成账号完成 Profile PUT 与头像 PUT/GET/DELETE 真实往返，数据与文件均已清理；用户手机重试与退出重登仍需当事人确认。 |
| v53.8 | 2026-09-03 | 用户清理磁盘后恢复独立模拟器验收：iPhone 17 Pro Max 基础 Profile/Settings 17/17，真实系统相册权限允许/拒绝 2/2，通知权限允许/拒绝分别在独立全新 iPhone 17 Pro Max/iPhone 16e 通过 2/2；新增本地 StoreKit Configuration 月/年/终身购买、失效与恢复测试，v53 unit 提升至 46/46，标准 Debug build 通过。模拟器证据不替代真实 Apple/生产后端/真机到点通知/App Store sandbox。 |
| v53.7 | 2026-09-03 | 二次复核外部门禁：物理 iPhone 仍为 Offline；生产法律入口同时存在裸域证书主机名不匹配、法律路径被 Varnish 301 到 `www` 根页、`www` 源站 Cloudflare 521 三层问题。明确后续须先修 DNS/源站/路由/证书与真实法律部署，再做 H-09/H-26 实环境验收；本地完成态不变。 |
| v53.6 | 2026-09-03 | 完成客户端身份切换与删号一致性复审：注销后清账号资料和全部头像 revision 缓存；旧账号异步资料/头像响应、失败回滚与 phase 收尾不得覆盖新账号；清缓存纳入头像文件与体积；云端删号成功后以持久补偿标记保障 account→guest 迁移在下次启动幂等重试，并丢弃旧 account 同步队列。v53 iOS 专项提升至 43/43，P8 UI 17/17，写盘门禁登记 129 个文件。 |
| v53.5 | 2026-09-03 | 完成隐私一致性复审：发现账号注销未删除头像文件，新增可测试的账号数据删除服务；先删除已知头像 revision，成功后再并行删除训练、角度和用户，存储失败时不丢失可重试的账号/revision。backend 提升至 13/13，其余当前源码门禁保持 unit 38/38、P8 UI 17/17、法律 HTTP 3/3。 |
| v53.4 | 2026-09-03 | 继续推进 H-09：新增中英双语隐私政策与用户协议、生产事实必填和默认 503 防误发布、可独立测试的 Express app、App 登录/关于/订阅三处动态 HTTPS 接线；校正 Privacy Manifest 的头像、Apple 姓名/邮箱与工具交互声明并移除未开放手机号。backend 11/11、iOS 定向单测 38/38、P8 UI 17/17、本地法律 HTTP 3/3；公开部署/TLS 和专业法律复核仍待 H-09。 |
| v53.3 | 2026-09-03 | 完成后实施契约复审：修复 iOS 40/服务端 20 的昵称上限分歧，以及 iOS/服务端球龄枚举不一致；服务端改为 canonical 写入并兼容旧别名读取。补充服务端身份与外观冷启动 UI 回归、昵称和球龄单测；后端 8/8、iOS 定向单测 37/37、P8 UI 17/17。H-09/H-26 仍为真实环境门禁。 |
| v53.2 | 2026-09-03 | W0–W6 代码与 W7 本地自动化/文档收口完成：新增服务端资料/头像真源、认证恢复、V4 owner 隔离与迁移同意、真实资料编辑/头像链路、根级外观、系统通知、诚实登录/法律/订阅状态，并删除训练时长目标。后端 7/7、iOS 定向单测 34/34、P8 UI 15/15、标准构建与 v47 基线门禁通过；H-09/H-26 未完成，故不标全量真实环境验收完成。 |
| v53.1 | 2026-09-03 | 按用户拍板移除“每次训练时长目标”：从需求、UserDTO、账号资料、W5 与验收中删除“让目标生效”的设计，改为删除现有 UI/偏好属性/摘要/旧键；明确保留真实训练计时、历史和统计数据。W0–W7 仍均待执行。 |
| v53.0 | 2026-09-03 | 按用户头像、昵称重登、外观设置和「我的」全量问题诉求立档；实读 Profile/Auth/API/backend/UserDefaults/SwiftData/sync/StoreKit/tests/spec 锚点；识别跨账号 owner 与迁移时序为最高风险，确立服务端 profile 真源、guest/account 隔离、V4 owner、设备/账号偏好分层、真实通知与诚实 UI，拆为 W0–W7 八批。仅方案，未实施、未运行 v53 验收。 |

---

## Changelog

| 日期 | 条目 |
|---|---|
| 2026-09-03 | v53.9：生产旧后端缺头像路由导致真机 404；已备份、定向部署 v53 资料/头像服务、配置应用目录外的头像持久路径并重启 PM2，合成账号 Profile 与头像 PUT/GET/DELETE 全链路通过且清理无遗留。 |
| 2026-09-03 | v53.8：清理磁盘后恢复两台独立 iOS 26.2 模拟器验收；补齐真实系统相册/通知允许拒绝、本地 StoreKit 购买失效恢复证据，v53 unit 46/46、Profile/Settings 基础 UI 17/17、标准 Debug build 通过；真实 Apple/生产头像/真机通知/sandbox 边界保留。 |
| 2026-09-03 | v53.7：二次核验生产域名和物理设备；确认法律入口还存在 Varnish 错误重定向与 Cloudflare 521，iPhone 仍 Offline，真实环境门禁继续保留。 |
| 2026-09-03 | v53.6：补齐 iOS 删号缓存、跨账号异步竞态、本地迁移补偿和头像缓存清理闭环；unit 43/43、P8 UI 17/17、写盘门禁 129。 |
| 2026-09-03 | v53.5：修复账号注销遗留头像文件，补存储失败时保留重试线索的顺序测试；backend 13/13。 |
| 2026-09-03 | v53.4：补齐 H-09 本地可部署法律包、生产 fail-closed 门禁、App 动态接线和 Privacy Manifest；本地门禁提升至 backend 11/11、unit 38/38、P8 UI 17/17、法律 HTTP 3/3。 |
| 2026-09-03 | v53.3：修复昵称上限和球龄枚举两处跨端契约缺陷，补强冷启动证据；本地门禁提升至 backend 8/8、unit 37/37、P8 UI 17/17。 |
| 2026-09-03 | v53.2：W0–W6 代码与 W7 本地门禁完成；真实法律 URL、Apple/相册/头像持久卷/通知/StoreKit sandbox 继续由 H-09/H-26 阻塞。 |
| 2026-09-03 | v53.1：训练时长目标从产品方案中移除；保留真实训练时长记录与统计。 |
| 2026-09-03 | v53.0 立档：账号资料、会话、owner 隔离、偏好、提醒与 Profile 诚实性方案完成，W0–W7 均待执行。 |
