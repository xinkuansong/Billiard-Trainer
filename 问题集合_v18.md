# 问题集合 v18 — 不穿模前提下的母球可行打点

> **本文件是本轮需求真源。** 高版本覆盖低版本；与 v16（练习 Tab 产品化）、v17（加塞旋转渲染，已收官）文件域零交集。
>
> - **版本**：v18.0
> - **日期**：2026-07-27
> - **状态**：⏳ 待用户拍板（D-v18-2 ～ D-v18-7）后开工 W1
> - **承接**：DR-027（球杆穿模修复：抬杆避障 / 通用碰撞收杆 / 跟杆前向钳制）
> - **排期前置**：无硬前置。与 v16 可并行（文件域不交）。
> - **锚点核实日期**：2026-07-27（下文 §六 全部逐条读代码确认）

---

## 一、需求回显

### 1.1 用户原始诉求（本轮对话）

1. **核心议题**：「在不穿模的基础上，母球可行打点的问题」——DR-027 解决的是「给定打点，杆怎么摆才不穿模」；本轮要解决反向的一半：**给定盘面与瞄准方向，打点盘上哪些 `(spinX, spinY)` 还摆得出杆**。
2. **用户直觉确认**：「比如球杆下方被挡，只能打出中高杆或者高杆」。
3. **用户追问**：「你有球杆的详细尺寸吗」。

### 1.2 已拍板结论

| 编号 | 结论 | 依据 |
|---|---|---|
| **D-v18-1** | **`spinY` 采用「相对杆轴」坐标系**（pooltool `a/b` 原义），非「球的世界正视图」 | 用户 2026-07-27 选择 `cue_axis` |

**D-v18-1 的连锁后果**（写入契约，执行者必须遵守）：

- **存量内容零改动**：drill JSON / 序列 JSON 的 `spinX/spinY` 与引擎 `a,b` 本就是「无仰角下的接触点偏移 / R」；平杆时两系重合，故 126 条序列与全库 drill 不需重烘焙。
- **打滑极限圈恒为正圆**：`miscueLimitFraction = 0.5` 的成立条件是「接触点法线与杆轴夹角」，本就定义在杆轴系。`BTSpinPad` 虚线圈、`SpinPadMath.nudge` 的合矢量钳制、`ShotIntent.clampToMiscueLimit`、`DifficultyModel` 加权范数**全部保持原样**。
- **代价**：抬杆时打点盘与 3D 观感错位（见 §二 2.3 数值），**用信息层补偿**（打点盘常驻显示所需仰角），**不改口径**。

### 1.3 主控的理解、假设与不确定点（澄清优先）

**我理解的需求**：把「不穿模」从「渲染期事后规避」升级为「击球前可查询的打点可行性判据」，并让这个判据向上贯通到 UI（打点盘）与求解器（反解不给摆不了杆的解）。

**我的假设**（若与用户本意不符请在拍板时纠正）：

- A1：本轮**不要求**完整斜杆物理（masse 弧线 / 跳球）。见 D-v18-6。
- A2：本轮**不要求**改变 `spinX/spinY` 的存储语义与内容格式。
- A3：「可行打点」的判据服务对象**同时**是渲染与教学，不只是修穿模——教学价值（「这球位后面有球，低杆打不出来」）是主要收益。

**不确定点**（已列为拍板项）：

- U1：`TaiQiuZhuo.usdz` 里真实渲染的球杆 `QiuGan` 的实际尺寸**未知**，仓库无任何记录（详见 §三 3.3）。这是本方案最大的地基风险。
- U2：`.blocked` 时现状「隐藏球杆但照样发球」是否可接受（详见 §三 3.4）。
- U3：灰区首批接线页范围。
- U4：求解器接入方式（硬过滤 vs 软降级）。

---

## 二、坐标契约（动代码前必须回显；`geometry-spatial-reasoning` 强制）

### 2.1 基础系（真源：`.kiro/steering/table-geometry.md` + `BTPhysicsConstants`）

- **SceneKit 世界系**：水平面 = **X–Z**，**Y 朝上**；+X = 右端，+Z = 顶视图上方；单位 **米**。
- 球半径 `R = BallPhysics.radius = 0.028575 m`（唯一真源 `BTPhysicsConstants.swift:25`）。
- 台面世界 Y = `BTTablePhysics.surfaceY`；球心高度 = `surfaceY + R`。
- 角度单位：**内部一律弧度**，仅 UI 文案转度。

### 2.2 杆的局部系（现状，DR-027 已定，本轮不改）

- 枢轴 pivot = 击球点；杆**局部 +z = back = −aim**；`euler(−elevation, yaw, 0)`，`yaw = atan2(back.x, back.z)`。
- 杆尖在 local `z = tipOffset + pullBack`，`tipOffset = R + 1mm`；杆身沿 +z 延伸 `shaftLength`。
- 抬杆时杆尾升高（`worldPoint` 中 `y = localZ · sin(elevation)`，`localZ > 0` 指向后方）。

### 2.3 打点系（**本轮新定义**，D-v18-1）

设瞄准水平单位向量 `â`，仰角 `θ`（正 = 杆尾抬起）：

```
d̂  = cos θ · â − sin θ · ŷ        // 杆轴前进方向（斜向下前方）
l̂  = ŷ × â = (a_z, 0, −a_x)       // 左向；= −right，right 与 AimingCorrectionMath.rightOfXZ 同构
û_d = sin θ · â + cos θ · ŷ        // 杆轴系「上」
```

- `spinX` 正 = **左塞** ⇒ 接触点偏向 `+l̂`（与 `CueStroke.strikePosition` 现状 `cue − right·spinX·R` 一致）。
- `spinY` 正 = **高杆** ⇒ 接触点偏向 `+û_d`。
- **真实接触点**（判据内部量）：

```
P = C + R(spinX·l̂ + spinY·û_d) − R·√(1 − spinX² − spinY²) · d̂
```

- **世界竖直分量**（灰区推导用）：

```
P_y − C_y = R · ( spinY·cos θ + √(1 − spinX² − spinY²) · sin θ )
```

- **渲染枢轴（保持向后兼容，红线）**：

```
pivot = C + R(spinX·l̂ + spinY·û_d)      // 垂直杆轴的偏移平面内，不含 −R√ 沿轴分量
```

  `θ = 0 且 spinY = 0` 时与现状 `CueStroke.strikePosition` **逐位一致**（W1 必须有回归断言）。`tipOffset` 语义不变。

### 2.4 已交叉核对的数值草稿（本轮已算，供 W1 当断言锚点）

| 场景 | 结论 | 推导 |
|---|---|---|
| 贴库时库顶相对球心 | 库顶**高出球心 9.425 mm** | `railTopAboveSurface 0.038 − R 0.028575` |
| 母球贴库所需最小仰角 | **θ ≥ 27.6°** | `atan((9.425 + 5.5)/28.575)`，5.5 = `tipContactRadius` |
| 贴库 + 杆轴系满低杆 | 接触点世界高度 **≈ −0.04R（几乎正在赤道）** | `R(−0.5·cos27.6° + 0.866·sin27.6°)` |
| 平杆满低杆离台面余量 | **8.8 mm** | `R − 0.5R − tipContactRadius = 28.575 − 14.29 − 5.5` |
| 后方 6cm 一球所需仰角 | ≈ 31.13°（现有基准） | `CueClearanceTests.test_elevation_ballSixCmBehind` |

> **贴库不能打低杆**因此不是手法问题，是几何上不存在那个接触点——这是本轮最重要的教学结论，也是灰区形状的物理依据。

### 2.5 禁止脑算清单（命中即须落数值草稿）

- `atan2` 参数顺序（水平角恒 `atan2(z, x)`；杆的 yaw 是 `atan2(back.x, back.z)`，**是既有例外，勿"纠正"**）。
- `l̂` 与 `right` 的符号（`l̂ = −right`）。
- 不动点迭代的收敛方向（见 §三 3.2）。
- 度 ↔ 弧度。

---

## 三、代码事实（§六 附行号锚点，全部读码核实）

### 3.1 缺口一：`spinY` 从未进入杆的姿态

`CueStroke.strikePosition(cue:aim:spinX:)` 只做横向偏移，`cue.y` 原样透传；全 App **16 处生产调用点无一传 spinY**（清单见 §6.3）。后果不只是「3D 看不出高低杆」——低杆时杆身应整体下移 0.5R（14.3 mm），现在没有，导致 `ballElevation` 算出的仰角**系统性偏小**，该挡的挡不到。

### 3.2 缺口二：仰角求解不知道打点是什么（且存在不动点）

`CueStick.requiredElevation(cueBallPosition:aimDirection:obstacleCenters:)` 签名无 spin，隐含假设「杆轴过球心」。

**新契约必须解不动点**：`θ_min` 依赖打点（低杆压低杆身 → 后方球更容易挡 → 需更大 θ），而打点的世界位置又依赖 `θ`（杆轴系 `û_d` 随 θ 旋转）。

- **红线**：不得写成单向函数，否则出现「算出 θ 后打点位置变了、原 θ 不够用」的漏检。
- **实现要求**：从 `θ = 0` 起**迭代至收敛**（建议 `|Δθ| < 1e-4 rad` 或上限 8 轮），或取保守上界（按该 `|spinY|` 能造成的最低杆头高度算 θ）。两种都可，但必须在单测中断言「收敛后再代回判据仍成立」。

### 3.3 地基风险：渲染的杆与碰撞代理几何不是同一根（U1，最高优先级）

**碰撞代理几何**（`CueClearance` / `CuePhysics`，全部硬编码）：

| 量 | 值 | 锚点 |
|---|---|---|
| `shaftLength` | 1.45 m | `CueClearance.swift:14` |
| `tipRadius` | 5.5 mm（直径 11 mm） | `CuePhysics.tipContactRadius` |
| `buttRadius` | 14 mm（直径 28 mm） | `CueClearance.swift:13` |
| 锥度 | tip→butt **单一线性** | `CueClearance.shaftRadius(alongShaftU:)` |
| `tipCurvatureRadius` ρ | 10.5 mm（nickel） | `BTPhysicsConstants.swift:229` |
| `tipOffset` | R + 1 mm = 29.575 mm | `CueClearance.swift:16` |
| `maxPullBack` | 150 mm（≈ v 2.9 m/s） | `CueClearance.swift:20` |
| `ballClearance` / `collisionMargin` | 2 mm / 3 mm | `CueClearance.swift:22,24` |

**但屏幕上渲染的是 `TaiQiuZhuo.usdz` 里名为 `QiuGan` 的节点**（`TableModelLoader.removeNodeNames = ["QiuGan"]`，`extractCueStick` 抽出后经 `CueStick(modelCueStickNode:)` 使用；USDZ 内含 `PiTou_*` 四张贴图，说明是带皮头建模的完整杆）。代码对它**只读了 `boundingBox` 定位杆尖锚点**，其真实长度 / 分段半径 / 缩放后世界尺寸**从未被测量，仓库无任何记录**。

**风险后果**：若 USDZ 杆比 1.45 m 长或比 28 mm 粗，`CueClearance` 判「过得去」的姿态画面上照样穿模——整套可行打点判据会建在没验过的数上。

**先例**：`QiuJiTests/TableGeometryProbeTests.swift` 就是从同一个 usdz 的**网格顶点直接实测**球台 jaw 几何的（文件头注明「直接实测」），本轮照抄该做法即可。

### 3.4 现有不自洽：`.blocked` 时隐藏球杆但照样发球（U2）

```swift
guard case .angle(let elevation) = elevResult else {
    // Blocked: do not draw a penetrating stick; still fire the shot.
    stick.hide()
    onContact()
    return
}
```

（`CueStroke.swift:158-163`）一旦引入「可行打点」语义，「这一杆物理上摆不出杆，但我们照样发球」就是**如实性问题**，不是观感问题。须拍板（D-v18-3）。

### 3.5 `cushionElevation` 的距离地板偏乐观

`atan2f(railRise + railClearance, max(0.05, distToCushion))`（`CueStick.swift:99`）把距离下限锁在 50 mm。母球贴库时真实距离 = R = 28.575 mm，算出 **23.2°**，而 §2.4 实算需要 **27.6°**。这个地板在新判据里必须拿掉或换成真实几何。

### 3.6 上游可复用的既有范式（不要另起炉灶）

- **难度分档**：`ShotDifficultyTier`（center / vertical / side / extremeSide）+ `DifficultyModel.tier/score`——E1–E5「可执行性感知求解」已确立「难度是分档 + 加权排序，不是二值门」的方法论。
- **预算优先 + 兜底**：`PositionPlaySolver.applySpinTierBudget`（`PositionPlaySolver.swift:239-255`）——「预算内有解则只给预算内；否则回退并标 `beyondSpinBudget`，**绝不因预算给出无解**」。可行性接入应**复刻这个范式**。
- **打点盘单一组件**：`BTSpinPad` / `BTSpinPadCard` / `BTSpinPadOverlay`（`Core/Components/BTSpinPad.swift`），**9 处调用点**，灰区只需改组件 + 各页喂数据。
- **miscue 守门单一入口**：`ShotIntent.clampToMiscueLimit`（`ShotIntent.swift:58-64`，由 `shotInput()` 第 110 行调用）。

---

## 四、批次表

| 批次 | 范围 | 量级 | 依赖 | 模型 |
|---|---|---|---|---|
| **W1** | 球杆实测探针 + 打点几何契约 + `isCuePoseClear` 判据（纯 Core，零 UI） | 中 | — | 待 D-v18-7 |
| **W2** | `BTSpinPad` 灰区 + 仰角角标 + 出杆门闩 + 首批页接线 | 中 | W1 | 待 D-v18-7 |
| **W3** | 求解器可行性感知（软降级 + 标注 + 排序惩罚） | 小–中 | W1 | 待 D-v18-7 |
| **W4** | 仰角一阶进物理（`v_eff = v·cos θ` + 阈值标注） | 小 | W1 | 待 D-v18-6 / -7 |
| ~~L1~~ | 完整斜杆物理（masse 弧线 / 跳球 / 台呢法向冲量） | 大 | — | **延后，本轮不做** |
| ~~L2~~ | 灰区推广到剩余 6–7 处 `BTSpinPadOverlay` 调用点 | 中 | W2 | **延后** |

**依赖图**：

```
W1 ──┬──> W2 ──> (L2 延后)
     ├──> W3
     └──> W4（若 D-v18-6 = 本轮做）
W2 ∥ W3 ∥ W4  （文件域不交，可并行；但均须等 W1 合入）
```

---

### W1 — 球杆实测 + 打点几何契约 + 可行性判据

**范围**

- **W1-a（闸门，必须最先跑）**：新增 `QiuJiTests/CueGeometryProbeTests.swift`，照 `TableGeometryProbeTests` 做法从 `TaiQiuZhuo.usdz` 的 `QiuGan` 节点**顶点级实测**：总长、沿轴分段半径剖面（判断是否 pro taper 而非线性锥）、皮头段位置与半径、`appliedScale` 后的世界尺寸。产物落 `build/v18-evidence/cue-probe.txt`。
- **W1-a 升级条款（红线）**：若实测与 §3.3 硬编码偏差**长度 > 5% 或任一分段半径 > 20%**，**W1 就地收敛为「实测 + 常量修正 + 证据 + 结论报告」**，契约改造顺延为 W1'（重新拍板 §2.3 是否仍适用）。禁止在偏差未澄清时继续往上盖判据。
- **W1-b**：`CueStroke.strikePosition` 增加 `spinY` 与 `elevation` 参数，按 §2.3 `pivot` 公式实现；12 处调用点同步（默认值保证零行为变化）。
- **W1-c**：`CueStick.requiredElevation` 升级为接受打点的姿态求解，**解不动点**（§3.2）；拿掉 `max(0.05, distToCushion)` 地板（§3.5）。
- **W1-d**：新增 `CueClearance.isCuePoseClear(spinX:spinY:cueBall:aim:obstacles:) -> (feasible: Bool, elevation: Float)`，五关判据：① 打滑（`spinX² + spinY² ≤ 0.5²`）② 杆身 vs 后方球 ③ 杆身 vs 库顶 ④ 杆头 vs 台面 ⑤ 杆头 vs 贴身球（杆身避得开、杆头未必）。θ 超 `maxElevationRadians`(60°) ⇒ 不可行。

**完成标准**

1. `make build` **BUILD SUCCEEDED**（贴真实输出尾部）。
2. `CueGeometryProbeTests` 跑通，`build/v18-evidence/cue-probe.txt` 落盘，**报告中逐项对照实测 vs `CueClearance` 硬编码**，并明示是否触发升级条款。
3. 新增 `CueFeasibilityTests`，至少覆盖 §2.4 五个数值锚点（贴库 27.6°、贴库满低杆接触点 ≈ 赤道、平杆满低杆余量 8.8 mm、后方 6cm ≈ 31.13°、打滑圆边界）。
4. **回归红线断言**：`θ = 0 且 spinY = 0` 时新 `strikePosition` 与旧实现**逐位一致**（对 `spinX ∈ {−0.5, −0.3, 0, 0.3, 0.5}` × `aim ∈ {+X, +Z, 斜 45°}` 全组合）。
5. 不动点收敛断言：收敛后的 θ 代回判据仍成立；迭代轮数有上界。
6. 既有套件不回归：`CueClearanceTests`、`BallFogDiagTests`、`SpinExportParityTests`、`TrajectoryPlaybackSpinTests` 全绿（贴真实输出）。
7. 出一张灰区形状图（复用 `ShotScenarioRenderTests` 的 PNG 出图路子）：给定「后方 6cm 一球」与「贴库」两个盘面，在打点盘平面上扫 `(spinX, spinY)` 网格标可行 / 不可行，**目视确认灰区从下缘啃起**（§2.4 的物理预期）。

---

### W2 — 打点盘灰区 + 出杆门闩

**范围**

- `BTSpinPad` 增加可选可行性输入（盘面 + aim，或预计算的可行性掩码），渲染灰区；拖动**钳到可行域边界**（与现有 `placementLimit` 钳制叠加，不替换）。
- `SpinPadMath.nudge` 同步受灰区约束（四向微调不得跨进灰区；撞墙返回 `moved = false`，复用既有反馈）。
- `BTSpinPadCard` 常驻显示当前所需仰角（D-v18-1 的信息层补偿）。
- 出杆门闩：按 D-v18-3 结论实现。
- 首批页接线：按 D-v18-4 结论（推荐编排台 + 思路训练）。

**完成标准**

1. `make build` **BUILD SUCCEEDED**。
2. 单测：灰区钳制（拖入灰区被拉回边界）、`nudge` 撞灰区返回 `moved = false`、无障碍盘面灰区为空（打点盘行为与改前逐位一致）。
3. **截图取证**（明 / 暗双色）：① 无障碍（无灰区）② 后方 6cm 一球（下缘灰区）③ 母球贴库（下缘大片灰区 + 仰角角标）④ 拖动被钳到边界。逐张目视核验后附路径。
4. `.blocked` 路径按 D-v18-3 结论有对应测试。
5. 未接线的 6–7 处调用点**行为零变化**（可行性参数默认 nil ⇒ 走原路径），须有断言或 grep 审计留档。

---

### W3 — 求解器可行性感知

**范围**

- 按 D-v18-5 结论接入。**推荐软降级**：`PositionPlaySolution` 增可行性字段（所需仰角 / 是否需抬杆），装配处填充；排序上强惩罚；复刻 `applySpinTierBudget` 的「优先 + 兜底、绝不因此无解」范式。
- 影响面：`PositionPlaySolver`（情形 A / 情形 B / 斯诺克三处装配）+ `PositionPlaySolverModels` + `DifficultyModel`（若新增档位）+ 三个反解页的 summary 文案。

**完成标准**

1. `make build` **BUILD SUCCEEDED**。
2. `PositionPlaySolverTests` / `SnookerSolverTests` / `DifficultySolverTests` 全绿（贴真实输出）。
3. 新增单测：构造「后方贴球盘面」，断言返回解**优先为不需抬杆的解**；构造「只有抬杆解」的盘面，断言**仍返回解**且标注正确（不得变无解）。
4. `SolverPerformanceTests` 复测，**耗时回归 ≤ +10%**（可行性判据是纯几何，不应显著拖慢；超出即须剪枝优化）。
5. 三页 summary 文案截图取证。

---

### W4 — 仰角一阶进物理（条件批次）

**仅当 D-v18-6 = 本轮做时执行。**

**范围**：有效水平速度 `v_eff = v·cos θ`；θ 超阈值（建议 20°）时标注「该姿态产生弧线 / 跳球，预测仅供参考」。**不做** masse 完整物理。

**完成标准**

1. `make build` **BUILD SUCCEEDED**。
2. 物理套件不回归：`PhysicsEngineTests` / `PhysicsInvariantTests` / `PhysicsMatrixTests` 全绿。
3. **存量内容影响评估**：θ = 0 的存量 drill / 序列必须**逐位不变**（`cos 0 = 1`），须有回归断言 + 至少一条 drill 的烘焙前后比对。
4. 新增断言：θ > 0 时 `v_eff < v`，且阈值标注按预期触发。
5. `IMPLEMENTATION-LOG` 更新 DR-027 的「已知不自洽」条目——**如实写明消掉了哪部分、剩下哪部分（masse / 跳球仍未做）**，禁止宣称"仰角已进物理"。

---

## 五、待拍板项

| 编号 | 议题 | 选项 | 主控推荐 |
|---|---|---|---|
| **D-v18-1** | `spinY` 坐标系 | 杆轴系 / 世界正视图 | ✅ **已定：杆轴系**（2026-07-27） |
| **D-v18-2** | W1-a 升级条款阈值 | 长度 >5% 或半径 >20% / 其他阈值 / 不设条款直接改 | **设条款**（地基未验时不往上盖） |
| **D-v18-3** | `.blocked` / 打点不可行时是否仍发球 | ① 维持现状（隐藏杆照发）② 禁用击球 + 提示 ③ 允许但明确标「此杆需架杆，演示为简化姿态」 | **③**（保住教学信息，不撒谎也不堵路） |
| **D-v18-4** | W2 首批接线页 | 9 处 `BTSpinPadOverlay` 调用点中选 | **编排台 + 思路训练**（前者是内容生产入口、穿模最易被看见；后者是教学价值最高） |
| **D-v18-5** | 求解器接入方式 | ① 硬过滤（不可行不进候选）② 软降级 + 标注 + 排序惩罚 | **②**（硬过滤会把「为什么打不了」这个最有教学价值的信息删掉，且与 E1–E5 方法论冲突） |
| **D-v18-6** | 是否本轮做 `v_eff = v·cos θ` | ① 本轮做（W4）② 延后，且灰区文案**不提具体角度** | **①**——灰区会把 DR-027 的隐性不自洽推到用户眼前；若选 ②，文案红线必须写死 |
| **D-v18-7** | 执行模型与路由 | 主控亲做 / 委派子智能体（模型按 `01-subagent-model-selection`） | **W1 用强推理模型**（几何判断密集 + 不动点 + 地基风险）；**W2/W3 可用快速模型**（条目明确）并升级验收强度 |

> D-v18-7 一经拍板，结论写回 §四 批次表「模型」列；后续会话读到即视为已指定，不再重复询问。

---

## 六、锚点索引（2026-07-27 逐条读代码核实）

### 6.1 杆体几何与姿态

| 锚点 | 位置 | 说明 |
|---|---|---|
| `CueClearance` 常量组 | `QiuJi/Core/Scene/CueClearance.swift:12-32` | tipRadius / buttRadius / shaftLength / tipOffset / maxPullBack / 各余量 |
| `CueClearance.shaftRadius(alongShaftU:)` | 同上 `:48-51` | 线性锥（tip→butt） |
| `CueClearance.shaftSegment` | 同上 `:56-71` | 世界 tip→butt 线段 |
| `CueClearance.worldPoint` | 同上 `:175-188` | 手工 euler 展开；**须用 `SCNNode.convertPosition` 交叉验证**（DR-027 检查点 1） |
| `CueClearance.firstCollisionTime` | 同上 `:87-119` | 触球后碰撞守卫 |
| `CueClearance.forwardSurfaceGap` | 同上 `:143-164` | 跟杆前向间隙 |
| `CueStick.Constants` | `QiuJi/Core/Scene/CueStick.swift:23-38` | `railTopAboveSurface 0.038` / `railClearance 0.012` / `minElevation 0.05` / `maxElevation π/3` |
| `CueStick.requiredElevation` | 同上 `:59-72` | **无 spin 参数**（缺口二） |
| `CueStick.cushionElevation` | 同上 `:75-100` | 第 99 行 `max(0.05, distToCushion)` 地板（§3.5） |
| `CueStick.ballElevation` | 同上 `:103-135` | 保守：忽略 lateral 折减 |
| `CueStick.init(modelCueStickNode:)` | 同上 `:155-169` | **只读 `boundingBox`**（§3.3） |
| `CueStroke.strikePosition` | `QiuJi/Core/Scene/CueStroke.swift:100-109` | **仅 spinX，`cue.y` 透传**（缺口一） |
| `CueStroke.clampedFollowThroughPull` | 同上 `:34-49` | 跟杆钳制 |
| `AngleTrainingScene.runCueStroke` | 同上 `:140-201` | `.blocked` 分支在 `:158-163`（§3.4） |
| `AngleTrainingScene.updateCueStick` | `QiuJi/Core/Scene/AngleTrainingScene.swift:315-351` | 内部取 `visibleBalls()`，含 `elevationOverride` |
| `AngleTrainingScene.setupCueStick` | 同上 `:300-310` | USDZ 优先，procedural 兜底 |
| `TableModelLoader.removeNodeNames` | `QiuJi/Core/Scene/TableModelLoader.swift:21` | `["QiuGan"]` |
| `TableModelLoader.extractCueStick` | 同上 `:172-188` | 抽杆并清平移 |
| USDZ 资产 | `QiuJi/Resources/TaiQiuZhuo.usdz` | 94 MB；内含 `TaiQiuZhuo2.usdc` + `PiTou_{basecolor,normal,roughness,metallic}.png` |

### 6.2 打点盘与 miscue

| 锚点 | 位置 |
|---|---|
| `BTSpinPad` 本体 | `QiuJi/Core/Components/BTSpinPad.swift:11-71`（钳制在 `:64`） |
| `SpinPadMath.nudge` | 同上 `:148-169` |
| `BTSpinPadCard` | 同上 `:182-252` |
| `BTSpinPadOverlay` | 同上 `:256-273` |
| `SpinDisplay.readout` | 同上 `:279-288` |
| `CuePhysics` 皮头几何 | `QiuJi/Core/Physics/BTPhysicsConstants.swift:222-239` |
| `ShotIntent.clampToMiscueLimit` | `QiuJi/Core/Physics/ShotIntent.swift:58-64`；调用点 `:110` |

**`BTSpinPadOverlay` 全部 9 处调用点**（W2 / L2 范围界定）：

| # | 文件 | 行 |
|---|---|---|
| 1 | `Features/PositionPlay/Views/PositionPlayComposerView.swift` | 318 |
| 2 | `Features/PositionPlay/Views/SiluTrainerView.swift` | 200 |
| 3 | `Features/PositionPlay/Views/PlanThreeView.swift` | 213 |
| 4 | `Features/PositionPlay/Views/FreePlayView.swift` | 163 |
| 5 | `Features/SnookerTactics/Views/SnookerTacticsView.swift` | 158 |
| 6 | `Features/AngleTraining/Views/ShotSimulationView.swift` | 143 |
| 7 | `Features/AngleTraining/Views/SolverStageChrome.swift` | 289 |
| 8 | `Features/BatchDrillStudio/BatchAuthoringView.swift` | 544 |
| 9 | `Core/Rack/BreakFlowRunner.swift` | 484 |

### 6.3 `strikePosition` 全部 16 处生产调用点（W1-b 范围界定）

| # | 文件 | 行 | 已接 DR-027 clearance |
|---|---|---|---|
| 1–3 | `Features/PositionPlay/ViewModels/PositionPlayViewModel.swift` | 911, 1209, 1495 | ✅ |
| 4–5 | `Features/PositionPlay/ViewModels/SiluTrainerViewModel.swift` | 734, 895 | ✅ |
| 6–7 | `Features/PositionPlay/ViewModels/PlanThreeViewModel.swift` | 1013, 1220 | ✅ |
| 8 | `Features/SnookerTactics/ViewModels/SnookerTacticsViewModel.swift` | 571 | ✅ |
| 9–10 | `Core/Media/SequenceVideoExporter.swift` | 683, 716 | ✅（含 `elevationOverride`） |
| 11–12 | `Features/AngleTraining/ViewModels/DiamondSystemViewModel.swift` | 600, 662 | ❌ `clearanceProbe` 仍 nil |
| 13–14 | `Features/AngleTraining/ViewModels/BankShotViewModel.swift` | 621, 687 | ❌ 同上 |
| 15 | `Features/AngleTraining/Views/AimPointSceneTrainingView.swift` | 211 | ❌ 同上 |
| 16 | `Core/Rack/BreakFlowRunner.swift` | 262 | ❌ 同上 |

> 另有测试调用 `QiuJiTests/BallFogDiagTests.swift:79,86,92`（左右塞符号金标准）——**W1-b 改签名后必须保持这三条断言全绿**。
>
> 11–16 未接 clearance 是 DR-027 有意留的范围；W1-b 只改签名与默认值（零行为变化），**不在本批扩接 clearance**。

### 6.4 求解器与难度

| 锚点 | 位置 |
|---|---|
| `ShotDifficultyTier` | `QiuJi/Core/PositionPlay/DifficultyModel.swift:24-44` |
| `DifficultyModel.tier / score` | 同上 `:91-97` / `:103-115` |
| `SearchParams.spinYValues`（standard） | `QiuJi/Core/PositionPlay/PositionPlaySolver.swift:64` = `[-0.4, -0.2, 0, 0.2, 0.4]` |
| `applySpinTierBudget`（优先+兜底范式） | 同上 `:239-255` |
| 情形 A / B / 斯诺克 三处装配 | 同上 `:857-862` / `:1151-1156` / `:1566-1570` |
| `PositionPlaySolution` 字段 | `QiuJi/Core/PositionPlay/PositionPlaySolverModels.swift:199-211` |

### 6.5 测试与证据先例

| 锚点 | 位置 | 用途 |
|---|---|---|
| `TableGeometryProbeTests` | `QiuJiTests/TableGeometryProbeTests.swift:35-60` | **USDZ 顶点级实测先例**，W1-a 照抄 |
| `CueClearanceTests` | `QiuJiTests/CueClearanceTests.swift` | DR-027 基准，W1 不得回归 |
| `ShotScenarioRenderTests` | `QiuJiTests/` | PNG 出图路子，W1 完成标准 7 复用 |
| `SpinExportParityTests` / `TrajectoryPlaybackSpinTests` | `QiuJiTests/` | v17 加塞渲染基准，W1 不得回归 |
| `SolverPerformanceTests` | `QiuJiTests/` | W3 性能门 |

---

## 七、本轮明确不做

| 项 | 理由 |
|---|---|
| **L1 完整斜杆物理**（masse 弧线 / 跳球 / 台呢法向冲量） | 与本议题正交的大工程；W4 只做 `cos θ` 一阶折算，**不得宣称"仰角已进物理"** |
| **L2 灰区推广到全部 9 处调用点** | 先在首批页验证手感与性能，再推广 |
| **改动 `spinX/spinY` 存储语义 / 内容格式** | D-v18-1 选杆轴系的核心收益就是存量零改动 |
| **改动 `miscueLimitFraction` / 打滑极限口径** | 杆轴系下该常量本就正确，无需动 |
| **改 `TrajectoryStyle` / 全局线语言** | 与本议题无关 |
| **重烘焙存量 drill 缩略图 / 序列出片** | θ = 0 下行为逐位不变（W4 完成标准 3 须实证这一点） |

---

## 八、版本记录

| 版本 | 日期 | 变更 | 原因 |
|---|---|---|---|
| v18.0 | 2026-07-27 | 初版落盘：坐标契约（D-v18-1 杆轴系）+ 五关判据 + W1–W4 批次 + 7 项拍板 + 全量锚点 | 用户提出「不穿模前提下母球可行打点」议题并拍板 spinY 坐标系；对话中发现 USDZ `QiuGan` 尺寸从未实测（U1），升为 W1 闸门 |

> **台账说明**：`tasks/PROGRESS.md` 曾于本日早些时候记录「问题集合 v18 立项，真源《问题集合_v18.md》v18.0」，但该文件在仓库中不存在（`git status -uall` 亦无未跟踪记录），应为上一会话未落盘。本文件为**首次真实落盘**的 v18.0，其批次划分（W1 含实测闸门、W4 条件批次）与那条台账描述（W1/W2/W3）**不同**，以本文件为准。
