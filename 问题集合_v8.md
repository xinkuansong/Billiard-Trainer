# 练习 Tab 问题集合 v8（可多会话执行版）

> **本文件是本轮需求真源**。来源：2026-07-17 用户提交的 v8 原始清单（练习页轻修 / 球库统一 / 开球对齐 / 翻袋反射求解器正确性与加塞 / 反解微调），经四路代码探路子代理核实锚点后重组。与既有规范冲突时以本文件为准，落地后回写规范。
> 状态：**待执行**（X1–X6 全部 ⏳）。
> 排期前置：无（v7 已于 2026-07-16 全批次收官；`32-v7收官人工冒烟清单.md` 人工冒烟可与本轮并行）。
> 全部锚点已读代码核实（2026-07-17，四路探路子代理，见 §五 锚点索引）。
> 编号约定：条目 K1–K14；批次 X1–X6；拍板项 D-v8-N。

---

## 一、需求回显（用户原始诉求，语义不增删）

1. 练习卡片标题字体大小（1.1）。
2. 角度预测：点击显示参考后 1 号球会挡住参考线，参考线长度适当减短（1.2）。
3. 3D 角度训练：① 角度值的方向感觉是倒的，最好沿瞄准线方向；② 新题目时摄像头有时距球桌特别远，滑动屏幕后恢复正常；③ 3D 瞄准点场景同样有该相机问题（1.3）。
4. 练/打/解三板块球库使用不同代码，**以「打」板块为基准统一**（1.4）。
5. 开球：① 打板块开球逻辑是对的（开完可选重开/完成/取消），解板块开完直接跳转了；打板块开球有随机性、解板块没有；② 参考打板块修改解板块开球；③ 开球页面应有打点盘；④ 开球瞄准线：接触到球则在球上显示红色瞄准点，未接触则延伸至库边（1.5）。
6. 翻袋求解器：① 会把同一解当成两个解（关于进球线对称的两个假想球，一真一假；打假解时实际按真解击打；有时只显示假解但击打是真解）；② 求解似乎没引入加塞变量，导致很多情况无解；③ 页面需显示打点盘（1.6）。
7. 反射求解器：问题与翻袋基本一致（1.7）。
8. 反解器结果应支持完全的用户调整（力度、打点；现打点微调好像只有左右且受限，这种微调相当于正向求解）；且微调后的解**不应写回原解序列**——循环「下一解」回到该解时应展示原来的解（2）。
9. **2026-07-17 追加拍板**：「求解范围」选项（允许左右塞 / 仅基础走位）**只留给思路训练**；其余页面一律默认允许左右塞、可吃多库（相当于全能力求解）。翻袋/反射本无此选项，其"允许加塞"由条目 6.② 承接。
10. **2026-07-17 追加**：翻袋求解器和反射求解器的**进球线、瞄准线、母球轨迹不符合统一标准，假想球样式也不对**；现在的**吃库标注保留**（碰库金点 + 库面法线）。
11. **2026-07-17 D-v8 拍板补充**：① 卡片问题的真实现象是**标题有大有小、副标题也有大有小**（同页不一致），要一起统一；② 3D 相机问题是**同一页面内有的题目正常、有的题目特别远**——是 bug，不是「机位统一偏远」；③ 球库统一范围按建议（侧栏壳留壳对齐参数，练页/FreePlay 保留只读语义）；④ Composer 开球同改手动交付；⑤ 翻袋/反射求解模式打点盘**本轮就允许微调**（微调=转正向求解，与 K13 草稿层同语义）。

---

## 二、条目清单（K1–K14，锚点已核实）

### 练习页轻修

| 条 | 内容 | 关键锚点（核实 2026-07-17） |
|---|---|---|
| K1 | 练习首页卡片**同页标题/副标题大小不一**（用户澄清后的真实现象）。**根因（与锚点吻合）**：标题 token 统一 17pt、副标题统一 12pt，但标题 `lineLimit(1)+minimumScaleFactor(0.8)`、副标题 `minimumScaleFactor(0.65)` 按各卡文字长度**不同比例缩排**——长文案被压小、短文案保持原大 → 同页忽大忽小。分区头 caption 同款缩排（:414-415）。改：去掉 minimumScaleFactor，统一截断策略（标题允许 2 行或尾部省略，副标题单行省略），全页卡片字号恒定 | `AngleHomeView.swift:439-449`（标题 0.8 / 副标题 0.65）、:411-415（分区头 caption 0.8）；`Typography.swift:28` |
| K2 | 角度预测参考线减短：参考虚线终点走满 `rayLen`，而 1 号球画在角度线终点 `angEnd` 且后绘制（ZStack 后画盖先画），故遮挡。改法 A（推荐）：参考虚线单独缩短（`rayLen*k` 或停在球缘前 `rayLen−ballR`），不动题面角度线与球位 | `GeometricAngleQuizView.swift` `AnglePredictionFigure` :315-330（长度/终点）、:372（1 号球） |
| K3 | 3D 角度训练角度值方向倒：弧画在**反向楔形**（`aStart + π`），数字标签也在背向侧，文字对齐固定世界 +Z 而非沿瞄准线。改：前向楔形（瞄准前向↔进球前向）+ 标签沿角平分/瞄准方向。**几何红线：强制 `geometry-spatial-reasoning` 坐标契约回显 + 数值草稿** | `AngleTrainingScene.swift` `updateAngleArc` :1742-1750、:1767-1793（标签+对齐） |
| K4 | 3D 场景新题相机 **bug：同一页面有的题目起始机位正常、有的特别远**（2026-07-17 用户澄清，非「统一偏远」）。代码事实：每题经 `applyAimingPoseForCurrentQuestion` → `enterAiming`，目标恒 `zoom=1`（远）；但存在**早退路径**（如 `cueBallNode == nil` 直接 return 不复位）与 0.6s smoothToPose 动画竞态 → 有的题被拉到远机位、有的题保住用户上一题滑到的近机位，表现为忽近忽远。改：执行会话先加日志/断点实证哪条路径产生「正常」帧，再统一为**确定性近景进场**（每题机位一致，保留竖滑调整能力）；若推翻 v5 enterAiming zoom=1 设计须记 DR。3D 瞄准点同源同修 | `CameraRig.swift` `enterAiming` :222-247（zoom=1）、`update` :365-370（targetZoom 回填）、`snapToAimPose` :435-446（zoom=0）；调用方 `SceneAimingView.swift:127-139,414-417,471-481`（含 cueBallNode nil 早退）、`AimPointSceneTrainingView.swift:346-349`；竖滑 `handleVerticalSwipe` :168-172 |

### 球库统一（以「打」为基准）

| 条 | 内容 | 关键锚点 |
|---|---|---|
| K5 | 基准 = Composer/ShotSim 形态：`BTBallPaletteBar` regular **36** + ghost + 拖回 + 底栏 94。**代码事实**：W4 后代码路径大多已进 `BTBallPalette*`，分叉在**形态档位**——解三页（Silu/PlanThree/Snooker）用 compact **30** + 底栏 78/116；Extraction/Batch 用私有两行壳 + Token@30；练页 2D 用装饰库@36（只读，产品语义）；FreePlay 用参考库@36（P10.1 禁手动摆球，产品语义）；Bank/Diamond 已 36（母球/黑8 固定灰显属产品规则）。改动主体：解三页 30→36 + 底栏高度重算；Extraction/Batch 壳按 D-v8-2 拍板；练页/FreePlay 语义保留仅确认视觉对齐 | `SiluTrainerView.swift:325`（直径）/:34（底栏78）；`PlanThreeView.swift:427`/:37（116）；`SnookerTacticsView.swift:251`/:34；`BallExtractionView.swift:570-597`（私有壳）；`BTBallPaletteBar.swift:9-24`（metrics）；`SolverStageChrome.swift:376-408` |

### 开球统一（解板块对齐打板块）

| 条 | 内容 | 关键锚点 |
|---|---|---|
| K6 | 解板块开球「直接跳转 + 无随机感」根因：① `BreakFlowRunner.autoDeliverOnSettle` 默认 `true`，Silu/PlanThree（及 Composer）停稳即 `onSettled`→销毁 runner，**settled 完成/重开态不可达**（FreePlay 传 `manualDeliver: true` 才有）；② jitter 代码同源**并未关掉**，但每次进开球新建 runner 且 `seed` 初值恒 1、停稳后无法 reRack → 首局永远同一散局。改：解板块（含 Composer，D-v8-3）统一手动交付（取消/重开/完成三态），seed 初值随机化或跨 runner 递增保留 | `BreakFlowRunner.swift:69`（默认true）/:275-283（auto 直接交付）/:286-292（reRack seed++）/:77,:88（seed=1）；`SiluTrainerViewModel.swift:1024-1033`；`PlanThreeViewModel.swift:940-957`；`PositionPlayViewModel.swift:1593-1610`（manualDeliver）；`FreePlayView.swift:84-98` |
| K7 | 开球打点盘：`BreakInstrumentsOverlay` 现只有瞄准轮+力度柱，`onSpinTap` 未传（打点迷你图不渲染）；runner 无 spin 属性，`breakNow` 恒传 `spinX:0, spinY:0`（`BreakSimulator.breakShot` API 本身支持）。改：runner 增 `@Published spinX/spinY` → overlay 接 `onSpinTap` + `BTSpinPadOverlay`（抄 FreePlay :162-164）→ `breakNow`/`CueStroke.strikePosition` 传真实 spin。与 V6「随机性只留球堆间距」不冲突（用户可控打点 ≠ 隐藏随机塞） | `BreakFlowRunner.swift:407-436`（overlay）/:198-227（恒0）；`BTShotInstrumentColumn.swift:17-18,40-47`；`BreakSimulator.swift:45-58`；`FreePlayView.swift:139-164`（范例） |
| K8 | 开球瞄准线红点：现 `drawAimLine` 一律画到库内边界（`rayToInnerRail`），无接触检测。改：接 Q7.1 真源 `AimLineGeometry.resolve`（垂距<R → 线停球面第一交点 + 球上红点；否则延伸库边），目标球取顶球（与 `aimAtApex` 一致）。**几何红线：强制 `geometry-spatial-reasoning`** | `BreakFlowRunner.swift:173-190`；`AimLineGeometry.swift:16-50`；`AngleSceneCalculator.swift:1125-1135`；消费范例 `AimPointSceneTrainingView.swift:274-331`；红点 `AngleTrainingScene.swift:1530-1557` |

### 翻袋 / 反射求解器（两页同构，一处修两页受益）

| 条 | 内容 | 关键锚点 |
|---|---|---|
| K9 | 镜像假解 / 重复解（**P0 正确性**）。根因（探路高置信）：① 展示层假想球/瞄准线 = **镜像种子几何**（`prepareBankAim` 只由 seedPath 算 ghost/aimDirection），而引擎在种子 ±8° 上精修出 `aimOffsetUsed`，`buildPrediction` **从不回写**精修后方向 → 展示与真实击打路径可呈镜像分叉（「看见假解、打出真解」）；② 去重仅比 `cushions` 相同 + **种子瞄准角差 <0.3°**，不比精修后几何 → 两个镜像种子收敛到同一真实解仍双双上屏。改：精修后回写展示几何（ghost/瞄准线/出杆方向统一由 finalAim 派生），去重升级为按精修后瞄准方向/objectPath 首段判同。**几何+引擎红线** | `ShotPredictor.swift:248-270`（prepareBankAim 种子 ghost）/:886-981（±8° 精修 aimOffsetUsed）/:343-427（buildPrediction 不回写）；`BankKickDifficulty.swift:234-236,317-334`（去重）；`BankShotViewModel.swift:988-994`（展示用 pred.ghost）/:303-308,377-383（出杆用种子 aimDirection、回放用 recorder）；反射同构 `DiamondSystemViewModel.swift:904-936` |
| K10 | 加塞变量（承接 §一.9 全能力裁决）：`solveBank`/`solveKick` 现 `spinX/spinY` 写死 0，搜索空间 = 库序×瞄准角；管线（`ShotInput.spin*`→`CueBallStrike` 含 squirt）已支持。改：外层加 `spinX ∈ {-0.3, 0, 0.3}` 档（≈×3 成本，走位反解同思路），缓存 key（`BankKickSolveKey`）与 pill 扩塞标注 | `BankKickDifficulty.swift:242-248,283-285`；`ShotPredictor.swift:614-617,886-958`；`AnalyticAimModel.swift:73-75` |
| K11 | 翻袋/反射页显示打点盘：现求解模式 `onSpinTap=nil`（W4 有意，因当时解无塞），自由模式才可开。K10 落地后解携带塞 → 求解模式打点盘显示解的塞，且**允许用户微调**（D-v8-5 拍板 b）：微调=转正向求解（按用户塞/力度重跑引擎预测），并与 K13 同语义走**草稿层**——不写回解序列，「下一解」回位展示原解 | `SolverStageChrome.swift:256-260,285-290`；`BTShotInstrumentColumn.swift:17-18,40-47` |
| K14 | 翻袋/反射解的**线语言/假想球不符全 App 统一标准**（2026-07-17 追加）。代码事实：两页 `drawSolution` 是**手搓绘制**，未走共享 `TrajectoryRenderer.draw` / `addCueTrajectory` / `addObjectTrajectory`：① 进球线画成**本色实线**（标准=本色**虚线** `addObjectTrajectory`）；② 瞄准线用 `UIColor.white` 直画（标准=`TrajectoryStyle.aimColor` 实线段）；③ 母球轨迹只画碰后 hint 虚线，碰前/碰后没有走标准「白实线瞄准段+白虚线碰后段」分段语义（`addCueTrajectory`）；④ 假想球用私有 `addGhostSphere`（半透明实心球），非标准 `ghostBallNode`（虚线圆环 + 瞄准点红心 aimDot）。改：两页解绘制改走 `TrajectoryRenderer` / scene 共享 API（含三档 detail 语义），删私有 addGhostSphere；**保留**翻袋/反射特有的吃库标注（碰库金点 + 库面法线，full 档），用户明确要求保留 | `BankShotViewModel.swift:935-1029`（drawSolution+addGhostSphere）、`DiamondSystemViewModel.swift:564,904-950`；标准真源 `TrajectoryRenderer.swift:60-158`、`AngleTrainingScene.swift:1095-1122`（addCueTrajectory/addObjectTrajectory）、:1440-1457（ghostBallNode 虚线环+红心） |

### 反解微调与求解范围

| 条 | 内容 | 关键锚点 |
|---|---|---|
| K12 | 「求解范围」只留思路训练（§一.9 已拍板）：PlanThree/Snooker 三点菜单删「求解范围」Section，两 VM 求解恒走全能力（`allowSideSpin=true`/`basicPositionOnly=false`，即现默认值；属性与 `SolveShotSnapshot` 字段保留以免破坏快照兼容，仅摘除 UI 与 didSet 依赖按需清理）；Silu 保留现状；BatchAuthoring（SIM only）保留不动；Bank/Diamond 本无此选项、全能力由 K10 达成，不补 UI | `PlanThreeView.swift:456-468`；`SnookerTacticsView.swift:280-292`；`SiluTrainerView.swift:354-366`（保留）；`BatchAuthoringView.swift:833-838`（豁免）；`BTShotPageChrome.swift:302-360`（模板） |
| K13 | 微调不污染解序列 + 完全可调（**P0**）。代码事实：① `adjustCurrentSolution` **原地写 `solutions[idx] = 新解`**，「下一解」循环回来展示的是微调版而非原解——与用户诉求直接冲突；② 打点微调 UI 无轴锁定，「只有左右/左右受限」的观感来自 miscue 圆钳制（\|spinY\| 大时 \|spinX\| 上限变小）+「瞄准只随 spinX 变、spinY 只改碰后走位」+（若开关关了）`allowSideSpin=false` 禁横塞；K12 落地后 PlanThree/Snooker 恒全能力，Silu 若关开关则微调也应放开（微调=正向求解，不受求解范围约束，需明确语义）。改：微调改**草稿层**（保留原解快照；展示/击打用草稿，`nextSolution`/循环回位展示原解；再次微调基于原解或草稿按交互定义），力度/打点全向可调（微调即正向重算预测，管线已支持） | `SiluTrainerViewModel.swift:87-92,461-462,475-518`；`PlanThreeViewModel.swift:669,1028-1064`；`SnookerTacticsViewModel.swift:387,589-624`；`BTSpinPad.swift:11-67,138-168`（miscue 圆）；`PositionPlaySolver.swift:20-25,62-64` |

---

## 三、批次表（X1–X6）

依赖图：

```
X1 练页轻修（K1–K4）        独立
X2 球库统一（K5）           独立
X3 开球统一（K6–K8）        独立
X6 反解微调与求解范围（K12+K13：草稿层语义真源） ──┐
X4 翻袋/反射真假解+线语言（K9+K14） ────────────────┴→ X5 加塞 + 可微调打点盘（K10+K11）
```

X1/X2/X3/X6 相互独立可并行或换序；**X5 双依赖 X4 + X6**（先修真假解正确性再扩搜索维，且 K11 可微调打点盘复用 K13 草稿层语义——X6 先定义「微调草稿不污染解序列」的状态机，X5 在翻袋/反射页移植同款）。X3 与 X6 无文件交集（BreakFlowRunner vs 三 VM 微调路径）；X2 与 X6 同触 Silu/PlanThree/Snooker 的 **View**（X2 改球库参数、X6 改 moreMenu/VM），并行需 worktree 隔离或串行。

| 批次 | 范围 | 量级 | 依赖 | 推荐模型（待确认） |
|---|---|---|---|---|
| X1 | K1 卡片字号缩排统一、K2 参考线减短、K3 角度弧方向（几何红线）、K4 相机忽近忽远 bug（先实证复现路径再修，DR 视结论） | 4–6 文件，点改为主；K3 需数值草稿、K4 需复现取证 | 无 | grok-4.5（K3/K4 验收强化：数值草稿 + 复现截图） |
| X2 | K5 球库统一：解三页 30→36 + 底栏高度、Extraction/Batch 留壳对齐球径/行距（D-v8-2a）、metrics 注释与 D7 档位约定同步 | 5–8 文件 | 无 | grok-4.5 |
| X3 | K6 手动交付（含 Composer，D-v8-3a）+ seed 随机化、K7 开球打点盘、K8 瞄准线红点（几何红线） | BreakFlowRunner 为中心 + 4 宿主接线，约 6–8 文件 | 无 | grok-4.5（K8 验收强化） |
| X4 | K9 翻袋/反射展示几何回写 + 去重升级、K14 解绘制改走 `TrajectoryRenderer`/共享 scene API + 标准假想球（吃库金点/法线标注保留），两条同触 `drawSolution` 一次改净 | ShotPredictor + BankKickDifficulty + 两 VM，判断密集 | 无 | **opus-4.8**（引擎/几何正确性批） |
| X5 | K10 加塞搜索档 + 缓存 key/pill 扩展、K11 求解模式可微调打点盘（草稿层语义，D-v8-5b） | BankKickDifficulty + SolverStageChrome + 两 VM，含性能验证 | **X4 + X6** | **opus-4.8** |
| X6 | K12 求解范围摘除（PlanThree/Snooker）、K13 微调草稿层 + 完全可调（草稿层语义真源，供 X5 移植） | 三 VM + 三 View，语义设计密集 | 无 | **opus-4.8**（K13 状态机语义） |

### 各批完成标准

**X1**：`make build` + 相关单测绿；K1 首页截图（长短标题卡同帧、字号恒定无缩排）；K2 角度预测「显示参考」90°/45°/0° 三帧截图（1 号球不再遮参考线）；K3 数值草稿（弧起止角 vs 瞄准/进球前向的数值验证）+ 3D 截图（弧与数字在瞄准线前向侧）；K4 先给出复现证据（哪条路径产生「正常」帧 vs「特别远」帧的日志/截图对照），修后连续出题 ≥5 题机位一致截图 + 结论记 DR（若改动 enterAiming 契约）。

**X2**：`make build` + `make test` 绿；解三页拖球/拖回/pulse 实操截图（36 球径不挤压底栏）；与 Composer 同帧对照截图；grep 复核 compact 档消费点清单与 metrics 注释一致。

**X3**：`make build` 绿；`BreakFlowRunnerV6Tests` 更新后全绿（手动交付状态机 + seed 随机性断言：同 runner reRack 异局、新 runner 首局非恒同）；Silu/PlanThree 开球全流程截图（racked→breaking→settled 完成/重开/取消三钮）；打点盘开合 + spin 传入断言（`breakNow` 非恒 0 单测）；瞄准线两态截图（接触红点 / 未接触到库边）+ K8 数值草稿。

**X4**：`make build` + 全量 `QiuJiTests` 绿；新增钉子单测：展示 ghost/瞄准方向与引擎 `finalAim` 一致（禁种子几何直出）、镜像种子收敛同解时去重后仅存一解；翻袋/反射典型盘面截图（假想球与回放路径同向）；性能基准不回退（PERF-W1/W2 同轮对照）。K14：与 Composer/Silu 同帧对照截图（进球线虚线本色、瞄准线 aimColor、母球实/虚分段、假想球虚线环+红心均一致），full 档截图确认吃库金点+法线仍在；grep 复核两 VM 无残留私有 `addGhostSphere`。

**X5**：`make build` + 测试绿；加塞后原「无解」盘面出解的对照截图（before/after）；性能基准（全枚举×3 塞档时延实测，超预算需降档策略留档）；缓存 key 扩展单测；求解模式打点盘可微调实操截图 + 钉子单测：微调走草稿层、原解不被改写、「下一解」回位展示原解（与 X6 同款断言）。

**X6**：`make build` + 三页求解回归单测绿；新增钉子单测：微调后 `solutions[idx]` 原值不变、`nextSolution` 循环回位展示原解、草稿击打走微调值；PlanThree/Snooker 三点菜单截图（无「求解范围」Section）、Silu 截图（保留）；打点全向微调实操截图（上下塞可调、瞄准线随 spinX 联动）。

---

## 四、拍板记录（D-v8 系列，2026-07-17 全部裁决完毕）

| 项 | 问题 | 用户裁决 |
|---|---|---|
| D-v8-1 | K1 卡片标题字号 | **澄清真实现象**：同页标题/副标题有大有小（缩排所致）。裁决=统一大小：去 `minimumScaleFactor`、统一截断策略，标题/副标题一起处理 |
| D-v8-2 | K5 统一范围 | **a**：Extraction/Batch 留侧栏壳只对齐球径/行距；练页装饰库与 FreePlay 参考库保留只读语义 |
| D-v8-3 | K6 Composer 开球 | **a**：同改手动交付，全 App 开球统一取消/重开/完成三态 |
| D-v8-4 | K4 相机 | **澄清为 bug**：同页有的题正常、有的特别远（忽近忽远），非「机位统一偏远」。裁决=先实证复现路径，修成每题确定性一致机位 |
| D-v8-5 | K11 翻袋/反射求解模式打点盘 | **b**：本轮就允许微调（微调=转正向求解），与 K13 草稿层同语义（X5 依赖 X6） |

此前已拍板：**求解范围只留思路训练**（K12）；解板块开球以打板块为准（K6）；球库以打板块为基准（K5）。

---

## 五、锚点索引（核实日期 2026-07-17，四路探路子代理 + 主控抽查）

| 域 | 文件 | 关键行 |
|---|---|---|
| 练习首页 | `QiuJi/Features/AngleTraining/Views/AngleHomeView.swift` | 439-443（卡片标题）、51-55/126-209（练打解分类） |
| 角度预测 | `QiuJi/Features/AngleTraining/Views/GeometricAngleQuizView.swift` | 315-330、372（AnglePredictionFigure） |
| 3D 场景 | `QiuJi/Features/AngleTraining/Scene/AngleTrainingScene.swift` | 1742-1793（updateAngleArc）、1530-1557（红点 API） |
| 相机 | `QiuJi/Features/AngleTraining/Scene/CameraRig.swift` | 222-247（enterAiming）、435-446（snapToAimPose） |
| 场景测验页 | `SceneAimingView.swift:475-481`；`AimPointSceneTrainingView.swift:274-331,346-349` | 相机调用 + 瞄准线红点范例 |
| 球库组件 | `QiuJi/Core/Components/BTBallPaletteBar.swift` | 9-24（metrics 两档 30/36） |
| 解三页球库 | `SiluTrainerView.swift:325,34`；`PlanThreeView.swift:427,37`；`SnookerTacticsView.swift:251,34` | 直径 + 底栏高 |
| Extraction 壳 | `BallExtractionView.swift:570-597,351,434,474-497` | 私有 paletteTwoRows |
| 开球 | `QiuJi/Core/PositionPlay/BreakFlowRunner.swift`（若路径有出入以 grep 为准） | 69/77/88（auto+seed）、173-190（drawAimLine）、198-227（spin 恒0）、275-292（settled/reRack）、332-436（ControlBar+Overlay） |
| 开球宿主 | `SiluTrainerViewModel.swift:1024-1033`；`PlanThreeViewModel.swift:940-957`；`PositionPlayViewModel.swift:1593-1610`；`FreePlayView.swift:84-98,139-164` | 接线与 manualDeliver |
| 瞄准线几何 | `QiuJi/Core/Geometry/AimLineGeometry.swift:16-50`；`AngleSceneCalculator.swift:1125-1135` | resolve / rayToInnerRail |
| 翻袋反射引擎 | `QiuJi/Core/Physics/ShotPredictor.swift` | 248-270（prepareBankAim）、343-427（buildPrediction）、886-981（精修）、1234-1253（predictBankAll） |
| 翻袋反射装配 | `QiuJi/Features/AngleTraining/BankKickDifficulty.swift` | 234-236/317-334（去重）、242-285（spin 恒0） |
| 翻袋反射页 | `BankShotViewModel.swift:988-994,303-308,377-383`；`DiamondSystemViewModel.swift:904-936`；`SolverStageChrome.swift:256-290,376-408` | 展示/击打/打点盘/球库 |
| 翻袋反射线语言 | `BankShotViewModel.swift:935-1029`；`DiamondSystemViewModel.swift:564,904-950`；`TrajectoryRenderer.swift:60-158`；`AngleTrainingScene.swift:1095-1122,1440-1457` | 手搓 drawSolution vs 共享标准（K14） |
| 反解微调 | `SiluTrainerViewModel.swift:87-92,461-518`；`PlanThreeViewModel.swift:669,1028-1064`；`SnookerTacticsViewModel.swift:387,589-624`；`BTSpinPad.swift:11-67,138-168` | adjustCurrentSolution 原地写 + miscue 圆 |
| 求解范围 UI | `SiluTrainerView.swift:354-366`；`PlanThreeView.swift:456-468`；`SnookerTacticsView.swift:280-292`；`BTShotPageChrome.swift:302-360`；`BatchAuthoringView.swift:833-838` | BTSolverMoreMenu solveRange |

> 注：行号为探路核实时快照，执行会话开工前应 grep 复核（尤其 `BreakFlowRunner.swift` 实际路径与 SolverStageChrome 行段）。

---

## 六、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v8.0 | 2026-07-17 | 用户原始清单（25 行） |
| v8.1 | 2026-07-17 | 按 `/issue-collection-restructure` 重组：四路探路核锚点，条目化 K1–K13，批次 X1–X6（X4→X5 依赖），D-v8-1~5 待拍板；并入用户追加裁决「求解范围只留思路训练」（K12） |
| v8.2 | 2026-07-17 | D-v8-1~5 全部裁决：K1 改写为「同页字号忽大忽小（缩排根因）」；K4 改写为「同页忽近忽远 bug，先实证复现」；K11 升级为可微调（草稿层同语义）→ X5 依赖改为 X4+X6；X1/X5 完成标准同步更新 |
| v8.3 | 2026-07-17 | 用户追加 K14：翻袋/反射进球线/瞄准线/母球轨迹/假想球不符统一线语言标准（吃库标注保留），锚点已读代码核实（手搓 drawSolution vs `TrajectoryRenderer` 真源），并入 X4 批次（与 K9 同触 drawSolution），X4 完成标准补同帧对照要求 |
