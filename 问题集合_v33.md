# 问题集合 v33 — 7 条新录序列落地：出片回填 + animation 重标 + 精讲撰写

> **本文件是本轮需求真源**，供 `plan-batch-execution` / `plan-delegated-execution` 执行。高版本覆盖低版本。
>
> - **版本**：v33.0
> - **日期**：2026-08-09
> - **状态**：✅ **已收官（2026-08-09，W0–W5 全部完成并由主控独立验收）**。见 §五 批次表验收记录与 §九 v33.1。⚠️ 未 `git commit`；模拟器人工目视未完成（详见 §五 W5 行）。
> - **来源**：2026-08-08 用户按 `新增训练动作.md` 新增 6 条 drill 骨架（c079–c084）并登记进 `index.json`；2026-08-09 用户完成编排台录制（含 c060 两球形、c066 开球 0 杆），本会话逐项拍板合并方式与节标题口径。
> - **编号约定**：条目 **E1–E8**；批次 **W0–W5**；拍板项 **D-v33-N**。
> - **模型路由定稿（2026-08-09 用户拍板）**：W0 = `cursor-grok-4.5-high-fast`；W1–W5 = `claude-opus-5-thinking-medium`。按 `01-subagent-model-selection` 视为**已指定**，后续会话不再重复询问。任一批首轮验收不达标，主控可提请改派。
> - **排期前置（必读）**：
>   1. **与 `问题集合_v31`（⏳ 待执行）交叠，v33 先行**。v31 的 R1（`secondaryCategories` 副分类）、R2（单动作训练量中等上调）、I6（`sets` 与球形数/杆数口径定稿）都会覆盖本轮 7 条 drill 的 `sets` 与分类归属。**v31 执行时必须把 c079–c084 + c060 纳入范围**（现值见 §二表），否则这 7 条会被漏改成口径孤儿。⛔ v33 各批不得自行调整 `sets`——那是 v31 的裁定范围。
>   2. 工作区停有 **v30 W0 未提交改动**（理论页基建）。v33 若与 v30/v31/v32 并行推进，各批须在独立 worktree 执行、主控串行合并。
>   3. **完成标准一律用「聚焦测试 + 既有失败基线对照」**，⛔ 不得写「全量 `QiuJiTests` 全绿」（既有债见 §二）。基线数字由 W0 现场实测，⛔ 不照抄 v31 文件头的「56 项」（那是 2026-08-07 的数）。

---

## 一 需求回显（用户拍板结论，语义基准）

| # | 结论 | 拍板方式 |
|---|---|---|
| R1 | `新增训练动作.md` 的 4 条走位进 `positioning`、2 条分离角进 `separation`，5 条防守要点并进安全球 `drill_c060` | 2026-08-08 用户原话「新增到走位和分离角中，而防守放到安全球中」 |
| R2 | 内容由用户自己在编排台录制，AI 先建骨架 | 2026-08-08「具体内容我自己来录，但是需要你先建相应的动作吧」 |
| R3 | 骨架**立即登记进 `index.json`** 使其在动作库可见（已完成，drill 总数 77 → 83） | 2026-08-08「但是我在动作库中没看到呀」 |
| R4 | c060 的两条已录球形**合并为一条序列**，不做成两个球形 | 2026-08-09「把安全球的那两个合并成一个吧」 |

---

## 二 现状关键事实（2026-08-09 实测核实）

### 2.1 已录序列（`content/position_play/sequences/`，9 个文件均未跟踪）

| 序列 | 杆数 | 备注 |
|---|---|---|
| `drill_c079__manual01-四球走位 · 球形1-4杆` | 4 | 走位链候选 |
| `drill_c080__manual01-上下半台走位 · 球形1-4杆` | 4 | — |
| `drill_c081__manual01-四球同袋叫位 · 球形1-4杆` | 4 | 走位链候选 |
| `drill_c082__manual01-横向色彩围 8 · 球形1-12杆` | 12 | 本轮最长 |
| `drill_c083__manual01-吃库分离角 · 球形1-8杆` | 8 | 独立阶梯候选 |
| `drill_c084__manual01-不吃库分离角 · 球形1-8杆` | 8 | 独立阶梯候选 |
| `drill_c060__manual01-安全球布置 · 球形1-4杆` | 4 | D-v33-1 合并保留方 |
| `drill_c060__manual02-安全球布置 · 球形2-4杆` | 4 | D-v33-1 合并后删除 |
| `drill_c066__manual01-开球训练（中式台球） · 球形1-0杆` | **0** | 空序列，渲不出片；本轮**不处理**（见 §七） |

**c060 两条序列的形态实测（决定 D-v33-1 可行性）**：两条序列**内部每一杆的盘面各不相同**——球形1 第1杆台上 `_1/_8/_9`、第2杆 `_1/_2/_8`、第3杆仅 `_8`、第4杆 `_1/_8/_9/_10/_11`；母球起点仅在成对的两杆间复用。即每一杆本就是一个**独立防守场面**，不是连贯走位链。~~全部 8 杆 `shot.targetKey` 与 `shot.pocket` 均为空串（freeAim 自由瞄准）。~~ ⚠️ **本句立档时有误，W5 实测更正（L11）**：合并后**第五杆** `targetKey="_1"` / `pocket="topCenter"`（左侧中袋），其余 **7 杆**才是 freeAim。其中 4 杆有球进袋：原球形2 的第 1/2 杆（进 `_1`）、第 3 杆（进 `_9`），原球形1 第 4 杆（进 `_11`）。

### 2.2 7 条 drill 的 JSON 现状

| drill | 目录 | subcategory | sets（**v31 将覆盖**） | animation.pocket | tutorial | shotIntent |
|---|---|---|---|---|---|---|
| c060 | specialShots | safety | 3 × 10 | `''` ⚠️ §8.16 | `singleShot` / 4 节（**非逐杆节**） | 有 |
| c079 | positioning | comboPosition | 10 × 4 | bottomCenter（占位） | 无 | 无 |
| c080 | positioning | halfTable | 3 × 15 | bottomCenter（占位） | 无 | 无 |
| c081 | positioning | comboPosition | 10 × 4 | bottomCenter（占位） | 无 | 无 |
| c082 | positioning | snakeDrill | 5 × 7 | bottomCenter（占位） | 无 | 无 |
| c083 | separation | cushionSeparation | 3 × 10 | bottomCenter（占位） | 无 | 无 |
| c084 | separation | directSeparation | 3 × 10 | bottomCenter（占位） | 无 | 无 |

c079–c084 的 `animation` 全部是骨架占位（`source: "manual"`、`generator: "skeleton/pending-recording"`、母球 0.5/0.15、目标球 0.5/0.3），**与实测序列无关**，W1 必须按序列重标。

### 2.3 门禁基线（`python3 scripts/verify_tutorial_sync.py --only C4 I5 I8 I9 I10`，2026-08-09 实测，总计 FAIL 0）

| 检查 | 结果 | 与本轮的关系 |
|---|---|---|
| C4 | 通过 66 / 不一致 **0** / 待迁移或无序列 11 | c060 落在 warn「legacy 无逐杆节，待迁移」（现 4 节无 `第N杆` 标题）；c079–c084 无 `tutorial` 故不进 C4。⚠️ **一旦写了带 `第N杆` 标题的精讲，C4 立刻按杆数强校验** |
| I5 | 通过 66 / 违规 0 / 豁免 0 | 精讲 `image` 须用 `manual01` token |
| I8 | 通过 90 / 违规 0 / **提示 9** | 9 条上游序列 Bundle 未同步 → W0 跑 `make tryout-sync` |
| I9 | 通过 73 / 违规 **0** / 豁免 8 / **提示 2** | ✅ **已因录制转绿**。c060 / c066 出现「豁免已可解除（已有序列）」→ 棘轮要求从 `i9_no_sequence_exempt` **删除这两条**（W0，E3） |
| I10 | 通过 84 / 违规 0 | 骨架 JSON 可解码，重标 animation 后须保持 |

### 2.4 已知失败（非本轮引入，⛔ 不得「修绿」——FL-027）

- `DrillContentValidationTests.test_allDrills_pocketValueIsValid`：`c045/c049/c054/c059/c060/c061` 的 `animation.pocket` 为空串（契约 §8.16，另立批次）。
- `DrillBoardBuilderTests.test_c042_multiShot_withObstacles_usesFirstShotOnly`、`DrillTryoutBoardStoreTests.test_briefLines_withoutFormation_unchanged`：c042 `shotIntent` 存量债。
- 上述三条已在干净 HEAD worktree 复现确认。**W0 须重新实测一次完整失败清单并落盘作本轮基线。**

### 2.5 可验证数字

- `content/position_play/sequences/drill_c*.json` 现 **99** 条 → D-v33-1 合并后 **98** 条。
- `QiuJi/Resources/DrillBoards/` 现 **90** → W0 `tryout-sync` 后应为 **98**。
- `index.json` 登记 drill 总数 **83**（测试断言已同步：`DrillContentValidationTests` 2 处、`DrillListViewModelTests` 4 处）。

---

## 三 拍板项（已收口）

| 编号 | 议题 | 结论 | 依据 |
|---|---|---|---|
| **D-v33-1** | c060 两球形如何合并 | **合并序列文件**：`manual02` 的 4 个 step 原样追加到 `manual01` 的 `steps` 末尾，文件改名为 `drill_c060__manual01-安全球布置 · 球形1-8杆.json`，删除 `manual02`；token 保持 `manual01`。精讲用 `tutorial.sections` 平铺（⛔ 不用 `formations`） | 每 step 自带完整 `before`/`after`、UUID 独立，属**机械拼接、不产生任何新坐标**（不触红线「AI 不生成球形坐标」）；且 §2.1 实测各杆本就独立。备选「留两序列 + 精讲写一个球形」会让 C4 FAIL（`verify_tutorial_sync.py:330` 直接比球形数/杆数），需新增豁免，成本更高 |
| **D-v33-2** | 4 杆有球进袋怎么处理 | **保留**，精讲如实说明「防守的判据是留下什么局面，进球是顺带结果」 | 用户选「保留 + 如实说明」。契约 §8.5 已认定安全球「成功判据非进袋」，故不按 `tutorial-authoring` 第 2 步红灯处理 |
| **D-v33-3** | c060 的 8 个独立场面节标题 | **折中式：`第一杆：贴库防守`**（冒号后写场面名，⛔ 不编造袋口）。⛔ **本轮不改** `SHOT_SECTION_RE` 与 `isShotStructureTitle` | 改成「场面一：…」会双向踩坑：`scripts/verify_tutorial_sync.py:95` 的 `^第.+杆` 数出 0 杆 → C4 FAIL 阻塞 push；`DrillTutorialView.swift:157-160` 的 `hasPrefix("第") && contains("杆")` 主结构识别失效 → 丢编号与图标、字号层级与其他 drill 不一致。真做属横切重构，另立批次。折中式两处正则均命中，编号解析（取「第」到首个「杆」之间）正常 |

> **§8.6 关系**：「`第N杆` 标题承载两种互斥语义」是契约已登记的既有偏差（8 个 drill 同形），c060 加入不算**新增**偏差；横切统一另行排期。

---

## 四 条目清单

| # | 条目 | 批次 |
|---|---|---|
| E1 | c060 序列合并（D-v33-1）+ 改名 + 删 `manual02` | W0 |
| E2 | `make tryout-sync` → `make position-export` → `import-engine-export-to-app.py --prune` 回填配图 | W0 |
| E3 | `i9_no_sequence_exempt` 删除 `drill_c060` / `drill_c066`（棘轮收紧）；契约 §8.5 同步改为 8 条 | W0 |
| E4 | 实测并落盘本轮测试失败基线 | W0 |
| E5 | c079–c084 的 `animation` 按序列实测重标（⛔ 禁脑算）+ `make thumbnails` | W1 |
| E6 | c079 / c081 精讲（走位链，各 4 杆） | W2 |
| E7 | c080（4 杆）/ c082（12 杆）精讲 | W3 |
| E8 | c083 / c084 精讲（各 8 杆，独立阶梯混合式）；c060 精讲（8 场面）+ 全轮收尾 | W4 / W5 |

---

## 五 批次表

| 批次 | 范围 | 量级 | 依赖 | 模型 | 状态 |
|---|---|---|---|---|---|
| **W0** | E1–E4：c060 合并 + 全管线出片回填 + I9 豁免收紧 + 测试基线实测 | 中（出片耗时长，判断少） | D-v33-1 已拍 | `cursor-grok-4.5-high-fast` | ✅ 2026-08-09（验收：序列 8 杆、DrillBoards 98、I8/I9 提示归 0、verify-gate FAIL 0、基线落 `build/v33-w0-logs/`） |
| **W1** | E5：6 条 drill `animation` 按序列重标 + 缩略图烘焙 + 门禁复验 | 中（**几何敏感**） | W0（需回填后的实测数据） | `claude-opus-5-thinking-medium` | ✅ 2026-08-09（主控独立复核：6 条 animation 四字段逐值等于序列首杆 `before`/`after`/`shot.pocket`；verify-gate 复跑 FAIL 0 / I10 84；聚焦测试失败集与 W0 基线逐字符相同 3 条；`make build` SUCCEEDED；6 张缩略图新增。日志 `build/v33-w1-logs/`） |
| **W2** | E6：c079 + c081 精讲（建立本轮语感基准） | 中 | W0 配图 + W1（可与 W1 并行，若配图已就绪） | `claude-opus-5-thinking-medium` | ✅ 2026-08-09（主控独立复核：8 节结构 + 逐杆节 `content==""` + 标题命中 `^第.+杆`；4 杆 `params` 逐字符等于序列 `shot`；verify-gate 复跑 C4 66→**68** 不一致 0 / I5 68 / FAIL 0；I5 空 token 合法性经读码确认为「单序列 + manual01」正当通过而非豁免；正文语域抽读合格） |
| **W3** | E7：c080 + c082 精讲 | 中大（c082 十二杆） | W2（对齐语感） | `claude-opus-5-thinking-medium` | ✅ 2026-08-09（主控独立复核：c080 8 节/4 逐杆节、c082 16 节/**12** 逐杆节，标题与袋口名逐字等于 digest；16 杆 `params` `repr()` 级全等；`content==""`、label 三名一字不差、19 处 `image` 实文件存在；verify-gate 复跑 C4 68→**70** 不一致 0 / I5 70 / FAIL 0；c082 篇幅实测非均摊，胜负手第七杆 506 字 vs 压缩档 165–193 字） |
| **W4** | E8a：c083 + c084 精讲（独立阶梯） | 中大 | W2 | `claude-opus-5-thinking-medium` | ✅ 2026-08-09（主控独立复核：各 12 节 / **8** 逐杆节，混合式落地——锚点档 4 档写全三条目 299–429 字、中间档仅 `自检` 78–131 字；16 杆 `params` `repr()` 级全等；正文扫描无「胜负手/关键档/定调杆」及内部词汇；verify-gate 复跑 C4 70→**72** 不一致 0 / I5 72 / FAIL 0。实测 `spinY` 单调递减（c083 +0.120→−0.482、c084 +0.050→−0.240）、c084 `spinX` 全 0，证实「变量 = 打点高低」而非预判的「切球厚薄」） |
| **W5** | E8b：c060 精讲（8 独立场面，`singleShot` → `multiShot` 重写）+ 全轮收尾回写 | 中 | W0（合并后序列）+ W2 | `claude-opus-5-thinking-medium` | ✅ 2026-08-09（12 节 / **8** 逐杆节，标题全部命中 `^第.+杆`；`tutorialKind` `singleShot`→`multiShot`、`sections` 平铺；8 杆 `params` 对序列 `shot` `repr()` 级全等；混合式落地——锚点档 第一/四/五/六 写全三条目、中间档 第二/三/七/八 仅 `自检`；verify-gate 复跑 **C4 72→73 / 待迁移 11→10 / 不一致 0**、I5 73、I10 84、总计 FAIL 0；`make build` SUCCEEDED；聚焦测试 38 项 6 失败 = 既有 `pocket ''` 一条用例，c060 该失败**仍在**未修绿。⚠️ 实测更正 §2.1：**第五杆并非 freeAim**（`targetKey=_1` / `pocket=topCenter` 左侧中袋），八杆里 7 杆 freeAim。日志 `build/v33-w5-logs/`） |

依赖图：

```
D-v33-1～3 拍清
   ↓
W0（合并 + 出片回填 + 豁免收紧 + 基线）
   ├──→ W1（animation 重标 + 缩略图）
   └──→ W2（c079/c081 精讲，语感基准）
             ├──→ W3（c080/c082）
             ├──→ W4（c083/c084）
             └──→ W5（c060 + 收尾）
```

> W3/W4 互不相交（不同 drill 文件），若走 `plan-parallel-delegated-execution` 可同波并行；W5 含收尾回写，须串行压尾。

---

## 六 各批次详情与完成标准

### W0 — c060 合并 + 全管线出片回填

**范围**：

1. **E1**：读 `drill_c060__manual02-…球形2-4杆.json`，把其 `steps` 数组的 4 个元素**原样**追加到 `manual01` 的 `steps` 末尾（⛔ 不改任何坐标、不重算 `after`、保留原 UUID）；`updatedAt` 更新；文件改名为 `drill_c060__manual01-安全球布置 · 球形1-8杆.json`；删除 `manual02` 文件。合并后校验：`len(steps) == 8`、JSON 可解析、文件名匹配 `§3.2` 协议（`drill_cNNN__<token>-<名称>-<N>杆.json`，token 内⛔无 `-`）。
2. **E2**：依次跑
   - `make -f scripts/Makefile tryout-sync`
   - `make -f scripts/Makefile position-export`
   - `python3 scripts/import-engine-export-to-app.py --prune`（`--skip-json` 已默认开启，⛔ 不要传 `--no-skip-json`，否则会把 v25 W1 清空的 `videos[]` 全部写回——契约 §8.14）
3. **E3**：`scripts/content_invariant_baselines.json` 的 `i9_no_sequence_exempt` 删除 `drill_c060`、`drill_c066`（10 → 8）；`_i9_doc` 的「10 个 drill」改为 8 个；`.kiro/steering/content-data-contract.md` §8.5 同步（标注 c060 已录 8 杆合并序列、c066 已录 0 杆空序列但仍无可出片内容 → 见 §七 遗留）。
4. **E4**：跑一次聚焦测试并把完整失败清单落 `build/v33-w0-logs/test-baseline.txt`，作为 W1–W5 的对照基线。

**完成标准**：

- [ ] `ls content/position_play/sequences/drill_c060*` 只剩 1 个文件，名含 `-8杆`；`python3 -c "import json;d=json.load(open(...));print(len(d['steps']))"` 输出 `8`。
- [ ] `make tryout-sync` 输出「已同步 **98** 个球形序列」。
- [ ] `make position-export` 成功；`ls build/position_play_export/ | grep -E "c079|c08[0-4]|c060"` 应有 7 个目录（c066 0 杆无产物，属预期）。
- [ ] 回填后 `ls QiuJi/Resources/DrillTutorials/ | grep -E "c079|c08[0-4]|c060" | wc -l` > 0，且张数与各序列杆数 + `initial` 吻合。
- [ ] `make verify-gate` → **总计 FAIL 0**，且 I8 提示归 0、I9 提示（豁免可解除）归 0。附完整输出。
- [ ] `build/v33-w0-logs/test-baseline.txt` 已落盘，内含逐条失败用例名。
- [ ] ⛔ 未改任何 drill JSON 的 `sets` / `category` / `secondaryCategories`（属 v31）。

**不做**：改 `animation`；写任何精讲；改 c066；改 §8.16 的 `pocket` 空串。

---

### W1 — animation 按序列重标 + 缩略图

**前置强制**：本批属几何/坐标任务，动手前**必读** `geometry-spatial-reasoning` 技能并回显坐标契约（所在坐标系 / 轴向 / 单位），与 `.kiro/steering/table-geometry.md` 交叉核对。⛔ 禁止脑算、禁止目测配图取值。

**范围**：

1. c079–c084 六条 drill 的 `animation` 从骨架占位改为**序列实测派生**：`cueBall.start` / `path`、`targetBall.start` / `path`、`cueDirection` 取自序列首杆的 `before`/`after` 与 `shot`；`source` / `generator` 字段按实际来源改写（⛔ 不再留 `skeleton/pending-recording`）。
2. `animation.pocket`：⚠️ c083/c084 是分离角练习、c079–c082 各杆袋口不同，**取值必须有依据**——只能来自序列该杆的实测进袋口或 `shot.pocket`；若序列本身为 freeAim / 无指定袋口，**如实上报并停下**，⛔ 不许为了让 `test_allDrills_pocketValueIsValid` 变绿而硬塞一个袋口（FL-027）。
3. `make -f scripts/Makefile thumbnails`（**必须在 animation 改完之后**，否则烤出的是占位假图）。
4. `shotIntent`：本批**可选**。若写，须逐杆来自序列实测；写不出就留空并在交付说明列为待办。

**完成标准**：

- [ ] 逐条给出「animation 字段 → 序列文件某杆某字段」的对照表（6 条 drill 全覆盖），每个数值可溯源。
- [ ] `make verify-gate` → 总计 FAIL 0（I10 须仍通过 84+）。
- [ ] `make thumbnails` 成功；`git status QiuJi/Resources/DrillThumbnails/` 显示 6 条新 drill 的 PNG 已生成/更新；目视抽查至少 2 张与序列开局盘面一致。
- [ ] `make build` → `BUILD SUCCEEDED`（附日志路径）。
- [ ] 聚焦测试 `DrillContentValidationTests` + `DrillListViewModelTests` 对照 W0 基线**无新增失败**（`pocket` 空串类既有失败可仍在，⛔ 不得修绿）。

**不做**：写精讲；改 `sets`；碰 c042 存量债。

---

### W2 / W3 / W4 — 精讲撰写（走位链 + 独立阶梯）

**统一前置**：严格按 `tutorial-authoring` 技能六步走，⛔ 禁止跳步。逐批必须：

1. 第 0a 步**逐张读图**（用 Read 直接看 PNG，⛔ 不是 `ls` 确认存在）+ 图健康检查四项；
2. 第 0b 步产出**意图卡四问**并回显。⚠️ c079–c084 **均无 `content/drill_profiles/`**，问 2（球形设计意图）必须**声明降级**，⛔ 禁止编造「设计者当初为什么这么摆」（意图红线 1）；
3. `python3 scripts/tutorial_digest.py "content/position_play/sequences/<完整文件名>.json"` 拿事实清单，**所有几何断言只准抄它**；
4. 第 2 步红灯检查（scratch / 某杆未进袋）→ 命中就停下报告，⛔ 不硬写；
5. **形态判定**（DR-062）后再动笔；
6. 写完逐条回显自查清单 15 条。

| 批 | drill | 预判形态（执行者须用事实清单**客观复核**，不得照抄本栏） |
|---|---|---|
| W2 | c079（4 杆）、c081（4 杆） | 走位链 —— 每杆落点因果决定下一杆；须**指认一个胜负手**并附客观依据（容差最小 / 达标最难环节） |
| W3 | c080（4 杆） | 元数据写「同一球形下多线路选择、球位与袋口固定」，⚠️ 可能是**独立阶梯**（单变量=线路），按判据复核 |
| W3 | c082（12 杆） | 蛇彩清台 → 走位链；12 杆⛔不得逐杆平均用力，重点分配须体现胜负手 |
| W4 | c083（8 杆）、c084（8 杆） | 独立阶梯（单变量 = 切球厚薄）→ **混合式结构**：`技术原理` 一次讲透变量；`开局与击球顺序` 附阶梯总表；仅锚点档（2–4 杆）写完整三条目，中间档只写一条 `自检`。⛔ 阶梯型禁用「胜负手/关键档」措辞 |

**每批完成标准**：

- [ ] 意图卡四问已回显；问 2 已声明「无变量档案，球形设计意图未溯源」。
- [ ] 形态判定结论 + 客观判据已回显；走位链附胜负手依据，独立阶梯附锚点档选择依据。
- [ ] 读图结果如实列出（图健康检查四项逐项过；发现坏图/图文矛盾必须上报而非照写）。
- [ ] `coachingPoints` 逐条给出在正文中的落点位置，无遗漏。
- [ ] 每杆 `params` 与事实清单原始 `spinX`/`spinY`/`velocity` **逐字符一致**（⛔ 不四舍五入、⛔ 不抄百分比读数）。
- [ ] 语域检查：⛔ 正文无内部词汇（退役/profile/序列文件/manual01/digest/出片/录制/W 批次号）、⛔ 无定理编号或「法则告诉我们」句式、⛔ `怎么打` 无 spin 数值或百分比；力度/杆法以定性词为主、数值只进括号或 `params`（DR-063）。
- [ ] 方位/袋口逐字来自事实清单，为**屏幕系**中文名（左上角袋 / 右上角袋 / 左侧中袋 / 右侧中袋 / 左下角袋 / 右下角袋）；⛔ 不出现旧横版袋口名。
- [ ] section 顺序与「应用课骨架」表完全一致；逐杆节 `content == ""`。
- [ ] `image` 指向**已确认存在**的文件（`manual01` token）；缺图⛔不虚构文件名，列入交付说明。
- [ ] 写回后 `python3 -c "import json; json.load(open('<file>'))"` 通过。
- [ ] `make verify-gate` → 总计 FAIL 0（**C4 此时开始按杆数强校验**：c079/c080/c081 各 4 节、c082 12 节、c083/c084 各 8 节）。
- [ ] 自查清单 15 条逐条 ✅/❌ 回显，有 ❌ 先修复。

**不做**：改 `animation` / `sets` / `category`；改序列文件；给 `theoremIds` 等未进 schema 的字段。

---

### W5 — c060 精讲重写 + 全轮收尾

**范围**：

1. c060 精讲**从 `singleShot` 4 节重写为 8 个逐杆节**（合并后序列 8 杆）：
   - 节标题按 **D-v33-3** 折中式：`第一杆：<场面名>`……`第八杆：<场面名>`。场面名须来自该杆实测盘面特征，⛔ 不编造袋口。
   - `tutorialKind` 改 `multiShot`；用 `tutorial.sections` 平铺，⛔ 不用 `formations`。
   - 按 **D-v33-2** 如实说明进袋的 4 杆（原球形2 第 1/2/3 杆、原球形1 第 4 杆）：防守判据是留下什么局面，进球是顺带结果。
   - 8 杆盘面互相独立（§2.1），逐杆节按「独立阶梯」精神分配篇幅，⛔ 不写成连贯走位链、⛔ 不指认胜负手。
   - 旧 4 节正文若有可保真复用的内容，按 `tutorial-migration` 技能处理（去伪几何断言后重写），⛔ 不直接丢弃也不原样照搬。
2. 全轮收尾回写：
   - `tasks/PROGRESS.md`：本轮 7 条 drill 状态、变更文件、下一步。
   - `/Users/song/projects/project-hub/projects/13.billiard_trainer.md`：最近完成 + 下一步 + 决策记录（D-v33-1～3）。
   - `tasks/IMPLEMENTATION-LOG.md`：若出现 DR/PD/FL 按 `00-orchestrator.mdc` § 实施知识回写流程处理（含回写到目标 `.mdc` / `SKILL.md` + Changelog）。
   - 本文件批次状态 → ✅，追加版本记录。

**完成标准**：

- [ ] c060 精讲 8 节，标题命中 `^第.+杆`（可用 `python3 -c` 逐节正则自检）；C4 对 c060 从 warn「legacy 待迁移」转为**通过**。
- [ ] `make verify-gate` → 总计 FAIL 0。
- [ ] `make build` → `BUILD SUCCEEDED`。
- [ ] 聚焦测试对照 W0 基线无新增失败。
- [ ] 模拟器目视：动作库 83 条、7 条新 drill 的缩略图与详情页精讲正常渲染（截图落 `build/v33-screenshots/`）。
- [ ] PROGRESS / hub 卡 / 本文件均已回写。

---

## 七 不做清单与遗留

- ⛔ **改 `sets` / 新增 `secondaryCategories`** —— 属 v31（R2 / R1 / I6），本轮碰即冲突。
- ⛔ **改 `SHOT_SECTION_RE`（`verify_tutorial_sync.py:95`）或 `isShotStructureTitle`（`DrillTutorialView.swift:157`）** —— D-v33-3 已裁定折中，横切统一另立批次。
- ⛔ **修 §8.16 的 6 条 `animation.pocket` 空串** —— 需人工判定各自正确袋口，契约已登记另立批次；本轮 c060 的 `pocket` 仍为空串（8 杆中 7 杆 freeAim、仅第五杆有指定袋口，drill 级无单一正确取值，属真实取值缺失而非疏漏；见 L11）。
- ⛔ **修 c042 存量债**（`DrillBoardBuilderTests` / `DrillTryoutBoardStoreTests`）。
- ⛔ **宣称全量 `QiuJiTests` 绿**。
- ⛔ **从 `shotIntent` 反推序列**（契约 §1.2 红线 2）；⛔ **AI 生成球形坐标**（红线 1）。
- **遗留 L1**：`drill_c066` 已录 0 杆空序列（仅摆球无击打），渲不出任何视频/静帧，无法写逐杆精讲。W0 已把它从 I9 豁免移除（因技术上「有序列」），但它**仍无可出片内容**。待用户决定：补录击打 / 只作开局盘面 / 恢复豁免。⚠️ W0 执行者若发现移除 c066 豁免导致任何检查 FAIL，**改为保留 c066 豁免并如实上报**，⛔ 不得为凑绿而伪造内容。
- **遗留 L6（W3 发现，⚠️ 需用户裁定）**：**元数据与实测录制不符**，按契约 §1.1 推论 4 应「改元数据不改序列」，但本轮禁改 `description`/`standardCriteria`（属 v31），故仅上报：
  1. `c082` 写「按顺序逐颗清掉，**最后打 8 号**」——实测是**彩球与 8 号交替**，8 号被打进并复位 **6 次**（12 杆 = 6 个来回）。「8 号始终在台上」这半句正确，「最后打 8 号」不正确。
  2. `c082` 教学要点「8 号会在哪一步变成障碍」——实测 8 号是每隔一杆的固定目标，不是障碍球。精讲正文已如实改写为「既是目标、也是走位时必须一直算进去的那颗球」。
  3. `c080` 写「从**高杆**定点校位起手建立基准」——实测第一杆是**中低杆**（低 28%）。正文保留「先立基准」的练法纪律，并如实说明示范起手用中低杆。
  4. `c080` 名为「上下半台走位」，但四杆落点为 上路左半 / 中路右半 / 中路左半 / 中路右半，**只有 1 次落上路、0 次落下路**。正文照抄清单区域名，未编造「送到下半台」。
- **遗留 L9（W4 发现，⚠️ 需用户裁定）**：`c084` 的 `description` 与 `coachingPoints` 2/3 把变量写作「不同**厚度** / 厚薄」，实测八杆**厚薄恒定**（切角恒 10°、母球与目标球每杆复位），真正在扫的是**打点高低**（`spinY` +0.050 → −0.240 单调递减、`spinX` 全 0）。c083 同理（切角恒 7°、`spinY` +0.120 → −0.482）。按契约 §1.1 推论 4 应「改元数据不改序列」，本轮禁改故仅上报。精讲已保留「单变量纪律」原则并如实说明锁死的是厚薄。
- **遗留 L10（W4 发现）**：`c083` 的 `description` 写「母球**先吃一库，再去碰目标球**」，措辞歧义——若「目标球」指参考球则与实测自洽；若指 8 号球则与实测相反（实测顺序为 撞 8 号 → 吃底库 → 碰参考球）。建议随 L9 一并明确。
- **遗留 L8 部分销账（W4）**：W4 用 `ffmpeg lagfun` 从 `full.mp4` 合成运动轨迹叠影图，核实了 c083 四档吃库形态与 c084 全程不触库，据此写出主题层区分（逐杆库数仍未断言）。该方法可复用于补写 c080/c082 的库数表述。⚠️ 注意本轮 7 条序列的导出目录**只有 `full.mp4`、没有 `sNN.mp4`**，逐杆时间戳需自行定位。
- **遗留 L7（W3 发现）**：`tutorial_digest.py` 的「开局布局 母球」读序列 `initial` 占位值（如 c080/c079 均为 `0.300/0.300`），与第一杆 `before` 及 `initial.png` 均不符。W2/W3 已回避该行改用第一杆 `before`，但脚本本身仍会误导后续写作者，建议修脚本或修序列 `initial` 字段。
- **遗留 L8（W3 发现）**：c080/c082 精讲**一律未断言吃几库**。原因：仅有静帧，部分杆次（如 c080 s02）虚线无法可靠数出库点，技能要求吃库表述须核对出片视频。已改用可溯源的「母球接触后移动距离」与「是否穿越纵向中线」替代。若后续核对 `sNN.mp4` 可补入库数表述。
- **遗留 L3（W1 发现）**：`.kiro/steering/table-geometry.md` §Canvas 归一化袋口坐标 的两个**中袋**行与代码常量派生值不符（doc `±0.5268` vs code `±0.5209`，差 0.0059）；同文档 SceneKit 表的中袋 Z=±0.688 反算得 0.5209，判断文档笔误。本轮 6 条 drill 只用角袋（角袋 doc/code 完全一致），故未受影响；W1 未改文档，待另立条目修正真源。
- **遗留 L4（W1 发现）**：`make thumbnails` 是全 index 全量烘焙（无单条选项），W1 顺带改动 19 张与本批无关的 PNG（15 张字节差 ≤5 属抗锯齿噪声；`c059/c060/c061/c073` 有实质变化，其中 c060 属 W0 合并序列后的正当重烘）。⛔ 不手工回滚（会让 PNG 与内容不同步）。待用户决定提交时是否剥离。
- **遗留 L5（W1 发现）**：c079–c084 的 `shotIntent` 仍为空（W1 可选项未做）。缩略图/试打页已全走 `boardSequence` 真源不依赖它。建议与 v31 定 `sets` 口径时合批。
- **遗留 L2 结论（W5 实测比对，⚠️ 需用户裁定）**：`新增训练动作.md` 5 条防守要点 vs 合并后 8 个场面**不是 1:1，且覆盖不全**。
  1. 要点 1「打进自己的球、踢死对手的球」→ 第四、五杆可对上（有球落袋且另一颗球被推离原位）✅。
  2. 要点 3「借对手球把目标球打进，同时踢死对手球」→ 第六杆形态吻合（推近处一颗过去、贴库那颗落袋）✅。
  3. 要点 5「轻推袋角防守」→ 第六、七杆是全组最轻的两杆（0.9 m/s）且各有一颗球落袋，方向吻合 ✅；但「袋角」是否成立需袋口判定，序列无 `pocket` 取值，⛔ 未断言。
  4. 要点 2「母球藏在自己球后面」、要点 4「吃库踢死对手球 + 己方球留袋口 + 母球贴住自己球」→ **无法证实**：判定它们需要**遮挡关系**与**吃库库数**，事实清单两者都不提供（前者无遮挡字段，后者须核对视频且本轮导出目录只有 `full.mp4`）。第一、二、三、八杆四个场面均无球落袋、也无可溯源的遮挡/库数依据，**未对应到任何一条原始要点**。
  5. 处置：W5 精讲按**实测的 8 个场面**写，⛔ 未按 5 条要点反向编造情节；「藏球/贴住自己球/吃库踢球」这三类做法只在 `技术原理` 与 `常见错误` 里作为**原则**出现，未指认到具体某一杆。若用户要求 8 个场面逐一兑现 5 条要点，须**补录**对应盘面，或改写 `新增训练动作.md` 的要点口径。
- **遗留 L11（W5 发现，⚠️ 已影响本文件事实）**：§2.1 与 W5 派发说明称「全部 8 杆 `targetKey` 与 `pocket` 均为空串」，**实测不成立**——第五杆 `targetKey="_1"`、`pocket="topCenter"`（左侧中袋），其余 7 杆才是 freeAim。精讲已按实测写（第五杆是唯一有指定目标与袋口的一杆），本行留作口径更正。附带影响：`tutorial_digest.py` 的「形态判定」与「击打顺序与袋口」两行是**基于这唯一一杆**给出的，对另外七杆无意义（已记 PD-029）。

---

## 八 锚点索引（2026-08-09 读码/实测核实）

| 锚点 | 路径 | 用途 |
|---|---|---|
| 序列真源目录 | `content/position_play/sequences/` | E1；唯一球形几何真源（契约 §1.1） |
| C4 结构对齐 | `scripts/verify_tutorial_sync.py:330-348` | 精讲球形数/杆数直接比序列；`sum(shots)==0` → warn「legacy 待迁移」 |
| 逐杆节识别正则 | `scripts/verify_tutorial_sync.py:95` `SHOT_SECTION_RE = ^第.+杆` | D-v33-3 依据；⛔ 本轮不改 |
| I9 豁免逻辑 | `scripts/verify_tutorial_sync.py:483-508` | 有序列 + 在豁免名单 → warn「豁免已可解除」；E3 依据 |
| I10 模型镜像 | `scripts/verify_tutorial_sync.py:511+` `MODEL_SPEC` | 改 Swift `Codable` 模型须同步改它 |
| 棘轮基线真源 | `scripts/content_invariant_baselines.json` | E3：`i9_no_sequence_exempt` 10 → 8 |
| App 主结构识别 | `QiuJi/Features/DrillLibrary/Views/DrillTutorialView.swift:157-181` | `hasPrefix("第") && contains("杆")`；编号解析取「第」→ 首个「杆」之间 |
| 条目标签配色 | 同文件 `:149-154` | label 必须一字不差 `为什么` / `怎么打` / `自检` |
| 管线命令 | `scripts/Makefile:150`(position-export) `:203`(thumbnails) `:231`(tryout-sync) `:245`(verify-tutorials) `:258`(verify-gate) | W0 / W1 |
| 回填脚本 | `scripts/import-engine-export-to-app.py` | `--skip-json` 已默认 True（v29 W8），⛔ 勿传 `--no-skip-json` |
| 精讲样板 | `QiuJi/Resources/Drills/positioning/drill_c042.json` | W2 写前先读，对齐语感 |
| 无「序列→animation」脚本 | `rg animation scripts/` 仅命中 verify / audit / v21 一次性脚本 | W1 是人工活，须逐值溯源 |
| 测试计数断言 | `QiuJiTests/DrillContentValidationTests.swift`、`QiuJiTests/DrillListViewModelTests.swift` | 83 条，已同步；⛔ 本轮无需再改 |

---

## 九 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v33.1 | 2026-08-09 | **W0–W5 全部 ✅，本轮收官。** 7 条 drill 落地：c079/c080/c081/c082/c083/c084 `animation` 按序列重标 + 缩略图烘焙（W1），c079/c081（W2）、c080/c082（W3）、c083/c084（W4）、c060（W5）精讲全部写完。C4 结构对齐由立档时的 66 通过 / 11 待迁移推进到 **73 通过 / 10 待迁移 / 不一致 0**，verify-gate 总计 FAIL 0，`make build` SUCCEEDED，聚焦测试无新增失败（c060 `pocket ''` 既有失败按 §七 保留未修绿）。**口径更正 L11**：§2.1「8 杆全 freeAim」不成立，第五杆有 `targetKey=_1` / `pocket=topCenter`（左侧中袋）。**新增遗留**：L2 已给出比对结论（5 条要点 vs 8 场面覆盖不全，要点 2/4 无法证实，需用户裁定补录或改口径）、L11 口径更正。**新增 PD-029**（digest 派生字段 `initial` / 形态判定 / 袋口行不可直接采信，已回写 `tutorial-authoring` SKILL v1.1）。**未验证项**：模拟器仅取到启动首屏截图，动作库列表与详情页精讲的**人工目视导航未完成**（`idb` 环境损坏、System Events 无辅助功能权限），如实标记未验证。**下一步**：v31（`sets` / 副分类 / 训练量口径，须把这 7 条纳入范围）；L1 c066 空序列、L6 / L9 / L10 元数据与实测不符、L2 / L11 待用户裁定。 |
| v33.0 | 2026-08-09 | 首版：按 `issue-collection-restructure` 立档。用户已拍 R1–R4 与 D-v33-1～3（c060 合并为单序列 8 杆 / 进袋 4 杆保留并如实说明 / 节标题折中式且不改两处正则）。模型路由定稿：W0 grok-4.5 快，W1–W5 opus 5.0。门禁基线实测（总计 FAIL 0；I9 已因录制转绿，c060+c066 豁免待解除；I8 提示 9 条待 `tryout-sync`）。排期声明 v33 先行于 v31，v31 须把本轮 7 条纳入剂量/分类范围。 |
