# 问题集合 v60：六袋口皮革选中效果与全场景统一

> **本文件是本轮需求真源。** 日期：2026-09-10；版本：v60.3；状态：W0–W4已完成，本地代码与模拟器验收通过。
> 用户完成计划审阅后认可暖金/琥珀色方向，并于2026-09-10明确「开始执行」。按本方案逐批实施验收。
> 本文件仅覆盖袋口皮革及其选择反馈，不替代 v59 理论瞄准点训练、v58 页面审核等专题。后续 v60.x 覆盖本专题低版本。
> 排期前置：刷新源码指纹和 dirty 文件基线；尤其 `AngleSceneView`、`AimPointSceneTrainingView`、`PositionPlayViewModel`、`BankShotViewModel` 等已有其他任务修改。保留原工作，不整文件回退；保留 `output/`、`tmp/`。v59 的瞄准点训练与本专题共享文件，合入前须检查最新 API、避免同时编辑同一文件。

## 1. 需求回显

### 用户已明确的方向

1. 当前选袋用红色阴影覆盖袋口，视觉不美观。
2. 使用球桌原有的六处皮革作为可独立控制的部件，选中反馈作用于皮革表面。
3. 应用到**所有相关场景**，不能只改每日清台，也不能只改共享 API 而忽略独立实现与状态残留。
4. 先整理计划（已完成），随后用户认可暖金/琥珀色并授权实施全部相关场景。

### 视觉方向与W0原型决策

- 单袋选中建议采用克制的暖金/琥珀提亮（优先复用合适的色彩 Token），皮纹、粗糙度和袋口立体轮廓仍可辨；不做持续脉冲、大范围辉光或覆盖袋洞的实心层。
- 未选中、不可行袋保持原皮革；选中表示目标身份，不用同一视觉暗示“保证可进”。不新增求解状态配色体系。
- “打一走二想三”必须保留①/②目标袋身份；具体材质配色及同袋双角色表现由 W0 的真实模型原型确定，不能一律染成同一种金色。
- W0已用真实SceneKit确定深色btAccent线性混合65%和同袋绿/青分区；细节见 `tasks/pocket-leather-W0-20260910.md`。W1–W4仍须在最终App各页面验证，不以原型替代全场景验收。

## 2. 已核实事实与待验证风险

核实日期：2026-09-10，依据当前工作区。

| 事实/风险 | 完善方向 |
|---|---|
| `AngleTrainingScene.addPocketMarkers` 创建六个 `SCNPlane` 圆盘；红色半透明、关闭深度读写、`renderingOrder=1000` | 替换可见载体；不继续靠提高层级、放大半径或抬高圆盘掩盖问题 |
| `setPocketHighlight` 只切 `isHidden`，`.viable/.infeasible` 均隐藏 | 保留现有语义；需要明确原皮革/选中皮革显隐互斥，避免叠面闪烁 |
| 圆盘实现有 iOS 17 渲染线程并发修改材质导致 `C3DSceneLock` 崩溃的历史注释 | 材质在进入 renderer 前准备；运行期安全切换，不重新引入逐次原位改共享材质 |
| `TableModelLoader` 有进程级模型缓存，克隆后复制 geometry/material；加载时做坐标转换、缩放、居中 | 提取/映射要与实际模型变换一致；缓存与各场景材质隔离、首次耗时和内存均需验证 |
| 已提取 `output/pocket-leather-20260910/pocket-leather.blend`：6网格、12304皮革面、6套UV，重新打开验证通过 | 是可用研究产物，尚不是 App 可直接加载的最终资产；不能把 Blender 编号直接当 App pocket index |
| App 模型源 `QiuJi/Resources/TaiQiuZhuo.usdz` 的 SHA-256 为 `617bcf941e3a153965de63fff27150b1c02504a433da12a8a587c902636c851b` | W0 重新校验；提取脚本、映射、最终产物均绑定源模型指纹，避免模型升级后静默错袋 |
| `AngleSceneView.handleTap` 先选球，再按 `pocketMarker_` 名命中，再按投影中心作44pt邻域兜底 | 皮革命中形状与袋洞不同；未选袋仍须可点，命名/父子命中和球/袋优先级一起验 |
| “打一走二想三”另外使用 `drawPocketRing` 画两种角色色的圆环 | 必须单独适配，不会因替换红盘自动完成 |
| `PositionPlayViewModel.clearTable`、`SiluTrainerViewModel.clearTable/startBreakFlow` 有索引复位而未直接刷新袋口高亮的路径 | 存在残留风险；W2 先复现与追踪所有间接调用，再修统一状态出口，不把静态判断冒充已复现 |
| `hideAllVisualization` 与袋口高亮是不同生命周期 | 不把普通轨迹重绘的“清辅助线”误解为“清选袋”；清桌/模式切换则不能只清轨迹漏选袋 |
| 反射解球、防守页调用 marker 创建，但没有当前目标袋高亮调用 | 列为“中性回归”，不能因为有球桌就擅自新增选袋模式 |
| 动作详情实时示意、离线缩略图和视频导出使用相关球桌/轨迹层，但当前未找到共用选袋高亮调用 | 必须纳入模型回归审计；已有非交互展示不自动添加选袋状态、不批量重烘焙素材 |

根因判断：主要问题是可见反馈使用覆盖袋洞的屏幕优先圆盘；完整改进还涉及“目标身份 → 六个皮革部件”的映射、状态生命周期及独立双色实现。不能把任务缩成一处颜色修改。

## 3. 场景覆盖台账

状态含义：**迁移**＝已有目标袋反馈需接入；**专项**＝有独立语义；**中性回归**＝原本没有选择反馈，验证保持自然；**入口继承**＝共用实现但要验证入口状态。每行必须在执行时填写结果/证据；不适用要记录理由。

| ID | 页面/入口 | 已核实实现链 | 本轮范围与验收重点 |
|---|---|---|---|
| S01 | 每日清台 | `MainTabView` / `RootView` → `FreePlayView(.dailyClearance)` → `PositionPlayViewModel` | 迁移；新局/继续局、不同玩法、手动换袋、重开、清台结束及重播 |
| S02 | 自由击球 | `FreePlayView(.standard)` → `PositionPlayViewModel` | 迁移；进袋/自由/开球切换、取消开球、重打、回放 |
| S03 | 自由走位/编排台 | `PositionPlayComposerView` → `PositionPlayViewModel` | 迁移；选球换袋、删球、清桌、录制/编辑序列、撤回 |
| S04 | 动作详情→上手试打 | `DrillDetailView` → `PositionPlayComposerView(sourceDrill:tryoutFormation:)` | 入口继承；默认自由模式不冒出选袋；切进袋后正确高亮，重摆恢复 |
| S05 | 试打中的逐杆演示 | `PositionPlayViewModel.playSequence` / `restoreShotParams` | 迁移；每杆目标袋、自由杆无目标袋、暂停/继续/上一杆/播完恢复，无跨杆残留 |
| S06 | 分离角与走位 | `ShotSimulationView` → `PositionPlayViewModel` | 迁移；默认教学球形、换袋、换模式、重置与回放 |
| S07 | 思路训练 | `SiluTrainerView` → `SiluTrainerViewModel` | 迁移；约束/求解重绘不丢目标，清桌/无目标/开球清除，恢复上一杆正确 |
| S08 | 2D 瞄准训练 | `SceneAimingView(.topDown2DRotated)` → `AimingQuizViewModel` | 迁移；固定题目袋、换题、结果/重试，不能误开放自由选袋 |
| S09 | 3D 瞄准训练 | `SceneAimingView(.perspective3D)` → 同上 | 迁移；透视、近远袋、遮挡、studio管线适用情况实测 |
| S10 | 2D 瞄准点训练 | `AimPointSceneTrainingView(.topDown2DRotated)` → `AimPointSceneQuizViewModel` | 迁移；题目袋、理论反馈/示范/换题；与v59最新流程衔接 |
| S11 | 3D 瞄准点训练 | 同一 View，`.perspective3D` | 迁移；角袋/中袋、透视观察、示范前后，保留特写与相机手势 |
| S12 | 角度与打点 | `AngleDynamicView` → `AngleDynamicViewModel` | 迁移；手动选袋、自动最佳袋、可行/不可行切换 |
| S13 | 分离角图谱 | `SeparationAngleAtlasView` → `SeparationAngleAtlasViewModel` | 迁移；预设/球形切换、手选袋、参数刷新 |
| S14 | 加塞吃库图谱 | `CushionEnglishAtlasView` → `CushionEnglishAtlasViewModel` | 迁移；同上，保留该页杆法约束与轨迹 |
| S15 | 翻袋解球器 | `BankShotView` → `BankShotViewModel` | 迁移；指定袋、换解/无解、自由调整、重置、撤回/回放；更新“高亮圈”帮助文案 |
| S16 | 打一走二想三 | `PlanThreeView` → `PlanThreeViewModel.refreshOverlays/drawPocketRing` | 专项；①/②不同袋、同袋、未选齐、当前角色、播放/推进/清空/恢复 |
| S17 | 批量制作中的编排 | `BatchAuthoringView` → `PositionPlayViewModel` | 入口继承；仅模拟器功能，选袋/求解回填/录制；检查时不得写正式内容资产 |
| S18 | 拍照建球形→编排/思路/打三 | `BallExtractionView` 三个目标 sheet | 入口继承；载入外部球形后目标与袋状态重建，空/仅母球路径无残留；不新增识别能力 |
| S19 | 反射解球器 | `DiamondSystemViewModel` | 中性回归；无指定进袋目标，不新增皮革选中，切页不继承其他场景材质 |
| S20 | 防守/斯诺克战术 | `SnookerTacticsViewModel` | 中性回归；同上，保留防守目标语义 |
| S21 | 动作详情/训练实时球台示意 | `DrillSceneController` / `DrillSceneView` | 中性回归；静态/播放/切球形后模型完整，独立实例不串色；不凭 `shot.pocket` 自动新增效果 |
| S22 | 缩略图/分享球台/视频导出/静态教学插图 | `DrillThumbnailRenderer`、`TableFigureRenderer`、`SequenceVideoExporter`、`BTAimTableView/BTPocketMark` | 审计真实调用链并作代表性中性回归；当前无此红盘选择实现，保持既有展示，禁止批量改历史图片/视频 |

**防遗漏要求：** 目前检索到11处 `addPocketMarkers()` 调用、8处控制器/VM直接使用高亮 API，以及 `PlanThreeViewModel` 的独立圆环。该数字是本次快照，不是最终硬编码门禁。执行前重搜调用方、`pocketMarkers` 直接显隐、`drawPocket*`、`targetPocket`、`selectedPocket`、中文“袋口/目标袋/高亮圈”及真实路由；新增命中必须落到台账，不允许只检查这张静态表。历史注释中的 `Scene2DAimingView/Scene3DAimingView` 不是当前页面名，当前真入口是 `SceneAimingView`。

## 4. 共享设计与技术约束

### 4.1 皮革载体与坐标

- 仅处理真实 `Leather` 表面；袋洞、袋网/金属圈、木库、台呢、球、球杆均不是本次选中载体。
- 原位置、尺度、法线、UV及有效面保持；面片存在性用渲染及引用面验证，不能用包围盒/顶点数代替。
- 方案候选：①离线导出最小皮革资产并复用原模型变换；②在加载原型阶段按真实材质/面片拆分并缓存。W0比较坐标保真、法线/色彩、重复几何、包体、加载时间及SceneKit兼容性后确定一路，后续不同时维护两套运行实现。
- 若采用追加皮革节点，原皮革对应面必须可控，保证正常/选中两态不重叠闪烁；禁止关闭深度检测让球杆、球或后侧袋口穿透显示。
- 拆分结果无缝回到原台桌；不移动皮革来追随物理袋心，不通过视觉提取反推物理参数。
- SceneKit：X–Z水平、Y向上、单位米；USDZ原始Z-up通过 `TableModelLoader` 变换。渲染部件绑定按实际变换核验，索引以 `AngleSceneCalculator.pocketPositions` / `ShotIntent` 代码契约为准。
- CAD袋心与 `pocketMarkerPositions` 的视觉偏移用途不同；物理、瞄准、进袋轨迹和检测半径不随高亮改动。旧文档方位名与代码存在历史差异，不按中文名或Blender 01–06顺序猜对应关系。
- 缺资产/缺材质/映射不全要有可诊断错误；完整保留原桌，禁止留下缺皮革或悄悄退回红色遮罩并宣称达标。

### 4.2 状态模型与生命周期

共享层至少支持：无选择、单袋选择、打三角色选择，以及清除/恢复；接口的最终签名由W1明确，兼容现有调用方逐批迁移。

| 触发 | 必须保证的行为 |
|---|---|
| 初次进入/再次进入 | 以当前页面目标为准；无目标不高亮，不继承前页 |
| 换袋/自动选最佳袋/切题 | 同一更新内恢复旧皮革、显示新皮革；单目标最多亮一袋 |
| 自由模式、清桌、无有效目标、进入开球 | 不显示过期的目标袋；取消开球按原页面状态恢复 |
| 普通求解/拖球/调参数重绘 | 不因清轨迹而闪退或丢失仍有效的目标袋 |
| 击球/暂停/回放/上一杆 | 根据该页当前杆和恢复快照驱动；不把播放临时状态写成下一杆选择 |
| 逐杆演示中的freeAim/无pocket杆 | 清除上一杆目标，不推测进袋去向充当显式选择 |
| 清台完成/退页/取消异步任务 | 清除失效状态；过期回调不能重亮旧袋 |
| 多场景共存/缓存复用 | 材质、选择、控制节点隔离，不互相染色 |
| 反复 setup/add markers | 幂等或显式重建并清理旧节点，不累加一套套高亮 |

### 4.3 打三双角色

- ①/②不同袋：两袋身份与球角色对应，不能丢失原双色含义。
- ①/②同袋：不能由最后一次调用覆盖前一角色；W0比较“皮革双角色分区”或“当前角色强调＋邻近①②身份标记”等真实模型方案，选定一种再实施。不得遮挡袋洞，标签不得与球/HUD混淆。
- 当前armedRole、缺省/不完整计划、推进后二号变一号、清除计划、播放时隐藏与恢复均要有明确断言。
- 新皮革效果完成后移除旧袋口角色环的重复绘制；球的角色环和停球扇形不属于本次替换范围。

### 4.4 点击、文案与可访问性

- 点击未选皮革和袋洞都能选择；原44pt近邻兜底保持可用，需验证缩放/投影深度/袋口密集时不会选到屏幕后方或不可见袋。
- 保留选球优先、自由瞄准点台面、固定题目禁止自行换目标袋等原手势语义；若需调整袋/球抢点，先以实际重叠场景证据定位。
- 检查原`pocketMarker_`命名解析是否仍适用于父子节点；视觉节点与交互命中无需强绑为同一表面。
- 找出可见帮助文案“红色阴影/红圈/高亮圈”等并与新表现对齐；不批量改与袋口选择无关的“绿环/角色环”。
- 若已有袋口AX元素，名称、选中值与新状态一致；若只有3D点按，评估提供同等六袋选择语义，不能用增加可见文字按钮破坏当前布局。VoiceOver真实操作不能由AX树检查冒充。

## 5. 批次安排

| 批次 | 范围 | 量级 | 依赖 | 主要产物 |
|---|---|---|---|---|
| W0 ✅ 2026-09-10 | 场景台账复核、改前基线、六袋映射、单袋/双角色真实原型、技术选型 | 中，1会话 | 当前计划 | 映射/资产报告、真实预览、最终视觉与运行方案 |
| W1 ✅ 2026-09-10 | 共享皮革渲染与命中接入，缓存隔离、错误诊断、幂等及状态API | 中–大，1会话 | W0 | 共享组件、代表页试接、几何/渲染/交互测试 |
| W2 ✅ 2026-09-10 | 共用单目标场景与所有入口状态闭环，清桌/开球/逐杆/恢复残留修复 | 中–大，1会话 | W1 ✅ 2026-09-10 | S01–S15、S17–S18逐行覆盖记录 |
| W3 ✅ 2026-09-10 | 打一走二想三独立双色角色迁移、同袋语义与生命周期 | 中，1会话 | W0、W1 | S16专项实现、双色/同袋/推进与恢复证据 |
| W4 ✅ 2026-09-10 | 全调用链复审、跨设备/Runtime回归、中性场景与最终交付 | 中，1会话 | W2、W3 | 完整覆盖台账、视觉报告、验证摘要与剩余边界 |

依赖：`W0 → W1 → {W2, W3} → W4`。建议顺序W0/W1/W2/W3/W4；W2、W3逻辑独立但共用场景API，默认串行，不自动创建子智能体或并行修改同一工作区。

### W0 完成标准

- [x] 重新核实S01–S22；新增调用/入口补表，不适用项附代码理由。
- [x] 标准手机获取每日清台、一个3D训练、打三双角色的真实改前截图；固定球形/外观/字号。此前用户截图可作问题参考，不能替代当前版本全部基线。
- [x] 对六袋建立 `pocketIndex ↔ 模型部件 ↔ portrait/landscape屏幕位置` 映射，至少角袋/中袋/六袋全亮检查；保存源指纹及有效面/UV/法线信息。
- [x] 单袋皮革、打三不同袋/同袋原型在真实SceneKit渲染核验；确定颜色与双角色表达，记录选择依据，未通过则留W0继续优化。
- [x] 技术选型明确；验证原皮革恢复一致、遮挡正常、无颜色空间漂移、无重叠闪烁，记录首次/热加载成本与产物大小。
- [x] 若试接代码或导出资源，运行适用构建/加载验证；Blender验证不计为SceneKit兼容性通过。

### W1 完成标准

- [x] 六袋均可单独显示/恢复，材质和节点实例不共享可变选中态；重复初始化不累加。
- [x] 真实模型面与材质读取失败可诊断；不丢原皮革、不静默冒充成功。
- [x] 几何不变量：完整六袋、一一对应、只含皮革面、正常/选中互斥、目标索引不变；不修改物理袋心/半径。
- [x] 代表2D/3D页所有六袋依次选择，旧袋复原；未选袋仍能点击，袋洞与皮革点击一致，球/台面原交互可用。
- [x] plain与enhanced两种管线真实渲染；最低支持iOS 17至少真实渲染一帧和实际进页，检查渲染崩溃与透明排序。
- [x] `make -f scripts/Makefile build` 及聚焦测试通过，原图已查看，保留改前/改后证据。

### W2 完成标准

- [x] 台账所列单目标页面及变体逐行标明继承/修改位置、验证方法与证据。
- [x] 清桌、无目标、目标球删除/落袋、自由/进袋/开球切换、取消/重开、回放/重打/离页均验证；发现残留先复现后修。
- [x] 至少一条含不同目标袋和freeAim杆的现有序列覆盖暂停/继续/上一杆/结束恢复，不改写正式JSON造测试数据。
- [x] 每日清台新局/继续/完成状态与现有支持玩法不被选袋反馈改变；试打/外部球形载入重新建立正确选择。
- [x] 2D/3D固定题目页保持同一题目标与成绩流程，v59接口若已变化使用最新版本验证。
- [x] 对已确认残留补行为测试；通过构建、对应VM/状态测试和真实页面操作截图，更新帮助文案并复看最终文案。

### W3 完成标准

- [x] ①/②不同袋、同袋、只设一袋、清空两袋、切角色、两球残局/一球残局全部可辨。
- [x] 求解/换解/击球/推进后二号转一号/上一杆/回放恢复，与角色模型一致。
- [x] 不再重复绘制旧袋环；球角色环/扇形/约束未被误删。
- [x] 覆盖同袋覆盖冲突与播放恢复的行为测试；标准手机和iPad实际页面原图可辨，构建及聚焦检查通过。

### W4 完成标准

- [x] 对最终代码重跑全仓检索，全部生产调用及独立袋口绘制均有台账处置；旧红盘实现不再作为正常选中载体，注释/帮助同步。
- [x] 每个迁移页面至少实际进入一次；共用实现的深度状态测试按等价类合并，但不能只测每日清台后声称全场景通过。
- [x] 标准iPhone当前Runtime、紧凑iPhone、iPad、最低支持iOS 17按§6执行；设备及Runtime从simctl实时解析。
- [x] S19–S22代表性中性场景无串色、皮革缺失或包体/帧耗时明显退化；必要离线测试只输出临时目录。
- [x] 最终构建、聚焦单元/集成/UI测试、受影响资源/工程配置检查及定向`git diff --check`通过；最终源码发生变化的对应证据重新获取。
- [x] 更新PROGRESS、UI审查报告；设计/API确定后按项目规则记录DR并同步UI规格/相关技能；不凭计划阶段预写“已完成”。
- [x] 总报告区分代码覆盖、测试覆盖、视觉通过与未验证边界；未覆盖项显式列出，不能用“全部相关场景”笼统结案。

## 6. 验证矩阵与证据纪律

- **标准手机/当前Runtime**：S01–S18所有真实页面入口巡检；六袋选择、单/双角色、单袋恢复、长序列状态作为深测。
- **紧凑手机/当前Runtime**：每日清台、单袋交互、试打、打三双角色；小尺寸皮革对比与点击命中。
- **iPad/当前Runtime**：2D与3D代表页、打三同袋/不同袋、转视角/缩放；验证共享材质实例及多次进出。
- **iOS 17**：plain/enhanced渲染、实际2D/3D进页、连续六袋切换与离页恢复，专项关注SceneKit线程和USDZ子集兼容。
- **Light/Dark**：跟随系统的宿主验证两态；强制深色的球桌页记录其真实有效外观，不能仅凭系统设置声称测试了两种实际皮革材质。
- **视觉**：全景和角袋/中袋局部原图均查看；2D、旋转顶视、透视、遮挡、缩放下保持洞口清晰。真实截图可对比，生成图不作验收。
- **几何/行为测试**：断言独立于实现，优先原模型面引用守恒、六袋映射、状态不串扰和生命周期；不为纯颜色复制写无意义镜像测试。
- **写盘边界**：默认不跑全量资源烘焙runner，不更新正式JSON、缩略图或视频。若扩大测试范围，先枚举所有写盘用例并核对scheme/testplan，再确定哪些进入本轮；工作区以“原有dirty + 本轮预期diff”为基线，不要求全局clean。
- **断言纪律**：不预先计划删除旧断言。发现失败须记录原文、失败机理及行为契约，不能改断言/跳测试换绿。
- **证据目录建议**：`output/pocket-leather-integration-20260910/<batch>/<runtime>/<device>/<state>/`，保留命令、真实退出码、xcresult、截图清单、源/资产指纹、首次失败与复验。
- **边界**：模拟器不证明真机GPU/功耗、VoiceOver完整体验或生产账号/发布。最终仅声明实际完成范围，不上传、不提交、不发布。

## 7. 锚点索引

核实日期：2026-09-10。行号随工作区变化，下游应按符号重新定位，不以行号硬贴补丁。

| 文件 | 已读锚点（行号） | 用途 |
|---|---|---|
| `QiuJi/Core/Scene/AngleTrainingScene.swift` | `setupTable` 114；`addPocketMarkers` 1304；`makePocketMarkerCircle` 1321；`setPocketHighlight` 1361；`hideAllVisualization` 1674 | 共用模型与当前红盘 |
| `QiuJi/Core/Scene/TableModelLoader.swift` | `parseModelScene` 57；`loadTable` 66；`deepCopyGeometryAndMaterials` 284附近 | 缓存、变换及隔离 |
| `QiuJi/Core/Scene/MaterialFactory.swift` | `enhancePocketMaterials` 224 | enhanced材质管线 |
| `QiuJi/Core/Scene/AngleSceneCalculator.swift` | `pocketPositions` 94；`pocketMarkerPositions` 101；`pocketMarkerRadius` 122 | 物理/视觉坐标区分 |
| `QiuJi/Core/Scene/AngleSceneView.swift` | `handleTap` 506；`pocketIndex(from:)` 566 | 球/皮革/袋洞命中 |
| `QiuJi/Features/PositionPlay/ViewModels/PositionPlayViewModel.swift` | `setupScene`内187；`updatePocketHighlights` 722；`restoreShotParams` 1446；`clearTable` 1479；`playSequence` 1699；`startBreakFlow` 1978 | 多宿主、模式与逐杆状态 |
| `QiuJi/Features/PositionPlay/ViewModels/SiluTrainerViewModel.swift` | 创建142；高亮362；`invalidateSolutions` 418；`refreshOverlays` 720；`clearTable` 1077；`startBreakFlow` 1123 | 独立VM残留检查 |
| `QiuJi/Features/PositionPlay/ViewModels/PlanThreeViewModel.swift` | 创建276；`selectPocket` 467；`refreshOverlays` 787；`drawPocketRing` 809；推进1397 | 双角色专项 |
| `QiuJi/Features/AngleTraining/ViewModels/AimingQuizViewModel.swift` | 创建151；题目高亮315 | 2D/3D瞄准训练 |
| `QiuJi/Features/AngleTraining/Views/AimPointSceneTrainingView.swift` | 创建81；题目高亮113 | 2D/3D瞄准点训练，v59共享文件 |
| `QiuJi/Features/AngleTraining/ViewModels/AngleDynamicViewModel.swift` | 创建61；高亮314 | 角度与打点 |
| `QiuJi/Features/AngleTraining/ViewModels/SeparationAngleAtlasViewModel.swift` | 创建78；高亮226 | 分离角图谱 |
| `QiuJi/Features/AngleTraining/ViewModels/CushionEnglishAtlasViewModel.swift` | 创建93；高亮241 | 加塞吃库图谱 |
| `QiuJi/Features/AngleTraining/ViewModels/BankShotViewModel.swift` | 创建160；高亮1141 | 翻袋解球 |
| `QiuJi/Features/AngleTraining/Views/BankShotView.swift` | 提示文案39 | “高亮圈”文案 |
| `QiuJi/Features/AngleTraining/ViewModels/DiamondSystemViewModel.swift` | 创建144 | 中性回归 |
| `QiuJi/Features/SnookerTactics/ViewModels/SnookerTacticsViewModel.swift` | 创建163 | 中性回归 |
| `QiuJi/App/MainTabView.swift` | 日清入口143；`angleDestination` 158–220 | 当前路由 |
| `QiuJi/Features/AngleTraining/Views/SceneAimingView.swift` | `AimingQuizViewModel` 16；相机参数24 | 两入口一VM |
| `QiuJi/Features/PositionPlay/Views/FreePlayView.swift` | entryMode18；VM20 | 日清/自由共用 |
| `QiuJi/Features/PositionPlay/Views/PositionPlayComposerView.swift` | 参数15–34；VM38 | 编排/试打/外部球形 |
| `QiuJi/Features/AngleTraining/Views/ShotSimulationView.swift` | VM10；默认球形30 | 教学变体 |
| `QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift` | 试打入口164 | 详情入口 |
| `QiuJi/Features/BallExtraction/Views/BallExtractionView.swift` | 三个目标67–73 | 外部球形入口 |
| `QiuJi/Features/BatchDrillStudio/BatchAuthoringView.swift` | composer257 | 批量制作继承 |
| `QiuJi/Core/Scene/DrillSceneView.swift` | `DrillSceneController.setup` 81附近 | 动作/训练中性实时示意 |
| `QiuJi/Core/Scene/TrajectoryRenderer.swift` | `Context.pocket` 53；延伸路径73 | 轨迹信息，不等同选袋高亮 |
| `QiuJi/Core/Media/SequenceVideoExporter.swift` | 场景构建549附近；进袋路径647 | 离线中性回归 |
| `QiuJi/Core/Components/BTAimTableView.swift` | `pockets` 172；`BTPocketMark` 217 | 静态教学组件，不是红盘选择器 |
| `output/pocket-leather-20260910/` | `extract.py`、`manifest.json`、`pocket-leather.blend`、`preview.png` | 前轮提取证据，未接App |

说明：历史记录中的`BTDrillTableView/BTMiniTable`并未在当前生产文件检索中找到，不能拿旧名虚构实施任务；W0以当前入口继续核验分享/缩略图边界。

## 8. 本轮状态与版本记录

- **最终状态**：W1–W4已连续执行完成，差量合入当前工作区；22类场景结果/等价类验证/未验证边界见`tasks/ui-reviews/UR-20260910-pocket-leather.md`。最终构建、门禁、四类模拟器矩阵及六袋普通/增强透视点选通过。原USDZ字节保持，未提交、推送或发布。
- **样本调整与证据边界**：现有c060的自由/进袋杆与c042的多袋组合验证恢复；完整播放用c042，未造正式内容。状态深测按共享VM等价类合并；不宣称每页×玩法×设备全组合。早期4份xcresult归档时已不存在，日志/退出码/独立PNG保留，最终7份结果包另归档；详报告。


- W0已完成静态22类入口复核、3页改前UI基线、4项原型/几何/成本测试和真实图审。原型代码保存在独立目录与output归档；主工作区App代码、正式球桌资产、物理和内容数据未改。报告 `tasks/pocket-leather-W0-20260910.md`。
- 上一条“开始实施”的调查阶段启动了改前构建，随后完成并输出`BUILD SUCCEEDED`，日志在`output/pocket-leather-integration-20260910/before-build.log`；它只证明当时工作区可构建，**不证明本方案已实施**。
- 已保存调查时的dirty基线，执行时必须刷新，不能把旧快照当最终输入。

| 版本 | 日期 | 变更 |
|---|---|---|
| v60.0 | 2026-09-10 | 按issue-collection整理22类场景台账、5批次依赖、真实皮革与双角色方案、状态残留检查及完整验收；用户最新要求先计划 |

| v60.1 | 2026-09-10 | 用户确认暖金/琥珀并授权执行；W0在独立工作目录验证，避免与同仓其他运行任务冲突 |

| v60.2 | 2026-09-10 | W0实证验收；锁定贴图保留暖金、同袋绿/青分区及首次子集缓存方案，记录首次失败与复验，W1依赖就绪 |

| v60.3 | 2026-09-10 | 用户要求连续执行至完成；W1–W4接入/状态修复/双角色/22类场景与四类模拟器回归完成，记录FL-055、DR-134、首次失败与复验及证据边界 |
