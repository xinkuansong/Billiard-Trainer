# 练习 Tab 问题集合 v5（可多会话执行版）

> **本文件是本轮需求真源**。来源：用户《问题集合_v4.md》（2026-07-13 提交，20 条）。与 `tasks/UI-IMPLEMENTATION-SPEC.md` 等既有规范冲突时以本文件为准，落地后回写规范。
> 状态：**全部批次 ✅（V11 总验收完成，2026-07-13）**——V1–V10 全部 ✅（V7 返工 1 轮〔仅 UI 测试取证层〕，其余批次 0 轮返工；Q19.2.5 半句用户未补，明确跳过并留档），V11 总验收主控独立实测通过（见 §四 V11 执行结论）。文档已回写 `tasks/UI-IMPLEMENTATION-SPEC.md`（§8.9 + §8.8 + Changelog）、`tasks/phases/P18-release-convergence.md`（§B3.8）、`tasks/PROGRESS.md`。排期前置：无（P18 主线仅剩人工项；翻袋反射 W1–W6 已收官，本轮条 17/18 是其后续 UI 反馈）。
> 全部锚点已读代码核实（2026-07-13，三路探索子代理 + 主会话复核）。
> 编号约定：全局规范延续 v3 的 G 系列（G13 起）；页面条目 Q 系列按 v4 原条号。

---

## 一、需求回显（v4 逐条，语义不增删）

| v4 条 | 诉求 | 归入 |
|---|---|---|
| 1 | 练习页各卡片标题字号与训练计划/动作库动作卡一致 | Q1 |
| 2 | 「角度与打点」改名「角度与瞄准」 | Q2 |
| 3 | 瞄准点对照表交互卡：瞄准点文字放左、接触点文字放右、拖动全程不遮挡 1 号球与假想球 | Q3 |
| 4 | 角度预测主体下移，上下留白相等，90° 时 1 号球完整可见 | Q4 |
| 5 | 3D 角度训练每题默认摄像机为最高点 | Q5 |
| 6 | 瞄准点训练：白瞄准线/红 ground truth 线宽调小、红瞄准点半径调小 | Q6 |
| 7 | 2D 瞄准点训练：①瞄准线不接触目标球则延伸到库边 + 红点规则修正（去假想球捕捉区）②布局复用共享规范 ③验证倒计时 3s→1.5s ④验证回放加球杆 | Q7 |
| 8 | **瞄准线交互重做（全局至关重要）**：点选瞄准线后相对调整，禁「点哪指哪」；拖动中不求解，停 0.5s 才求解；瞄准刻度条同理 | **G13/G14** |
| 9 | 3D 瞄准点训练：滑屏改为控制摄像机（参考 3D 角度训练），默认摄像机最高 | Q9 |
| 10 | **所有球停止不截断尾速（全局重要，含渲染管线）** | **G15** |
| 11 | 所有场景打点卡片白色打点盘半径增大，卡片大小不变 | **G16** |
| 12 | **所有场景「上一杆」恢复完整数据：球形、目标袋、瞄准方向、打点等（全局重要）** | **G17** |
| 13 | **开球通用规范（重要）**：随机性只留球堆间距；开放瞄准方向（拖屏 + 左侧瞄准条）；显示力度条默认 6 m/s；力度/瞄准条用既有标准；重开与完成按钮位置互换 | **G18** |
| 14 | 思路训练开球对齐通用开球规范 | Q14 |
| 15 | 打一走二想三：①扇形为默认选区（用户自选则扇形变灰；删自选恢复；求解按扇形真实几何，禁外接矩形近似）②<3 球也可求解击打直至无球 ③清除键正常尺寸、放「摆球」右侧（其他求解场景同）④开球通用规范 | Q15 |
| 16 | 做斯诺克改名「防守」+ 逻辑重做：符合中八规则，只指定目标球即可推断防守（让对方看不到其球组/8 号）；双组在桌不得以 8 号为目标；更广义防守解（长台/大角度等高难度可行解） | Q16 |
| 17 | 翻袋解球器 8 项 UI 统一（解描述/标题/按钮列/力度瞄准条高度/无解提示/球库补白球黑球/i→设置+网格） | Q17 |
| 18 | 反射解球器同 17 | Q18 |
| 19 | 动作库：①侧栏点击分组滚到组顶（无记忆）②上手试打四项（球数不含白球/标题居中/i 并入三点/新增**序列模式**为默认） | Q19 |
| 20 | 所有页面齿轮设置统一为三个点设置 | **G19** |

---

## 二、G 系列全局规范（G13–G19，延续 v3 G1–G12）

### G13 瞄准交互语义重做（选中 + 相对调整）

现状（已核实）：`AngleSceneView.handlePan` 空白处起手即 `onAimDragged` 逐帧回调台面点，各 VM（`PositionPlayViewModel.setFreeAim(toward:)` 等）把瞄准方向**绝对设为母球→手指方向**（「点哪指哪」）。

目标语义：
1. 处于自由瞄准（或可挪动瞄准）状态时，用户**第一个落点 = 选中瞄准线**（不改变当前方向）；
2. 随后左右滑动是对**当前瞄准方向的增量调整**（类似 `BTAimWheel` 的 `rotatedAim` 相对旋转，需定义合理的 度/pt 增益，可随母球距离缩放）；
3. 适用所有接了 `onAimDragged` 的页面：自由击球、自由走位（含试打变体）、分离角与走位、批量出片台、翻袋自由、反射自由、2D/3D 瞄准点训练（2D 保留拖动瞄准，语义同改；3D 见 Q9 改为相机）。
4. 点击（tap）瞄准的绝对指向行为是否保留：**按现状保留**（v4 只针对拖动语义），如执行中发现冲突再上报。

### G14 求解触发去抖（拖动不求解，停 0.5s 再求解）

现状（已核实）：`PositionPlayViewModel.recompute()` 为 20ms 去抖 + 单飞（拖动中持续求解）；翻袋/反射求解模式拖球 120ms 去抖；`BTAimWheel` 回调无内置节流。

目标语义（v4 条 8.3，「至关重要，确保所有相关场景符合」）：
- 任何**需要求解**的瞄准/拖动变更（拖瞄准线、拖球、瞄准刻度条），拖动过程中**不触发求解**，只做纯几何预览（瞄准线/假想球/首碰点，即 `freeAimFirstContact` 一类闭式计算）；
- 用户停下（无新输入）**0.5s** 后才触发求解；
- 落点：`PositionPlayViewModel.recompute` 调度层（20ms→拖动中挂起 + 0.5s idle 触发）、`BankShotViewModel`/`DiamondSystemViewModel` 求解模式（120ms→同规范）、`BTAimWheel` 消费方、以及后续新页一律遵守。
- 纯几何预览路径（翻袋/反射自由模式拖动）不受影响——它们本来就不求解。

### G15 球停止禁尾速截断（含渲染管线）

现状（已核实）：回放层 `TrajectoryPlayback.perceptibleSettleTime(speedThreshold: 0.07)` 从末尾截断「肉眼不可见 creep」+ `stateAt` 到 stationary 帧速度强制置零；各 VM 播放收尾用它算 `maxSimTime`。物理引擎 `EngineNumerics.determineMotionState` 的 0.001 m/s 静止判定是**物理语义，保留不动**。

目标：回放/渲染一律播到引擎自然静止，不做 0.07 m/s 感知截断（删除或旁路 `perceptibleSettleTime` 的消费点）；`SequenceVideoExporter` 渲染管线同源同改。验收看点：球停止前无「最后一跳/瞬移」。

### G16 打点盘白盘增大

现状：`BTSpinPad` 白盘半径 = `min(w,h)/2 − inset`，`inset = 5`；`BTSpinPadCard` 盘区固定 104×104、卡片 `maxWidth: 228`。
目标：卡片尺寸不变，白盘半径增大（减小 inset / 增大盘区在卡内占比，具体数值执行时截图定稿）。改共享组件一处，全场景生效。
**定稿（V2，2026-07-13）**：改 `BTSpinPad` 一处 `inset 5 → 2`（盘区 104×104、卡片 228 不变）；白盘直径 94 → 100（占盘区 90.4% → 96.2%，radius 47 → 50pt）。打滑极限虚线圈/皮头接触斑均以 ballR 为基准，随盘径等比放大（已核实）。104 盘区为硬上限，inset=2 已近满盘（留 2pt 安全边），若需更大须扩盘区 frame（会牵动卡片布局，超本条范围）。

### G17 「上一杆」恢复完整快照

现状（已核实）：`PositionPlayViewModel.replayCurrent()` 已是全参数恢复（球位+目标+袋口+速度+打点+瞄准，`restoreShotParams`）——**作为基准语义**；思路训练/打一走二想三/做斯诺克 `undoLastShot()` 仅 `loadBoard(before)` 球位；翻袋/反射自由模式仅球位。
目标：所有场景「上一杆」= 回到上一杆击打前的**完整状态**：球形、目标球/袋口（或约束/角色）、瞄准方向、打点、力度。快照结构建议在 `PositionPlayModels` 层共享（`BoardSnapshot + PlannedShot` 已可表达大部分，缺角色/约束的页面自扩）。
落地拆分：思路/打三在 V3 批实施；斯诺克随 Q16 重做批实施；翻袋/反射随 Q17/Q18 批实施（写进各批 DoD，标准同此条）。

### G18 开球通用规范（单一真源：`BreakFlowRunner`）

现状（已核实）：`BreakFlowRunner` 开球=自动锁顶球瞄准（`drawAimLine`）、`breakPower = 7.0` 固定无 UI、`breakJitter` 注入随机塞（spinX ±0.10 / spinY ±0.15）、仅母球可拖；`RackLayout` 球堆 `jitterRadius = gap×0.45` seed 扰动；底部条两套——`BreakControlBar`（取消/换一局/开球，Silu/PlanThree/Composer）与 FreePlay 私有 `FreePlayBreakBar`（取消/完成/开球|重开）。

目标（一处改 runner + 共享条，全宿主生效）：
1. **随机性只保留球堆的球与球间距**（`RackLayout` jitter 保留；`breakJitter` 的随机塞移除；原「目标球瞄准点随机」如仍存在一并移除）；
2. **开放瞄准方向**：开球模式支持拖屏调整瞄准方向（遵循 G13 相对调整语义），左侧显示瞄准刻度条 `BTAimWheel`；
3. **显示力度条**（右侧 `BTShotInstrumentColumn`），默认速度 **6 m/s**（替代固定 7.0）；
4. 力度条/瞄准条布局遵循既有 `ShotStageProxy` 标准（G4/G5/G7）；
5. 按钮：取消/完成/重开三钮语义保留，但**「重开」与「完成」位置互换**；`BreakControlBar` 与 `FreePlayBreakBar` 收敛为同一共享开球条（消灭双真源）；
6. 适用宿主：自由击球、自由走位 Composer、思路训练（Q14）、打一走二想三（Q15.4）。

### G19 设置入口统一为「三个点」

现状（已核实）：击打类页面已统一 `ellipsis.circle`；残留 `gearshape`：`AngleDynamicView` L57、`SceneAimingView` L77（训练设置 sheet 入口）、外围 `CustomPlanBuilderView` L233、`BTExerciseRow` L96、`ProfileView` L361。
目标：**所有相关页面**设置入口统一三个点（`ellipsis.circle`），原齿轮菜单内容并入三点菜单。范围以练习 Tab 为主（AngleDynamic / SceneAiming 两处必改）；外围三处（训练计划构建/练习行/个人中心）一并扫替，若有交互语义冲突执行中上报。
翻袋/反射的 i→设置（含网格 Toggle）在 Q17/Q18 内落地，标准同此条。

---

## 三、页面特有条目（Q 系列）

### Q1 练习页卡片标题字号统一

- 事实：练习页 `AngleGridCard`（`AngleHomeView.swift` ~L439）与动作库 `BTDrillGridCard`（~L117）同为 `.btHeadline`（17pt semibold）；训练计划卡（`TrainingHomeView.planPosterCard` ~L372、`PlanListView.PlanCard` ~L341）硬编码 `15pt bold`，**三方本就不一致**。
- 做法：统一到 `.btHeadline` token（推荐——消灭硬编码；即训练计划卡改 15pt bold → `.btHeadline`，练习页保持）。⚠️ 若用户本意是「练习页改小到 15pt」，执行前看一眼截图对比再定，偏差回写本文件。

### Q2 「角度与打点」改名「角度与瞄准」

- 锚点：`AngleDynamicView.swift` L47 `.navigationTitle`、`AngleHomeView.learnEntries` L110–112 入口卡、历史/成绩若有引用同步（参照 B3.5 T-P18-50「页名=卡名」纪律，grep 全部用户可见字符串）。
- 背景：用户指出页内并未详细讲「角度和打点的关系」，改名使名实相符；不新增内容。

### Q3 瞄准点对照表交互卡标签

- 锚点：`ContactPointTableView.swift` `aimFigure` L120–184——「瞄准点」标签现在瞄准点**右上方**（`aimPoint.x + d*0.95`）、「接触点」标签在接触点左侧（`contact.x - d*0.95`）；无遮挡避让逻辑。
- 目标：①「瞄准点」文字放**左侧**（不遮挡 1 号球）；②「接触点」文字放**右侧**（更贴近接触点）；③拖动角度滑条全程两标签不得遮挡 1 号球与假想球——需按滑条全域（0–90°）做几何核验（最坏角度下标签框与两球圆区不相交），不是只调一个静态偏移。几何任务，先读 `geometry-spatial-reasoning`。

### Q4 角度预测主体垂直居中

- 锚点：`GeometricAngleQuizView.swift` `AnglePredictionFigure` L312–317——`vertex = (w*0.15, h*0.82)`、`rayLen = 0.82h`，θ=90° 时 1 号球心 y=0，上半球被 320pt 画布 clip。
- 目标：整体下移使**上下空白相等**：全角度域（含 90°）1 号球完整可见。做法＝重算 vertex.y / rayLen（如 vertex.y = h − margin − R、rayLen = h − 2(margin + R) 一类闭式），数值草稿验证 0°/45°/90° 三点后落码。几何任务。

### Q5 3D 角度训练默认相机最高点

- 锚点：`CameraRig.enterAiming`（L222–242）每题切换将目标 pose 设 **zoom=0**（最低 aim 视角）；zoom 梯定义在 `AimingCameraConfig`（0=aimHeight 0.45m ↔ 1=standHeight 1.5m）。
- 目标：每题开始默认 **zoom=1（现有滑动可达的最高点）**，用户仍可下滑压低。只改 enterAiming 目标值，不动 zoom 梯定义。

### Q6 瞄准点训练线宽/红点调小

- 锚点：`AimPointTrainingView.swift`——白瞄准线与红 ground truth 线均 `proj.lineMainWidth`（1.6–3.2pt，L347/L356）；用户红点 `max(4, d*0.10)`（L366）、正确红点 `max(5, d*0.12)`（L358）。
- 目标：两线线宽调小、红瞄准点半径调小（具体数值截图定稿；注意 `lineMainWidth` 是共享投影量，**本页单独传参收窄，不改全局真源**）。

### Q7 2D 瞄准点训练四项

锚点集中在 `AimPointSceneTrainingView.swift`（View + `AimPointSceneQuizViewModel` 同文件）：

- **Q7.1 瞄准线绘制与红点规则**（几何，先读 skill）：现 `aimLineEnd` 与目标球心 2R 圆求交、相交即把白线截停在假想球心位（≈「假想球半径捕捉区」，即用户观察到的异常）。目标：
  - 瞄准线**未接触目标球**（线到球心距离 ≥ R）→ 白线直接延伸到库边；
  - 若瞄准线与「过目标球心的垂线（虚线）」的交点距球心 < R → 在**该交点**显示红色瞄准点（G1 垂足口径，`AimPointGeometry.aimPoint` 已是垂足，主要是白线画法与红点显隐规则对齐）；
  - 瞄准线**接触目标球**（距球心 < R）→ 在瞄准线与目标球的**接触点**增加一个红点；
  - 删除 2R 捕捉截停行为。
- **Q7.2 布局复用**：球桌尺寸/球库/瞄准条/提交按钮改走 `ShotStageProxy` 共享布局（现全屏 `AngleSceneView`、提交按钮右下浮动 overlay 非贴边规范）；提交按钮位置按 G6 按钮约定。
- **Q7.3 倒计时**：`submit` 内 `Task.sleep(3_000_000_000)` → **1.5s**，同步 HUD 文案「3 秒后自动击球验证」→「1.5 秒」。
- **Q7.4 验证回放加球杆**：现 `strike()` 仅 `TrajectoryPlayback` 播球、`nextQuestion()` 显式 `hideCueStick()`。目标：验证击球走 `AngleTrainingScene.runCueStroke`（运杆→出杆→触球起播）同其他击打页单一权威口径。

### Q9 3D 瞄准点训练滑屏改相机

- 锚点：`AimPointSceneTrainingView` `interactionMode = .tapsOnly` + `onAimDragged` 接 `aimToward`（滑动=调瞄准）；3D 角度训练的相机滑动在 `AngleSceneView.Coordinator.handlePan` `.cameraControl` 分支 + `CameraRig.handleVerticalSwipe`。
- 目标：3D 模式下滑屏改为**调整摄像机高度与视角**（同 3D 角度训练：横滑 yaw、竖滑 zoom 梯）；默认摄像机高度设为**最高**（同 Q5 口径）。瞄准调整改由**瞄准刻度条 `BTAimWheel`（已在页内）+ 点击瞄准**承担（假设项：v4 未指明 3D 下瞄准新通道，按此推定，执行截图后如不顺手上报）。2D 模式保留拖动瞄准（G13 相对语义）。

### Q14 思路训练开球对齐 G18

- 锚点：`SiluTrainerView` L340–341 `BreakControlBar`（取消/换一局/开球，`autoDeliverOnSettle=true`）。
- 目标：接入 G18 统一开球（开放瞄准+力度条+按钮布局）；「换一局」保留与否按共享条定稿语义（换一局=重摇球堆间距 seed，与「重开」语义合并或并存，执行时定稿并回写）。

### Q15 打一走二想三三项

- **Q15.1 扇形默认选区**（几何+求解，量级最大）：
  - 现状：`PlanThreeSectorSolver` 扇形纯视觉引导**不进求解**；求解只吃用户画的 `SolveRegion`（rect/circle/point 的 SDF 评分，**不存在扇形→外接矩形转换**——v4 「外接矩形误差大」按「禁止用外接矩形来近似实现」理解）。
  - 目标：②球+②袋就绪且用户未画约束时，**扇形即默认落区**直接可求解；用户自画落区/落点/过点后扇形**变灰**（视觉降级为参考），求解用用户约束；用户删除自画约束后扇形**恢复**为选区；求解按扇形**真实几何**评分——`SolveRegion` 新增 `.sector`（顶点/半径区间/角域的 SDF，`signedDistanceMeters` 扩展 + `scoreRegionCell` 接入），禁外接矩形近似。先读 `geometry-spatial-reasoning`，扇形 SDF 需单测（内/外/弧边/角边界样本）。
- **Q15.2 <3 球终局**：现无球数 guard，但角色流程（①②③指派）在少于 3 球时无法走通的环节需梳理——支持 2 球（打一走二）、1 球（只打进）降级求解与击打，直至台面无球（终局 UX：清空后提示/重开）。锚点：`PlanThreeViewModel.solve/canStrike/finishStrike/rollWindow`。
- **Q15.3 清除键**：现为顶栏 chip 行末小 eraser 图标（13pt 无 frame）。目标：调成正常按钮尺寸、放「摆球」chip 右侧；思路训练/防守页同布局同改（`SiluTrainerView` L166–173、`SnookerTacticsView` L130–138——后者随 Q16 批落地）。
- **Q15.4 开球**：接入 G18（随 V6 批落地，本批只验收）。

### Q16 做斯诺克 → 「防守」重做

- 现状（已核实）：手动指定目标球+障碍球二元模型，`solveSnooker` 硬约束=终位障碍球完全挡死目标球视线；不理解中八规则。
- 目标：
  1. 改名「防守」（导航题/入口卡/历史等全部用户可见字符串）；
  2. 语义重做：用户只指定**目标球**（我方将击打的球），系统按**中八规则**推断防守对象——击打目标球后让**对方球组**（目标球是全色则对方=花色 9–15，反之亦然；对方只剩 8 号则防 8 号）全部/尽量不可见；
  3. 规则约束：台面同时存在全色与花色时**不得以 8 号为目标球**（选球拦截+提示，复用/对齐 `BilliardRulesEngine.legalTargetKeys` 语义）；
  4. 解的谱系扩展：完全斯诺克（看不到球）为最优；无完全解时给「高难度可行解」——对方球全部剩长台、大切角等（需定义可见性/难度综合评分，建议复用 `snookerCoverage` + `BankKickDifficulty`/`DifficultyModel` 难度语义，扩展 `solveMulti-target` 对对方球组逐球评估）。⚠️ 评分权重细节属设计决策，批内先出 1 版可跑评分再截图/实测调优，定稿回写本文件。
- 涉及：`SnookerTacticsView(+VM)`、`PositionPlaySolver.solveSnooker`（多目标扩展）、`AngleSceneCalculator.snookerCoverage`（多被困球）、Q15.3 同款清除键、G17 上一杆全量恢复。量级 L，独立会话。

### Q17/Q18 翻袋 + 反射解球器统一（两页同构，一批做完）

锚点：`BankShotView.swift` / `DiamondSystemView.swift`（结构镜像）。逐项：

| # | 诉求 | 现状（已核实） | 做法 |
|---|---|---|---|
| 17.1 | 解描述放标题下方（与其他页一致） | 解 pill 在球桌左下 overlay | 参照三解页 principal 副标题（`SnookerTacticsView` L86–99 样式）：标题下 statusText 承载解描述 |
| 17.2 | 标题字号/颜色与其他页一致 | 系统 inline 标题，无 principal 品牌绿样式 | 换 principal 品牌绿 14pt + 11pt 副标题（同三解页） |
| 17.3 | 去掉「演示中/中断/重置」 | 求解模式动作列=击打/下一解/重置，演示中换「停止」 | 删演示中 pill/停止/重置形态（演示不可中断或点击台面即停，执行定稿） |
| 17.4 | 「下一解」放左侧 | 右下动作列内 | 移到球桌左下（对应现「恢复球形」位置带） |
| 17.5 | 右侧保留击打/上一杆/回放 | 自由模式已是；求解模式为击打/下一解/重置 | 求解模式右列也收敛为击打/上一杆/回放（上一杆按 G17 全量恢复） |
| 17.6 | 力度条与瞄准条上下高度一致 | 求解模式无 BTAimWheel（自由才有）；有 wheel 时 `ShotStageMetrics` 已同底等长 | 求解模式常显瞄准条（配合 G13/G14 微调再求解）或按现 G5 校验；执行时核像素 |
| 17.7 | 无解说明放标题下 | 左下 noSolutionPill | 同 17.1 入 principal 副标题 |
| 17.8 | 球库补白球+黑球 | `paletteKeys = objectKeys.filter { $0 != "_8" }`，白球/黑8 固定台面 | 球库含全 16 球位（白/黑在台面时球库位灰显），参照其他页球库标准 |
| 17.9 | i→设置（三点），加显示网格 | `info.circle`→原理 sheet，无网格 | 三点菜单：原理说明入口 + `BTTableGridMenuToggle`（G19 口径） |

依赖：G13/G14/G17 语义先行（V1/V3）。

### Q19 动作库两块

- **Q19.1 侧栏无记忆**：`DrillListView.categorySidebar`（L88–112）点击分组切 `categoryFilter`；目标：每次点击分组，右侧内容列表**滚动到该分组顶部**（含重复点击当前分组也回顶）——`ScrollViewReader.scrollTo` 挂 `categoryFilter` 变更 + 同值点击手动触发。
- **Q19.2 上手试打四项**（锚点：`PositionPlayComposerView` 试打变体、`DrillTryoutBoardStore`、`DrillDetailView`）：
  1. 选球形副标题球数不含白球：`DrillDetailView` L171 `initial.onTable.count` → 排除 `cueBall`；
  2. 标题居中：代码已有 principal 居中（L355–370），按截图复核实际是否偏移（可能长 drill 名+返回钮挤压），如确有偏差修布局；
  3. 右上角 i 内容并入三点菜单：`briefInfoButton`（L115–117）删除，说明卡召回入口进 `moreMenu`；
  4. **新增序列模式（默认）**：三模式=序列/进袋/自由。序列模式按 `PositionPlaySequence.steps` 逐杆呈现击打信息（当前杆的目标/袋口/打点/力度/轨迹），**隐藏打点盘、力度条、瞄准条**，用户点「击打」走完整序列（逐杆播放，杆间停顿）。数据缺口（已核实）：`DrillTryoutFormation` 现只带 `initial/firstShot/stepCount`，完整 `steps` 未传入 VM——需把序列整体接入试打变体（新增序列播放状态机或复用 `SequenceStep` 回放）。默认模式改为序列模式（现 `.free`）。
  5. ⚠️ v4 条 19.2.5「在进袋模式下，默认使用」**原文未写完**——待用户补充（推测是「默认使用序列第一杆的目标球/袋口」，执行前确认，不脑补落码）。

---

## 四、会话批次划分（V1–V11）

> 依赖图：
>
> ```
> V1 (G13+G14 瞄准交互地基)
>  ├─→ V5 (瞄准点/角度训练场景页)
>  ├─→ V6 (开球通用规范) ─→ V7 (打一走二想三)
>  ├─→ V9 (翻袋/反射，另依赖 V3)
>  └─→ V10 (动作库试打，另依赖 V2 的 G19)
> V2 (G15+G16+G19 全局小件) — 独立，可与 V1 并行
> V3 (G17 上一杆) — 独立，可与 V1 并行
> V4 (学习/训练页轻修) — 完全独立，随时可插
> V8 (防守重做) — 独立（含 Q15.3 同款清除键与 G17 落地）
> V11 (总验收) — 最后
> ```

### V1 瞄准交互地基（G13 + G14）★基准批

**范围**：`AngleSceneView.handlePan` 拖动语义改「选中+相对调整」；各 VM `handleAimDrag` 适配（编排台系/翻袋/反射/2D 瞄准点训练）；求解触发统一「拖动中纯几何预览 + 停 0.5s 求解」（`recompute` 调度层、翻袋反射 120ms、`BTAimWheel` 消费方）。
**量级**：L。**依赖**：无。
**DoD**：
- 自由击球页实测：点瞄准线不跳变、滑动为相对微调、拖动中轨迹预览流畅无卡顿、停 0.5s 后解上屏；
- 其余接线页逐页冒烟（自由走位/分离角/批量出片台/翻袋自由/反射自由/2D 瞄准点训练）；
- 增益系数（度/pt）截图+手感定稿并回写本文件；
- 单元测试（相对旋转语义、去抖调度时序）+ `make build` + 相关 UI 测试绿。

**执行结论（2026-07-13，执行智能体，代码级已完成，🔬 真机手感待验证）**：
- **G13 增益模型定稿 = 绕母球公转（杠杆自适应，非固定 度/pt）**：`AngleSceneView.Coordinator.handlePan` 空白处拖动改为——第一落点**只选中瞄准线不转向**（`.began` 不回调），随后每帧把手指绕**母球屏幕投影**的角位移 Δφ 换算为瞄准相对旋转（`AngleSceneCalculator.aimNudgeDegrees`，屏幕顺时针为正，直接喂 `rotatedAim`）。**推导**：手指绕母球公转 1° ⇒ 瞄准线转 1°（"抓住线甩"），径向拖动不转向、切向才转向；等价切向增益 = (180/π)/r 度/pt（r=手指到母球屏幕距离 pt），故**天然随母球距离缩放**（远=细调、近=粗调）。近母球处发散，用**最大切向增益 `maxGainDegPerPt = 0.6` 度/pt** 封顶（等价最小杠杆臂 minLever = (180/π)/0.6 ≈ 95.5pt，落在其内按 r/minLever 线性衰减）。0.6 取值依据：约为 `BTAimWheel` 细调 0.15 度/pt 的 4 倍（滑屏=粗调、刻度轮=细调的既有分工，T-P18-43）；典型 r≈200pt ⇒ ≈0.29 度/pt，r≈300pt ⇒ ≈0.19 度/pt。⚠️ 0.6 为**代码默认待真机手感定稿**，若真机偏灵/偏钝调此单一常量即可（`aimNudgeDegrees` 默认参数）。
- **G14 定稿 = 0.5s idle 去抖**：`PositionPlayViewModel.recompute(interactive:)` + `SolveDebounceScheduler`（交互态拖动中不求解、仅纯几何预览〔假想球/首碰点/闭式瞄准线〕，停 0.5s 无新输入才求解；离散态保留 ~20ms）；翻袋/反射求解模式 120ms→0.5s idle。tap 绝对指向语义按现状保留（未发现冲突）。
- 点击（tap）瞄准绝对指向：**保留未改**，与 G13 拖动语义无冲突。

### V2 全局小件三合一（G15 + G16 + G19）

**范围**：删回放感知截断（`TrajectoryPlayback` 消费点 + `SequenceVideoExporter`）；`BTSpinPad` 白盘增大；齿轮→三点扫替（AngleDynamic/SceneAiming/外围三处）。
**量级**：M。**依赖**：无（可与 V1 并行/换序）。
**DoD**：任一击打页回放球自然滚停无瞬移（录屏/逐帧核验）；出片管线抽一条序列重渲核验同口径；打点盘截图对比（卡片尺寸不变、白盘可见增大）；grep `gearshape` 练习 Tab 清零；build + 回归 UI 测试绿。

**执行结论（2026-07-13，执行智能体，✅ 已验证）**：
- **G15**：删除 `TrajectoryPlayback.perceptibleSettleTime()` 方法 + 全部 **14 个消费点**（8 个 VM 各 2 处：Diamond/Bank/PositionPlay/Silu/PlanThree/Snooker 的击打+回放，AimPointScene 1 处，BreakFlowRunner 1 处），统一改 `let settle = playback.duration`（播满引擎自然静止；`maxSimTime: settle` 等价传 `nil`，收尾计时用 `TimeInterval(duration)`）。`stateAt` L245–252 stationary 置零与 `EngineNumerics` 0.001 静止判定**保留不动**（物理语义）。`SequenceVideoExporter` **无需改**——它本就循环到 `pred.duration + tail`、从不调 `perceptibleSettleTime`（已 grep 核实），与 App 回放现同口径。新增引擎级单测 `TrajectoryPlaybackSettleTests` ✅：确定性 recorder 证明旧 0.07 截断会遗留 **12.2mm 瞬移间隔**、额外播放 **0.35s** 到自然静止（`stateAt` 尾段速度 0.033m/s 未被置零）。**副作用如实评估**：各页击球/开球收尾等到自然静止，比旧感知截断晚 ~0.3–0.5s（即被消除的 creep 尾段真实时长），属预期正确行为，未新增任何截断。
- **G16**：`BTSpinPad` `inset 5 → 2`（见 G16 定稿）。
- **G19**：`ellipsis.circle` 扫替 4 处——AngleDynamicView L57（Menu 标签）、SceneAimingView L77（Button 开 sheet，按「保功能只换图标」）、CustomPlanBuilderView（每 drill 设置钮）、BTExerciseRow（`.settings` 配件字形）。**ProfileView「偏好设置」行（`BTIcon.gear`）保留齿轮并上报**：它是带文案的 iOS 设置风格导航行徽标图标（非图标按钮式设置入口），换三点语义错误。grep `gearshape`：练习 Tab 清零；全仓仅余 `IconToken.gear` token 定义（供 Profile 行 + 一个 #Preview 徽标）。
- **验证（真实输出）**：`make build` **BUILD SUCCEEDED**；`TrajectoryPlaybackSettleTests` TEST SUCCEEDED；`PositionPlaySequenceExportRunnerTests` TEST SUCCEEDED；UI 回归 `S5_TrainingPagesLayoutUITests`（testAngleDynamicLayout + testTableGridBrightness，验证 G19 三点菜单仍可开/网格 Toggle 生效）、`ScreenshotTourUITests`（testBankShotRealMode + testReflectionRealMode，验证 G15 击打回放不卡死）全绿 TEST SUCCEEDED。

### V3 上一杆完整恢复（G17 思路/打三先行）

**范围**：共享完整快照口径（含约束/角色扩展）；`SiluTrainerViewModel.undoLastShot` / `PlanThreeViewModel.undoLastShot` 从仅球位升级为全量恢复（球形+约束/角色+解+瞄准+打点+力度）。斯诺克/翻袋/反射的落地随 V8/V9（DoD 引用本条标准）。
**量级**：M。**依赖**：无。
**DoD**：思路/打三各实测「击打→上一杆」回到击打前完整状态（约束还在、解还在、参数还原，无需重画重求解）；单测覆盖快照往返；build 绿。

**执行结论（2026-07-13，执行智能体，✅ 已验证）**：
- **共享快照结构定稿**：在 `QiuJi/Core/PositionPlay/PositionPlaySolverModels.swift`（`PositionPlay` 核心层，与 `PositionPlayModels.swift` 同层；因快照要引用 `PositionPlaySolution`/`ShotPrediction` 求解产物，放这个「瞬态求解输入/输出」文件更内聚——如实说明的选址取舍）新增两个**纯内存**类型：
  - `SolveConstraintDraft`（`region/restPoint/passPoint`）——把两页各自重复定义的 `Draft` 上收为单一共享口径（两页改 `typealias Draft = SolveConstraintDraft`，调用点零改动）。它保留了 `SolveConstraint` 会塌缩掉的「落点 vs 落区圆」视觉/语义分叉，是「上一杆」忠实还原绘制层的最小信息。
  - `SolveShotSnapshot`——反解页「上一杆」完整快照：`before`(球形) + `shot` + `prediction` + **`solutions`+`currentIndex`（解缓存回填，免重解）** + `draft`(约束) + `velocity/spinX/spinY` + `allowSideSpin/basicPositionOnly`(求解选项)。页面特有的**选择模型**不进共享层（思路=目标球+袋口；打三=①②③角色），由各 VM 的 `UndoContext` 以本快照为基座另行携带。
  - **V8/V9 扩展预留**：防守/翻袋/反射落地时复用 `SolveShotSnapshot`，只需仿照本批为其页面特有选择模型加一层同款 `UndoContext` 包装即可（斯诺克的目标/障碍、翻袋反射的库序等）；`SolveShotSnapshot` 本身无需改动。纯内存态 → 无 Codable/向后兼容负担（已按红线如实取舍：不涉及持久化，故不加可选字段迁移）。
- **「解还在」= 直接缓存解对象**（非重求解）：`play()` 击打前把当前 `solutions/currentIndex` 连同球形/约束/参数一并存入 `UndoContext`；`undoLastShot()` 调 `restore(from:)` 把它们**原样回填**，`isComputing` 恒为 false，无需用户重画约束、无需自动重求解。恢复后若 `currentSolution` 非空则走 `showSolution` 重绘轨迹/球杆/约束/叠加（含①②③角色环与扇形）。
- **改动**：`SiluTrainerViewModel.undoLastShot` 与 `PlanThreeViewModel.undoLastShot` 从「仅 `loadBoard(before)` 恢复球位」升级为**全量恢复**（球形 + 目标/袋口或①②③角色 + 约束 draft + 解集 + 打点/力度/瞄准 + 求解选项）。工具态复位到 `.none`（约束已还原可见、用户可直接再选/再画；`activeTool` 属瞬态 UI 选择、非该杆数据的一部分——如实说明这一范围决策；`regionShape` 同理未纳入快照）。捕获逻辑抽为 `makeUndoContext(shot:prediction:)` 由 `play()` 与单测共用（单一真源，避免测试与生产分叉）。
- **未改（按明确不做）**：斯诺克页（V8）、翻袋/反射页（V9）的 undo 保持现状；`PositionPlayViewModel.replayCurrent()/restoreShotParams` 作为基准语义未动。
- **验证（真实输出）**：`make build` **BUILD SUCCEEDED**；新增 `QiuJiTests/PositionPlayUndoSnapshotTests`（3 例）+ 回归 `PositionPlayFreeAimTests`（12 例）合计 **Executed 15 tests, 0 failures**（`test_silu_restore_reproducesEveryField`：手工快照含 2 解，逐字段断言球形/目标/袋口/约束/解计数与档位/打点力度/选项全一致，且 `basicPositionOnly` 从默认翻转以验证「选项先于 solutions 恢复」的顺序安全；`test_planThree_restore_reproducesRolesAndSolve`：①②③角色 + 解还原；`test_silu_realSolvePipeline_captureAndRestore`：真实反解→捕获→抹除→恢复，解集计数/选择模型往返一致且 `isComputing==false` 证明不重解）。UI 回归 `S2_ShotPagesLayoutUITests`（testSiluLayout + testPlanThreeLayout）**Executed 2 tests, 0 failures**。
- **存疑/待真机**：单测在 `restore` 层证明状态逐字段一致；「击打→上一杆」的完整**手感/动画**链路（`play()` 依赖 SCNAction 运杆+回放，非单测友好）建议真机冒烟一次确认无视觉异常。

### V4 学习/训练页轻修批（Q1 + Q2 + Q3 + Q4 + Q6）

**范围**：卡片标题字号统一（Q1）；角度与瞄准改名（Q2）；对照表标签左右换位+全角度不遮挡（Q3，几何核验）；角度预测垂直居中（Q4，几何核验）；瞄准点训练线宽红点调小（Q6）。
**量级**：M。**依赖**：无，完全独立可随时插队。
**DoD**：五项逐项截图核验（Q3/Q4 附最坏角度帧：0°/90°）；Q1 三处卡片同 token；改名后 grep 旧名清零；build + 截图巡游测试绿。

**执行结论（2026-07-13，执行智能体，✅ 已验证）**：
- **Q1**：训练计划卡两处标题硬编码 `15pt bold` → `.btHeadline`（17pt semibold，Typography 真源）——`TrainingHomeView.planPosterCard`、`PlanListView.PlanCard`。练习页 `AngleGridCard`、动作库 `BTDrillGridCard` 本就是 `.btHeadline`，三方现同 token。grep `size: 15, weight: .bold`：两卡文件已清零（其余命中均为结果胶囊/其他页无关标签）。截图 `v4-q1-training-home`（计划卡）与 `v4-q1q2-angle-learn-cards`（练习学分类卡）目测同字号。
- **Q2**：`AngleDynamicView.navigationTitle`、`AngleHomeView.learnEntries` 入口卡标题 → 「角度与瞄准」；UI 测试 `ScreenshotTourUITests`（3 处 tap/tuple）、`S5_TrainingPagesLayoutUITests`（2 处 openCard）同步。grep 全仓 `.swift` 用户可见字符串「角度与打点」清零（仅余 8 处代码注释/文档，按纪律豁免）。改名后 S5 两用例（openCard 走新卡名）复跑通过。
- **Q3**（几何，坐标契约：SwiftUI 画布 y 向下、原点左上、pt）：`ContactPointTableView.aimFigure` 标签换位——「瞄准点」放瞄准点**左上**（`aimPoint − (0.95d, 0.40d)`）、「接触点」放接触点**右侧沿两球连线右法向**（`contact + (cos,sin)·0.95d`）。全角度域（0–90°）数值核验：瞄准点标签框与 1 号球/假想球最小间隙 **4.2pt@0°**、接触点 **6.4pt@8°**，均 >0。最坏帧根因：90° 时假想球心与瞄准点重合，「左上」偏移用空的上半区避让。截图 0°/45°/90° 三帧两标签均不压 1 号球/假想球。
- **Q4**（几何）：`GeometricAngleQuizView.AnglePredictionFigure` 垂直居中——闭式 `rayLen = min(w·0.74, h − 2·margin − 2R)`、`vertex.y = h/2 + rayLen/2`（内容带 rayLen+2R 居中，上下留白恒相等 = (h−rayLen−2R)/2；margin=20）。数值核验（h=320,R≈14.7）：0°/45°/90° 1 号球完整落 [0,320]，90° 上缘 y≈20（原方案 y=0 被 clip）。截图 `v4-q4-angle-prediction-90deg` 90° 1 号球完整、上下留白目测相等。
- **Q6**：`AimPointTrainingView` 本页传参收窄，不改全局 `lineMainWidth`/dot 系数真源。**最终数值**：白瞄准线/红 ground truth 线宽 `proj.lineMainWidth`（本特写恒钳到 3.2pt）→ `max(1.2, lineMainWidth·0.55)`≈**1.76pt**；用户红瞄准点 `max(4, d·0.10)`→`max(4, d·0.06)`（d≈118 ⇒ 11.85→7.1pt 直径）；正确红瞄准点 `max(5, d·0.12)`→`max(5, d·0.075)`（14.2→8.9pt 直径）。截图 `v4-q6-aimpoint-training-result` 线细红点小且清晰可辨。
- **测试基建（供 Q4 90° 取证）**：`GeometricAngleQuizView.onAppear` 加 UITest-only 确定性角度钩子（读 `-geometricQuiz.forcedAngle`，默认 0 不生效，生产行为不变）；`ScreenshotTourUITests.testV4Evidence` 用滑条/该钩子拍 0°/90° 最坏帧。
- **验证（真实输出）**：`make build` **BUILD SUCCEEDED**；`testV4Evidence` + `S5_TrainingPagesLayoutUITests.testAngleDynamicLayout`（走新卡名）+ `testTableGridBrightness` **Executed 3 tests, 0 failures, TEST SUCCEEDED**；截图产物 `build/v4-shots/v4-*.png`（Q1/Q3 0·45·90°/Q4 90°/Q6）逐张核验。

### V5 场景训练页相机与瞄准点行为（Q5 + Q7 + Q9）

**范围**：3D 角度训练默认最高机位（Q5）；2D 瞄准点训练四项（Q7.1 线/红点规则重做〔几何〕、Q7.2 布局接 `ShotStageProxy`、Q7.3 1.5s、Q7.4 球杆）；3D 瞄准点训练滑屏改相机+默认最高（Q9）。
**量级**：L。**依赖**：V1（拖动瞄准相对语义在 2D 瞄准点训练同批适配）。
**DoD**：Q7.1 几何单测（线不触球延伸库边/垂线交点红点显隐/接触点红点三情形）；2D/3D 各截图核验布局与新交互；倒计时实测 1.5s；验证回放出杆动画可见；3D 每题进场机位=最高（与 3D 角度训练一致）；build + UI 测试绿。

**执行结论（2026-07-13，执行智能体，✅ 已验证）**：
- **改动文件清单**：
  - `QiuJi/Core/Scene/CameraRig.swift`（Q5/Q9：`enterAiming` 进场目标 pose zoom 0→1）；
  - `QiuJi/Core/Geometry/AimLineGeometry.swift`（**新增**，Q7.1 纯几何真源）；
  - `QiuJi/Features/AngleTraining/Views/AimPointSceneTrainingView.swift`（Q7.1 白线/红点重做、Q7.2 ShotStageProxy 布局、Q7.3 1.5s+文案、Q7.4 runCueStroke、Q9 3D 相机/2D 拖动分流）；
  - `QiuJiTests/AimLineGeometryTests.swift`（**新增**，Q7.1 三情形单测）；
  - `QiuJiUITests/S5_AimPointSceneV5UITests.swift`（**新增**，2D/3D 截图取证）。
- **Q5/Q9 默认最高机位**：`CameraRig.enterAiming` 进场目标 pose 由 zoom=0（aimHeight/aimPitch/aimFov）改为 **zoom=1 等价值**（`standRadius/standHeight/standPitchRad/standFov`）——即竖滑可达上界，落地后 `update` 回填 targetZoom=1，用户仍可下滑压低。**未动** `AimingCameraConfig` zoom 梯定义与 `applyCameraTransform`（zoom→pose 映射不变，z=1 时恰给出该 pose，自洽）；`snapToAimPose`（2D→3D 切换路径，非每题进场）保持 zoom=0 不动（按「只改 enterAiming」范围纪律）。**副作用如实评估**：3D 角度训练页（`SceneAimingView` 也调 `enterAiming`）每题进场同步改为最高机位——属 Q5 直接目标，非回归；S3/P5 相关 UI 测试仍绿。
- **Q7.1 几何（坐标契约已钉死）**：坐标系 = SceneKit 水平面 X–Z、Y 朝上、单位米；平面映射 `x→x、z→y`（与 `AimPointGeometry`/误差口径一致）。新增纯 2D 真源 `AimLineGeometry.resolve`：① 目标球心到瞄准线**垂距 ≥ R**（不触球）→ 白线终点 = `rayToInnerRail`（V1 共享库内边界，各方向缩一颗球半径）、无红点；② 垂距 < R（触球，等价「瞄准线与过球心垂线的交点距球心 < R」）→ 白线停在**射线与球面（半径 R）第一交点**，并画两枚红点：垂足（G1 `AimPointGeometry.aimPoint`）+ 接触点。**删除旧 2R 捕捉截停**（`aimLineEnd` 已移除）。数值草稿验证：θ→垂距 = 2R·sinθ（G1）；接触点手算 `x=−sqrt(R²−p²)`（p=垂距）与实现 1e-6 吻合；边界 R±0.001 触球判定翻转。单测 `AimLineGeometryTests`（5 例）覆盖三情形 + 反向射线无交点 + 与真实 `rayToInnerRail` 联动，**全绿**。
- **Q7.2 布局**：改走 `ShotStageProxy`——2D 页 `GeometryReader` + 顶栏(46)/scene/装饰球库(94) 三段（同 `SceneAimingView` 语法），球桌 `autoFitsRotatedTable`、球库 16 球排（目标球高亮、G8 总宽=球桌宽）、瞄准刻度轮 `aimWheelFrame`（右缘贴球桌左缘，G4）、提交按钮 `bottomTrailingFrame`（左缘贴球桌右缘、齐球桌底线，G6）；3D 无球桌矩形，刻度轮 + 提交保持右下浮动。截图 `v5-q72-2d-aiming-layout` 核验贴边合规。
- **Q7.3 倒计时**：`submit` 内 `Task.sleep(3_000_000_000)` → `1_500_000_000`；HUD 文案「3 秒后自动击球验证」→「1.5 秒后自动击球验证」。grep 证据 + 截图 `v5-q73-2d-result-countdown` 核验。
- **Q7.4 验证回放加球杆**：`strike()` 改走单一权威 `AngleTrainingScene.runCueStroke`（运杆→出杆→触球 `onContact` 起播球体，收杆由跟杆序列接管，勿在此 hideCueStick），球体回放抽为 `launchStrikePlayback`；收尾统一 `playback.duration`（V2/G15）。截图 `v5-q74-2d-strike-2/3` 明确可见木质球杆运杆动画。
- **Q9 3D 滑屏改相机（推定项落地）**：3D `interactionMode` `.tapsOnly`→`.cameraControl`（横滑 yaw、竖滑 zoom 梯，走 `handlePan` cameraControl 分支 + `CameraRig.handleVerticalSwipe`），滑屏不再接瞄准；瞄准调整由 `BTAimWheel`（页内已有）+ **点击台面绝对指向**（`onTableTapped→aimToward`，G13 保留 tap 语义）承担。2D 保留拖动瞄准 G13 相对语义（`onAimNudged→nudgeAim`，未回退点哪指哪）。截图核验：`v5-q5q9-3d-entry-highest`（进场最高）、`v5-q9-3d-after-horizontal-swipe`（yaw 旋转）、`v5-q9-3d-after-vertical-swipe`（上滑降 zoom 至低机位）、`v5-q9-2d-relative-drag-aim`（2D 拖动后瞄准线相对旋转、不触球延伸库边）。**推定项手感**：截图层面工作正常、无明显不顺手；真机点击/滑屏细腻手感建议冒烟一次确认（未上机）。
- **验证（真实输出）**：`make build` **BUILD SUCCEEDED**；单测 `AimLineGeometryTests` + 回归 `AimPointGeometryTests` + V1 地基 `PositionPlayFreeAimTests` 合计 **Executed 24 tests, 0 failures**（含 G13/G14 去抖时序用例，未破坏地基）；UI 测试 `S5_AimPointSceneV5UITests`（2 例）+ 回归 `S3_AimPointUITests`（3 例）**全绿 TEST SUCCEEDED**；截图产物 `build/v5-shots/v5-*.png`、`build/v5b-shots/v5-q9-3d-after-vertical-swipe.png` 逐张核验。
- **偏差说明（如实）**：UI 测试 destination 用 **iPhone 17 Pro**（本机无 iPhone 16 Pro 模拟器，任务指定机型不可用，已用仓库默认机型替代）。

### V6 开球通用规范（G18 + Q14 + Q15.4）

**范围**：`BreakFlowRunner` 改造（去随机塞、开放瞄准、力度条默认 6 m/s、共享开球条、完成/重开互换）；四宿主接线（自由击球/Composer/思路/打三）；`FreePlayBreakBar` 与 `BreakControlBar` 收敛单真源。
**量级**：L。**依赖**：V1（开球瞄准用 G13 语义与 BTAimWheel）。
**DoD**：四宿主逐页截图（瞄准条+力度条贴边合规、按钮位置互换）；同 seed 两次开球球堆间距一致性/随机性单测（只有间距随机）；默认力度 6 m/s 断言；开球散开→停稳→交付全流程回归；build + UI 测试绿。

**执行结论（2026-07-13，执行智能体，✅ 已验证；坐标契约：SceneKit 世界系 X–Z 平面、Y 朝上、单位米）**：
- **改动文件清单**：
  - `QiuJi/Core/Rack/BreakFlowRunner.swift`（去随机塞、开放瞄准 `aimDir`+`nudgeAim`、力度 `velocity` 默认 6.0、`drawAimLine` 沿可调方向、统一 `BreakControlBar`、新增 `BreakInstrumentsOverlay`）；
  - `QiuJi/Core/Rack/BreakSimulator.swift`（`breakShot` 新增 `aimDirection` 可选参，nil 兜底锁顶球——向后兼容既有测试签名）；
  - `QiuJi/Features/PositionPlay/Views/FreePlayView.swift`（删私有 `FreePlayBreakBar`、底部条换共享 `BreakControlBar`、stage 加开球仪表叠加、`onAimNudged` 路由 runner）；
  - `PositionPlayComposerView.swift` / `SiluTrainerView.swift` / `PlanThreeView.swift`（stage 加开球仪表叠加、开球模式 `onAimNudged` 路由 runner；Silu/PlanThree 新加 `onAimNudged` 参数）；
  - `QiuJiTests/BreakFlowRunnerV6Tests.swift`（**新增** 5 例）、`QiuJiUITests/S6_BreakUniversalUITests.swift`（**新增** 四宿主截图）。
- **① 随机性只保留球堆间距**：删除 `BreakFlowRunner.breakJitter`（spinX ±0.10 / spinY ±0.15 随机塞）+ `clampSpin` + `BreakJitter` 结构；`breakNow` 改传 `spinX:0, spinY:0`。`RackLayout.jitterRadius = gap×0.45` 球堆间距 jitter **保留不动**。无「目标球瞄准点随机」残留（现状即锁顶球，非随机）。grep 证据：`breakJitter` 仅存于注释；`grep breakJitter *.swift` 无代码命中。
- **② 开放瞄准方向（G13 语义）**：`BreakFlowRunner` 新增 `aimDir`（默认锁顶球 `BreakSimulator.aimAtApex`）+ `aimManuallyAdjusted` 标志：拖屏（`onAimNudged`→`nudgeAim`）与左侧 `BTAimWheel`（`onNudge`→`nudgeAim`）共用入口，复用 V1 链路 `AngleSceneCalculator.aimNudgeDegrees`（绕母球公转增益、屏幕顺时针为正）+ `rotatedAim`（XZ 相对旋转，未改增益模型）。手势层（`AngleSceneView.handlePan`）「第一落点只选中不转向」由 V1 既有实现保证；命中母球=拖开球点、空白=调瞄准（互不干扰）。未手动调向时母球移动即重锁顶球（默认跟随），一旦手动调向则固定绝对方向。`drawAimLine` 改沿 `resolvedAim` 画（前段淡绿延伸库内边界 `rayToInnerRail` + 后段实线尾）。
- **③ 力度条默认 6 m/s 可调**：新增 `BreakFlowRunner.velocity`（`@Published`，默认常量 `defaultBreakVelocity = 6.0`，替代原固定 `breakPower = 7.0`）；右侧 `BTShotInstrumentColumn` 绑定 `$runner.velocity`（量程 `ShotTuning.velocityRange` 0.5…8.0，无打点盘=纯力度柱），`breakNow` 与运杆 `runCueStroke` 均用 `Float(velocity)`。单测断言默认 6.0（常量 + 实例）。
- **④ 布局遵循 ShotStageProxy 标准**：新增共享 `BreakInstrumentsOverlay(runner:proxy:)`——左瞄准轮走 `proxy.aimWheelFrame()`（右缘贴球桌左侧、底对齐 `controlBottomY`），右力度柱走 `proxy.instrumentFrame()`（左缘贴球桌右侧、同底）。内含 `ZStack(alignment:.topLeading).frame(maxWidth/height:.infinity)` 使 `.position` 与球桌矩形同坐标系（首版未包 ZStack 致两竖条错位 ~72pt，已修复为同底，截图核验）。仅 `.racked` 可调，computing/breaking/settled 期禁用+半透明。
- **⑤ 按钮互换 + 双条收敛单真源**：`FreePlayBreakBar` **删除**（grep 证据：仅注释），FreePlay 底部条改用共享 `BreakControlBar`。统一 `BreakControlBar`（四宿主唯一条）：`取消`（最左）/ `重开`（次级，恒显）/ 主按钮（最右：手动交付且停稳=`完成`，否则=`开球`）。相对旧 `FreePlayBreakBar` 布局 `完成`↔`重开` **位置互换**（完成移到最右主位、重开在其左；截图核验 settled 态）。
- **「换一局 vs 重开」定稿语义**：**合并为单一「重开」按钮**——`runner.reRack()`（换 seed 重摆 = 重新洗球号 + 重新扰动球堆间距 jitter，`.racked`/`.settled` 均可用）。原 `nextRack()` 删除（与 reRack 逐字节同逻辑）。「换一局」名不再出现（原自动交付宿主的「换一局」即此语义，仅改名+统一位置）。
- **⑥ 四宿主接线**：FreePlay（手动交付 manualDeliver）/ Silu（Q14，自动交付）/ PlanThree（Q15.4，自动交付）均已接开球仪表叠加 + 共享条 + 拖屏调瞄准，逻辑集中在 runner + 共享条，宿主只做叠加/路由（未复制开球逻辑）。**Composer（自由走位编排台）如实说明**：本页开球入口按既有设计（条 19.2「本页无开球」）为**禁用态**（`BTBreakSideButton(isEnabled:false)`），故开球模式 UI 不可达、无截图；已一并接好共享条/叠加/`onAimNudged` 路由（若未来启用即生效），是否放开开球入口属产品决策，未在本批范围内改动——**留待主控/用户确认**。
- **验证（真实输出）**：`make build` **BUILD SUCCEEDED**。单测 `BreakFlowRunnerV6Tests`（5 例：同 seed 球堆位置逐球一致、异 seed 扰动且受 `2·jitterRadius` 上界锁死、固定瞄准/力度/无塞两次开球确定性、改瞄准方向散局改变、默认 6 m/s 常量+实例）+ 回归 `RackGeneratorTests`（8 例）合计 **Executed 13 tests, 0 failures**；`BreakRackPhysicsTests` **Executed 10 tests, 0 failures**（含 jitter 不互穿/确定性/破对称/散开发散）。UI 测试 `S6_BreakUniversalUITests`（1 例，截图产物 `build/v6-shots/v6-{freeplay-break-racked,freeplay-break-settled,silu-break-racked,planthree-break-racked}.png`）+ 回归 `ScreenshotTourUITests.testB2ShotControls` 均 **TEST SUCCEEDED**。四宿主截图逐张核验：三开球宿主 racked 均左瞄准轮+右力度柱「大力 6.0」贴边同底、瞄准线沿可调方向、底部条取消/重开/开球；FreePlay settled 态取消/重开/完成（完成最右主位=按钮互换）、球堆已真实散开停稳。
- **存疑/偏差（如实）**：(a) UI 测试 destination 用 **iPhone 17 Pro**（任务指定机型，本机可用）；(b) 力度柱在开球模式无打点盘图标占顶部预留区，力度条**本体**较左瞄准轮略长但**底部严格对齐**（G5「同底」满足），如需两竖条等长可后续补顶部占位（不影响功能）；(c) autoDeliver（Silu/PlanThree）散开→停稳→**自动交付** loadBoard 路径为 V6 未改动的既有代码，由 `BreakSimulator` 停稳单测 + FreePlay 手动 settled 截图（散开动画已跑完停稳）间接覆盖，无专门 UI 用例端到端驱动自动交付（既有测试缺口，非本批引入）；(d) Composer 开球禁用见 ⑥；(e) 真机手感（拖屏调瞄准细腻度）未上机，建议冒烟一次。

### V7 打一走二想三（Q15.1 + Q15.2 + Q15.3）

**范围**：`SolveRegion.sector` 扇形 SDF + 求解接入；扇形默认选区/自选降灰/删除恢复状态机；<3 球降级求解与终局；清除键尺寸位置（打三+思路两页；防守页随 V8）。
**量级**：L。**依赖**：V6（开球已通用化，本批不碰开球）；几何任务先读 skill。
**DoD**：扇形 SDF 单测（内/外/弧/角边界）；无自选约束直接求解可行（解落扇形内）；自选→灰、删除→恢复截图核验；2 球/1 球局面可解可打、清台终局提示；清除键正常尺寸在摆球右侧截图；build + `PositionPlaySolverTests` 回归绿。

**执行结论（2026-07-13，执行智能体，✅ 已验证）**：
- **坐标契约回显（钉死后落码）**：SDF 全程在 **SceneKit 世界系 X–Z 水平面、Y 朝上、单位米**（与既有 rect/circle/point SDF 同系）；bearing = `atan2(z, x)`（与 `AngleSceneCalculator.rotatedAim`/`bearingDeg`/`snookerCoverage` 同口径）。扇形顶点存**归一化**（经 `normalizedToScene` 转场景，仅平移+均匀缩放 ×2.54、无旋转/镜像 ⇒ 场景 bearing 与存储角区间一致）；半径带存**米**（sMin=0.11/sMax=0.55 物理停球距离，不乘 sceneScale）。**doc/code 已知分歧**（table-geometry.md 归一化 Y↔SceneKit Z 反向 vs 代码 `normalizedToScene` 同向）：SDF 全程留在场景系、不混用归一化 Y，故不受影响；以代码为准。
- **改动文件清单**：
  - `QiuJi/Core/PositionPlay/PositionPlaySolverModels.swift`（`SolveRegion` 新增 `.sector` case + `SectorAngleInterval` + `annularSectorSDF`/`arcDistXZ`/`angleWithin`/`pointSegmentDistXZ`；`centerNormalized`/`isSector` 扩展）；
  - `QiuJi/Features/PositionPlay/ViewModels/PlanThreeViewModel.swift`（`PlanThreeSectorSolver.defaultRegion` + `preferredSide` 抽取；VM `sectorRegion`/`canPotOnly`/`potOnlyRegion`/`canSolve`/`sectorIsDefaultRegion`/`objectBallCount`；`currentConstraint` 扇形默认+pot-only 兜底；`hintForState`/`advanceAndArm`/`invalidateSolutions` 文案；`drawSector`/`addSector` 降灰；`finishStrike` 清台终局；UITest 钩子 `uiTestConfigure`）；
  - `QiuJi/Features/PositionPlay/Views/PlanThreeView.swift`（求解按钮 `!hasConstraint`→`!canSolve`；清除键移到「摆球」右侧用 `BTEraserButton`；UITest onAppear 钩子）；
  - `QiuJi/Features/PositionPlay/Views/SiluTrainerView.swift`（清除键同款移位 `BTEraserButton`）；
  - `QiuJi/Core/Components/BTEraserButton.swift`（**新增**，正常尺寸清除键，两页共用）；
  - `QiuJi/App/RootView.swift`（UITest 深链——直接进反解页取证，launch arg 门控）；
  - `QiuJiTests/PlanThreeSectorSolverTests.swift`（**新增** 6 例）、`QiuJiUITests/S7_PlanThreeV7UITests.swift`（**新增** 6 例深链截图）、`QiuJiUITests/Helpers/XCUIApplication+Extensions.swift`（`launchClean(extraArgs:)` 重载）。
- **Q15.1 扇形 SDF 数学定义**：环形扇区 = 共享顶点 A（②假想球 `t2 − 2R·u`，u=②→②有效进球点单位向量）+ 半径带 `[rMin, rMax]` + 角区间 `[lo, hi]`（bearing = `atan2(u.z,u.x)+π ± [5°,20°]`；无③两侧两区间、有③朝③单区间）。`signedDistanceMeters` = 点到**四条真实边界**（内弧 rMin、外弧 rMax、两条径向线段）的最近距离，区内取负；多区间取 `min`（并集）。**禁外接矩形近似**——直接解析环形扇区边界。
- **数值草稿验证**（`/tmp/sector_sdf.py`，与暴力最近点采样对照，误差 ~1e-8）+ 单测金标准样本（apex=场景原点、[0.11,0.55]m、[−175°,−160°]，中轴 −167.5°）：内部中轴 r=0.33 ⇒ −0.043074（=−min(0.22,0.22,0.33·sin7.5°) 手算）；近内弧 r=0.12 ⇒ −0.01；近外弧 r=0.54 ⇒ −0.01；内孔 r=0.05 ⇒ +0.06；外圈 r=0.70 ⇒ +0.15；角外(+30°) ⇒ +0.126289；内/外弧上、径向边上 ⇒ ≈0。两侧并集：±侧中轴均在区内、中间 <5° 死区在区外。
- **Q15.1 状态机**：② 就绪且无自选约束 ⇒ 扇形即默认落区（`sectorIsDefaultRegion=true`、高亮青、`求解` 启用、`currentConstraint` 返回 `.restRegion(sector)`）；用户画落区/落点/过点 ⇒ 扇形降级为灰参考、求解改用用户约束；删除自选 ⇒ 扇形恢复默认。单测 `test_sectorDefault_dimmedByDraft_restoredOnClear` + 截图 `v7-sector-default`（高亮）/`v7-sector-dimmed`（灰 + 用户矩形）核验。
- **Q15.2 <3 球终局**：新增 `canPotOnly`（①+①袋 且目标球 ≤1）⇒ `currentConstraint` 走全台面落区 pot-only（1 球「只打进」）；2 球「打一走二」用扇形（②+②袋）。`finishStrike` 检测 `objectBallCount==0` ⇒ 清台终局提示。单测 `test_potOnly_singleObjectBall_solvableAndEndgamePrompt` + 截图 `v7-oneball-potonly`（「台面仅剩此球·点求解直接打进」，求解启用）/`v7-endgame-cleared`（「清台完成 🎉·用恢复默认重开一局」）。
- **Q15.3 清除键**：抽为共享 `BTEraserButton`（42×32 圆角按钮），放「摆球」chip 右侧；打三 + 思路两页同布局同改（防守页随 V8）。截图 `v7-planthree-clearkey`/`v7-silu-clearkey` 核验。
- **求解器改动**：`PositionPlaySolver` **零改动**——`.sector` 经 `SolveRegion.signedDistanceMeters` 通用消费（`scoreRegionCell`/`solveRestRegion` 走非落点分支），自动接入评分/排序/降级。
- **验证（真实输出，主控独立复跑，2026-07-13 会话中断后恢复验收）**：`make build` **BUILD SUCCEEDED**。单测 `PlanThreeSectorSolverTests`（6 例）+ 回归 `PositionPlaySolverTests`（13 例）+ `PositionPlayUndoSnapshotTests`（3 例）合计 **Executed 22 tests, 0 failures**；UI 回归 `S2_ShotPagesLayoutUITests` 全 6 用例（PlayEntriesOrder/ShotSimulation/Composer/Silu/PlanThree/Snooker）分两轮复跑**全部 passed**；截图取证 `S7_PlanThreeV7UITests` **5 用例全套 passed**（ClearKeyLayout / SectorDefault / SectorDimmed / OneBallPotOnly / ClearedEndgame）；产物 `build/v7-shots/v7-*.png` 六张主控逐张读图核验。
- **返工 1 轮（仅测试取证层）**：首版 `testV7ClearKeyLayout` 确定性失败（进求解页后 TabBar 不可达，二次 `switchTab` 报 No matches for TabBar）+ 打三截图拍在过渡泛白帧。返工后两页各独立 launch 深链取证、snap 前等「摆球」chip existence + 2s，用例 passed、截图清晰。产品代码零改动。
- **DoD 逐条**：①扇形 SDF 单测（内/外/弧/角边界 + 数值草稿手算对照）✅；②无自选约束直接求解可行、满足约束解停点在扇形真实几何内（`test_sectorRegion_satisfiedSolutionsRestInsideSector` 不变量 + `v7-sector-default` 高亮扇形 + 求解启用）✅；③自选→灰、删除→恢复（单测 + 截图）✅；④2 球扇形/1 球 pot-only 可解、清台终局提示（单测 + 截图）✅；⑤清除键正常尺寸在摆球右侧（打三+思路截图）✅；⑥`make build` SUCCEEDED、`PositionPlaySolverTests`/`PositionPlayUndoSnapshotTests`/S2 回归全绿 ✅。
- **存疑/偏差（如实）**：(a) DoD#2「存在满足扇形约束的解」依赖具体盘面物理，单测只钉死「满足解 ⇒ 停点在扇形内」的数学不变量，"存在"由真机深链求解截图 `v7-sector-default`（高亮扇形 + 求解可点）取证——未在单测里强断言某盘必出满足解（避免脆弱）。(b) 「击打→停稳→清台」的完整**动画链路**（依赖 SCNAction 运杆+回放，非单测/无头友好）未端到端自动驱动；清台**终局状态与文案**由 `finishStrike` 逻辑 + `uiTestConfigure("cleared")`（等价「打进最后一颗、母球留台」）截图取证，实际击打入袋动画建议真机冒烟一次。(c) UI 测试机型用 **iPhone 17 Pro**（任务指定，本机可用）；深链取证绕过 Tab 导航（本机冷启动 Tab 导航偶发卡顿，与本批改动无关）。(d) 快照结构 `SolveShotSnapshot`/`SolveConstraintDraft` **未改**——本批新增字段均为 VM 计算态（`sectorRegion`/`canPotOnly` 等）或场景可重算量，不进快照，V3 undo 全量恢复回归绿。

### V8 防守（Q16 + G17/Q15.3 在本页落地）

**范围**：改名+中八规则语义重做+对方球组可见性求解+8 号目标拦截+难度谱系解；同批带上一杆全量恢复、清除键新规范。
**量级**：L（本轮最大单批，必要时拆 V8a 求解层 / V8b 页面层）。**依赖**：无硬依赖（建议在 V3 后，直接按新上一杆标准实现）。
**DoD**：规则单测（双组在桌禁 8 号目标/对方球组推断/只剩 8 号防 8 号）；`snookerCoverage` 多球扩展单测；典型盘面求解截图（完全斯诺克解 + 无完全解时高难度可行解 + 诚实无解）；改名后旧名 grep 清零；上一杆全量恢复实测；build + `SnookerSolverTests` 重写后全绿。

**执行结论（2026-07-13，plan-delegated-execution：opus-4.8 执行子智能体 + 主控独立验收，0 轮返工；坐标契约：SceneKit X–Z 水平面、Y 朝上、米，水平角 `atan2(z,x)` 与既有 `snookerCoverage` 同口径）**：
- **改动文件清单**：`AngleSceneCalculator.swift`（新增 `snookerCoverageMulti` 多遮挡球取最优 + `DefenseBallCoverage`/`defenseCoverage` 多球联合可见性 + `opponentPottingDifficulty` 对手进球难度 + 权重常量）；`PositionPlaySolver.swift`（`solveSnooker` 签名 `blockerKey → opponentKeys: [String]`，新增 `DefenseScore`/`defenseScore`，三阶段扫描与排序改防守优劣，完全解/高难度可行解/诚实无解三分支）；`SnookerTacticsViewModel.swift`（去二元障碍模型→单目标；`opponentKeys`/`canTarget` 中八推断与 8 号拦截；G17 `UndoContext`+`makeUndoContext`+全量 `undoLastShot`；UITest 钩子）；`SnookerTacticsView.swift`（改名「防守」、chip 行=目标球/摆球+`BTEraserButton`）；`AngleHomeView.swift`（入口卡 斯→防）；`RootView.swift`（`-deeplink.snooker` 深链）；测试：重写 `SnookerSolverTests`(6)、`PositionPlayUndoSnapshotTests` +1 防守往返、`SolverPerformanceTests` 签名同步、S2/巡游改名断言、新增 `S8_SnookerV8UITests`(4)。
- **中八规则语义**：用户只选目标球；`opponentKeys(for:onTable:)` 推断对方球组（目标全色 1–7 ⇒ 对方=在台花色 9–15，反之亦然；对方组清空 ⇒ 防 8 号）；`canTarget` 拦截=双组在桌禁 8 号（选球点击+摆球变更双处 guard + 提示），单组在桌 8 号可选（对齐 `legalTargetKeys` 首触语义）。
- **解的谱系**：硬约束=①进不进袋不要求但母球不进袋、②母球真停稳、③首触目标球合法；`satisfiesConstraint = 对方球组全部挡死`（完全斯诺克）；无完全解 ⇒ 输出「高难度可行解（未完全斯诺克）· 挡死 m/n」（诚实标注不满足）；对方球全遮首触线等真无解 ⇒ 「未找到可行防守解」+ 移动建议。
- **评分权重 v1（待实测调优，集中在 `AngleSceneCalculator` 常量单点可调）**：每颗对方球——被挡死记难度 1.0；未挡死 `d = 0.6·(bestCut/90°) + 0.4·min(1, dist/2.54m)`，无可行袋记 1.0。聚合排序 maximin 防守：挡死球数↑ → 最弱环难度↑ → 难度和↑ → 覆盖余量和↑ → 执行难度↓（复用 `DifficultyModel.executionEffort`）→ 力度↓。理由：防守强度取决于对手最好的机会；切角/长台是对手误差的一阶来源。
- **G17/Q15.3 落地**：`undoLastShot` 复用 V3 `SolveShotSnapshot` + 页面 `UndoContext`（携带防守目标球选择模型），全量恢复不重解；清除键 `BTEraserButton` 在「摆球」右侧。
- **验证（真实输出，主控独立复跑）**：`make build` **BUILD SUCCEEDED**；`SnookerSolverTests`(6)+`PositionPlaySolverTests`(13)+`PositionPlayUndoSnapshotTests`(4)+`PlanThreeSectorSolverTests`(6) 合计 **Executed 29 tests, 0 failures**；`S8_SnookerV8UITests` 4/4 + `S2 testSnookerLayout` 全 passed；grep `"…做斯诺克…"` 用户可见字符串清零；截图 `docs/ui-polish/screenshots-20260713/v8/` 四张主控逐张读图核验（完全斯诺克/高难度可行解/诚实无解/清除键）。
- **存疑/待办（如实）**：评分权重 0.6/0.4 为 v1 未经真机标定；击打→上一杆动画链路建议真机冒烟；UITest 机型 iPhone 17 Pro。

### V9 翻袋/反射页统一（Q17 + Q18 + G17 在两页落地）

**范围**：两页九项逐条（标题/解描述上移、按钮列重排、下一解左移、演示态裁剪、球库补白黑、三点设置+网格）；上一杆全量恢复。
**量级**：L。**依赖**：V1（G14 求解去抖已全局）、V3（G17 标准）。
**DoD**：两页逐项截图核验（对照上表 9 行）；解/无解文案在标题下；球库 16 球位；网格 Toggle 生效；上一杆全量恢复实测；build + `captureSolverStates` UI 测试更新后全绿。

**执行结论（2026-07-13，执行智能体，0 轮返工，✅ 已验证）**：
- **共享抽取（避免双真源）**：两页导航件抽入 `QiuJi/Core/Components/BTShotPageChrome.swift`——`BTSolverNavStatus`（principal 品牌绿 14pt 标题 + 11pt 副标题）+ `BTSolverMoreMenu`（三点菜单：原理说明 + `BTTableGridMenuToggle` + 恢复默认）。两 VM 各加 `statusText`/`noSolutionText`/`SolveUndoContext`/`makeSolveUndo`/`restoreSolve`/`undoSolveShot`/`replaySolveShot`/`pulsePaletteBall`（镜像同构）。
- **改动文件清单**：`BankShotView.swift`、`DiamondSystemView.swift`、`BankShotViewModel.swift`、`DiamondSystemViewModel.swift`、`Core/Components/BTShotPageChrome.swift`（+共享件）、`QiuJiTests/PositionPlayUndoSnapshotTests.swift`（+2 例）、`QiuJiUITests/ScreenshotTourUITests.swift`（`captureSolverStates` 按新 UI 更新）。
- **17.1/17.2/17.7**：删球桌左下 overlay pill（含 `infoPill`/`demoPill`/`solutionPill`/`noSolutionPill`/`freeContactPill` 等死码），解描述/无解/求解中/自由首碰统一入 principal 副标题（VM `statusText`），标题品牌绿 14pt + 副标题 11pt（同三解页 `SnookerTacticsView`）。翻袋无解仍含「暂无翻袋解」子串（UI 测试判定）。
- **17.3 定稿 = 演示不可中断**：删「演示中」pill + 「停止」钮 + 「重置」钮；演示期（`isPlaying`）动作列禁用（击打标题→「击球中」）、顶栏/球库/仪表柱锁定不变；「重置」移入三点菜单「恢复默认」（`vm.reset()`）。
- **17.4**：求解态「下一解」移到球桌左下（`nextSolutionButton`，对应自由态「恢复球形」位置带 `bottomLeadingFrame`）。
- **17.5**：求解模式右列收敛为 击打/上一杆/回放（复用自由态同款 `BTShotActionColumn`）。「上一杆」按 G17 全量恢复——页面 `SolveUndoContext` 携带 `board`(场景系球位)/`pocket`(翻袋)/`cushions`/`solutions`(解集缓存)/`currentIndex`/`power`；`restoreSolve` 抑制 `reflectionPower.didSet` 重求解（`isRestoringSolve`）、原样回填解集，`isSolving` 恒 false（免重解）；捕获逻辑 `makeSolveUndo()` 由 `strike()`/`replaySolveShot()`/单测共用（单一真源）。
- **17.6 定稿 = 按现 G5 校验（不在求解模式加瞄准条）**：求解模式的瞄准是 `BankKickSolvePipeline` 反解的**输出**，非用户输入；加可调 `BTAimWheel` 需把瞄准喂进求解，属改求解逻辑（违「明确不做」）且与「解=引擎全枚举最优」语义冲突。故求解模式保持右侧纯力度柱；自由模式左 `BTAimWheel` + 右力度柱由 `ShotStageProxy.aimWheelFrame()`/`instrumentFrame()`（G4/G5）已同底等长，截图 `r02-bankshot-free-default` 核验两竖条底部对齐。
- **17.8**：球库渲染改 `PositionPlayBall.allKeys`（全 16）、列数 7→8（2×8）；母球（`cueBall`）与黑 8（`_8`）为固定在桌球 → 球库位灰显（opacity 0.3）、`.subviews` 禁拖、点击走 `pulsePaletteBall`（脉冲桌上对应球）；障碍球增删逻辑不变（`paletteKeys` 仍为 14 障碍键）。
- **17.9（G19）**：`info.circle` 直开原理 sheet → `ellipsis.circle` 三点菜单（原理说明入口 + 台面网格 4×8 Toggle + 恢复默认）；演示期禁用菜单。
- **G17 类型取舍（如实）**：翻袋/反射引擎解为 `BankEngineSolution`/`KickEngineSolution`（含 `ShotPrediction`），与共享 `SolveShotSnapshot`（`BoardSnapshot` 归一化 + `[PositionPlaySolution]` + `PlannedShot`）类型不兼容；按方案「若上一杆需要缓存解集，用 VM 层已有 solutions 即可」以本页原生类型建 `SolveUndoContext`，`SolveShotSnapshot` 结构未改（符合明确不做）。因求解态「击打」是演示（结束复位球形），「上一杆」在球形层常为幂等，其价值在于还原求解选择（袋口/库数/解索引/力度）+ 解集缓存——如实标注此语义。
- **验证（真实输出）**：`make build` **BUILD SUCCEEDED**；单测 `PositionPlayUndoSnapshotTests`（新增翻袋/反射各 1 例：真实反解→`makeSolveUndo` 捕获→`reset` 抹除→`restoreSolve` 恢复，逐字段〔球形场景系 x/z、袋口、库数、解集计数、当前档位、力度〕一致且 `isSolving==false`）**Executed 6 tests, 0 failures**；回归 `BankKickDifficultyTests` **10/10, 0 failures**；UI `ScreenshotTourUITests.testBankShotRealMode` + `testReflectionRealMode` **TEST SUCCEEDED**。截图逐张读图核验（`docs/ui-polish/screenshots-latest/light/`）：`r0{1,2}-*-default`（principal 标题+副标题解读数、16 球库白/黑灰显、下一解左移、右列击打/上一杆/回放）、`r02-bankshot-strike-playing`（副标题「演示中…」、无停止钮）、`r02-bankshot-grid-on`（4×8 网格上屏）、`r02-bankshot-solve-undo`（解/球形/参数全量恢复、上一杆消费后置灰）、`r02-bankshot-free-default`（自由态瞄准轮+力度柱同底）。
- **存疑/偏差（如实）**：(a) 真机手感（拖球/演示细腻度）未上机，建议冒烟一次；(b) `stopStrike()` VM 方法保留但已无 UI 调用（无害死码，未删以降 churn）；(c) UI 测试机型 iPhone 17 Pro；(d) 求解态「上一杆」球形幂等性见上「G17 类型取舍」，为演示语义的固有结果，非缺陷。

### V10 动作库与试打（Q19）

**范围**：侧栏点击回组顶；试打球数不含白球、标题居中复核、i 并入三点、**序列模式**（数据通路补 steps + 播放状态机 + 三模式切换 + 默认序列）。
**量级**：L。**依赖**：V2（G19 三点口径）；19.2.5 半句**先向用户澄清再实现**，未澄清则该子项跳过并留档。
**DoD**：侧栏重复点击回顶录屏；球数断言（多球局 onTable−1）；序列模式截图（打点盘/力度条/瞄准条隐藏、逐杆信息呈现、点击击打走完全序列）；进袋/自由模式回归不破坏；`DrillTryoutUITests` 扩展后全绿；build 绿。

**执行结论（2026-07-13，执行智能体，0 轮返工，✅ 已验证）**：
- **改动文件清单**：
  - `QiuJi/Features/DrillLibrary/Views/DrillListView.swift`（Q19.1：`ScrollViewReader` + 顶部锚点 `drillGridTop`，侧栏点击/重复点击自增 `scrollToken` → 立即滚 + 去抖后补滚回组顶）；
  - `QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift`（Q19.2①：选球形副标题球数 `initial.onTable.count` → `formation.objectBallCount`，排除母球）；
  - `QiuJi/Core/PositionPlay/DrillTryoutBoardStore.swift`（`DrillTryoutFormation` 新增 `steps: [SequenceStep]`（序列数据通路）+ `objectBallCount`/`hasSequence`；`formations()` 回填 `sequence.steps`）；
  - `QiuJi/Features/PositionPlay/ViewModels/PositionPlayViewModel.swift`（Q19.2④ 序列模式状态机：`isSequenceMode`/`isSequencePlaying`/`sequenceStepIndex`/`sequenceFinished`/`configureSequence`/`enterSequenceMode`/`exitSequenceMode`/`restartSequence`/`presentSequenceStep`/`playSequence`/逐杆 `runSequenceStep`+`runSequencePlayback`+`applySequenceRest`+`scheduleNextSequenceStep`+`finishSequencePlayback`；`recompute`/`placeFromPalette` 加 `isSequenceMode` 拦截）；
  - `QiuJi/Features/PositionPlay/Views/PositionPlayComposerView.swift`（Q19.2③ 删 `briefInfoButton`、说明卡召回入 `moreMenu`「试打说明」；Q19.2④ 三模式选择器（序列/进袋/自由）+ 当前杆信息条 + 序列模式隐藏打点盘/力度条/瞄准条、单「击打」列、台面只读、重摆=重演；onAppear 有序列默认进序列模式，无序列降级自由）；
  - 测试：`QiuJiTests/DrillTryoutBoardStoreTests.swift`（+Q19.2① objectBallCount + steps 通路断言）、`QiuJiUITests/DrillTryoutUITests.swift`（+`testSidebarTapScrollsToGroupTop`、+`testTryoutSequenceModeSwitching`，改写 `testTryoutC042Flow` 走序列默认 + 菜单召回说明卡）。
- **Q19.1 侧栏回顶**：`ScrollViewReader` 包裹右侧内容 `ScrollView`，`LazyVStack` 顶插零高锚点 `.id("drillGridTop")`；侧栏每次点击（含重复点击当前分组）`scrollToken &+= 1`，`.onChange(scrollToken)` 立即滚 + 去抖（VM 300ms）后 0.36s 补滚一次（覆盖同分组内容不变 与 切分组内容重建两情形）。UI 测试 `testSidebarTapScrollsToGroupTop`：记录首卡纵向位置 → 下滚 3 屏 → 重复点「全部」→ 断言首卡回原位（accuracy 40）+ 截图 `q191-01/02/03`。
- **Q19.2① 球数不含白球**：`DrillTryoutFormation.objectBallCount = 初始在桌键 filter 非母球 count`；选球形副标题改用它。单测 `test_c042_…`：球形1 = 6 球（含母球）⇒ objectBallCount = 5。
- **Q19.2② 标题居中（复核结论：无偏差）**：Composer `navStatus`（principal）本就居中；截图复核试打页标题（`初级蛇彩走位`/`软打控位`）在返回钮 + 三点之间对称居中、副标题居中，长名 `lineLimit(1)` 截断不挤偏——**未改布局**（如实：截图复核无偏差）。
- **Q19.2③ i 并入三点**：删 `briefInfoButton` toolbar 项；`moreMenu` 试打态加「试打说明」项（`accessibilityIdentifier("tryout.info")`）召回说明卡（G19 口径，与 V9 解球页三点收敛同型）。UI 测试改走「三点菜单 → 试打说明」召回。
- **Q19.2④ 序列模式（默认）**：
  - **数据通路补法**：`DrillTryoutFormation` 新增 `steps: [SequenceStep]`（store 从 `PositionPlaySequence.steps` 原样回填）；Composer onAppear 有 steps ⇒ `vm.configureSequence(steps)` + `vm.enterSequenceMode()`。
  - **状态机（复用既有回放骨架，不造第二套播放器）**：逐杆用既有 `PositionPlayShotSolver.solve(before:shot:)` 求预测 + `scene.runCueStroke`（运杆）+ `TrajectoryPlayback`（球体回放，收尾 `playback.duration` 遵 G15）；一次「击打」`playSequence` 从当前杆连播到末杆，杆间 `Task.sleep(0.7s)` 停顿；`presentSequenceStep` 静态预览当前杆轨迹/瞄准/假想球（复用 `apply(pred)`）。序列模式 `recompute`/球库摆球被拦截（台面只读）；隐藏打点盘/力度条/瞄准条，右列仅单「击打」（演示中禁用），左下「重摆球形」=从头重演。
  - **降级策略（自定夺并留档）**：无 `steps` 的 drill（无 DrillBoards 序列，走 `DrillBoardBuilder` shotIntent 兜底；或单序列 0 杆）**隐藏「序列」模式**、默认回既有「自由」模式（保持既有行为最小意外）；有 steps 的 drill 默认「序列」。三模式选择器仅在 `isTryout && vm.hasSequence` 出现。
  - 取证：`testTryoutSequenceModeSwitching`（默认序列信息条在位 → 切进袋恢复常规击球列 → 切自由 → 切回序列信息条恢复）+ `testTryoutC042Flow`（默认序列、点击打逐杆演示：截图 `t05a` 播放中「演示中」/球运动、`t05b` 逐杆推进多球入袋、`t06` 重演）。
- **Q19.2.5 明确跳过并留档**：v4 原文「在进袋模式下，默认使用」半句未写完、用户未补充——本批**不实现任何猜测语义**，进袋模式保持现状（选目标球 + 袋口闭环求解、常规击球/上一杆/回放），登记进 V11 前置待确认清单。
- **验证（真实输出，本机 iPhone 17 Pro，derivedData build/DerivedData）**：`make build` **BUILD SUCCEEDED**；单测 `DrillTryoutBoardStoreTests` **Executed 8 tests, 0 failures**；UI 测试 `DrillTryoutUITests`（6 例：Sidebar回顶 / C042序列流 / 序列模式切换 / Premium锁 / 首次手势提示 / 暗色入口）**Executed 6 tests, 0 failures**；回归 `S2_ShotPagesLayoutUITests.testComposerLayout`（非试打编排台）**passed**。
- **存疑/偏差（如实）**：(a) `testTryoutPremiumLocked` 曾一度失败——排查为**本机模拟器残留 StoreKit Pro 授权**（isPremium=true 使 c050 未锁），与本批改动无关；`simctl erase` 清 StoreKit 后该用例通过（副作用：erase 重置了 onboarding 持久标记，已用 `simctl spawn defaults write com.xinkuan.qiuji hasCompletedOnboarding YES` 恢复测试环境）。(b) DoD#3「序列走完帧」取证 `t05b` 为第 4/5 杆推进帧（蛇彩 5 杆总时长 > 测试等待窗），完整终帧由 `finishSequencePlayback` 逻辑 + 「序列演示完成」副标题保证，逐杆多球入袋进程已截图可见；未额外拉长测试等待。(c) 真机手感（逐杆衔接节奏、0.7s 停顿）未上机，建议冒烟一次。(d) 序列模式逐杆用**实时引擎求解**呈现（非直接读存档 after），与出片渲染同源同引擎，个别杆若新引擎不可行则跳过该杆动画直接落位（不阻断整条演示）。

### V11 总验收

**范围**：对照本文件 G13–G19 + Q1–Q19 逐条核验；全量 `QiuJiTests` + 关键 UI 测试；截图巡检全部涉改页面；回写 `tasks/UI-IMPLEMENTATION-SPEC.md`（交互规范：瞄准相对调整/0.5s 求解去抖/开球规范/上一杆语义）、`tasks/PROGRESS.md`、`tasks/phases/P18-release-convergence.md`（或新批号）、Project Hub 状态卡。
**DoD**：验收清单逐条 ✅ 或注明偏差原因；文档回写完成。

**执行结论（2026-07-13，主控独立验收 + 文档回写执行智能体，✅ 已完成）**：
- **全量单测（主控实测）**：全量 `QiuJiTests` **Executed 560 tests, 2 skipped, 0 failures**（903s）。
- **关键 UI 套件（主控实测）**：S1 / S2(6) / S5(5) / S5-AimPoint(2) / S6 / S7(5) / S8(4) / ScreenshotTour（bank+reflection）/ DrillTryout(6) **全部 passed**。
- **clean-rebuild 事件（如实记录）**：验收期间发现 3 个用例（S1 / S6 / S5-3D）失败，`sample` 定位为**陈旧增量构建产物**导致的 SwiftUI `_ConditionalContent` 析构 SIGSEGV；`xcodebuild clean` 全量重建后全部通过——**非代码回归**（增量构建缓存问题）。
- **G13–G19 + Q1–Q19 逐条**：由 V1–V10 各批执行结论逐条落地并验收（见各批段落），无未落地条目；4 项遗留（真机冒烟、V8 权重调优、Q19.2.5 半句、V6 Composer 开球入口、UI 测试环境依赖）转入 §六待确认 / P18 §B3.8 遗留清单。
- **文档回写**：`tasks/UI-IMPLEMENTATION-SPEC.md` 新增 §8.9「瞄准与求解交互规范（问题集合 v5 定稿）」+ §8.8 词表增补页面改名 + Changelog（2026-07-13 行）；`tasks/phases/P18-release-convergence.md` 新增 §B3.8 批次表 + V11 结论 + 遗留清单；`tasks/PROGRESS.md` 顶部追加 V11 总验收条目；本文件状态行 + 本段 + 版本表 v5.1。

---

## 五、锚点索引（全部读代码核实，2026-07-13）

| 条目 | 文件 | 符号/行号 |
|---|---|---|
| Q1 | `QiuJi/Features/AngleTraining/Views/AngleHomeView.swift` / `QiuJi/Features/Training/Views/TrainingHomeView.swift` / `PlanListView.swift` / `QiuJi/Core/Components/BTDrillCard.swift` / `QiuJi/Core/DesignSystem/Typography.swift` | `AngleGridCard` ~L439 `.btHeadline`；`planPosterCard` ~L372 与 `PlanCard` ~L341 硬编码 15pt bold；`btHeadline` = 17pt semibold |
| Q2 | `QiuJi/Features/AngleTraining/Views/AngleDynamicView.swift` | `.navigationTitle` L47；入口卡 `AngleHomeView.learnEntries` L110–112 |
| Q3 | `QiuJi/Features/AngleTraining/Views/ContactPointTableView.swift` | `aimFigure` L120–184，标签 `BTFigureTag` L178–181，无避让逻辑 |
| Q4 | `QiuJi/Features/AngleTraining/Views/GeometricAngleQuizView.swift` | `AnglePredictionFigure` L312–317（vertex 0.82h / rayLen 0.82h ⇒ 90° 球心 y=0 被 clip） |
| Q5/Q9 相机 | `QiuJi/Core/Scene/CameraRig.swift` / `AimingCameraConfig.swift` / `AngleSceneView.swift` | `enterAiming` L222–242 目标 zoom=0；zoom 梯 `AimingCameraConfig` L11–51；`handlePan` cameraControl 分支 L428–448 |
| Q6 | `QiuJi/Features/AngleTraining/Views/AimPointTrainingView.swift` | 线宽 `proj.lineMainWidth` L347/L356；红点 L358–366 |
| Q7 | `QiuJi/Features/AngleTraining/Views/AimPointSceneTrainingView.swift` | `aimLineEnd` 2R 捕捉 L312–336；`submit` 3s L175–180；无球杆（`strike()` L185–218）；布局无 `ShotStageProxy`；`interactionMode .tapsOnly` L406 |
| G13/G14 | `QiuJi/Core/Scene/AngleSceneView.swift` / `QiuJi/Features/PositionPlay/ViewModels/PositionPlayViewModel.swift` / `BankShotViewModel.swift` / `DiamondSystemViewModel.swift` / `QiuJi/Core/Components/BTAimWheel.swift` | `handlePan` L353–426 绝对指向；`setFreeAim(toward:)` L489–496；`recompute` 20ms 去抖 L644–668；翻袋求解 120ms L714–715；wheel 无节流 L14–82 |
| G15 | `QiuJi/Core/Physics/TrajectoryPlayback.swift` / `QiuJi/Core/Media/SequenceVideoExporter.swift` | `perceptibleSettleTime(speedThreshold: 0.07)` L441–458；`stateAt` 置零 L245–252；引擎 0.001 静止判定保留（`EngineNumerics` L117–140） |
| G16 | `QiuJi/Core/Components/BTSpinPad.swift` | `inset = 5` L19–37；`BTSpinPadCard` 104×104 L246–268 |
| G17 | `QiuJi/Core/PositionPlay/PositionPlayModels.swift` / 各 VM | `BoardSnapshot` L43 / `PlannedShot` L77；基准 `restoreShotParams` L1240–1256；思路 L764 / 打三 L999 / 斯诺克 L569 / 翻袋 L570 仅球位 |
| G18 | `QiuJi/Core/Rack/BreakFlowRunner.swift` / `RackLayout.swift` / `FreePlayView.swift` | `breakPower=7.0` / `breakJitter` L283–287 / `drawAimLine` 锁顶球 L128–157 / `BreakControlBar` L307–357；`FreePlayBreakBar` L521–592；球堆 jitter `RackLayout` L76–129 |
| G19 | `AngleDynamicView.swift` L57 / `SceneAimingView.swift` L77 / `CustomPlanBuilderView.swift` L233 / `BTExerciseRow.swift` L96 / `ProfileView.swift` L361 | 残留 gearshape 清单 |
| Q15.1 | `QiuJi/Features/PositionPlay/ViewModels/PlanThreeViewModel.swift` / `QiuJi/Core/PositionPlay/PositionPlaySolverModels.swift` / `PositionPlaySolver.swift` | `PlanThreeSectorSolver` L20–114（不进求解）；`SolveRegion.signedDistanceMeters` L25–89；`solveRestRegion` L516–676；无外接矩形转换 |
| Q15.2 | `PlanThreeViewModel.swift` | `solve` L544–579 / `canStrike` L189–192 / `rollWindow`；无球数 guard，但角色流程未支持 <3 球 |
| Q15.3 | `PlanThreeView.swift` L141–176 / `SiluTrainerView.swift` L166–173 / `SnookerTacticsView.swift` L130–138 | 顶栏小 eraser |
| Q16 | `QiuJi/Features/SnookerTactics/`（View/VM）/ `PositionPlaySolver.solveSnooker` L1176+ / `AngleSceneCalculator.snookerCoverage` | 二元目标+障碍模型；无中八规则 |
| Q17/Q18 | `BankShotView.swift` / `DiamondSystemView.swift` | pill 左下 L158–407；动作列 L206–244；`paletteKeys` 滤 `_8`（VM L80）；i=原理 sheet L62–67；网格组件 `BTShotPageChrome.BTTableGridMenuToggle` L15–28 |
| Q19.1 | `QiuJi/Features/DrillLibrary/Views/DrillListView.swift` | `categorySidebar` L88–112，无滚动控制 |
| Q19.2 | `DrillDetailView.swift` L171（球数含母球）/ `PositionPlayComposerView.swift`（`briefInfoButton` L115–117、principal L355–370、试打默认 `.free` L153）/ `DrillTryoutBoardStore.swift`（`DrillTryoutFormation` 无 steps） | 序列模式数据缺口已核实 |

---

## 六、待用户确认项（不阻塞开工，涉及批执行前确认）

1. **Q19.2.5**：v4 原文「在进袋模式下，默认使用」未写完——V10 已执行但**该子项明确跳过并留档**（进袋模式保持现状），待用户补充后另起小批实现，转入 V11 前置待确认。
2. **Q1 目标字号**：训练计划卡（15pt bold）与练习/动作库卡（17pt `.btHeadline`）本就不同，按「统一到 `.btHeadline`」执行；如本意是全部改 15pt，V4 批看截图时指出即可。
3. **Q16 防守评分权重**：「更广泛的防守解」的难度/可见性权重先出可跑版再实测调优（V8 批内定稿回写）。
4. **Q9 3D 瞄准通道**：滑屏让位相机后，瞄准调整按「BTAimWheel + 点击」推定实现，V5 批截图后确认手感。

---

## 七、执行约定

1. 每批开新会话，开头声明「执行问题集合 v5 批次 Vn」，先读本文件该批范围 + 锚点行；
2. 几何/坐标/角度类（Q3/Q4/Q7.1/Q15.1/Q16 可见性）强制先读 `geometry-spatial-reasoning` 技能并钉死坐标契约；
3. 声称完成必须附真实 build/test 输出，UI 条款附截图核验结论（含最坏情形帧）；
4. 批次完成后：更新 `tasks/PROGRESS.md`（批次状态+下一批）、遗留项登记进 V11 前置清单、用户执行中修订回写本文件（版本号递增）；
5. 涉及组件 API 变更/设计解读调整按 orchestrator 规则记 DR-NNN 并回写 SPEC Changelog。

---

## 八、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v5 | 2026-07-13 | 由《问题集合_v4.md》20 条重组：G13–G19 全局规范 + Q 系列页面条目 + V1–V11 批次；全部锚点读代码核实；登记 4 项待确认 |
| v5.1 | 2026-07-13 | V1–V11 收官：各批执行结论逐批回写；V11 总验收完成（全量 560 tests 0 failures、关键 UI 套件全绿、clean-rebuild 转绿非回归）；状态行更新为「全部批次 ✅」；文档回写 SPEC §8.9/§8.8/Changelog + P18 §B3.8 + PROGRESS |
