# 问题集合 v31 — 动作库多分类 + 剂量口径定稿（I6）+ 计划绑定重写

> **本文件是本轮需求真源。**
> **日期**：2026-08-07 **状态**：✅ **全案收官**（v31.7，2026-08-10；W0→W5 七批完工，§六 P1–P4 全部裁定完毕，无待办；遗留 X-v31-1 / X-v31-3 与 6 条 `pocket ''` 基线按判定另立批次）
> **来源**：2026-08-07 用户与 Orchestrator 会话逐项拍板（多分类 / 训练量 / 计划合理性 / 剂量考虑球形与杆数 / 动作库-计划绑定 / 多球形展开）。
> **模型路由定稿（2026-08-09 用户拍板）**：**全部批次 = `claude-opus-5-thinking-medium`**。按 `01-subagent-model-selection` 视为已指定，后续会话不再重复询问。
> **排期前置（2026-08-09 复核更新）**：
> 1. ~~v30 W0 未提交改动~~ **已解除**：工作区干净，HEAD `0053e79`；v33 已于 `5771d04` 收官。
> 2. **失败基线（2026-08-09 W0 现场重测，取代作废的「56 项」）**：W0 相关聚焦测试
>    （`SwiftDataMigrationTests` + `CustomPlanBuilderViewModelTests` + `DrillContentValidationTests`，改动前 42 例）
>    实测 **失败用例 1 个 / 断言 6 条**，全部集中在 `DrillContentValidationTests.test_allDrills_pocketValueIsValid`
>    （c045/c049/c054/c059/c060/c061 `pocket ''`，契约 §8.16）。证据：`build/v31-w0-logs/baseline-focused-tests.log` L306–313。
>    ⛔ 这 6 条不得修绿（FL-027：属内容侧取值缺失，需人工判定袋口，另立批次）。
>    「c042 两条 `shotIntent`」不在本聚焦集合内（落在 `DrillBakeRunnerTests` / `DrillBoardBuilderTests` 等），
>    W2/W5 扩大聚焦范围时须先重测其基线再对照。完成标准一律用**聚焦测试 + 基线对照**，不得写「全量全绿」。
> 3. **范围扩容**：§二 底账写的 72 条 drill 已过时，现为 **83 条**。按 v33 §排期前置的强制要求，v31 **必须把 c079–c084 + c060 纳入全部内容批范围**；§二 各项统计数字（多球形条数、无序列条数、未引用条数）执行时一律脚本重取，W0 负责回写。

---

## 一 需求回显（用户拍板结论，语义基准）

| # | 拍板内容 | 拍板方式 |
|---|---|---|
| R1 | **多分类 = 主分类 + 副分类标签**。drill 保留单值 `category`（文件目录、统计归属、详情主徽章），新增 `secondaryCategories`（每条 ≤1 个）；副分类只影响动作库浏览/筛选，统计**只记主分类**；跨类条目控制在 15 条左右 | 用户选「主分类+副分类（推荐）」 |
| R2 | **单动作训练量中等上调**：专项类基准 4~5×10，基础功保持 3×15，综合类（按局/次）单独评估；**总量护栏 40–60 球，多球形×长序列时轮数向下取** | 用户选「中等上调（推荐）」 |
| R3 | **剂量必须考虑球形数与逐球形杆数**：剂量结构下沉到球形级；每球形标注训练模式 `sequence`（1 轮 = 按序打完 N 杆，每轮球数**锁死 = 序列杆数**）或 `repetition`（序列仅示范，ballsPerRound = 重复次数，人工定）；无序列 drill 人工定量并豁免几何校验。**I6 就此定稿为机检不变量** | 用户提出，口径按 Orchestrator 建议 |
| R4 | **动作库与计划绑定**：drill JSON 是训练剂量唯一真源；计划（官方 + 自定义）**不再存裸 `sets`/`ballsPerSet`**，只存强度系数（`roundsPerFormation` 或按球形逐条）；实际球数在激活训练时由内容解析，落 `DrillSet` 时快照冻结（与契约 §6.5 一致：计划→训练是允许的活引用） | 用户确认「应该绑定在一起」 |
| R5 | **计划调整以换动作为主**：补齐 31 条从未被引用的 drill（走位 c034–c042 整组、specialShots 与 combined 整类），量微调并守住 `minutesPerSession` 预算 | 用户选「换动作为主（推荐）」 |
| R6 | **多球形展开**：训练组列表按「球形 1 轮 1 → 球形 1 轮 2 → … → 球形 2 轮 1」生成，逐组带 `formationToken/formationName` 快照（字段已在 schema V2 就位）；计划允许按球形 token 引用（球形即难度阶梯，如 c075 塞量三档）；token 由此升级为计划外键，删球形前门禁须扫计划引用 | 用户提出「多球形怎么办」，口径按 Orchestrator 建议 |
| R7 | **逐球形模式标注采用人工逐条判定**（72 条），以 v26 W1–W12 已完成的「走位链 / 独立阶梯」形态判定结论为底稿（走位链→sequence，独立阶梯/目录→repetition），机读化落库 | Orchestrator 推荐人工判定，用户令「把这些问题都解决掉」视为采纳 |

**红线（继承契约，不得绕过）**：⛔ 不改任何序列文件与产物目录；⛔ `drillId` 永久不变；⛔ 声称完成必须附真实 build/test/脚本输出；⛔ 几何数值走脚本验算不脑算。

---

## 二 事实底账（**2026-08-09 W0 脚本重取**，真源 `scripts/v31_w0_baseline_stats.py`，输出 `build/v31-w0-logs/baseline-stats.{md,json}`）

> 口径：drill 清单取 `Drills/index.json`；球形数取 `content/position_play/sequences/*.json`（契约 §1.1 球形几何真源，§3.2 文件名归属判定）；计划引用取 `Resources/Plans/plan_*.json`。下列数字**已取代 2026-08-07 的 72 条口径**。

- **83 条 drill / 8 分类**：fundamentals 8、accuracy 15、cueAction 12、separation 10、positioning 14、forceControl 8、specialShots 8、combined 8。序列文件 98 个，覆盖 75 条 drill。
- 量分布（2026-08-07 抽样结论，内容批复核时逐条重验）：基础功 8 条全 3×15；专项类多为 3×10；已隐式对齐杆数的（c001 3×5=杆数、c013 2×9≈球形数×最大杆数、c053 2×13）约占一半；完全脱节的如 c020（单球形 16 杆写 4×10）、c056/c057（3 个 1 杆变式球形写 3×10）、c063/c072（2 杆示范序列写 3×15）。
- **多球形 drill（≥2 球形）19 条**：c003(4,5)、c004(4,4)、c006(0,0)、c013(8,9)、c022(1,2)、c024(1,6)、c025(6,7)、c026(5,7,5)、c042(8,5)、c053(10,13)、c056(1,1,1)、c057(1,1,1)、c058(1,1)、c069(8,10)、c071(8,15)、c073(7,8)、c074(6,9)、c075(7,5,5)、c076(14,14)（括号内为逐球形杆数，杆数异构常见）。逐球形 token 见 `baseline-stats.md`。
- **无序列 drill 8 条**：c008/c043/c059/c061/c065/c067/c068/c070（与契约 §8.5 及门禁 `i9_no_sequence_exempt` 完全一致）。c060 已录 8 杆序列、c066 已录 0 杆空序列（v33 W0 棘轮收紧），不再计入；c006 为双 0 杆球形，计入多球形而非无序列。
- 10 份官方计划：单次训练 113–168 球（新手 60min 135 球 ≈27 秒/杆，密度已近上限，2026-08-07 测算）；**37/83 条 drill 从未被任何计划引用**：c014/c015/c017/c022/c023/c034–c043/c054–c061/c064–c071/c079–c084；c005 被引用 160 次居首（次为 c003 119、c001 112、c004 110、c010 93）。**新增 drill c079–c084 全部在未引用清单内**，R5 换动作范围随之扩大。
- **v29 已落地、本方案直接复用**：SwiftData schema V2（`DrillSet.formationToken/formationName/unitLabel/passMade/passTotal/durationSeconds` 等，W3）；录入表格球形列 + unitLabel 派生（W4）；统计按 category 分组、drill category 由 drillId 回查（W6，多分类天然只记主分类，**统计层零改动**）；计划按完成推进 + 跳过/回退（W7）；写盘测试 `BakeRunnerGate` 收口（W1，执行 W1a 前复核 gate 仍生效）。

---

## 三 批次表

| 批次 | 范围 | 量级 | 依赖 | 模型 | 状态 |
|---|---|---|---|---|---|
| W0 | 横切基建：契约/schema 文档 + DTO 扩展 + SwiftData V3 迁移 | 中 | 排期前置清障 | `claude-opus-5-thinking-medium` | ✅ 2026-08-09 |
| W1a | 内容批①：fundamentals(8)+accuracy(15)+cueAction(12)+forceControl(8) = **43 条** 剂量标注 + 副分类 | 中大 | W0 | `claude-opus-5-thinking-medium` | ✅ 2026-08-09（返工 0 轮）|
| W1b | 内容批②：separation(10)+positioning(14)+specialShots(8)+combined(8) = **40 条** 剂量标注 + 副分类 + 综合类量值建议表 | 中大 | W0 | `claude-opus-5-thinking-medium` | ✅ 2026-08-09（返工 0 轮）|

> **分批口径更正（2026-08-09，主控）**：原表按 72 条时代的分类条数切分（separation 8 / positioning 10），W0 重取后实为 separation 10 / positioning 14，两批变成 35 / 48 严重失衡。故把 **forceControl(8) 从 W1b 移入 W1a**，改为 43 / 40。⚠️ 这是**排期切分调整，不改任何需求语义**；两批合计仍是全部 83 条，`c079–c084` 与 `c060` 按各自主分类落入对应批。
| W2 | 运行时绑定：dose 解析、多球形组展开、录入/展示/构建器 UI | 大 | W0+W1a+W1b | `claude-opus-5-thinking-medium` | ✅ 2026-08-09 |
| W3a | 官方计划重写①：6 份专项（accuracy/cueball/english/force/separation/positioning） | 中大 | W1+W2 | `claude-opus-5-thinking-medium` | ✅ 2026-08-09（返工 0 轮）|
| W3b | 官方计划重写②：4 份综合（beginner/intermediate/advanced/fullskill） | 中大 | W1+W2 | `claude-opus-5-thinking-medium` | ✅ 2026-08-09（返工 0 轮）|
| W4 | 门禁：I6 两条不变量 + I10 计划校验接 verify-gate + selftest 构造性用例 | 中 | W1+W3 | `claude-opus-5-thinking-medium` | ✅ 2026-08-09（返工 0 轮）|
| W5 | 收尾：聚焦测试全跑 + 模拟器截图走查 + 契约销账 + 台账回写 | 小中 | 全部 | `claude-opus-5-thinking-medium`（台账由主控回写）| ✅ 2026-08-09（返工 0 轮）|

依赖图：`W0 → (W1a ∥ W1b) → W2 → (W3a ∥ W3b) → W4 → W5`

> 模型列按 `.cursor/rules/01-subagent-model-selection.mdc` 在**委派时**经用户确认后回填本表；建议路由：横切/代码批（W0/W2/W4）强推理模型，内容/计划批（W1x/W3x）快速模型 + 主控每批抽 2 条全文复核。

---

## 四 批次详情与完成标准

### W0 横切基建（文档 + 数据结构）

**范围**：
1. 契约 `content-data-contract.md` 升 v1.8：
   - §3 追加多分类口径（主/副分类语义、统计只记主分类、副分类 ≤1）；
   - §5.6 新增「剂量口径」：sequence/repetition 二分定义、sequence 型每轮球数=杆数锁死、总量护栏 40–60 球轮数向下取、无序列 drill 人工定量豁免；**I6 定稿**（见 W4）；
   - §6 追加绑定模型（计划只存 dose、激活时解析、落库快照）与「球形 token 成为计划外键」的删除连带（§6.2 破坏性变更范围扩大）；
   - §9 记录本轮全部裁定（编号续 D10+）。
2. `QiuJi/Resources/Drills/schema.md` + `docs/00-讨论记录.md` 同步。
3. **DrillContent DTO**（`DrillContentService.swift` L5–68）：`secondaryCategories: [String]?`、`DrillSetsConfig` 加 `perFormation: [FormationDose]?`（`token/mode/ballsPerRound/defaultRounds`），全部可选、旧 JSON 照常解码；`defaultSets/defaultBallsPerSet` 保留为汇总兜底。
4. **PlanDrillRef**（`PlanContentService.swift` L58–63）：新增 `dose`（`roundsPerFormation: Int` 或 `formations: [{token, rounds}]`）；**旧 `sets/ballsPerSet` 一次切净不留兼容解码**（裁定理由：无 OTA、内容版本≡App 版本、Bundle 内计划随本方案 W3 全量重写，无存量旧格式风险）——W3 未完成前 Bundle 计划暂不动，故本批解码器需兼容两种格式，**W5 收尾时删旧格式分支**。
5. **SwiftData V3 迁移**：`CustomPlanDrill`（`CustomPlan.swift` L22–35）`sets/ballsPerSet` → `roundsPerFormation`（迁移规则：旧值折算 rounds = max(1, sets/球形数)，无球形按 1 球形算）；`QiuJiMigrationPlan` 加 V2→V3 轻量迁移；**写 ADR**（挂 P2 或新 Phase 文件末尾）。
6. 同步修 `CustomPlanBuilderViewModelTests` 构造断言（失败机理：`CustomPlanDrill` init 签名变更导致编译失败，属预期重构，非删断言求绿）。

**完成标准**：`make build` SUCCEEDED；`SwiftDataMigrationTests` + `CustomPlanBuilderViewModelTests` + `DrillContentValidationTests` 聚焦全绿（附输出）；V2 真实落盘库经 V3 迁移打开零丢失（沿用 v29 W3 验证法）；契约/schema/讨论记录三处 diff 齐备；ADR 落档。

**执行结果（2026-08-09 ✅）**：全部达标，证据落 `build/v31-w0-logs/`。
- `make build` **BUILD SUCCEEDED**（`build-1.log`）；`make verify-gate` **FAIL 0**（`verify-gate.log`）。
- 聚焦测试（+`DrillTrainingPlanServiceTests`）50 例，失败 6 条断言 = 改动前基线原样（`after-focused-tests.log`）；
  另跑 `V22W3/W4/W5PlanTests` + `ActiveTrainingViewModelTests` 41 例全绿（`plan-regression-tests.log`）。
- V2→V3 迁移零丢失实证：`SwiftDataMigrationTests` 新增 2 例（落盘 V2 库 → 当前容器打开），
  三种取整分支 6/1=6、6/2=3、2/3→1 全部命中，`PRAGMA table_info` 证明就地改列。
- 契约升 **v2.0**（非 v1.8——1.8 已被 v30 X-1 占用，最新为 1.9，故进位）；新增 §3.3 / §5.6 / §6.6，
  §6 规则 2 扩范围，§7 I6 定稿拆 I6a/I6b + 新增 **I11**（W4 的「I10 计划校验」与既有 I10 编号冲突，顺延）；
  §9 记 D10–D14。`schema.md` 补 `secondaryCategories` 与 `FormationDose`；`docs/00-讨论记录.md` 立条目。
- ADR：`tasks/phases/P2-data-layer.md` 末尾 **ADR-004（ADR-v31-01）**。
- ⚠️ 交给 W2/W5 的过渡实现（本批只求编译与行为不变）：`PlanDrillRef.legacyVolume(fallback:)`、
  `TrainingHomeViewModel` / `CustomPlanBuilderViewModel` 的每组球数回落 `defaultBallsPerSet`。

### W1a / W1b 内容批：剂量标注 + 副分类

**前置产物（批内先做）**：脚本生成底稿表（drillId / token / 序列杆数 / v26 形态结论→mode 建议 / 现量 / 按 R2·R3 公式建议量），底稿表落 `build/v31-dose-draft.csv` 供人工复核。v26 形态结论取自 `tasks/PROGRESS.md` W1–W12 条目与各 drill 精讲正文（走位链→sequence；独立阶梯/目录/多形单杆→repetition）。

**范围**（每条 drill JSON 只动 `sets` 块 + `secondaryCategories`，⛔ 不碰 tutorial/序列/意图块）：
- 逐球形写 `perFormation`：mode 人工判定（R7）、sequence 型 `ballsPerRound` = 序列实测杆数（脚本取值，禁止手抄）、`defaultRounds` 按护栏（总量 40–60 球，轮数向下取，最少 1 轮）；repetition 型 `ballsPerRound` 10~15；
- 汇总兜底 `defaultSets = Σrounds`、`defaultBallsPerSet` 取主球形每轮球数；
- 副分类标注：候选（执行时逐条核内容后定稿，允许增删）——c076/c077/c078→+cueAction；c030/c031→+positioning；c042→+combined；c003/c004→+positioning；c046/c051→+positioning；c018/c020/c021→+positioning；c069/c071→+positioning；
- W1b 额外产出：**综合类与无序列 drill 量值建议表**（Ghost 局数、清台次数等，逐条附理由）→ 用户确认后落 JSON。

**完成标准**：底稿表与最终 JSON 逐条 diff 留档 `build/v31-w1x-logs/`；批内每条 sequence 型球形满足 `ballsPerRound == 序列杆数`（脚本复验输出）；每条总量落 40–60 球区间或附豁免理由（综合类/基础功）；`make verify-gate` FAIL 0；`make build` SUCCEEDED；全量聚焦内容测试后 `git status` 中 `Resources/` 无本批意图外 diff（BakeRunnerGate 复核）。副分类清单汇总后交用户确认（W5 前）。

### W2 运行时绑定与多球形展开

**范围**：
1. `TrainingHomeViewModel`：官方计划条目（L184–212）与自定义计划（L232–245）从 `ref.sets/ballsPerSet` 改为 dose × drill `perFormation` 解析出组列表与总量；`TodayDrillItem` 结构随之调整。
2. `ActiveTrainingViewModel`：`addDrill`（L317–345）与 `initializeRecords`（L367–382）按球形展开组序列（球形 1 轮 1 → … → 球形 N 轮 M），逐组预填对应 `formationToken/formationName`（替换现「全部预填第一个球形」的行为，用户仍可用既有球形 Menu 改）；`targetBalls` 逐组取该球形 `ballsPerRound`。
3. 展示文案：`TrainingHomeView` L317「N 组 × N 球」→ sequence 型「N 轮 × N 杆」（单位跟 `unitLabel`）；`DrillDetailView` 规格行、`PlanDetailView` 条目、动作库列表卡同步；`DrillDetailView` L258 附近加副分类次徽章；`DrillListViewModel` L166–184 筛选命中主+副、分组仍按主分类。
4. `CustomPlanBuilderViewModel/View`：录入从 sets/balls 双 stepper 改为轮数 stepper（球数由内容派生只读展示）。

**完成标准**：`make build` SUCCEEDED；聚焦测试 `ActiveTrainingViewModelTests` + `TrainingHomeViewModelTests`（如无则新增）+ `CustomPlanBuilderViewModelTests` + `DrillListViewModelTests` 全绿，新增用例覆盖：多球形展开顺序与逐组 token、sequence 型 target=杆数、副分类筛选命中；模拟器实跑一次 c013（双球形异构杆数）自由训练，sqlite 直查逐组 `formationToken` 与 `targetBalls` 符合展开（沿用 v29 W4 验证法，附查询输出）；截图：动作库副分类筛选、训练录入球形分组。

**执行结果（2026-08-09）**：✅ 完成。新增 `QiuJi/Data/Services/TrainingDoseResolver.swift` 作为剂量解析唯一入口（ADR-005 / ADR-v31-02 落 `tasks/phases/P2-data-layer.md`），`TodayDrillItem`/`ActiveDrill`/`CustomDrillItem` 均改为携带 `[PlannedTrainingSet]`。证据：`make build` SUCCEEDED、聚焦 4 套 88 例全绿、`verify-gate` FAIL 0（日志 `build/v31-w2-logs/`），UI 实跑 + sqlite 直查见 `build/v31-w2-logs/sqlite-drillset-latest-session.txt`、截图 `build/v31-w2-screenshots/`。
⚠️ **完成标准 4 的事实更正**：真源 §二 记 c013 逐球形杆数 (8,9)，但 W1b 落进 `drill_c013.json` 的 `perFormation` 两球形均为 `repetition` / `ballsPerRound: 10`（repetition 型球数是人工定量，不等于序列杆数，契约 §5.6），故 c013 实跑落库为 2 轮×10 + 2 轮×10 —— **展开顺序与逐组 token 可证，异构球数不可证**。同一次实跑追加了 `drill_c042`（`manual01` repetition 10 / `manual02` sequence 5）补齐异构证据。全库 `ballsPerRound` 异构的多球形 drill 仅 3 条：c042、c053、c071。
**X-v31-2 已修复（2026-08-09，主控返工要求）**：录入表格球形列把「球形2」显示成「…」。**根因（实测，非静态列宽）**：球形列是行内唯一的可变宽文本列，却与纯数字列平分弹性空间，分到的宽度只比标签固有宽度多 1–2 pt；**SF Pro 的比例数字里「2」比「1」宽**，故同为三字的「球形1」（固有 31.3 pt）刚好放得下、「球形2」（固有 33.0 pt）放不下，被截成「…」——与行状态、`lineLimit` 无关。证据取自 UI 层级 dump 的实测宽度：修复前 `球形1` 31.3 pt / `球形2` **22.3 pt（省略号宽度）**，修复后 `球形2` 恢复 **33.0 pt**，Menu 按钮框在两种情况下都是 46 pt（证明不是行状态导致列宽变化）。**修法**：`BTFormationMenu` 的 label 加 `.fixedSize(horizontal: true, vertical: false)`，让该列取固有宽度、由弹性数字列让出这 1–2 pt；⛔ 未调宽度常数、未改 `lineLimit`、未缩字号。验证：`make build` SUCCEEDED、聚焦 4 套 88 例 0 失败、UI 实跑重取截图第 3 组起完整显示「球形2」（`build/v31-w2-screenshots/09-record-c042-formation-after-fix.png`，修复前对照留在 `x-v31-2-before/`）。

### W3a / W3b 官方计划重写

**范围**（10 份 `Resources/Plans/plan_*.json`）：
- 条目全量迁移到 dose 格式（`roundsPerFormation` 为主，难度阶梯型多球形 drill 可按球形引用做周进阶，如 c075）；
- 换动作：plan_positioning 引入 c034–c042 阶梯；advanced/fullskill 后期周引入 specialShots 与 combined（清台/Ghost 做周期末检验）；杆法专项补 c014–c017/c073–c075；31 条未引用清单尽量清零（确实不适合进计划的逐条附理由留档）；
- 每份计划复核 `minutesPerSession`：按「派生总球数 × 单杆耗时估值（含摆球，取 25–30 秒/杆）」校验，超预算则减轮数或减动作，禁止只改分钟数凑数；
- 主题周与动作类别对齐（focused 阶段动作的主分类须与周主题一致或为其副分类）。

**完成标准**：批内脚本复验：每条目 dose 可按 drill `perFormation` 解析、按球形引用的 token 全部存在、每 session 派生球数与时长预算偏差 ≤15%（输出留 `build/v31-w3x-logs/`）；未引用 drill 清单收敛报告（目标 ≤5 条且逐条有理由）；`make build` SUCCEEDED；模拟器实跑重写后计划的「今日安排」一屏截图。

**执行结果（2026-08-09 ✅ W3a 返工 0 轮 / W3b 返工 0 轮）**

10 份官方计划全部切到 dose 格式，**全库零残留 `sets`/`ballsPerSet`**（W5 删兼容分支的前置已就绪）。

- **W3a**（6 份专项，493 条目）证据 `build/v31-w3a-logs/`：`build.log` L45 BUILD SUCCEEDED；`verify-gate.log` L41 FAIL 0；`focused-tests.log` L519 **81 例 0 失败**（9 套，含主动纳入的 `V22W2PlanAccuracyTests`/`V21W5PlanContentTests`/`V29W7PlanProgressTests`）；`verify-plans.log` L171 **FAIL 0**（2032 次检查：V1 格式 / V2 drillId / V3 dose 可解析 / V4 token 外键 / V5 逐 session 预算 / V6 主题对齐 / V7 结构）。
- **W3b**（4 份综合，729 条目）证据 `build/v31-w3b-logs/`：`04-build.log` L45 BUILD SUCCEEDED；`05-verify-gate.log` L41 FAIL 0；`06-focused-tests.log` L493 **82 例 0 失败**；`03-verify-plans.log` L238 **FAIL 0**（3475 次判定）。
- **未引用 drill：37 → 16（W3a）→ 0（W3b）**。全库 83 条全部被官方计划引用 ⇒ 真源 §六 **P4 豁免清单为空，无需用户确认**。
- **主控独立验收**（不采信子智能体自述）：亲读上述日志原始行；**自写脚本独立复算全 10 份计划**——按 `TrainingDoseResolver` 语义重算 164 个 session 的派生球数，越界 **0**；独立复算未引用集合 = **空集**（83/83）；抽验 `plan_positioning` W5D1 独立算得 174 球，与 `verify-plans.log` L102 逐字一致。核 diff 确认 `Resources/Drills/`、`content/` 零新增改动（mtime 全量比对），计划元数据 `nameEn`/`nameZh`/`minutesPerSession`/`durationWeeks`/`sessionsPerWeek` 零改动，测试无断言删除（W2 遗留的 `updateSets/BallsPerSet` 用例删除已在 v31.5 验收）。
- **主控采纳的三项判断**（用户授权「需要拍板的按你的建议」）：
  1. **c079–c082 落 plan_positioning 而非 plan_accuracy**（真源 W3a 范围原文写 accuracy）。理由：这四条主分类为 `positioning` 且副分类不含 accuracy，进 accuracy 的 focused 段直接违反主题周对齐标准。plan_accuracy 改补 c022/c023，收敛总条数不变。**属排期落位调整，不改需求语义。**
  2. **fullskill W5–W12 周主题改写**：原文案「本周重复 Week N 主题（循环）」无法推导分类集合、且该计划重写前 12 周只用 c001–c013 共 10 条 drill（separation/forceControl/specialShots/combined 一条没有，与「全能」名实不符）。改为点名分类的主题后覆盖全部 8 分类。W1–W4 主题原样保留。同理 intermediate W9「薄弱环节补强」→「…— 分离角与力度」。
  3. **specialShots 提前到 advanced W4**（真源写「后期周」）：W4 既有主题就是「防守策略 — 安全球」，全库仅 c058–c061 对口；推后则该周只能继续用与主题无关的 c002/c005。fullskill 侧仍严格落 W7/W8。
- **按局/按次口径**（W3b 自定，主控核可）：按局（c065/c067/c070）实算 ≈27.5 秒/球，正落 25–30 秒基准带中点 ⇒ 系数 1.0；按次（c066 开球，摆全副球不可摊薄）≈75 秒/次 ⇒ 系数 2.7。判据取**原始球数与球当量两条都要落带内**（严于真源要求，非放宽）。⚠️ 系数中的秒数是估值假设非实测，已写入脚本文档块，改 `UNIT_BALL_EQUIV` 一处即可重算。
- ⚠️ **留给 W5**：真源本节完成标准中的「模拟器实跑今日安排截图」两批均未做（已如实登记），并入 W5 截图走查。

### W4 门禁与校验

**范围**（`scripts/verify_tutorial_sync.py` + `scripts/content_invariant_baselines.json`）：
- **I6 定稿双检**：I6a——每条有序列 drill 的 `perFormation` token 集合 == 序列 token 集合；I6b——sequence 型球形 `ballsPerRound == 序列实测杆数`；无序列 drill 与 repetition 型豁免 I6b（豁免钉到 drillId/token，走棘轮基线）；
- **I10 计划校验**：官方计划每条目 drillId 存在于 index、dose 结构可解析、按球形引用的 token 存在于该 drill 序列集合；
- 总量护栏（40–60 球）做**警告级**不阻塞（综合类天然越界）；
- 三项全部接 `make verify-gate` 阻塞项 + `make invariant-selftest` 构造性用例（每项各造一个必 FAIL 的用例并还原，沿用 v29 W9 影子库法）；契约 §7 表更新（I6 状态改「✅ 已接门禁」，I10 新增）。

**W4 追加项（2026-08-09 主控登记，来自 W1a 实测）**：
- ⚠️ **编号更正**：本节所称「I10 计划校验」与既有 I10（模型可解码性，v30 X-1）冲突，契约 §7 已顺延登记为 **I11**。W4 实施一律以契约表编号为准。
- **必补 `MODEL_SPEC`**（`scripts/verify_tutorial_sync.py` 约 L536）：`DrillSetsConfig` 目前仍只有 `defaultSets`/`defaultBallsPerSet`，检查器忽略未知键 ⇒ **I10 对 W1x 新写的 `perFormation` / `secondaryCategories` 是盲区**。按 FL-029 第 3 条（放宽/收紧模型必填性必须同步更新门禁），W4 必须把新字段纳入 spec，否则新约束无人看守。W1a 期间新字段的解码证据只来自 Swift 侧 `DrillContentValidationTests`。
- **待裁口径张力**：契约 §5.6 给 `repetition` 型的「`ballsPerRound` 10–15」与超长阶梯冲突——c020/c078 各 16 档、c076 双形各 14 档，一轮 10–15 球走不完一趟阶梯。W1a 已取带内上界（16 档取 15，仍差 1 档）。W4 定 I6 细则时须处理：要么放宽为「阶梯型取档数」，要么显式承认一轮不必走完一趟。

**完成标准**：`make verify-gate` FAIL 0（附输出）；`invariant-selftest` 新增用例全部「先 FAIL 后还原绿」（附两段输出）；门禁拦截实证一次（临时改坏一个 plan 条目 → push 本地裸仓被拦 → revert 放行，沿用 v29 W9 方法）。

**执行结果（2026-08-09 ✅ 返工 0 轮）**：证据 `build/v31-w4-logs/`。

- **三项常驻门禁上线**，阻塞项 9 → **12**（`C1/C2/C3/C4/I5/I6a/I6b/I7/I8/I9/I10/I11`）。`06-verify-gate.log`：I6a 通过 75 违规 0（L12）、I6b 通过 12 违规 0 + **规则豁免 86**（L18）、I11 通过 1222 违规 0（L47）、**总计 FAIL 0**（L51）。
- **构造性用例 23/23**（新增 8 条：I6a×2 / I6b×3 / I11×3，另含 3 条「应放行」对照防止豁免变通行证），`05-invariant-selftest.log` L327。
- **拦截实证**：`plan_advanced` W1D3 的 c075 球形 token 改成 `manual99` → 本地裸仓 push **exit=1**（`07-gate-block-FAIL.log` L48 违规 1 / L70）；还原后同一 push **exit=0**（`08-gate-restore-PASS.log` L63 new branch / L64）。裸仓与 remote 已清理，`manual99` 仅存在于 selftest 脚本（构造用例本体）。
- **I10 盲区消除的对照实证**（FL-029 第 3 条）：同一注入（`drill_c001.perFormation[0].ballsPerRound: 5 → "五"`）下 **HEAD 版检查器 FAIL 0**（证明确为盲区）、**W4 版 FAIL 1**、还原后 FAIL 0（`10-i10-perFormation-blindspot.log` L9/L17/L23）。`MODEL_SPEC` 已补 `secondaryCategories` / `perFormation` / `FormationDose`（含 `mode` 枚举域），并把计划侧全链模型纳入 I10（理由同 FL-029：`loadAllPlans` 的 `compactMap` 会让解码失败的计划整份静默消失）。
- **契约升 v2.1**：§7 表 I6a/I6b/I11 标「✅ 已接门禁」、§7.1 阻塞项 9→12、§5.6.2 写入阶梯型上界放宽、§9 记 **D15**。
- `09-build.log` L45 BUILD SUCCEEDED；`11-focused-tests.log` L589 聚焦 11 套 **122 例 6 条失败** —— **主控逐条核对（L439–444）：6 条全部是既有 `pocket ''` 基线（c045/c049/c054/c059/c060/c061），与排期前置登记逐条一致，新增失败 = 0**（按 FL-027 不得修绿）。
- **主控独立验收**：亲读上述所有原始日志行；核 `git status` 确认 `content/` 与全部产物目录零改动、drill JSON 本批只动 c020/c078 的 `ballsPerRound`。

**主控裁定（用户授权按建议执行）**：

1. **阶梯型口径放宽已落地（D15）**：`repetition` 型球形若档数 > 15，`ballsPerRound` 取档数（语义「一轮 = 走完一趟阶梯」，与 sequence 型「一轮打完整条序列」同构）。全库档数 > 15 的 repetition 球形**只有 2 个**：c020/manual01、c078/manual01，各 15→16。⚠️ 真源 W4 追加项原写「c076 也要改到 14」有误——c076 双球形本就各 14 档且现值即 14，**无需改动，未动该文件**。
2. **repetition 豁免做成规则性判定而非棘轮清单**（`i6a_token_mismatch_exempt` / `i6b_shots_exempt` 两个基线键**均为空字典**，零条 FAIL 被塞进基线）。理由：这是契约 §5.6.2/§5.6.4 的结构性口径而非历史债，把 86 个 repetition 球形逐条钉进基线会退化成「每加一条 drill 就改基线」的维护陷阱。可观察性由两点保住：门禁常驻打印「规则豁免 N」、非法 `mode` 值直接 FAIL（selftest `i6b_mode_flip_not_a_dodge` 证明「把 sequence 悄悄改成 repetition」不能绕过 I6b）。
3. **c020 放宽的副作用交 W5 修**：c020 每轮 15→16 使 `plan_cueball` W5D3 派生球数 171→174，超出 W3a 批内脚本的严格预算带上界 172.5 共 **1.5 球（0.87%）**。⚠️ 注意该「两端严格带」是 W3a 自设的更严读法；按**真源原文**「偏差 ≤15%」在 27.5 秒/杆中点口径下为 +6.3%，**本就合规**。仍指派 W5 把该 session 减 1 轮使两种读法都绿，避免留下 `03-w3a-verify-plans-after-ladder.log` L171 `FAIL 1` 这种账实不符的记录。

### W5 收尾

**范围**：删 `PlanDrillRef` 旧格式兼容分支；聚焦测试全套复跑 + 与 X-v30-1 既有失败基线对照（不新增失败）；模拟器截图走查（动作库主/副分类、drill 详情规格行、多球形训练展开、计划今日安排、自定义计划构建器）；契约偏差登记核对销账；副分类清单与综合类量值表的用户确认结果落库；`PROGRESS.md` + Project Hub 状态卡回写。

**完成标准**：上述截图全套留 `build/v31-w5-screenshots/`；聚焦测试对照报告（新失败 = 0）；`make verify-gate` FAIL 0；`make build` SUCCEEDED；台账三处（PROGRESS / hub 卡 / 契约版本记录）diff 齐备。

**执行结果（2026-08-09 ✅ 返工 0 轮）**：证据 `build/v31-w5-logs/` + `build/v31-w5-screenshots/`。

- **计划侧旧格式一次切净**：`PlanDrillRef` 现只剩 `drillId` + `dose`（`PlanContentService.swift` L58–64）；`TrainingDoseResolver` 删 `legacySets/legacyBallsPerSet` 兼容路径，解析由三路径降为两路径（`perFormation` 展开 + 汇总兜底）；`TrainingHomeViewModel`/`PlanDetailView` 调用点同步。全仓 `legacySets|legacyBallsPerSet|legacyVolume` 仅剩 1 条**注释**。⛔ drill 侧 `defaultSets`/`defaultBallsPerSet` 按契约 §5.6 原样保留（未误删）。
- **W4 遗留副作用清账**：`plan_cueball` W5D3 热身 c001 6→5 轮（174→169 球）。选热身而非专项段的依据：超额仅 1.5 球，减 5 球是能落回带内的最小改动；c001 直线球热身与本周主题无关，不影响主题对齐；专项段 c020/c021/c018 是 3/3/3 对称设计，动任一条都破坏配比。⛔ 未改阈值 / 未改 `minutesPerSession` / 未改 drill JSON。`v31_w3a_verify_plans.py` 由 `FAIL 1` 回到 **FAIL 0**（`01-w3a-verify-before.log` L171 对照 `14-w3a-verify-final.log` L171）。
- `13-build-final.log` L45 BUILD SUCCEEDED；`12-verify-gate-final.log` L51 **FAIL 0**（12 项阻塞项齐全）；`15-w3b-verify-final.log` L238 FAIL 0。
- **聚焦 13 套 146 例 / 6 条失败**（`07-focused-tests.log` L764）——**主控独立核对：过滤 `pocket ''` 后非基线报错行数 = 0，新增失败 = 0**。
- **构造性用例 23 → 24**：新增 `i11_legacy_volume_keys` 守护「I11 旧格式残留由 WARN 转 FAIL」这条新规则（字段已不存在，写了只是哑数据）；原 23 条未改且全部仍通过（`11-invariant-selftest-final.log` 末行 24/24）。**主控核可这一扩项**——新转的阻塞规则若无构造性证明就是无人看守的约束（FL-029 第 3 条同理）。
- **契约升 v2.2**：§6.6 增补「W5 已切净」并显式区分 drill 侧保留字段；§7 表 I11 状态改写；`MODEL_SPEC.PlanDrillRef` 同步删两键。
- **截图 9 张 + 8 份 UI 层级 dump** 落 `build/v31-w5-screenshots/`，新增常驻 UI 走查 `QiuJiUITests/V31W5WalkthroughUITests.swift`。**主控亲验两张**：`06-today-schedule-准度专项.png` 今日安排三条目「2 轮 × 15 球 / 7 轮 × 10 球 / **7 轮 × 5 杆**」（sequence 型单位为「杆」，与 plan_accuracy W1D1 dose 一致）；`05-record-c042-formation-groups.png` 录入表 7 组 = 球形1 ×2 组各 10 球 + 球形2 ×5 组各 5 球，抬头 0/45，多球形异构展开与逐组 token 可证。
- ⚠️ **今日安排截图取免费计划（准度专项 / 新手入门计划）而非 plan_positioning / plan_fullskill**：后两者 `isPremium: true`，未订阅时 `PlanDetailView` 走 `isPremiumLocked` 只给前几周预览、无法激活。W3a / W3b 各一份的批次覆盖要求仍满足。**遗留：付费计划的今日安排走查需先有订阅态注入手段**（登记为 X-v31-3）。

---

## 五 锚点索引（2026-08-07 全部读码核实）

| 锚点 | 位置 | 用途 |
|---|---|---|
| `DrillContent` / `DrillSetsConfig` | `QiuJi/Data/Services/DrillContentService.swift` L5–68 | W0 DTO 扩展 |
| `DrillCategory` 枚举 | 同上 L248–285 | 副分类展示复用 |
| `PlanDrillRef` | `QiuJi/Data/Services/PlanContentService.swift` L58–63 | W0 dose 格式 |
| `CustomPlanDrill` | `QiuJi/Data/Models/CustomPlan.swift` L22–35 | W0 V3 迁移 |
| 计划条目→今日训练 | `QiuJi/Features/Training/ViewModels/TrainingHomeViewModel.swift` L184–212（官方）、L232–245（自定义） | W2 绑定 |
| 组展开与球形预填 | `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift` L317–345、L367–389（`formationOptions` 现仅多球形返回非空） | W2 多球形展开 |
| 「N 组 × N 球」文案 | `QiuJi/Features/Training/Views/TrainingHomeView.swift` L317 | W2 文案 |
| 分类筛选/分组 | `QiuJi/Features/DrillLibrary/ViewModels/DrillListViewModel.swift` L166–184 | W2 副分类 |
| 分类徽章/详情 | `QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift` L258、L443 | W2 副徽章 |
| 统计分类回查 | `QiuJi/Features/History/ViewModels/StatisticsViewModel.swift`（`categoryForDrill`/`primaryCategory`） | 确认零改动（只记主分类） |
| 门禁脚本/基线 | `scripts/verify_tutorial_sync.py`、`scripts/content_invariant_baselines.json` | W4 |
| 内容 | `QiuJi/Resources/Drills/*/*.json`（72）、`Drills/index.json`、`Drills/schema.md`、`Resources/Plans/plan_*.json`（10）+ `Plans/index.json` | W1x/W3x |
| 序列真源（只读） | `content/position_play/sequences/*.json`（90，覆盖 67 drill） | 杆数/token 取值 |
| 受影响测试 | `QiuJiTests/CustomPlanBuilderViewModelTests.swift` L103/106/224+、`DrillContentValidationTests.swift` L106–109、`ActiveTrainingViewModelTests.swift`、`DrillTrainingPlanServiceTests.swift` L40/66/155 | W0/W2 同步改（构造 fixture 为主，改动附机理） |
| 自定义计划构建器 | `QiuJi/Features/Training/ViewModels/CustomPlanBuilderViewModel.swift`、`Views/CustomPlanBuilderView.swift`（内部结构待批内细读，**待定位**具体 stepper 行号） | W2 |

**PD-027 自查**：① 写盘测试全集已由 v29 W1 `BakeRunnerGate` 收口（6 runner + V21W2/3/4BakeTests），W1x 完成标准含 gate 复核；② 本方案涉及的断言改动（`CustomPlanBuilderViewModelTests` 构造签名、`DrillTrainingPlanServiceTests` fixture）失败机理均为「schema/init 签名变更导致编译失败」，属预期重构；`DrillContentValidationTests` 的 `>0` 断言与新结构兼容，保留不动。

---

## 六 待拍板 / 待用户确认

| # | 事项 | 时点 |
|---|---|---|
| P1 | 各批次子智能体模型路由（01 规则，建议已列批次表） | 委派前 AskQuestion 确认，回填批次表 |
| P2 | ~~副分类最终清单（W1x 产出汇总）~~ | ✅ **2026-08-10 用户确认**（照单通过，无增删）。全库定稿 **15 条**（R1 目标「15 条左右」）：`c076/c077/c078` accuracy+cueAction；`c018/c020/c021` cueAction+positioning；`c046/c050/c051` forceControl+positioning；`c030/c031` separation+positioning；`c069/c071` combined+positioning；`c042/c082` positioning+combined。统计仍只记主分类（`StatisticsViewModel` 零改动），副分类只影响动作库浏览/筛选。 |
| P3 | ~~综合类/无序列 drill 量值建议表（W1b 产出）~~ | ✅ **2026-08-09 已裁定**：c065 每局球数 7→**8**（己方 7 + 黑八，与 c070 口径对齐，已落 JSON，总量 80 按局豁免）；按局条目**维持「每局一行」**（10 组 × 每局满清球数，made 记该局清台球数），⛔ 不改为「1 组 × 10 局」。 |
| P4 | ~~计划中确实不适合引用的 drill 豁免清单（W3x 产出）~~ | ✅ **已消解**：未引用 drill 由 37 → 16（W3a）→ **0**（W3b），全库 83 条**全部**被官方计划引用，豁免清单为空，无需用户确认。 |

---

## 六点五 遗留登记

### X-v31-1 — 四条 drill 的元数据自称「走位链」，被序列真源证伪（2026-08-09，W1b 发现，主控独立复算确认）

**事实**（主控用「第 i 杆 `before.cueBall` 是否等于第 i−1 杆 `after.cueBall`（阈值 0.005）」独立复算，非采信子智能体结论）：

| drill | 序列 | 杆间衔接率 | 母球起点重复 | 元数据自称 |
|---|---|---|---|---|
| c035 | manual01 8 杆 | **0/7** | 5 处（三组：×3 / ×3 / ×2） | 「八杆高杆一库走位链」 |
| c036 | manual01 9 杆 | **0/8** | 6 处（×3 / ×4 / ×2） | 「九杆低杆一库走位链」 |
| c005 | manual01 4 杆 | **0/3** | 0（四个互不衔接的起点） | 「四杆连续一库走位」 |
| c031 | Snipaste…13 杆 | **0/12** | 12 处（13 杆同一起点 0.535,0.153） | criteria 含跨杆表述 |

对照组 c079（真走位链）实测衔接率 **3/3**，证明该判据本身有效、不是脚本假阳性。

**结论**：这四条是「同一盘面的多次独立尝试 / 多起点变式」，不是走位链。按契约 §1.1 推论 4（元数据与序列冲突时改元数据、不改序列），W1b 判 `repetition` 正确；⛔ 但 `description` / `standardCriteria` / `coachingPoints` 中的「走位链」「第七杆后母球停位能让第八杆…」等跨杆表述**仍留在内容里，属未修复的错误元数据**。

**处置**：不在 v31 范围内（v31 内容批红线是「只动 `sets` 与 `secondaryCategories`」）。**另立批次修元数据**，修时须逐条重写为「多起点变式」口径并同步精讲正文；⛔ 不得反向改序列去迁就文案。

### X-v31-3 — 付费计划的「今日安排」无法走查（2026-08-09，W5 发现）

`plan_positioning` / `plan_fullskill` 等 `isPremium: true` 的计划，在未订阅态下 `PlanDetailView` 走 `isPremiumLocked` 分支，只给前几周预览、**无法激活**，因而拿不到「今日安排」一屏截图。W5 改用免费的 plan_accuracy（W3a）与 plan_beginner（W3b）完成走查，批次覆盖要求满足，但**付费计划的运行时行为未被 UI 走查覆盖**。

**处置**：不在 v31 范围。需要时先做「订阅态注入手段」（测试用 launch argument 或 debug 开关），再补走查。

---

## 七 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| **v31.7** | 2026-08-10 | **全案收官**。P2 副分类 15 条经用户确认照单通过（无增删），§六 P1–P4 全部裁定完毕，v31 无待办项。本轮改动按内容 / 计划 / 应用代码 / 测试 / 脚本门禁 / 文档六组分批提交。遗留项 X-v31-1、X-v31-3 与 6 条 `pocket ''` 基线维持「另立批次」判定。 |
| v31.6 | 2026-08-09 | **v31 全部批次收官**（W3a / W3b / W4 / W5 均返工 0 轮）。**W3a/W3b**：10 份官方计划全量 dose 化（1222 条目，零残留 `sets`/`ballsPerSet`），未引用 drill **37 → 0**（83/83 全部进计划，P4 豁免清单消解）；164 个 session 预算全部落带内。**W4**：I6a/I6b/I11 接 `make verify-gate`，阻塞项 9 → 12，构造性用例 +8；I10 的 `perFormation`/`secondaryCategories` 盲区用「旧检查器 FAIL 0 / 新检查器 FAIL 1」对照证明消除；门禁拦截实证（裸仓 push exit=1 → 还原 exit=0）；阶梯型口径放宽 **D15**（c020/c078 各 15→16，c076 本就合规未动）。**W5**：`PlanDrillRef` 旧格式一次切净、resolver 三路径降为两路径；`plan_cueball` W5D3 减 1 轮清账 W4 副作用；聚焦 13 套 146 例**新增失败 0**；9 张模拟器截图 + 常驻 UI 走查。契约 **v2.0 → v2.2**。**主控独立验收**：四批全部亲读落盘原始日志行，并自写脚本独立复算全 10 份计划的派生球数（164 session 越界 0）与未引用集合（空集），抽验 plan_positioning W5D1 = 174 球与日志逐字一致；亲验两张截图（sequence 型「7 轮 × 5 杆」、c042 逐组 10/10/5×5）。**遗留**：P2 副分类 15 条待用户确认；新增 X-v31-3（付费计划今日安排无法走查）；X-v31-1 与 6 条 `pocket ''` 基线按判定不在 v31 范围。 |
| v31.5 | 2026-08-09 | **W2 收官**（返工 1 轮）。剂量解析收口为唯一入口 `TrainingDoseResolver`（三路径：旧格式同构兼容 / `perFormation` 逐球形展开 / 无序列汇总兜底），`TodayDrillItem`·`ActiveDrill`·`CustomDrillItem` 改携 `[PlannedTrainingSet]`；录入按「球形 1 轮 1 → … → 球形 N 轮 M」展开并逐组快照 token；筛选命中主+副、分组仍按主分类；构建器双 stepper→轮数单 stepper。ADR-005（ADR-v31-02）落 P2。**主控独立验收**：亲读 `build-final.log` L45 BUILD SUCCEEDED、`focused-tests-final.log` L494 88 例 0 失败（含新增 `TrainingHomeViewModelTests` 9 例，期望值全部取自内容真源）、`verify-gate-final.log` FAIL 0、`sqlite-drillset-latest-session.txt`（c042 逐组 10/10/5×5 + 逐组 token）、两类截图；核 diff 确认删除的三个 `updateSets/BallsPerSet` 用例已由 `updateRounds` 等价用例替代，`Resources/`·`content/` 零越界。**返工 1 轮**：X-v31-2 首轮被归因为「`lineLimit` + 列宽」并判不修，主控以「同宽文本仅部分行截断」证伪并退回，重查根因=比例数字「2」宽于「1」致固有宽 33.0 pt 超出可用宽（截断态实测 22.3 pt = 省略号宽），`.fixedSize(horizontal:)` 修复并重取截图。 |
| v31.4 | 2026-08-09 | **W1b 收官**（返工 0 轮）。40 条标注完成（37 条 diff，c065/c067/c070 现值即目标量故零 diff）；**主控独立复算全库 83 条**：I6a/I6b/`defaultSets=Σrounds` **FAIL 0**，无 `perFormation` 的 8 条正好等于无序列清单（c008/c043/c059/c061/c065/c067/c068/c070），护栏越界 4 条（c065=70/c067=90/c070=80 按局、c066=10 按次）均逐条附豁免理由。副分类全库定稿 **15 条**（W1b 增 c030/c031/c042/c069/c071/c082）。综合类量值建议表落 `build/v31-w1b-logs/combined-dose-proposal.md`（含两个待用户拍板项，真源 §六 P3）。**新增遗留 X-v31-1**（见下）。 |
| v31.3 | 2026-08-09 | **W1a 收官**（返工 0 轮，主控独立复算 43 条 I6a/I6b/Σrounds/40–60 护栏 FAIL 0）。4 条判 `sequence`（c001/c062/c063/c072，`ballsPerRound` 锁死实测杆数 5/4/2/2）、39 条 `repetition`；c008/c043 无序列且现值即目标量故零 diff。副分类定稿 9 条（c018/c020/c021/c046/c050/c051→positioning，c076/c077/c078→cueAction；剔除真源候选 c003/c004、增补 c050）。批次表分批口径更正为 W1a 43 / W1b 40（forceControl 移入 W1a）。W4 追加三项（I10→I11 编号更正、必补 `MODEL_SPEC` 新字段、repetition 10–15 与 16 档阶梯的口径张力）。证据 `build/v31-w1a-logs/`。 |
| v31.2 | 2026-08-09 | **W0 收官回写**。§二 底账全量脚本重取（72→83 条、多球形 18→19 条、无序列 10→8 条、未引用 31→37 条，含 c079–c084 与 c060），脚本 `scripts/v31_w0_baseline_stats.py`；排期前置第 2 条写入现场重测的失败基线（1 用例 / 6 条断言，全为 `pocket ''`）；W0 批次标 ✅ 并附执行结果与证据路径。契约升 v2.0（§3.3 / §5.6 / §6.6 / I6 定稿 + I11 / D10–D14），ADR-004 落 P2。 |
| v31.1 | 2026-08-09 | 开工。模型路由定稿=全批次 `claude-opus-5-thinking-medium`（用户拍板）并回填批次表；排期前置两条复核解除/作废（工作区干净 `0053e79`、56 项失败数字作废由 W0 现场重测）；登记范围扩容 72→83 条（c079–c084 + c060 必须纳入内容批，v33 强制要求）。 |
| v31.0 | 2026-08-07 | 立档。需求回显 R1–R7；批次 W0→W1a/b→W2→W3a/b→W4→W5；锚点全量读码核实；I6 定稿口径与 I10 计划校验写入 W4；排期前置登记 v30 未提交改动与 X-v30-1 基线。 |
