# 问题集合 v57：人工截图反馈与实施方案

> **本文件是本轮需求真源。** 日期：2026-09-05；版本：v57.2；状态：用户已授权全批实施，W0–W4、W6 已完成，W5 返工中，W7 实施中，W8 待实施。
> 原任务「确认接收图片问题反馈」连续断流后，由当前任务接手。原任务 ID：`01a06c91-ca1d-7e01-89a4-a32e36bd44ac`。
> 当前授权：用户已要求“开始执行吧，直到完成所有任务”，按本方案实施并验证 W0–W8；未授权提交或发布。
> 排期前置：实施前刷新工作区基线、相关规则及本文件；既有 v53/v54/v56 人工项不阻塞编写本方案，但其验收不能被本方案代替。W6 游客样式与 W4 次数口径已由用户在接手任务中确认。

## 1. 需求回显与证据

共 **15 组反馈、19 张图片**，按原发送顺序编号 R01–R15。原话、原消息 ID、原文件路径与原图副本保存在 [SOURCE.md](output/v57-feedback/SOURCE.md)，逐图 SHA-256 在 [manifest.json](output/v57-feedback/manifest.json)。原图仅归档到本地 output，不接入 App 资产或公开发布。

用户要求重新确认后，本次逐张打开全部 19 张原图，重新对照原话与代码；纠正了 v57.1 的球库组件定位，补齐 R15 两者已入队主场景、旧课程索引/跨课完成误判、直接长休息冻结与加时缺陷的区别。逐条复审记录见 [RECHECK.md](output/v57-feedback/RECHECK.md)。

| 编号 | 用户要求 | 图片 | 实施归属 |
|---|---|---|---|
| R01 | 今日安排全部完成后，右下增加「自由训练」，点击后可从动作库添加动作训练 | R01-1 | W2 |
| R02 | 3D 默认视角提高，参考第二张效果；母球、目标球、目标袋口都在视野内 | R02-1、R02-2 | W5 |
| R03 | 无激活计划、仅自由训练已完成时，不再重复展示「选择一个计划开始训练」大空态；所有相关用户可见「自由记录」改为「自由训练」 | R03-1 | W2 |
| R04 | 阶段下恢复第一天、第二天等课序单位；课标题过长要精简；不显示「围绕……」冗余说明；点动作进入动作详情，返回仍在当前计划 | R04-1 | W3 |
| R05 | 官方计划卡显示当前进行中角标；允许多个计划保留激活/进度状态，但同一时刻仅一个进行中，切换保留进度 | R05-1 | W0、W2 |
| R06 | 官方计划等级筛选、官方计划/我的模版切换时，不改变该区域在屏幕中的位置 | R06-1、R06-2、R06-3 | W2 |
| R07 | 所有相关页面（含练习页）的球库都更集中，接近相互贴近的视觉效果；球库保持辅助占比，不通过放大球体、增加底栏高度挤压球桌 | R07-1 | W5 |
| R08 | 「已练」改为「已练 N 次」，与多杆等类型标签放同一行，保留彩色勾选胶囊；0 次不显示；适配 1/10/100 及更大数 | R08-1 | W4 |
| R09 | 删除动作库筛选菜单中的「已完成」选项 | R09-1 | W4 |
| R10 | 动作详情「加入训练」采用右边「上手试打」的品牌绿实心按钮样式 | R10-1 | W4 |
| R11 | 游客账号区与登录后外观接近；Pro 会员/有效期展示更精致 | R11-1、R11-2 | W6 |
| R12 | 3D 角度训练的「辅助」「答题」按钮稍微增大 | R12-1 | W5 |
| R13 | 灵动岛展开内容向中间集中，避免两侧圆角遮挡 | R13-1 | W7 |
| R14 | 锁屏休息倒计时超过一分钟会停住，需要修复 | R14-1 | W7 |
| R15 | 模版主体点击进入编辑，加入今日安排放右侧三点菜单；模版和官方课在今日安排并列为课块；右侧展示动作总数、完成数、总预计时间 | R15-1 | W1、W2 |

### 1.1 已确认的语义

1. **R11 后续澄清**：去掉整块品牌绿，接近登录后的中性账号卡。原话里的「背景要品牌绿」不再作为执行依据。点击登录与真实认证路径保留。
2. **R08 后续澄清**：每个已保存的动作条目算 1 次。同一场中同动作出现两条已保存 `DrillEntry`，计 2 次；组数、球形数、进球数都不额外计次。
3. 多个「激活」计划解释为多个保留进度的计划；`active` 只用于唯一进行中记录，其余保留记录使用现有 `paused`。不新增多个同时推进的主线。
4. 第一/二天是课序展示，保留 v54 `stages[].lessons[]` 内容结构、同一天多课能力与稳定 lesson ID，不恢复旧 `weeks[].sessions[]` 写入。
5. 本文的「自由训练」是记录动作与组数的 `.free` 训练会话；「自由击球」「每日清台」是工具入口，不作同义替换。

### 1.2 实施建议与仍需证据的事项

- 建议课序在每个阶段内显示「第 N 天」，由阶段内课序生成；一天可完成多课，必要时短说明「按课序推进」。这属于展示方案，未把自然日约束加回模型。
- R15 **主场景是官方课与模版都已加入今日安排**：必须像同计划的两课一样并列、独立展开/开始/完成，不得把官方课降成未加入的建议。原图只显示模版，无法仅凭图证明官方课当时究竟已落库、尚为建议或被旧索引读错，应结合用户原话复现这三条路径。另一个兼容场景才是“模版已入队 + 官方课尚为建议”：可同级显示建议，明确开始/加入后才持久化；此设计建议不替代前述主需求。
- **R14 用户复审澄清：没有加时，直接开始长休息也会出现。** 主复现用例必须直接从 120/180 秒开始，不点 +30S。静态图 0:52 不能证明连续冻结；加时导致总时长不同步是独立缺陷，不能替代此根因。
- R02 的第二图给出取景方向，不能据此反推唯一 pitch/height 数值；最终参数需投影验证。

## 2. 当前代码核对与问题分析

核对日期：2026-09-05。下文“已确认”指当前文件静态路径已读取；并不表示本轮重现了设备运行现象。

### 2.1 计划进度已保存，读取与切换缺少统一契约（R05）

已确认 `UserActivePlan` 有 `status/currentLessonId/currentWeek/currentDay`。`PlanDetailView.activatePlan()` 会把其他 active 记录置 paused，并复用目标记录。现有能力可保留进度，无需先新增一套“我的已激活计划”表。

但 `TrainingHomeViewModel.load()` 只按 owner 查询后取 `.first`；`TrainingHomeView.hasActivePlan` 仅判断记录非空。暂停/完成记录可能被当作当前主线。`activePlanSignature` 只包含 plan ID 与旧周/日游标，遗漏 status/currentLessonId，切换或新游标变化不一定触发应有刷新。另一方面 `activeOfficialPlan` 已过滤 active，页面内部口径互相不一致。

复审补充：`loadOfficialPlan` 仍按 `currentWeek/currentDay → plan.weeks[].sessions[]` 兼容投影选课，没有以 `currentLessonId` 查询稳定课 ID。W0 必须一并统一实际选课路径，不能只更新刷新签名。`fetchTodayCompletedDrillIds` 仅按当天 + planId 聚合 drillId，未区分 lessonId/scheduleItemId；若同计划两课包含相同动作，旧建议路径可能误把后课动作标完成。W1 必须按课/条目身份核对完成事实。

切换目前位于 View 内，并有 `try? save` 后直接更新显示的路径；应收束为可报错的事务服务。约束为：每 owner 至多一条非 custom active；同计划不重复建档；切 A→B→A 保留 A 的游标。数据库写入失败时不提前显示“进行中”。

产品展示建议区分“未开始 / 已激活待继续 / 进行中 / 已完成”，让用户可找到保留进度的其他计划并明确切换；后台 `paused` 是内部实现词，不意味着用户取消激活。至少给唯一当前卡“进行中”角标，保存进度的卡不能与从未开始完全无法区分。不因此新增收藏系统或额外 Tab。

旧计划已入队课程必须保留来源快照。现有 `PlanProgressService.settleCompletedScheduleItem` 要求当前 active 匹配，不能因切换后完成旧课误推进新计划。冻结的 `progressRole` 不随切换重写；旧主线课程完成后是否推进仍按当前契约处理，并在界面解释。任何放宽结算规则的建议需另行明确，不能在角标修复中悄悄加入。

### 2.2 今日安排存在两套展示来源（R01/R03/R15）

`activePlanContent` 在持久化 `orderedScheduleItems` 非空时只展示 `scheduledBlocksSection`，否则展示 ViewModel 的建议课。用户加入模版后，非空队列分支会遮掉尚未入队的官方建议课。因此单改卡片布局无法解决来源缺失。

上述原因只适用于“官方课还没入队”，不是 R15 全部现象的已确认根因。若两者已正确落在同一 currentSchedule，现有 ForEach 本身没有按来源排除官方课；必须先查入队记录、owner/日历键、选中 currentSchedule、lesson 身份与 UI 行。验收分别覆盖“官方先入队→模版”“模版先入队→官方”“两者重启后恢复”，不能只造建议课截图就关闭 R15。

`scheduledBlocksSection` 右侧目前是 completed item 数 / item 总数；一个模版内多个动作仍只算 1，这正是 0/1 信息不足的原因。`canStartPrimaryTraining` 只考虑未完成队列或空队列下的 active 建议，全部完成后无自由训练浮动入口。无主线时 `emptyStateContent` 即使有 `todaySupplementalDrills` 也继续显示 `quickStartBanner`，造成“全部完成”与入门空态叠加。

方案：先建立统一展示投影，再接 UI。投影输入为当天持久队列、当前主线建议、当天已保存且未被队列覆盖的自由训练记录；建议与事实在同一列表体系内但保持不同身份。不能把历史 session 再写成队列，或按 drillId 去重整个列表，避免把两课中的同动作错误合并。

### 2.3 模版编辑与滚动是明确的交互路径问题（R06/R15）

`customPlanRow` 主体 `.onTapGesture { requestUseForToday(plan) }`，三点菜单里才有编辑。改为主体 push `customPlanEdit`，菜单承担加入；`PlanListView` 的对应入口也需核对并保持一致。

等级筛选点击会调用 `requestBrowseScroll(scrollBrowsingID)`，随后 `scrollTo(..., anchor: .top)`，直接把货架顶到屏幕顶部；内容长短变化还会触发系统底部钳制。取消这一主动跳转只是第一步。应在切换前记录“官方计划/我的模版”区顶部的真实屏幕 Y，更新后恢复同一锚点相对位置，短内容必要时补足滚动容器最小高度，避免因总内容缩短被迫上跳。点击筛选与从计划详情返回是两套行为，不应共用强制回顶逻辑。

### 2.4 课程信息层级与详情导航需拆开（R04）

`PlanDetailView.lessonSection` 当前直接显示完整 `lesson.title` 和 `lesson.summary`，没有独立日序标签。`drillTrackRow` 的 Button 对单球形直接 return，多球形只展开剂量。因此新导航必须与剂量展开分开：行主体进入动作详情，独立展开按钮仅展开多球形剂量，不能嵌套互抢点击。

`TrainingRoute` 当前没有 drillDetail 分支；应把动作详情压入 training NavigationStack，传当前 owner，返回自然落到该计划并保留阶段/课/球形展开与滚动位置。不能切到动作库 Tab 来模拟跳转。

标题精简涉及真实 12 计划/129 课内容：逐课人工语义审阅，短标题与课程训练重点一致，不用截断字符串冒充改文案。隐藏 summary 只影响该页面呈现，暂不删除 JSON 字段或修改已冻结历史。修改标题时保留全部 plan/stage/lesson/drill ID、顺序与剂量，后续执行须加载内容文案技能。

### 2.5 已练次数来自持久条目，不来自布尔集合（R08/R09/R10）

`DrillListView.completedDrillIds` 目前将所有 session 下条目 ID 扁平化为 Set，只能回答是否练过；`BTDrillGridCard.isCompleted` 也是 Bool，封面右下 `BTPracticedBadge` 写死“已练”。

按用户确认改为每 owner 的 `drillID → 已保存条目数`。从规范化的持久 `DrillEntry` 计数；同一条记录因关联查询或同步重读出现多次时按 entry ID 去重，同场同动作不同 entry ID 仍分别计数。删除记录减次、保存增次、切换账号重新计算；未保存内存条目不计，0 分已保存条目照计，不附加用户未要求的命中率门槛。工具会话本就不产生动作条目。

更新订阅不能继续只观察 ID Set：同动作 1→2 时 Set 不变，必须观察计数投影或数据版本。卡片采用 `practiceCount`，将胶囊放到 meta 行；与“多杆”等同排，默认字号下至少覆盖 100 次，窄屏/最大字号可整行重排，完整数字与 AX 标签保留，不用“已练很多次”替换。

`DrillBadgeFilter.completed` 是独立菜单选项，移除它及其筛选分支；不删练习数据或其余类型筛选。`DrillDetailView` 底栏当前加入按钮为 secondary，试打为 primary；在现有可用态把加入按钮也改 primary，保留加入 sheet、权限判断与写入服务。

### 2.6 3D 取景、右侧按钮与全局球库间距（R02/R07/R12）

截图实际为 `SceneAimingView` 的「3D 角度训练」，不是 `GeometricAngleQuizView`。`applyAimingPoseForCurrentQuestion()` 只传母球位置与母球→目标球方向；`CameraRig.enterAiming` 固定 `entryZoom = 0`、近景 height/pitch/radius，未接收目标袋口或当前可见安全矩形。这能解释“默认低/近且无三者全入镜保证”，但尚未验证一套新姿态。

实施约束：SceneKit 世界系 X–Z 水平、Y 向上、单位 m；归一化由 `AngleSceneCalculator` 转换。投影后使用实际 SCNView 坐标，扣除顶部读数、右侧按钮与底部覆盖物。不要拿世界 Z 正负直接当屏幕上下，也不调整台面/物理球位。

将母球、当前题目标球及当前题袋口作为取景目标，使用可见包围体而非只验中心点。先确定可读的较高观察角，再在有界候选范围内求距离/FOV/可允许 pivot，使三个包围体均落入安全区域且近裁面有效；无可行姿态则明确回退到全目标可见的远景。保留横向环绕/缩放交互，只在首次进入、新题、2D→3D 和必要视口变化时重新取景；不在每帧把用户拖动拉回默认姿态。

R07 原图标题是“每日清台”。实际路径为 `FreePlayView.paletteBar → BTReferenceBallPalette`，是点球定位/提示的参考球库，**不支持拖球摆放**；v57.1 以可拖动 `BTBallPaletteBar` 为主锚点不准确。用户随后明确：球太散是所有相关页面的问题，包括练习页面。截图定位不能缩小需求范围；应统一排查 reference / decorative / interactive 三种球库及提球页自定义槽位。

`BTReferenceBallPalette` 将 `libraryWidth` 均分 8 列，宽度来自 `ShotStageMetrics.paletteWidth`；视觉球 30/36pt、槽至少 44pt。优先保持图中的两行球序并居中收紧整体球库，联动球直径与槽宽获得接近相邻的视觉，不可只减行间距或只缩整体背景。不要默认改成更多行/横滚，这不是用户要求；只有真实窄屏约束无法兼容时才提出退化方案。全体消费者均为本次整改与验收范围：参考页验收点击高亮与未在台提示，装饰球库验收视觉间距，交互页验收点击/拖入/拖回与槽不重叠，保留各页原有功能。

`SceneAimingView` 右侧列当前按 68pt 容器放两个 30pt 高按钮，字体 13pt。局部增大按钮视觉和命中尺寸时要同步更新列总高度及定位输入；不要全局把所有 `BTTextActionButton` 使用点一起放大。2D 复用同页也要复验。

### 2.7 账号卡在既有 v56 色彩系统内调整（R11）

`guestHeader` 现在 `.background(Color.btPrimary)` 配白字，改为中性卡面、正文/次正文语义色和品牌色可操作提示；对齐 loggedInHeader 的头像、内边距、圆角。访客提示、真实登录动作与账号隔离保持原路径。

Pro 区现在是两行小字和细边框。建议保留 v56 Premium tokens 与现有星/冠符号，形成“图标 + Pro 会员 + 实际权益有效期”的清楚层级，给 1 行/2 行自适应空间。`entitlementStatusLabel` 为真源，不把月/年会员写成永久，也不新造权益状态或营销文案。小尺寸图标遵守已有材质回退规则。

### 2.8 Live Activity：布局问题与计时根因分开处理（R13/R14）

**2026-09-06 用户真机复测更新**：大于一分钟时锁屏数字可以正常倒计时；当前明确缺陷为最左侧进度几乎不变、灵动岛文字仍遮挡。后续优先修复按时间比例变化的进度与系统布局，不再把直接长休息数字冻结作为当前未定位主故障。用户没有提供本次安装源码指纹或完整59/60/61等矩阵，因此该反馈是其本次真机观察，不扩大为所有版本/状态通过。

`RestTimerLiveActivity` 展开态把标题置 leading、倒计时置 trailing，底部进度铺满；截图显示两端内容靠近圆角。调整为居中的内容组合与合理区域边距，避免硬编码某型号宽度。Apple 文档定义了 leading/trailing/center/bottom 区域，实施时核对目标 SDK 的边距 API availability。参考：[展开区域](https://developer.apple.com/documentation/widgetkit/dynamicislandexpandedregionposition)、[区域边距](https://developer.apple.com/documentation/widgetkit/dynamicislandexpandedregion/contentmargins(_:_:))。

锁屏数字已使用 `Text(timerInterval:countsDown:)`，不能写成“缺少系统倒计时”。圆环 `progress()` 读 `timeIntervalSinceNow`，只是该次 body 计算值；这说明圆环缺少独立时间驱动，并不证明文字大于一分钟停住。App 内 Dispatch timer 也已经按绝对 endDate 算剩余量，并非单纯 remaining -= 1。

W7 先记录 activity ID、start/end/totalSeconds、系统版本、普通锁屏/常亮状态、更新事件和屏幕连续时间；用 59/60/61/90/120/180 秒复现跨分钟差异。最小对照分别去掉 `contentTransition`、自定义圆环、强制 locale，逐项验证，不能一次改完后猜原因。

**v57.2 新证据与边界**：R13 原图同屏右上 1:28、进度条下 1:00，存在不同读数。代码确认 `addRestTime` 增加 App 的 `restTotalSeconds`，但 manager 的 `updateEndDate` 保留旧 `activity.content.state.totalSeconds`；扩展按 `endDate - totalSeconds` 重建起点。因此加时可把起点推到未来，圆环比值也可大于 1。这是可确定复算的数据一致性缺陷，已用 6 组算例检查，见 `output/v57-feedback/recheck/time-state-arithmetic.json`；不是 SwiftUI 渲染实测。

**用户明确未加时也冻结**，所以主故障仍未定位。直接从 120/180 秒开始的代码会同时传入对应 totalSeconds/endDate，算例中不存在上面的未来起点问题。W7 必须优先“直接长休息、不加时”复现，再独立修加时输入不一致；不能写成“发现加时 bug，所以原故障根因已找到”。R13 两个读数的具体系统格式/刷新行为同样需实机验证。

方向是所有表面共享同一结束时间，文字和进度用系统计时初始化器；灵动岛避免普通 Date `.timer` 到点后反向正计时。对延长/缩短、取消、旧活动结束和新活动创建的异步先后也要做 activity ID 校验与诊断。到点数字显示 0 与 activity 被系统移除是不同验收项，不能承诺仅靠视图到零自动调用 App 代码。

Apple 初始化器参考：[Text 时间区间](https://developer.apple.com/documentation/swiftui/text/init(timerinterval:pausetime:countsdown:showshours:))、[ProgressView 时间区间](https://developer.apple.com/documentation/swiftui/progressview/init(timerinterval:countsdown:))。检索确认 API；文档 Markdown 抓取受工具内容类型限制，具体平台 availability 和渲染行为须在执行时结合本机 SDK/真机核对。这里不将社区经验当根因证据。

## 3. 数据与状态契约

### 3.1 今日安排投影与去重

建议建立可单测的展示投影（新类型名由实施者按架构确定，本文不把未存在的名字写进已核实锚点）。

| 来源 | 稳定身份与去重 | 可执行行为 | 完成口径 |
|---|---|---|---|
| 已持久队列课块 | schedule item UUID；sourceKind/sourceId 用于检查同来源未完成重复入队 | pending 明确开始；inProgress 继续；completed 只回看 | 已落库 session 与 item 状态，不能按加入/点击算完成 |
| 当前官方建议课 | owner + planID + lessonID；相同课已在当天队列则不重复建议 | 开始/加入时通过服务幂等入队，再进入训练 | 未接受建议不计“已安排”分母 |
| 当天自由训练历史 | sessionID + entryID；已有 scheduleItemId 匹配队列则不二次展示 | 回看；新开自由训练使用独立入口 | 每个已保存条目显示完成事实 |

「总动作数」建议按已安排的动作条目数，不按唯一 drillId 去重；官方课与模版均含动作 X 时是两个任务。建议课单独显示动作数与预计时长，标“未加入”，不悄悄混入总计。若产品希望总数含建议，须明确改标签为“含建议”，再同步用例。

当天已有安排且全部完成后，不因主线游标推进就自动补出下一课、重新变成未完成态；主动作按 R01 显示自由训练。用户仍可从计划详情明确选择下一课加入。这样“建议来源统一”不会破坏已完成反馈与追加训练入口。

计数公式：已安排有效课块的冻结动作条目数 + 未被课块覆盖的当日自由训练已保存条目数。已放弃课块不占当前待练分母，保留可回看记录；删除 pending 后对应分母减少；未保存的当前编辑不算完成。completed 课块优先按实际落库条目计完成，若允许删减动作提前结束，必须区分“课已结束”与“计划动作全部完成”，不能用课完成标志把所有原定动作直接涂绿。

时间：明确显示“预计共 X 分钟”，用冻结剂量和既有时长估算口径，官方排除不计时 ritual，遵守 `countsTowardSessionMinutes`；不直接把所有动作球数相加后另造一套舍入算法。自由训练已经完成且无原计划剂量时显示真实用时，并与预计值分开标注，避免把实际耗时冒充预计。需要扩充冻结 payload 的估算字段时做版本化兼容，先证明现有字段不足，再决定是否涉及 schema/ADR。

### 3.2 首页状态表

| 状态 | 今日区 | 右下主动作 | 空态 |
|---|---|---|---|
| 无主线、无队列、无当日历史 | 尚未安排 | 自由训练入口可用 | 可显示选计划引导 |
| 只有官方建议 | 建议课，明确未入队 | 开始该课，先幂等加入 | 不显示首次使用大空态 |
| 官方建议 + 模版已入队 | 两块同级，建议身份可见 | 优先未完成队列 | 不因队列非空隐藏建议 |
| 官方课 + 模版均已入队 | 两个正式课块，按多课交互；两者都计动作汇总 | 开始/继续下一块，独立记录来源 | 无；不能把官方课标成未加入 |
| 单块已入队 | 直接显示动作明细 | 开始/继续 | 无 |
| 多块已入队 | 点标题展开/折叠，展开不开始 | 开始/继续下一块 | 无 |
| 当日全部完成，含仅自由训练 | 完成事实与汇总保留 | 自由训练 → 动作选择 | 无“选择一个计划开始训练”大空态 |
| 已有最小化训练会话 | 保留状态 | 优先继续原会话，不建第二场 | 无 |
| 切换 owner 或跨日 | 重新按 owner/日历键计算 | 由新状态确定 | 不残留上一账号/昨日完成态 |

### 3.3 对旧契约的覆盖与保留

- R08 覆盖 DR-077 “封面右下已练”位置；R09 覆盖旧“保留已完成筛选”；R11 覆盖 v56 游客头部整块品牌绿。只改这些明确反馈，不重做全局配色。
- R02 覆盖 v8 “每题 zoom=0”的固定近景要求，但保留“每题确定性初始化”意图；相机变更后记录 DR 与 UI 规格变更，禁止单删旧注释就算完成。
- v51 命中区、真实 safe area、Light/Dark/动态字体契约继续成立；球库收紧与按钮增大都必须跨紧凑设备复验。
- v54 来源冻结、单 item 单训练 session、连续主线结算、跨日显式带入继续成立。W0/W1 若发现必须修改其语义，要先在方案版本里写清 delta。
- 保留当前 60 张封面试装、既有诊断测试改动与 output；v57 不涉及封面重新生成、统计页 v55 或发布。

## 4. 批次安排与依赖

共 9 批。W0–W2 共享首页/数据读写，串行完成；W3 的内容与导航、W5 的投影与空间、W7 的真机时间链各自留出独立验收上下文。暂不启动子智能体或新任务。

| 批次 | 范围 | 量级 | 依赖 | 状态 |
|---|---|---|---|---|
| W0 | 主线唯一性、读取统一、切换保留进度，R05 数据基础 | 中 | 本方案 | ✅ 2026-09-05，62 单测 / 2 UI / build；output/v57/W0 |
| W1 | 今日队列/建议/自由历史统一投影与动作/时间汇总，R15 数据基础 | 中大 | W0 | ✅ 2026-09-05，50 单测含磁盘恢复；output/v57/W1 |
| W2 | 首页完成态/自由入口、模版编辑、角标、滚动稳定，R01/R03/R05/R06/R15 UI | 中大 | W0、W1 | ✅ 2026-09-05，48 主矩阵 + 6 补验，261 图；output/v57/W2 |
| W3 | 课序与精简标题、动作详情往返，R04 | 中 | W0；与 W2 同文件改动先合并 | ✅ 2026-09-05，35 UI / 98 图 / 内容指纹及门禁；output/v57/W3 |
| W4 | 已练次数、移除筛选、加入按钮，R08/R09/R10 | 中 | 次数口径已确认 | ✅ 2026-09-05，31单测/12渲染图、24 UI/54图、标准build；output/v57/W4 |
| W5 | 3D 三目标取景、球库集中、答题按钮，R02/R07/R12 | 中大 | 几何技能；刷新 v51 基线 | ⚠️ 用户否决40pt球体/增高底栏，已撤回，按球桌主视觉重做；见FL-047 |
| W6 | 中性游客账号卡与 Pro 状态，R11 | 小 | 游客样式已确认；v56 tokens | ✅ 2026-09-05，27单测、16 UI/44图、build/门禁；output/v57/W6 |
| W7 | Live Activity 展开布局与跨分钟倒计时，R13/R14 | 中大 | 真机复现优先 | ⏳ |
| W8 | 跨批综合回归、原图逐条销账、文档收口 | 中 | W0–W7 | ⏳ |

依赖图（文本）：

```text
W0 -> W1 -> W2 ----+
 |-------> W3 ----|
W4 ---------------|
W5 ---------------+-> W8
W6 ---------------|
W7 ---------------+
```

### W0：主线状态服务

- 文件范围：`UserActivePlan`、`PlanDetailView.activatePlan`、首页 ViewModel/刷新签名、`PlanProgressService` 的读写入口；查清同模型全部 `.first` 消费者。
- 先把 owner 内唯一 active 查询与切换移至服务；支持 A/B 多个存档，只切状态，不删旧游标。重复点击幂等；save 失败明确反馈且 UI 不提前提交。已完成计划的重新开始是显式操作，不混同 paused 恢复。
- 历史异常多 active 必须有确定性处理与证据，不以无排序 `.first` 随机选择；优先复用现有迁移/规范化规则。
- 首页选课改由 currentLessonId 定位 stages/lessons；旧周/日仅留兼容影子。构造“新 lesson ID 与旧周/日不一致”的夹具，验证选课以新 ID 为准。
- **DoD**：A→B→A 游标一致；owner A/B 不串；全 paused 不显示进行中；切换失败回滚；active/status/lesson 更新后首页刷新；旧课完成不推进新主线；v54 相关单测与标准 build 通过。
- **交付证据**：状态 fixture、服务测试结果、计划卡与详情状态截图。若触及 schema/跨模块边界，按规则写 ADR，不默认先迁移模型。

### W1：今日安排展示模型

- 文件范围：`TodayTrainingScheduleService`、`ScheduledTrainingBlock`、`TrainingHomeViewModel` 与必要的纯聚合 helper。UI 改动限制为接入点，为 W2 留完整呈现。
- 按 §3 实现已持久课块/建议课/自由历史三源投影；身份稳定、去重明确、使用冻结 payload。一个模版与一节官方课按两块展示，不合并动作去掉来源。
- 共用动作/剂量估算，补足预计/真实用时区分；损坏或缺少 payload 显示不可用原因，不能静默空数组然后显示“0 项已完成”。
- **DoD**：官方课与模版均已入队时按两个正式课块展示，分别验证两种加入顺序与重启恢复；官方建议+模版作为另一场景；已入队官方课不重复；同动作跨两课计两项，完成第一课不能自动完成第二课；仅自由历史有完成态；已覆盖 session 不双计；删除/放弃/跨日/源删除/OTA 不改历史；同日/owner 边界测试通过；保存/退出/重试不虚增完成。
- **交付证据**：每个投影 fixture 的输入/输出对照与聚焦单测。聚合测试使用固定日期/时区与内存容器；持久恢复另用临时文件验证。

### W2：首页交互闭环

- 文件范围：`TrainingHomeView`、`TrainingHomeViewModel`、`PlanListView`；不重复实现 W0/W1 的数据规则。
- 全部完成后显示右下自由训练按钮，进入空白训练并打开动作选择；已有训练时先恢复。隐藏完成态下首次使用大空态。统一活跃 UI 文案“自由训练”；保留 `.free`、旧标识符及历史文档，非必要不改内部命名。
- 模版主体编辑；三点菜单加入；重复加入保持现有幂等提示；菜单点击不同时触发行手势。当前计划卡角标来自 W0 状态；保留进度的其他计划可辨识、可进入并切换，和未开始计划区分。
- 单课直接明细、多课标题折叠沿用 v54；建议课与持久课视觉同级但身份可辨。
- 滚动按真实锚点 Y 保持；明确从详情返回时的卡片恢复独立于筛选切换；短结果/空模版可容纳当前锚点位置。
- **DoD**：§3.2 全状态可见行为通过；自由训练添加→保存→返回→再次自由训练成功；切换筛选/Tab 后锚点最终 Y 漂移建议 ≤2pt，并录屏确认无中间闪跳；无/少/多结果、底部位置、标准/最大字体覆盖；模版点击不入队；菜单只加入一次；SE/标准手机/iPad Light/Dark 截图通过。

### W3：计划课序、标题与详情返回

- 文件范围：`PlanDetailView`、`TrainingRoute`/`MainTabView`、12 份 `QiuJi/Resources/Plans/*.json` 中必要的标题字段；读文案技能与内容契约后逐课处理。
- 独立日序标签 + 简短课题 + 状态/预计时间；移除页面 summary 展示。短标题需人工语义审阅，不按字符截短生成。阶段标题与课题避免同义重复，但课 ID/剂量/顺序不动。
- 行主体 push 同栈动作详情，展开剂量有独立按钮与 AX label。Pro 锁态走既有权限，不绕过；不可用动作有明确返回路径。
- **DoD**：12 计划、43 阶段、129 课结构和 ID/剂量指纹不变；逐课短标题审阅有清单；首/中/末阶段课序正确；单/多球形均能进入详情；返回恢复展开与位置；来源失效和 Pro 锁态可返回；内容门禁、导航测试与 build 通过。

### W4：动作库次数与操作样式

- 文件范围：`DrillListView`、`DrillListViewModel`、`BTDrillCard`、`DrillDetailView`。
- 持久条目聚合遵循 §1.1/§2.5；更新卡片接口的全部调用方。不要借次数改统计页的训练次数口径。
- 移除 completed 筛选分支与可见菜单；其他 tutorial kind 选项/筛选数量角标仍正确。仅加入训练可用态改 primary，保留执行逻辑。
- **DoD**：0/1/2/10/100/1000 次；同场同动作两 entry=2；同 entry 重读不累加；保存、删除、重启、账号切换更新；1→2 不被 Set 去重漏刷新；0 次无胶囊；数字与多杆标签不拥挤，最大字号 AX 数字完整；菜单无已完成；加入按钮仍打开正确 sheet/落库。聚焦单测、大小屏 Light/Dark 与 build 通过。

### W5：3D 与球桌控件

- 文件范围：`SceneAimingView`、`CameraRig` 的可选取景接口、`FreePlayView.paletteBar`、`BTBallPaletteBar.swift` 中三种球库及 metrics、`ShotStageMetrics`、全部球库调用页、AngleDynamicView 自定义两行球库与提球页自定义槽位、局部 `BTTextActionButton` 参数。完整调用清单见复审记录，练习页属于直接整改范围。另一 `enterAiming` 调用者 `AimPointSceneTrainingView` 必须回归，未授权不改变其默认语义。
- 先记录旧/参考图可辨的视觉差异，再建立带 viewport/HUD 安全区的三目标投影求解，最后调整控件。相机算法先输出诊断，后接 UI；不能只调 height 一个常量。
- **DoD**：固定种子覆盖六袋口、远近球/贴库球、左右摆向；记录母球/目标球/袋口包围体投影及近裁面断言；首题/连续换题/快速 2D↔3D/旋转尺寸后全部可见；环绕不被自动取景抢回；数值/截图均有证据。所有相关页面（包括练习页）的球库两行居中、视觉球间隙显著收紧，逐页截图核验；可点击页首末槽不串号，可拖动页拖入/拖回落点正确，参考/装饰页维持原有行为。辅助/答题不仅命中 ≥44pt，视觉字号/胶囊也适度增大，列定位不遮目标；SE、标准手机、iPad 和最低支持 Runtime 复验。
- **止损点**：一套取景范围不能覆盖合法题面时先提交失败样例与回退方案，不加题目 ID 特判。参数和视觉效果未验证前不宣称相机问题修好。

### W6：账号卡和会员信息

- 文件范围：`ProfileView`，确需复用时提取局部展示组件；复用 v56 tokens，无全局 token 调色。
- 游客中性卡与已登录头像/文字区对齐；点击只触发一次登录。Pro 图标、名称与真实权益分层，长昵称/较长有效期优先换行。
- **DoD**：Guest、登录 Free、月/年/永久 Pro、过期/恢复中状态；Light/Dark、高对比、最大字体和窄屏；不虚构永久权益；登录后的资料刷新与已有 v53 账号隔离行为保持。聚焦 UI、build 通过，真实 Apple/StoreKit 验证沿用人工边界。

### W7：Live Activity

- 文件范围：`QiuJiLiveActivity/RestTimerLiveActivity.swift`、`RestTimerAttributes`、manager、`ActiveTrainingViewModel` 的休息时钟。
- 按 §2.8 先完成最小复现与变体对照，再修文字/圆环/更新竞态的真实原因。核心时间使用统一 endDate；延长后起止区间/totalSeconds 明确同步，进度始终限于有效范围。
- 主复现：设置 120/180 秒直接启动，不加时后锁屏观察；独立对照：60/120 秒启动后 +30S。加时缺陷修复通过不能关闭直接长休息冻结。截图中 1:28/1:00 的显示一致性单独验收。
- 展开区域居中布局；compact/minimal/锁屏分开验收；长动作名不能把倒计时挤出。API availability 以实际支持 iOS 与 SDK 为准。
- **DoD**：59/60/61/90/120/180 秒连续观察；锁屏 120→119→61→60→59→0 的真实时间视频/逐帧证据；普通锁屏、常亮开/关、前后台切换；延长/缩短/取消/快速重启；到 0 不反向增长；新活动不被旧的清理任务结束；系统禁用活动不影响 App 内训练。真机长锁屏未测时只能标自动化完成，不能关闭 R14。

### W8：综合回归与收口

- 一条完整流程：A 主线加入两课→加入模版→进入模版编辑但不改冻结安排→完成部分课→切 B→回 A 进度正确→全部完成→自由训练追加动作→动作库次数更新→详情返回计划→组间休息锁屏倒计时。
- 逐条附 R01–R15 修复前/后证据、源码指纹、设备/系统/外观、状态 fixture 与结果；明确尚未完成的真机项。
- **DoD**：所有批次证据与最终源码一致；聚焦回归、标准 build、相关内容门禁与文档门禁通过；复核实际渲染 Light/Dark；开放项写明阻塞边界；PROGRESS、受影响规格/DR/ADR 同步。不自动 commit/push，不把方案完成当实施完成。

## 5. 已核实代码锚点索引

行号以 2026-09-05 文件为准；后续修改前用符号重新定位。这里仅列实际读过的实现，不列拟新增类型。

| 文件 | 行/符号 | 用途 |
|---|---|---|
| `QiuJi/Data/Models/UserActivePlan.swift` | 5、14–19 | owner/游标/status |
| `QiuJi/Features/Training/ViewModels/TrainingHomeViewModel.swift` | 126 load、134 descriptor、137 first | 主线未过滤 active |
| 同上 | 205 loadOfficialPlan、339 fetchTodayCompletedDrillIds | 旧周/日选课；当天按 planId/drillId 合并完成 |
| `QiuJi/Features/Training/Views/TrainingHomeView.swift` | 75 activePlanSignature、189 hasActivePlan、207 canStartPrimaryTraining | 刷新/主动作条件 |
| 同上 | 374 activePlanContent、391 scheduledBlocksSection | 队列和建议二选一、课块计数 |
| 同上 | 457 scheduleItemRow、551 scheduleItemDetails | 单块直接展开/多块折叠与明确开始 |
| 同上 | 1235 planBrowsingSection、1259 filterChips | 货架身份/主动 scrollTo |
| 同上 | 1424 onTapGesture、1499 requestUseForToday、1544 emptyStateContent | 模版入队/空态 |
| `QiuJi/Features/Training/Views/PlanDetailView.swift` | 457 lessonSection、551 drillTrackRow、1003 activatePlan | 标题说明/剂量展开/切换 |
| `QiuJi/Data/Services/PlanProgressService.swift` | 128 settleCompletedScheduleItem、297 fetchOfficialMainline | 结算与唯一主线读取 |
| `QiuJi/Data/Services/TodayTrainingScheduleService.swift` | 197 today、226 addOfficialLessons、296 addTemplate、331 addLibraryDrill | 入队/冻结/幂等 |
| 同上 | 125 ScheduledDrillSnapshot、492 snapshots | 冻结动作和剂量 |
| `QiuJi/Data/Models/TodayTrainingSchedule.swift` | 17 TodayScheduleItemState、25 TodayTrainingSchedule | 今日数据身份 |
| `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift` | 123 TrainingMode、148 ScheduledTrainingBlock、835 startRestTimer | 来源训练模式/冻结块/休息 |
| `QiuJi/Data/Services/TrainingDoseResolver.swift` | 146 estimatedMinutes | 既有估算规则 |
| `QiuJi/Features/Training/Views/PlanListView.swift` | 6 TrainingRoute | 训练栈路由 |
| `QiuJi/App/MainTabView.swift` | 150 trainingDestination | 同栈动作详情接入点 |
| `QiuJi/Features/DrillLibrary/Views/DrillListView.swift` | 28 completedDrillIds、50 onChange、246 卡片 | Bool 聚合与刷新 |
| `QiuJi/Features/DrillLibrary/ViewModels/DrillListViewModel.swift` | 59 DrillBadgeFilter、64 completed、121–122 Combine | 筛选及刷新依赖 |
| `QiuJi/Core/Components/BTDrillCard.swift` | 90 BTDrillGridCard、188 BTPracticedBadge | 已练布尔/位置/固定文案 |
| `QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift` | 484 bottomBar、508 secondary、518 primary | 加入/试打按钮 |
| `QiuJi/Features/Profile/Views/ProfileView.swift` | 112 loggedInHeader、138 Pro、182 guestHeader | 账号卡和真实权益 |
| `QiuJi/Features/AngleTraining/Views/SceneAimingView.swift` | 241 overlayLayer、488 applyAimingPoseForCurrentQuestion | 实际反馈页控件/取景 |
| `QiuJi/Core/Scene/CameraRig.swift` | 250 enterAiming、266 entryZoom | 固定近景姿态 |
| `QiuJi/Core/Components/BTBallPaletteBar.swift` | 9 metrics、140 columnWidth、158 paletteRow | 球直径/列间隙/槽 |
| 同上 | 244 BTReferenceBallPalette、272 referenceRow | R07 真正的参考球库 |
| `QiuJi/Features/PositionPlay/Views/FreePlayView.swift` | 626 paletteBar | 每日清台实际入口，点击高亮/提示，无拖动 |
| `QiuJi/Core/Components/ShotTableLayout.swift` | 136 paletteDiameter、142 paletteWidth | 参考球库的直径与宽度来源 |
| `QiuJi/Core/Components/BTShotPageChrome.swift` | 132 BTTextActionButton | 13pt/30pt 文字按钮 |
| `QiuJiLiveActivity/RestTimerLiveActivity.swift` | 5 Widget、14 leading、19 trailing、54 lockScreenView | 三种计时呈现/布局 |
| `QiuJi/Core/LiveActivity/RestTimerLiveActivityManager.swift` | 15 start、38 update、49 end、114 stale cleanup | 活动生命周期 |
| `QiuJi/Core/LiveActivity/RestTimerAttributes.swift` | 5 ContentState | endDate/totalSeconds |
| `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift` | 919 addRestTime | App 总时长更新但未传入 manager |
| `.kiro/steering/content-data-contract.md` | §5.3、§9.1、§9.2 | 训练 kind、工具排除、v54 编排真源 |

## 6. 验证纪律、冲突与恢复

### 6.1 本轮验证范围

本轮只做文本方案与原图归档：读完原任务全部用户反馈、查看 19 张图的分组联系表、读取表中关键实现、核对已有数据契约。未运行 App、未做真机复现、未跑业务 build/XCTest，所有 W0–W8 仍为待实施。

v57.2 复审进一步逐张打开 19 张原始截图，重新校验哈希 19/19、核对各用户原话，补做 6 组时钟输入数值复算。复算只验证数据流，未重现系统倒计时冻结。没有业务代码变更。

文档交付执行：原图 SHA-256 一致性、15 组/19 图映射、R01–R15 与 W0–W8 覆盖、锚点文件存在、`git diff --check` 和 `make -f scripts/Makefile verify-doc-size`。实际结果另存 `output/v57-feedback/VERIFICATION.md`。

### 6.2 后续测试选择与旧断言

- 数据优先回归现有 `V54ScheduleDomainTests`、`V54TrainingTransactionTests`、`V54PresentationStateTests`、`TrainingHomeViewModelTests`；如触及迁移，再跑 `V54DataMigrationTests`。UI 使用 `V54ScheduleUITests` 和受影响页面聚焦用例，新增能击中本轮失效机理的用例。
- `DrillListViewModelTests` 已有 single/multi/ruleset 筛选测试；这些前提未变，不删不降级。移除 completed enum case 导致的编译引用要逐处查明，不将所有“已完成”断言一起替换，因为今日课/训练完成仍合法。
- 计数用例必须检出“同动作再次保存但 ID Set 不变”的缺陷；滚动用例要测前后 anchor Y，而非只查筛选按钮存在；相机要测实际投影而非仅检查 zoom 参数；倒计时要测时间变化而非静态截图有数字。
- 不预先指定删除任何既有断言。实施时若必须修改，记录断言原文、旧前提、新行为与失效机制，确认是需求变化再更新；不能靠跳过/删断言求绿。

### 6.3 写盘与并行工作区

接手基线已有 60 张封面修改、`QiuJiTests/BankFrozenRailDiagTests.swift` 修改以及 output/tmp/测试 selector 等未跟踪内容。本轮不清理、不回退、不暂存这些内容。

`QiuJi.xcscheme` 默认包含 QiuJiTests 和 QiuJiUITests，未限定安全 selector；不能直接把无筛选全量测试当成无副作用检查。本方案不承诺“全仓零 diff”，只验证“原有变更 + 本轮预期变更”。执行批次前根据该批测试明确写盘清单；跑全量前用 `rg -l '\.write\(|FileManager.*create' QiuJiTests QiuJiUITests` 加人工检查脚本调用/实际 scheme，全量盘点后才能承诺测试资产不被污染。诊断/bake/export 测试隔离到任务输出目录，禁止用清理未知资产恢复所谓干净状态。

| 冲突面 | 处理 |
|---|---|
| W0/W1/W2 首页 ViewModel 和 View | 串行实施，前批验证后再改 |
| W0/W3 PlanDetailView | W0 先落状态服务，W3 再改内容/导航 |
| W4 与 W3 DrillDetail/导航 | 先合已完成批，再跑双入口返回/加入训练 |
| W5 与既有 v51 布局/CameraRig 消费者 | 不覆盖已修响应式契约，按实测适配 |
| W6 与 v53/v56 Profile、Premium | 使用现有 tokens 与真实账号/权益数据 |
| W7 与训练休息/最小化 | 单独验证休息覆层、训练会话、Live Activity 三套生命周期 |
| 进度/报告 | 每批结束写当前状态，超过 10 条滚动归档，不占用业务文件做交接 |

### 6.4 每批恢复包

每批以本文件对应章节开始，保存：当前 HEAD、相关 dirty 文件/哈希、实际变更清单、测试命令与 exit code、源码/fixture 指纹、截图或视频路径、未完成项。输出采用 `output/v57/Wn/` 或任务指定 build 目录。断流后只恢复未完成项，不要求重读整段长会话；用户原话与图始终从 SOURCE.md/manifest 找回。

## 7. 版本记录

| 版本 | 日期 | 变化 |
|---|---|---|
| v57.2 | 2026-09-05 | 逐张重审 19 原图；纠正 R07 截图组件定位，并纳入用户补充的所有相关页面（含练习页）统一收紧球库；补 R15 两者已入队独立场景、旧周日选课/跨课完成误判、其他已激活计划可辨识；用户确认直接长休息不加时也冻结，加时 totalSeconds 缺陷单列，禁止替代主根因。 |
| v57.1 | 2026-09-05 | 接手断流任务，归档 15 组/19 图，重新核对代码并拆 W0–W8；纳入用户确认：中性游客账号卡、按每个已保存动作条目计次。区分已确认路径与锁屏/相机待验证推测，保留 v54 显式入队和冻结语义。 |

高版本覆盖低版本；方案交付与实施完成分别记录。
