# 问题集合 v54 — 官方主线计划、今日编排与模版并行重构

> **本文件是本轮需求真源**（v54.10，制定日期：2026-09-03，状态：**代码完成（W0–W9；人工体验检查 H-27 待执行）**）。
> **用户目标**：官方计划保持唯一主线，模版与动作不再冒充或替换计划；用户只通过“编排今天”选择想练的内容和顺序，系统自动判断复练、连续推进或提前练习，并允许少练、多练、一天练多课、漏练及重复任意已学课程。
> **执行边界**：v54.0 仅使用 `issue-collection-restructure` 完成规划；用户随后已授权按 W0–W9 执行。实施仍按批次、定向路径和证据门禁推进，不创建 worktree、不配置执行模型；此前生成的 UI 概念图仅是讨论材料，不是本方案的视觉真源。
> **前置依赖**：v53 的 SwiftData V4/owner 隔离工作必须先稳定收口。执行 v54 时若仓库实际 schema 版本已高于 V4，以下“V5”一律顺延为“下一个可用版本”，禁止改写已发布历史 schema。
> **版本说明**：仓库已有其他主题的 `问题集合_v2.md`–`问题集合_v53.md`；本轮新建 v54，不覆盖既有问题集合及当前工作区其他未提交改动。

---

## 一、需求回显与不可弱化口径

| ID | 讨论诉求 | 本方案的不可弱化口径 |
|---|---|---|
| R1 | 官方计划和模版应并行，官方计划只允许一个 | 每个 owner 最多一个进行中的官方主线计划；模版只是可复用训练内容，不占“激活计划”名额，也不改变主线游标 |
| R2 | 今日有计划时，用户仍可选模版或动作训练 | 模版、动作、官方计划课程都能加入同一个“今日安排”；添加任一来源不删除其他来源 |
| R3 | 不要让用户处理过多“替换/附加/前后”选项 | 用户只负责选内容和必要时拖动顺序；系统负责归类复练/推进/预习，默认追加到未完成内容之后 |
| R4 | 可重复官方计划中的任意一天 | 官方计划详情允许选择已完成课程再次加入今日安排；复练产生真实记录，但不倒退或重复推进主线 |
| R5 | 可同时选择练过的、今天的、明天或更远内容 | 已完成课程为复练；从当前课开始的连续课程可逐课推进；跳过中间课程选更远课程只算提前练习，不跨缺口推进 |
| R6 | 一周 4 练只练 3 次、想多练、一天练两课、今天没练 | 周目标是统计和预计完成时间的参数，不是自动排课器；少练不跳课，多练可多推进，同一天仍只计一个训练日，没练不推进 |
| R7 | 计划不应有固定“多少周”的概念 | 官方计划内容结构从“第几周/第几天”改为“阶段/课程”；周数只根据剩余课程与个人周目标动态估算，不再写死进度 |
| R8 | 预计周数应与一周几练关联 | 展示“按每周 X 练，预计约 Y 周完成”；X 来自当前 owner 的周目标，Y 随剩余主线课程动态变化，仅作估算 |
| R9 | “编排今天”放在当前训练计划里 | 当前官方计划详情主按钮为“编排今天”；计划选择器只选该计划的课程，不混入模版或动作库入口 |
| R10 | 模版可从模版页加入，动作可从动作库加入 | 各来源在各自语境中完成“加入今日安排”；今日卡片负责统一排序、删除未完成项和开始下一项 |
| R11 | 今日安排保留来源，但此前来源标签方案不合适 | 来源必须持久化并进入历史记录/回溯；训练首页不强制展示彩色来源标签，以简洁标题、阶段课程副标题和记录详情面包屑表达 |
| R12 | 先整体整理方案，不改代码 | 本文件只定义决策、问题、批次、验收和锚点；所有实现项保持未开始 |

### 1.1 一句话产品模型

> **计划回答“长期按什么顺序成长”，今日安排回答“今天具体练什么”，模版回答“哪些内容值得反复组合使用”。**

三者不再共用“激活”概念：

```text
唯一官方主线计划 ──提供当前课/历史课/未来课──┐
我的模版 ─────────提供可复用训练块──────────┼──> 今日安排 ──> 逐块训练 ──> 训练记录
动作库 ───────────提供单动作训练块──────────┘                     │
                                                                  └──仅连续主线课推进官方计划
```

### 1.2 本轮默认产品决策

1. **官方计划唯一，模版不激活。** `UserActivePlan` 最终只表示官方计划；模版仍可创建、编辑、删除和重复加入今日安排。
2. **今日安排按本地自然日存在。** 每个 owner、每个本地日期最多一个可编辑安排；时区按创建时设备日历冻结，跨零点不把旧日条目静默算到新一天。
3. **安排以“训练块”为单位。** 一节官方课程、一个完整模版、一个动作分别是一块；不把模版无提示拆散成若干来源不明的动作。
4. **默认追加，不弹策略选择。** 从计划、模版或动作页加入时，放到今日未完成项末尾；用户只在今日卡片编辑态拖动顺序。
5. **完成项固定，未完成项可改。** 已开始或完成的训练块不能通过拖动/删除改写历史；未完成块可排序和删除。
6. **主线推进只看连续前缀。** 只有从当前课程开始、且没有课程缺口的选中课程具备推进资格；历史课程是复练，跨缺口未来课程是提前练习。
7. **推进资格在保存编排时冻结。** 后续完成别的课程不能让原先的“跨缺口预习”悄然变成可推进项；需要再次加入时重新判定。
8. **实际完成才推进。** 加入、排序、开始或中途退出都不推进；保存完成记录后，按幂等规则结算。
9. **一次训练会话对应一个训练块。** 混合来源不再塞进一个只有单一 `planId` 的会话，保证来源、恢复、同步和推进准确。
10. **日常清台等工具模式不进入今日安排。** 它们仍按 `kind="tool"` 独立记录，不计训练目标、不影响计划游标。

---

## 二、当前实现事实与问题清单（2026-09-03 实读）

### 2.1 当前事实

| 领域 | 当前实现 | 直接后果 |
|---|---|---|
| 激活关系 | `UserActivePlan` 同时保存官方计划和自定义模版，用 `isCustom` 区分 | “使用模版训练”必须先替换官方计划，两个概念被错误互斥 |
| 模版入口 | `DrillTrainingPlanService.activate` 删除所有 active row，再插入 custom active row | 模版并行需求无法成立，官方主线在 UI 中消失 |
| 今日内容 | `TrainingHomeViewModel` 从第一条 active plan 临时推导一个 `TodaySessionInfo` | 没有持久化队列、排序、块状态或跨来源来源快照 |
| 补充训练 | 今日其他 `TrainingSession` 完成后才被重建为 supplemental drills | 用户训练前无法编排；只看结果，不能表达待练和顺序 |
| 官方内容 | `OfficialPlan → weeks → sessions(dayNumber)`，游标为 `currentWeek/currentDay` | 周和天既承担课程结构又承担日历语义，少练/多练/跨日无法自然解释 |
| 完成推进 | 保存的 `TrainingSession.planId` 与 active plan 相等就推进一次 | 无法区分复练、连续下一课和跳课预习；同一会话只能有一个来源 |
| 训练模式 | `.plan(drills:, planId:)` 一次启动整组 drill | 混合编排会丢失块边界，无法逐块保存来源与进度作用 |
| 训练记录 | `TrainingSession` 只有 `planId`，没有模版/动作/课程/今日项来源 | 来源删除、OTA 更新或跨端恢复后无法可靠解释“这次从哪来” |
| 计划详情 | 主 CTA 只有“开始此计划”或被动“当前已激活”；列表按第 N 周/第 N 天展示 | 当前计划没有“编排今天”，已完成课程也没有明确复练入口 |
| 动作详情 | “加入训练”只能加入自定义模版；官方计划激活时提示不能加入 | 动作无法直接加入今天，用户被迫先创建模版并替换计划 |
| 计划元数据 | 首页和详情使用固定 `durationWeeks/sessionsPerWeek` | 预计周数与用户每周目标脱节，也把课程误当日历排期 |
| 统计口径 | `weeklyGoalDays` 是 owner 目标，训练按自然日去重 | 已具备“同日多练仍算一天”的正确基础，可继续沿用 |
| 数据迁移 | 当前工作区处于 SwiftData V4 owner 隔离阶段 | v54 必须建立在 V4 稳定版本上，以新 schema 引入今日安排 |

### 2.2 问题总表

| ID | 优先级 | 问题/风险 | 归属批次 |
|---|---:|---|---|
| P54-01 | P0 | 官方计划与模版共用 active row，模版会替换官方主线 | W4、W8 |
| P54-02 | P0 | 没有“今日安排”实体，无法在训练前保存跨来源队列、顺序和状态 | W4、W5 |
| P54-03 | P0 | 仅靠 `TrainingSession.planId` 自动推进，复练和预习会误推进 | W5、W6 |
| P54-04 | P0 | week/day 游标无法可靠表达少练、多练、一天多课和无训练日 | W1–W5 |
| P54-05 | P0 | 跳过中间课选更远课程时缺少“不跨缺口”的确定性规则 | W0、W5 |
| P54-06 | P1 | 计划详情没有选课式“编排今天”，已完成课程难以复练 | W7 |
| P54-07 | P1 | 模版和动作无法直接加入今天，必须经过替换计划或先建模版 | W8 |
| P54-08 | P1 | 单次混合会话只有一个 planId，来源、同步和恢复语义会失真 | W6 |
| P54-09 | P1 | 固定预计周数不读取 `weeklyGoalDays`，改变训练频率后不更新 | W5、W7 |
| P54-10 | P1 | 官方内容与校验脚本大量依赖 week/day 和建议周 | W1–W3 |
| P54-11 | P1 | 旧 custom active、重复 official active 和半迁移状态没有升级策略 | W4 |
| P54-12 | P1 | owner 转移没有今日安排，登录迁移后可能丢失或串用当日队列 | W4 |
| P54-13 | P1 | 训练来源未同步，换设备恢复后历史详情不能解释课程/模版来源 | W6、W9 |
| P54-14 | P2 | 未完成的昨日安排若直接丢弃或自动顺延，都会违背部分用户预期 | W5、W8 |
| P54-15 | P2 | 来源标签若直接堆在今日卡片，会增加噪声并重复此前不满意方案 | W7–W9 |
| P54-16 | P1 | 内容、代码、测试、研究文档和规格对“周”的定义可能各自迁一半 | W1–W3、W9 |

### 2.3 当前工作区边界

- 当前工作区已有大量未提交/未跟踪改动，包含 v50–v53、训练首页、计划详情、SwiftData V4、截图与内容资产。
- v54 执行时每批只记录本批**定向路径基线**；禁止 `reset --hard`、`checkout --`、`clean`、整仓覆盖或把他人改动纳入本批结果。
- v54.0 的创建不代表上述问题已修复；当前没有运行迁移、构建、测试、模拟器或真机验收。
- 若 v53 尚未完成迁移/发布门禁，v54 最多推进到不触碰 schema 的 W0–W3；W4 前必须重新核对实际 current schema。

---

## 三、目标领域模型

### 3.1 三个独立对象

| 对象 | 职责 | 数量约束 | 是否同步 |
|---|---|---|---|
| `UserActivePlan` | 当前唯一官方成长主线和课程游标 | 每 owner 0 或 1 个进行中计划 | 维持当前本地边界；本轮不扩云端计划同步 |
| `TodayTrainingSchedule` | 某 owner 在某本地日期的可编辑训练容器 | 每 owner + localDayKey 0 或 1 个 | 本地；登录 owner 迁移时随 owner 转移 |
| `TodayScheduleItem` | 有序训练块、来源快照、运行状态和进度作用 | 每个 schedule 0–N 个 | 随 schedule 本地保存；完成后来源快照写入可同步记录 |
| `CustomPlan` | 用户维护的可复用模版 | 0–N 个 | 延续当前本地边界，不再激活 |
| `TrainingSession` | 已发生训练的不可变事实 | 0–N 个 | 延续现有同步，并增加向前兼容来源字段 |

### 3.2 官方计划内容：从周课表到课程序列

目标结构：

```text
OfficialPlan
├── id / title / description
├── estimatedMinutesPerLesson
└── stages: [PlanStage]
    ├── id                 // 稳定，不用数组下标承担身份
    ├── order
    ├── title / goal
    └── lessons: [PlanLesson]
        ├── id             // 全计划稳定唯一
        ├── order
        ├── title / summary
        ├── phases         // 沿用 warmup/main/cooldown 与剂量
        └── prerequisite / review 元数据（若现有内容需要）
```

迁移原则：

- 旧 `week` 可作为第一版 `stage` 的编辑基础，但不是简单把中文“周”改成“阶段”；阶段标题要表达能力目标。
- 旧 `day` 迁为 `lesson`，每节课获得稳定 `lessonId` 和可读标题；后续 OTA 排序不能靠 `dayNumber` 猜身份。
- 12 个官方计划当前共 129 节旧 session，分两批迁移和审校，避免一个会话同时改完全量内容、脚本和测试。
- `durationWeeks`、`sessionsPerWeek` 不再是内容结构真源；过渡解码仅为兼容旧 bundle/OTA，全部内置内容迁完后删除生产依赖。
- 现有“建议周”“本周齐全”等门禁不能直接删除：分别迁为“最早阶段/先修顺序”和“阶段覆盖完整性”。

### 3.3 官方主线游标

`UserActivePlan` 目标字段语义：

```text
planId: String
currentLessonId: String?      // 第一节尚未完成的主线课程
status: active | paused | completed
startedAt: Date
updatedAt: Date
completedAt: Date?            // 末课完成后置位
ownerKey: String
```

- 同一 owner + planId 最多一条进度，同一 owner 最多一个 `status == active`；paused/completed 进度可保留多条。
- 激活另一官方计划必须确认“切换主线”；原 active 变为 paused，新计划恢复已有游标或从首课开始，不删除训练历史和旧计划进度。
- 计划完成后不制造“永远停在最后一天”的假 current；展示完成态，仍可从详情复练任意课程。
- `currentWeek/currentDay/isCustom` 仅作为迁移输入；新业务不得继续写入或分支判断。

### 3.4 今日安排与训练块

建议字段语义（具体 SwiftData 标注在 W4 落地时确定）：

```text
TodayTrainingSchedule
├── id / ownerKey
├── localDayKey / calendarIdentifier / timeZoneIdentifier
├── createdAt / updatedAt / archivedAt
└── items: [TodayScheduleItem]

TodayScheduleItem
├── id / orderIndex
├── sourceKind: officialLesson | template | libraryDrill
├── sourceId / sourceParentId
├── sourceTitleSnapshot / sourceSubtitleSnapshot
├── payloadVersion / payloadSnapshot
├── progressRole: review | advanceEligible | preview | neutral
├── planId? / lessonId?
├── state: pending | inProgress | completed | abandoned
├── startedAt? / completedAt? / trainingSessionId?
└── createdAt
```

关键约束：

1. `payloadSnapshot` 是版本化 Codable 数据，冻结实际 drill、剂量、顺序和必要显示信息；源模版被编辑/删除或官方内容 OTA 更新后，当日安排与历史记录不漂移。
2. 同一未完成来源默认去重并提示“已在今日安排”；已完成后再次加入会创建新 item，文案为“再练一次”。
3. 删除只允许 `pending`；开始后若退出，保留 `inProgress` 并显示“继续”。确需放弃时显式记 `abandoned`，不伪装成从未存在。
4. 当日训练块可拖动顺序，但顺序不改变已冻结的 `progressRole`。
5. schedule 本身不计统计；只有生成 `TrainingSession(kind="drill")` 的完成块才计训练日和训练量。

---

## 四、用户选择到系统结果的统一算法

### 4.1 课程位置分类

设官方计划所有课程按稳定顺序映射为 ordinal，当前游标为 `C`，本次从该计划选中的课程 ordinal 集合为 `S`：

1. `s < C`：`review`，复练，不改变游标。
2. 从 `C` 开始，`C, C+1, ... C+k` 连续存在于 `S` 的最长前缀：全部为 `advanceEligible`。
3. `s > C+k`：`preview`，提前练习；即便完成，也不跳过缺失课程。
4. 模版与动作：`neutral`，只产生训练记录，不改变官方计划。

### 4.2 典型场景结果

| 用户选择 | 系统归类 | 完成后的主线结果 |
|---|---|---|
| 只选已完成第 2 课 | 复练 | 不动 |
| 选第 2 课 + 当前第 5 课 | 复练 + 可推进 | 第 5 课完成后推进 1 课 |
| 选当前第 5 课 + 下一第 6 课 | 两课均可推进 | 两课都完成后最多推进 2 课 |
| 选当前第 5 课 + 第 7 课 | 第 5 课可推进，第 7 课为预习 | 最多推进到第 6 课，不跳过第 6 课 |
| 只选未来第 7 课 | 预习 | 不动 |
| 当前课 + 模版 + 单动作 | 当前课可推进，另两块中立 | 当前课完成后推进 1 课 |
| 同一已完成课程再加入 | 新的复练 item | 不动，历史新增一次真实训练 |

### 4.3 完成顺序与幂等结算

- `progressRole` 在保存编排时冻结；训练块可以按用户拖动后的任意顺序完成。
- 每次完成 item 后，进度服务从该 schedule 的冻结集合中重新计算“已完成的可推进连续前缀”。因此先练第 6 课、后练第 5 课时，最终仍可一次结算到第 7 课。
- 每个 `scheduleItemId + lessonId` 只能结算一次；同步重放、保存重试、App 重启或重复回调不得二次推进。
- 原本因缺第 6 课而归为 preview 的第 7 课，之后不会自动转成 eligible；用户在它成为当前课后重新加入，才获得新的推进资格。
- 用户切换官方主线后，旧计划尚未完成的 item 可继续训练并留来源记录，但自动失去推进新计划的资格；不能用旧计划 item 推动新游标。

### 4.4 周目标、预计完成时间和漏练

公式：

```text
预计剩余周数 = ceil(剩余主线课程数 / max(weeklyGoalDays, 1))
```

该估算以“一个训练日完成一节主线课程”为默认节奏；同日多练会让实际完成更早，因此文案必须使用“预计约”，不能显示为承诺日期。

显示规则：

- 计划未开始：`按每周 4 练，预计约 3 周完成`。
- 进行中：按第一节未完成主线课计算剩余课数，动态更新。
- 同一天完成两节主线课：游标推进两节，剩余周数随之下降；周目标统计仍只增加 1 个训练日。
- 一周目标 4 天只练 3 天：周目标显示 3/4，官方游标停在首个未完成课程，不补签、不跳课、不判计划失败。
- 今天没有练：不推进。次日仍建议同一当前课程。
- 用户把周目标从 4 改为 3：只重算估计，不改内容、游标或历史。
- “预计约 N 周”不是截止日期，不生成强制日历课表；需要节奏提示时复用现有提醒能力，不另造逾期状态。

### 4.5 跨日与昨日未完成

- 新自然日首次进入训练首页时，昨日 schedule 归档，未完成项不自动塞进今天，避免形成不可控待办积压。
- 若昨日有未完成块，在今日卡片顶部提供一条轻量入口：`昨日有 2 项未完成 · 加入今天`。
- 用户点击后把仍可解析的 payload 快照复制成今日新 item，并按今天的当前游标重新分类；昨日 item 保持归档事实，不改成完成。
- 不提供全局自动顺延开关；它会把“训练建议”变成任务压力，也会导致跨多日重复堆积。

---

## 五、UI 与交互方案

### 5.1 训练首页：保持两张独立卡

继续保持“本周进度”和“今日安排”为两个同级卡片，不合并成一张大卡。

今日卡片状态：

| 状态 | 内容 | 主操作 |
|---|---|---|
| 无官方计划、无今日项 | 简短空态，提示可从计划/模版/动作加入 | `去选择内容` |
| 有官方计划、未编排 | 展示当前课建议，不立即建持久化 item | `开始当前课`、次操作 `编排今天` |
| 已有今日项 | 按训练块显示标题、副标题、完成/继续状态 | `开始下一项` / `继续训练` |
| 全部完成 | 今日完成摘要，可继续追加或复练 | `再加一项` |
| 昨日有未完成 | 卡片顶部一行轻提示 | `加入今天` |

编辑态：

- 入口放在“今日安排”卡片右上角；只显示未完成块的拖动和删除控制。
- 一块官方课程显示课程标题与“阶段名 · 第 N 课”；模版显示模版名及动作数；单动作显示动作名。**不使用大面积彩色“官方/模版/动作库”标签。**
- 来源只在需要解释和回溯时露出：长按/更多菜单、训练记录详情、来源仍存在时的返回入口。
- 单一底部“训练”按钮启动/继续排序最前的未完成块，不一次把所有块合成一个 session。

### 5.2 官方计划详情

头部元数据：

```text
3 个阶段 · 9 节课 · 每课约 25 分钟
按每周 4 练，预计约 3 周完成
```

CTA：

| 状态 | 主按钮 | 行为 |
|---|---|---|
| 无主线计划 | `开始此计划` | 激活并将第一课作为当前建议；不强制立即训练 |
| 当前主线计划 | `编排今天` | 打开本计划课程选择器 |
| 另一个主线已激活 | `切换到此计划` | 明确确认主线切换；今日已有安排不被静默删除 |
| 计划已完成 | `选择课程复练` | 打开同一选择器，默认不产生推进作用 |

课程列表：

- 以阶段折叠展示课程，不再出现“第 N 周/第 N 天”。
- 课程状态控制在四类：已完成、当前、提前练过、未开始。
- 点课程进入课程详情；每行可以选入编排，但不再单独堆一个长期可见的“选择”按钮。
- 页面主 CTA 打开选择器后用 checkbox 多选，更适合连续选择当前课和下一课。

### 5.3 “编排今天”选择器

- 这是**计划内选择器**，只显示当前官方计划的课程；模版与动作仍从各自页面加入，避免顶部再出现来源 Tab。
- 默认勾选当前课；已完成课程可选作复练，未来课程可选作连续推进或预习。
- 底部只给一条结果摘要，不要求用户理解算法：
  - `将加入 3 项：复练 1 课，完成后最多推进 2 课`
  - `第 7 课会作为提前练习，不会跳过第 6 课`
- 点击 `加入今日安排` 后默认追加；若同一 pending 课程已存在，则保持一份并定位到已有项。
- 排序统一回到今日卡片编辑态完成，选择器不重复实现拖动。

### 5.4 模版与动作入口

模版：

- 模版卡点击进入详情/编辑，不再直接“用于今日训练”并替换 active plan。
- 主操作为 `加入今日安排`；整个模版作为一个训练块，副文案显示动作数和预计量。
- 若同一 pending 模版已存在，显示 `已在今日安排`；该块完成后变为 `再练一次`。
- `sessionsPerWeek` 从模版 UI 和行为语义移除；若为旧数据兼容字段，只在迁移层保留默认值。

动作详情：

- 现有“加入训练”sheet 拆成两个真实目的：`加入今日安排`、`加入我的模版`。
- 即使官方主线已激活，也能直接加入今日安排，不弹“先替换官方计划”。
- `上手试打` 保持即时工具语义，不自动加入今日安排、不写主线来源、不计训练目标。

### 5.5 训练记录中的来源

- 列表页不增加醒目标签墙；记录详情在标题下用一行面包屑：
  - `基本功入门 · 阶段 2 · 第 4 课`
  - `来自模版：中袋专项`
  - `来自动作库：直线定杆`
- 面包屑读取保存时快照；来源仍存在时可点回详情，来源已删除时仍展示快照但不提供失效链接。
- 历史会话永远不因计划 OTA、课程改名或模版编辑而回写改变。

---

## 六、训练保存、来源与同步契约

### 6.1 训练模式

将当前“整组计划 + 一个 planId”迁为按块启动：

```text
TrainingMode.scheduled(
    scheduleItemId,
    payloadSnapshot,
    sourceSnapshot,
    progressRole
)
```

- 一次完成一个 `TodayScheduleItem`，保存一个 `TrainingSession`。
- 完成后回到今日安排，自动定位下一 pending item；是否马上继续由用户决定，不强制连播。
- free/tool 现有模式继续保留，不能为了统一 API 把工具训练伪装成 scheduled drill。

### 6.2 `TrainingSession` 来源字段

建议新增可空字段，旧记录保持可解码：

```text
scheduleItemId: UUID?
sourceKind: String?             // officialLesson | template | libraryDrill
sourceId: String?
sourceParentId: String?
sourceTitleSnapshot: String?
sourceSubtitleSnapshot: String?
officialPlanId: String?
officialLessonId: String?
progressEffect: String?         // review | advanced | preview | neutral
```

- 现有 `planId` 在过渡期继续保留；新记录只在官方课程来源时填写，模版不再把 UUID 填进 `planId`。
- `progressEffect` 记录实际结算结果，不只复制编排预期；中途退出或切换计划导致未推进时必须诚实保存。
- 新字段进入 `TrainingSessionDTO`、后端 Mongoose schema 和 restore 映射；全部采用旧服务端/旧记录兼容的 optional + `decodeIfPresent` 策略。
- 今日 schedule、active plan 和 custom template 延续本地边界；完成后的 TrainingSession 来源快照随训练记录同步。

### 6.3 推进事务

保存完成必须形成可恢复顺序：

1. 创建/更新 `TrainingSession` 和完整 drill 子树。
2. 将 schedule item 标为 completed 并绑定 session id。
3. 用 item 的冻结资格和当前官方计划身份做幂等结算。
4. 更新 `UserActivePlan.currentLessonId/completedAt`。
5. 单一 `ModelContext.save()` 提交本地事务；同步队列在本地成功后入队。

若进度结算失败：

- 不得丢弃已完成训练成绩；记录明确诊断并在下次加载通过 `scheduleItemId` 重试幂等结算。
- UI 可以提示“训练已保存，计划进度待更新”，不能假装全部成功或要求用户重练。

---

## 七、迁移与兼容方案

### 7.1 SwiftData 下一版本迁移

执行 W4 时建立下一可用 schema（按当前事实预计 V5）：

- 新增 `TodayTrainingSchedule`、`TodayScheduleItem`。
- `UserActivePlan` 增加 lesson 游标与完成态；兼容读取旧 week/day/isCustom。
- `TrainingSession` 增加来源与结算字段。
- `OwnerTransferService` 纳入 schedule 及 item 父子树；重复 owner 合并必须有确定策略。

### 7.2 旧 active row 归一化

| 旧数据 | 迁移结果 |
|---|---|
| 一条官方 active | 用旧 `currentWeek/currentDay` 映射到迁移后 stable lessonId |
| 一条 custom active | custom plan 保留为模版；把其冻结内容复制为迁移当日的今日训练块；删除 custom active 语义 |
| 同 owner 多条官方 active | 按最新 `startDate` 选择当前主线；其他进度保留为可恢复快照并记录诊断，禁止无声随机取第一条 |
| 官方 + custom 同时存在 | 官方保留主线；custom 复制到今日安排，两者并行 |
| 找不到旧 plan/session | active row 进入可诊断失效态或安全移除；训练历史不删，不能索引越界 |
| 迁移中断后重开 | 扫描 sentinel/缺失字段并幂等补偿，不重复创建今日 item |

### 7.3 内容兼容窗口

- W1 先实现新旧双读和结构校验；W2/W3 把 12 个 bundle 计划全部迁为 stage/lesson。
- 兼容期只允许读旧结构，新增或修改官方内容必须写新结构。
- W3 完成后 production bundle 不得再依赖 `durationWeeks/currentWeek/dayNumber`；是否删除 decoder 兼容由 W9 依据 OTA 最低兼容版本决定。
- 内容迁移前后要保留 drill 引用、phase 剂量、reviewFrom、intro 顺序和能力递进，不借结构重命名偷改训练内容。

---

## 八、范围与冻结项

### 8.1 本轮实施范围

1. 官方计划 stage/lesson 内容模型、12 个内置计划迁移及校验门禁。
2. 唯一官方主线、动态预计周数与完成态。
3. 今日安排 SwiftData 实体、跨日归档、昨日未完成恢复、owner 转移。
4. 复练/连续推进/预习/中立的纯规则与幂等进度服务。
5. 按训练块运行、训练记录来源快照、客户端/后端同步恢复。
6. 官方计划详情与计划内“编排今天”。
7. 模版/动作加入今日安排、训练首页排序和逐块开始。
8. 历史来源详情、规格、测试与多状态 UI 验收。

### 8.2 明确冻结

- 不做多官方计划同时激活，不做多条主线并行推进。
- 不做按周自动生成强制日历、不做补签、请假、逾期惩罚或自动跳课。
- 不做云端同步 active plan、custom template 或未完成 today schedule；只同步完成训练来源快照。
- 不做 AI 自动排课、智能推荐排序或基于表现动态改课。
- 不改变训练剂量、评分、付费权限和动作内容本身，除 stage/lesson 必需的标题与稳定 ID。
- 不把日常清台、自由试打或工具使用纳入今日安排/周训练目标。
- 不在首页堆叠彩色来源标签，不再以此前生成图作为验收依据。
- 不在本方案阶段创建 worktree、分派实现智能体或选择模型。

---

## 九、执行批次与依赖

### 9.1 批次总表

| 批次 | 目标 | 主要范围 | 规模 | 前置 | 状态 |
|---|---|---|---:|---|---|
| W0 | 契约与 fixture 冻结 | 决策表、算法样例、迁移样本、基线测试 | M | v53 边界确认 | ✅ 2026-09-03 |
| W1 | 新内容模型与双读门禁 | stage/lesson 类型、stable ID、validator/脚本兼容 | L | W0 | ✅ 2026-09-03 |
| W2 | 官方计划内容迁移 A | 基础/精度/母球/力度/分离角等 6 计划 | L | W1 | ✅ 2026-09-03 |
| W3 | 官方计划内容迁移 B | 旋转/走位/中高级/全技能等 6 计划，收口旧周依赖 | L | W2 | ✅ 2026-09-03 |
| W4 | SwiftData 与迁移 | 下一 schema、今日安排实体、active 归一、owner 转移 | XL | W3、v53 schema 收口 | ✅ 2026-09-03 |
| W5 | 编排与进度领域服务 | 分类、去重、跨日、预计周数、幂等推进 | L | W4 | ✅ 2026-09-03 |
| W6 | 训练运行与来源同步 | scheduled mode、保存事务、DTO/backend/restore | XL | W5 | ✅ 2026-09-03 |
| W7 | 官方计划详情 UI | 阶段课程、状态、动态预计、计划内编排器 | L | W5 | ✅ 2026-09-03 |
| W8 | 首页、模版与动作 UI | 今日队列、排序、逐块训练、直接添加、昨日恢复 | XL | W6、W7 | ✅ 2026-09-03 |
| W9 | 历史、回归与文档收口 | 来源详情、迁移回归、UI 矩阵、规格同步 | L | W8 | ✅ 2026-09-03（代码；H-27 待验） |

### 9.2 依赖图

```text
v53 schema 收口 ───────────────────────────────┐
                                               v
W0 -> W1 -> W2 -> W3 ------------------------> W4 -> W5 -> W6 ----┐
                                                        └-> W7 ----┼-> W8 -> W9
```

W6 与 W7 可在 W5 稳定后并行开发，但 W8 必须等两者均完成；计划编排 UI 不应先于真实保存/推进契约接假数据。

### 9.3 每批完成标准

#### W0 — 契约与 fixture 冻结

- 把 §4 的场景变成纯数据 expected table：历史+当前、当前+下一、当前+隔课、只未来、乱序完成、切换计划、重复回调。
- 制作至少四类旧库迁移 fixture：official only、custom only、official+custom、duplicate official。
- 记录 12 计划旧 week/day → 新 stage/lesson ID 映射规则，明确 ID 生成后不可随标题变更。
- 记录 v53 实际 schema/current worktree 状态和 v54 允许触碰路径。
- **完成证据**：fixture/contract 测试先红；本批不出现业务 UI 或模型实现。

#### W1 — 新内容模型与双读门禁

- `OfficialPlan` 新增 stage/lesson 生产模型，lesson ID 全计划唯一、稳定且非空。
- decoder 能在受控兼容窗口读取旧结构；新结构缺 ID、重复 order、空 stage/lesson 必须失败。
- 把 I11/I13 等门禁从“周齐全/建议周”拆为课程引用、阶段覆盖、先修顺序、剂量与复习约束。
- 更新模型 spec 和脚本测试，禁止新增内容继续写旧 week/day 结构。
- **完成证据**：内容模型/validator 单测通过；12 个旧计划仍可兼容读取但尚未声称迁移完成。

#### W2 — 官方计划内容迁移 A

- 迁移 6 个偏基础计划，每个 stage/lesson 有稳定 ID、可读标题和明确顺序。
- 保持所有 drill 引用和剂量语义；迁移前后 session/lesson 数、phase 数及引用集合可核对。
- 对能力阶段名做人工内容复核，禁止只显示“阶段 1/课程 1”作为最终文案。
- **完成证据**：6 个计划新结构门禁、引用门禁、剂量门禁全通过；输出机器核对表和人工抽查记录。

#### W3 — 官方计划内容迁移 B

- 迁移剩余 6 个走位/旋转/中高级计划，并同步研究表中的建议周语义。
- 内置 12 计划全部停止依赖固定 durationWeeks/sessionsPerWeek。
- `rg` 审计生产代码、内容、脚本和规格中的 week/day；保留项必须逐条注明是兼容层或自然周统计，不得混淆。
- **完成证据**：12/12 新结构通过；129 节映射数量和 stable ID 唯一性通过；旧周结构只存在批准的兼容/历史位置。

#### W4 — SwiftData 与迁移

- 在下一可用 schema 新增 schedule/item 及 session 来源字段，建立明确关系删除规则。
- 完成旧 active row 的五类归一、幂等补偿和失败诊断。
- `UserActivePlan` 新业务只允许官方 plan；custom active 被复制为当日模版块。
- owner transfer、账号删除/迁移路径包含 schedule，合并去重可测试且事务失败可回滚。
- **完成证据**：V4→next store 迁移、重复打开、半迁移恢复、A/B owner 隔离测试通过；旧库训练历史数量不减少。

#### W5 — 编排与进度领域服务

- 实现不依赖 View 的 `TodayTrainingScheduleService` 与课程分类纯函数。
- 覆盖 §4 全矩阵、同源去重/再练、顺序不改资格、跨日归档/复制、时区和零点边界。
- 进度推进显式消费 eligible item/lesson，不再监听任意相同 planId session。
- 预计周数读取 owner 的 `weeklyGoalDays`；同日多课推进多课但训练日去重。
- **完成证据**：领域单测全部通过，重复结算 2 次与 20 次结果相同；没有 UI 快照冒充规则验收。

#### W6 — 训练运行与来源同步

- scheduled block 逐块启动、暂停/继续、完成返回今日队列；free/tool 行为无回归。
- 本地保存、item 完成、游标结算和同步入队满足 §6.3 的可恢复顺序。
- iOS DTO、后端 schema、restore 映射完整保留来源；旧客户端/旧后端缺字段仍可工作。
- 删除模版或更新官方内容后，已有今日 item 和历史 session 仍显示冻结内容。
- **完成证据**：保存失败注入、重启恢复、上传重试、下载恢复、旧 DTO 兼容和后端测试通过。

#### W7 — 官方计划详情 UI

- 详情头部显示阶段/课程/每课时长和动态预计；不再把固定周数当结构。
- 四种 CTA 状态、四种课程状态和计划完成态可独立 fixture 截图。
- 编排器默认当前课，支持历史/当前/未来多选，并准确输出结果摘要和跨缺口提示。
- 已有其他来源今日项时，计划选课只追加/更新计划块，不清空队列。
- **完成证据**：业务状态测试 + iPhone/iPad Light/Dark 截图；VoiceOver 能读出课程状态和选择结果。

#### W8 — 首页、模版与动作 UI

- 今日卡片显示块级队列；pending 可排序/删除，inProgress 可继续，completed 固定。
- 单 CTA 始终启动正确的下一块；完成一块返回后不会自动开始下一块。
- 模版与动作直接加入今日安排，官方主线持续显示且游标不变；旧“替换今日/激活模版”文案和路径清零。
- 昨日未完成入口按当天游标重新分类，不自动顺延、不重复复制。
- **完成证据**：无计划/仅建议/混合来源/部分完成/全完成/昨日未完 6 类状态 UI 测试和截图通过；来源不以高噪声标签堆叠。

#### W9 — 历史、回归与文档收口

- 历史详情展示来源面包屑，源存在可跳转、源删除仍保留快照。
- 回归周目标、本月统计、active plan 切换、账号 owner 迁移、tool 不计目标、历史不可变。
- 运行内容门禁、SwiftData 迁移、后端、XCTest/XCUITest、全量 build；记录失败归属，不用无关脏改动冒充 v54 失败。
- 更新 `docs/00`、`docs/04`–`docs/07`、content contract、任务卡和人工测试计划的最终口径。
- **完成证据**：定向测试/构建退出码、迁移 fixture 结果、设备矩阵截图、Dark Mode/动态字体/VoiceOver 人工项齐全；只有此时才能把 v54 标为代码完成。

---

## 十、测试与验收矩阵

### 10.1 领域规则最小矩阵

| Case | 当前游标 | 选择 | 完成顺序 | 期望游标 |
|---|---:|---|---|---:|
| A | 5 | 2 | 2 | 5 |
| B | 5 | 2,5 | 2,5 | 6 |
| C | 5 | 5,6 | 5,6 | 7 |
| D | 5 | 5,6 | 6,5 | 7 |
| E | 5 | 5,7 | 5,7 | 6 |
| F | 5 | 7 | 7 | 5 |
| G | 5 | 5 + template + drill | 任意 | 6（仅当 5 完成） |
| H | 5 | 5 | 完成回调重复 20 次 | 6 |
| I | 末课 | 末课 | 完成 | completed，无 current |
| J | completed | 任意已学课 | 完成 | completed，复练记录 +1 |

### 10.2 日期与统计

- 周目标 4，周内只练 3 个自然日：进度 3/4，未完成课程不跳过。
- 同日练 2 节官方课 + 1 模版：官方课可推进 2，周目标只 +1 天，训练量包含三次真实 drill session。
- 23:59 开始、00:10 完成：session 以真实完成/既有统计口径记录；schedule item 仍绑定创建日并可在次日首页显示完成结果，不能生成重复今日项。
- 旅行改变时区：旧 schedule 的 `localDayKey + timeZoneIdentifier` 保持可解释，新日按当前时区创建，不能因回拨重复生成两个同 key 容器。
- 没有训练：不生成完成 session、不推进；昨日 pending 只出现恢复入口。

### 10.3 数据与失败注入

- 旧 official/custom/mixed/duplicate active 四类 store 升级。
- 迁移中断、容器重开、owner 转移保存失败、重复登录迁移。
- schedule payload 解码失败、源计划/模版/动作已删除、官方 OTA 改名/重排。
- session 保存成功但进度结算待重试、同步上传重复、旧后端丢新字段、恢复旧 DTO。
- 账号 A/B/guest 的 active plan、schedule、template 和 session 完全隔离。

### 10.4 UI/可访问性

- iPhone SE/标准 iPhone/大屏 iPhone/iPad，Light/Dark。
- 动态字体最大辅助级别时，课程标题、结果摘要和底部 CTA 不遮挡。
- VoiceOver 读出：课程状态、是否选中、完成后作用、队列顺序、继续/完成状态。
- Reduce Motion 下拖动/状态切换不依赖动画理解。
- 截图重点验证信息层级和操作结果；截图通过不替代规则、迁移、保存和同步测试。

---

## 十一、代码锚点索引（2026-09-03 核验）

> 行号按 2026-09-03 当前工作区记录，执行前必须用符号/`rg` 重新定位。标为“拟新增”的路径不是现存文件。

| 领域 | 当前锚点 | 当前职责 / v54 落点 |
|---|---|---|
| active model | `QiuJi/Data/Models/UserActivePlan.swift:5` | 当前 week/day/isCustom；迁为官方-only lesson cursor |
| 计划进度 | `QiuJi/Data/Services/PlanProgressService.swift:4`、`:109`、`:162` | 当前 week/day 计算和 planId 自动推进；改为 explicit lesson/item 幂等结算 |
| 模版激活 | `QiuJi/Data/Services/DrillTrainingPlanService.swift:33`、`:83`、`:122` | 当前查询/创建/替换 active；删除模版激活语义，改为加入 today schedule |
| 官方内容模型 | `QiuJi/Data/Services/PlanContentService.swift:5`、`:18`、`:31` | 当前 durationWeeks/weeks/dayNumber/phases；迁 stage/lesson |
| 首页 VM | `QiuJi/Features/Training/ViewModels/TrainingHomeViewModel.swift:6`、`:23`、`:126`、`:203`、`:273`、`:360` | 当前单 active 推导 today + 完成记录补充；改为 suggestion + persisted queue |
| 首页 UI | `QiuJi/Features/Training/Views/TrainingHomeView.swift:299`、`:323`、`:410`、`:974`、`:1145`、`:1189` | 今日卡、整组启动、模版替换与底部 CTA；落块队列/editor/逐块运行 |
| 计划详情 | `QiuJi/Features/Training/Views/PlanDetailView.swift:85`、`:151`、`:198`、`:253`、`:304`、`:601`、`:688` | 当前激活、固定周元数据、周时间线；落阶段课程、动态预计和编排器 |
| 周时间线组件 | `QiuJi/Core/Components/BTPlanWeekTimeline.swift:1` | 替换或重构为 lesson/stage progress；具体新组件名 W7 确认 |
| 动作详情 | `QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift:58`、`:502`、`:548`、`:609` | 当前只能加模版且官方 active 时阻止；新增直接加今天 |
| 模版模型 | `QiuJi/Data/Models/CustomPlan.swift:5` | 保留可复用内容，清除 sessionsPerWeek 业务含义 |
| 模版编辑 VM | `QiuJi/Features/Training/ViewModels/CustomPlanBuilderViewModel.swift:175`、`:215` | 保存后可 activate；改为保存/加入今日两个独立动作 |
| 训练模式 | `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift:123`、`:911`、`:925`、`:973` | 当前 `.plan`、单 planId 保存和自动推进；落 scheduled block |
| 训练记录 | `QiuJi/Data/Models/TrainingSession.swift:5` | 增加来源快照、lesson/item 和实际进度作用 |
| 路由 | `QiuJi/App/AppRouter.swift:50` | 训练启动参数与计划/模版路由需分清 source |
| DTO | `QiuJi/Data/Services/BackendSyncService.swift:6`、`:99`、`:135` | 加 optional provenance 并保持旧回包兼容 |
| 恢复 | `QiuJi/Data/Services/SyncRestoreService.swift:284` | DTO → TrainingSession 还原新增来源字段 |
| 后端 session | `backend/src/models/TrainingSession.js:1`、`:50` | Mongoose schema 增加 optional provenance |
| owner 转移 | `QiuJi/Data/Services/OwnerTransferService.swift:4`、`:36`、`:47`、`:179` | 当前六类顶层数据；纳入 today schedule 和合并策略 |
| 当前 schema | `QiuJi/Data/Models/QiuJiSchemaV4.swift:4`、`:23` | v53 owner schema；v54 不得覆盖，新增下一版本 |
| 容器 | `QiuJi/Data/Models/ModelContainerFactory.swift:4`、`:11`、`:33`、`:52` | 注册 next schema/migration 和迁移后补偿 |
| 迁移计划 | `QiuJi/Data/Models/QiuJiSchemaV3.swift:193` | 当前 migration plan 定义位置，执行时重新核对是否已移动 |
| 周目标 | `QiuJi/Data/Services/OwnerProfileStore.swift:17`、`:44`、`:101` | `weeklyGoalDays` 为动态预计参数 |
| 周目标 UI | `QiuJi/Features/Profile/Views/TrainingGoalView.swift:1` | 保持训练日目标，不改成每周课程硬排期 |
| 内容契约 | `.kiro/steering/content-data-contract.md:149`、`:219`、`:275`、`:309`、`:720`、`:924` | 同步边界、模型、历史冻结、统计、I11/I13、tool 独立 |
| 内容门禁 | `scripts/verify_tutorial_sync.py:964`、`:1195`、`:1250`、`:1537` | 从建议周/周齐全迁为阶段/课程/先修门禁 |
| 计划资源 | `QiuJi/Resources/Plans/*.json` | 12 个计划、129 个旧 session；W2/W3 分批迁移 |
| 研究真源 | `docs/research/20260818-v39-语义课表.md`、`docs/research/20260814-v38-先决与主课名单.md` | “建议周”迁为阶段/先修语义，保留内容证据 |
| 今日模型（拟新增） | `QiuJi/Data/Models/TodayTrainingSchedule.swift` | schedule/item SwiftData 模型与 payload version |
| 今日服务（拟新增） | `QiuJi/Data/Services/TodayTrainingScheduleService.swift` | 编排、分类、跨日、去重与恢复 |
| 计划编排 UI（拟新增） | `QiuJi/Features/Training/Views/PlanLessonComposerView.swift` | 计划内多选与后果摘要 |
| 今日编辑 UI（拟新增） | `QiuJi/Features/Training/Views/TodayScheduleEditorView.swift` | pending 排序/删除、来源详情 |

---

## 十二、文档与验收产物

执行完成至少同步：

- `docs/00-讨论记录.md`：记录 v43 “模版替换今日”被 v54 的并行今日安排取代，以及进度算法最终决策。
- `docs/04-功能规划.md`：计划/模版/今日编排能力和完成条件。
- `docs/05-信息架构与交互设计.md`：PlanDetail、TrainingHome、Template、DrillDetail、History 路由与状态。
- `docs/06-技术架构.md`：schedule/item、session provenance、进度事务与同步边界。
- `docs/07-路线图与MVP.md`：去除固定周排期承诺，改为阶段/课程与动态预计。
- `.kiro/steering/content-data-contract.md`：stage/lesson、stable ID、snapshot、统计和 tool 边界。
- `tasks/PROGRESS.md`：逐批状态，不因 v54.0 方案创建而标完成。
- `tasks/HUMAN-REQUIRED.md`：只记录确需真机完成的拖动、动态字体、VoiceOver、跨零点/时区等检查。
- `tasks/test-plans/TP-P*-v54*.md`：规则、迁移、UI 和人工验收步骤。

---

## 十三、版本记录

| 版本 | 日期 | 变更 | 状态 |
|---|---|---|---|
| v54.0 | 2026-09-03 | 汇总官方主线唯一、模版/动作并行、今日块编排、复练/连续推进/预习算法、动态预计周数、stage/lesson 内容迁移、来源快照与 W0–W9 执行批次 | 方案待执行 |
| v54.1 | 2026-09-03 | W0 完成：冻结 9 个编排 expected cases、5 类 legacy active 迁移 fixture、12 计划/129 课基线与 stable lesson ID 规则；fixture 门禁通过，生产实现红灯按预期建立 | 执行中（下一批 W1） |
| v54.2 | 2026-09-03 | W1 完成：落地 `PlanStage/PlanLesson` 与旧 `week/session` 双读；I10/I11/I13 迁为课程语义，并以存量 allowlist 禁止新增 legacy 计划 | 执行中（下一批 W2） |
| v54.3 | 2026-09-03 | W2 完成：6 个基础向计划迁为 stage/lesson；54 节课、198 phase、148 次引用前后一致，标题完成内容复核 | 执行中（下一批 W3） |
| v54.4 | 2026-09-03 | W3 完成：12/12 bundle 计划全部迁为 43 stage/129 lesson；I11 修复新结构遍历，研究表“建议周”改为最早阶段语义 | 执行中（下一批 W4） |
| v54.5 | 2026-09-03 | W4 完成：冻结 V4 历史 schema，V5 新增今日安排树、主线 lesson 游标与训练来源；旧 active 五类归一、owner 合并与事务回滚通过实库测试 | 执行中（下一批 W5） |
| v54.6 | 2026-09-03 | W5 完成：课程选择纯分类、持久化今日编排、同源去重/复练、跨日归档复制、动态预计与连续前缀幂等结算落地 | 执行中（下一批 W6） |
| v54.7 | 2026-09-03 | W6 完成：新增 scheduled block 运行模式，完成时以单次保存同步落 session、item、主线游标与上传队列；provenance 贯通 iOS DTO、backend 和 restore | 执行中（下一批 W7） |
| v54.8 | 2026-09-03 | W7 完成：计划详情改为阶段/课程与动态预计，落地四种主 CTA、四种课程状态和默认当前课的“编排今天” | 执行中（下一批 W8） |
| v54.9 | 2026-09-03 | W8 完成：首页跨来源今日队列、单一下一块 CTA、pending 编辑、昨日复制和模版/动作直接加入已落地 | 执行中（W9 收口） |
| v54.10 | 2026-09-03 | W9 代码收口：历史来源面包屑与失效来源降级、规格/路线/写面审计、专项/后端/UI/构建门禁完成；完整测试中的旧周结构测试已迁移，H-27 保留真实交互与系统辅助功能验收 | 代码完成（人工待验） |

### v54.1 检查点

- `[检查点] W0 完成（2026-09-03）｜fixture：9 个编排场景 + 5 类迁移场景 + 12/129 内容基线｜验证：fixture PASS，implementation gate 4 项 RED（符合先测后实现）｜下一批：W1`。
- W0 没有创建业务 UI、SwiftData model 或迁移计划，也没有修改官方计划 JSON。
- W1 只落 stage/lesson 新内容模型、旧格式兼容读取和门禁；不提前迁移 12 个 JSON，不触碰 today schedule schema 或 UI。

### v54.2 检查点

- `[检查点] W1 完成（2026-09-03）｜Swift：V54PlanContentModelTests 8/8 PASS｜内容：I10 88、I11 365、I13 915，FAIL 0｜构造性门禁：8/8 PASS｜下一批：W2`。
- 12 个 bundle 计划在 W1 仍按 legacy 兼容读取；只有冻结清单中的既有 ID 可继续使用旧结构，任何新 legacy 计划会被 I10 阻塞。
- 本批未迁移官方计划 JSON，未触碰 SwiftData schema、今日安排服务或 UI。

### v54.3 检查点

- `[检查点] W2 完成（2026-09-03）｜迁移：6 计划/18 阶段/54 课程｜语义核对：198 phase、148 引用前后一致｜验证：I10/I11/I13 FAIL 0，bundle 回归 14/14 PASS｜下一批：W3`。
- 机器核对和人工标题抽查记录见 `docs/research/20260903-v54-W2-计划内容迁移复核.md`。
- 未迁移的 6 个计划仍由 W1 allowlist 兼容读取；W3 完成后该清单必须清空。

### v54.4 检查点

- `[检查点] W3 完成（2026-09-03）｜Bundle：12/12 新结构、43 阶段、129 课程、129 唯一 ID｜内容门禁：I10 88 / I11 365 / I13 915，FAIL 0｜构造性门禁：52/52 PASS｜下一批：W4`。
- 旧结构内容 allowlist 已清空；legacy decoder 仅保留 OTA/旧数据兼容能力，新增旧结构内容必定失败。
- `week/day` 剩余命中已按兼容层、迁移输入、自然周统计、历史工具和后续批次改造面归类，详见 `docs/research/20260903-v54-W3-计划内容迁移与周语义审计.md`。

### v54.5 检查点

- `[检查点] W4 完成（2026-09-03）｜Schema：V4 历史快照冻结、V5 当前模型｜迁移：official/custom/混合/重复/半迁移/失效课程/保存失败 9/9 PASS｜回归：owner 与旧迁移 12/12 PASS｜构建：BUILD SUCCEEDED｜下一批：W5`。
- custom active 只复制冻结模版快照到迁移当日安排并删除 active 语义；官方 row 按课程解析结果归一为 active/paused/invalid，同 owner 至多一条 active。
- owner transfer 已纳入 schedule/item 树，同日合并按稳定 item 身份去重；保存失败时同时恢复 owner 与父子关系，重复执行结果为空。

### v54.6 检查点

- `[检查点] W5 完成（2026-09-03）｜领域测试：7/7 PASS｜矩阵：复练/连续/跨缺口/只未来/乱序/切换/20 次重放/末课完成｜编排：去重、再练、排序资格冻结、跨日不自动堆积、昨日复制、时区边界通过｜下一批：W6`。
- `TodayTrainingScheduleService` 不依赖 View；官方课程、模版与动作都冻结版本化 payload，未完成同源去重，完成后允许创建新的复练 item。
- 新进度结算只消费 completed + `advanceEligible` 的 schedule item，并按当前官方 plan 身份和连续前缀推进；同一回调重复 20 次结果不变。

### v54.7 检查点

- `[检查点] W6 完成（2026-09-03）｜事务测试：V54TrainingTransactionTests 6/6 PASS｜后端：15/15 PASS｜恢复：新旧 DTO 可选字段兼容｜下一批：W7`。
- scheduled block 用冻结 payload 启动；重启可恢复 in-progress item，源删除不破坏当次训练。
- 保存失败注入证明 session/item/cursor/queue 同退，重试后来源字段完整上传。

### v54.8 检查点

- `[检查点] W7 完成（2026-09-03）｜业务状态：V54PresentationStateTests 4/4 PASS｜UI：开始/切换/编排/完成复练四态已取证｜下一批：W8`。
- 编排器默认从真实主线游标选当前课；游标缺失时取第一个未完成课，不再因异步顺序出现“0 项”假默认。
- 页面只显示阶段、课程、每课时长与基于 `weeklyGoalDays` 的动态预计，不将固定周当作完成承诺。

### v54.9 检查点

- `[检查点] W8 完成（2026-09-03）｜首页：6 种今日状态｜来源：官方/模版/动作同时保留｜UI 矩阵：iPhone/iPad × Light/Dark 均 3/3 PASS｜下一批：W9`。
- 唯一主 CTA 只开始或继续队首未完成块；完成一块后返回队列，不自动串联下一块。
- 旧“激活模版/替换今日”生产文案扫描为 0；昨日 pending 只在用户显式选择后复制并重新分类。

### v54.10 检查点

- `[检查点] W9 代码完成（2026-09-03）｜v54 XCTest：35/35 PASS｜旧周结构回归迁移：8/8 PASS｜backend：15/15 PASS｜XCUITest：iPhone/iPad × Light/Dark 共 12/12 PASS、52 张截图｜通用模拟器构建：BUILD SUCCEEDED｜verify-gate：PASS｜人工：H-27 待执行`。
- 完整 `QiuJiTests` 尝试共 1,279 项：1,247 passed、17 skipped、15 failed。其后已把其中 5 个受 v54 影响的旧 `weeks` 扫描测试迁为 `stages/lessons` 并定向 8/8 通过；剩余 10 项均不在 v54 路径：7 个 `BankFrozenRailDiagTests` 诊断输出目录缺失、`AimCloseupEvidenceTests` 1 项、`ShotTableLayoutTests` 1 项、`AuthStateTests` 进程 trap 1 项。
- 历史详情使用冻结来源快照；源仍存在时可进入详情，源已删除时只降级为不可点击说明，不影响训练记录阅读。
- 自动化截图证明布局与状态矩阵，不替代拖动排序真实手势、删除/继续链路、最大动态字体、VoiceOver、Reduce Motion、跨午夜/时区和真机离线恢复；这些统一登记在 H-27，故本版本只标“代码完成”，不标“全量人工验收完成”。
