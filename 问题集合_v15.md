# 问题集合 v15 — 分离角图谱图例/打点盘/球库 + 学卡顺序

> **本文件是本轮需求的真源。** 日期：2026-07-20　状态：✅ **全部批次收官**（W1；plan-delegated-execution：`cursor-grok-4.5-high-fast`；主控验收 0 轮返工）
> 来源：2026-07-20 会话——分离角图谱去「纯高杆/纯低杆」文字、左侧 8 打点盘、复用球库逻辑、学页两卡换序；同日补强：**球库须同时支持点击与拖拽**。
> 编号约定：条目 **A1–A4**；批次 **W1**；拍板项 **D-v15-N**（2026-07-20 用户「直接用 grok / 按推荐」全部采纳）。
> 与 **v14**（六文档学页壳）：**文件域不重叠主战场**（v14 红线排除本两沙盘页）；仅共享 `AngleHomeView.learnEntries` 的卡序一行——可与 v14 B2/B3 并行，合并时注意勿互盖。
> 与 **v11 Y3**（分离角图谱首版）：本轮为增量修订，不推翻 8 色轨迹 / 并行 simulateFree / 切片降级契约。

---

## 〇、问题陈述（事实 / 推测分开）

**事实（已读代码，2026-07-20）**

- 「分离角图谱」=`SeparationAngleAtlasView` + `SeparationAngleAtlasViewModel` + `SeparationAngleAtlasGeometry`：SceneKit 真台、可拖母球/`_8`、8 档 spinY 并行 `simulateFree`、碰后→第一库（未吃库降级停点）、右缘 `BTShotInstrumentColumn`、点击打点打开只读 8 点 overlay。
- 台面两端文字「纯高杆」「纯低杆」由 VM `drawTrajectories` 经 `scene.addFlatLabel` 写入（~L324–342）。
- 底栏当前为提示文案（`BottomBarHeight.paletteOnly`=78），**无球库**；默认仅母球+黑 8；`currentIntent` 硬编码 `allBallNodes["_8"]`。
- 「角度与瞄准」=`AngleDynamicView`：底栏 2×8 **仅点击**球库（私有 `paletteBar`，无拖拽）。
- 「分离角与走位」=`ShotSimulationView`：共享 **`BTBallPaletteBar`**——组件 API 已含 **点击 + 拖放到台**（`onTap` / `onPlace`）+ 台面拖回球库（`BTBallPaletteDragBack`）；页级另有摆球上限 2。
- `BTBallPaletteToken` 注释写明「tap + drag-to-place」（`BTBallPaletteBar.swift` ~L55）。
- 学卡顺序 `AngleHomeView.learnEntries`：…旋转与加塞 → **分离角图谱** → **角度与瞄准** → 浅谈球感…

**已确认（用户 2026-07-20）**

- 图谱球库：**点击与拖拽都要支持**（A3 硬约束）。

**推测（待拍板确认）**

- UI 组件真源 = `BTBallPaletteBar`（已具备双交互）；页级语义（换目标 / 障碍进模拟 / 母球不可撤）对齐「角度与瞄准」VM，**不**照搬走位页「上限 2」除非另裁。
- 「球桌左侧放 8 个打点盘」= 左侧竖列 **8 个只读迷你打点盘**（每档一盘、一点、与轨迹同色），常驻替代台面文字与（可选）底部弹出盘；不是把右侧力度柱挪到左侧。

---

## 一、需求回显（语义基准，不增删）

| # | 用户诉求（原话意） | 验收语义 |
|---|---|---|
| A1 | 分离角图谱中**去除**「纯高杆」「纯低杆」文字 | 台面 SceneKit 标签不再出现上述两字符串；`rg "纯高杆\|纯低杆"` 在图谱 View/VM 用户可见路径清零（测试注释可用英文/描述性措辞） |
| A2 | 在球桌**左侧**放 **8 个打点盘**，位置合理 | 左缘（贴球桌左侧空隙，对齐 `ShotStageProxy` 左缘控件带）常驻 8 个只读打点示意，与 8 色轨迹档一一对应；不挡力度柱、不挡拖球主区；截图可目视 |
| A3 | **复用**球库逻辑；球库中球**支持点击，也支持拖拽** | 底栏接共享 `BTBallPaletteBar`：**点击**上桌（或在桌则按页语义脉冲/撤下/换目标）+ **拖放到台面落点**；宜支持台面拖回球库；换目标/障碍进 `simulateFree` 按 D-v15-3；轨迹随球局重算 |
| A4 | 学页面卡片顺序：分离角图谱放到角度与瞄准**后面**（两卡对调） | `learnEntries` 中两卡相邻顺序变为：…旋转与加塞 → **角度与瞄准** → **分离角图谱** → 浅谈球感…；入口卡可点达不变 |

**明确不做（本轮，除非拍板扩大）**

| 不做项 | 依据 |
|---|---|
| 改 8 档 spinY 采样或页内色板算法 | 非诉求；DR-025 色板保留 |
| 改切片语义（碰后→第一库 / 未吃库降级） | v11 Y3 契约保留 |
| 把图谱改成可调 spin 的编辑打点盘 | 仍为只读图例（与 Y3「只读」一致，除非 D-v15-3 改口） |
| 动 v14 六文档学页壳 | 范围外 |
| 改「分离角与走位」/「角度与瞄准」既有页业务 | 只**复用** `BTBallPaletteBar` 与 VM 语义；不顺手改那两页 |

---

## 二、代码现状（事实，已核实 2026-07-20）

### 2.1 分离角图谱

| 项 | 锚点 | 现状 |
|---|---|---|
| View 布局 | `SeparationAngleAtlasView.swift` | G10 顶 46 / 底 `paletteOnly` 78；右缘 `instrumentFrame`；`showSpinPad` → `SeparationAngleAtlasSpinPadOverlay`（单盘 8 点） |
| 两端文字 | `SeparationAngleAtlasViewModel.drawTrajectories` ~L324–342 | `addFlatLabel("纯高杆")` / `("纯低杆")` |
| 意图硬编码 | `currentIntent` ~L265–290 | 目标固定 `_8`；`balls` 仅目标一颗 |
| 几何真源 | `SeparationAngleAtlasGeometry.swift` | `spinYLevels()` / `trackColors` / 切片 API |
| 单测 | `SeparationAngleAtlasTests.swift` | 档位端点、切片、90°、低力度降级；**不断言**台面中文标签 |
| 巡游/交互 | `ScreenshotTourUITests` a15 + `testSeparationAngleAtlasInteractions` | 点卡名进页；不依赖卡序索引 |

### 2.2 球库对照（复用分层，2026-07-20 修订）

| 层 | 真源 | 形态 | 图谱怎么用 |
|---|---|---|---|
| **交互组件（必用）** | `BTBallPaletteBar` / `BTBallPaletteToken` | **点击 + 拖放**已内建；`ShotSimulationView.paletteBar` 为接线样板 | View 底栏直接接组件，满足「点也拖」 |
| **页级语义（推荐对齐）** | `AngleDynamicViewModel` `placeFromPalette` / `removeFromTable` / `selectTarget` | 多球、换目标、母球不可撤、障碍进计算 | VM 移植/对齐语义；**不要**抄 AngleDynamic 私有只点球库 UI |
| 走位页约束（默认不照搬） | `ShotSimulationView` 摆球上限 2 | 与图谱「可加障碍看分离轨迹」可能冲突 | 除非 D-v15-3 改口限球数 |

### 2.3 学卡顺序

| 项 | 锚点 |
|---|---|
| 现行顺序 | `AngleHomeView.swift` `learnEntries` L113–148：图谱在 L130–133，角度与瞄准在 L134–137 |
| 巡游列表 | `ScreenshotTourUITests.testAngleLearningPages` 按**卡名**点击，不按数组下标 |

### 2.4 左缘布局可用 API

| API | 位置 | 说明 |
|---|---|---|
| `aimWheelFrame()` | `ShotTableLayout.swift` ~L130 | 左缘竖条带（现用于瞄准轮）；图谱无瞄准轮，可作 8 盘列占位参考 |
| `bottomLeadingFrame` | 同文件 ~L166 | 左下角控件 |
| 右缘力度柱 | `instrumentFrame()` | **保留**；勿与左侧图例抢位 |

---

## 三、拍板项

| # | 问题 | 推荐 | 状态 |
|---|---|---|---|
| D-v15-1 | 球库复用怎么拆？ | **组件**=`BTBallPaletteBar`（点击+拖放，已具备）；**语义**=角度与瞄准式换目标/加减障碍/母球不可撤；**不**限走位页「最多 2 目标球」 | ✅ 2026-07-20 用户按推荐开工 |
| D-v15-2 | 「8 个打点盘」形态 | **左缘竖列 8 个迷你只读盘**（每档一点、同色、高→低自上而下或与 spinY 符号一致）；**删除**台面「纯高杆/纯低杆」；右侧力度柱保留；原底部弹出 8 点盘 **退役**（避免双真源），`onSpinTap` 可置 nil | ✅ 2026-07-20 用户按推荐开工 |
| D-v15-3 | 球库接到图谱后的物理范围 | 换目标球号 + 障碍球进入 `simulateFree` 的 `balls`（碰障如实改变轨迹）；母球不可撤下；默认仍教学双球（母+8）；支持台面拖回球库 | ✅ 2026-07-20 用户按推荐开工 |
| D-v15-4 | 执行模型 | 单批 `cursor-grok-4.5-high-fast` + 主控逐文件读 diff；几何触碰则强制 `geometry-spatial-reasoning` | ✅ 2026-07-20 用户指定 grok |

---

## 四、批次表

量级：单会话可验收（图谱 UI+VM 球库接线 + 卡序一行）。若 D-v15-3 扩大到大量共享抽取再拆批。

```
W1（图谱图例/左缘 8 盘 + 球库接线 + 学卡换序）
```

| 批次 | 范围 | 量级 | 依赖 | 模型 |
|---|---|---|---|---|
| **W1** ✅ 2026-07-20 | A1–A4：去两端文字；左缘 8 只读打点盘（D-v15-2）；球库按 D-v15-1/3；`learnEntries` 两卡对调；底栏高度改 `composer`（或与角度与瞄准同档）；更新底栏提示文案；SPEC §9.3 图谱行；回归单测 + a15/交互截图 | M–L | D-v15-1～3 ✅ | `cursor-grok-4.5-high-fast` |

**波次**：单批串行；可与 v14 B2/B3 并行（文件交集仅 `AngleHomeView` 卡序，合并时手核）。

### W1 完成标准

1. **A1**：图谱页默认态/高力度/拖球截图均无「纯高杆」「纯低杆」台面字；业务源码用户可见串清零。
2. **A2**：左缘常驻 8 个只读打点示意，色序与 `trackColors`/轨迹一致；不遮挡球桌可拖区与右缘力度柱；位置经 `ShotStageProxy` 计算（禁止魔法绝对坐标硬塞全屏）。
3. **A3**：底栏为 `BTBallPaletteBar`；**点击**与**拖放到台**均可上球；在桌球宜可拖回库或点击按页语义处理；换目标/加障碍后 8 轨迹重算；母球不可撤。截图或 UI 测至少覆盖点上 + 拖上各一路径（或交互钩子等价取证）。
4. **A4**：学分段卡片目视顺序为角度与瞄准在前、分离角图谱紧随其后。
5. `make build` SUCCEEDED；`SeparationAngleAtlasTests` 全绿；`testSeparationAngleAtlasInteractions`（及必要巡游帧）绿或按新 UI 更新断言后绿。
6. SPEC §9.3「分离角图谱」行更新（去两端文字、左缘 8 盘图例、球库）；Changelog 一行。
7. 主控读 diff：未改引擎/`ShotPredictor` 内核；未误改 v14 六文档页。

---

## 五、锚点索引（读代码核实，2026-07-20）

| 锚点 | 位置 | 用途 |
|---|---|---|
| 两端标签 | `SeparationAngleAtlasViewModel.swift` `drawTrajectories` ~L324–342 | A1 |
| 弹出打点盘 | `SeparationAngleAtlasView.swift` `SeparationAngleAtlasSpinPad*` ~L145–280 | A2 退役/迁移素材 |
| 右缘力度柱 | 同文件 `BTShotInstrumentColumn` ~L117–143 | A2 保留 |
| spinY / 色板 | `SeparationAngleAtlasGeometry.swift` `spinYLevels` / `trackColors` | A2 一一对应 |
| 意图硬编码 `_8` | `SeparationAngleAtlasViewModel.currentIntent` ~L265–290 | A3 扩展点 |
| **球库组件（点+拖）** | `Core/Components/BTBallPaletteBar.swift`（`BTBallPaletteToken` onTap+DragGesture；`BTBallPaletteDragBack`） | A3 必接 |
| 拖放接线样板 | `ShotSimulationView.swift` `paletteBar` ~L269–301 | A3 View 接线 |
| 角度与瞄准球库 VM | `AngleDynamicViewModel.swift` `placeFromPalette`/`removeFromTable`/`selectTarget` ~L84–144 | A3 语义对齐（勿抄其只点 UI） |
| 学卡顺序 | `AngleHomeView.swift` `learnEntries` L130–137 | A4 |
| 左缘 frame | `ShotTableLayout.swift` `aimWheelFrame` ~L130 | A2 布局 |
| 底栏高度 | `ShotStageMetrics.BottomBarHeight`（`paletteOnly` 78 / `composer` 94） | A3 底栏 |
| 巡游 | `ScreenshotTourUITests.swift` a15 / `testSeparationAngleAtlasInteractions` | 验收 |
| SPEC 契约 | `tasks/UI-IMPLEMENTATION-SPEC.md` §9.3 分离角图谱行 ~L918 | 回写 |

---

## 六、依赖与排期

- **前置**：D-v15-1～3 用户确认（尤其球库映射，守 FL-026）。
- **几何**：换目标/加障碍后轨迹变化属引擎实况，禁止用 magic offset「摆正」标签；动球位钳制复用角度与瞄准/`clampBall` 既有路径。
- **并行**：勿与另一会话同改 `SeparationAngleAtlas*`；与 v14 并行时 `AngleHomeView` 只动 `learnEntries` 两行顺序。

---

## 七、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v15.4 | 2026-07-20 | **主控验收 W1 ✅（0 轮返工）**：diff 逐读（View/VM/学卡序/SPEC/UI 测）；落盘日志亲核 build SUCCEEDED + 单测 4/0 + UI 1/0；截图目视（无端点字、左缘 8 盘、底球库、点/拖上球）；主控亲跑主树 `make build` SUCCEEDED。留档：拖回库无单独 UI 断言；a15 全巡游未重跑。证据 `build/v15-w1-{logs,screenshots}/`（含 `main-tree-build-orchestrator.log`）。v15 收官 |
| v15.3 | 2026-07-20 | W1 执行交付：A1–A4 落盘；build / SeparationAngleAtlasTests 4/4 / testSeparationAngleAtlasInteractions 绿；证据 `build/v15-w1-{logs,screenshots}/`；待主控验收 |
| v15.2 | 2026-07-20 | 开工：D-v15-1～4 全部按推荐采纳；模型定稿 `cursor-grok-4.5-high-fast`；W1 🔄 委派 |
| v15.1 | 2026-07-20 | **A3 补强**：球库须**点击+拖拽**；复用分层改为组件=`BTBallPaletteBar`、语义≈角度与瞄准 VM；D-v15-1 交互侧 ✅；完成标准补点/拖双路径取证 |
| v15.0 | 2026-07-20 | 初版：issue-collection-restructure；A1–A4 + 单批 W1；D-v15-1～4 待拍板；锚点已读代码核实 |
