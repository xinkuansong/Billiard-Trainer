# Stitch Prompt — P2-04 v2 (Incremental Fix)

Please make these adjustments to the current OnboardingView design:

1. **Shorten feature subtitles to avoid wrapping** — all 3 rows should be single-line subtitles for consistent row height:
   - Row 1: "海量练习动作，轻松记录训练" (was "海量台球练习动作，轻松记录每次训练")
   - Row 2: "模拟球台场景，提升角度判断力" (keep as-is, already fits)
   - Row 3: "可视化训练进度，发现薄弱项" (was "可视化训练进度，发现薄弱环节")

2. **Increase vertical breathing room** — move the Hero area (logo + app name + tagline) slightly upward, and increase the gap between the feature rows section and the bottom button area, so the content feels more spaciously distributed across the full screen height rather than slightly compressed in the middle.

3. **Feature icon circles should be visibly filled** — each 48pt icon circle should have a clearly visible light green filled background (rgba(26,107,60,0.1)), not just a thin outline ring. The circle should look like a soft, solid disc behind the icon.

Everything else (colors, button style, layout order, typography) is correct — keep unchanged.
