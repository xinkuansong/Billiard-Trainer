# Stitch Prompt — P1-05 Frame 1 v2 (增量修正)

> 在 Stitch 同一会话中发送以下修正指令：

Please make these 3 changes to simplify the page back to a clean navigation hub:

1. **Remove the hero banner entirely.** Delete the large billiard table photo card with "PRO SESSION" / "掌握精准角度 提升进球效率" text. The two feature entry cards ("角度测试" and "进球点对照表") should appear directly below the large title "角度训练" with no banner in between.

2. **Remove the top-left avatar icon and the top-right settings gear icon.** The large title "角度训练" should stand alone — no icons flanking it. This is a simple entry page with no header actions.

3. **Remove the two statistics mini-cards** ("平均误差 2.4°" and "今日排名 Top 12%") below the "测试历史" row. After the "测试历史" row, the rest of the page should be empty #F2F2F7 background space down to the tab bar.

Keep everything else unchanged — the two feature entry cards, the "测试历史" row, and the 5-tab bottom bar are all correct.
