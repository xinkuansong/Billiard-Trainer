# Stitch 增量修正 — A-04 v2

> 任务 ID: A-04 | 版本: v2 | 日期: 2026-04-03
> 在同一 Stitch 对话中发送以下修正指令

---

## 修正指令（粘贴到 Stitch 同一对话）

Please update **only the "模式对比" (Mode Comparison) section** at the bottom. Replace the two generic icons with more descriptive mini-illustrations that visually represent each lock mode:

**Left card (渐进式锁):**
- Instead of the generic "visibility_off" icon, show a small vertical stack of 3 horizontal lines: the top line is solid dark, the middle line is lighter/faded, and the bottom line is very faint or blurred. Below the lines, show a tiny gold lock icon (#D4941A). This visually represents "content fading into blur → lock."

**Right card (全遮罩):**
- Instead of the generic "view_module" icon, show a small solid dark rectangle (representing the overlay, using a dark gray like #666) with a tiny gold lock icon (#D4941A) centered on it. This visually represents "dark overlay with lock on top."

Keep everything else exactly the same — do not change the progressive lock section, the full overlay section, colors, fonts, or spacing.
