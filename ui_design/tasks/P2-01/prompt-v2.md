# Stitch 修正指令 — P2-01 帧 1 PlanListView v2

> 在 PlanListView 的同一 Stitch 会话中发送以下修正：

---

Remove the floating green "＋ 创建新计划" (Create New Plan) button that is overlaying the last plan card ("全能综合计划"). This button is redundant — the "新建" text button already exists in the top-right of the navigation bar as the entry point for creating custom plans.

After removing the floating button, the 6th official plan card "全能综合计划" (L3 熟练, 12 周, PRO) should be fully visible and readable, with proper spacing before the "自定义计划" section below it.

Everything else on the screen is correct — keep the navigation bar, level groups, all 6 plan cards, PRO badges, BTLevelBadge colors, and the custom plan section exactly as they are.
