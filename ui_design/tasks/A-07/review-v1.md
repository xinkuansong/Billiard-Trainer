# 审核报告 — A-07 v1

**审核日期**: 2026-04-03
**截图**: stitch_task_07/screen.png（⚠️ 缺失，基于 code.html 审核）
**整体评分**: ⭐⭐⭐☆☆ (3/5)

## ✅ 通过项

- **页面背景** `#F2F2F7` 正确
- **区块标题** 22pt Bold `#1A6B3C` 品牌绿 ✅
- **无导航栏/Tab 栏**，纯文档展示风格 ✅
- **BTExerciseRow 卡片结构**：80pt 高度 + 12pt 圆角 + 56pt 缩略图 ✅
- **进度圆点指示器**：8pt 实心绿 `#1A6B3C`（完成）/ 空心灰 `#D1D1D6`（待完成）✅
- **BTSetInputGrid 表头行**：`#F2F2F7` 灰底 + 5 列标签 ✅
- **当前组（Row 3）**：2pt 左侧品牌绿边框 + 44pt 输入方块绿色边框高亮 ✅
- **待完成组（Row 4）**：灰色边框 + 占位符 ✅
- **"+ 添加一组" 虚线按钮**：虚线边框 + `#D1D1D6` 颜色 ✅
- **空状态（Section 3）**：表头 + 空区域 + 图标 + "还没有组数" ✅
- **品牌色 `#1A6B3C` 全局一致**，无替代色 ✅
- **无渐变**，纯色填充 ✅

## ⚠️ 问题项（需修改）

### P1: BTExerciseRow 布局 — 进球统计位置错误（高）

**期望**：右侧独立区域显示累计进球 "45/180"（15pt）+ 齿轮图标，与中部文字（名称 + 组数）形成左右分区布局
**当前**：进球统计 "45/180" 与组数 "5 组" 排在同一行，成为中部文字的一部分，失去了右侧独立信息区的层次感

### P2: BTExerciseRow 字号偏小（高）

**期望**：Drill 名称 17pt Semibold / 组数 13pt / 进球统计 15pt
**当前**：名称 15pt Bold / 组数 12pt / 进球统计 12pt — 整体偏小约 2pt，且进球统计（应 15pt）与组数（应 13pt）字号相同，无法体现信息层级差异

### P3: 热身组「热」标记位置和样式错误（高）

**期望**：「热」标记替换组号列（#列）的数字，使用浅橙底 `rgba(245,166,35,0.15)` + 橙色文字 `#F5A623`
**当前**：「热」标记出现在进球列（而非组号列），且使用实心橙色填充 `#F5A623` + 白色文字。同时组号 "5" 仍然保留，导致同行出现两个标识

### P4: 已完成组数据展示缺少输入方块（中）

**期望**：已完成组的进球/总球仍显示在 44pt 圆角方块内（白底 + 粗体数字），与当前组/待完成组保持一致的网格结构
**当前**：已完成组（Row 1、Row 2）的进球/总球为浮动文字，无方块容器，与其他行的方块布局不统一

### P5: 已完成组组号颜色（低）

**期望**：已完成组的组号使用品牌绿 `#1A6B3C`（表示完成状态）
**当前**：已完成组组号为灰色 `slate-400`，与待完成组无区分

## 💡 建议项（可选优化）

- **缩略图圆角**：当前 12pt，设计文档指定 8pt — 12pt 也可接受但与文档有偏差
- **三个 BTExerciseRow 示例缩略图**：全部使用相同的绿色背景 `#E8F5E9`，建议按 prompt 使用不同颜色（绿/琥珀/蓝）以示区分
- **空状态缺少引导副标题**：当前只有 "还没有组数"，建议补充 "点击下方按钮添加"
- **Section 包裹卡片圆角**：当前 24pt (1.5rem)，规范为 16pt — 与 A-01~A-06 保持一致即可

## 验收标准核对

- [ ] BTExerciseRow：白色背景卡片（圆角 12pt、高度约 80pt）+ 左侧球台缩略图（56pt 方形圆角）+ 中部 Drill 名称（17pt Semibold）+ 「X 组」组数（13pt 辅助色）+ 右侧累计进球「0/180」（15pt）+ 设置齿轮图标（辅助灰色）
  - **部分通过**：卡片结构正确，但名称/组数/进球字号偏小（P2），进球统计未在右侧独立区域（P1）
- [ ] BTExerciseRow 底部：进度圆点指示器（●●●○○），品牌绿 #1A6B3C 已完成 / 灰色待完成
  - ✅ **通过**
- [ ] BTSetInputGrid：表格式布局，5 列定义——组号（32pt）/ 进球数输入框（44pt 圆角方块）/ 总球数输入框（44pt）/ 完成勾选（44pt）/ ⋯ 溢出菜单（44pt）
  - **部分通过**：当前组和待完成组有方块，已完成组缺少方块容器（P4）
- [ ] BTSetInputGrid 行状态：已完成组品牌绿浅底（btPrimaryMuted）+ 填充 ✓ / 当前组品牌绿色边框高亮 / 未完成组灰色边框
  - **部分通过**：当前组和未完成组正确，已完成组浅底 + 勾选正确但缺少方块（P4）
- [ ] 热身组：组号位置显示橙色「热」标记区分
  - **未通过**：位置和样式均不正确（P3）
- [x] 需展示两种状态：有数据（部分完成）+ 空状态（无组数）— ✅
- [x] 组件表不含导航栏/Tab 栏 — ✅
- [x] 整体视觉风格与 A-01~A-06 一致 — ✅
- [x] 品牌色确认为 #1A6B3C，纯色填充无渐变 — ✅

## DESIGN.md 偏离检查

延续 A-01~A-06 模式：Stitch DESIGN.md 使用 primary `#005129`、"No-Line Rule"、"Glass & Tone Rule" 等创意发挥。以 code.html 实际值为准，DESIGN.md 仅作参考。code.html 中 `primary-container` 正确为 `#1A6B3C`。

## 结论

- [ ] **通过** — 可标记为 done
- [x] **需修改** — 修改后重新生成

**需修改的 3 个高优先级问题**：
1. BTExerciseRow 进球统计移至右侧独立区域 + 修正字号（17pt/13pt/15pt）
2. 热身组「热」标记移至组号列、改为浅橙底+橙色文字
3. 已完成组的进球/总球数值放入 44pt 方块容器中
