# Stitch 修正指令 — P2-02 v2

Please make the following changes to the current design. Do NOT change the layout or structure — only fix the text language and one icon.

## Fix 1: Navigation bar text → Chinese

- Change center title from "Edit Training Plan" to "编辑计划"
- Change right button from "Save" to "保存"

## Fix 2: Bottom toolbar labels → Chinese

Change all 4 bottom toolbar labels from English to Chinese:
- "COLOR" → "配色"
- "START" → "开始训练"
- "ADD DRILL" → "添加动作"
- "CAPACITY" → "容量"

## Fix 3: Capacity item — replace icon with number

- The rightmost toolbar item (currently showing a person/people icon) should instead display a large bold number "960" (20pt Bold, color #000000) with the label "容量" below it
- Remove the person icon entirely, replace with the number "960"

Everything else (layout, colors, drill cards, thumbnails, stepper, spacing) is perfect — keep unchanged.
