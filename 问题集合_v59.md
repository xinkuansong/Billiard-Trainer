# 问题集合 v59：2D / 3D 理论瞄准点训练与标准击球示范

> **本文件是本轮需求真源。** 日期：2026-09-10；版本：v59.3；状态：W0 诊断完成，距离覆盖未达准入要求，待范围裁定；W1–W3 未执行。
> 用户于 2026-09-10 授权开始执行。以本文件为输入，按批次验收；W0 覆盖结论通过前不得跳入页面实施。
> 排期前置：刷新工作区状态和文件指纹；目标训练页已有其他任务修改，保留特写、刻度轮、布局、相机和保存失败处理等现有工作。不得整文件回退，不覆盖 `output/`、`tmp/` 或其他专题方案。
> 本文件仅覆盖本轮瞄准点训练需求；不替代 v58 等其他专题。后续 v59.x 覆盖本专题较早版本。

## 1. 需求回显与语义优先级

### 1.1 用户拍板结论

1. 当前固定某一力度的击球验证不能适应不同角度、母球与目标球距离、目标球与袋口距离。
2. 最新补充是最高优先级：训练目的是练习**不同角度下的理论瞄准点**，用户应看到按照理论答案展示的结果。
3. 答题正确性由理论瞄准误差决定；力度搜索服务于标准答案示范，不反过来改成绩。
4. 答对后展示理论方向的标准示范；答偏后先展示用户点与理论点差异，再展示标准示范，不强制播放一次用户失误击球。
5. 系统为理论方向寻找适合当前球形的力度；不为进球暗中改变理论方向、不改变物理参数、不伪造落袋。
6. 出题前验证可演示性，并审计筛题对不同角度覆盖的影响。
7. 力度搜索和力度余量留在系统内部，用户仍只需调线、提交；不新增力度评分或“进球概率”。

**被最新结论替代的早期建议：**“固定用户方向，只要搜索到可进力度就判正确”不再是本训练的判分口径。“力度容错宽窄”不成为用户成绩。不要从早期讨论恢复这两项。

### 1.2 本方案的实施默认值与待实测项

- 保留当前理论误差分档：绝对误差 ≤2 mm 为正确、≤6 mm 为接近、其余为偏差较大。本轮不借物理搜索重新设计阈值。
- 保留当前 G1 瞄准点定义及有效入袋点几何口径，不改成目标球表面接触点、假想球球心或简单袋心连线。
- 中杆、不加塞、水平出杆；搜索变量仅为杆头速度，单位 m/s。击球后的旋转演化仍由引擎处理。
- 搜索精度、预算、预取数量、成功区间余量及可用覆盖率由 W0/W1 实测定稿，不把未测参数写成有效保证。
- 母球落袋不改变理论成绩；选题与示范优先采用母球不落袋的成功结果，不能为此悄悄牺牲整类角度覆盖。
- 本轮不增加历史数据字段、后端接口、旋转控制或理论/实战模式切换。

## 2. 当前代码事实与问题归因

核实日期：2026-09-10；依据当前工作区，不以 HEAD 替代已有未提交修改。

| 事实 | 对方案的影响 |
|---|---|
| 2D / 3D 路由均使用 `AimPointSceneTrainingView`，仅入口相机不同 | 共用一次实现，不建立两套判分/搜索 |
| `AimPointSceneQuizViewModel` 与 View 同文件 | 数学快照/搜索可抽取为本功能服务，页面整合放在同一批，避免同文件跨批反复重写 |
| 提交使用有效入袋点→假想球→理论方向，按 G1 有符号偏移差算 mm | 保留其教学定义；需要形成唯一理论答案快照，评分、标记、示范同源 |
| 当前正确档使用理论方向，其他档使用用户方向 | 改为所有答题档都展示标准答案，用户方向只用于误差及对照 |
| 当前固定 `ShotTuning.aimPointVerifyVelocity = 3.3` | 移除本训练对固定速度作为唯一示范解的依赖，不全局修改其他训练速度 |
| 提交立即记录成绩和消耗题数，1.5 秒后 `simulateFree` 并回放 | 保留成绩语义；把可演示性前置，回放异常不能撤销成绩或重复计数 |
| 题目角度随机为 10°–80°、5° 步进；生成器被其他训练复用 | 本页增加准备/筛题层，不无条件修改共享生成器行为 |
| 传给生成器的目标球到袋距离范围为归一化 `0.15...0.55` | 不能把它写成米；扫描要保存换算后的真实距离 |
| 生成器内部 `cueDist` 为假想球到母球距离，归一化 `0.15...0.35` | 不能冒充母球到目标球的实测距离；距离分桶从最终球位计算 |
| `simulateFree` 接受固定方向且返回 recorder、落袋和碰撞事件 | 可复用；`objectPocketed` 只代表有非母球落袋，不能单独证明进指定袋 |
| `SimulationWorker` 是单次模拟的快照/取消接口 | 可参考其线程边界，不假定它已经支持整段力度搜索；批搜索调度需明确适配 |

根因假设：目前将“理论答案评分”与“一次固定力度物理试打”并置，且答题档位会改变示范方向，使反馈同时承担教学与实战验证，产生语义冲突。本方案从职责上分开评分与示范，不靠放宽阈值修补。

## 3. 目标体验与状态行为

### 3.1 正常流程

准备合格题目及标准示范 → 用户调线 → 提交并冻结答案 → 展示理论误差与两点对照 → 标准答案运杆/击球示范 → 下一题。

- 正确档：显示理论误差和“瞄准正确”；正确点/标准线仍可辨识。
- 接近/偏差较大档：显示偏厚或偏薄、毫米差值，保留用户点与理论点的可辨对照。
- 标准示范统一使用理论方向及已找到的速度，不能继续显示“按你的瞄准验证”。
- 建议文案：“即将演示理论瞄准”“理论瞄准示范中…”。保留约 1.5 秒结果停留作为初始实现基线，不新增必须点击的步骤；实测对照阅读不清时再局部调整。
- 精确零误差不显示“偏薄/偏厚”；对显示为 0.0 mm 的舍入边界验证文案一致性。该显示处理不得改真实误差或评分阈值。
- 用户答案与标准答案身份必须在切换时清楚，不能让示范看起来是在复现用户原线。

### 3.2 准备、取消与异常

| 状态 | 行为 |
|---|---|
| 首题准备 | 后台计算，展示简洁准备状态；尚未展示的题不计数、不保存 |
| 下一题准备 | 可后台预取少量题，数量由内存/耗时测试确定；不提前扣次数 |
| 候选在预算内未找到解 | 同角度层内重新取球形，有候选次数/总时长上限，不无限循环 |
| 预算耗尽且无可用题 | 显示“题目暂未准备好，请重试”，可重试/返回；不得塞入未经验证的失败示范 |
| 已答题但回放异常 | 保留已经记录的理论成绩，显示“示范暂不可用”，可继续；不归咎用户 |
| 离页、换题、次数满额 | 取消准备/搜索/回放后续动作，旧题结果不能覆盖新题或在离页后启动击球 |
| 成绩保存失败 | 沿用现有错误态与重试，答案留存；不得由示范成功掩盖保存失败 |

这里的异常提示是防御路径；不能把高比例准备失败作为达标的正常体验。

## 4. 理论答案、搜索与证据契约

### 4.1 唯一理论答案

以最终题目球位和 `pocketIndex` 生成不可变理论快照：球位、台面高度、有效入袋点、假想球、理论方向、G1 理论点。评分、提交后标记、球杆方向、模拟输入使用同一份快照。

坐标实施前必须按 `geometry-spatial-reasoning` 核对：SceneKit 水平面 X–Z、Y 向上、世界单位米；归一化坐标由 `AngleSceneCalculator` 转换。以代码几何真源确认袋口映射，不按中文左右袋名猜索引；已有文档方向/袋口尺寸差异不能用于新计算。

### 4.2 力度搜索

1. 固定理论快照、`spinX=0`、`spinY=0` 和当前引擎/球桌配置。
2. 允许范围先以 `ShotTuning.velocityRange`（当前 0.5...8.0 m/s）为扫描外边界。教学示范最终偏好的子范围由 W0 的覆盖与耗时决定，不擅自扩大物理量程。
3. 全范围粗采样，对成功候选邻域及有依据的接近成功区域细化；不能把进球视为对速度单调，禁止整区间直接二分求“进球速度”。
4. 区分找到可用候选、预算内未找到、取消、模拟无效/截断。有限搜索不证明物理无解；仅 `feasible=true` 也不代表到达终态。
5. 找到成功候选后补测附近速度，优先选择已测连续成功采样段内较远离失败边界的候选，再考虑正常演示时长。有限采样只证明采样点，不宣称整个连续区间已严格证明。
6. 搜索不调用会自动补偿瞄准的普通 `predict`/走位求解路径；固定方向调用 `simulateFree`。评分阶段可省展示折线，但保持相同物理精度与边界设置。
7. 最终选定候选生成完整 recorder 并再次验收，以该次完整模拟作为最终准入证据与回放源。若复跑不一致，视为失败调查，不播放前一次“预计成功”。
8. 逐候选支持取消；记录题目 ID 与输入指纹，后台只接收值快照，不跨线程读取 SceneKit 节点。

### 4.3 示范成功判据

- 母球先直接碰到本题目标球，目标球最终进入**指定袋**，由有球名和袋 ID 的真实事件确认。
- 排除母球先绕库再碰球、目标球绕主库碰巧入袋、进入其他袋和异常截断等情况。
- 不一刀切排除所有袋口 jaw 接触。当前 `ShotEvent.ballCushion` 不带主库/jaw 分类；实现时需读取实际引擎事件/几何映射，不能仅凭此简化事件声称完成主库排除。具体提取位置在 W0 定位，最小增量暴露，不改碰撞算法。
- 母球不落袋为优先示范条件；若某覆盖层只能找到母球落袋的结果，单列报告，不改理论成绩、不静默取消该层。
- 禁止修改袋口捕获半径、摩擦、碰撞偏移等参数“保进”；禁止手绘轨迹、隐藏未进球、播放其他方向的成功结果。

### 4.4 性能与缓存

准备与搜索在后台执行，主线程只接收完成快照和更新页面。测量单候选/整题准备 P50、P95、最慢耗时、首题等待、候选失败率、连续训练内存峰值；数值门槛由 W0 建立并在 W1 前写入本方案版本更新。

先采用有限预取和本次会话缓存，不要求建设永久题库。缓存键包含完整球形、袋索引、理论定义、力度搜索配置及引擎/几何版本标识；禁止仅按角度复用，禁止引擎变化后继续使用旧证据。

## 5. 角度与球形覆盖：先测再筛

- 确定性诊断集覆盖 10°、15°……80° 全部 15 档；角袋/中袋、左右切、两段实测距离的近/中/远桶。以合法且可生成的组合为全集，不宣称每种笛卡尔组合均几何可达。
- 保存最终球位而不仅是随机 seed；现有随机生成接口不保证可设 seed，可用测试夹具或不改变生产行为的诊断入口。
- W0 先冻结每桶样本数与合法性判据，输出尝试数、合法题数、找到示范数、未找到数、超时/截断数、母球落袋数及成功速度分布。
- 筛选不能改变题目声明角度；用最终几何实测值复核标签，不能把名义角度当证据。
- 按角度层分配题目，层内替换不可示范候选，保留袋型/切向/距离的覆盖记录。不得用“总成功率高”掩盖大角度或长距离整层消失。
- 若完整搜索仍出现某必需角度/合法球形层持续不可示范：停止将筛题方案标为完整可交付，给出具体案例、尝试范围与覆盖缺口。需要改变教学范围或改用明确标识的理想几何动画时，形成具体替代方案再由用户决定，不能擅自实现。

## 6. 会话批次与依赖

| 批次 | 范围与交付 | 量级 | 依赖 | 状态 |
|---|---|---|---|---|
| W0 可行性与覆盖基线 | 可重复扫描夹具、固定理论方向多力度诊断、事件分类定位、覆盖/性能报告、参数冻结 | 中，1 会话 | 用户已授权；诊断及 53 项物理回归通过，距离覆盖待裁定 | ⚠️ 诊断完成，准入未通过 |
| W1 理论快照与示范准备服务 | 同源理论答案、固定方向搜索、完整轨迹准入、有限预取/取消/缓存及领域测试 | 中，1 会话 | W0 覆盖与预算结论可接受 | ⏳ |
| W2 2D/3D 共享页接入 | 理论评分保留、统一标准示范、点线对照、状态/文案/次数/保存处理、聚焦 UI 验证 | 中，1 会话 | W1 | ⏳ |
| W3 完整验收与文档收口 | 角度球形矩阵、两入口多设备视觉/交互、性能复验、产品规格/任务记录 | 中，1 会话 | W2 | ⏳ |

依赖图：`W0 → W1 → W2 → W3`。W0 发现覆盖冲突时先形成证据与裁定，不能跳过进入 W2。四批按一次会话内完成实施和验收规划；本次方案任务不等于启动四批执行。

### W0 完成标准

- [x] 记录代码/配置指纹、实际命令与退出码，复现固定力度示范失败案例；未复现则如实说明。
- [x] 全部 15 角度档的合法样本、两类袋型、切向和距离桶覆盖可追溯，失败不得从分母删除。
- [x] 确认固定理论方向多力度是否能支撑完整角度教学，列出仍未找到解的具体盘面。
- [x] 定位袋 ID 和主库/jaw 事件分类依据，确认截断与自然静止判据。
- [x] 冻结搜索采样/细化/预算、每桶样本数、准备耗时与覆盖验收目标，并更新 v59.x 版本记录；未满足即记录阻塞原因。
- [x] 输出 `output/aim-point-theory/W0/REPORT.md` 及机器可读逐样本结果；诊断代码编译/定向测试通过，不改生产评分或物理常量。

W0 证据：[完整诊断报告](output/aim-point-theory/W0/REPORT.md)。采样/参数已保存；后续服务预算与覆盖范围因下述裁定尚未冻结，不能据此启动 W1。

### W1 完成标准

- [ ] 理论快照与当前几何函数对拍；所有候选方向一致，仅速度变化。
- [ ] 在 W0 固定样本上，指定袋、主库排除、母球落袋、未找到/截断/取消分别有断言。
- [ ] 完整回放模拟与搜索候选结果一致，播放证据来自最终 recorder。
- [ ] 同角度不同球形不能错用缓存；输入/引擎版本变化使缓存失效；旧任务结果丢弃。
- [ ] 保持理论误差评分独立于任何搜索结果；源码定向检查与领域测试通过。
- [ ] Debug 构建和 `git diff --check` 通过；达到 W0 冻结预算，保存原始耗时。

### W2 完成标准

- [ ] 相同题目/相同用户方向在 2D、3D 得到相同理论误差、等级与标准示范输入。
- [ ] 正确、接近、偏差大均显示理论对照并播放标准答案；不再按档位切换用户击球线。
- [ ] 测试 ±2/±6 mm 邻域、零误差文案、对错不被“用户线碰巧可进”或示范失败覆盖。
- [ ] 每次提交仅存一次成绩/消耗一次次数；预取不计数；保存失败重试保留既有语义。
- [ ] 连续换题、离页、满额、后台任务过期、准备失败与回放失败路径均能退出或继续，无串题。
- [ ] 现有相机、调线手势、刻度轮、特写和球杆运杆链路回归通过；不按旧测试“最高机位”注释回退当前机位。
- [ ] 真实 2D/3D 改前/改后截图、结果对照与示范关键帧已目视；Debug 构建与定向测试通过。

### W3 完成标准

- [ ] 最终代码重跑冻结矩阵；逐角度/袋型/距离统计满足 W0 目标，所有最终准入题都通过完整示范验证。
- [ ] 小屏 iPhone、常规 iPhone、iPad 的 2D/3D 核心流程；浅深色、标准字号截图与实际页面身份核对。专用大字/VoiceOver未跑时明确列缺口，普通设备先确认无残留大字设置。
- [ ] 连续训练复验首题等待、预取、取消、内存、次数与保存；不只靠截图证明事件或物理结果。
- [ ] `make -f scripts/Makefile build`、`verify-gate`、`verify-doc-size`、`git diff --check` 及本次相关领域/UI 测试通过；存命令、日志、退出码。门禁失败先归因，不更新签名或断言来绕过。
- [ ] 同步 `docs/05` 相关交互、UI 规格 Changelog、实施日志与 PROGRESS；需要跨模块边界决策时按项目规则补 ADR。
- [ ] 最终报告区分代码/模拟器/物理模型证据与真机体验；未做真机不能称真机通过，未授权不提交发布。

## 7. 已核实锚点索引

以下路径及行号按 2026-09-10 当前工作区读取确认；实施时须重新定位。拟新增服务/测试文件尚未创建，不当作现有锚点。

| 文件与行号 | 组件/内容 |
|---|---|
| `QiuJi/App/MainTabView.swift:189` | 两个入口复用同一训练 View |
| `QiuJi/Features/AngleTraining/Views/AimPointSceneTrainingView.swift:15` | `AimPointSceneQuizViewModel`、三个现有阶段 |
| 同文件 `:88` | `nextQuestion`，随机角度、球形布局、答案复位 |
| 同文件 `:161` | `submit`，理论方向/误差、成绩、次数、延迟击球 |
| 同文件 `:253` | `strike`，固定速度 `simulateFree` |
| 同文件 `:282` | `launchStrikePlayback`，共享运杆后轨迹回放 |
| 同文件 `:741` | 示范状态文案、`:755` 结果 HUD，`:781` 等级颜色 |
| `QiuJi/Features/AngleTraining/AngleCalculator.swift:23` | `AngleQuestion` 与袋索引 |
| 同文件 `:74` | `generateQuestion` 及共享生成范围；`:196` 附近 fallback 分支 |
| `QiuJi/Core/Scene/AngleSceneCalculator.swift:35` | 假想球几何；`:185` 有效入袋点 |
| `QiuJi/Core/Geometry/AimPointGeometry.swift:20` | G1 `aimPoint`；`:43` `signedOffset` |
| `QiuJi/Core/Components/BTFeedback.swift:43` | mm 正确/接近/偏差等级 |
| `QiuJi/Core/Physics/BTPhysicsConstants.swift:183` | 速度量程；`:191` 当前固定验证速度 |
| `QiuJi/Core/Physics/ShotPredictor.swift:500` | `simulateFree`，固定方向、物理精度与输出；`:585` 附近事件提取 |
| `QiuJi/Core/Physics/SimulationWorker.swift:19` | 值快照；`:84` 单任务提交/取消，不是现成的批搜索器 |
| `QiuJiTests/AimPointGeometryTests.swift` | 现有 G1 几何测试，实施前按实际断言复核 |
| `QiuJiTests/AngleResultSaveFailureTests.swift:130` | 场景瞄准点成绩保存失败保留答案 |
| `QiuJiUITests/S3_AimPointUITests.swift:59` | 场景 2D 布局既有测试 |
| `QiuJiUITests/S5_AimPointSceneV5UITests.swift:43` | 2D 运杆测试；`:73` 3D 相机旧测试，不能把旧命名当最新需求 |

## 8. 证据与跨会话交接

实施证据统一放 `output/aim-point-theory/W0`–`W3`，不覆盖其他任务输出。每批报告记录输入样本、方向/速度、袋 ID、事件结果、引擎配置指纹、预算、设备/runtime、原始退出码及失败案例。测试若写文件，使用专题输出目录，不要求整个原本脏工作区零 diff。

每批完成更新本文件状态及 PROGRESS；未达标保留进行中和缺口。下一会话先读本文件、上一批报告与当前差异，不从讨论早期口径重新设计。禁止为了新流程删掉无关断言；若既有测试需改，先给出断言原文、最新语义及失败机理。

## 9. 版本记录

| 版本 | 日期 | 原因与状态 |
|---|---|---|
| v59.0 | 2026-09-10 | 按用户最新理论教学目标及 issue-collection-restructure 形成四批系统方案；已核对代码，尚未运行覆盖扫描/构建或实施 |

| v59.1 | 2026-09-10 | 用户授权开始；启动 W0 固定样本扫描，未改变教学语义 |

| v59.2 | 2026-09-10 | W0 探针发现零时长状态转换被过滤，停球状态不收敛；加入保持物理参数不变的状态转换前置修复和不变量回归，之后重跑冻结样本。产品判分/示范需求不变 |

| v59.3 | 2026-09-10 | W0 完成固定矩阵、细扫、纠正补采和停球前置修复；全部角度可覆盖，但 9 个中袋距离组未找到可用示范，W1 前置未通过，等待覆盖范围裁定 |

## 10. W0 结果与待裁定项（v59.3）

- 471 固定球形：固定 3.3 m/s 可用 233 个，多力度粗扫可用 295 个；29 个失败代表细扫新增 4 个有解样本。全部 60 个角度/袋型/切向组都有可用样本。
- 额外 4,051 次生产范围生成，取得 67 个合法球形；75°远角袋补到示范，40/50/55/60/65/70/75/80°远中袋及70°中距离中袋仍未找到。有限预算未找到，不证明绝对无解。
- 零时长状态转换前置修复已通过 53 项物理回归，未调整物理常量。完整代码/日志/指纹/失败记录见 W0 报告。
- **建议裁定**：全部 15 角度档必须保留；允许筛除当前没有可用示范的具体球形，并接受部分距离组缺失，继续真实物理示范。
- **另一分支**：若距离范围也必须完整覆盖，先设计明确标识的理论示意并更新示范契约。
- 上述两项均未替用户拍板。保留 §5 不得静默丢层的红线，W1–W3 未开始；决定后更新版本与准入条件。
