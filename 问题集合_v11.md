# 问题集合 v11 — 学区瞄准法/球理内容扩充 + 分离角可视化交互页

> **本文件是本轮需求的真源。** 日期：2026-07-17　状态：✅ Y1–Y3 全部完成（2026-07-18；D-v11-1~5 已全部拍板 2026-07-17）　排期前置：无硬前置；与 `问题集合_v8.md`（练习 Tab 交互反馈，X1–X6 执行中）**无关不覆盖**，但 Y 批与 X 批都触练习 Tab，建议不与 X 批同波并行（见 §六）。
> 全部锚点已读代码核实（2026-07-17，见 §五）。编号约定：条目 **N1–N2**；批次 **Y1–Y3**；拍板项 **D-v11-N**。

---

## 一、需求回显（语义基准，不增删）

| # | 用户诉求（原意） | 验收语义 |
|---|---|---|
| N1 | 练习 Tab「学」区目前只有角度瞄准法一种瞄准法，要补充其他瞄准法：**管道瞄准法**、**撞击点瞄准法**（用户注明名字可能不准确）、**平行线瞄准法**（基本可从现有角度瞄准法推演出来），以及其他常用理论：**投掷效应**、**加塞理论**、**母球和目标球接触时母球的旋转状态**等；可做必要调研；参考现有图文样式 | 学区新增上述内容，图文样式与现有学页（真台插图 + 分节卡片 + 术语词表）一致，术语/几何口径与既有页不冲突 |
| N2 | 新增一个**分离角可视化交互展示**，样式有点像「角度与瞄准」。用户可选择**力度**，展示从**纯高杆到纯低杆**时白球（碰撞后）到吃第一库的轨迹，共 **8 条轨迹线、不同颜色**；文字标注**只标注纯高杆和纯低杆**两条；力度条是可选变量，故**按统一标准把力度条和打点盘展示出来** | 新交互页：真台场景 + 8 条不同色母球轨迹（碰撞点→第一库），随力度实时更新；仅两端轨迹带文字标注；右缘统一仪表柱（力度条 + 打点盘） |

---

## 二、调研结论（内容口径，写作时的事实来源）

> 调研于 2026-07-17（网络检索：新锐台球学院、Mosconi 平行瞄准、假想球/重合比例法多源交叉；另盘点项目 16 理论库）。写作时以此为口径，禁止临场编造。

### 2.1 瞄准法谱系（N1 前半）— **v11.2 按用户原意勘误（FL-026）**

> v11.0 曾把三法映射为「单管道 / 重合比例法 / Mosconi 平行线」，2026-07-17 用户指正与原意不符，本节为**唯一有效口径**。记号：T 目标球心、P 袋口、n = unit(P−T) 进球方向、G = T − 2R·n 假想球心、C 母球心、θ 切角；**接触点对**：Pt = T − R·n（目标球背袋点）、Pc = C + R·n（母球沿 n 方向的球面点）。核心恒等式（数值草稿已验证，`build/y1-evidence/y1r1-user-definitions-draft.txt`）：**Pc→Pt ≡ G − C**（同向等长，θ 5°–70° 扫描误差 <1e-14）。

| 方法 | 定义（用户原意，已几何验证） | 从角度瞄准法的推演 |
|---|---|---|
| 角度瞄准法 | （已有，瞄准原理页）第三人称看出切角 θ → d = 2R·sin(θ) 翻译成瞄准点 | 基准 |
| 管道瞄准法 | **瞄准线与进球线各扩成一条管道**（半径 = 球半径 R，管宽 = 球径；勘误：若半径取球径 2R 两管道互相穿越 0.057m，不成立），**两管道外切处 = 两球接触点 Q** | 两管道外切 ⇔ \|G−T\| = 2R ⇔ 母球心到达 G，切点 Q = (G+T)/2 = Pt；即「相切」是 d = 2R·sin(θ) 的体积化等价表述 |
| 接触点瞄准法（点对点） | 标出目标球接触点 Pt 与母球对应点 Pc，想象击球过程**把两点碰到一起** | Pc→Pt ≡ G−C ⇒ 两点重合 ⇔ 母球心平移到 G。同时量化经典误区：球心**直指** Pt 的方向与真瞄准线差约 1.8°（θ=30°、球距 0.42m），必须「点对点」而非「心对点」 |
| 平行线瞄准法 | 连接 Pt 与 Pc，**过母球心作该连线的平行线即瞄准方向**；碰撞瞬间两球心关于接触点 Q **点对称**（从瞄准视角看两球心分居切点两侧） | 由恒等式，该「平行线」与 C→G 不止平行、完全重合同向；点对称已数值验证（中点重合 <1e-15）。Mosconi 版（过球心作**进球线**平行线再收拢）降级为节末变体附注 |
| 重合比例法（厚薄法，补充节保留） | 俯身看重合比例定厚薄（1/2 球≈30°、3/4 球≈14.5°、1/4 球≈48.6°），数值唯一真源 `AngleSceneCalculator.NamedBallThickness` | θ↔重合度对照，v11.0 曾误挂「接触点」冠名，现为独立补充节 |

**Y1 交互化设计（用户 2026-07-17 拍板「可视化或可交互」）**：三法节共享可调切角 θ（滑杆 5°–75°，默认 30°）——①管道节：用户拖「试瞄角」φ，实时判定两管道相离/相切/相交（徽章：太薄/✓相切/太厚），φ≈θ 吸附并高亮切点 Q；②接触点节：标 Pt/Pc + 连线，播放键母球沿瞄准线平移至两点碰合于 Q（球心落 G），旁置灰色误导线（心对点）标注误差角；③平行线节：金色 Pc→Pt 连线 + 过球心白色平行线，θ 联动展示恒平行。

注意：现有「瞄准点对照表」页已覆盖 θ↔瞄准点/接触点速查；「接触点≠瞄准点」误区已在瞄准原理页「常见误区」卡讲过。新内容**交叉引用**这两处，不重复造表。

### 2.2 球理理论（N1 后半）

| 主题 | 内容要点 | 既有资产 |
|---|---|---|
| 母球接触时的旋转状态 | 滑动（stun，纯平移）/ 前旋（follow）/ 后旋（draw）/ 自然滚动；出杆后母球经历「滑动→滚动」过渡，**接触瞬间处于哪种状态决定分离角**：stun 走 90° 切线（tangent line），前旋向前弯（30° 法则），后旋向后弯 | 项目 16 `theory/theorems/T01-30-degree-rule.md`、`T02-90-degree-rule.md`、`T03-tangent-line.md`；App 内 `SeparationAngle` 枚举（UI 经验提示） |
| 投掷效应（throw） | 切球投掷（cut-induced throw）与旋转投掷（spin-induced throw）：碰撞瞬间两球面摩擦把目标球「带偏」进球线 1–5°，薄球/慢速/加塞时最明显；假想球法的系统性误差来源 | 引擎碰撞模型含 throw（`resolveBallBallPure` throw 同源，见 PROGRESS B2 解析瞄准层记录）；可如实说明 App 求解器已计入 |
| 加塞理论 | 三件套：**让点/喷射**（squirt，打点偏离中心母球出射偏移）、**弧线**（swerve，台呢摩擦使带塞母球走弧线）、**投掷**（throw）；以及加塞下的瞄准修正（管道法即为此服务）、最小加塞原则 | 引擎 `CueBallStrike` 含 squirt；项目 16 `T09-minimum-english.md`；打滑极限 0.5R（`CuePhysics.miscueLimitFraction`）解释「塞的上限」 |

**红线**：所有涉及角度/几何的正文与插图，执行时遵循 `geometry-spatial-reasoning` 技能（坐标契约回显 + 数值草稿），厚薄数值一律引 `NamedBallThickness` 真源；术语查 `tasks/UI-IMPLEMENTATION-SPEC.md` §8.8 词表（切角/假想球/力度/瞄准点=G1 垂足定义）。

---

## 三、代码现状（事实，已核实）

### 3.1 「学」区结构

- 学区 = 硬编码 SwiftUI 文档页，**非 JSON 数据驱动**。入口卡数组 `learnEntries`（`AngleHomeView.swift:105–124`），现 4 卡：瞄准原理 / 角度与瞄准 / 浅谈球感 / 瞄准点对照表。
- 「角度瞄准法」不是独立页，是瞄准原理页内 `whyAngleSection`（`AimingPrincipleView.swift:91–110`，L102 点名「角度瞄准法」）。
- 图文样式真源：`BTTableFigure`（USDZ 真台离屏底图 + 世界坐标投影，`QiuJi/Core/Components/BTTableFigure.swift`）+ `FigureLine`（线语言取色对齐 `TrajectoryStyle`）+ `BTFigureBall`/`BTGhostCircle`/`BTAimPointDot`/`BTContactDot`/`BTFigureTag`；段落 = `.btTitle`/`.btBody`/`.btCaption` + `.btBGSecondary` 圆角卡。参考范式：`AimingPrincipleView` 的 `AimingFigure`（L366+）。
- 新增一页的注册点（四处）：`AngleRoute` 枚举 + `learnEntries` 卡（`AngleHomeView.swift`）、封面色 `AngleCoverPalette.swift`、destination 映射 `MainTabView.swift` `angleDestination`（L110–158）。`project.yml` 为 folder 源，新 .swift 自动进 target。学区无 Freemium 锁。

### 3.2 「角度与瞄准」交互页（N2 样式参照）

- `AngleDynamicView.swift` + `AngleDynamicViewModel.swift`：SceneKit 真台（`AngleSceneView`/`AngleTrainingScene`）+ 纯几何计算（`AngleSceneCalculator`），**不跑物理引擎**；布局走 `ShotStageProxy` 锁桌；HUD `btHudGlass` 指标条；三点菜单 `BTSolverMoreMenu`（仅网格）。

### 3.3 力度条/打点盘统一标准（N2 直接复用）

- `BTShotInstrumentColumn`（`QiuJi/Core/Components/BTShotInstrumentColumn.swift:14–22`）：右缘仪表柱 = 打点迷你图（`onSpinTap` 非 nil 才显示）+ 竖直力度柱 + 读数；`BTSpinPad`/`BTSpinPadOverlay`（`BTSpinPad.swift:11–71, 251–341`；约定 spinY 正=高杆）。
- 力度量程 `ShotTuning.velocityRange = 0.5...8.0`、默认 1.5、γ=1.8（`BTPhysicsConstants.swift:179–211`）。

### 3.4 物理能力（N2 轨迹来源）

- `ShotPredictor.simulateFree(cueBall:aimDir:velocity:spinX:spinY:…)`（`ShotPredictor.swift:440–452`）：给定方向+力度+打点跑全保真物理；输出 `ShotPrediction.cuePath`（母球折线）+ `events: [ShotEvent]`（`.ballBall`/`.ballCushion`/`.pocket`，L75–83, 138）。
- 「碰撞后到第一库」段无现成 API，组合即可：取首次 `.ballBall`（母球-目标球）时刻 → 找其后母球首条 `.ballCushion` → 按时间在 `cuePath`/`recorder` 上切片。已有同款切片范式：`BankKickDifficulty.swift` ~L365–373（按 `firstContact` 截 `cuePath`）。
- 「纯高杆/纯低杆」物理上界 = 打滑极限 `CuePhysics.miscueLimitFraction = 0.5`（`BTPhysicsConstants.swift:233`）⇒ 8 条轨迹 = spinY 在 [+0.5, −0.5] 均匀取 8 档、spinX=0。
- 性能：simulateFree 单次数十 ms 量级，拖力度条需 8 次/帧不可行 ⇒ 复用既有「20ms 去抖 + 单飞 + 末班车」模式（P3，`PositionPlayViewModel` 有范式）+ `concurrentPerform` 并行 8 条；`simulateFree` **不填** `separationAngleDeg`（仅 predict 路径填，L94–95 注释），本页只画轨迹不依赖该字段。

### 3.5 与既有页的关系（防重复）

- 「打」区已有「分离角与走位」（`ShotSimulationView`）：单杆自由模拟沙盘。N2 是**多轨迹对比图谱**（同一杆 8 种杆法并排），定位教学可视化，不与其重复；命名需区分（见 D-v11-2）。
- 8 条不同色轨迹与线语言 v2「线色=球的身份」冲突 ⇒ 需页内专用色板豁免并记 DR（对齐 `AngleCoverPalette` 式局部色板先例），执行批落 `tasks/IMPLEMENTATION-LOG.md` DR-NNN + SPEC Changelog。

---

## 四、拍板项（开工前需用户确认；带推荐）

| # | 问题 | 推荐 | 状态 |
|---|---|---|---|
| D-v11-1 | N1 内容组织：塞进「瞄准原理」页 vs 新增独立卡？ | **新增 2 张学卡**：①「瞄准方法」（管道/接触点重合比例/平行线 + 各法与角度瞄准法的推演关系）；②「旋转与加塞」（母球旋转状态/投掷效应/加塞三件套）。瞄准原理页已 7 节，再塞会过长；角度瞄准法留在原页，新页交叉引用 | ✅ 按推荐（2026-07-17 用户拍板） |
| D-v11-2 | N2 归属与命名 | 放「学」分段（样式像角度与瞄准，定位是理解分离角，非沙盘）；命名「**分离角图谱**」以区分打区「分离角与走位」 | ✅ 按推荐（2026-07-17） |
| D-v11-3 | N2 打点盘角色：可交互 vs 只读？ | **只读展示态**：盘上高亮 8 个纵向采样打点（与 8 色轨迹同色对应），spinX 恒 0 不可拖（8 条轨迹已覆盖纵轴变量；开放 spinX 会让「8 条线」语义失效）。力度条可调 | ✅ 按推荐（2026-07-17） |
| D-v11-4 | N2 球形是否可拖（改切角/球位）？ | **可拖**（母球/目标球可拖，复用 AngleDynamic 拖球交互）——「样式像角度与瞄准」隐含可拖；固定球形版体验太死。默认球形取 ~30° 切角典型位 | ✅ 按推荐（2026-07-17） |
| D-v11-5 | 执行模型 | 用户拍板：三批统一 `cursor-grok-4.5-high-fast`；配套纪律——委派提示词红线前置 + 主控验收升级为**逐文件读 diff** | ✅（2026-07-17） |

> 拍板项不阻塞 Y1（Y1 仅受 D-v11-1 影响，且推荐方案即默认）；Y3 受 D-v11-2/3/4 影响，开工前须定。

---

## 五、锚点索引（全部读代码核实，2026-07-17）

| 锚点 | 位置 | 用途 |
|---|---|---|
| `AngleRoute` 枚举 / `learnEntries` | `QiuJi/Features/AngleTraining/Views/AngleHomeView.swift:5–33, 105–124` | 新页注册 |
| destination 映射 | `QiuJi/App/MainTabView.swift:110–158`（`.aimingPrinciple` L114） | 新页注册 |
| 封面色 | `QiuJi/Core/DesignSystem/AngleCoverPalette.swift:13–29` | 新卡封面 |
| 「角度瞄准法」现文 | `AimingPrincipleView.swift:91–110`（`whyAngleSection`）；整页节结构 L10–25；`AimingFigure` L366+ | 图文样式范式 + 交叉引用点 |
| 厚薄真源 | `AngleSceneCalculator.NamedBallThickness`（v7 C1 定稿：name↔θ↔overlap↔d/R 四元组） | 接触点/重合比例法数值 |
| 图示基建 | `QiuJi/Core/Components/BTTableFigure.swift:15–47, 93–111`（`FigureLine`） | 新页插图 |
| 交互页范式 | `AngleDynamicView.swift:46, 84–106`；`AngleDynamicViewModel.swift`（`updateCalculations` ~L330+，纯几何） | Y3 页面骨架 |
| 仪表柱 | `QiuJi/Core/Components/BTShotInstrumentColumn.swift:14–22`；`BTSpinPad.swift:11–71, 251–341` | Y3 力度条+打点盘 |
| 力度量程 | `QiuJi/Core/Physics/BTPhysicsConstants.swift:179–211`；打滑极限 L230–233 | Y3 量程/spinY 上下界 |
| 物理入口 | `ShotPredictor.swift:440–452`（`simulateFree`）；`ShotPrediction.events` L75–83, 138；`separationAngleDeg` 仅 predict 填 L94–95 | Y3 轨迹计算 |
| 轨迹切片范式 | `QiuJi/Features/AngleTraining/BankKickDifficulty.swift` ~L365–373 | 碰后→第一库切片 |
| 去抖/单飞范式 | `SolveDebounceScheduler`；`PositionPlayViewModel` P3 单飞模式 | Y3 力度拖动性能 |
| 词表/口径 | `tasks/UI-IMPLEMENTATION-SPEC.md` §8.8；G1 瞄准点=垂足（`Core/Geometry/AimPointGeometry.swift`） | 全批术语红线 |
| 理论素材 | `/Users/song/projects/16.billiard_theory/theory/theorems/`（T01/T02/T03/T09） | Y2 内容来源 |

---

## 六、批次表（Y1–Y3）

三批彼此内容独立，但**都触同一组注册文件**（AngleHomeView / MainTabView / AngleCoverPalette）⇒ **建议串行**（并行 worktree 合并收益小于冲突成本）；顺序可换。每批一次会话可完成并验收。与 v8 的 X 批（练习 Tab 交互）文件面有交集（AngleHomeView 等），**不与 X 批同波并行**。

| 批次 | 范围 | 量级 | 依赖 | 建议角色 | 模型（派发前按 01 规则确认） | 状态 |
|---|---|---|---|---|---|---|
| **Y1** | 新学页「瞄准方法」：管道瞄准法（双管道相切）/ 接触点法（点对点）/ 平行线瞄准法（接触点连线过心平行）三节按 §2.1 v11.2 口径 + 交互化（θ 滑杆/试瞄/碰合动画）+ 厚薄法补充节 + 学卡注册 + 瞄准原理页交叉引用 CTA | L（1 新 View + 交互 Figure ×3 + 注册 4 处） | D-v11-1 | SwiftUI Developer（内容写作密集） | `cursor-grok-4.5-high-fast`（用户拍板 2026-07-17；主控逐文件读 diff 验收） | ✅ 2026-07-17（返工 1 轮，FL-026；主控独立验收通过：逐文件读 diff + r1 日志/截图亲验 + 主控亲跑主树 build SUCCEEDED；证据 `build/y1-*` r1 前缀；单测 `AimingMethodsGeometryTests` 5/5、巡游明/暗 + 交互取证 UI 测全绿；词表增补入 SPEC §8.8） |
| **Y2** | 新学页「旋转与加塞」**（v11.3 边界重定，per v12 D-v12-1）**：聚焦母球旋转状态与分离角（滑动/滚动/前旋/后旋 → 分离角联动，T01/T02/T03 + 最小加塞 T09 + 打滑极限）；~~投掷效应 / 加塞三件套深讲~~ 改为一句话概念 + CTA 指向 v12 新页「瞄准修正」（投掷/挤偏/弧线深讲及其对瞄准的影响归该页）+ 注册 + 指向 Y3 页的学→学 CTA（若 Y3 已落地） | M（同 Y1 结构，范围缩小） | D-v11-1；**v12 Z 批先行**（CTA 指向的「瞄准修正」页，后落地者补链）；与 Y3 的 CTA 互链软依赖（后落地者补链） | SwiftUI Developer | `cursor-grok-4.5-high-fast`（用户拍板 2026-07-17） | ✅ 2026-07-17（0 轮返工；用户指令 Y2 先于 Z 批执行，「瞄准修正」CTA 降为文字预告、Z 批落地后补链；主控独立验收通过：逐文件读 diff + `build/y2-*` 日志/截图亲验 + 主控亲跑主树 build SUCCEEDED；单测 `SpinAndEnglishGeometryTests` 2/2、巡游明/暗 a14 五帧全绿；词表 4 条增补入 SPEC §8.8） |
| **Y3** | 新交互页「分离角图谱」：SceneKit 真台 + 可拖双球 + 8 条 spinY 均匀档轨迹（碰撞点→第一库，8 色页内专用色板 + DR）+ 仅两端标注「纯高杆/纯低杆」+ 右缘 `BTShotInstrumentColumn`（力度可调、打点盘只读 8 点高亮）+ 去抖/单飞 + 并行 8 次 simulateFree + 注册 | L（View + VM + 物理接线 + 性能 + DR/SPEC 回写） | D-v11-2/3/4 | SwiftUI Developer + 物理接线（**几何红线：`geometry-spatial-reasoning` 强制**） | `cursor-grok-4.5-high-fast`（用户拍板 2026-07-17） | ✅ 2026-07-18（**返工 1 轮**：主控验收发现默认力度 1.5 纯低杆碰后停球不吃库 → 第 8 条轨迹与「纯低杆」标注缺失；根因实证后切片扩展「未吃库降级到停球点」；r1 后 `make build` SUCCEEDED + 主控亲跑主树 SUCCEEDED；单测 `SeparationAngleAtlasTests` 4/4；巡游 a15 明/暗 + 交互取证全绿；8 路并行实测 ~66ms；DR-025 线语言豁免；证据 `build/y3-*`，r1 产物带 `-r1` 后缀） |

### 依赖图

```
D-v11-1 ─▶ Y1 ─▶ Y2 ─┐
                      ├─（互链 CTA 后补）
D-v11-2/3/4 ─▶ Y3 ────┘
（Y1/Y2/Y3 内容独立，注册文件共享 ⇒ 串行；顺序可按用户优先级调整）
```

### 各批完成标准

**Y1 / Y2（学页）**
1. `make build` BUILD SUCCEEDED（真实输出）。
2. 新卡出现在学分段，导航可达；明/暗两态整页截图逐节核验（插图无溢出/标签无遮挡，对齐 S4 批验收口径）。
3. 插图几何经数值草稿核验（坐标契约回显，禁脑算）；厚薄/角度数值与 `NamedBallThickness`、既有对照表口径一致。
4. 术语过 §8.8 词表；不与「瞄准点对照表」「瞄准原理」重复成段内容（重复处改交叉引用）。
5. `ScreenshotTourUITests` 学区巡游用例扩新页帧（对齐 `testAngleLearningPages` 模式）并通过。
6. 内容事实来源可追溯（§二调研口径 / 项目 16 定理），不编造数值。

**Y3（分离角图谱）**
1. `make build` BUILD SUCCEEDED；新增单测：① spinY 8 档端点=±miscueLimit、② 轨迹切片起点=首次球-球碰撞、终点=其后母球首个 ballCushion（对 events 断言）、③ 中杆（spinY=0 高速）首段方向≈切线方向（90° 法则数值容差断言）。
2. UI 测试截图：默认态（8 色轨迹 + 两端标注 + 仪表柱）/ 拖高力度后轨迹变化 / 拖球改切角后更新，逐张核验。
3. 拖力度条不卡顿：去抖 + 单飞路径有代码契约（必要时留性能日志实测一次 8 条并行耗时）。
4. 8 色板 + 线语言豁免记 DR-NNN 落 `tasks/IMPLEMENTATION-LOG.md` + SPEC §8 Changelog；页面契约补进 SPEC。
5. 布局走 `ShotStageProxy` 定高锁桌（G10 口径），仪表柱贴边同底（G5）。

---

## 七、明确不做（本轮范围外）

- 不改「瞄准原理」页现有七节结构（只加交叉引用 CTA）。
- 不做学区 JSON 化/CMS 化（维持硬编码页模式）。
- 不给学区加 Freemium 锁（现状无锁）。
- 不动「分离角与走位」（ShotSimulationView）与打点盘全局组件行为。
- 不做视频/动画出片素材（纯 App 内页面）。

---

## 八、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v11.0 | 2026-07-17 | 初版：N1/N2 需求回显 + 调研口径 + 锚点核实 + Y1–Y3 批次表 + D-v11-1~5 拍板项 |
| v11.1 | 2026-07-17 | D-v11-1~4 全部按推荐拍板；D-v11-5 定 `cursor-grok-4.5-high-fast`（三批统一，红线前置 + 主控逐文件读 diff）；波次定性：三批业务代码交集（注册文件）⇒ 全串行，每会话一批 |
| v11.2 | 2026-07-17 | **§2.1 勘误（FL-026）**：三瞄准法按用户原意重定义（双管道相切 / 接触点点对点 / 平行线=接触点连线的过心平行线），厚薄法降为补充节、Mosconi 降为变体附注；新增 Y1 交互化设计（θ 滑杆 + 管道试瞄 + 碰合动画）；Y1 状态 ✅ 撤回 → 🔄 返工中 |
| v11.3 | 2026-07-17 | **Y2 边界重定**（per `问题集合_v12.md` D-v12-1，用户拍板 2026-07-17）：Y2 聚焦「母球旋转状态与分离角」，投掷效应/加塞三件套深讲移交 v12 新学页「瞄准修正」（Z1–Z3），Y2 内只留一句话概念 + CTA 互链；Y2 排期改为 v12 Z 批之后 |
| v11.4 | 2026-07-17 | **Y2 收官**：用户指令 Y2 先于 v12 Z 批执行（排期偏差留档；「瞄准修正」为软依赖，CTA 降为文字预告、后落地者补链）。新学页「旋转与加塞」落地：旋转四态→分离角联动主轴（分段选择器 + 三路径示意图，几何真源 `SpinAndEnglishGeometry` 不变量单测锁定 90°/60°/120°）+ 最小加塞与打滑极限图（`miscueLimitFraction` 真源）+ 挤偏/弧线/投掷一句话概念；注册 4 处（琥珀封面色）+ 巡游 a14 扩帧明/暗全绿。词表 4 条入 SPEC §8.8。剩 Y3（依赖 D-v11-2/3/4 已拍板，随时可开工） |
| v11.5 | 2026-07-18 | **Y3 收官**：学分段交互页「分离角图谱」落地——SceneKit 真台 + 可拖双球 + 8 档 spinY 并行 `simulateFree`（碰后→第一库）+ 页内 8 色板（DR-025 豁免线语言 v2）+ 仅两端标注 + 右缘仪表柱（力度可调/打点盘只读）+ 去抖单飞；Y2 CTA 互链；单测 3/3、巡游 a15 明/暗 + 交互取证全绿。证据 `build/y3-*`。v11 Y1–Y3 全部 ✅ |
| v11.6 | 2026-07-18 | **Y3 返工 r1（主控验收打回）**：默认力度 1.5 + spinY=−0.5 纯低杆碰后回拖停球、无 ballCushion 事件（诊断实证 `build/y3-evidence/y3-default-lowspin-rootcause.txt`）→ 旧切片返回空 ⇒ 默认态仅 7 条轨迹、「纯低杆」标注缺失。修复：`pathAfterContactToFirstCueCushion` 未吃库时降级「碰撞点→停球点」（语义如实，吃库路径行为不变）；新增回归单测（4/4）；默认态截图重拍 8 线 + 两标注齐全；补性能实测 8 路并行 ~66ms（`y3-parallel-sim-measured.txt`）。SPEC §9.3 契约补切片降级语义 |
