# 问题集合 v43 — 我的计划改为我的模版（当日训练清单）

> **本文件是本轮需求真源**（v43.5，状态：**全案收官**；默认裁定已写入 §二，执行前若要推翻先改本文件再动手）。
> 来源：用户原话三轮——①「我的计划，修改为我的模版，然后里面不要每周几次的选项，这个更像是当天的训练清单，此外，应该支持长按删除模版」+ 健身 App 模版集截图；②「模版应该支持使用模版进行当天的训练」；③「写个详细的方案，一定要把里面所有的逻辑理顺」。
> **覆盖关系**：`问题集合_v35.md` **未执行**。本轮**接管**其 Q1（周次假配置）、Q2（非空无新建入口）、Q3（点卡无激活）及 W1 / W3 / W4 的自定义计划入口语义。v35 其余项（保存拦截、Picker Pro 锁、遍数粒度等）**不在本轮**，文件冻结，不改其历史裁定正文。
> **排期前置**：工作区已有大量未提交改动（动作库短标签、下架课、c022、DR-075/076/077 等）。本轮只改训练/模版相关 Swift 与对口测试/文档，**禁止顺手提交或改动无关文件**。开工前不必清空工作区，但 diff 必须能单独看出本轮范围。
> **模型**：用户已确认（2026-08-26）全批 `cursor-grok-4.6-high-fast`。后续会话读到本列即视为已指定。

---

## 一、需求回显（语义基准）

用户原话与已拍板结论（不增删语义）：

1. **「我的计划」改成「我的模版」**——只改用户自建那一块的叫法，不是把官方课表也改名。
2. **不要每周几次**——编辑器、卡片、加入训练 sheet 都去掉「每周训练天数 / N 次/周 / 每周 N 练」。它不是课表。
3. **更像当天的训练清单**——一份模版 = 一组动作 + 剂量倍数，拿来当「今天练这些」。
4. **长按删除模版**——带确认，不可撤销。官方计划不加长按。
5. **支持用模版进行当天的训练**——主路径是「把这份清单设为今日安排」，不是进编辑器。

| # | 需求 | 细化口径 | 状态 |
|---|------|----------|------|
| R1 | 自建区改称「我的模版」 | 界面用字一律「模版」（用户原字，与参考图一致）。代码类型名仍 `CustomPlan` | 本轮做 |
| R2 | 去掉每周几次 | 删除编辑器 `weeklyConfigCard`；三处副文案不再报周次。字段留库，写入冻结 | 本轮做 |
| R3 | 当天训练清单 | 用于今日后，首页「今日安排」= 该模版扁平动作表。不展示第 N 周第 M 天 | 本轮做 |
| R4 | 长按删除 | 首页自定义卡 + 计划列表自定义卡，长按 → 现有 `confirmationDialog`（取消在前） | 本轮做 |
| R5 | 用模版练今天 | 点卡主路径 =「用于今日训练」。已有激活则替换确认。设完回训练首页露出清单，**不**自动进记录页 | 本轮做（D-v43-1） |

**术语对照（FL-026）**

| 你的原话 | 采用定义 | 状态 |
|----------|----------|------|
| 「我的模版」 | 用户自建的 `CustomPlan` 列表。不是官方 `OfficialPlan` | ✅ 原话 |
| 「每周几次」 | 编辑器「每周训练天数」+ 卡片「N 次/周」+ sheet「每周 N 练」。**不是**个人设置里的「每周训练天数」（`TrainingGoalView`） | ✅ |
| 「当天的训练清单」 | 激活后的「今日安排」动作表；勾完成按**自然日** `TrainingSession`（已有） | ✅ |
| 「使用模版进行当天的训练」 | 写入 `UserActivePlan(isCustom: true)`，首页出现该清单，再由右下角「训练」开记。不是立刻 `startTraining` | ✅ D-v43-1 |
| 「长按删除」 | 长按卡 → 确认后删 `CustomPlan`；若正被今日使用，一并清 `UserActivePlan` | ✅ |
| 参考图双列卡 / 预览弹层 | **只作家教心智**（清单、无周次、用它开练）。本轮不抄双列网格、不抄预览 modal | ✅ D-v43-4 |

---

## 二、本轮裁定

| 编号 | 裁定 | 依据 |
|------|------|------|
| D-v43-1 | 「用于今日」= 设为今日安排并回训练首页，**不**自动进 `ActiveTrainingView`。开记仍走右下角「训练」/ 今日卡 GO | 用户要的是「当天清单」；立刻开练会跳过看清单。上一轮推荐，写入真源 |
| D-v43-2 | **点卡 = 用于今日**。编辑走 ⋯ / 齿轮。长按 = 删除确认。已是今日使用中的那张卡：点卡 toast「已是今日训练」，不重复写库 | 用户同时要求「用于今日」和「长按删除」 |
| D-v43-3 | **推翻 v35 D-v35-1**：周次控件删除，不是改文案留下。字段 `CustomPlan.sessionsPerWeek` 保留，新建/保存一律写 **1**（仅内部进位，用户不可见） | 用户明确「不要每周几次」 |
| D-v43-4 | 不做参考图双列卡、不做「编辑模版 / 用模版开始」预览弹层 | 范围纪律；心智已由 R3/R5 覆盖 |
| D-v43-5 | **不做 SwiftData 迁移**。不删 `sessionsPerWeek` 列。历史用户值可残留，但 UI 不再读写 | 继承 v35 红线，避开 ADR |
| D-v43-6 | **单激活替换制**（继承 D-v35-3）：全 App 同时只有一份 `UserActivePlan`。用模版会换掉官方计划（或另一份模版）。⛔ 多清单叠加合并今日安排不做 | 已有模型；用户未要求叠加 |
| D-v43-7 | 界面用字「模版」，不用「模板」。F-PL-09 当年统一成「我的计划」被本轮产品口径改回 | 用户原字 |
| D-v43-8 | 代码标识符不改名（`CustomPlan` / `isCustom` / `customPlanBuilder` 等）。只改用户可见文案与交互 | 最小正确改动 |
| D-v43-9 | 模版激活时，今日安排**隐藏**周/天文案和「跳过今天 / 回退一天」。`PlanProgressService` **算法不改**（v29 契约与 `V29W7PlanProgressTests` 不动） | 清单无课表；藏 UI 即可理顺，不必重写推进 |
| D-v43-10 | 个人设置 `TrainingGoalView`「每周训练天数」、官方计划 JSON / `PlanDetailView`「N 次/周」**不动** | 另一套功能 |

---

## 三、逻辑真源（执行时不得偏离）

> 本节是本轮「把逻辑理顺」的正文。实现以本节为准；与旧文案/旧点击冲突时改 UI，不改本节。

### 3.1 两个东西，不要混

| | 官方计划 | 我的模版 |
|--|----------|----------|
| 数据 | Bundle `Resources/Plans/*.json`，不可写 | SwiftData `CustomPlan` + `CustomPlanDrill` |
| 是什么 | 多周课表（周主题、每天不同动作） | 一张扁平动作清单（每次用都是同一张表） |
| 用户动作 | 点卡 → 详情 →「开始此计划」 | 点卡 → **用于今日**；⋯ 编辑；长按删除 |
| 今日安排 | 游标指向的那一天（随完成推进而变） | **整张模版表**（与游标无关） |
| 周/天 | 要展示，可跳过/回退 | **不展示**，不提供跳过/回退 |
| 删除 | 不能删 | 能删 |
| 本轮 | **零改动**（除被模版替换激活时的确认文案可复用） | 本轮全部工作 |

代码里两者共用 `UserActivePlan`（`isCustom` 分流）。这个模型保留。改的是**自定义这一支的用户语义**：从「也是一份计划」改成「可选用的今日清单」。

### 3.2 今日安排从哪来

```
UserActivePlan 至多 1 条
 ├─ 无          → 首页无「今日安排」，无右下角「训练」圆钮；空态引导选官方计划或自由记录
 ├─ isCustom=false → loadOfficialPlan：按 currentWeek/currentDay 取 JSON 当天
 └─ isCustom=true  → loadCustomPlan：按 planId(UUID) 取 CustomPlan，按 order 排出动作表
                     清单内容不读 currentWeek/currentDay
```

「勾完成」**已经是自然日**：`fetchTodayCompletedDrillIds` 只看今天的 `TrainingSession.drillEntries`。换自然日勾选自动清零，清单内容不变。这正是「当天清单」，本轮不改判定。

完成一次带 `planId` 的训练后，`PlanProgressService.advanceAfterPlanSession` 仍会给自定义支把游标 +1 天（按 `sessionsPerWeek` 进位、无末尾）。用户看不见。本轮不改这个函数，以免动 v29 契约。写入侧把 `sessionsPerWeek` 冻成 1，内部计数变成「练一次进一周」，依然不可见。

### 3.3 状态机：用于今日 / 编辑 / 删除

**用于今日（模版 T）**

1. 若当前 `UserActivePlan` 已是 T（`isCustom && planId == T.id`）→ toast「已是今日训练」，不写库。
2. 若存在任意激活（官方或另一模版）→ `confirmationDialog`：
   - 标题：用于今日训练
   - 正文：将替换当前的今日安排。确定用「T.name」作为今天的训练吗？
   - 按钮：取消（先）/ 确定（后）
3. 若无激活 → 可省略确认，直接写入；或仍弹「用「T.name」作为今天的训练吗？」（实现选**无激活也确认一次**，避免误触点卡立刻换今日）。
4. 写入：删光现有 `UserActivePlan`，插入 `UserActivePlan(planId: T.id.uuidString, isCustom: true)`（`currentWeek=1, currentDay=1`）。**唯一写口** = `DrillTrainingPlanService.activate`（已有，本轮让列表/首页/构建器都走它）。
5. 导航：`router.trainingPath` 清空，落回训练首页；`onChange(activePlanSignature)` 已会 `load`，今日安排应立即变成 T 的动作表。
6. 不调用 `router.startTraining`。

**编辑（模版 T）**

- 仅 ⋯ 菜单「编辑」或编辑器入口（列表 toolbar「新建」、溢出「新建模版」、空态 CTA）。
- `TrainingRoute.customPlanEdit(planId:)` 保留。点卡**不再**进编辑器。

**删除（模版 T）**

- 长按卡，或 ⋯ 里「删除」（可留，与长按同一套 dialog）。
- 确认后：`modelContext.delete(T)`；若 T 是当前激活，同时删对应 `UserActivePlan`（`PlanListView.deleteCustomPlan` 已如此，首页必须同样做）。
- 官方卡：无长按、无删除。

**官方计划激活（对照，本轮不改实现）**

- 仍走 `PlanDetailView`「开始此计划」。它也会替换 `UserActivePlan`。若当时今日是模版，会被官方计划换掉——这是 D-v43-6 的对称面，正确。

### 3.4 从动作库「加入训练」

官方 JSON 不可写。能改的只有模版。

| 当前今日 | 「今日训练」区 | 「我的模版」区 | 「新建模版」 |
|----------|---------------|----------------|--------------|
| 今日 = 某模版 T | 一行：加入 T（已有，改文案） | 列出全部模版，点 = 追加进该模版（不自动换今日，除非点的是 T） | 名称 + Toggle「设为今日训练」= 新建并可选替换 |
| 今日 = 官方计划 | **没有**可写的「今日」。说明：「当前今日来自官方计划，不能往课表里加动作。可加入已有模版，或新建模版并设为今日（将替换官方计划）。」 | 同上 | 同上；Toggle 开且已有激活 → **必须确认替换**（v35 D-v35-3，本轮补上，现网 sheet 创建仍是静默替换） |
| 无激活 | 说明可新建模版并设为今日 | 已有模版则列出 | 同上 |

追加进非今日模版：toast「已加入模版「name」」。追加进今日模版：toast「已加入今日训练「name」」。

### 3.5 编辑器

- 标题：新建模版 / 编辑模版。
- 名称 placeholder：「我的模版」（原「我的训练计划」）。
- **删除整块** `weeklyConfigCard`。VM 不再把 `sessionsPerWeek` 暴露给 UI；`save` 时写死 `1`。
- 训练项目列表、遍数 sheet、添加动作：**不动**（v34 剂量语义红线）。
- 保存菜单：「仅保存」/「保存并用于今日」（原文案「保存并激活」）。
- 「保存并用于今日」：先 `save`，再走与点卡相同的确认 + `DrillTrainingPlanService.activate`，然后 dismiss 并清空 `trainingPath`。

### 3.6 文案表（用户可见，必须对齐）

| 位置 | 旧 | 新 |
|------|----|----|
| 首页分段 | 我的计划 | 我的模版 |
| 计划列表分区标题 | 我的计划 | 我的模版 |
| 分区副标 | 用户创建 | 可保留，或改「自建清单」 |
| 列表/首页卡副文案 | `N 次/周 · M 项训练` | `M 项` + 动作名顿号串（`lineLimit(2)`）；无动作名时只写 `M 项训练` |
| 卡徽章「自定义」 | 自定义 | 模版 |
| 卡「已激活」 | 已激活 | 今日使用中 |
| ⋯ 菜单 | 编辑 / 激活此计划 / 删除 | 编辑 / 用于今日训练 / 删除 |
| 列表页 toolbar | 新建 | 新建 |
| 首页溢出菜单 | 新建自定义计划 | 新建模版 |
| 首页空态 | 还没有自定义计划 / 创建计划 | 还没有模版 / 新建模版 |
| 编辑器标题 | 新建计划 / 编辑计划 | 新建模版 / 编辑模版 |
| 保存菜单 | 仅保存 / 保存并激活 | 仅保存 / 保存并用于今日 |
| 动作详情 sheet | 我的计划 / 新建计划 / 每周 N 练 / 计划名称 | 我的模版 / 新建模版 / 去掉周次 / 模版名称 |
| toast | 已加入计划「…」 | 已加入模版「…」 |
| 创建失败 | 请填写计划名称 | 请填写模版名称 |
| 今日安排（模版） | 第 N 周 · 第 M 天；主题「自定义训练」 | 不显示周/天；主题可空或「今日清单」 |
| 删除 dialog | 删除计划 | 删除模版 |
| 激活确认 | 激活训练计划 / 从第 1 周第 1 天开始 | 用于今日训练 / 不提周天 |

读屏 / `accessibilityIdentifier`：能不改的不改（如 `customPlan-<uuid>`、`addToPlan_<uuid>`）。**会变的可见字**必须同步改 UITest 点按标签。

### 3.7 明确不做

- 官方计划货架、详情、周进度、Pro 锁。
- 个人设置周目标。
- Schema 迁移、改 `CustomPlan` / `UserActivePlan` 字段形。
- 改 `PlanProgressService` 推进公式；不删 `V29W7` 自定义进位用例。
- 改剂量 / `roundsPerFormation` / Picker / 未保存返回拦截（v35 W2）。
- 双列卡、预览弹层、点卡立刻开练。
- 多模版合并成一份今日清单。
- 把类型/路由改名为 Template*。
- 把模版做成按自然日自动轮换的真排程。

---

## 四、批次表

依赖图：`W1 → W2 → W3 → W4`。W1 收口写库与编辑器；W2 改两处货架交互（依赖激活真源）；W3 改今日安排显示与动作库 sheet（依赖「用于今日」已通）；W4 只收测试/文档，不添产品行为。

| 批 | 范围 | 主要改动面 | 量级 | 依赖 | 模型 |
|----|------|-----------|------|------|------|
| **W1 编辑器去周次 + 激活写口收口** | D-v43-3/5/8。删 `weeklyConfigCard`；`save` 冻写 `sessionsPerWeek = 1`；`CustomPlanBuilderViewModel.activate` 改为调 `DrillTrainingPlanService.activate`（可按 UUID fetch）；保存菜单/标题/placeholder 按 §3.6；错误文案改「模版」 | `CustomPlanBuilderView.swift` / `CustomPlanBuilderViewModel.swift` / `DrillTrainingPlanService.swift` + `CustomPlanBuilderViewModelTests` | M | 无 | cursor-grok-4.6-high-fast |
| **W2 点卡用于今日 + 长按删除 + 货架文案** | R1/R4/R5、D-v43-2/6/7。`PlanListView` + `TrainingHomeView` 自定义卡：点卡走用于今日（确认 → service.activate → 清 `trainingPath`）；长按删除（首页补齐与列表同一套删激活逻辑）；⋯ 改文案；副文案去周次；分段/空态/溢出菜单改「模版」；非空列表底部加「新建模版」行（点卡不再进编辑器后，这是首页可见的新建入口） | `PlanListView.swift` / `TrainingHomeView.swift` / `TrainingHomeViewModel.swift`（只改 `PlanBrowseTab.custom` 文案） | M | W1 | cursor-grok-4.6-high-fast |
| **W3 今日安排藏周天 + 动作库加入** | R3、D-v43-9、§3.4。`TodaySessionInfo` 增 `isFromTemplate`（或等价）；模版支隐藏周/天与 `progressMenu`；主题不再用「自定义训练」唬人。`AddDrillToTrainingSheet` 按 §3.4/§3.6；官方计划当今日时改说明；新建 Toggle 在已有激活时走替换确认 | `TrainingHomeView.swift` / `TrainingHomeViewModel.swift` / `DrillDetailView.swift` | M | W2 | cursor-grok-4.6-high-fast |
| **W4 测试、UITest 标签、产品文档** | 对口测试跟新语义（见下「断言机理」）；UITest 可见字；`docs/05` 「我的计划」→「我的模版」一句；`docs/09` 自定义区补「点卡用于今日 / 长按删除」；本文件批次打勾 | 测试 + `docs/05` / `docs/09` / 本文件 | S | W3 | cursor-grok-4.6-high-fast |

### 各批完成标准

**通用**：`make build` SUCCEEDED（或本环境等价 `xcodebuild`）；`ReadLints` 改动文件 0 新增；聚焦单测 0 **新增**失败（既有基线失败原样保留，⛔ FL-027）；git diff 不碰官方 `Resources/Plans/*.json`、不碰 `TrainingGoalView`、不碰 `PlanProgressService` 算法。

**W1**

1. 编辑器页面无「每周训练天数」控件（grep `weeklyConfigCard` / `每周训练天数` 在该文件为 0）。
2. 新建保存后 `CustomPlan.sessionsPerWeek == 1`，即使用例曾设过旧 VM 属性。
3. `activate` 仍替换为单条 `UserActivePlan(isCustom: true)`，且实现走 `DrillTrainingPlanService.activate`（列表/构建器不再各写一份 insert）。
4. 单测：见下「W1 断言机理」——改期望，不删用例、不喂绿。

**W2**

1. 首页 / 计划列表：点自定义卡**不**进编辑器；有/无激活两条确认路径可走通；确定后 `UserActivePlan.planId` 为该 UUID，首页「今日安排」标题为模版名。
2. 长按自定义卡出「删除模版」确认；取消不删；确定后该 `CustomPlan` 消失；若是今日使用中，今日安排消失。
3. ⋯ 仍能进编辑器、仍能用于今日、仍能删除。
4. 非空首页「我的模版」区可见「新建模版」行，点进新建页。
5. grep 用户可见「我的计划」「N 次/周」「激活此计划」「新建自定义计划」在 `TrainingHomeView` / `PlanListView` 为 0。

**W3**

1. 模版作为今日时：今日安排卡**无**「第 N 周」「第 M 天」，无「跳过今天 / 回退一天」菜单；官方计划作为今日时这两样仍在。
2. 动作详情「加入训练」：无「每周 N 练」；分区标题为「我的模版」；官方计划当今日时说明不再说「当前没有自定义今日训练」这种半对半错的话。
3. 已有激活时，sheet 新建并勾选「设为今日训练」会确认再替换（可用单测或 UI 断言）。

**W4**

1. 下列测试按新语义更新后跑绿（或声明某条为既有失败未动）：`CustomPlanBuilderViewModelTests`、`DrillTrainingPlanServiceTests`、`TrainingHomeViewModelTests`（只动本轮相关断言）、`V31W5WalkthroughUITests`（「新建自定义计划」标签）、`W26ScreenshotUITests`（「我的计划」）、`W27ScreenshotUITests`（「创建计划」）。
2. `V29W7PlanProgressTests` **零改动**（证明没动推进算法）。
3. `SwiftDataMigrationTests` 里 `sessionsPerWeek == 4` 的迁移断言 **零改动**（证明没迁 schema、没改历史列语义）。
4. `docs/05-信息架构与交互设计.md` 训练树「我的计划」改为「我的模版」并加半句「用于今日」；`docs/09` 3.1.2 自定义区与现网交互对齐。

### W1 断言机理（PD-027，改期望必须写清）

| 用例 | 现断言 | 本轮为何会红 | 改法 |
|------|--------|--------------|------|
| `test_initial_state` | `sessionsPerWeek == 3` | 属性删除或默认改为 1 | 删对该属性的断言，或改为 1 |
| `test_save_creates_customPlan` | 设 4 后落库 4 | save 冻写 1，4 不再被尊重 | 断言落库 `== 1`；删「设 4」行 |
| `test_edit_existing_plan` | load 后 VM `sessionsPerWeek == 2` | UI 不再回填周次 | 删该断言，改断言 name/drills |
| `test_save_existing_plan_updates` | 改 5 后落库 5 | 冻写 1 | 断言更新后 name 变、`sessionsPerWeek == 1` |
| 迁移 `sessionsPerWeek == 4` | 旧库 V2 值为 4 | **不应红**——没迁 schema | **禁止改** |

禁止：为让旧断言绿而重新露出周次控件；禁止删迁移用例。

---

## 五、锚点索引（2026-08-26 亲读核实）

> 行号是立档日快照。动手前重读，漂移了按符号重定位，禁止按过期行号盲改。

| 区域 | 文件 | 锚点（立档日） |
|------|------|----------------|
| 路由 | `QiuJi/Features/Training/Views/PlanListView.swift` | `TrainingRoute` L6–11（`customPlanBuilder` / `customPlanEdit` 保留） |
| 列表删除/激活 dialog | 同上 | L73–98；`activateCustomPlan` L348–356；`deleteCustomPlan` L358–371 |
| 列表「我的计划」 | 同上 | `customSectionHeader` L197–228（标题 L209）；卡 L230–319（⋯ L247–269；`次/周` L272；点卡 = `NavigationLink` edit L233） |
| 首页溢出新建 | `QiuJi/Features/Training/Views/TrainingHomeView.swift` | L122–134「新建自定义计划」 |
| 今日安排周/天 | 同上 | `scheduleInfoBlock` L221–263；`scheduleMetaText` L290–296；`progressMenu` L266–288 |
| 首页自定义区 | 同上 | `customPlanBrowsing` L553–583（空态 L556–566；点卡 = edit L569–571）；卡 `次/周` L603 |
| 开始训练 | 同上 | `startTrainingCircle` L667–672（W3 不改此钮语义） |
| 分段文案 | `QiuJi/Features/Training/ViewModels/TrainingHomeViewModel.swift` | `PlanBrowseTab.custom` L51–54（F-PL-09 注释） |
| 模版→今日 | 同上 | `loadCustomPlan` L244–293（L283 已写明「每天同一张表」；`weekTheme: "自定义训练"` L289） |
| 勾完成 | 同上 | `fetchTodayCompletedDrillIds` L301–311（自然日，本轮不改） |
| `TodaySessionInfo` | 同上 | L23–39（无 `isFromTemplate`，W3 加） |
| 编辑器周次 | `QiuJi/Features/Training/Views/CustomPlanBuilderView.swift` | 引用 L35；`weeklyConfigCard` L136–191；标题 L44；保存菜单 L305–325；激活 alert L95–102 |
| 编辑器 VM | `QiuJi/Features/Training/ViewModels/CustomPlanBuilderViewModel.swift` | `@Published sessionsPerWeek` L49 默认 3；load L82；save L189/L199；`activate` L210–218（与 service 重复） |
| 模型 | `QiuJi/Data/Models/CustomPlan.swift` | `sessionsPerWeek` L8；init L12–15。**不删字段** |
| 激活游标 | `QiuJi/Data/Models/UserActivePlan.swift` | L4–21 |
| 服务 | `QiuJi/Data/Services/DrillTrainingPlanService.swift` | `createPlan` 冻写 3→改为 1（现 L88）；`activate` L115–122（本轮唯一写口） |
| 动作库 sheet | `QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift` | `AddDrillToTrainingSheet` L586–677（「我的计划」L627；「每周 N 练」L636；「新建计划」L648；Toggle L651；官方当今日时说明 L619–623 **不区分官方/无激活**） |
| 推进（只读） | `QiuJi/Data/Services/PlanProgressService.swift` | 自定义按 `sessionsPerWeek` 无限循环 L10–11、L43–45、L123。**算法不改** |
| 周目标（禁动） | `QiuJi/Features/Profile/Views/TrainingGoalView.swift` | 「每周训练天数」L113 |
| 官方详情（禁动） | `QiuJi/Features/Training/Views/PlanDetailView.swift` | 「N 次/周」L148 |
| Debug 空态 | `QiuJi/App/RootView.swift` | `-w29.emptyCTA` L63–66（文案跟空态一起改） |
| 单测 | `QiuJiTests/CustomPlanBuilderViewModelTests.swift` | 初值 L28；save=4 L150/159；edit load=2 L263；update=5 L279/287 |
| 单测 | `QiuJiTests/DrillTrainingPlanServiceTests.swift` | create/activate；文案断言若有「计划」需跟 toast |
| 单测 | `QiuJiTests/V29W7PlanProgressTests.swift` | 自定义进位 L192–211。**W4 零改** |
| 单测 | `QiuJiTests/SwiftDataMigrationTests.swift` | V2→V3 `sessionsPerWeek == 4` L296。**W4 零改** |
| UITest | `QiuJiUITests/V31W5WalkthroughUITests.swift` | 「新建自定义计划」L111–124 |
| UITest | `QiuJiUITests/W26ScreenshotUITests.swift` | 「我的计划」L47 |
| UITest | `QiuJiUITests/W27ScreenshotUITests.swift` | 「创建计划」L92–95 |
| 产品 IA | `docs/05-信息架构与交互设计.md` | L58「我的计划」 |
| 设计稿 | `docs/09-UI设计交付文档.md` | 3.1.2 L164–171 已写「长按/滑动删除」，现网未做长按 |

---

## 六、批次验收记录

（执行时逐批填：日期、执行者/模型、证据路径、返工轮次）

| 批 | 状态 | 日期 | 证据 |
|----|------|------|------|
| W1 | ✅ | 2026-08-26 | 返工 0。build SUCCEEDED；Builder 20/0；Service 6/0。日志 `build/w43-w1-logs/`。未开模拟器。 |
| W2 | ✅ | 2026-08-26 | 返工 0。build SUCCEEDED；W2-5 grep 0；Service 6/0、HomeVM 21/0。日志 `build/w43-w2-logs/`。点卡/长按未开模拟器。 |
| W3 | ✅ | 2026-08-26 | 返工 0。build SUCCEEDED；HomeVM 24/0（含 3 条确认策略）。日志 `build/w43-w3-logs/`。未开模拟器。 |
| W4 | ✅ | 2026-08-26 | 返工 0。build SUCCEEDED；三份单测 20/0、6/0、24/0。W27 整文件绿。V31W5/W26 新字能点到，后段既有失败未动。日志 `build/w43-w4-logs/`。 |

---

## 七、版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v43.5 | 2026-08-26 | W4 ✅。W1–W4 全案收官（返工 0）。未 commit。未做模版货架真人点验。 |
| v43.4 | 2026-08-26 | W3 ✅（返工 0）。W4 开工。 |
| v43.3 | 2026-08-26 | W2 ✅（返工 0）。W3 开工。 |
| v43.2 | 2026-08-26 | W1 ✅（返工 0）。W2 开工。 |
| v43.1 | 2026-08-26 | 全批模型定为 `cursor-grok-4.6-high-fast`。W1 开工。 |
| v43.0 | 2026-08-26 | 立档。理顺官方计划 vs 我的模版；用于今日 / 编辑 / 删除 / 加入训练状态机；推翻 v35 D-v35-1；默认 D-v43-1～10；批次 W1–W4。 |
