# 问题集合 v28 — 三 Tab 表层语法与专业内容封面

> **真源声明**：本文件是「训练 / 动作库 / 练习三 Tab 视觉统一」本轮需求的**真源**，供 `plan-batch-execution` / `plan-delegated-execution` 执行。高版本覆盖低版本。
>
> - **版本**：v28.1
> - **日期**：2026-08-04
> - **状态**：✅ 收官（W0–W4，2026-08-04）
> - **来源**：`tasks/ui-reviews/UR-20260804-v27全局风格.md` + 用户拍板（针对性结构调整 + 混合封面）
> - **与 v27 关系**：保留 W1/W2 设计系统底座（`BTFilterChip` / `CoverPalette` / `BTThumbnailFrame`）；**不合并** v27 W3 薄黑台垫（`BTDarkStageMat`）。视觉目标由本文件接管。
> - **基线分支**：`main@1e19b4f` 或其后继 → `feat/v28-surface-grammar`
> - **编号约定**：条目 **E1–E6**；批次 **W0–W4**；拍板项 **D-v28-N**。

---

## 一、需求回显

### 1.1 用户原始诉求（经 v27 复审校正）

训练 / 动作库 / 练习「不像同一个专业产品」。v27 解决了 token 与配色纪律，但未解决：

1. 三 Tab 页面骨架、信息密度、卡片内容表达仍是三套语言
2. 练习封面同区单调，大字水印无法表达功能差异
3. 动作库首屏筛选与缩略图信息过密
4. 训练计划色与练习分区色语义混淆
5. 明→暗「转场」以 4pt 黑边冒充，收益不足

### 1.2 拍板项

| 编号 | 结论 | 影响批次 | 状态 |
|---|---|---|---|
| **D-v28-1** | 允许针对性调整页面结构和信息密度，不全面重做导航与核心业务流程 | W0–W3 | ✅ |
| **D-v28-2** | 练习封面混合策略：学=几何微插图；练/打/解=真实球桌底图+场景语义预览 | W1 | ✅ |
| **D-v28-3** | 统一卡片壳、栏目头、元信息与留白节奏；不强迫三种业务内容形态相同 | W0 | ✅（方案默认） |
| **D-v28-4** | 封面只消费静态、确定性视觉描述；禁止网格卡内启动 VM/求解器/完整 SceneKit 页；真台复用 `BTTableFigure` + `TableFigureRenderer` 缓存 | W1 | ✅（方案默认） |
| **D-v28-5** | v26 收官前保留「旧版/新版」语义，允许从台面覆层移入元信息行 | W3 | ✅（方案默认） |

---

## 二、条目清单

| # | 条目 | 批次 |
|---|---|---|
| E1 | 共享表层语法：`BTContentGridCard` / `BTLibrarySearchBar` / `BTLibrarySectionHeader` | W0 |
| E2 | 练习混合封面：`PracticeCoverVisual` + `BTPracticeCover`（25 正式入口映射） | W1 |
| E3 | 训练计划独立等级色阶 + 卡片接壳 | W2 |
| E4 | 动作库首屏减噪（球种进菜单；台面覆层减量；元信息行） | W3 |
| E5 | 三 Tab Light/Dark 截图总验收 + UITest | W4 |
| E6 | SPEC / SKILL / PROGRESS / Hub 回写 + DR | W4 |

---

## 三、批次表

| 批次 | 范围 | 主要文件 | 量级 | 依赖 |
|---|---|---|---|---|
| **W0** | E1 共享组件 + 三样卡 Preview；各页样板接线 | 新建 `BTContentGridCard` / `BTLibrarySearchBar` / `BTLibrarySectionHeader`；轻触 `AngleHomeView` / `DrillListView` / `TrainingHomeView` | 中 | 无 |
| **W1** | E2 练习混合封面 | `AngleHomeView`、新建 `BTPracticeCover` / `PracticeCoverVisual`、单测 | 大 | W0 |
| **W2** | E3 计划色阶 + 接壳 | `CoverPalette`、`BTPlanCover`、`TrainingHomeView`、`PlanListView` | 中 | W1（色板定稿） |
| **W3** | E4 动作库减噪 | `DrillListView`、`BTDrillCard`、`DrillListViewModel` | 中 | W0（可与 W1/W2 并行） |
| **W4** | E5+E6 总验收 | `V28VisualUnificationUITests`、SPEC/SKILL/PROGRESS | 小 | W1+W2+W3 |

依赖图：`W0 → W1 → W2`；`W0 → W3`；`W1 + W2 + W3 → W4`。

---

## 四、各批次完成标准

### W0
- [x] `make build` 通过
- [x] 三样卡外壳圆角 / 标题基线 / 内边距 / 元信息字号一致（Preview 或渲染测）
- [x] 不改变业务路由与筛选结果

### W1
- [x] route→visual 映射单测全覆盖、无 generic fallback
- [x] 练习首页首次进入无明显长时间 spinner
- [x] 四分区 Light/Dark 全滚动截图；同区卡片可凭图形区分

### W2
- [x] 计划色阶亮度/顺序/唯一性单测
- [x] 训练首页 / 计划列表 / 详情 Hero Light/Dark 截图
- [x] 「开始训练」CTA 不遮挡关键信息；路由与 Pro 状态不变

### W3
- [x] 既有筛选组合与清空路径测试通过
- [x] 首屏只剩一行 quick chips（等级）
- [x] 网格无徽章互遮；2:1 缩略图不裁左右库；旧/新版筛选仍可达

### W4
- [x] `make build`；相关单测；核心 UI 回归
- [x] Light/Dark 截图主控通过
- [x] 无 `HUDStyle` / `ShotStageMetrics` / 出片脚本 / Drill PNG diff
- [x] SPEC / SKILL Changelog + DR 回写

**证据**：`build/v28-build4.log`（BUILD SUCCEEDED）、`build/v28-logs/unit-tests.log`（30/0）、`build/v28-logs/uitest-{light7,dark}.log`、`build/v28-screenshots/`。DR-045。

---

## 五、不做清单

- ⛔ 5 Tab 导航重做
- ⛔ 场景页 HUD / `ShotStageMetrics` / `HUDStyle` 色值
- ⛔ Drill PNG 重渲、出片脚本、物理引擎
- ⛔ 精讲结构、记录 Tab、我的 Tab
- ⛔ Hero / 第三方转场框架
- ⛔ 继续微调 v27 W3 薄黑台垫

---

## 六、锚点索引（2026-08-04 核实）

见执行计划；关键文件：`AngleHomeView.swift`、`BTTableFigure.swift`、`CoverPalette.swift`、`TrainingHomeView.swift`、`PlanListView.swift`、`DrillListView.swift`、`BTDrillCard.swift`、`BTPlanCover.swift`。

## 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v28.0 | 2026-08-04 | 初版：v27 视觉复审重组；D-v28-1~5；W0–W4 |
| v28.1 | 2026-08-04 | W0–W4 实施收官；DR-045；截图与单测证据落盘 |
