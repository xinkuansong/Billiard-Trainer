# 问题集合 v55 — 统计页纯训练数据重构与视觉验证

> **本文件是本轮需求真源**（v55.6，制定日期：2026-09-03，状态：**W0 + W0.1 已完成；G1 已裁定；详细实施方案已就绪，W1 待开始**）。
> **用户目标**：统计页的主任务是展示用户已产生的训练数据；不展示“教练结论”、“训练建议”或“下一步练什么”。先用 Codex 内置文生图生成 3 个高保真视觉方向供选择，再实施被选方向。
> **执行边界**：本次使用 `issue-collection-restructure` 只将用户选定方向转为可执行批次；本轮不修改 SwiftUI、ViewModel、数据模型或测试。后续从 W1 开始逐批实施与验收。
> **工作区边界**：当前主工作区存在大量 v50–v54 并行未提交改动。执行时只改本批定向路径，不覆盖、回退或顺带整理其他改动。
> **版本说明**：仓库已有其他主题的 `问题集合_v2.md`–`问题集合_v54.md`；本轮新建 v55，高版本覆盖本文件低版本，不覆盖其他问题集合。

---

## 一、需求回显与不可弱化口径

| ID | 用户拍板 / 讨论结论 | 本方案的不可弱化口径 |
|---|---|---|
| R1 | 统计页主要展示用户的训练数据 | 页面信息架构从“已发生的数据”出发，不把教学、诊断或训练编排混入统计主页 |
| R2 | 教练结论和训练建议都不要 | 禁止“你的弱项”、“建议加强”、“下一步练习”、“教练评语”等推断性模块及 CTA |
| R3 | 先规划，再生成几张图看方向 | 先锁定数据口径和样例 fixture，再以同一数据生成 3 个可比较候选；不同图不得换数据来制造好看效果 |
| R4 | 改为直接使用 Codex 内置文生图 | W0 改用内置 `image_gen`，不再依赖 API key 或 Python SDK；内置入口不暴露底层模型，因此不宣称产物由 GPT-Image-2 生成 |
| R5 | 生成的页面是方向图 | 内置文生图产物只用于选信息层级、图表比例和视觉语法；中文字形、数值、图表坐标、间距和交互都要在 SwiftUI 中确定性重建 |
| R6 | 仅有周态不足以选方向 | G1 前必须让 A/B/C 均覆盖周、月、年；月态检查 30 日密度，年态检查 12 月密度，不得只看周态就拍板 |
| R7 | “好的，按照你的建议” | G1 选定 **C 双训练域分区为主体 + A 顶部总览与留白**；不再保留 B 的趋势优先层级 |
| R8 | 周/月/年都要能比较后再定案 | 页顶周/月/年作为全页唯一时间维度，同时驱动球台训练与屏内练习；屏内保留题型和袋口筛选，移除独立“本周/本月/全部”时间切换 |

### 1.1 一句话产品模型

> **统计页只回答“我练了多少、什么时候练、各类练得怎么样”，不回答“教练认为什么、接下来应该练什么”。**

### 1.2 显示边界

#### 必须显示

- 当前统计区间与起止日期。
- 训练天数、总训练时长、球台训练组数。
- 训练时长随时间的分布，及必要的上一周 / 月 / 年同口径数值对比。
- 真实球台训练（`drill`）和屏内认知训练（`cognitive`）的数据分区。
- 真实球台成绩按主分类分别统计；屏内角度训练单独显示次数、误差、正确率、趋势和区间分布。

#### 禁止显示

- 全局单一成功率 / 准确率：不同分类的单位可能是球、局或次，不可共用一个分母。
- `tool` 工具使用数据：它不是训练量或球技成绩，本轮从统计主页可视区移除；不删底层记录、不改同步和隐私口径。
- 强项 / 弱项、评分、排名、诊断结论、教练解读、训练建议、建议路径、训练推荐。
- 根据涨跌自动写出“进步”或“退步”；允许中性显示 `+12%` / `-8%` 这类事实差值。

### 1.3 不在本轮改动的口径

1. 保留“记录 / 统计”根页分段入口，不改 Tab 结构。
2. 保留 `StatisticsTimeRange.week/month/year` 当前滚动区间语义；本轮不把它静默改成自然周 / 自然月 / 自然年。
3. 保留 Pro 门槛和原订阅入口；本轮只调整锁定内容的排布与预览，不改商业权益。
4. 保留历史记录的数据、owner 隔离、同步、迁移和删除语义。
5. 不新增持久化字段或 SwiftData schema 版本；若选定设计真的需要新数据，必须另起问题集合，不在实施时偷加。

---

## 二、当前实现事实与问题清单（2026-09-03 实读）

### 2.1 当前数据链路

```text
TrainingSession(kind = drill | cognitive | tool)
    └─ StatisticsViewModel
         ├─ drill + cognitive → 训练天数 / 总时长 / 时长趋势
         ├─ drill             → 组数 / 分类成功率 / 分类对比
         ├─ cognitive         → 由 AngleHistoryViewModel 另行读取详细成绩
         └─ tool              → 不进训练量聚合，现 UI 仍有一行独立显示
```

### 2.2 现状判断

| 领域 | 当前实现 | 对本轮的影响 |
|---|---|---|
| 根页结构 | `HistoryCalendarView` 常驻保活“历史 / 统计”两个 pane | 无需重构导航，只改 `StatisticsView` 内容 |
| 统计顺序 | 区间 → 概况 → 时长 → 分类成功率 → 分类对比 → 角度训练 | 数据已较全，但长页面分节彼此像独立页，球台 / 屏内两类数据的层级不足 |
| 首屏概况 | 训练天数、总分钟、球台组数，再分 drill/cognitive/tool | 基本指标正确，但 `tool` 行与“纯训练数据”目标冲突 |
| 时长卡 | 显示“平均时长”、环比和柱图；实际月值是总时长 / 4，年值是总时长 / 52，却标为小时/月或小时/年 | 柱图与中性同期差保留；语义不成立的“平均时长”移除，不将它换名后继续显示 |
| 球台成绩 | 成功率与上期对比分成两个区块 | 同一分类的当期数值、样本量、上期差值被拆散，扫读成本高 |
| 屏内成绩 | `AngleHistorySection` 内又有类型、时间、袋口三套筛选 | 数据丰富，但与页顶“周 / 月 / 年”不是同一时间模型，不得在视觉上假装它们已联动 |
| 错误状态 | 主统计失败有重试；角度数据加载 `catch` 后静默降级 | 角度查询失败可能被误解为“没有数据”，实施时需真实失败态 |
| Free / Pro | 主统计受 `BTPremiumLock(.fullMask)` 保护，角度统计位于锁层之外 | 不改权益前提下重排；必须专门验证 Free 可见边界没有意外扩大或缩小 |
| 响应式 | v47 已有 Light/Dark/Free/紧凑/交互证据，AX Large 曾暴露图表轴标签重叠 | 新布局不得只在默认字号下视觉通过 |

### 2.3 问题总表

| ID | 优先级 | 问题 / 风险 | 归属批次 |
|---|---:|---|---|
| P55-01 | P0 | 没有将“只展示训练数据、禁止教练结论和建议”写成可验收的页面合约 | W0、W5 |
| P55-02 | P0 | 若直接从生成图仿制，容易把错字、假数据、错误坐标或不可实现布局带入 App | W0、W1 |
| P55-03 | P0 | 真实球台与屏内训练成绩不可被合成一个全局准确率 | W0、W2、W3 |
| P55-04 | P1 | 首屏指标正确，但页面长且各卡等权，难以快速建立总量 → 趋势 → 分类明细层级 | W0–W3 |
| P55-05 | P1 | `tool` 不进任何训练聚合，却仍出现在训练概况可见区 | W2 |
| P55-06 | P1 | 分类成功率与分类对比分离，用户需在两个区块之间对齐同一分类 | W2 |
| P55-07 | P1 | 主统计区间与角度统计区间当前不同，视觉重排后容易制造“已统一联动”的错误感知 | W3 |
| P55-08 | P1 | AX Large 的图表轴标签历史上已出现重叠，需把可读性作为验收门槛 | W4、W5 |
| P55-09 | P1 | 角度训练加载错误静默变成空数据，统计页不能诚实区分失败与空态 | W3、W4 |
| P55-10 | P1 | 现有全量巡游使用空库基线，不能代替统计页专用的确定性有数据 fixture | W0、W5 |
| P55-11 | P1 | `hasTrainingSessions` 查全部历史而非当前区间；用户有旧记录但当前周/月/年无数据时可能看到一页 0，而不是区间空态 | W1、W4 |

---

## 三、目标信息架构

### 3.1 全局层级

```text
统计区间（周 / 月 / 年 + 确切日期）
└─ 总览：训练天数 / 总时长 / 球台训练组数
   ├─ 训练时长：时间分布 + 上一区间中性差值
   ├─ 真实球台训练
   │  └─ 各主分类：成功/目标、分类成功率、组数、上期差值
   └─ 屏内认知训练
      └─ 测试次数 / 平均误差 / 最佳误差 / 正确率 / 趋势 / 角度区间
```

### 3.2 首屏要回答的问题

1. 这个区间练了多少天？
2. 总共练了多久？
3. 完成了多少球台训练组？
4. 训练主要发生在哪些日期？

首屏不需回答“我什么最弱”或“我该去练什么”。

### 3.3 球台与屏内数据分区

- “球台训练”展示 `drill` 的可比成绩和样本量。
- “屏内练习”展示 `cognitive` 的问题次数、误差和正确率，不与球台训练相加成一个成绩。
- 总训练天数为两类训练日期的并集，不等于两类天数简单相加；样例数据必须刻意覆盖同日发生两类训练的情形。

### 3.4 分类成绩的单行语法

同一分类的信息尽量在一行 / 一卡内完成扫读：

```text
准度    42/60 球    70%    6 组    较上期 +4%
```

- `42/60 球` 是原始样本，`70%` 是同分类内聚合结果。
- 差值只表达事实，不翻译成评价文案。
- 同分类出现混合单位时，必须原样暴露或分行，不得假装是统一“球”。

### 3.5 G1 裁定：C 骨架 + A 首屏

**选定结构**

1. 页顶保留一组周 / 月 / 年分段控件，其下用一行弱化文字显示确切起止日期。不实施生成图中的前后翻页箭头，本轮只看当前 rolling 区间。
2. 首张强调表层合并“训练总览 + 时长趋势”：大号显示总时长，同层显示训练天数与球台组数，下方接时长柱图和上一同口径区间差值。
3. 下方分为两个明确数据域：“球台训练”使用低强度品牌绿色表层；“屏内练习”使用中性表层与边框。颜色只建立域归属，不表示强弱。
4. 球台区不再拆成“分类成功率”与“分类对比”两张卡；每个主分类只有一行。
5. 屏内区保留题型、次数、平均误差、最佳误差、正确率、误差趋势和角度区间分布；不生成综合评分或文字结论。
6. 移除当前摘要卡的装饰性球路工程线；数字和图表是页面主角。

**生成图的使用边界**

- C 周/月/年图决定“两个数据域”，A 周/月/年图决定“顶部总览和留白”。
- 生成图中的中文、趋势点、柱数、区间百分比和像素坐标都不是实施真源。
- 真实 UI 只用 SwiftUI / Swift Charts / 项目 Design Token 确定性构建，不将 PNG 裁成背景图。

### 3.6 全页时间口径

| 选项 | 当期起点 | 当期终点 | 趋势分箱 | x 轴标签 |
|---|---|---|---|---|
| 周 | 当天零点往前 6 天 | 次日零点（左闭右开） | 7 个日箱 | 7 个星期标签；AX 可改为隔日读取，数据不减 |
| 月 | 当天零点往前 1 个日历月 | 次日零点（左闭右开） | 覆盖完整区间的每日分箱 | 首尾 + 等距稀疏日期，最多 5 个 |
| 年 | 当天零点往前 1 个日历年 | 次日零点（左闭右开） | 与实际区间相交的日历月箱 | 稀疏月份标签，最多 6 个 |

- 保留现有 rolling 语义，不改成自然周 / 月 / 年。生成图中的“30 日”与“12 柱”只是密度试验，实施时必须覆盖真实起止区间；年区间可与 13 个日历月相交，不得静默丢掉首月的部分数据。
- 当期与上期必须从同一区间定义生成，上期为相邻、不重叠的同口径区间，不再分散写死 `-13 天 / -2 月 / -2 年`。
- `StatisticsViewModel` 注入 `now` 与 `Calendar`，或注入统一的区间生成器；测试不再依赖真实系统时钟。
- 页面把当期 `DateInterval` 传给屏内练习区，避免 `AngleHistoryViewModel` 反向依赖 History 特性的 enum。
- `LocalAngleTestRepository.fetchInRange(from:to:)` 当前对上界使用 `<=`，与本契约的左闭右开不一致。W1 将它收口为 `< to`，并增加“终点前一刻计入、终点整点不计入”边界测试。

### 3.7 展示语法与排序约定

- 时长差值显示“较上期 `+32 分钟 · +17%`”；上期为 0 时显示“上期无训练”，不显示虚假 `0%`。
- 分类差值优先显示“较上期 `+4 个百分点`”，不用“成功率相对增长 4%”制造歧义；上期无样本时显示“新增数据”。
- 球台分类使用 `DrillCategory.allCases` 稳定业务顺序，只显示当期有样本的分类。不再按成功率高低排序，避免视觉上暗示强项/弱项并在切换时跳行。
- 同分类混合单位时，分段显示每种单位的 `made / target`；不合并分母。如本轮只能获得集合而无法回溯分项，先显示“混合单位”和总组数，停止伪造单一比率。
- 所有涨跌颜色都必须有正负号和文字语义；颜色不能是唯一编码。

---

## 四、内置文生图视觉候选契约

### 4.1 共用确定性数据 fixture

三张候选图必须使用同一组显示数据，用于比较布局，不作为真实用户数据：

| 区域 | 样例数据 |
|---|---|
| 时间 | 周；`2026-08-28 ~ 2026-09-03` |
| 总览 | `4 天`、`222 分钟`、`28 组` |
| 类型分解 | 球台训练 `3 天 · 178 分钟`；屏内练习 `2 天 · 44 分钟`（其中 1 天重合，故总训练天数为 4） |
| 时长趋势 | 周五至周四：`35, 0, 48, 55, 0, 42, 42` 分钟；较上周 `+32 分钟 / +17%` |
| 基础 | `38/50 球`、`76%`、`5 组`、`+3%` |
| 准度 | `42/60 球`、`70%`、`6 组`、`+4%` |
| 走位 | `21/40 次`、`53%`、`5 组`、`-2%` |
| 控力 | `16/30 次`、`53%`、`4 组`、`0%` |
| 屏内角度 | `96 题`、平均误差 `2.8°`、最佳误差 `0.6°`、正确率 `74%` |

#### 月态扩展 fixture

| 区域 | 样例数据 |
|---|---|
| 时间 | 月；`2026-08-05 ~ 2026-09-03`（30 日滚动区间） |
| 总览 | `14 天`、`780 分钟`、`92 组` |
| 类型分解 | 球台训练 `12 天 · 640 分钟`；屏内练习 `6 天 · 140 分钟`（4 天重合，故总训练天数为 14） |
| 时长趋势 | 30 根细日柱，x 轴仅标 `08-05 / 08-12 / 08-19 / 08-26 / 09-03`；较上月 `+96 分钟 / +14%` |
| 基础 | `126/170 球`、`74%`、`18 组`、`+2%` |
| 准度 | `138/200 球`、`69%`、`22 组`、`+3%` |
| 走位 | `81/150 次`、`54%`、`19 组`、`-1%` |
| 控力 | `73/130 次`、`56%`、`17 组`、`+2%` |
| 屏内角度 | `320 题`、平均误差 `2.6°`、最佳误差 `0.4°`、正确率 `76%` |

#### 年态扩展 fixture

| 区域 | 样例数据 |
|---|---|
| 时间 | 年；`2025-09-04 ~ 2026-09-03`（365 日滚动区间） |
| 总览 | `126 天`、`6840 分钟`（UI 可同时辅助显示 `114 小时`）、`812 组` |
| 类型分解 | 球台训练 `108 天 · 5650 分钟`；屏内练习 `54 天 · 1190 分钟`（36 天重合，故总训练天数为 126） |
| 时长趋势 | 9 月至次年 8 月共 12 柱：`410, 460, 380, 520, 560, 610, 590, 650, 670, 720, 610, 660`，合计 6840；较上年 `+920 分钟 / +16%` |
| 基础 | `1120/1450 球`、`77%`、`150 组`、`+5%` |
| 准度 | `1260/1760 球`、`72%`、`184 组`、`+4%` |
| 走位 | `760/1320 次`、`58%`、`162 组`、`+3%` |
| 控力 | `650/1100 次`、`59%`、`145 组`、`+4%` |
| 屏内角度 | `2840 题`、平均误差 `2.1°`、最佳误差 `0.2°`、正确率 `81%` |

### 4.2 三个候选方向

| 候选 | 首要观感 | 结构重点 | 取舍 |
|---|---|---|---|
| A · 总览优先（推荐） | 3 秒内看到训练量 | 顶部三指标，下接周时长图，再分球台 / 屏内明细 | 最贴合“统计首先是数据仪表盘”，但底部明细仍需滚动 |
| B · 趋势优先 | 训练节奏最醒目 | 时间分布图占首屏主位，总指标变为紧凑数字带 | 更像数据产品，但低频用户的图表可能空洞 |
| C · 训练域分区 | 球台与屏内差异最清楚 | 总览之后并列两个数据域，各自拥有指标与图表 | 语义最严谨，但可能形成“两张报告拼接”的长页面 |

### 4.3 生成方式与负向约束

- 路径：Codex 内置 `image_gen`；不传递或伪造模型名、quality 与精确尺寸参数。
- 生成策略：A/B/C 三个独立 prompt、每方向 1 张，要求竖版长页高保真 iOS UI。
- 输出目录：`output/imagegen/statistics-v55-built-in/`。
- 建议文件名：`01-overview-first.png`、`02-trend-first.png`、`03-domain-split.png`。
- 画布：单独的 iOS 页面，不画手机外壳，不放广告海报式背景。
- 风格：现实、可实施的产品 UI；使用项目绿、中性灰白底、少量琥珀图表色；不使用 3D 球桌、拟物球、氛围插画或装饰性轨迹。
- 文字：尽量按 fixture 原样呈现，但候选评审不以生成图文字绝对准确为通过条件。
- 负向约束：不要教练、人物、对话气泡、AI 标识、弱项结论、强项结论、训练建议、下一步行动、推荐卡、总评分、全局准确率、工具使用数据、水印。

### 4.4 用户选择门槛 G1

**已通过（2026-09-03）。** 用户在补齐周/月/年态后接受建议，裁定为：

- 主骨架选 C：球台训练与屏内练习两个独立数据域。
- 首屏吸收 A：总览数字优先，并保留更充足的留白。
- 页顶周/月/年是全页唯一时间维度，不保留屏内练习的独立时间分段控件。
- 生成图仍仅是布局证据；不新增 schema，不把图中虚构趋势当真实数据。

---

## 五、执行批次、依赖与完成标准

### 5.1 批次总表

| 批次 | 范围 | 量级 | 依赖 | 主要产物 |
|---|---|---:|---|---|
| W0 | 确定性 fixture、内置文生图三方向、对比画廊 | 中 | 无 | ✅ 3 PNG + prompt/manifest + 联系表 |
| W0.1 | A/B/C 公平补齐月态与年态 | 中 | W0 | ✅ 6 PNG + 2 联系表 + manifest |
| G1 | 用户选择视觉方向 | 人工 | W0.1 | ✅ C 双域 + A 首屏 |
| W1 | 统一时间契约、确定性 fixture 与数据结构 | 中 | G1 | 可测区间生成器 + 联合行模型 + UI fixture |
| W2 | A 式总览/趋势 + C 式球台训练域 | 中 | W1 | 主统计 SwiftUI + ViewModel 回归 |
| W3 | C 式屏内练习域与全页时间联动 | 中 | W1、W2 | 角度数据域 + 题型/袋口筛选 + 错误/空态 |
| W4 | 状态、权益、响应式和可访问性 | 中 | W2、W3 | 全状态收口 + 设备/AX 截图 |
| W5 | 回归、证据、规范与进度收口 | 中 | W4 | 定向门禁 + UI 审查报告 + 文档回写 |

### 5.2 依赖图

```text
W0 → W0.1 → G1（已通过） → W1 → W2 → W3 → W4 → W5
```

W2 与 W3 的聚合可分开理解，但都会触及 `StatisticsView`、共用时间状态和截图 fixture，强行并行容易合并出错口径。因此按单会话粒度串行，每批都可独立回归。

### W0 — 内置文生图候选与可比较证据

**范围**

1. 用 §4.1 同一 fixture 通过内置 `image_gen` 生成 A/B/C 三张竖版数据页。
2. 每个方向使用独立 prompt，但共用数据、色板、禁止项和页面边界。
3. 将最终 PNG 保存到项目工作区，并保存执行方式、完整 prompt 和生成时间；明示底层模型未由工具暴露。
4. 生成一张等宽联系表，同屏比较首屏占比、主次关系和滚动长度。

**完成标准**

- 3/3 PNG 存在且可解码，尺寸一致，画面无手机外壳、水印、教练卡或建议卡。
- manifest 中明确记录 `execution_mode=built-in image_gen`、且“底层模型未暴露”；不声称为 GPT-Image-2 产物。
- 三图的信息字段与样例数据范围一致；生成文字错误单独记录，不在选布局时当成真实产品缺陷。
- 产物仅供 G1 选方向，未改 SwiftUI。

**W0 执行证据（2026-09-03）**

- `A-overview-first.png`、`B-trend-first.png`、`C-domain-split.png` 3/3 完成，均为 `864 × 1821` RGB PNG，可解码。
- 联系表 `contact-sheet.png` 与完整 prompt / fixture / SHA-256 记录已保存到 `output/imagegen/statistics-v55-built-in/`。
- 执行方式为 `built-in image_gen`；工具未暴露底层模型，未声称为 GPT-Image-2 产物。
- 可见内容审查通过：无教练结论、训练建议、强弱项、总评分、全局准确率、工具数据、手机外壳或水印。
- 共用 fixture 锁定值一致；生成器对未锁定的小趋势点和区间比例有装饰性补全，已在 manifest 明示不可当作产品真值。
- 本批未改 `StatisticsView.swift`、ViewModel、数据模型、测试或设计规范；现停在 G1。

> 2026-09-03 用户指出仅有周态不足以选型，因此撤回“G1 已就绪”判断；上述周态证据保留，继续执行 W0.1。

### W0.1 — 月/年时间密度扩展

**范围**

1. 分别以 A/B/C 周态原图为布局与视觉参考，各生成月态、年态 1 张，共 6 张。
2. 月态保留 30 日密度但稀疏显示 x 轴标签；年态用 12 月聚合，不绘制 365 根日柱。
3. 三方向均须显示该时间态的确切区间、总览、中性同期差值、球台分类和独立屏内角度数据。
4. 产出月态、年态联系表，追加 prompt、文件哈希与生成偏差。

**完成标准**

- 6/6 PNG 存在且可解码，A/B/C 在周/月/年三态中保持各自的主层级。
- 月态能看出 30 日数据密度，年态能看出 12 月聚合密度，坐标不以全量标签造成人为拥挤。
- 9 张候选中均无教练结论、建议、强弱项、全局准确率或工具数据。
- 未改 SwiftUI；仅在 W0.1 通过后重新开放 G1。

**W0.1 执行证据（2026-09-03）**

- A/B/C 月态 + 年态 6/6 已产出，均为 `864 × 1821` RGB PNG，可解码。
- `contact-sheet-month.png` 与 `contact-sheet-year.png` 已生成；完整 fixture、prompt 模式、SHA-256 和生成偏差已追加到 manifest。
- 月态三图均呈现 30 根日柱与 5 个稀疏日期标签；年态三图均呈现 12 个月份柱。
- 锁定的总览、域分解、分类和屏内角度数值与对应 fixture 一致；未锁定的误差趋势点/区间分布仍属装饰性生成值，不是产品真值。
- 9/9 可见候选无教练结论、训练建议、强弱项、全局准确率、排名或工具数据。
- 未改 SwiftUI、ViewModel、数据模型、测试或设计规范；W0.1 通过，重新开放 G1。

### W1 — 统一时间契约、行模型与确定性 fixture

**依赖**：G1 已通过。

**主要文件**：`StatisticsViewModel.swift`、`AngleHistoryViewModel.swift`、`LocalAngleTestRepository.swift`；拟新建 `QiuJi/App/V55StatisticsFixtureSeeder.swift` 与 `QiuJiTests/AngleHistoryViewModelTests.swift`；窄改 `StatisticsViewModelTests.swift`。

**范围**

1. 从单一真源生成 `current` / `previous` 两个左闭右开区间，注入固定 `now` 与 `Calendar`，让过滤、日期文案、柱图分箱和同期比共用它。
2. 月态改为覆盖完整 rolling 区间的日分箱；年态改为覆盖完整 rolling 区间的日历月分箱，不丢首尾部分月。
3. 把当期分类比率、按 `unitLabel` 分开的原始样本、组数与上期百分点差合并为一个行模型，使用 `DrillCategory.allCases` 稳定排序。
4. `AngleHistoryViewModel` 接受页面传入的 `DateInterval`，并预留 repository 注入、`isLoading` / `errorMessage` 以便 W3 做诚实状态；`fetchInRange` 统一为左闭右开。
5. 在内存 `ModelContainer` 中新增 v55 专用 seed，以明确启动参数触发；同一 fixture 包含 drill、cognitive、tool、同日重叠、当期/上期、混合单位、空分类和月/年边界。
6. 将“用户历史上是否训练过”与“当前区间是否有训练数据”拆开；页面状态只依据当前 `DateInterval` 判断区间空态。

**完成标准**

- 固定时钟下，周/月/年的起止时刻、文案、分箱、总和、当期/上期不重叠均有单元测试。
- 月态旧断言 `durationBarData.count == 4` 会因“4 个近似周箱 → 全区间日箱”失效；替换为“覆盖起止日 + 合计等于区间总时长”，不是删断言求绿。
- 年态旧断言 `durationBarData.count == 12` 会在 rolling 区间跨 13 个日历月时失效；替换为“所有相交月箱连续、无遗漏、总和守恒”。
- 旧“按成功率降序”断言会因分类行改为业务稳定顺序而失效；替换为 `DrillCategory.allCases` 相对顺序断言。
- 新 seed 只在 `-v50.inMemoryStore -v55.statisticsFixture=<state>` 这类 DEBUG 参数下工作，生产启动不受影响；不新增 schema。
- 定向验证：`make -f scripts/Makefile test ONLY_TESTING=QiuJiTests/StatisticsViewModelTests` 与 `make -f scripts/Makefile test ONLY_TESTING=QiuJiTests/AngleHistoryViewModelTests`。

### W2 — 总览/时长与球台训练域

**依赖**：W1 区间与联合行模型通过定向测试。

**主要文件**：`StatisticsView.swift`、`StatisticsViewModel.swift`、`StatisticsViewModelTests.swift`、`V29W6HistoryStatisticsKindTests.swift`。

**范围**

1. 按 §3.5 构建首张强调卡：总时长是最强数字，训练天数与球台组数次之，时长差值和趋势在同一视觉语境。
2. 删除可见 `tool` 行和摘要卡装饰工程线；保留底层 tool 会话、同步和既有排除回归。
3. 以“球台训练”为单一分区，区头只显示球台天数/时长；区内按稳定主分类顺序渲染联合行。
4. 联合行展示“分类名、`made/target + unit`、分类成功率、组数、较上期百分点差”；不显示排名、进步/退步结论或可点击的训练推荐。
5. 保留主分类唯一归属；不读 `secondaryCategories`，不伪合并混合单位。图表轴标签由分箱策略提供，不在 View 中按索引猜测隐藏。
6. 从 UI 移除语义不成立的 `averageDurationHoursPerPeriod` / `periodLabel`；确认无其他消费者后再删对应私有展示属性，保留总时长和完整趋势。

**完成标准**

- `StatisticsViewModelTests` 与 `V29W6HistoryStatisticsKindTests` 定向回归通过；同日 drill + cognitive 仍只计 1 个总训练日。
- tool-only 数据不改变总天数、总时长、组数、图表和分类行，且页面可访问性树中不出现“工具使用”。
- 分类样例的比率可从 `made / target` 逐项手算；混合单位不产生伪造合并比率。
- 原 `StatisticsCategoryRatesCard` 取证断言不得因组件被替换而裸删：将相同手算值和空态证据迁到新生产联合行/分区。

### W3 — 屏内练习域与全页时间联动

**依赖**：W1 区间契约稳定，W2 已固定页面根布局。

**主要文件**：`StatisticsView.swift`、`AngleHistorySection.swift`、`AngleHistoryViewModel.swift`、`QiuJiTests/AngleHistoryViewModelTests.swift`。

**范围**

1. 移除 `AngleHistorySection` 内的 `AngleTimeRange` 分段控件，以页顶当期 `DateInterval` 过滤 `AngleTestResult`；周/月/年切换时两个训练域同步更新。
2. 保留 `AngleQuizTypeFilter` 和趋势袋口筛选，筛选顺序固定为“页级时间 → 本地题型 → 本地袋口”。
3. 区头显示屏内训练天数与时长；指标保留总测试次数、平均误差、最佳误差和正确率。
4. 趋势和角度区间分析只使用实际 `AngleTestResult`。默认保留现有“每 5 题”分组，仅补清晰的辅助功能文案；若实施中发现横轴仍无法被用户理解，停止并将“分组口径”单独裁定，不从生成图猜数据。
5. repository 失败显示错误和重试；成功返回空数组才显示屏内练习空态。角度正确率只来自屏内结果，不与球台分类比率混合。

**完成标准**

- 切换周/月/年时，主总览、球台行、屏内指标和页面日期同步切换；页面不存在第二组时间分段控件。
- 题型/袋口切换只改屏内数据，不改球台总览和页级区间。
- 人为注入 repository 失败时显示错误 + 重试，不显示“还没有训练记录”；重试成功后恢复数据。
- 不改 `AngleTestResult` schema、四个写入点、owner 或同步逻辑；在 W5 更新数据契约中“统计子页仅读 TrainingSession”的已漂移描述。
- 屏内“天数/时长”只来自 `kind=cognitive` 的 `TrainingSession`，题数/误差/正确率来自 `AngleTestResult`；不用答题时间戳反推虚构时长。
- 页面中没有由误差高低推导出的教练性文案。

### W4 — 状态、权益与响应式

**依赖**：W2/W3 两个生产数据域完成。

**主要文件**：`StatisticsView.swift`、`AngleHistorySection.swift`、`QiuJi/App/V55StatisticsFixtureSeeder.swift`、`QiuJiUITests/V55StatisticsUITests.swift`。

**范围**

1. 分别验证：首次 loading、主数据失败、主数据空/角度有数据、主数据有/角度空、两者均有、两者均空、Free 锁层。
2. 验证 Light / Dark，iPhone SE 紧凑屏、主流 iPhone 和 iPad 竖屏。
3. 为 AX Large 重排图表轴标签和指标网格；不通过隐藏核心数据或硬编码缩小字号取巧。
4. 保留现权益边界：Free 用户可操作页顶时间，总览 + 球台域继续由 `BTPremiumLock(.fullMask)` 保护，屏内练习仍在锁层外；不扩大或缩小锁层。
5. 屏内有数据但无 drill/cognitive `TrainingSession` 时，不允许顶层“还没有训练数据”提前截断整页；各域按自身数据状态独立显示。

**完成标准**

- 全部状态都有 `-v55.statisticsFixture=<both|tableOnly|screenOnly|empty|mainError|screenError>` 等稳定 fixture 或等价明确启动参数，不依赖模拟器残留数据。
- 图表在 AX Large 下坐标、图例、数值和卡片无重叠、截断或不可达。
- VoiceOver 能以“指标名 → 数值 → 单位 / 时间”顺序读出图表核心信息，不依赖颜色表达涨跌。
- Free / Pro 交互、解锁入口与原有路由不变。

### W5 — 回归、截图证据与文档回写

**依赖**：W4 状态/权益/响应式门禁通过。

**主要文件**：新建 `QiuJiUITests/V55StatisticsUITests.swift`，窄改 `ScreenshotTourUITests.swift`，新建 UI 审查报告，回写 UI 规范、数据契约和进度。

**范围**

1. 新建 `QiuJiUITests/V55StatisticsUITests.swift`，把 v55 确定数据与状态证据从现有巨型、且已有并行改动的 `ScreenshotTourUITests.swift` 中分离；旧 v47 流只做标签/入口兼容性更新。
2. 执行统计 ViewModel、kind 口径、角度统计、历史根页保活与 Pro 锁层定向回归。
3. 用同一 fixture 完成主流 iPhone Light/Dark 的周/月/年首屏+底部，iPhone SE，iPad，AX Large，Free，空态，主错误和屏内错误截图。
4. 交互证据覆盖：历史/统计切换后状态保活；周/月/年同时改变两域；题型/袋口只改屏内数据；Free 解锁路由不变。
5. 实施前盘点默认测试集中所有写盘用例：`rg -l '\.write\(|FileManager.*create' QiuJiTests QiuJiUITests`，并检查 scheme/testplan。v55 截图统一写入已忽略的 `build/v55-statistics/`；不用“全测后零 diff”作未盘点的虚假门禁。
6. 产出 `tasks/ui-reviews/UR-20260903-v55-statistics.md`，并实际更新 `tasks/UI-IMPLEMENTATION-SPEC.md` Changelog、`.kiro/steering/content-data-contract.md` 和 `tasks/PROGRESS.md`。

**完成标准**

- `make -f scripts/Makefile test ONLY_TESTING=QiuJiTests/StatisticsViewModelTests`、`QiuJiTests/AngleHistoryViewModelTests`、`QiuJiTests/V29W6HistoryStatisticsKindTests` 与 `QiuJiUITests/V55StatisticsUITests` 真实通过。
- `make -f scripts/Makefile build` 与 `git diff --check` 通过；若被并行改动阻塞，必须给出具体非本批错误和定向证据，不宣称全量通过。
- 截图中的数值可与 fixture / ViewModel 断言逐项对齐，不凭目测判定“大概正确”。
- 审查报告明确区分：生成图方向证据、SwiftUI 实现截图、自动测试、手动/系统验收。
- 用户最终视觉确认依然是独立门槛；自动化全绿不等于用户已认可视觉。

---

## 六、验收矩阵

| 验收面 | 必须证明 | 不能代替它的证据 |
|---|---|---|
| 语义 | drill/cognitive/tool 边界、同日去重、分类内比率 | 一张好看的截图 |
| 视觉方向 | G1 已选 C 双域 + A 首屏 | 生成图的字段或像素细节 |
| 真实实现 | SwiftUI 中的文字、数据、图表和交互 | 生成图 |
| 数据状态 | 有数据/无数据/查询失败可区分 | 单一 happy path |
| 响应式 | SE / 主流 iPhone / iPad、Light/Dark、AX Large | 仅一台 Light 模拟器 |
| 权益 | Free 锁层和 Pro 内容范围不漂移 | `-forcePremium` 下的截图 |
| 用户裁定 | 用户明确选定方向并确认最终 SwiftUI | 代理或测试的“推荐通过” |

### 6.1 状态与权益矩阵

| 主数据 | 屏内数据 | 权益 | 期望页面 |
|---|---|---|---|
| 成功有数据 | 成功有数据 | Pro | 总览、趋势、球台域、屏内域均显示 |
| 成功有数据 | 成功空 | Pro | 上半部完整，屏内域显示局部空态 |
| 成功空 | 成功有数据 | Pro | 球台域局部空态，屏内域可用，不整页截断 |
| 成功空 | 成功空 | Pro | 两域空态，仅提供去记录/开始训练的导航 CTA，不推荐具体练习 |
| 查询失败 | 任意 | Pro | 主错误明示 + 重试；已能独立加载的屏内区不伪装成主数据 |
| 成功 | 查询失败 | Pro | 主区正常，屏内域显示局部错误 + 重试 |
| 任意 | 任意 | Free | 页级时间可用；总览+球台域维持原 Pro 锁；屏内域维持原可见性 |

### 6.2 并行改动冲突台账

| 文件 | 2026-09-03 现状 | v55 策略 |
|---|---|---|
| `StatisticsView.swift` | 定向基线干净 | W2/W3 主改；每批开始前复核 diff |
| `StatisticsViewModel.swift` | 定向基线干净 | W1/W2 主改；局限在区间与聚合层 |
| `AngleHistorySection.swift` / `AngleHistoryViewModel.swift` | 定向基线干净 | W1/W3 主改；不触及练习写入页 |
| `LocalAngleTestRepository.swift` | 已有 owner 隔离/会话归属并行改动 | 只将 `fetchInRange` 上界由 `<=` 改为 `<`，先保留并复核 owner predicate |
| `StatisticsViewModelTests.swift` | 已有“我的/本月概览”并行改动 | 仅在统计页测试分区窄改，保留月概览断言 |
| `V29W6HistoryStatisticsKindTests.swift` | 已有数据关系并行修复 | 只迁移统计卡取证，不覆盖其他回归 |
| `ScreenshotTourUITests.swift` | 已有 v50/v51 并行改动 | 不承载新 v55 主证据；只做旧标签兼容 |
| `QiuJiApp.swift` / `tasks/UI-IMPLEMENTATION-SPEC.md` / `content-data-contract.md` | 均有其他版本改动 | 通过独立 seed 类和窄 Changelog/契约补丁降低冲突；不整段重写 |

---

## 七、代码与证据锚点索引（2026-09-03 实读）

| 范围 | 已核实锚点 | 用途 |
|---|---|---|
| 页面分支 | `QiuJi/Features/History/Views/StatisticsView.swift:15` | loading / 错误 / 无主训练 / Free / Pro 入口 |
| 主内容顺序 | `QiuJi/Features/History/Views/StatisticsView.swift:116` | 区间、概况、时长、分类、角度区的顺序真源 |
| 类型分解 | `QiuJi/Features/History/Views/StatisticsView.swift:226` | drill/cognitive/tool 当前可见文案 |
| 时长图 | `QiuJi/Features/History/Views/StatisticsView.swift:264` | 平均、环比、柱图与图例 |
| 分类成绩 | `QiuJi/Features/History/Views/StatisticsView.swift:339` | 分类成功率卡和分类对比分离现状 |
| 数据区间 | `QiuJi/Features/History/ViewModels/StatisticsViewModel.swift:67` | 滚动周/月/年边界 |
| kind 分流 | `QiuJi/Features/History/ViewModels/StatisticsViewModel.swift:89` | 训练量、球台成绩、屏内成绩、tool 边界 |
| 总览聚合 | `QiuJi/Features/History/ViewModels/StatisticsViewModel.swift:156` | 天数去重、分 kind 天数/时长、总时长、组数 |
| 分类口径 | `QiuJi/Features/History/ViewModels/StatisticsViewModel.swift:231` | 禁止全局准确率，按主分类聚合 |
| 趋势分箱 | `QiuJi/Features/History/ViewModels/StatisticsViewModel.swift:239` | 周 7 日、月 4 周、年 12 月的现状实现 |
| 角度过滤 | `QiuJi/Features/AngleTraining/ViewModels/AngleHistoryViewModel.swift:4` | 角度时间 / 类型筛选与指标 |
| 角度加载 | `QiuJi/Features/AngleTraining/ViewModels/AngleHistoryViewModel.swift:92` | 当前静默错误边界 |
| 角度视图 | `QiuJi/Features/AngleTraining/Views/AngleHistorySection.swift:13` | 类型、指标、时间、趋势、区间分析 |
| 角度仓储协议 | `QiuJi/Data/Repositories/Protocols/AngleTestRepositoryProtocol.swift:4` | 既有 `fetchInRange(from:to:)` 接口 |
| 角度仓储实现 | `QiuJi/Data/Repositories/Local/LocalAngleTestRepository.swift:56` | 现上界为 `<= to`，W1 改为左闭右开 |
| 根页保活 | `QiuJi/Features/History/Views/HistoryCalendarView.swift:26` | 历史/统计两 pane 与导航边界 |
| 训练口径真源 | `.kiro/steering/content-data-contract.md:285` | 计数原语、kind、按分类聚合裁定 |
| 统计读取文档漂移 | `.kiro/steering/content-data-contract.md:139` | 文档称统计仅读 TrainingSession，但当前页面已嵌入 AngleHistorySection |
| kind 常量 | `QiuJi/Data/Models/TrainingSessionKind.swift:7` | `countsTowardGoal` / `countsTowardAccuracy` |
| 内存容器启动参数 | `QiuJi/App/QiuJiApp.swift:16` | 现有 `-v50.inMemoryStore` 是 v55 确定性 seed 入口 |
| 定向逻辑测试 | `QiuJiTests/StatisticsViewModelTests.swift` | 区间、时长、天数、分类指标回归 |
| kind 回归 | `QiuJiTests/V29W6HistoryStatisticsKindTests.swift:150` | 三种 kind 手算与 tool 不影响聚合 |
| 现有统计截图流 | `QiuJiUITests/ScreenshotTourUITests.swift:537` | Light/Dark/AX/Free/紧凑/交互证据入口 |
| v47 审查记录 | `tasks/ui-reviews/UR-20260830-v47-statistics-sample.md` | 现有通过面与 AX Large 未闭合证据 |
| 实现规范 | `tasks/UI-IMPLEMENTATION-SPEC.md:779` | 历史/统计色彩与图表旧决策；G1 后若覆盖须写 Changelog |

---

## 八、排期前置与停止条件

1. **开工条件**：G1 已通过，W1 可开始；实施前先复核 §6.2 冲突台账和定向 diff。
2. **数据扩张停止条件**：如实施需要现有模型没有的指标（例如自动弱项、击球轨迹评分），停止并另起数据方案，不用 UI 伪数据补齐。
3. **口径冲突停止条件**：如新布局要求全局准确率、tool 进训练量、副分类重复计数，或试图将屏内与球台成绩合并，视为与数据真源冲突，不执行。
4. **权益停止条件**：如实施需把屏内练习收入 Pro 锁层，或把球台统计移出锁层，必须先取得新商业裁定。
5. **并行工作区停止条件**：执行前若定向基线文件出现本方案未盘点的新改动，先停止并重建基线，不覆盖、不回退。

---

## 九、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v55.6 | 2026-09-03 | G1 通过：选定 C 双训练域 + A 顶部总览/留白。将页级周/月/年统一为两域唯一时间口径，补齐 rolling 区间、分箱、联合分类行、状态/权益矩阵、断言失效机理、写盘测试盘点和冲突台账；拆为 W1–W5 五个可独立验收批次。本版仅规划，未改生产代码。 |
| v55.5 | 2026-09-03 | W0.1 完成：A/B/C 各补月态和年态，6 PNG、2 联系表、fixture / prompt / 哈希证据齐备；30 日与 12 月密度、锁定数值及禁止项审查通过。未改 SwiftUI，重新开放 G1。 |
| v55.4 | 2026-09-03 | 用户指出只有周态难以选方向；撤回 G1 就绪判断，新增 W0.1，要求 A/B/C 各补月态和年态，锁定 30 日 / 365 日 fixture、图表密度与禁止项；未改 SwiftUI。 |
| v55.3 | 2026-09-03 | W0 完成：内置 `image_gen` 生成 A/B/C 三个同数据方向，保存 3 PNG、联系表、prompt / fixture / 哈希 manifest；可见禁止项审查通过。未改 SwiftUI，停在 G1 等用户选 A/B/C 或组合。 |
| v55.2 | 2026-09-03 | 用户明确改为直接使用 Codex 内置文生图。W0 移除 API key / Python SDK 前置，不再声称或验收 GPT-Image-2 模型身份；三方向、共用 fixture、负向约束和 G1 人工门槛不变。 |
| v55.1 | 2026-09-03 | 用户批准开始执行。W0 前置复核仍为 `OPENAI_API_KEY` 未注入 Codex 进程、Python `openai` 包未安装；为保证显式 `gpt-image-2` 证据，本次未调用内置 ImageGen，W0 保持未开始。 |
| v55.0 | 2026-09-03 | 初版。回显“统计页只展示用户训练数据，不要教练结论和训练建议”；锁定 drill/cognitive/tool 边界与禁止全局准确率；定义 GPT-Image-2 三候选、确定性 fixture、G1 用户选择门槛及 W0–W5 执行批次。 |
