1. 练习页里各卡片里的标题字体大小不一致，应该保持和训练计划和动作中每个动作卡片页中标题的字体大小一致；
2. 角度与打点：
  1. 标题修改为角度与瞄准吧，这里面也没有详细的去讲角度和打点的关系；
3. 瞄准点对照表：
  1. 瞄准点与接触点交互卡片：
    1. 红色瞄准点文字放到左侧，这样就不会遮挡1号球；
    2. 接触点文字放到右侧，这样就更能接近接触点；
    3. 瞄准点和接触点的文字在拖动下面角度调过程中，不要遮挡1号球和假想球；
4. 角度预测：
  1. 预测页面中的整体（白球，基准线等）可以下移些，现在还是会出现90度时，1号球近半颗球不可见，最好是做到上下空白区域大小相同；
5. 3D角度训练：
  1. 现在的摄像机系统可以上下滑动，现在每一题开始时默认的是摄像头处于最低点，修改为默认开始时是现在的最高点
6. 瞄准点训练：
  1. 白色瞄准线，红色ground truth的线宽调小一点，红色瞄准点的半径也调小一点；
7. 2D瞄准点训练：
  1. 现在目标球周围好像有个假想球的半径，当瞄准线进入这个半径范围，行为就和预期不同了，预期如下：
    1. 只要瞄准线没有和目标球接触，就应该直接延伸到库边，而且如果瞄准线和穿过目标球的虚线的交点距离目标球小于目标球半径（相当于进入半径范围），则应该显示瞄准线和虚线的交点应该作为红色瞄准点；
    2. 如果可以和目标球接触则瞄准线和目标球接触的点应该增加一个红点；
  2. 球桌大小，球库，瞄准条大小位置都复用其他页面的吧，提交按钮的位置也按照按钮位置的约定吧
  3. 3秒后开始验证时间有点久，修改为1.5s；
  4. 验证过程中的球杆逻辑加上。
8. 瞄准线逻辑：
  1. 现在瞄准线的逻辑是，点到哪里，会先移动哪里，这种交互逻辑是不对的；
  2. 当处于自由瞄准或者用户可以通过上下左右挪动进行瞄准时，用户点的第一个点，应该是选中了瞄准线，左右滑动时，是对当前瞄准方向的调整，而不是直接移动到当前点到的瞄准方向；
  3. 此外，对于移动瞄准顺带有需要求解的情况，不应该是实时的求解，比如拖动过程中就是开始求解，这样一方面性能消耗高，同时用户拖动会有卡顿感，正确的做法是用户拖动过程中，不求解，当用户拖动停下来，比如0.5s，再开始求解，这样更顺畅，对于需要求解的情况，瞄准刻度条应该也是这种逻辑。（这一条至关重要，确保所有相关的场景都符合要求）
9. 3D瞄准点训练：
  1. 现在滑动屏幕是调整瞄准点，修改为滑动屏幕是修改摄像头高度和视角，参考3D角度训练，而且同样的将默认的摄像头高度设置为最高。
10. 所有的球停止不要进行最后尾速的截断，让它自己停止，现在看起来会有最后阶段的位置变动，不真实（重要，应用到所有的场景中，包括渲染管线中）；
11.  将所有场景中的打点卡片中的白色打点盘的半径增大，但是卡片大小不变；
12. 所有场景中的上一杆，需要回到上一杆完整的数据，包括球型，目标袋口，瞄准方向，打点等等（重要）
13. 开球：（通用，重要）
  1. 之前设计过开球的随机性，我记得是包括球堆的球与球之间的距离和目标球的瞄准点；
  2. 由于现在开球支持了移动目标球，所以也把瞄准方向进行开放吧，相当于拖动屏幕可以修改瞄准方向，左侧也增加瞄准条；
  3. 相当于系统的随机性只保留球堆的球与球之间的距离；
  4. 开球中的力度条也显示出来，默认速度用6m/s；
  5. 力度条和瞄准条的标准规范用之前定义的；
  6. 现在的取消，完成，重开按钮是正确的，但是把重开和完成的位置进行互换；
14. 思路训练：
  1. 开球使用通用的开球规范，现在不符合要求。
15. 打一走二想三
  1. 现在会自动生成扇形区域，可以默认选区是这个扇形区域，如果说用户重新指定了新的落区，落点，过点，则将这个默认的扇形区域修改为灰色，使用用户选定的；如果说用户将自己选的给删除后，则恢复这个扇形区域作为选区，此外，在求解过程中，这个扇形区域需要重新处理，以方便进行求解（直接外接矩形的形式不合适，这种做法误差太大了）；
  2. 当台面少于三颗球时，也要支持求解和击打，直至桌面上没有球；
  3. 现在的清除键（清除落区，落点，过点的那个键）太小了，把它调成正常的尺寸，而且放到摆球的右侧；（其他场景的求解中的清除按钮也使用相同的逻辑）；
  4.  开球使用通用的开球规范，现在不符合要求；
16. 做斯诺克：
  1. 名称修改为防守；
  2. 现在的逻辑有点问题，只有目标球和障碍球；
  3. 合理的逻辑：
    1. 符合中八的规则；
    2. 目的是通过击打己方的球，让对方看不到自己的球，举个例子：目标球是4号，通过击打4号，让对方看不到自己的花色球（9-15），如果对方该打8号球了，那目的是让对方看不到八号球；
    3. 相当于只通过目标球，就可以推断出，怎么去防守；
    4. 此外符合中八的规则，还有一个点是如果球桌上同时有全色和花色球，就不能将八号球当作目标球；
    5. 当然更广泛的防守是让对方进球难度很高，刚才的看不到球属于很难的了，最好还有那种长台，大角度等等比较难的，作为可行解。
17. 翻袋解球器：
  1. 解的描述信息放到翻袋解球器下面，和其他页面的一致；
  2. 标题的字号，颜色和其他页面的不一致；
  3. 不需要展示演示中，中断，重置；
  4. 将下一解放到左侧；
  5. 右侧保留现有的击打，上一杆，回放就可以；
  6. 力度条和瞄准条上下高度一致；
  7. 没有解的说明也是放到翻袋解球器标题下面；
  8. 现在球库里没有白球和黑球，参考其他页面的球库标准；
  9. 右上角的i按钮修改为设置，在里面增加一个显示网格的选项，参考其他页面的；
18. 反射解球器：
  1. 问题和翻袋解球器的基本相同。
19. 动作库：
  1. 侧边栏不应该具有记忆功能，每一次点击，都应该显示最上面，比如不论之前点击里哪一组，现在重新点击这一组，都会显示这一组的最上面
  2. 上手试打：
    1. 选择球形的球数量中不要算上白球；
    2. 标题没有居中；
    3. 右上角的i的内容合并到三个点（设置）中；
    4. 现在有进袋和自由两种模式，增加一种序列模式，序列模式是指按照序列中的解进行呈现击打信息，此时，将打点盘，力度条，瞄准条都隐藏，相当于用户通过点击击打走完整个序列；将默认的模式修改为序列模式；
    5. 在进袋模式下，默认使用
20. 将所有相关页面中的三个点设置和齿轮设置，统一为三个点设置；































































































