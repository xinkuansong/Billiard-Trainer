# 练习 Tab 问题集合 v3（可多会话执行版）

> 来源：`问题集合_v2.md`（2026-07-08）。本文件把 v2 中标注「全局通用」的条目抽取为 **G 系列全局规范**，页面条目只保留页面特有项并引用 G 规范；再按依赖关系切成 **S1–S7 会话批次**，每批次可在一次独立会话内完成并验收。
> 本文件是本轮改造的需求真源：与 `tasks/UI-IMPLEMENTATION-SPEC.md` 等既有规范冲突时以本文件为准，落地后回写规范。
> 翻袋解球器 / 反射解球器本轮不动。

---

## 一、G 系列全局规范（Global Rules）

### G1 瞄准点定义修正（概念级，影响文档 + 训练逻辑）

瞄准点是**瞄准线与「过目标球中心且垂直于瞄准线的直线」的交点**：

- 若瞄准线穿过目标球，瞄准点即瞄准线与目标球的接触点；
- 若瞄准线从目标球侧面经过，瞄准点是瞄准线与该垂线的交点（在球外）；
- 即瞄准点是**参考目标球**定义的，不等于假想球心的旧口径。

需同步修正：瞄准原理文档、瞄准点对照表、瞄准点训练（7）、2D/3D 瞄准点训练中所有涉及瞄准点的计算与标注。

### G2 台面网格颜色

4×8 台面网格换成**亮度更高的灰白色**（当前为 `white @ 0.28 alpha`，定义在 `AngleTrainingScene.setTableGridVisible`，改一处全局生效）。

### G3 轨迹三档切换按键位置

轨迹切换按键（`BTTrajectoryDetailChip`）不得遮挡球桌：放到球桌上方空隙带内，**下沿紧贴球桌上沿**，横向**靠屏幕最右侧**。（2026-07-08 S1 执行中用户修订：由"右侧与球桌右缘对齐"改为"靠屏幕最右"。）

### G4 侧栏控件贴边不重合

- 瞄准刻度条（`BTAimWheel`）右侧**紧贴球桌左侧**，与球桌不重合；
- 力度条 + 打点（`BTShotInstrumentColumn`）左侧**紧贴球桌右侧**，与球桌不重合。

### G5 侧栏控件底部对齐

瞄准刻度条和力度条**高度相同**，且**距球桌底部的高度相同**（两侧对称）。

### G6 按键统一三原则

1. 按键不遮挡球桌；
2. 按键需紧贴球桌；
3. 按键不超出球桌范围（例：开球按键底边应与球桌底边水平线齐平）。

### G7 侧栏控件加长

瞄准刻度条和力度条长度调整为当前的 **1.2 倍**（与 G5/G6 联动校验，不得因加长而超出球桌范围）。（2026-07-08 S1 执行中用户修订：1.5 倍视觉过长，定稿 1.2 倍 = `maxBarLength 264`。）

### G8 球库布局

球库收紧居中、两侧留空白；球库中**所有球排开的总宽度与球桌宽度保持相同**，给左右两侧预留按键空间。

### G9 开球按键样式

开球按键（`BTBreakSideButton`）三角形中增加**三个圆圈**（示意排球）。改组件一处，全局生效。

### G10 球桌尺寸严格锁定

任何操作（进袋/自由切换、开球、显隐底栏等）**都不得改变球桌渲染尺寸**，用户不能感到球桌抖动：

- 点击进袋/自由切换时球桌大小不得变化；
- 点击开球后球桌大小须与常规模式一致；
- 建议做法：SCNView 容器高度不随 overlay/底栏变化，或锁定 `fitRotatedTable` 的输入尺寸。

### G11 按键互不遮挡（重要）

所有按键之间互不遮挡，逐页核验。

### G12 球库瘦身

删除球库区域里的打点盘迷你图与解的文字说明（涉及 `ShotControlBar` 的解摘要行）。

### G 规范适用矩阵

| 页面 | G2 | G3 | G4 | G5 | G6 | G7 | G8 | G9 | G10 | G11 | G12 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 自由击球 FreePlayView | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | — |
| 分离角与走位 ShotSimulationView | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓(禁用态) | ✓ | ✓ | — |
| 自由走位 PositionPlayComposerView | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓(禁用态) | ✓ | ✓ | — |
| 批量出片台 BatchAuthoringView | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓(禁用态) | ✓ | ✓ | — |
| 思路训练 SiluTrainerView | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓(可用) | ✓ | ✓ | ✓ |
| 打一走二想三 PlanThreeView | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓(可用) | ✓ | ✓ | ✓ |
| 做斯诺克 SnookerTacticsView | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓(禁用态) | ✓ | ✓ | ✓ |

> G9 备注（2026-07-08 S2 执行修订）：思路训练/打一走二想三本身内置开球流程（T-P18-47），S2 保留其**可用**开球按钮（三圆圈样式同源 `BreakRackGlyph`），不降级为禁用态；其余无开球能力页维持禁用态。
| 2D/3D 角度训练 SceneAimingView | ✓ | — | ✓(刻度侧) | — | ✓ | — | ✓(装饰球库) | — | ✓ | ✓ | — |
| 2D/3D 瞄准点训练 AimPointSceneTrainingView | ✓ | — | ✓(刻度侧) | — | ✓ | — | — | — | ✓ | ✓ | — |
| 瞄准点训练 AimPointTrainingView | — | — | — | — | ✓ | — | — | — | ✓ | ✓ | — |
| 角度与打点 AngleDynamicView | ✓ | — | — | — | ✓ | — | ✓ | — | ✓ | ✓ | — |
| 角度预测 GeometricAngleQuizView | ✓ | — | — | — | ✓ | — | — | — | ✓ | ✓ | — |

> G1 为概念修正，适用于：瞄准原理文档、瞄准点对照表、瞄准点训练、2D/3D 瞄准点训练。

---

## 二、页面特有条目（Page Items）

### 学习页

**P1 瞄准原理**
- P1.1 母球与目标球距离拉开（当前过近导致文字拥挤、进球线基本不可见）；
- P1.2 按 G1 修正瞄准点定义与配图标注；
- P1.3 新增一句说明：日常玩家从目标球看向袋口方向（进球线）时，看到的目标球上那个点其实是击球过程中正确的「母球与目标球的接触点」，而不是瞄准点，所以瞄准这个点打会偏。

**P2 浅谈球感**
- P2.1 2D→3D 视角差异图中 2D 图占比太小，将 2D 图的长度延伸至屏幕宽度。

**P3 瞄准点对照表**
- P3.1 交互卡片高度加高（当前拖动过程中「袋口方向」文字会超出卡片）；
- P3.2 实战估角演示中把进球线也画出来；
- P3.3 按 G1 校对瞄准点口径。

### 训练页

**P4 角度与打点**
- P4.1 球桌置于球库上方，球库不遮挡球桌；球桌大小与其他页面一致，球库左右留白（对齐 G8）；
- P4.2 从球库选球改为：点击球后自动放置到球桌中；
- P4.3 黑球（8 号）的进球线当前仍是白色，改为黑色（进球线=目标球本色的规则要覆盖黑球）。

**P5 角度预测**
- P5.1 数字键盘再小一点，不得遮挡「换题」「显示参考」按钮。

**P6 2D 角度训练**
- P6.1 球桌/球库布局问题同 P4.1/P4.2，修改方式相同；
- P6.2 「辅助」「答题」按键放到球桌右侧；
- P6.3 假想球中心加红点；
- P6.4 网格按 G2。

**P7 3D 角度训练**
- P7.1 同步 P6 的全部适用修改（共用 SceneAimingView，注意 3D 相机下核验）。

**P8 瞄准点训练**
- P8.1 增加一条过目标球（1 号）球心的水平线；
- P8.2 去掉假想球中心红点；
- P8.3 改用 G1 定义的正确瞄准点：瞄准点用红色小点标注（现有大点用户选不准）；
- P8.4 「向左切/向右切」方向当前是反的，调换：向左切指目标球向左移动，故母球应打目标球右侧；
- P8.5 目标球在屏幕中的占比放大；
- P8.6 上方统计文字（如「本次平均 50.7mm」）会折成两行，改为单行。

**P9 2D/3D 瞄准点训练**
- P9.1 按 G1 校对瞄准点计算与误差口径（若现实现以假想球心为准需修正）。

### 走位与对局页

**P10 自由击球**
- P10.1 当前模式下禁止手动摆球；
- P10.2 按当前玩法规则，禁止用户选择不合法目标球，并给出提示；
- P10.3 作为 G 系列布局规范的首个基准落地页。

**P11 打页入口顺序**
- P11.1 当前顺序：自由击球、分离角与走位、自由走位、拍照建球形、批量出片台；将**自由击球移到第三位**（改 `AngleHomeView.basePlayEntries`）。

**P12 批量出片台**
- P12.1 「点按球桌选起点」当前不生效：点完起点后「确认」按钮点不了，修复 `BatchGuideLine` 选点/确认链路。

**P13 分离角与走位 / 自由走位 / 思路训练 / 打一走二想三 / 做斯诺克**
- 仅执行各自适用的 G 系列规范（见矩阵），无额外页面特有项。

**P14 翻袋解球器 / 反射解球器**
- 本轮不动。

---

## 三、会话批次划分（S1–S7）

> 每个批次设计为一次会话可完成。执行顺序有依赖：S1 是布局基准，S2 依赖 S1；S3–S6 相对独立可并行/换序。每批次结束须通过 DoD 并回写 `tasks/PROGRESS.md`。

### S1 全局布局引擎 + 自由击球基准页

**范围**：G3–G11 在共享组件层落地（`BTTrajectoryDetailChip` 位置、`BTAimWheel`/`BTShotInstrumentColumn` 贴边与 1.2 倍加长、球库宽度规则、`BTBreakSideButton` 三圆圈、球桌尺寸锁定机制），并以自由击球（FreePlayView）为基准页完成全部布局验收；同时做 P10.1 / P10.2。
**建议产出**：把布局几何计算收敛到一个共享布局容器/计算器（各页只传差异参数），避免 S2 逐页手调。
**DoD**：
- 自由击球页满足 G3–G11 全部条款（截图核验：贴边、对齐、不遮挡、不超界）；
- 进袋/自由切换、开球、完成等任意操作球桌尺寸零变化（可加 UI 测试断言 SCNView frame 不变）；
- 开球中禁止摆球；选非法目标球被拦截并提示；
- 单元测试 + 构建通过。

### S2 全局规范推广到其余 6 个击打页

**范围**：用 S1 的共享容器改造分离角与走位、自由走位、批量出片台、思路训练、打一走二想三、做斯诺克（G3–G11，开球按钮禁用态）；三个 B 类页同时执行 G12（删球库区打点盘与解说明）；顺带完成 P11.1（入口顺序）与 P12.1（批量出片台辅助线选点 bug）。
**DoD**：
- 6 页逐页截图核验 G 矩阵条款；
- G12 后思路训练/打一走二想三/做斯诺克的解读数入口仍可用（右柱打点盘保留）；
- 打页顺序：分离角与走位、自由走位、自由击球、拍照建球形、批量出片台；
- 批量出片台辅助线：点起点 → 确认可点 → 点终点 → 确认成线；
- 构建 + 相关 UI 测试通过。

### S3 瞄准点概念修正（G1）+ 瞄准点训练页

**范围**：G1 定义在文档与代码中统一（瞄准原理文档、瞄准点对照表口径、瞄准点计算工具函数）；P8.1–P8.6 全部；P9.1。
**注意**：涉及几何计算，动代码前按规则先读 `geometry-spatial-reasoning` 技能并钉死坐标契约。
**DoD**：
- 瞄准点几何有单元测试（穿球/不穿球两种情形的交点计算）；
- 左右切方向与「目标球移动方向」口径一致（测试覆盖）;
- 瞄准点训练页：水平线、红色小瞄准点、无假想球红点、目标球占比放大、统计单行，截图核验；
- 2D/3D 瞄准点训练误差口径核对无误。

### S4 学习页三项

**范围**：P1.1–P1.3（瞄准原理，含 G1 文案落地）、P2.1（浅谈球感 2D 图宽度）、P3.1–P3.3（瞄准点对照表）。
**DoD**：三页截图核验：文字不拥挤、进球线可见、2D 图占满屏宽、卡片文字不溢出、实战估角有进球线；文案与 G1 定义一致。

### S5 训练页布局与渲染

**范围**：P4.1–P4.3（角度与打点）、P6.1–P6.4 与 P7.1（2D/3D 角度训练）、P5.1（角度预测键盘）、G2（网格颜色，一处改动）。
**DoD**：
- 角度与打点/2D 角度训练：球桌在球库上方且不被遮挡、点击选球自动上桌、黑球进球线为黑色；
- 2D 角度训练：辅助/答题在右侧、假想球红点存在；3D 同步核验；
- 角度预测：键盘不遮挡换题/显示参考；
- 网格为亮灰白，任一带网格页面截图核验。

### S6 机动补漏批次

**范围**：S1–S5 执行中登记的遗留项、跨页回归发现的问题（预留；执行时从 `tasks/PROGRESS.md` 阻塞区取任务）。若无遗留可跳过。

### S7 总验收

**范围**：对照本文件 G1–G12 与 P1–P14 逐条核验；全量单元测试 + 关键 UI 测试；截图巡检全部 12 个页面；回写 `tasks/UI-IMPLEMENTATION-SPEC.md`（布局规范 v3）、`tasks/PROGRESS.md`、`tasks/phases/P18-release-convergence.md`、Project Hub 状态卡。
**DoD**：验收清单逐条 ✅ 或注明偏差原因；文档回写完成。

---

## 四、执行约定

1. 每个批次开新会话，会话开头声明「执行问题集合 v3 批次 Sx」；
2. 批次内先读本文件对应 G 条款 + 页面条目，再动代码；
3. 布局/几何类任务强制先读 `geometry-spatial-reasoning` 技能；
4. 批次完成后：更新 `tasks/PROGRESS.md`（批次状态 + 下一批次）、登记遗留项到 S6；
5. 声称完成必须附真实构建/测试输出，UI 类条款附截图核验结论。
