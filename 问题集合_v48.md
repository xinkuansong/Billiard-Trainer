# 问题集合 v48 — Live Activity 统一训练状态系统

> **本文件是本轮需求真源**（v48.0，日期：2026-08-30，状态：**待拍板，未实施**）。
> **用户目标**：把灵动岛和锁屏收敛为同一套训练状态系统，只表达 **当前动作 + 当前状态 + 当前时间**；状态只保留「训练中 / 休息中」。
> **执行前置**：W0 的 Q-v48-1～Q-v48-5 未拍板前，W1–W6 不开工。拍板后先把答案回写为 D-v48-* 并升级本文件版本；聊天中的推荐项不等于用户结论。
> **工作区约束**：当前工作区有大量未提交改动，且 `问题集合_v47.md`、`tasks/PROGRESS.md` 均为 dirty。本轮立档只新增本文件；实施时每批先记录目标文件的定向 `git status` / `git diff`，不得覆盖或顺带提交无关改动。
> **覆盖关系**：本文件仅接管 Live Activity 的状态模型、灵动岛、锁屏、训练/休息切换、计时与验收；不接管已取消的 v47，不改训练内容、计划剂量、记录页主体布局或跨 Tab 浮标语义。

---

## 一、需求回显（语义基准）

| # | 用户要求 | 本方案中的不可弱化口径 |
|---|----------|------------------------|
| R1 | 只保留「训练中」「休息中」两种主状态 | Live Activity 不新增「暂停中」「已完成」等第三主状态 |
| R2 | 两种状态都展示当前动作名 | 动作名属于动态状态；切换动作后锁屏与灵动岛都更新 |
| R3 | 训练中展示累计训练时间 | 用系统时间戳推导，App 不在前台也连续；是否排除休息由 Q-v48-1 拍板 |
| R4 | 休息中展示休息计时/倒计时 | 展示方向由 Q-v48-5 拍板；不得误显示训练累计时间 |
| R5 | 灵动岛和锁屏共用同一套状态数据 | 只允许一个 `ActivityAttributes.ContentState` 真源；视图只负责呈现，不自行维护状态机 |
| R6 | 灵动岛坚持单行 | Expanded 主信息为 `动作 · 状态 · 时间`；不得加组数、今日进度或说明文字 |
| R7 | Compact / Minimal 进一步压缩 | 即使空间更小，也必须通过文案缩写与状态图标区分训练/休息，不能只靠颜色 |
| R8 | 锁屏坚持单行主信息 | 顺序与灵动岛一致；空间变大不等于增加内容或恢复卡片式多层结构 |
| R9 | 两种状态有明确视觉差异 | 文案 + 符号共同区分；训练用积极语义符号，休息用暂停/休息语义符号；颜色只作辅助 |
| R10 | 状态切换完整 | 开始训练→训练中；进入休息→休息中；继续训练→训练中；换动作只更新动作名；结束训练立即结束 Live Activity |
| R11 | 计时不依赖前台逐秒刷新 | 以起止时间戳与累计基数计算；后台、锁屏、回前台后保持连续 |
| R12 | 明确休息结束策略 | 必须在自动切回与用户确认之间拍板，禁止实现期临时决定 |
| R13 | 四类场景验收 | App 前台训练中、锁屏训练中、灵动岛训练中、训练↔休息往返；重点验状态一致、计时连续、单行、无重复、可一眼识别 |
| R14 | 最终目标 | 所有尺寸都围绕 **当前动作 + 当前状态 + 当前时间**，不把 Live Activity 做成训练摘要卡 |

### 1.1 示例文案

- 训练中：`五分点 · 训练中 · 28:36`
- 休息中：`五分点 · 休息中 · 01:24`

动作名「五分点」只是格式示例，运行时取当前 `ActiveDrill.nameZh`，不得硬编码。

---

## 二、代码现状与根因（2026-08-30 亲读）

### 2.1 当前实现不是“训练状态系统”，而是“休息倒计时挂件”

1. `RestTimerAttributes.ContentState` 只有 `endDate` 与 `totalSeconds`，静态 attributes 只有 `drillName`，无法表达训练状态、训练累计基数、动作切换或会话身份。
2. 同一个 `RestTimerAttributes` 在 App 与 Extension 各复制一份。字段改动必须人工同步，已经违背“共用同一套状态数据”的目标。
3. `RestTimerLiveActivityManager.startActivity` 只在进入休息时申请 Activity；训练开始时没有 Live Activity。
4. 当前休息结束会直接 `endActivity()`，因此不存在「休息中 → 训练中」的同一 Activity 状态回切。

### 2.2 当前信息结构与目标相反

1. Expanded 灵动岛拆为 leading / trailing / bottom 三层，并显示进度条，不是单行。
2. 锁屏包含圆形进度环、两组纵向文案与品牌名，是卡片式多层结构。
3. Compact / Minimal 只有 `timer` 图标，无法识别现在是在训练还是休息。
4. 当前动作名放在静态 attributes 中；换动作不会自然进入 `ContentState` 更新链路。

### 2.3 当前计时虽使用时间戳，但生命周期仍依赖前台定时器与静音音频

1. 训练累计时间已经有 `timerStartDate + accumulatedBeforePause` 的基础，但 `Date()` 写死，单测无法注入确定时间。
2. 休息倒计时已有 `restEndDate`，但用 `DispatchSourceTimer` 驱动完成逻辑，并开启循环静音音频维持后台。
3. `staleDate` 只会标记 Live Activity 内容可能过期，不会在到点时替 App 执行「休息 → 训练」状态变更。
4. 在没有 ActivityKit push 更新链路时，App 被挂起后不能承诺到秒自动切换主状态。静音音频不是可接受的状态机基础，也不应作为后台常驻手段。

### 2.4 动作切换入口分散，容易漏发状态更新

`currentDrillIndex` 除 `goToDrill(at:)` 外，还在完成组、休息结束、跳过休息、删除动作修正、概览点击以及 `TabView(selection:)` 中直接写入。若只在某一个函数更新 Live Activity，动作名必然出现漏同步。

### 2.5 结束与清理不完整

`endTraining()` 与 `cleanup()` 只暂停训练计时；若 Live Activity 已经扩展为整场训练，它们必须明确结束或保留的边界，否则可能遗留活动。

### 2.6 根因结论

当前问题不是两个视图“长得不够漂亮”，而是底层模型把 Live Activity 定义成了休息计时器。正确改造顺序必须是：

```text
单一会话状态与时间契约
    → 灵动岛纯渲染
    → 锁屏纯渲染
    → Activity 生命周期管理
    → 训练 ViewModel 事件接线
    → 自动化与真机视觉验收
```

---

## 三、W0 待拍板项（推荐不等于结论）

| 编号 | 必须选择的问题 | A | B | 推荐及理由 |
|------|----------------|---|---|------------|
| Q-v48-1 | 「累计训练时间」是否排除休息？ | 排除休息，只累计实际训练段 | 包含休息，等于整场会话时长 | **推荐 A**：文案叫“训练累计”，且休息已有独立时间；两种时间语义最清楚 |
| Q-v48-2 | 休息归零后如何回到训练？ | 停在「休息中 · 00:00」，用户点“继续训练”后切回 | 到点自动切回「训练中」 | **推荐 A**：App 被挂起时无法靠本地 tick 准时执行状态变更；A 能保证锁屏、岛、App 一致。若选 B，须把 ActivityKit push / 服务端调度与离线降级纳入新范围，且仍只能承诺尽力送达，不能承诺离线时准点切换 |
| Q-v48-3 | 训练计时被手动暂停/跳过时，Live Activity 怎么办？ | 保持主状态「训练中」，时间冻结；不增加第三状态 | 立即结束 Live Activity | **推荐 A**：严格保留两种主状态，也方便恢复；但要接受“训练中”的时间可暂时不走 |
| Q-v48-4 | 自由训练尚未选动作时是否创建 Live Activity？ | 不创建，选中首个动作并开始计时后再创建 | 用占位动作名「自由训练」创建 | **推荐 A**：严格满足“两种状态都有当前动作名”，避免占位文案冒充动作 |
| Q-v48-5 | 休息时间展示方向？ | 倒计时：`01:24 → 00:00` | 正计时：`00:00 → 01:24` | **推荐 A**：沿用现有休息体验与“+30 秒”能力；到点语义也更明确 |

### 3.1 技术红线（不作为可选项）

- 不用前台每秒 `activity.update` 推动数字；系统 `Text(timerInterval:)` / 时间戳负责显示连续变化。
- 不用循环静音音频维持后台；实施时移除 `AVFoundation` 静音播放器与仅为它声明的 `UIBackgroundModes: audio`。若未来有真实后台音频产品需求，另立项恢复。
- 不把 `staleDate` 当成定时回调或状态切换机制。
- 不为了“自动切回”伪装成已可靠：选 Q-v48-2B 时，必须追加 push token、服务端调度、离线/延迟策略和对应验收批次；ActivityKit push 也可能因无网络未送达，因此产品文案只能承诺“尽力自动切换”，不能承诺后台离线准点切换。

---

## 四、目标状态与数据契约

> 本节是待 W0 拍板后实现的结构草案；字段名可在 W1 小幅调整，但语义不可改变。

### 4.1 单一 Activity 类型

将休息专用命名收敛为整场训练命名：

```swift
struct TrainingActivityAttributes: ActivityAttributes {
    struct ContentState: Codable, Hashable {
        var actionName: String
        var phase: TrainingActivityPhase

        // 训练累计：基数 + 当前运行段起点
        var accumulatedTrainingSeconds: Int
        var trainingSegmentStartedAt: Date?

        // 休息：时间区间；展示方向由 Q-v48-5 决定
        var restStartedAt: Date?
        var restEndsAt: Date?
    }

    var sessionID: UUID
}

enum TrainingActivityPhase: String, Codable, Hashable {
    case training
    case resting
}
```

约束：

1. `sessionID` 是 Activity 生命周期身份，创建后不变。
2. `actionName` 必须在 `ContentState`，因为动作会在同一训练中改变。
3. `phase` 只有 `.training` / `.resting`。
4. 显示层不得读取 `ActiveTrainingViewModel`，也不得复制状态机。
5. App 与 Extension 编译同一个物理 Swift 文件，不再维护两份同名结构体。

### 4.2 时间计算契约

若 Q-v48-1 选择“排除休息”：

```text
开始训练：base = 0，trainingSegmentStartedAt = now
进入休息：base += now - trainingSegmentStartedAt，segment = nil
继续训练：base 不变，trainingSegmentStartedAt = now
换动作：只换 actionName，不碰 base / segment
结束训练：以 now 封口后结束 Activity
```

训练中显示值：

```text
accumulatedTrainingSeconds
+ (trainingSegmentStartedAt == nil ? 0 : now - trainingSegmentStartedAt)
```

休息中显示值：

- 倒计时方案：`max(0, restEndsAt - now)`。
- 正计时方案：`max(0, now - restStartedAt)`；如有目标时长，目标只用于到点逻辑，不另上屏。

### 4.3 Clock 可测试性

训练 ViewModel 与纯状态投影器不得直接散落调用 `Date()` / `Date.timeIntervalSinceNow`。W1 引入最小 `Clock`/`now` 注入（生产返回系统时间，测试传固定时间），以确定性验证后台跨时、休息冻结、恢复累计与动作切换不重置。

### 4.4 状态投影边界

建立一个纯函数或小型 projector：

```text
训练会话事实（动作、计时段、休息区间）
    → TrainingActivityAttributes.ContentState
    → 同一份 state 同时供灵动岛与锁屏渲染
```

`ActiveTrainingViewModel` 负责业务事件；`TrainingLiveActivityManager` 只负责 `request / update / end / reconcile`，不判断训练规则；Widget 只渲染。

---

## 五、信息结构与视觉契约

### 5.1 共用单行结构

所有可容纳完整文案的区域统一顺序：

```text
[状态图标] 当前动作 · 当前状态 · 当前时间
```

- 当前动作优先保留；过长时单行尾部截断，不换行。
- 状态必须显式写「训练中」或「休息中」。
- 时间使用等宽数字，避免逐秒跳宽。
- 不显示品牌名、组数、训练进度、说明、完成态或第二条副信息。

### 5.2 Dynamic Island Expanded

- 视觉上只形成一条主信息，不使用 `.bottom` 进度条。
- 推荐结构：leading 放状态图标；center/单一 HStack 放动作、状态、时间；布局实现须以真机/模拟器不换行为准，而不是机械复刻 region 名称。
- 训练图标候选：`figure.strengthtraining.traditional` 不符合台球语义，禁止使用；优先从现有台球/瞄准语义自定义图标或克制的 `play.fill` 中选择。
- 休息图标候选：`pause.fill`。
- 图标与辅助色可以不同，但去色后仍能靠文字与形状识别。

### 5.3 Compact

| 区域 | 训练中 | 休息中 |
|------|--------|--------|
| compactLeading | 训练语义图标 | `pause.fill` |
| compactTrailing | 训练累计时间 | 休息时间 |

Compact 空间不足以稳定放动作名与完整状态文案，因此动作名可省略；区别必须由不同图标 + 不同辅助色共同承担。若系统尺寸允许，可用极短状态字「练 / 休」，但必须截图确认不会拥挤后再采用。

### 5.4 Minimal

- 只显示状态图标；训练与休息必须使用不同轮廓/填充语义，不能只是同一个图标换颜色。
- VoiceOver label 分别为「训练中」与「休息中」。

### 5.5 锁屏 Live Activity

- 单个 `HStack` 完成主信息，不恢复圆环、VStack、副标题或品牌尾标。
- 顺序与 Expanded 一致：动作 → 状态 → 时间。
- 可用更大的字号、状态图标尺寸、间距和字重强化层级，但不增加字段。
- 支持浅色/深色系统外观与锁屏背景对比；不硬编码当前 `brandGreen` RGB。

### 5.6 长文案与本地化边界

- 至少覆盖短名「五分点」、中名、超长动作名三档预览。
- 动作名 `lineLimit(1)` + `truncationMode(.tail)`；状态和时间设置更高布局优先级，保证不会被挤没。
- 时间最少覆盖 `00:00`、`59:59`、`1:00:00`。若 Expanded/锁屏容纳不了小时格式，必须在 W2 截图后拍板紧凑格式，禁止静默截断。

---

## 六、状态切换真源

| 事件 | 前态 | 后态 | 动作名 | 训练累计 | 休息时间 | Activity 操作 |
|------|------|------|--------|----------|----------|---------------|
| 计划训练加载完成并开始计时 | 无 | 训练中 | 当前首个动作 | 从 0 开始 | 空 | `request` 一次 |
| 自由训练选中首个动作并开始计时 | 无 | 训练中 | 所选动作 | 从 0 开始 | 空 | 按 Q-v48-4 `request` |
| 进入组间休息 | 训练中 | 休息中 | 当前动作 | 按 Q-v48-1 冻结或继续 | 建立 start/end | `update`，不得新建第二个 Activity |
| 休息中 +30 秒 | 休息中 | 休息中 | 不变 | 不变 | 终点 +30 秒 | `update` 时间戳 |
| 用户跳过/确认结束休息 | 休息中 | 训练中 | 若有待前进动作，先切到新动作 | 从当前时刻续计 | 清空 | `update` |
| 休息自然归零 | 休息中 | 由 Q-v48-2 决定 | 按拍板 | 按拍板 | 钳到 0 | 按拍板；不得直接结束整场 Activity |
| 左右滑动/概览点选动作 | 训练中或休息中 | 主状态不变 | 更新为新动作 | 连续，不归零 | 若休息则连续 | `update` |
| 暂停/跳过训练计时 | 训练中 | 由 Q-v48-3 决定 | 不变 | 冻结 | 空 | 更新冻结 state 或结束 |
| App 从后台回前台 | 任意 | 由系统时间重算 | 与当前索引一致 | 连续 | 连续/归零 | reconcile 后必要时 `update` |
| 用户结束整场训练 | 任意 | 无 | — | 封口 | 清空 | `end(.immediate)`，不显示“已完成” |
| 非最小化离开训练页/会话销毁 | 任意 | 无 | — | — | — | 明确 `end`；不得遗留僵尸 Activity |

### 6.1 动作切换必须收口

实施时把所有直接写 `currentDrillIndex` 的入口收敛到单一方法或 `didSet` 后的明确事件链。至少覆盖：

- 概览点击动作；
- `TabView` 滑动；
- 完成当前动作后自动前进；
- 休息结束/跳过后前进；
- 删除动作导致索引修正；
- 程序调用 `goToDrill(at:)`。

完成标准不是“某一条路径能更新”，而是上述入口逐个有单测或明确集成用例。

### 6.2 单 Activity 原则

同一 `sessionID` 最多一个 Activity。进入休息、继续训练、切动作都走 `update`。只有开始训练时 `request`，结束训练时 `end`。App 重启或恢复时用 `Activity<TrainingActivityAttributes>.activities` 对账，结束不属于当前会话的陈旧 Activity。

---

## 七、批次总表

| 批次 | 主题 | 主要范围 | 量级 | 依赖 |
|------|------|----------|------|------|
| W0 | 拍板 + 基线 | 回答 5 个问题；记录 before 截图、当前行为、目标文件 dirty 状态 | 小 | 无 |
| W1 | 统一状态模型与时间契约 | 单一共享 attributes、两状态 enum、时间投影、Clock 注入、纯逻辑单测 | 中 | W0 |
| W2 | 灵动岛重做 | Expanded 单行、Compact/Minimal 状态差异、长名/小时预览与截图 | 中 | W1 |
| W3 | 锁屏重做 | 单行 HStack、与岛同序、浅深色/长名/时间截图 | 小～中 | W2 |
| W4 | Activity 生命周期管理 | 统一 manager、request/update/end/reconcile、移除静音音频与后台 audio 配置 | 中 | W1；排在 W3 后减少共享文件往返 |
| W5 | 训练/休息状态接线 | ViewModel 事件收口、动作同步、休息切换、结束清理、回前台重算、manager mock 测试 | 大 | W4 |
| W6 | 综合验收与文档收口 | 自动化测试、构建、4 场景矩阵、真机/模拟器截图、更新测试计划/进度 | 中 | W2–W5 |

### 7.1 依赖图

```text
W0 拍板/基线
  ↓
W1 统一状态模型
  ↓
W2 灵动岛
  ↓
W3 锁屏
  ↓
W4 生命周期管理
  ↓
W5 训练/休息接线
  ↓
W6 综合验收
```

说明：W2、W3、W4 技术上都只依赖 W1，但 W2/W3 会共同修改 Extension 视图，W4/W5 会共同修改 App 状态链。为降低 dirty worktree 冲突并遵循用户要求的实施顺序，本轮固定串行，不做并行批次。

---

## 八、逐批任务与完成标准

### W0 — 产品拍板与改前基线

**范围**

1. 用户回答 Q-v48-1～Q-v48-5。
2. 把答案写成 D-v48-1～D-v48-5，并升级本文件版本。
3. 记录目标文件定向 `git status` / `git diff`，确认是否与用户现有改动重叠。
4. 在可运行设备上记录当前休息 Live Activity 的锁屏、Expanded、Compact、Minimal 截图；训练中“无 Activity”也记为基线事实。
5. 记录系统版本、设备/模拟器型号、Light/Dark 外观和是否支持灵动岛。

**完成标准**

- 5 个问题全部有用户明确答案，方案中没有“推荐项冒充结论”。
- Before 证据覆盖锁屏 + Expanded + Compact + Minimal；无法取得的尺寸明确记为“未验证”，不伪造。
- 后续批次涉及文件的初始 dirty 状态已落档；发现重叠先停，不覆盖。

### W1 — 统一状态模型与时间契约

**范围**

1. 新建单一共享 `TrainingActivityAttributes.swift`，同时编入 App 与 Extension target；删除两份重复 `RestTimerAttributes.swift` 前先确认没有其他引用。
2. 只定义 `.training` / `.resting` 两种 phase；动作名放动态 `ContentState`。
3. 落实 §4 的时间字段与纯投影器；注入可测试 Clock。
4. 在 `project.yml` 声明共享文件的双 target 归属，通过项目生成命令同步 `QiuJi.xcodeproj/project.pbxproj`，禁止只手改产物。
5. 新增独立纯逻辑测试文件，例如 `TrainingActivityStateTests.swift`。

**必须覆盖的测试**

- 训练 10 分钟 → 休息 1 分钟 → 继续 5 分钟，累计值符合 Q-v48-1。
- App “后台”跨过任意时间后，用固定 Clock 重算仍连续。
- 切动作只改变 `actionName`，累计训练时间不变。
- 休息 +30 秒只改变休息终点。
- phase 枚举无法产生第三主状态。
- `ContentState` Codable 往返保持等值。

**完成标准**

- 代码库中只剩一个 Activity attributes 定义；`rg` 无旧类型生产引用。
- 纯状态测试全绿；测试不依赖真实等待一秒。
- `make xcodegen` 后检查 `project.yml` 与 `project.pbxproj` 两头都含正确 target membership。
- `make build` 成功。未执行 UI 截图时不得宣称视觉完成。

### W2 — Dynamic Island 单行化

**范围**

1. 将 Widget 重命名/重构为训练会话语义。
2. Expanded 移除 bottom 进度条与“组间休息”副标题，只保留一条主信息。
3. Training / Resting 各自使用明确文案与图标。
4. Compact / Minimal 按 §5.3–5.4 压缩。
5. 建立可枚举 preview fixtures：两状态 × 短/长动作名 × 分钟/小时计时。

**完成标准**

- Expanded 训练截图能读出 `动作 · 训练中 · 时间`，休息截图能读出 `动作 · 休息中 · 时间`。
- Expanded 无第二行、无 progress、无品牌尾标、无组数/进度/说明。
- Compact 与 Minimal 在灰度/不看颜色时仍能通过不同图标区分两状态。
- 长动作名不挤掉状态和时间；无换行、无明显遮挡。
- 改后截图与 W0 before 并排留档；至少覆盖支持灵动岛的设备尺寸。

### W3 — 锁屏 Live Activity 单行化

**范围**

1. 删除圆形进度环、两个 VStack 与品牌尾标。
2. 用单个主 `HStack` 呈现动作、状态、时间。
3. 与 Expanded 复用同一 display model / formatter，避免文案顺序漂移。
4. 验证锁屏浅/深背景对比、长动作名、小时计时与动态字体边界。

**完成标准**

- 锁屏只有一行主信息，顺序与灵动岛一致。
- 训练与休息文案明确，不能只靠颜色辨别。
- 没有重复动作名、重复计时或额外说明。
- Light/Dark 截图均可读；长名保持单行并优先保住状态与时间。
- 锁屏空间变大后仍没有擅自增加组数、进度或品牌说明。

### W4 — Activity 生命周期与系统配置

**范围**

1. 以 `TrainingLiveActivityManaging` 协议隔离 ActivityKit，生产 manager 实现 `start/update/end/reconcile`。
2. 同一 session 始终更新同一个 Activity；开始前清理不属于当前 session 的陈旧 Activity。
3. `update` 接收完整 `ContentState`，不再提供“只改 endDate”这种休息专用 API。
4. 移除 `AVFoundation`、静音音频生成/循环播放和相关调用。
5. 从 `project.yml` 移除仅为静音保活声明的 `UIBackgroundModes: audio`，生成并核对 `Info.plist` 与 `project.pbxproj`。
6. 错误不得只 `print` 后失联；至少形成可测试返回值或可观察日志边界，UI 不因 ActivityKit 不可用而阻塞训练。

**完成标准**

- Manager mock 可验证 request/update/end 的次数与顺序。
- 一次训练只有一次 request；训练↔休息、换动作都只 update；结束只 end 一次且立即消失。
- `rg` 无 `silentPlayer`、`activateBackgroundAudio`、`rest_silence.caf` 生产引用。
- `project.yml` 与生成后的 App Info.plist 均不含本功能遗留的 audio background mode。
- Live Activities 被系统关闭时，训练流程仍正常，且不产生崩溃/阻塞。
- `make build` 成功。

### W5 — ViewModel 状态切换接线

**范围**

1. 将 `ActiveTrainingViewModel` 的系统时间与 manager 依赖注入，测试使用固定 Clock + spy manager。
2. 训练开始、进入休息、继续训练、+30 秒、换动作、结束训练、cleanup、回前台分别投影并发出正确 state。
3. 收口全部 `currentDrillIndex` 写入口，保证动作名更新不漏路径。
4. 按 Q-v48-2 实现休息归零；若选择确认继续，App 休息 UI 必须提供明确继续入口并与 Live Activity 同步。
5. 按 Q-v48-3 处理暂停/跳过计时，但不新增第三主状态。
6. 保持现有组间休息覆层最小化语义，不把“休息最小化”误接成“整场训练最小化”。

**必须覆盖的测试**

- 开始计划训练 → request(training, 当前动作, 0 起算)。
- 进入休息 → update(resting)，Activity 未结束。
- 继续训练 → update(training)，累计值按拍板继续。
- +30 秒 → update(resting) 的终点延后。
- 概览点击、TabView 滑动、自动前进、跳过休息、删除索引修正均更新动作名。
- 换动作不重置累计训练时间。
- 结束训练时从 training/resting 两个前态都立即 end，不遗留“已完成”。
- cleanup 在会话真正销毁时 end；整场训练最小化时不误 end。
- 后台跨时后回前台，训练/休息数字按 Clock 对齐，不靠补 tick。
- 系统禁止 Live Activity 时，业务状态与训练记录不受影响。

**完成标准**

- 所有动作索引变化都有统一副作用入口；`rg` 不再发现绕开入口的直接赋值（Binding 若保留，必须有显式 `onChange` 契约与测试）。
- ViewModel 新测试不使用真实 sleep；状态切换和时间语义均可确定复现。
- 现有 `ActiveTrainingViewModelTests` 全绿，未删除无关合法断言求绿。
- `make build` 成功。

### W6 — 自动化、视觉与四场景综合验收

**范围**

1. 运行新增定向测试、`ActiveTrainingViewModelTests` 与全量 `make test`。
2. 运行 `make build`；若 W1/W4 改过项目生成配置，再跑一次 `make xcodegen` 后复验 build，确保真源与产物一致。
3. 在支持灵动岛的设备/模拟器执行 §十验收矩阵。
4. 至少录制一次训练→休息→继续训练→换动作→结束训练的连续视频或逐步截图组。
5. 更新 `tasks/test-plans/TP-P4.md` 的 Live Activity 场景、`tasks/UI-IMPLEMENTATION-SPEC.md` Changelog、`tasks/PROGRESS.md`；只有发生设计裁定/API 变化时才按项目规则追加 DR。

**完成标准**

- 定向测试、全量 XCTest、build 均有真实命令输出与结果记录。
- 训练/休息两状态在 App、锁屏、灵动岛的动作名和状态一致。
- 前后台跨时、锁屏跨时、回前台后计时连续，误差口径在测试计划中明确（目标 ≤1 秒，不靠等待补偿）。
- Expanded 与锁屏始终单行；Compact/Minimal 一眼可分状态。
- 结束训练后 Live Activity 立即消失；没有“已完成”停留或僵尸 Activity。
- 若无法取得真机锁屏/灵动岛证据，只能标“自动化与构建通过、真机视觉待验”，不得写全案完成。

---

## 九、代码锚点索引（2026-08-30 核实）

| 范围 | 文件与行号 | 当前事实 | 预计批次 |
|------|------------|----------|----------|
| Extension 状态模型 | `QiuJiLiveActivity/RestTimerAttributes.swift:4-10` | 只有休息 endDate/totalSeconds；动作名为静态 attributes | W1 |
| App 状态模型副本 | `QiuJi/Core/LiveActivity/RestTimerAttributes.swift:4-10` | 与 Extension 手工重复 | W1 |
| 灵动岛 | `QiuJiLiveActivity/RestTimerLiveActivity.swift:8-48` | Expanded 三块 + bottom progress；Compact/Minimal 只有 timer | W2 |
| 锁屏 | `QiuJiLiveActivity/RestTimerLiveActivity.swift:51-114` | 圆环 + 双 VStack + 品牌名，多层卡片 | W3 |
| Extension 入口 | `QiuJiLiveActivity/QiuJiLiveActivityBundle.swift:11-15` | 注册 `RestTimerLiveActivity` | W2 |
| Activity 生命周期 | `QiuJi/Core/LiveActivity/RestTimerLiveActivityManager.swift:5-62` | 仅休息 start/update/end；start 前结束全部旧活动 | W4 |
| 静音后台保活 | `QiuJi/Core/LiveActivity/RestTimerLiveActivityManager.swift:64-112` | AVAudioSession + 循环静音 CAF | W4 |
| 陈旧 Activity 清理 | `QiuJi/Core/LiveActivity/RestTimerLiveActivityManager.swift:114-120` | 无 session 对账，直接结束全部同类型 Activity | W4 |
| ViewModel 主状态 | `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift:203-242` | 训练/休息字段与 manager 单例直接持有；Clock 不可注入 | W1/W5 |
| 训练时间 | `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift:399-454` | 时间戳 + 前台 tick；`Date()` 写死 | W1/W5 |
| 动作导航 | `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift:456-461` | 有 `goToDrill`，但不是唯一写入口 | W5 |
| 删除后索引修正 | `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift:488-503` | 直接写 `currentDrillIndex` | W5 |
| 完成动作自动前进 | `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift:596-635` | 多处直接改变/延迟改变索引 | W5 |
| 休息状态机 | `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift:714-809` | 休息时新建 Activity、静音保活；归零结束 Activity | W4/W5 |
| 结束训练 | `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift:843-852` | 只 pause，不结束整场 Activity | W5 |
| cleanup | `QiuJi/Features/Training/ViewModels/ActiveTrainingViewModel.swift:970-974` | 只 pause | W5 |
| 回前台 | `QiuJi/Features/Training/Views/ActiveTrainingView.swift:149-167` | active 时刷新 timers；页面销毁时 cleanup | W5 |
| 概览点动作 | `QiuJi/Features/Training/Views/ActiveTrainingView.swift:236-250` | 绕过 `goToDrill` 直接写索引 | W5 |
| 滑动切动作 | `QiuJi/Features/Training/Views/ActiveTrainingView.swift:471-483` | `TabView` 直接绑定索引 | W5 |
| 工程真源 | `project.yml:37-109`、`project.yml:125-136`、`project.yml:156-176` | 主 target/Extension 分离；主 App 声明 Live Activity 与 audio 后台模式 | W1/W4 |
| 生成后的 Info.plist | `QiuJi/Resources/Info.plist:60-65` | `NSSupportsLiveActivities=true` + `audio` background mode | W4 |
| 现有测试 | `QiuJiTests/ActiveTrainingViewModelTests.swift:10-80`、`:743-751` | 有基础 timer/rest overlay/cleanup 测试；无 ActivityKit 状态测试 | W1/W5 |

> 行号是 v48.0 立档时锚点。实施中代码移动后，以符号搜索为准，并在版本更新中刷新行号；禁止继续引用失效行号装作已核实。

---

## 十、最终验收矩阵

| 场景 | 操作 | 必看结果 | 证据 |
|------|------|----------|------|
| A. App 前台训练中 | 开始有动作的训练，等待跨分钟 | App 与 Activity 均为训练中；动作一致；训练时间连续 | App + 锁屏/岛同刻截图 |
| B. 锁屏训练中 | 训练中锁屏，等待至少 70 秒 | 单行；时间自行增长；解锁后误差 ≤1 秒 | 锁屏前后截图/录屏 |
| C. 灵动岛训练中 | 查看 Expanded、Compact、Minimal | Expanded 完整三字段；Compact/Minimal 能分训练/休息 | 三尺寸截图 |
| D. 训练→休息 | 进入组间休息 | 同一个 Activity 更新为休息中；动作不丢；时间切换为休息语义 | 连续录屏或带时间戳截图 |
| E. 休息→训练 | 按 Q-v48-2 的方式继续 | 同一个 Activity 回训练中；训练累计符合 Q-v48-1 | 连续录屏 |
| F. 休息 +30 秒 | 休息中加时 | 休息时间连续延后，不重置训练累计，不新建 Activity | 操作前后截图 + spy 测试 |
| G. 切动作 | 训练中与休息中各切一次 | 动作名更新；主状态不变；训练累计不归零 | 两状态截图 + 单测 |
| H. 后台连续性 | 训练/休息各后台 2 分钟再回前台 | 不靠补 tick；时间按系统时间对齐 | 录屏/时间记录 |
| I. 结束训练 | 从训练中、休息中各结束一次 | Activity 立即结束；无“已完成”残留；无陈旧 Activity | 锁屏/岛消失证据 |
| J. 系统禁用 Live Activity | 关闭权限后训练一轮 | App 训练、休息、保存仍可用；只是不显示 Activity | 结果记录 |
| K. 长文案/小时 | 超长动作名 + 1 小时以上训练 | 状态和时间不被挤掉；所有主界面仍单行 | Preview + 实机截图 |
| L. 可访问性 | VoiceOver 读取两状态 | 读出动作、训练/休息、时间语义；Minimal 也有正确 label | 人工记录 |

### 10.1 一眼判断检查

每张截图都回答下面五问，任一为“否”即未通过：

1. 不看 App，能否一眼判断现在在练还是在休息？
2. 能否看出当前练哪个动作？
3. 当前数字的含义是否唯一，不会把休息时间误认成训练累计？
4. 主信息是否严格单行，没有重复信息？
5. 锁屏与灵动岛是否像同一套系统，而不是两个独立设计？

---

## 十一、风险、边界与回退

### 11.1 主要风险

| 风险 | 触发方式 | 预防/回退 |
|------|----------|-----------|
| 后台自动切换承诺失真 | 选自动切回但没有 push | W0 优先选确认继续；选自动则扩范围并单独设计 push，不用静音音频冒充可靠 |
| 两 target 模型漂移 | 继续复制 attributes | W1 强制单物理文件 + 双 target membership；生成前后两头核对 |
| 动作名漏更新 | 保留分散索引写入口 | W5 收口入口并逐路径测试 |
| 时间重复累计/漏累计 | 训练与休息同时推进同一基数 | Clock 纯逻辑测试覆盖每次边界；状态投影集中实现 |
| 长名字挤掉状态 | 平均文案预览通过但极端失败 | W2/W3 建短/中/长 fixture；状态与时间提高 layout priority |
| Dirty worktree 覆盖用户改动 | 直接改已有目标文件 | 每批定向 status/diff；遇重叠停下说明，不 restore 用户文件 |
| xcodegen 只改一头 | 只改 yml 或只改 pbxproj | `make xcodegen` 后同时检查真源与产物，并重新 build |
| 休息最小化语义串线 | 把组间覆层收起当成整场最小化 | 遵守 FL-033；两条状态与导航路径分别测试 |

### 11.2 明确不做

- 不增加组数、今日进度、训练说明、完成摘要。
- 不重做 App 内训练记录页主体视觉。
- 不改训练计划/Drill JSON、数据保存口径或 Freemium。
- 不新增第三主状态。
- 不引入第三方依赖。
- 不把 Live Activity 做成交互控制台；“继续训练”仍回 App 操作，除非后续另立 App Intent 需求。
- 不在本方案阶段实施代码、改图或声称视觉通过。

### 11.3 回退边界

每批只提交该批文件；若某批验证失败，回退到上一批已通过的统一状态契约，不回退用户的其他 dirty 改动。旧 `RestTimer*` 文件只有在新类型完成引用迁移、构建和测试后才删除；迁移中间态不得跨批遗留为无法构建的主分支状态。

---

## 十二、参考依据

- [Apple：Displaying live data with Live Activities](https://developer.apple.com/documentation/activitykit/displaying-live-data-with-live-activities)：Live Activity 的动态内容由 App 或 ActivityKit push 更新；它不使用普通 Widget 的 timeline 更新机制。
- [Apple：Starting and updating Live Activities with ActivityKit push notifications](https://developer.apple.com/documentation/ActivityKit/starting-and-updating-live-activities-with-activitykit-push-notifications)：push 可更新/结束 Activity，但网络不可用时仍可能无法送达，必须设计过期与离线降级。
- [Apple：SwiftUI `Text(timerInterval:pauseTime:countsDown:showsHours:)`](https://developer.apple.com/documentation/swiftui/text/init%28timerinterval%3Apausetime%3Acountsdown%3Ashowshours%3A%29)：计时文字可由系统依据日期区间正计时/倒计时并在指定时间冻结，不需要 App 每秒推送 state。
- 项目现行规则：`project.yml` 是 Xcode 工程生成真源；UI 改动必须有 before/after 截图；“完成/通过”必须附真实 build/test 输出。

---

## 十三、版本记录

| 版本 | 日期 | 状态 | 变更 |
|------|------|------|------|
| v48.0 | 2026-08-30 | 待拍板，未实施 | 首次正式立档；按统一模型→灵动岛→锁屏→生命周期→状态接线→验收拆为 W0–W6；登记 5 个待拍板项、代码锚点与完成标准 |

> 后续规则：任何产品答案、范围变化、批次合并/拆分或完成标准变化，都先升级本文件版本并记录原因；高版本覆盖低版本。聊天草稿不覆盖本真源。
