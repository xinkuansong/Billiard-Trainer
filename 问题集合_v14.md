# 问题集合 v14 — 练习 Tab「学」区文档学页版式与交互控件统一

> **本文件是本轮需求的真源。** 日期：2026-07-19　状态：🔄 **B1 ✅ → 等待 v13 收官后开 B2**（plan-delegated-execution；模型全程 `cursor-grok-4.5-high-fast`）
> 来源：2026-07-19 会话——对比动作库「初级蛇彩」精讲舒适度 vs 练习 Tab「学」卡内容格式；用户确认不做精讲三标签 / 蛇彩 CTA；要求六张文档学页含交互统一设计；明确排除「角度与瞄准」「分离角图谱」。
> 编号约定：条目 **L1–L8**；批次 **B1–B3**；拍板项 **D-v14-N**。
> 与 **v13**（瞄准方法内容诚实性，同碰 `AimingMethodsView`）：**串行避让**——v13 B2 仍 🔄；本轮 B2 接方法页须等 v13 ✅。
> 执行：2026-07-19 用户指定全程 `cursor-grok-4.5` → slug `cursor-grok-4.5-high-fast`；波次定性 **全串行**（B1 横切 → B2/B3 页面接壳，文件链依赖，**不可** plan-parallel 同波）。

---

## 〇、问题陈述（事实 / 推测分开）

**事实（已读代码）**

- 动作库精讲（样板 `drill_c042`）由 `DrillTutorialView` 渲染：节卡 + 主色正文 + 行距 + `items`/`params` 结构化块（DR-019 / ADR-P11-14）。
- 练习 Tab「学」入口 8 卡见 `AngleHomeView.learnEntries`；其中 6 张为浅色 `ScrollView` 文档页，2 张为暗色 `ShotStage` 沙盘。
- 六张文档页普遍：灰底节卡（`btBGSecondary`）+ 正文 `.btBody`/`.btTextSecondary` 长段；交互页各自手写 `Slider`/`Picker` 排版，无共享控件条组件。
- 「瞄准修正」已有较完整的 `sharedControls`（力度 / 高低杆 / 左右塞），可作实况条抽离样板；「瞄准方法」「旋转与加塞」各有页级 θ 滑杆；「对照表」有 0°–90° 与估距滑杆。

**推测（舒适度根因）**

- 用户感到「不舒服」主要来自**文档壳与控件排版不一致 + 灰字段落密度**，而非缺精讲「为什么/怎么打/自检」节奏（用户已否决套用三标签）。

---

## 一、需求回显（语义基准，不增删）

| # | 诉求 | 验收语义 |
|---|---|---|
| L1 | **文档学页统一节卡壳** | 六页节卡：标题层级、正文主色、行距、脚注次级色、圆角/内边距口径一致（守现有 Design Token，不引入新字体族） |
| L2 | **正文可读性** | 主阅读路径用 `.btText`（或等价主色）+ 明确行距；次级灰留给脚注/caption/误区旁注，禁止整页主文长期灰字墙 |
| L3 | **公式不压主路径** | 推导/速查类公式块视觉降级或收拢（脚注区 / 次级卡 / 折叠其一），正文保留教练向短句；**不删公式内容** |
| L4 | **交互控件条统一** | θ 条、实况三轴（力度/高低杆/左右塞）、对照表角度读数条共用同一视觉规格（标签 + monospaced 读数 + Slider/Picker）；各页语义范围不变 |
| L5 | **局部试错与全局 θ 可区分** | 管道「试瞄角 φ」等局部控件保留，但视觉标注为试瞄/局部，不与全局 θ 条混淆（不改几何语义） |
| L6 | **页末导流密度** | `PracticeCTA`：学→练优先保留；同「学」互链收敛（建议 ≤2 张大卡或改为轻量链）；**不**新增动作库/蛇彩深链 |
| L7 | **插图语言不回退** | 继续 `BTTableFigure` / `FigureLine` / 球组件；禁止再私设线色台面比例 |
| L8 | **验收可证** | `make build`；学区巡游/相关 UI 测绿；六页明暗各至少顶区+一交互态截图可目视壳一致 |

**明确不做（本轮）**

| 不做项 | 依据 |
|---|---|
| 「角度与瞄准」「分离角图谱」任何改动 | 用户 2026-07-19 划出范围 |
| 套用精讲「为什么 / 怎么打 / 自检」三标签 | 用户否决：学页是概念页，非逐杆应用课 |
| 新增「去蛇彩 / 动作库」CTA | 用户否决：与格式问题无关 |
| 八页同一滚动模板 / 把文档页改成全屏沙盘 | 会话结论：统一零件，不统一剧本 |
| 重写各页球理定义或几何真源 | 本轮只动壳与控件呈现；几何改动归 v13 等专批 |
| 改「练 / 打 / 解」入口与沙盘页 | 范围外 |

---

## 二、代码现状（事实，已核实 2026-07-19）

### 2.1 范围内六页

| 页 | 文件（约行数） | 交互 | 现状壳特征 |
|---|---|---|---|
| 瞄准原理 | `AimingPrincipleView.swift`（~706） | 只读 + 真台图 | 多节灰卡；正文 `.btBody`+`.btTextSecondary`；`termRow`；页末 `PracticeCTA`→角度预测 |
| 瞄准方法 | `AimingMethodsView.swift`（~739） | 页顶 θ；管道 φ；接触点播放 | 同灰卡；**与 v13 同文件**；`crossRefsSection` 多枚 `PracticeCTA` |
| 瞄准修正 | `AimingCorrectionView.swift`（~930） | `sharedControls` 三轴 | 样板级实况条（L61+）；正文灰段；交叉引用 + 练 CTA |
| 旋转与加塞 | `SpinAndEnglishView.swift`（~638） | θ + 旋转分段 | 正文灰段；页末多 CTA（含分离角图谱） |
| 浅谈球感 | `BallFeelView.swift`（~406） | 只读（含全宽出血图） | 灰卡 + 灰正文；页末 CTA→2D 角度训练 |
| 瞄准点对照表 | `ContactPointTableView.swift`（~562） | 0°–90° + 估距滑杆 | 交互节圆角 `md`、他节混用 `md`/`lg`；读数规格页内自定 |

### 2.2 共享与对照（范围外仅作对照，不改）

| 事实 | 锚点 |
|---|---|
| 「学」入口八卡（含排除的两张） | `AngleHomeView.swift` `learnEntries` ~L112–148 |
| 导流卡组件 | `PracticeCTA` `AngleHomeView.swift` ~L532–571 |
| 精讲舒适度对照（只读参考） | `DrillTutorialView` `paragraphs`/`itemRow`/`sectionCard`（主色+行距+结构化） |
| 暗色沙盘（**本轮不改**） | `AngleDynamicView` / `SeparationAngleAtlasView` |
| 学区巡游 | `ScreenshotTourUITests.testAngleLearningPages` ~L175+ |
| 图语言 | `BTTableFigure.swift` + SPEC §9.3 学页契约行 |

### 2.3 改动波及面（估）

- **新增**：学区文档壳 / 控件条组件（建议落 `QiuJi/Features/AngleTraining/` 或 `Core/Components/`，B1 定路径）。
- **改造**：上表六 View 接壳；不改 ViewModel/几何文件，除非控件绑定需要极薄包装。
- **文档**：`tasks/UI-IMPLEMENTATION-SPEC.md` 增「文档学页壳」短契约（建议 §9.3 旁或 §9.x）。
- **测试**：扩/回归 `testAngleLearningPages`；方法页交互测勿破坏 v13 已加 identifier。

---

## 三、拍板项

| # | 问题 | 推荐 | 状态 |
|---|---|---|---|
| D-v14-1 | 范围 | 仅六张浅色文档学页；排除角度与瞄准、分离角图谱 | ✅ 用户 2026-07-19 |
| D-v14-2 | 不做精讲三标签 / 蛇彩 CTA | 不做 | ✅ 用户 2026-07-19 |
| D-v14-3 | 统一策略 | **一种文档壳 + 共用零件**（节卡、正文 token、控件条、CTA 密度）；不强制八页同模板 | ✅ 会话结论 |
| D-v14-4 | 与 v13 顺序 | **B1 可并行**；**B2 接方法页须 v13 收官**（避免同文件双写） | ✅ 方案默认（执行前再确认 v13 状态） |
| D-v14-5 | 公式降级形态 | **次级卡 / caption 区收拢**，默认不做 Disclosure 折叠（少交互债）；若单页公式块＞2 处再局部折叠 | ✅ 2026-07-19 开工采纳推荐 |
| D-v14-6 | 同「学」CTA | 页末 `PracticeCTA` 大卡 **≤2**；超出改为 `NavigationLink` 文字行或 chip；学→练 CTA 优先保留 | ✅ 2026-07-19 开工采纳推荐 |
| D-v14-7 | 执行模型 | 三批均 `cursor-grok-4.5-high-fast` + 主控逐文件读 diff | ✅ 2026-07-19 用户指定 |

---

## 四、批次表

三批、壳→交互页→只读页，**全串行**。量级均按单会话可验收估算。

```
B1（契约 + 共享壳/控件条）
  └─► B2（交互四页接壳：方法 / 修正 / 旋转 / 对照表）
        └─► B3（只读两页接壳：原理 / 球感 + 六页巡游验收）
```

| 批次 | 性质 | 范围 | 量级 | 依赖 | 模型 |
|---|---|---|---|---|---|
| **B1** ✅ 2026-07-19 | 横切 | L1/L4 的**组件侧**：文档节卡容器、正文/脚注样式助手、`LearnControlStrip`（θ 档 + 实况三轴档 + 通用读数行）；SPEC 短契约；Preview 或轻量 UI 烟测；**不改六页业务文案与几何** | M | —（可与 v13 B2 并行） | `cursor-grok-4.5-high-fast` |
| **B2** ⏳ | 页面域 | L2/L4/L5/L6/L7：方法、修正、旋转、对照表接壳；抽离重复 Slider 排版；φ 与全局 θ 视觉区分；CTA 密度按 D-v14-6；保持既有 `accessibilityIdentifier` | L | B1；**方法页另依赖 v13 ✅** | `cursor-grok-4.5-high-fast` |
| **B3** ⏳ | 收口 | L2/L3/L6/L8：原理、球感接壳；公式块降级（D-v14-5）；页末学→练 CTA 保留；`testAngleLearningPages` 扩帧/回归；六页明暗截图归档；SPEC Changelog | M | B2 | `cursor-grok-4.5-high-fast` |

**波次表（plan-parallel 评估结论）**：无同波并行——B1 横切必须先落；B2/B3 文件链依赖且 B2 与 v13 抢 `AimingMethodsView`。全程走串行 `plan-delegated-execution`。

### B1 完成标准

1. 仓库内存在可复用的文档学页节卡 + 至少两档控件条 API（θ / 实况三轴），命名与路径写入本真源锚点表（升版记录）。
2. SPEC 有「文档学页壳」短契约：字色/行距、节卡、控件条、CTA 密度、插图红线、范围排除两沙盘页。
3. `make build` SUCCEEDED；组件 Preview 或单测可编译引用。
4. **零**业务改动于六学页（或仅 `@_exported`/import 级，无视觉切换）——接壳留 B2/B3。

### B2 完成标准

1. 四交互页主滑杆/分段外观对齐控件条规格；修正页三轴不再是唯一「好看」的特例。
2. 方法页：全局 θ 与管道 φ 可一眼区分；v13 已修几何/文案行为不回退；试瞄管线色不回退为绿禁令违规色。
3. 旋转/方法页末大卡 CTA 数量符合 D-v14-6；无新增蛇彩/动作库链。
4. 相关 UI 测（`testAimingMethodsInteractions` 等）绿；`make build` SUCCEEDED。
5. 明/暗各至少 1 帧/页顶区+控件区截图入 `build/v14-screenshots/`（或 UITest attachment 拷贝）。

### B3 完成标准

1. 原理 / 球感主文不再整页次级灰；节卡壳与 B2 四页一致。
2. 原理页公式/速查不压在与「名词/切角」同级的主阅读权重上（D-v14-5）。
3. `testAngleLearningPages` 绿；六页均有明或暗取证帧。
4. 主控读 diff：未改 `AngleDynamicView` / `SeparationAngleAtlasView`；未引入精讲 `items` 三标签。
5. SPEC Changelog + 本真源状态 → ✅；`tasks/PROGRESS.md` 回写。

---

## 五、锚点索引（读代码核实，2026-07-19）

| 锚点 | 位置 | 用途 |
|---|---|---|
| 学区入口 | `AngleHomeView.swift` ~L112–148 | 范围清单 |
| `PracticeCTA` | `AngleHomeView.swift` ~L532–571 | L6 |
| 原理节卡/灰正文/`termRow` | `AimingPrincipleView.swift` L37–89, L93–136 等 | L1–L3；B3 |
| 方法 θ / 开篇 / CTA | `AimingMethodsView.swift` L14, L37–75, L162+ | L4–L6；**v13 避让** |
| 方法管道 φ Slider | `AimingMethodsView.swift`（`trialAngleDeg` / `aimingMethods.pipe.trialSlider`） | L5 |
| 修正 `sharedControls` | `AimingCorrectionView.swift` L61–120+ | L4 抽离样板 |
| 旋转 θ + 多 CTA | `SpinAndEnglishView.swift` L10, L84–86, L313–331 | L4/L6 |
| 球感灰正文 + CTA | `BallFeelView.swift` L33–72, L18–20 | B3 |
| 对照表交互滑杆 | `ContactPointTableView.swift` L63–113, L234–285 | L4 |
| 精讲对照（只读） | `DrillTutorialView.swift` `paragraphs`/`sectionCard` | 舒适度参照，不抄三标签 |
| 排除页 | `AngleDynamicView.swift` / `SeparationAngleAtlasView.swift` | 红线：不改 |
| 巡游 | `ScreenshotTourUITests.swift` `testAngleLearningPages` ~L175 | L8 |
| SPEC 学页契约 | `tasks/UI-IMPLEMENTATION-SPEC.md` §9.3 | B1 回写 |
| **B1 节卡 + 正文样式** | `QiuJi/Features/AngleTraining/LearnDocChrome.swift` — `LearnDocSectionCard` / `LearnDocText` / `.learnDocBodyStyle()` / `.learnDocFootnoteStyle()` | L1/L2 组件；B2/B3 接壳 |
| **B1 控件条** | `QiuJi/Features/AngleTraining/LearnControlStrip.swift` — `LearnControlStrip.Theta` / `.LiveAxes` / `.ReadoutRow` | L4 组件；Binding API |
| **B1 编译引用测** | `QiuJiTests/LearnDocChromeCompileTests.swift` | B1 完成标准 3 |
| SPEC 文档学页壳 | `tasks/UI-IMPLEMENTATION-SPEC.md` §9.3.1 | B1 契约 |

---

## 六、依赖与排期

- **前置**：D-v14-1～3 ✅；D-v14-5/6 推荐默认可开工时默认采纳或改口。
- **v13**：方法页内容批；本轮 B2 方法接壳 **等 v13 ✅**。
- **几何**：本轮不改角度/坐标算法；若 B2 误触几何文件须走 `geometry-spatial-reasoning` 并升版说明。
- **并行**：不可与改同一六页的其他方案同波合并；B1 新组件文件除外。

---

## 七、版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v14.2 | 2026-07-19 | **B1 ✅（0 轮返工）**：`LearnDocChrome.swift`（`LearnDocSectionCard`/`LearnDocText`）+ `LearnControlStrip.swift`（`Theta`/`LiveAxes`/`ReadoutRow`）；SPEC §9.3.1；单测 4/0；主控落盘产物亲验 + 主树 `make build` SUCCEEDED；六学页未接壳。**阻塞：v13 B2 未收官 → 暂缓本轮 B2** |
| v14.1 | 2026-07-19 | 开工：D-v14-5/6/7 齐；模型全程 grok-4.5；波次定稿全串行；B1 委派 |
| v14.0 | 2026-07-19 | 初版：会话结论重组——六文档学页壳+控件统一；排除两沙盘页；否决三标签与蛇彩 CTA；批次 B1–B3；与 v13 串行避让 |
