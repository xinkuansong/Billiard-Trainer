# 问题集合 v27 — 全局风格统一：封面语言收敛 + 浅色组件收口 + 明暗转场衔接

> **真源声明**：本文件是「训练 / 动作库 / 练习三 Tab 及练习实操页风格割裂」这一轮需求的**真源**，供 `plan-batch-execution` / `plan-delegated-execution` 执行。高版本覆盖低版本。
>
> - **版本**：v27.0
> - **日期**：2026-08-04
> - **状态**：⚠️ W1 ✅；W2 附条件通过；W3 已实现但未通过视觉目标审核（2026-08-04，见 `tasks/ui-reviews/UR-20260804-v27全局风格.md`），待用户裁决停止或另立视觉重设计
> - **来源**：2026-08-04 会话——用户指出训练页、动作库、动作详情、练习页「风格差异很大，不像同一个专业的产品」，要求截图分析；主控实机截图六页（证据固化于 `docs/research/20260804-风格统一截图/`）+ 读源码定位原因后，用户指令 `/issue-collection-restructure`。
> - **编号约定**：条目 **E1–E8**；批次 **W1–W3**；拍板项 **D-v27-N**。
> - **与既有方案关系**：
>   - **v25（动作库重设计）尚未收官**（W3→W4 缩略图重渲、W8 精讲基建待做）。本方案不触碰静帧渲染契约（`docs/research/20260729-drill缩略图静帧契约.md`）与出片脚本；W2 的缩略图「相框」处理只改 App 侧覆层/描边，与 v25 W3 无文件交集。
>   - **v26 已预订给 legacy 精讲全量迁移**（D-v25-9），故本方案取号 v27。
>   - v25 曾声明「不做 AngleGridCard 海报封面」；本轮为新授权，练习页封面**纳入范围**（E2/E3）。
> - **排期前置**：
>   - `tasks/HUMAN-REQUIRED.md` 无 ⏳ `[BLOCKING]` 项（沿用 2026-08-03 核对结论，开工前复核）。
>   - 工作区有未提交改动（`SequenceVideoExporter`、`DiamondSystemViewModel`、脚本与 skill 文件等，见 git status），**开工前先处理干净**，否则批次验收 diff 不可读。
>   - 与 v25 剩余批次（W3/W4/W8）无文件冲突，可并行推进；若同时开工，v27 各批合并前须重跑 `make xcodegen`（新增文件时）。
> - **锚点核实日期**：2026-08-04（§五全部读代码核实）
> - **执行模型**：`cursor-grok-4.5-high-fast`（2026-08-04 用户指定，全批次统一；已回填批次表「模型」列）。

---

## 一、需求回显（语义基准，不增删）

### 1.1 用户原始诉求

1. 训练页面、动作库、动作库中动作页面（详情）、练习页面**风格差异很大，感觉不像同一个专业的产品**，分析原因。
2. 要求实机截图后结合真实视觉分析（已完成，六页截图见 `docs/research/20260804-风格统一截图/`）。
3. 认可主控分析出的收敛方向，指令整理成具体方案。

### 1.2 主控分析 → 收敛方向映射（用户已认可）

| # | 割裂来源（截图+源码双重证实） | 收敛方向 |
|---|---|---|
| 1 | 三个 Tab 封面美术色彩情绪不同：训练金+低饱和、动作库满屏深绿缩略图、练习 24 组高饱和彩虹色 | 统一封面色值规范（→E1–E4） |
| 2 | 「旧版/新版」内部生产状态角标暴露给用户 | 见 D-v27-1（本轮不动） |
| 3 | 浅色页 → 全黑 HUD 页铬件硬切、无转场预告 | 转场衔接（→E7） |
| 4 | 动作库两套 chip 实现 + 详情页非 token 写法 | 组件收口（→E5/E6） |

### 1.3 拍板项（2026-08-04 用户确认）

| 编号 | 结论 | 影响批次 | 状态 |
|---|---|---|---|
| **D-v27-1** | 「旧版/新版」角标**保留现状**，等 v26 全量迁移完成后自然消失（届时 `legacyTutorial` 分支无命中，另在 v26 收官时移除枚举与角标分支）。本轮**不改** `tutorialCornerBadge` / `DrillBadgeFilter` | 无（记录） | ✅ |
| **D-v27-2** | 封面色板**按分区收敛色系**：练习页「学 / 练 / 打 / 解」各定 1 个主色系，同区卡片只做深浅/明度变化；与训练页 `BTPlanCover` 共用同一套色值规范（同一文件定义、同一饱和度约束） | W2 | ✅ |
| **D-v27-3** | 明→暗转场衔接**做**，排最后一批（W3），可随时砍 | W3 | ✅ |

---

## 二、条目清单（现状全部读代码核实）

### E1 — 封面语言现状修正（分析结论，指导 W2）

实情比初判乐观：练习页 `AngleGridCard` 注释自述「与训练计划 BTPlanCover 同语言」（渐变+大字水印），骨架已对齐 `BTDrillGridCard` 上图下文。**真正发散的是色板与 glyph 规范**（E2/E3），不需要重造封面组件。动作库缩略图为真实台面渲染（内容即信息），**保留**，只做相框统一（E4）。

### E2 — 练习页 24 组色对 → 分区色系（W2 主体）

- `QiuJi/Core/DesignSystem/AngleCoverPalette.swift`（127 行）：学 9 组、练 6 组、打 5 组（含 SIM 卡）、解 5 组，共 24 组独立 RGB 色对，橙/紫/玫红/蓝青混杂，是「彩虹感」根源。
- 按 D-v27-2 重构：每分区定 1 个主色系（基准 Hue），区内卡片按明度/深浅阶梯取色；色值与 `BTPlanCover` 的 CoverStyle 放进**同一个规范文件**（建议合并为 `Core/DesignSystem/CoverPalette.swift`，保留旧名 typealias 以减小 diff，具体由执行批决定）。
- 分区主色系建议（执行批可微调，需在验收截图中整体和谐）：学=品牌绿系、练=金/琥珀系（呼应 btAccent）、打=蓝青系、解=石墨/深灰系；训练页 6 档等级色卡同步纳入同一饱和度约束。
- ⚠️ `AngleCoverPalette.swift` 头注释「do not invent dark variants」约定保留：色板仍 Light/Dark 共用。

### E3 — 封面 glyph 规范统一（W2）

- `BTPlanCover.swift:30-33`：glyph = `system(size: 96, weight: .black, design: .rounded)`、白 0.16；左上角期号标签（`showIssueLabel`）。
- `AngleHomeView.swift:508-511`（AngleGridCard.coverArea）：glyph = `.btCoverWatermark`（56pt black rounded，`Typography.swift:19`）、白 0.22。
- 统一为单一 token 规范：glyph 字号相对封面尺寸的比例、水印透明度、字体三者收敛（建议以 `btCoverWatermark` 家族为准扩展，杀掉 BTPlanCover 内联 `system(size:96)`）。

### E4 — 动作库缩略图相框 + 覆层徽章对比度（相框归 W2，徽章归 W1）

- 相框：`BTDrillCard.swift:140-166`（tableArea）已有底部 36pt 渐变压暗 + Dark 0.5pt 描边；统一为「所有真实渲染缩略图同款相框」规范（描边、暗角、圆角三件套），并检查 `BTDrillThumbnail`（64×64 行卡缩略图，`BTDrillCard.swift:252-264`）是否同规范。
- 徽章对比度：`BTLevelBadge.swift:54-65`——L1–L4 浅色模式用「彩字 + 同色 15% 透明底」，设计给浅卡片背景；叠在深绿台面上（`BTDrillGridCard` 左上角覆层，`BTDrillCard.swift:167-170`）对比度不足，截图实证 L1 蓝几乎不可读。修复方向：**新增覆层变体**（如 `onDarkSurface` 参数：白字 + 黑 45% 底，与 E20 特征胶囊 `BTDrillCard.swift:182-189` 同族），列表白卡场景不变。⛔ 禁止直接改默认样式反伤浅色场景。

### E5 — 三处 filter chip 实现 → 共享组件（W1 主体）

- `TrainingHomeView.swift:333-372`：`filterChipButton` + 三个配色函数（chipTextColor/chipBackground/chipBorderColor）。
- `DrillListView.swift:349-401`：`levelChipButton` + trainingStyleChip* 三函数——**与训练页逐字重复**。
- `DrillListView.swift:303-331`：`ballTypeChips`——另一套（btSubheadlineMedium / Spacing.lg / 0.5pt 描边 / 选中白字），与紧邻的等级 chip 字号、内边距、选中文字色均不同。
- 收敛：抽共享组件（建议 `Core/Components/BTFilterChip.swift`），训练页 chip 样式为基准（SPEC §6.3/§7 已锁定 `btChipActiveFill*` 色值）；三处调用方切换；球种 chip 旧样式删除。
- ⚠️ `.cursor/skills/swiftui-design-system/SKILL.md` 与 `tasks/UI-IMPLEMENTATION-SPEC.md` 若已有 chip 条目，收口后按 DR-NNN 流程同步（组件 API 变更回写）。

### E6 — DrillDetailView 非 token 写法清理（W1）

- `DrillDetailView.swift:191-239`（formationPickerSheet）：`.primary/.secondary/.tertiary` 系统色 → `btText/btTextSecondary/btTextTertiary`；`Font.system(size: 15, weight: .bold, design: .rounded)` 字面量 → 语义字体（该 sheet 强制 `.preferredColorScheme(.dark)` 是有意衔接试打页，**保留**）。
- `DrillDetailView.swift:714-727`（GoldFilledButtonStyle）：页面私有金色按钮样式，48 高胶囊 vs `BTButtonStyle` 家族形制——收编进 `BTButtonStyle`（如 `.gold`）或改用现有样式加 tint，执行批依据 `BTButtonStyle` 现有 API 定（锚点：`Core/Components/` 下 BTButtonStyle 定义，执行时核实）。
- 附带核查同文件其余字面量（如 `DrillSceneView` 覆层试打钮 `system(size:12/10)`，`Core/Scene/DrillSceneView.swift:365-390`）——属 HUD 覆层语言，允许保留，仅记录不强改。

### E7 — 明→暗转场衔接（W3，可砍）

- 切换点 1：`DrillDetailView.swift:131-135` `navigationDestination(isPresented: $showTryout)` → `PositionPlayComposerView`（全黑，`PositionPlayComposerView.swift:84-113`：`Color.black.ignoresSafeArea()` + `toolbarColorScheme(.dark)` + 黑导航栏）。
- 切换点 2：练习页 `AngleHomeView` → 各场景页（同款黑 chrome，如 `AngleDynamicView`）。
- 方向（批内先出 1 页微方案再动手，禁止直接堆动画）：利用球台渲染作共享视觉元素 + 导航栏背景色过渡（如推入时 toolbar 背景由浅渐深），或最低成本方案「详情页台面区四周加深色收边，让暗世界在浅色页内先露一角」。**红线**：不改 HUDStyle 语言本身、不引入跨页 Hero 动画框架、不动 `ShotStageMetrics` 布局常量。

### E8 — 「旧版/新版」角标（记录，本轮不动）

按 D-v27-1 保留。锚点备查：`BTDrillCard.swift:226-246`（tutorialCornerBadge / coverMetaChip）、`DrillListViewModel.swift:57-79`（DrillBadgeFilter）。**移交 v26**：v26 收官验收标准中应含「全库无 legacy 精讲后，移除 `legacyTutorial/modernTutorial` 枚举与角标分支，统一为『精讲』标」。

---

## 三、批次表

| 批次 | 范围 | 主要文件 | 量级 | 依赖 | 模型 |
|---|---|---|---|---|---|
| **W1 浅色组件收口** ✅ | E5（共享 chip 组件 + 三处切换）+ E6（详情页 token 清理）+ E4 徽章部分（BTLevelBadge 覆层变体） | 新建 `Core/Components/BTFilterChip.swift`；`TrainingHomeView.swift`、`DrillListView.swift`、`DrillDetailView.swift`、`BTLevelBadge.swift`、`BTDrillCard.swift`（换用覆层变体一行） | 中 | 无 | `cursor-grok-4.5-high-fast` |
| **W2 封面色板分区收敛** ✅ | E2（色板重构）+ E3（glyph 规范）+ E4 相框部分 | `AngleCoverPalette.swift`（重构/合并）、`BTPlanCover.swift`、`AngleHomeView.swift`（AngleGridCard）、`Typography.swift`（token 扩展）、`BTDrillCard.swift`（相框规范） | 中大 | W1（TrainingHomeView/BTDrillCard 同文件，避免并行冲突） | `cursor-grok-4.5-high-fast` |
| **W3 明暗转场衔接（可砍）** | E7（先微方案后实现） | `DrillDetailView.swift`、`AngleHomeView.swift` 路由处、`PositionPlayComposerView.swift` chrome（不动布局常量） | 小中 | W1、W2（验收截图基于新样式） | `cursor-grok-4.5-high-fast` |

依赖图：`W1 → W2 → W3`（纯串行；W3 可砍）。

> 会话粒度说明：W1/W2 各为一次会话可完成并验收的量；三批合一会超载（涉及 9+ 文件、两类验收方式）。E8 零改动不占批次。

---

## 四、各批次完成标准

### W1
- [x] `make build` 通过（`cd scripts && make build`）。
- [x] 三处 chip 调用方（训练页 filter、动作库等级、动作库球种）视觉一致：同字号、同内边距、同选中态（模拟器截图训练页 + 动作库并列取证，Light/Dark 各一）。
- [x] `DrillListView` 中 trainingStyleChip*/chipActive* 私有配色函数与 `TrainingHomeView` 中 chip 配色函数删除，仅剩共享组件。
- [x] `DrillDetailView` 无 `.primary/.secondary/.tertiary` 前景色、无 `Font.system(size:` 字面量（HUD 覆层 `DrillSceneView` 除外）；`GoldFilledButtonStyle` 收编或替换。
- [x] 动作库网格卡左上角等级徽章在深绿台面上可读（截图取证，重点 L1 初级）；列表白卡场景（PlanList 等）徽章外观不变（截图取证）。
- [x] Lint 干净；变更提交到分支。
- [x] 组件 API 变更按 DR-NNN 回写 `UI-IMPLEMENTATION-SPEC.md` Changelog 与 `swiftui-design-system/SKILL.md`。

### W2
- [x] `make build` 通过。
- [x] 练习页四分区封面各自成色系、区内深浅阶梯（模拟器截图练习 Tab 全滚动取证）。
- [x] 训练页海报卡与练习页封面同一色值规范文件、同一 glyph token（代码断言：`BTPlanCover` 无内联 `system(size: 96)`；两组件引用同一 palette 定义）。
- [x] 训练 / 动作库 / 练习三 Tab 并排截图（Light/Dark 各一组）整体色彩情绪协调——主控人工验收项。**返工 1 轮**后通过：初版全分区共用明度公式，致练区（金 hue 40°）深档落入泥褐、解区首档过浅使白水印不可辨；改为按分区独立 `ZoneLadder`（金系明度地板 ≥0.44、深度由饱和承担；石墨起点压暗）+ 水印按 sRGB 相对亮度 `|ΔL| ≥ 0.14` 自适应白/深，`CoverPaletteContrastTests` 4/0 守护。
- [x] 动作库缩略图相框（描边/暗角/圆角）网格卡与 64×64 行卡同规范（网格截图取证；行卡入口为空态，以两侧同走 `.btThumbnailFrame` 代码断言替代）。
- [x] 不触碰 `scripts/` 出片脚本与静帧 PNG 资产（git diff 断言）。
- [x] Lint 干净（项目未配 SwiftLint，等价编译零 error/无新增 warning）；变更提交到分支；DR-044 回写。

### W3
- [ ] 批内先产出 ≤1 页微方案（转场机制 + 改动点清单），主控确认后实现。
- [ ] 详情页→试打页、练习页→场景页推入时无「白屏闪黑」硬切（录屏或连续截帧取证）。
- [ ] `ShotStageMetrics` 常量与 `HUDStyle` 色值零改动（git diff 断言）。
- [ ] `make build` 通过；Lint 干净；提交到分支。

> **2026-08-04 视觉复审**：W3 仅实现详情页 4pt `BTDarkStageMat`，练习→场景路径因放弃候选 C 未覆盖；连续帧证明目的页正常，但不能证明转场衔接有可感知改善。因此第 2 条不通过，W3 不按原目标收官。详见 `tasks/ui-reviews/UR-20260804-v27全局风格.md`。

---

## 五、锚点索引（2026-08-04 全部读代码核实）

| 锚点 | 位置 | 说明 |
|---|---|---|
| BTPlanCover 封面组件 | `QiuJi/Core/Components/BTPlanCover.swift`（117 行；glyph 30-33、CoverStyle 63-101） | 训练页海报，6 档内联色 |
| AngleCoverPalette | `QiuJi/Core/DesignSystem/AngleCoverPalette.swift`（127 行） | 练习页 24 组色对唯一真源 |
| AngleGridCard | `QiuJi/Features/AngleTraining/Views/AngleHomeView.swift:458-514` | 练习页封面卡；glyph 508-511 |
| btCoverWatermark | `QiuJi/Core/DesignSystem/Typography.swift:19` | 56pt black rounded |
| BTDrillGridCard 相框/覆层 | `QiuJi/Core/Components/BTDrillCard.swift:140-198` | 渐变压暗 152-165、等级徽章 167-170、特征胶囊 182-189、精讲角标 226-246 |
| BTDrillThumbnail 行卡缩略图 | `QiuJi/Core/Components/BTDrillCard.swift:252-264` | 64×64，仅描边 |
| BTLevelBadge | `QiuJi/Core/Components/BTLevelBadge.swift`（88 行；配色 31-65） | 15% 透明底为深底对比问题根源 |
| 训练页 chip | `QiuJi/Features/Training/Views/TrainingHomeView.swift:333-372` | 基准样式（SPEC §6.3/§7 锁色值） |
| 动作库等级 chip | `QiuJi/Features/DrillLibrary/Views/DrillListView.swift:349-401` | 与训练页逐字重复 |
| 动作库球种 chip | `QiuJi/Features/DrillLibrary/Views/DrillListView.swift:303-331` | 旧样式，待删 |
| formationPickerSheet | `QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift:191-239` | 系统色 + 字面量字体 |
| GoldFilledButtonStyle | `QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift:714-727` | 页面私有按钮样式 |
| 明→暗切换点 1 | `QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift:131-135` | push PositionPlayComposerView |
| 暗页 chrome | `QiuJi/Features/PositionPlay/Views/PositionPlayComposerView.swift:84-113` | 黑底 + dark 工具栏 |
| HUDStyle（红线，只读） | `QiuJi/Core/DesignSystem/HUDStyle.swift` | panelBackground 禁改（19-20 注释） |
| DrillBadgeFilter（E8 备查） | `QiuJi/Features/DrillLibrary/ViewModels/DrillListViewModel.swift:57-79` | 本轮不动 |
| 截图证据 | `docs/research/20260804-风格统一截图/`（6 张） | 训练/动作库/详情/试打/练习/练习内页 |

## 六、不做清单

- ⛔ HUD「仪表玻璃」语言本身（`HUDStyle.swift`）——双语言是既定架构决策，本轮只做切换衔接。
- ⛔ 「旧版/新版」角标与筛选（D-v27-1，移交 v26 收官）。
- ⛔ 动作库缩略图静帧重渲、出片脚本、`ballScale`（v25 W3/W4 管辖）。
- ⛔ 页面骨架/信息密度重排（动作库侧栏、训练页版式均不动——密度断层问题记录在案，另立需求再做）。
- ⛔ 记录 Tab、我的 Tab（用户本轮未提）。

## 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v27.0 | 2026-08-04 | 初版：截图取证 + 源码核实后拆 E1–E8 → W1–W3；D-v27-1~3 拍板落定 |
