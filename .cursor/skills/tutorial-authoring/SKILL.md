---
name: tutorial-authoring
description: 把走位序列 JSON（PositionPlaySequence）转写成 drill 图文精讲（tutorial 应用课模板），含理论引用的教练话转写。Use when 根据击打序列生成/修订精讲、写「第N杆」逐杆节、或需要把 16.billiard_theory 定理转成正文解释。
---

# Tutorial Authoring Skill — 序列 → 图文精讲

## 你要做的事（一句话）

读一条走位序列 JSON，跑事实提取脚本，看配图，把事实**组织成中文教练话**，写回 drill JSON 的 `tutorial` 字段。你只做语言组织，**不做任何几何计算**。

## 为何而写（总原则，后面所有规则都从这里推出）

用户打开精讲页时，手上**已经有**：详情页动画演示、上手试打、每杆配图（图上有轨迹线、打点盘、力度读数）。

> **所以文字的唯一价值，是提供这些看不出来的东西**：为什么这么打、怎么判断自己做对了、错了是哪里错、这一杆在整体训练里占什么位置。

三条推论，构成本技能的骨架——遇到规则冲突或本文件没写到的情况，回到这三条判断，不要机械套模板：

1. **凡是已被呈现过的，文字再说一遍就是负价值。** 图上看得见母球从哪走到哪，文字重述位置轨迹只是占用注意力。→ §不重复原则
2. **要知道该写什么，先得知道图已经说了什么。** 所以读图是写作的前置条件，不是事后校验。→ 第 0 步
3. **每杆平均用力 = 没有重点。** 全组必须有一个明确的胜负手，其余相应压缩。→ §重点指认

⛔ **反面典型**：「母球在 1 号球右上方，向下中袋有自然切角；用轻高杆让母球继续前行，停到 2 号球右下方。」——三句话全是图上一眼可见的信息，读者看完不知道为什么值得练。这叫**几何转述**，是本技能最常见的失败模式，不予验收。

## 路径地图（先记住，最容易搞混的地方）

以下均为**仓库根目录**（`/Users/song/projects/13.billiard_trainer/`）下的相对路径。三个长得像的目录职责完全不同，⛔ 禁止混用：

| 路径 | 是什么 | 你怎么用 |
|------|--------|----------|
| `content/position_play/sequences/*.json` | **走位序列 JSON（真相源，编排台录制）**。文件名以 drill id 开头，如 `drill_c042__…-初级蛇彩走位 · 球形1-5杆.json` | **唯一输入**。喂给 digest 脚本 |
| `QiuJi/Resources/Drills/<category>/drill_xxx.json` | **drill 内容 JSON（App Bundle）** | **唯一写回目标**（只改其中 `tutorial` 字段） |
| `QiuJi/Resources/DrillTutorials/*.png` | 精讲配图（出片产物落位后） | 只确认存在 + 引用文件名，不生成 |
| `QiuJi/Resources/Videos/<drillId>/*.mp4` | 详情页示范视频 | 不归你管，缺了列待办 |
| `build/position_play_export/seq_<id8>/` | 出片管线原始产物（未落位） | 只在缺图时告知用户来源；核对轨迹表述时看这里的 `sNN_still.png`/`sNN.mp4` |
| `build/position_play_sequences/` | 出片收件箱（临时） | ⛔ 不读不写 |

⛔ **序列 JSON 只在 `content/position_play/sequences/`，不在任何 `Resources/` 目录下**。`QiuJi/Resources/` 下没有序列文件；如果在那里找序列，说明你走错了。找不到序列文件时用：

```bash
ls content/position_play/sequences/ | grep <drillId>
```

## 总流程（严格按顺序，禁止跳步）

```
第0步  意图锚定 + 读图：产出「意图卡」，并逐张看图（划定文字边界 + 图健康检查）
第1步  跑脚本拿事实清单（唯一几何事实来源）
第2步  检查事实清单里的红灯（有红灯 → 停下报告，不写精讲）
第3步  确认配图/视频资产存在
第4步  按模板逐节填写（§模板 + §写作公式 + §不重复原则 + §重点指认）
第5步  跑自查清单，逐条回显结果
第6步  写回 drill JSON（QiuJi/Resources/Drills/<category>/drill_xxx.json，只改 tutorial 字段），验证 JSON 可解码
```

**三条平行的真源，不可互相替代**：

| 真源 | 管什么 | 缺了会怎样 |
|---|---|---|
| 事实清单（digest 脚本） | 发生了什么：几何、参数、落点 | 编造几何 |
| 意图卡（元数据 + profile） | 为什么值得练：训练主题、球形设计、本杆角色 | 写成流水账 |
| **配图**（逐张看） | 用户已经看到了什么 | 与图重复、或与图矛盾 |

## 第0步：意图锚定 + 读图（写作前必做）

### 0a. 读图（先做这个，它决定文字该写什么）

**逐张看**开局图与每一杆的图，用 Read 工具直接读 PNG（可以看图，不要只 `ls` 确认存在）：

```bash
ls QiuJi/Resources/DrillTutorials/ | grep <drillId>        # 已落位配图
ls build/position_play_export/ | grep <drillId>            # 未落位的原始产物（含 sNN.mp4 可看轨迹动态）
```

#### 读图的三个目的（按重要性排序）

1. **划定文字边界**——图上一眼可见的（球在哪、线怎么走、打点力度读数），文字**不再重述**。这是「不重复原则」的落地前提。
2. **图健康检查**——图坏了而文案照写，是本项目已发生的事故（见下）。
3. **发现疑点，触发回查**——图文感觉对不上时，回事实清单核对。

#### ⛔ 看图的边界（与 `geometry-spatial-reasoning` 铁律的切分，必须精确理解）

| 能做 | ⛔ 不能做 |
|---|---|
| 判断图上**有没有**某个对象（几颗球、有没有线） | 目测角度、距离、切球厚薄 |
| 判断图文**是否矛盾**（图注说三颗球、图上零颗） | 从图上读出方位词写进正文 |
| 发现疑点 → **回事实清单核对** | 发现疑点 → 按目测结论改写正文 |

一句话：**看图用于证伪，不用于取值。** 所有几何断言仍只能来自事实清单。

#### 图健康检查清单（逐项核对，别笼统「看一下」）

- [ ] **球数对不对**：图上目标球颗数与序列一致。
- [ ] **跨图连贯**：开局图的母球位置 == 第一杆图的母球位置；第 N 杆的落点 ≈ 第 N+1 杆的母球起点。
- [ ] **图注可兑现**：caption/正文提到的每个对象，图上真的存在。
- [ ] **HUD 与文字同语言**：图上 HUD 若显示「高 43% · 右 1%」而正文写「中上约一个皮头」，属已知问题（v25 E11），正文**维持档位语言**，并在交付说明中记一笔图待重出。

> **真实事故（v25 E7，写进技能以防重演）**：`drill_c042_initial.png` 上三颗彩球一颗都没渲染、只有母球，且母球位置与 `s01` 图对不上（说明可能根本不是本序列的产物）；而 JSON 图注写着「1、2、4 号球沿中线从右到左排开」。**当时写精讲的人没看图**。这一条检查若执行，事故不会发生。

#### 方位词：用参照物，不用绝对方位

当前配图是**竖版**（横向台面旋转 90°），而正文用的是横向坐标系的方位词。用户读「母球在 1 号球右上方」时，得在脑子里把图转 90° 才能对上——这是 v25 E9 的教学后果。

在图的方向统一之前（v25 W5）：

- ✅ 优先用**参照物**表述：「停在 2 号球与下库之间约一颗球」「贴近上库」「越过中线」。
- ⚠️ 绝对方位词（左上/右下）**可以用**（事实清单就是这么给的，不许自己改），但尽量搭配一个参照物让读者能对上图。
- ⛔ 不要为了「对上竖图」而自行把方位词旋转改写——那是篡改事实清单。

### 0b. 读意图源（都是现成的，不要凭空想）

```bash
cd /Users/song/projects/13.billiard_trainer
ID=drill_c042
# ① drill 元数据 —— 「这个 drill 在练什么」的第一手真源
python3 -c "
import json,glob
d=json.load(open(glob.glob('QiuJi/Resources/Drills/*/${ID}.json')[0]))
for k in ['nameZh','category','subcategory','level','difficulty','description','coachingPoints','standardCriteria','sets']:
    print(k,'=',json.dumps(d.get(k),ensure_ascii=False))
"
# ② 球形变量档案 —— 「这个球形为什么这么摆」的真源（仅部分 drill 有）
ls content/drill_profiles/${ID}.profile.json 2>/dev/null && python3 -c "
import json
p=json.load(open('content/drill_profiles/${ID}.profile.json'))
print('钉死变量:',json.dumps(p['fixedVariables'],ensure_ascii=False,indent=1))
for v in p['targetVariables']: print('目标轴',v['name'],'档位',v['levels'],'理由',v['rationale'])
for v in p['conditionVariables']: print('条件轴',v['name'],'理由',v['rationale'])
for f in p['formations']: print(' 球形',f['id'],'难度#',f['difficultyRank'],f['variables'],'代表性' if f.get('representative') else '')
"
```

③ 变量含义与训练价值：`docs/research/20260720-球形设计变量词典.md` — 每个变量有「**训练意义**」项（如 `cutAngle`：「核心厚薄训练轴，薄球有效命中窗口随 θ 增大按 cosθ 收缩」），是把变量取值翻译成教学语言的现成语料。⛔ 只取「训练意义」和「语义」，几何约束项照抄不重算。

### 意图卡四问（逐条写下答案，回显给用户）

| # | 问题 | 依据来源 | 无依据时 |
|---|---|---|---|
| 1 | **训练主题**：这个 drill 在练什么能力？ | `nameZh` / `subcategory` / `description` / `coachingPoints` | 必有，不存在无依据情况 |
| 2 | **球形设计意图**：这个球形为什么这么摆？钉死了什么、变的是什么、难在哪？多球形还要答：它与同 drill 其他球形差在哪个变量？ | profile 的 `fixedVariables` 理由 / `targetVariables[].rationale` / 本球形 `variables` + `difficultyRank` + 变量词典「训练意义」 | 无 profile 时**降级**，见下方红线 |
| 3 | **本杆打法意图**：这一杆为什么用这个杆法？在训练主题里承担什么角色（主训点 / 铺垫 / 收尾 / 容错最低的一杆）？ | 事实清单的 params + 落点 + 理论参考，结合问 1 | 参数不支持就不写角色判断 |
| 4 | **达标含义**：`standardCriteria` 在教学上代表什么水平？失败通常卡在哪一环？ | `standardCriteria` / `sets` / 事实清单最难那一杆 | — |

### 意图红线

1. ⛔ **无 profile 不得虚构设计意图。** 当前仅 11 条 drill 有 profile（`c016`/`c018`/`c020`/`c021`/`c053`/`c073`–`c078`）。其余 drill 只能从 `description` / `coachingPoints` / 实测参数能支撑的范围内写，**禁止编造「设计者当初为什么这么摆」**。写不出问 2 就在交付说明里如实说「本 drill 无变量档案，球形设计意图未溯源」。
2. ⛔ **禁止空泛意图。** 每句意图陈述必须能指回一个具体的变量 / 参数 / 元数据。「提升整体球感」「培养手感」「增强信心」这类不可证伪的话一律删除。
3. ⛔ **多球形禁止复制同一段意图。** N 个球形的「技术原理」若能互换而不违和，说明没写出球形差异——这是多球形精讲最常见的塌陷。每个球形必须点出**本球形相对其他球形变了哪个变量、因此难在哪**（如 c053 的 A1 是 15° 右切近距、A2 是 15° 左切远目标球，同切角不同 `side`/`dtp`）。
4. ⛔ **意图不得与实测矛盾。** 若元数据说「练轻力控制」而事实清单里 velocity 偏大，如实上报冲突，不硬圆（FL-027）。

## 第1步：跑脚本

在仓库根目录执行（序列文件在 `content/position_play/sequences/`，见§路径地图；文件名含空格和中点号，**必须加引号**）：

```bash
cd /Users/song/projects/13.billiard_trainer
ls content/position_play/sequences/ | grep <drillId>   # 先列出该 drill 的序列（多球形会有多条）
python3 scripts/tutorial_digest.py "content/position_play/sequences/<完整文件名>.json"
```

输出是「事实清单」：开局布局、逐杆 params/打点读数/力度/相对方位/切球角/落点/与下一杆的衔接/理论参考、终局。

⛔ **铁律：精讲里所有几何事实（方位、距离、角度、落点区域）只准抄事实清单，禁止自己看坐标心算，禁止看渲染图目测。** 这是本项目 `geometry-spatial-reasoning` 技能的核心铁律在内容写作上的落地。清单里没有的几何事实 = 不准写。

> ⚠️ **与第 0a 步「必须读图」不矛盾**，两者分工明确：读图**证伪**（图上有没有这个对象、图文是否矛盾），事实清单**取值**（方位、距离、角度到底是多少）。看图看出疑点 → 回清单核对；⛔ 永远不要把目测结果直接写进正文。

清单**不含轨迹细节**（吃几库、路径形状、是否走弧线）。要写「一库走位」「贴库滚动」这类表述，必须先看对应出片（`build/position_play_export/seq_<id8>/sNN.mp4` 或 `sNN_still.png` 预告线）确认，看不到就不写。

## 第2步：红灯检查（任一命中 → 停止写作，如实报告）

| 红灯 | 处理 |
|------|------|
| 清单出现「含母球 scratch ⚠️」 | 序列本身有缺陷（示范杆母球进袋），报告用户重录，不写精讲 |
| 某杆目标球未进袋（无「本杆进袋」行且非自由球） | 同上，报告用户 |
| 序列文件不存在 / 解码失败 | 报告，不要试图手动解析 JSON |
| 用户要求「从 drill 的 shotIntent 反推序列再写精讲」 | 拒绝并说明：红线规定示范击球以编排台人工录制为准（2026-06-13 用户拍板），存量 shotIntent 精度不可靠 |

## 第3步：资产确认

> 图的**内容**在第 0a 步已经看过了；这一步只管**命名与存在性**（`image` 字段该填什么、文件在不在）。

精讲配图约定（真源：content-engineering SKILL §媒体配套）：

| section | image 字段值 | 文件应在 |
|---------|--------------|----------|
| 开局与击球顺序 | `<drillId>_initial` | `QiuJi/Resources/DrillTutorials/<drillId>_initial.png` |
| 第N杆 | `<drillId>_sNN`（NN 两位数：s01, s02…） | `QiuJi/Resources/DrillTutorials/<drillId>_sNN.png` |

多球形 drill（一个 drill 多条序列）图名带球形序号，以目录中实际文件为准。用 `ls` 确认存在；**缺图不虚构文件名**——在交付说明中列出缺哪些图、对应出片产物路径（`build/position_play_export/seq_<id8>/initial.png`、`sNN_still.png`），提醒用户先跑 `make position-export` 落位。图缺但事实清单齐全时，可以先写文字、image 字段留待补图（在交付说明中明确标注）。

## 不重复原则（总原则推论 1，三个方向都要查）

信息只在**第一次有价值的地方**说一次。三个方向的重复都算缺陷：

| 方向 | 表现 | 怎么改 |
|---|---|---|
| **跨媒介**（字 vs 图） | 文字重述图上一眼可见的球位、轨迹形状、打点百分比 | 删掉描述，改写这一杆的**意图与判据** |
| **跨杆次**（第 N 杆 vs 第 N+1 杆） | 每杆都从头解释一遍同一个原理 | 原理在**首次出现处**讲透；后续杆只写**本杆的差异与新增难点** |
| **跨 drill**（本 drill vs 同类 drill） | `c032` 中台角度球 / `c033` 远台斜线 / `c052` 三分点角度，技术原理写成同一段「角度球要判断厚薄」 | 写作前扫一眼同 `category`/`subcategory` 的邻近 drill 精讲，本条只写**它独有的那一层** |

**判据（可自测）**：把某段文字删掉，读者是否损失了信息？没有损失 = 该删。

⚠️ 但**不重复 ≠ 惜字如金**。判据不是字数，是增量信息密度。该讲透的原理（首次出现的杆法机理、反直觉物理）要讲够。

## 序列形态判定：走位链 vs 独立阶梯（DR-062，写逐杆节前必判）

同为多杆序列，有两种本质不同的形态，**逐杆节的写法完全不同**。写作前先按客观判据判定：

| 形态 | 客观判据（读事实清单，不靠感觉） | 逐杆节写法 |
|---|---|---|
| **走位链** | 每杆落点因果决定下一杆（目标球位置/距袋逐杆变化，「为下一杆创造的局面」有实义） | 每杆全文三条目（本节以下各规则的默认对象） |
| **独立阶梯** | 每杆独立重置：目标球距袋恒定 + 袋口不变 + 杆法力度锁死，只有一个变量在扫（切角/距离/塞量/力度档） | **混合式**（见下），⛔ 禁止逐杆全文解说 |

> 原理：精讲的信息量应与**变量数**成正比，不与**杆数**成正比。杆数只是采样密度；对单变量阶梯逐杆写「为什么/怎么打」，每节必然同构，全是冗余（W1 c032/c053 教训）。

### 独立阶梯的混合式结构（DR-062）

1. **`技术原理`**：讲透这一个变量（是什么、范围、失败模式如何随它变化），一次讲完；后面不再复述。
2. **`开局与击球顺序`**：摆位说明 + **阶梯总表**（每档一行：档位、变量值、一句差异），让用户一眼看到整条阶梯。
3. **逐杆节保留**（配图与 `params` 依赖它，C4 门禁按杆数对齐），但分两档写：
   - **锚点档**（全组 2–4 杆）写完整三条目：入门档（建立基准）、教学锚点档（如半颗球 ≈30°）、上限档、以及带缺陷/特殊情况的档。
   - **中间档**只写一条 `自检`（一句话：进袋判据 + 母球落点参照，抄事实清单）；⛔ 不写「为什么/怎么打」——它们与上一锚点档无差异。
4. **多球形阶梯**：从第 2 个球形起，`技术原理` 首段必须先答三问——**与前一形共享什么 / 变了什么（数值对数值）/ 为何按此顺序练**，答完再讲本形独有内容。

**⚠️ 只写差异有个下限（多球形必读）**：多球形用 Picker 切换，用户**可能直接点到后面的球形**，不一定从第一个读起。所以——

- 差异描述必须**自带参照**：写「切角从 15° 加到 30°，球厚从 3/4 减到 1/2」，⛔ 不写「角度更大了」。
- 每个球形**至少能独立回答「本档练什么」**，不能只剩一句「同球形1，但更远」。
- 参照对象要写明是哪一档（「比球形1远约 2 颗球」），⛔ 不写「比上一档远」——用户不一定知道上一档是哪个。

## 语域：面向用户写作，不面向验收写作（DR-062）

精讲正文的读者是**台球学员**，不是主控/验收者。以下内部词汇 ⛔ 禁止出现在任何面向用户的字段（`description` / `coachingPoints` / `standardCriteria` / `tutorial` 全部正文）：

- **仓库工作流词**：退役/retired、profile、序列文件、manual01、digest、出片、录制、待重录、W 批次号。
- **验收自证话术**：「与已退役的旧 X 球形不同」「现行示范」这类向验收者证明你了解历史的句子——用户没有历史包袱，直接写现在是什么。
- **数值口径**：力度只写数值（「力度 2.1」），打点只写事实（「中心球」「低杆一格」）；⛔ 不堆叠「轻力 2.1 档」式的标签+数值混合体。定性词（轻/中/重）只允许在 `技术原理` 里出现一次用于建立感觉。
- **示范缺陷的用户视角写法**：序列有 scratch 等缺陷时，只写用户需要知道的一句（「示范动画里母球也落袋了，这不是标准答案——你以母球留台为合格」）；内部的待重录/登记信息写进**交付说明**，不进正文。

## 重点指认（总原则推论 3；仅走位链适用，DR-062 限定）

⛔ **每杆平均用力 = 没有重点。** **走位链**全组必须显式指认**一个**胜负手，并让篇幅体现出来。

- **怎么找**：客观依据优先——事实清单里容差最小/落点要求最苛刻的那一杆、意图卡问 4 答出的「失败通常卡在哪一环」、`standardCriteria` 最难兑现的环节。⛔ 不靠感觉指定。
- **怎么写**：在该杆的 `为什么` 里明说它是全组关键（如 c042「这一杆是全组精度要求最高的一杆，落点对力度敏感，先求稳」），其余杆相应压缩。
- **诚实的低价值判断也是有用信息**：某杆确实只是过渡，就明写「这一杆是铺垫，难度低于第二杆，重点是别走过头」，一两句带过即可。⛔ 强行给每杆编出深意，反而稀释真正的重点。
- ⛔ **独立阶梯没有胜负手**：各档独立，最薄/最难一档只是「上限档」，不存在决定链条存亡的一杆。阶梯型正文禁用「胜负手/关键档」措辞，重点分配走混合式结构（锚点档全文、中间档一句）。

## 模板：应用课 Section 骨架（固定顺序，不增不减不换名）

真源：ADR-P11-14 / content-engineering SKILL；样板：`QiuJi/Resources/Drills/positioning/drill_c042.json`（写之前先读一遍对齐语感）。

| # | title（一字不差） | 用什么容器 | 配图 |
|---|------------------|-----------|------|
| 1 | `技术原理` | `content`（1–2 段）——**意图卡问 1 + 问 2 的落点** | 无 |
| 2 | `开局与击球顺序` | `content`——**必须回答「为什么这样排」** | `<id>_initial` + caption |
| 3..N | `第N杆：X号球 · 袋口`（袋口用中文名：左上袋/右上袋/左下袋/右下袋/上中袋/下中袋） | `items` 3条 + `params`，`content` 填 `""` | `<id>_sNN` + caption |
| N+1 | `常见错误与纠正` | `items`（label=错误名，2–4条） | 可选 |
| N+2 | `进阶练习` | `content`（降阶 + 升阶各至少一个方向） | 无 |

### 前两节的内容要求（意图承载区，⛔ 别写成摆球说明）

样板 `drill_c042` 的这两节是达标线，写之前先读：

- **`技术原理`**：第 1 段点明训练主题（「蛇彩是走位综合训练的经典方法…核心原理是**角度管理**」）；第 2 段说明本组示范如何体现该主题（「用最少的杆法完成三杆连续走位，体现『力度优先于杆法花样』」）。⚠️ **`coachingPoints` 每一条都要在正文里有落点**——它是产品侧已确定的教学重点，逐条核对，遗漏视为缺陷（c042 三条全部被吸收）。多球形还须点出本球形与其他球形的差异变量（意图红线 3）。
- **`开局与击球顺序`**：先客观描述摆位与顺序，然后**必须**有一段「为什么这样排」——把顺序与袋口的选择理由讲出来（c042：「同一颗球打上袋还是下袋，取决于母球停在它的哪一侧——顺序和袋口是一起规划出来的，不是逐杆临时决定」）。只写「1、2、4 号球从右到左排开，依次进袋」就是摆球说明，不是精讲。

- 数字标题用中文：第一杆、第二杆…（对齐 drill_c042 样板）。
- `content` 支持 `\n\n` 分段和 `**加粗**`；每张图配一句话 `caption`。
- `params` = `{"spinX": …, "spinY": …, "velocity": …}`，**从事实清单「params（照抄进 JSON）」行原样抄**，不四舍五入不改写。
- 多球形 drill：`tutorial.formations: [{id:"f1", title:"球形 A：…", sections:[…]}]`，每球形一套完整骨架；单球形用 `tutorial.sections` 平铺。二选一，不能同时存在。

## 逐杆节 items 三条：写什么（⚠️ 不是「怎么造句」）

label 固定为 `为什么` / `怎么打` / `自检`（App 渲染有专属配色，一字不差）。

> ⚠️ **下面的句式是参考，不是填空模板。** 验收查的是**信息是否齐全**（内容契约），不是**句子是否长这样**（形式契约）。照着句式硬填，产出的正是那种一眼假的机械文案。允许——也鼓励——按这一杆的实际情况自然组织语言，甚至调整三条之间的信息分配（见下方 `为什么` 末条）。

### `为什么`（两层意图，缺一层即不合格）

**必须包含的信息**（形式自定）：

| 层 | 回答什么 | 来源 |
|---|---|---|
| **战术意图** | 这一杆要达成什么：用什么杆法、把母球送到哪、**为下一杆创造了什么** | 事实清单【为下一杆创造的局面】【为下一杆留出的切球角】 |
| **训练意图** | 这一杆为什么值得练：在本 drill 主题里的角色（主训点 / 铺垫 / 收尾 / 全组容错最低）、难在哪个具体维度 | 意图卡问 3 |

参考句式（可完全不用）：「用{杆法}把母球送到{落点，带参照物}，为下一杆留出{什么}——这一杆是{角色}，难在{维度}。」

- 非最后一杆**必须**写出下一杆需要什么（反向规划的落地）；最后一杆写不需要再留位置。
- ⛔ **只有战术层 = 几何转述，不予验收**——这是本技能最常见的塌陷（见 §为何而写 的反面典型）。
- ⛔ 按 §不重复原则，母球位置/轨迹形状**图上已有的不重述**；这一条的篇幅应该花在「为什么」上，不是「是什么」上。
- 两层可以合成一段自然的话，也可以分散到三条里——c042 第一杆把训练意图放在了 `怎么打`（「这一杆是全局精度要求最高的一杆」），完全可接受。**要求是这层信息存在，不是它在哪一条。**

### `怎么打`（要领，不重复数字）

- 打点用身体语言：「中上约一个皮头」「低杆加足」「中心击球」——**禁止**出现 spinX/spinY 数值或百分比读数（params 行已经展示了）。
- 力度用档位语言（轻推/轻/中/中大/大力）+ 节奏描述（「轻推出杆、送透不急收」）。
- 事实清单「理论参考」提示了反直觉物理时（如短距高杆更贴切线），在这条里用教练话点破。

### `自检`（可证伪）

> ⚠️ **这一条是「参考句式」原则的例外，句式该守就守。** 理由：图给的是标准答案，**用户唯一看不出来的就是「我怎么知道自己偏了」**——按 §为何而写，这里是文字增量价值最高的地方，宁可结构固定也要保证可证伪。三条 items 里这条最不该省。

> 母球应停在{参照球/库边}的{方位}约{N颗球}距离，{与下一杆袋口的角度关系}；若停在{错误位置}，说明{力度/打点}偏了。

- 落点全部抄事实清单的 after 落点行和【为下一杆…】行；方位尽量带参照物（§第0a步）。
- 必须给出至少一个「若…则说明…」的失败征兆。
- 最后一杆的自检写整组完成判据（对齐 drill 的 `standardCriteria`）。

## 理论引用：三层渐进式披露（SOP-A，⛔ 禁教材腔）

事实清单每杆给出「理论参考」（T 编号 + 一句话）。你的职责是把它**翻译成教练话**放进正文：

- ⛔ **正文绝对禁止出现定理编号或术语引用**。禁写：「根据 T03 切线法则」「依据 30° 法则」「切线法则告诉我们」。
- ✅ 把原理说成「就是这样」的常识。对照表：

| 清单里的提示 | 正文教练话写法（示例，可改写） |
|--------------|------------------------------|
| T03 切线法则 | 「母球吃球后总是先沿垂直于两球连线的方向走，之后才被旋转拉弯」 |
| T01 30°法则（自然滚动） | 「中杆滚动球吃半颗球厚度时，母球大致偏开 30° 左右」 |
| T02 90°法则（stun） | 「定杆吃球，母球沿切线直角分开」 |
| 短距高杆贴切线 | 「母球到目标球距离短，高杆来不及转成滚动，分离会比想象中更贴切线——这是正常物理，照参数打即可」 |
| 直线球无切角 | 「直线球没有切角，母球只能沿击球线前后走，走位全靠跟进量/缩杆量」 |
| T05 反向规划 | 「顺序和袋口是一起规划出来的，不是逐杆临时决定」「这一杆的落点是从下一杆倒推出来的」 |
| T06 关键球 | 「这是全局真正的胜负手——它决定收尾那杆好不好打」 |
| T09 最少加塞 | 「能用中杆走到的位置绝不加塞；这里不得不用塞，是因为…（写出具体原因）」 |

- 深挖原文（可选）：`/Users/song/projects/16.billiard_theory/contracts/theorem-tags.json` 的 `statement_one_liner` / `common_errors` 字段；`common_errors` 是「常见错误与纠正」节的优质素材来源（挑与本序列杆法相关的 2–4 条，改写成本局面的说法）。
- `theoremIds`/`moduleIds` 数据字段目前**未进** drill schema，不要往 JSON 里加。

## 「常见错误与纠正」与「进阶练习」

- 常见错误：label 用错误名（如「走成直线球」「力度过大」），text = 现象 + 后果 + 纠正方法。优先从本序列的具体风险出发（哪杆最容易失误、失误后局面如何），再用 theorem-tags 的 `common_errors` 补充通用项。
- 进阶练习：至少一个降阶（简化：减球数/放宽落点）+ 一个升阶（加难：加球/换袋口规划/在编排台自摆对比）方向。

## 自查清单（写完必须逐条回显 ✅/❌，有 ❌ 先修复）

**读图层（第 0a 步的验收）**

1. 每张图都实际看过（不是只 `ls`）；图健康检查四项逐项过；发现的坏图已上报而非照写。
2. 图文无重复：抽查任一杆 `为什么`，其中没有「图上一眼可见即可获得」的纯位置/轨迹描述。

**意图层（第 0b 步的验收）**

3. 意图卡四问已回显；问 2 有 profile 依据或已明确声明降级；无空泛意图（「提升球感」「培养手感」类）。
4. `coachingPoints` 逐条核对：列表给出每条在正文中的落点位置，无遗漏。
5. 每杆 `为什么` 都含训练意图层；多球形/递进型的各档「技术原理」互不可替换（能互换 = ❌），差异描述自带参照且指明参照的是哪一档。
6. **全组已指认一个胜负手**，依据客观（容差/失败环节/达标难点）；其余杆篇幅相应压缩，无强行拔高。

**事实层**

7. section 顺序与命名和骨架表完全一致；逐杆节 `content == ""`。
8. 每杆 `params` 与事实清单原始数值逐字符一致（抄的是原始 spinX/spinY/velocity，不是百分比读数）。
9. 正文中每个几何断言都能在事实清单中找到对应行——逐杆列出「断言 → 清单行」对照；⛔ 无一条来自看图目测。
10. 涉及吃库/路径形状的表述已核对出片；意图与实测无矛盾（冲突已上报而非硬圆）；无「文案说留角度、数据是直线球」这类矛盾。

**表达层**

11. `怎么打` 条目不含任何 spin 数值/百分比；正文无定理编号、无「法则/原理告诉我们」句式。
12. 走位链：非收尾杆的 `为什么` 都写了对下一杆的意义；独立阶梯：锚点档/中间档分配符合混合式结构、无逐杆同构解说。`自检` 都有失败征兆。
12b. 语域检查（DR-062）：正文无内部词汇（退役/profile/manual/digest/待重录等）、力度为纯数值口径、阶梯型无「胜负手」措辞。
13. 方位表述尽量带参照物（当前配图为竖版，绝对方位词读者难对上）。

**交付层**

14. `image` 字段指向已确认存在的文件（或已在交付说明中列为待补）。
15. 写回后 JSON 语法有效（`python3 -c "import json; json.load(open('<file>'))"`）。

## 交付说明格式（最后回复用户时）

- 写了哪个 drill / 哪些球形、各几杆。
- **意图卡四问的答案** + **形态判定（走位链/独立阶梯）**；走位链附胜负手及依据，独立阶梯附锚点档选择及依据（让用户能直接判断专业性是否达标，这是最值得他看的部分）。
- **读图所见**：图健康检查结果；发现的坏图/图文矛盾（这是最可能揪出真缺陷的一项，务必如实列出）。
- 自查清单 15 条的逐条结果。
- 未验证/待办事项如实列出（缺图、待跑出片、待人工审核 H-11 等）。
- 事实清单中值得注意的点（如某杆理论参考提示了加塞但正文难以自圆其说 → 如实上报，不硬编理由）。
