---
description: 数据工程师角色。在创建或编辑 *Model.swift、*Repository.swift、*Service.swift 以及 CloudKit / LeanCloud 集成代码时激活。负责 SwiftData schema、离线优先策略、云同步逻辑。
globs: ["**/Models/**/*.swift", "**/Repositories/**/*.swift", "**/Services/**/*.swift"]
alwaysApply: false
---

## Changelog

- 2026-03-27 v0.1：初始版本

# Data Engineer — 数据工程师

## 数据分层架构

```
本地层（SwiftData）  ←→  用户数据云层（LeanCloud）
                    ←→  内容云层（CloudKit 公开库，只读）
```

**离线优先原则**：所有功能在无网络时必须可用（SwiftData 为主存储），云同步为后台补充。

## SwiftData 规范

```swift
// Model 命名：<Entity>（不加 Model 后缀）
@Model final class TrainingSession { ... }
@Model final class DrillEntry { ... }
@Model final class AngleTestResult { ... }

// ModelContainer 在 App Entry 统一初始化，通过 Environment 传递
// 禁止在 View 中直接使用 ModelContext；通过 Repository 抽象
```

**主要实体**（参照 `docs/06-技术架构.md` § 4.1）：
- `TrainingSession`：单次训练记录
- `DrillEntry`：训练中的某个 Drill 记录
- `DrillSet`：某 Drill 的单组记录
- `AngleTestResult`：角度测试单题结果
- `UserPlan`：用户当前激活计划
- `DrillFavorite`：收藏的 Drill ID

**迁移规范**：Schema 变更必须定义 `MigrationPlan`，禁止删除现有属性（改用 optional + 默认值）。

## CloudKit 公开库（只读内容）

- 用于：Drill 内容、官方训练计划（热更新，无需发版）。
- Record Type 命名：`DrillContent`、`OfficialPlan`。
- 获取策略：App 启动时后台静默刷新；本地缓存 Bundle 内 fallback JSON（`Resources/Drills/`）。
- **禁止**写入 CloudKit 公开库（仅私有库可写，但本项目用户数据在 LeanCloud）。

## LeanCloud 用户数据

- 支持三种登录（微信 OAuth / 手机验证码 / Sign in with Apple），实现见 `tasks/dependencies.md`。
- 用户私有数据对象：`TrainingSession`、`AngleTestHistory`、`UserSettings`。
- **同步策略**：
  1. 本地 SwiftData 写入成功后，异步上传 LeanCloud（后台队列）。
  2. 登录时拉取远端增量（按 `updatedAt` 对比）。
  3. 冲突解决：以 `updatedAt` 较新者为准（Last Write Wins）。
- **匿名用户**：数据仅本地，不上传；登录后触发一次性迁移上传。

## 敏感数据处理

- LeanCloud App ID / App Key **不硬编码**在源码中；通过 `Config.xcconfig` + `.gitignore` 管理。
- 用户 openid / 手机号 **不写入本地日志**（见 `observability.md`）。

## 网络错误处理

- 所有网络调用包裹 `do/catch`，失败时降级到本地缓存，**不向用户展示技术错误信息**。
- 错误状态通过 `AppError` enum 统一定义，ViewModel 转译为用户可读提示。
