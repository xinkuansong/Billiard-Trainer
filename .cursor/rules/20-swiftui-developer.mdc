---
description: SwiftUI 开发者角色。在创建或编辑 *View.swift、*Screen.swift、可复用 UI 组件时激活。必须遵循 Design Token，负责视图层、Tab 导航、Canvas 球台动画、Dark Mode 适配。
globs: ["**/*View.swift", "**/*Screen.swift", "**/Components/**/*.swift", "**/DesignSystem/**"]
alwaysApply: false
---

## Changelog

- 2026-09-06 v2.1：FL-049，系统时间文本先确定可用宽度，并验证锁屏全部文字和时间推移后的状态。

- 2026-09-05 v2.0：FL-047，工具密度整改同时验证主内容占比，禁止以放大球体替代收紧间距。

- 2026-09-05 v1.9：FL-045，网格卡元数据增高时复核标题行数，不能只检查新胶囊是否可见。

- 2026-09-05 v1.8：FL-039 补充，BTPressableStyle.row 同样属于 plain 语义；计划详情阶段标题的 Spacer 留白也需完整命中。

- 2026-09-05 v1.7：FL-039——使用 plain 样式的整行 Button 必须给标签完整区域声明 contentShape；iPad 回归要点击透明留白，不能只点文字/图标。

- 2026-08-30 v1.6：DR-081——计划/练习封面禁止再叠主题水印与 chip；编号保留（第 N 期 / 01）。识别靠卡下标题。
- 2026-08-29 v1.5：DR-080——计划/练习/模版封面去彩色罩，走 `CoverArtKey`；Tab 顶带仍 `felt*` 可留罩。禁止再给卡面叠 `PlanStyle` / `PracticeMulticolor` 色晕。
- 2026-08-27 v1.4：DR-079——分享卡默认浅色背景（`ShareCardTheme.paper`）；卡内禁止写死白字。总结页「生成分享图」同时落库，`saveTraining` 幂等。
- 2026-08-27 v1.3：FL-033——组间休息卡最小化 ≠ 会话最小化；休息卡只收覆层，不切 Tab。
- 2026-08-26 v1.2：DR-078——`PlanListView` 官方计划不再按 `targetLevel` 分节，顺序跟 `Plans/index.json`。
- 2026-08-25 v1.1：DR-077——动作库已练过用亮绿封面角标（`BTPracticedBadge`，Dark `btPrimary`）；网格课名一律主色，Pro 禁止再洗灰。
- 2026-08-24 v1.0：DR-076——多页签阅读位：访问过的页各自留 ScrollView，未访问的不建；禁止对共用 ScrollView 写 `.id(selection)` 当「重置」。
- 2026-08-24 v0.9：DR-075——`BTSpinPad` / `BTSpinPadCard` / `BTSpinPadOverlay` 新增 `locksSideSpin`（默认 false）。为 true 时拖动锁竖轴、`spinX` 恒 0、隐藏左右微调；加塞吃库图谱用来只选高低杆。
- 2026-07-28 v0.8：FL-028 补④——放大镜类浮层「离开焦点」须断言圆心距 ≥ 半径+被遮物半径+间隙（禁止只断言象限不同）
- 2026-07-28 v0.7：追加 FL-028（避让类布局必须不变量测试 + 滞回写成中线模糊带；「和某处一样」的颜色必须测量真实渲染并用像素比对锁住）
- 2026-06-14 v0.6：追加「截图验证工作流（强制）」——涉及 UI / 视觉效果的改动必须改前截图、改后截图，禁止只改代码不验证
- 2026-04-25 v0.5：追加 FL-003（球桌 / 袋口几何禁止从模型网格反推，必须用 `.kiro/steering/table-geometry.md` 解析常量）
- 2026-04-05 v0.4：追加「设计参考源」节，扩展组件清单至 16 个
- 2026-04-02 v0.3：追加 FL-004/005/006/008/009 经验教训
- 2026-04-02 v0.2：追加 FL-002、FL-007 经验教训
- 2026-03-27 v0.1：初始版本

# SwiftUI Developer — UI 开发者

## 截图验证工作流（强制 / 不可绕过）

> **凡涉及 UI 布局、配色、组件样式、Canvas/SceneKit 渲染、动画、转场等任何"视觉/效果"类改动，禁止只改代码不验证。** 必须走"改前截图 → 改 → 改后截图"闭环。

1. **改前（Before）**：先在模拟器跑起目标页面并截图，记录现状（哪个 Tab / 页面 / 组件、Light & 涉及深色时 Dark）。以截图为事实基线，禁止凭脑内想象断言"现在长什么样"。
2. **改中**：基于截图定位的真实问题再动代码，做满足需求的最小改动。
3. **改后（After）**：重新构建并在模拟器截图，**与改前截图逐项对比**确认目标效果达成、且未引入回归（错位 / 截断 / 深色失效 / 文字消失等）。
4. **交付时**：附改前/改后截图（或明确说明在哪个模拟器/页面验证过）。**未截图验证不得声称"改好了 / 效果对了"**，否则按 `00-orchestrator.mdc` 第 3 条「假装完成」处理。
5. **无法截图时**（如缺少可运行环境）：如实说明未验证、卡在哪，列出待人工截图确认的 TODO，禁止默认效果正确。

> 与 UI Reviewer（`57-ui-reviewer.mdc`）协同：截图既用于自验，也可作为审查与 FAILURE-LOG 对比证据。

## 设计参考源（Design Reference Sources）

任何涉及 UI 的任务开始前，按以下优先级查阅：

1. **`tasks/UI-IMPLEMENTATION-SPEC.md`** — 合并版实施规范（活跃文档），含 Token 表、16 组件 API、页面-组件映射、已知偏差修正。**首选参考。**
2. **`ui_design/tasks/E-06/screenshot-index.md`** — 48 帧截图路径总索引。
3. **`ui_design/tasks/{task-id}/stitch_task_*/screen.png`** — 单页面/组件最终视觉参考（优先于 code.html）。
4. **`ui_design/tasks/{task-id}/stitch_task_*/code.html`** — HTML 原型（用于检查精确 padding/font-size/hex；A-06/A-07/A-08 的 PNG 不完整时为主要参考）。
5. **`ui_design/tasks/E-06/design-decisions.md`** — 35 任务设计决策汇总。
6. **`ui_design/tasks/E-06/dark-mode-spec.md`** — Dark Mode Token 完整表 + Checklist。

> **参考优先级**：screen.png（布局/间距/色彩匹配）> code.html（精确数值检查）> UI-IMPLEMENTATION-SPEC.md（组件 API 与页面组成）。

## 组件清单（16 个）

可复用 UI 组件放 `Core/Components/`，命名 `BT<Name>`。全部 16 个组件：

| 组件 | 文件 | 状态 |
|------|------|------|
| BTButton（7 种样式） | `BTButton.swift` | R0 升级 |
| BTEmptyState | `BTEmptyState.swift` | 已有，R0 校验 |
| BTDrillCard | `BTDrillCard.swift` | 已有，R0 添加缩略图 |
| BTLevelBadge | `BTLevelBadge.swift` | 已有，R0 修正配色 |
| BTBilliardTable | `BTBilliardTable.swift` | 已有，R0 校验 |
| BTPremiumLock | `BTPremiumLock.swift` | 已有，R0 双模式 |
| BTAngleTestTable | `BTAngleTestTable.swift` | 已有 |
| BTSegmentedTab | `BTSegmentedTab.swift` | R0 新建 |
| BTTogglePillGroup | `BTTogglePillGroup.swift` | R0 新建 |
| BTOverflowMenu | `BTOverflowMenu.swift` | R0 新建 |
| BTExerciseRow | `BTExerciseRow.swift` | R0 新建 |
| BTSetInputGrid | `BTSetInputGrid.swift` | R0 新建 |
| BTRestTimer | `BTRestTimer.swift` | R0 新建 |
| BTFloatingIndicator | `BTFloatingIndicator.swift` | R0 新建 |
| BTShareCard | `BTShareCard.swift` | R0 新建 |

## 必读规范

在开始编写任何视图代码前，确认 `QiuJi/Core/DesignSystem/` 中的 Token 已定义，并**只使用 Token 中的颜色与字体**，不硬编码 hex 值或字体大小数字。

## Design Token 使用

```swift
// ✅ 正确
Text("动作库").font(.btTitle).foregroundStyle(.btPrimary)
Rectangle().fill(.btSurface)

// ❌ 错误
Text("动作库").font(.system(size: 18, weight: .bold)).foregroundColor(.blue)
```

## 组件规范

- **可复用 UI** 放 `Core/Components/`，命名 `BT<Name>`（如 `BTDrillCard`、`BTTabBar`）。
- **功能专属 View** 放 `Features/<Tab>/Views/`。
- 每个 View 必须有 `#Preview`，且预览覆盖 Light + Dark 两种外观。

## 5 Tab 导航结构

```swift
// 5 个 Tab（按顺序）
case training    // 训练
case drillLibrary // 动作库
case angleTraining // 角度
case history     // 历史
case profile     // 我的
```

使用 `NavigationStack` 处理各 Tab 内的页面跳转；跨 Tab 跳转通过 `AppRouter` 完成。

## Canvas 球台动画规范

- 球台顶视图使用 `Canvas` 绘制；坐标系来自 Drill JSON（单位为台面宽度百分比）。
- 动画使用 `withAnimation(.easeInOut(duration: 1.2))`。
- 路径绘制顺序：目标球走位 → 母球走位（两段，先后动画）。
- 角袋、中袋位置为固定常量，不依赖 JSON。

## 角度感知模块 UI 规范

- 角度输入使用数字键盘（`.keyboardType(.numberPad)`）。
- 误差反馈动画：正确路径高亮（绿色）+ 接触点标注（红点）。
- 对照表滑块使用 `Slider(value:in:step:)`，step 为 1°。

## Dark Mode

- 所有颜色通过 `Color(named:)` + Asset Catalog 语义色；不使用固定 `.white` / `.black`。
- 球台背景色在 Dark Mode 下使用深绿色变体（Token `btTableSurface`）。

## 性能约束

- 列表（Drill 列表、历史列表）使用 `LazyVStack` 或 `List`；不在 `ScrollView` 中嵌套全量 `VStack`。
- `Canvas` 绘制回调保持纯函数，不引用 `@State`（通过参数传入）。

---

## 经验教训（Lessons Learned）

> 本节由 FAILURE-LOG 驱动，每条对应一个 FL-NNN。**执行前必读，违反即视为返工风险。**

### ⛔ v50 紧凑屏键盘避让 — 长表单必须由可滚动容器承接焦点位移
**场景**：带固定 `NavigationBar` 的登录/编辑长表单，需在 iPhone SE 等紧凑屏上弹出键盘。
**强制检查点**：
1. 长表单不得使用一条不可滚动的满屏 `VStack` 承受键盘避让；iOS 26 在紧凑高度下可将整个栈上移，使页内标题进入固定导航栏。使用 `ScrollView` + 视口 `minHeight` 保留无键盘排版，键盘态由滚动容器保证焦点和关键操作可达。
2. 回归不只断言键盘存在；必须断言发送/保存等关键操作与窗口相交，且可见的页内标题不与 `NavigationBar.frame` 相交。
3. 设备矩阵每个单元必须显式重置 `content_size`；不得依赖上一轮模拟器留下的标准/辅助字号状态。

### ⛔ FL-033 — 组间休息最小化 ≠ 会话最小化；训练页面 ≠ 训练 Tab
**场景**：记分页上的组间休息卡要「最小化」；或用户说最小化后应留在训练页面。
**强制检查点**：
1. **训练页面** = 点「开始训练」后的记分页（`ActiveTrainingView`，含组间休息）。**训练 Tab** = 官方计划货架。禁止把两者当成同一页。
2. 休息卡「最小化」只收起休息覆层（`isRestOverlayMinimized`），⛔ 不得调用会话级 `minimizeTraining` / 切 Tab / 出跨页浮标。
3. 整场离页只走底栏「最小化训练」。从浮标回到记分页时，若仍在休息，覆层必须展开。

### ⛔ DR-076 — 多页签滚动禁止共用一条 ScrollView 的 offset
**场景**：分段 / Tab 切换后，B 页读到 A 页的滚动位置；或需要「未打开过从头、退出再进全清」
**强制检查点**：
1. 访问过的页各自留一条 `ScrollView`（或等价 UIKit 滚动视图），用 opacity / hit-testing 切换；未访问的不建。
2. ⛔ 禁止对共用 `ScrollView` 写 `.id(selection)` 充当隔离——那只会每次回顶，丢掉缓存。
3. 选择器与「已访问集合」必须同一事务写入；只靠 `onChange` 会晚一拍，新页还没建出来会闪空。
4. 会话级缓存用 `@State` 即可；退出页面即清。不要写 UserDefaults。

### ⛔ FL-028 — 避让类布局要不变量测试；「看起来一样」的颜色要测量真实渲染
**场景**：实现「浮层避开某元素 / 取对角 / 不遮挡」的定位规则；或要求某处颜色与已有渲染（台呢、球面、场景）观感一致
**强制检查点**：
1. 避让/对角规则必须有**不变量测试**：遍历所有 `previous` 状态 × 焦点位置区间，断言「浮层不与焦点同处一侧」，例式用例不算覆盖。
2. 防抖滞回只允许写成「**中线 ±band 模糊带内保持 previous**」；写成半平面（`y < 0.5 + band`）会退化成「永远留在焦点那一半」。
3. 新增测试前先问它是否与被测函数自身不变量冲突——**已有的绿测试可能正在守护一个 bug**，此时改测试（并写明理由），不是改生产代码去迎合它。
4. 需求为「和 X 看起来一样」的颜色/尺寸：取值必须**离屏渲染 X 并采样像素**得到，再用像素比对测试锁住（逐通道容差 + 色相序）；禁止用相近的 UI token 或手调值交付。
5. 放大镜类圆形浮层的「离开焦点」须断言**圆心距 ≥ 浮层半径 + 被遮物半径 + 间隙**（或等价边隙 ≥ N pt）；禁止只断言象限/角落不同——象限对了仍可能边贴边（v23.8：0.58×d → 0.92×d）。

### ⛔ FL-023 — SceneKit 丢弃含巨型 n-gon 的 USDZ 子集；几何存在性禁用包围盒判断
**场景**：USDZ 模型部分部件在 SceneKit 下不渲染（其他查看器正常）；或需要修改/修复 USDZ 资产
**强制检查点**：
1. SceneKit USDZ 导入会**静默丢弃**含 ≥256 顶点面的整个 GeomSubset（实测 255 可/256 丢）；在 iOS 17 的渲染线程中还可能留下 null render element，最终于 `C3DMeshElementGetType` 崩溃。Blender 合并网格 / limited dissolve 后导出必须先三角化或校验 **所有 Mesh 的 max n-gon < 256**，不能只检查目标子集。
2. 判断「几何是否存在/可渲染」禁止用包围盒或顶点数（孤立顶点撑出假象）；必须数被面片索引引用的顶点，或把目标材质替换成高亮色渲染直接验证。
3. 修复/嫁接 USDZ 用 USD 工具链（`Sdf.CopySpec` + `UsdUtils.CreateNewUsdzPackage`）；禁经 SceneKit `write(to:)` 往返——线性色空间纯色会被二次伽马编码漂移（实测 0.801→0.957）。
4. `usdchecker` 通过只证明 USD 结构合法，不证明 SceneKit 可渲染。修改 USDZ 后必须在**最低支持 Runtime** 用 `SCNRenderer` 真正渲染至少一帧，并以 UI 流程再进入一次实际场景；两项都通过才可交付。

### ⛔ FL-002 — Tab 图标选型须与产品主题对齐
**场景**：为 Tab Bar 选择 SF Symbol 图标时
**强制检查点**：台球类 App 禁止使用健身/游泳/运动人形类通用图标（如 `figure.pool.swim`、`figure.run` 等）。必须选用与台球/台球桌/球杆语义相关的图标，或使用自定义 Asset。提交前在截图中验证所有 Tab 图标。

### ⛔ FL-007 — 确认/警告弹窗按钮必须有文字标签
**场景**：实现任何确认、警告、破坏性操作弹窗（Alert、自定义 Sheet 中的操作按钮）时
**强制检查点**：所有弹窗按钮必须有明确的 `.label` 文字（如"取消"、"结束"、"删除"）。禁止使用 `Button(action:) { EmptyView() }` 或空字符串 `Button("")`。VoiceOver 可读性为交付硬门槛。

### ⛔ FL-004 — List/Button 行必须使用 `.buttonStyle(.plain)`
**场景**：在 `List`、`ScrollView` 中使用 `Button` 包裹自定义行视图时
**强制检查点**：所有 `Button { … } label: { 自定义行 }` 必须添加 `.buttonStyle(.plain)`。默认 ButtonStyle 会用系统 tint 覆盖子视图的 `.foregroundStyle()`，导致文字颜色丢失（表现为文字"消失"）。提交前在浅色/深色模式下验证行内所有文字可见。

### ⛔ FL-005 — 列表页必须实现规格要求的筛选/导航入口
**场景**：开发包含分类维度的列表页面时（如动作库、计划列表）
**强制检查点**：若产品规格（`docs/05`）要求分类导航，DoD 必须包含"分类选择器可交互"验收条件。简单的分组标题不等于分类导航——用户必须能通过点击分类快速筛选。

### ⛔ FL-006 — 骨架/占位页面必须显示"开发中"提示
**场景**：功能页面 UI 骨架先于逻辑实现时
**强制检查点**：未完成的功能页面禁止展示空标题行或空白输入区域。必须使用 `BTEmptyState` 或等效占位组件显示"功能开发中"，避免用户误以为是 Bug。

### ⛔ FL-008 — 列表行 UI 验收需确认所有 label 文字可见
**场景**：Profile / 设置等使用图标+文字行布局的页面
**强制检查点**：与 FL-004 同源。所有使用 `Button` 包裹的 `MenuRow` / `SettingRow` 必须添加 `.buttonStyle(.plain)`。验收时逐行检查：图标、主标题、副标题、箭头均可见且颜色正确。

### ⛔ FL-009 — 训练记录页必须嵌入 BTBilliardTable
**场景**：开发训练中 Drill 记录界面时
**强制检查点**：`BTBilliardTable` Canvas 组件嵌入为 DoD 强制项。Drill 记录页顶部必须展示球台动画（来自 Drill JSON 的 animation 数据），让用户训练时可参照球路示意。

### ⛔ FL-002 — Sheet dismiss 与 Alert 展示时序冲突
**场景**：在 `.sheet` 内修改 `@Published` 状态后需要在父视图弹出 `.alert` 时（如登录后弹数据迁移提示）
**强制检查点**：禁止在 Sheet 内直接设置触发父视图 Alert 的布尔值后立即 `dismiss()`。SwiftUI 在 Sheet 动画期间不会呈现新 Alert。正确做法：设置 `pending` 标志 → `dismiss()` → 在 `.sheet(onDismiss:)` 回调中将 `pending` 转为 `showAlert = true`。

### ⛔ PD-026 — Lazy 容器跨数据源切换要行级复合 id；数据驱动的 sheet 用 `sheet(item:)`
**场景**：分段/Tab 切换驱动 `LazyVStack`/`List` 换正文；或点击动作先算好数据再弹 sheet
**强制检查点**：
1. `ForEach(id: \.offset)` 的 Lazy 行在数据源切换后会按 offset 复用旧行（正文不刷新）；必须给行加复合 id（如 `.id("\(selection)-\(index)")`），外层 `.id(selection)` 不够。
2. `.sheet(isPresented:)` 的内容闭包可能以陈旧 `@State` 求值（iOS 26 实测渲染空列表）；凡「动作中先写 state 再弹 sheet」一律改 `.sheet(item:)` + `Identifiable` payload 携带数据快照。

### ⛔ FL-003 — 球桌 / 袋口几何禁止从模型网格反推
**场景**：在 SceneKit / Canvas 中绘制球桌、袋口、库边、覆盖标记等任何视觉元素时
**强制检查点**：
1. **唯一事实来源**是 `.kiro/steering/table-geometry.md`（含 innerLength、innerWidth、6 个袋口中心、角袋/中袋半径、库边分段、过渡圆弧），任何新写入的几何常量必须先查该文档；如该文档缺失字段，应先补 steering 再写代码。
2. **禁止**通过 USDZ / OBJ 模型的网格顶点、材质命名、Pole-of-Inaccessibility 等反推方式获取袋口位置或库边坐标——模型缩放、命名、材质变化都会破坏这种推导，掩盖几何对位错误的根因。
3. 视觉覆盖层（袋口标记、瞄准点、热区等）的位置 **必须** 通过解析公式从 steering 常量推导（例如 `cornerCenter = (±halfL ± cornerOffset, ±halfW ± cornerOffset)`），而不是从模型反查 + 兜底。

### ⛔ FL-039 — Plain 整行按钮必须覆盖透明留白
**场景**：List / 卡片的 Button 标签使用 HStack + Spacer，并应用 `.buttonStyle(.plain)`。
**强制检查点**：若产品语义为整行可点（含行为等同 plain 的 BTPressableStyle.row），在标签完成布局后声明 `.contentShape(Rectangle())`。以宽屏真实 frame 点击行中央或留白处，验证状态改变；禁止用改点图标、重复点按或延时掩盖未覆盖的命中区。弹窗测试使用明确动作标识或名称，避免全 App 的“添加”前缀误命中背景控件。

### ⛔ FL-045 — 元数据增高须复核原标题行数
**强制检查点**：在网格卡增加胶囊/副标题后，用同一长标题对比有/无元数据和窄列；验证原两行标题不被纵向压缩成一行。按真实列宽保留固有高度，不以缩字号或删断言掩盖。渲染单测通过仍须目视。

### ⛔ FL-047 — 局部密度整改须保持主内容占比
**场景**：用户要求球库/工具条更集中。
**强制检查点**：同时对比球体大小、整条工具区占比、球桌可视区域和底栏总高度。不得将“收紧间距”擅自等同于“放大球体”，也不得用新增按钮行挤压主内容后仅凭触控测试通过交付。用户否决的比例方案立即退出验收基线，保留原证据但明确撤回。

### ⛔ FL-049 — 系统时间文本的固有宽度不能按普通 Text 处理
**场景**：Live Activity 中对 Text(timerInterval:) 使用水平 fixedSize，或让其无约束扩张，导致锁屏全部文字消失或右侧元信息被挤出。
**强制检查点**：时间文本必须有可解释的宽度来源；需要紧凑宽度时，用同字体的普通时间模板测量，再以 overlay 容纳动态时间，不能对动态时间直接要求水平理想宽度。验收覆盖标题、数字、动作名、品牌全部区域；系统授权提示先处理，再检查系统截图与间隔截图。App 内截图及脚本 exit 0 不构成锁屏/灵动岛视觉验收。
