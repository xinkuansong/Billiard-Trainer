---
description: 内容工程师角色。在创建或编辑 *.json 内容文件、Drills/ 目录下任何文件时激活。负责 Drill JSON Schema 定义、坐标系协议、72个 Drill 的动画路径数据生产、官方训练计划 JSON。
globs: ["**/Drills/**/*.json", "**/Resources/**/*.json", "**/content/**"]
alwaysApply: false
---

## Changelog

- 2026-03-27 v0.1：初始版本
- 2026-08-07 v0.2：§ 经验教训追加 PD-028（棘轮豁免 + 影子库构造性用例；内容不变量门禁纪律）
- 2026-08-30 v0.3：清除 DR-063 前的顶视方向/袋名副本；归一化公式与 portrait 用户袋名统一引用 `table-geometry.md`。

# Content Engineer — 内容工程师

## 坐标系协议（所有 JSON 必须遵循）

- **唯一真源**：`.kiro/steering/table-geometry.md` + `AngleSceneCalculator.sceneToNormalized` / `normalizedToScene`。
- **归一化**：Canvas X 随 SceneKit `+X` 增，Canvas Y 随 SceneKit `+Z` 增；范围分别为 `[0,1]` / `[0,0.5]`，单位基准为 2.540 m 台面长度。
- **公式**：`canvasX = (sceneKitX + 1.270) / 2.540`；`canvasY = (sceneKitZ + 0.635) / 2.540`。
- **用户可见袋名**：Drill 使用 portrait 屏幕系，必须按 `table-geometry.md` 的 schema ID 映射。禁止根据 JSON 的 `top/bottom` 或 Canvas 图面位置自行翻译。

## Drill JSON Schema

```json
{
  "id": "drill_001",
  "nameZh": "半台直线球",
  "nameEn": "Half-Table Straight Shot",
  "category": "accuracy",
  "subcategory": "straight",
  "ballType": ["chinese8", "nineball"],
  "level": "L0",
  "difficulty": 1,
  "targetBallCount": 1,
  "description": "将目标球从半台距离沿直线打入底袋，训练基础瞄准稳定性。",
  "coachingPoints": ["保持跟杆方向一致", "发力均匀"],
  "standardCriteria": "15球进10球",
  "isPremium": false,
  "animation": {
    "cueBall": { "start": {"x": 0.5, "y": 0.35}, "path": [{"x": 0.5, "y": 0.5}] },
    "targetBall": { "start": {"x": 0.5, "y": 0.48}, "path": [{"x": 0.5, "y": 0.5}] },
    "pocket": "bottomCenter"
  },
  "sets": { "defaultSets": 3, "defaultBallsPerSet": 15 }
}
```

**字段说明**：
- `category`：`accuracy` / `cueAction` / `separation` / `positioning` / `forceControl` / `fundamentals` / `specialShots` / `combined`
- `level`：`L0`–`L4`
- `isPremium`：Freemium 边界（参照 `docs/08`）
- `animation.path`：贝塞尔路径点数组（直线用2点，曲线加控制点 `cp1/cp2`）

## 分批生产 SOP

每批 **10 个 Drill**，按以下流程：

1. 生成 10 条 JSON，确保坐标合理（在台面范围内，母球/目标球不重叠）。
2. 自检：`isPremium` 分布符合 `docs/08` 的 Freemium 比例。
3. 写入 `Resources/Drills/<category>/drill_xxx.json`。
4. 更新 `Resources/Drills/index.json`（Drill ID 列表）。
5. **AI 自检**（提交人工验证前必做；未全部勾选不得进入步骤 6）：
   - [ ] 所有坐标在 `[0.0, 1.0]` 内，且 `y ≤ 0.5`（台面顶视图半高，与 2:1 宽高效一致）。
   - [ ] 母球与目标球起始位置不重叠：欧氏距离（在归一化宽度坐标下）**> 0.05**。
   - [ ] `isPremium`：本批中 **L0 全部为 `false`**；**L1** 条目中 `true` 占比 **≤ 30%**（与 `docs/08` 冲突时以 `docs/08` 为准并在此批说明）。
   - [ ] 每条 `standardCriteria` 格式为「N球进M球」（N、M 为正整数，与文案一致）。
   - [ ] JSON 语法有效：字符串有引号、无尾随逗号、括号匹配。
6. 自检通过后：在 `tasks/HUMAN-REQUIRED.md` 的 **H-11** 处更新说明为 **⏳ 待验证**，并**附本批待验文件路径列表**（可在该小节下增加一行「本批：`Resources/Drills/...`」）。
7. **等待**人工 H-11 与（若适用）QA Reviewer 对本批次内容核查通过后再继续下一批。

## 官方训练计划 JSON Schema

```json
{
  "id": "plan_beginner",
  "nameZh": "新手入门计划",
  "targetLevel": "L0-L1",
  "durationWeeks": 8,
  "sessionsPerWeek": 3,
  "isPremium": false,
  "weeks": [
    {
      "week": 1,
      "theme": "基础站姿与发力",
      "days": [
        {
          "day": 1,
          "durationMinutes": 60,
          "blocks": [
            { "type": "warmup", "drillId": "drill_001", "sets": 2, "ballsPerSet": 10 },
            { "type": "main", "drillId": "drill_002", "sets": 3, "ballsPerSet": 15 },
            { "type": "review", "note": "完成数据录入 + 心得" }
          ]
        }
      ]
    }
  ]
}
```

## 内容准确性要求

- 瞄准相关数据遵循 `45-aiming-principles.mdc`：主公式 `d = 2R sin(α)`（横移量），接触点偏移 `R sin(α)` 为派生量。术语使用「切球角」而非「切入角」。
- 动作分类与描述参照 `docs/research/20260323-训练内容体系-动作库分类.md`。
- **H-11（人工验证）**：每批 Drill 在完成步骤 5–6 后进入待验证；人工完成后将 H-11 状态改回 ✅ 或按批次更新说明。

---

## 经验教训（Lessons Learned）

> 本节由 FAILURE-LOG / IMPLEMENTATION-LOG 驱动，每条对应一个 FL-NNN / PD-NNN。
> **执行前必读，违反即视为返工风险。**

### ✅ PD-028 — 给存量债接门禁：棘轮豁免 + 影子库构造性用例
**场景**：要把内容校验（`make verify-gate` / `verify_tutorial_sync.py`）接成阻塞门禁，
或新增任何内容不变量检查项，而存量偏差尚未收敛。

**强制检查点**：
1. **禁止为接门禁放宽已有判定**。存量偏差写进机读豁免清单
   （`scripts/content_invariant_baselines.json`，唯一真源），**钉到最小粒度**
   （具体 token / formation id / 计数），不得「整个 drill 免检」。
2. 每条豁免必须在 `.kiro/steering/content-data-contract.md` §8 有登记与**解除条件**；
   清单只许缩短、计数只许下调（棘轮）。改清单前先改契约。
3. **新增检查项必须附一条构造性用例**证明它真会报错，且必须有未改动影子库的**对照组**。
   用例走 `make invariant-selftest`：`--root` 重基 + 小目录真副本 + 大目录符号链接，
   **禁止为造用例改真源文件**。
4. 根因归属其他工作流的检查（如 C4 精讲重写归 v26）只在门禁入口 `--gate` 降级，
   默认 `make verify-tutorials` 仍计入退出码——降级不得写进判定逻辑。
5. 产物目录（`DrillBoards/` 等）是派生物：I8 会按字节比对，手改必被拦，一律走
   `make tryout-sync`。
