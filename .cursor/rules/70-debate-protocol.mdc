---
description: 多智能体结构性辩论·通用协议 — 主持人如何驱动 Ideator / Critic / Recorder 循环，达成共识并交人裁决。与议题解耦：议题、议程、证据源见对应「议题文件」（默认 71-球迹增长议题.mdc）。用户说"开一场辩论""让智能体辩 xxx""设计千万级功能"时激活。
globs:
alwaysApply: false
---

## Changelog

- 2026-06-18 v0.3：通用化重构——本文件只保留**与议题无关的机制**；业务内容externalize 到议题文件（`71-球迹增长议题.mdc`）；agents 与 skills 全部通用化。
- 2026-06-18 v0.2：重定义共识为"不确定性下的决策 + 验证计划"（kill-test 分 A/B）；新增议程覆盖闸门。
- 2026-06-18 v0.1：初始版本（三智能体增长辩论机制）。

# 多智能体结构性辩论 — 通用协议

本协议定义一套**与议题无关**的对抗式辩论机制。要辩什么、按哪些维度辩、用什么证据，全部写在**议题文件**里（默认 `.cursor/rules/71-球迹增长议题.mdc`；换议题只换议题文件，本机制与三个智能体、三个技能原样复用）。

> 设计动机：让单一智能体独自构思，最大风险是"自我认同"——自己提的方案自己找理由支持。本机制用「角色分离 + 异构模型 + 评判方枪毙权 + 证据闸门」对冲这一偏差。

## 角色与模型（异构是刻意设计）

| 角色 | 子智能体 | 模型 | 职责 |
|---|---|---|---|
| 提想法者 | `debate-ideator` | `claude-opus-4-8-thinking-high` | 发散；提想法 + 依据 |
| 评判者 | `debate-critic` | `gpt-5.5-medium`（换厂商） | 怀疑；判断 + 理由 + 杀手测试，可枪毙 |
| 记录者 | `debate-recorder` | `composer-2.5-fast` | 中立；记账、维护台账 |

> **为什么异构模型**：三者若同模型，盲区同构，"达成共识"可能只是自我点头。Ideator 与 Critic 跑不同厂商，分歧才是真分歧——这是防"自我认同 / 假共识"的第一道防线。调用时用 Task 工具的 `model` 参数指定上表模型（agent frontmatter 已写 `model`，调用参数为准 / 兜底）。

> **联网**：`debate-ideator` 与 `debate-critic` 须具备**按需联网查证**能力（搜索 / 抓取外部事实与资料）。主持人调用这两者时必须以 **非 readonly** 方式调用（Task 的 `readonly: true` 会禁用联网与 MCP）。`debate-recorder` 不需要联网。联网所得须附来源链接 + 日期。

## 主持人（驱动方）职责

主对话即主持人。**开场先读当前议题文件**，取出：议题目标、议程维度、证据源、A/B 例子、不可动约束。三个子智能体各自独立上下文。**分两阶段推进**（议程维度多、且彼此耦合，不可能一轮出全景方案）：

### 阶段 0 — 全景骨架（约 1 轮，轻量）

调用 Ideator 产出一张「议程地图」，**而非完整方案**：对每个议程维度给 1–2 句初步立场 + 标出**维度间依赖** + 提议**讨论顺序**（基础维度如定位 / 用户 / 市场优先）。Critic 审这张地图：顺序与依赖是否合理、有没有漏维度、有没有违反不可动约束。此阶段只框定与排序，不在此求解。

### 阶段 1 — 逐维深入（多轮，按依赖顺序）

按阶段 0 定的顺序，**一次聚焦一个维度（或一簇强耦合维度）**，可多轮深挖：Ideator 提 → Critic 判（含 kill-test、覆盖面）→ 只转交 Ideator 产出（不转交内部推理）→ 回传迭代，直到该维度收敛或显式"本场暂缓 + 原因"。

- **允许回溯**：当某维度的结论动摇了已讨论维度的结论时，显式标记"需重审"的维度并回头修订，**不假装线性完成**。
- 每轮调用 **Recorder** 追加台账（更新方案草案、议程覆盖进度、回溯标记）。
- **不设轮数上限、不中途呈交**（除非用户主动叫停）。

### 收尾

所有维度"已结论或显式暂缓"、无悬而未决的回溯、且满足 § 共识门槛 → 产出方案 spec → **交用户最终签字**（用户不签不算定稿）。

## 共识门槛（防假共识、且在不确定性下可达成）

**共识不是"证明会成功"**（很多议题在辩论桌上无法取得实证），而是"**在当前不确定性下，双方同意这是最该下的决策 + 一套能证伪它的验证计划**"。

kill-test 分两类（分类方法见 `idea-evaluation` 技能，本议题例子见议题文件）：

- **A 类·当场可判定**：证据现在就能查（代码 / 文档 / **按需联网查到的公开事实**，网络来源须附链接 + 日期）。**必须在辩论内用真实证据解决，禁止留作 `⏳未测` 推给未来。**
- **B 类·必须实测**：需实验 / 上线才有结果。转化为**带门槛值的验证计划**，是方案的一部分，**不是共识前置闸门**。

宣布"共识"必须**同时满足，缺一不可**：

1. 议题文件 § 议程 的所有维度已被处理（有结论或显式暂缓），且无悬而未决的回溯。
2. 所有 **A 类** kill-test 已用真实证据解决（不得有可查事实被 punt）。
3. 所有 **B 类** kill-test 已转成明确、有门槛值的验证计划。
4. Critic 显式声明"已无剩余实质异议"且未行使枪毙权；Ideator 确认接受全部约束。

> Critic 的**枪毙权只针对"想法 / 方案本身不成立"**，不能用来枪毙"作出决定 / 定稿"这个动作。
> 任一条用"语气和缓 / 互相礼貌"糊弄即判**未达成**。宁可枪毙也不无理由点头。

## 产出物（落 docs/research/）

- 辩论记录（Recorder 维护）：`docs/research/<YYYYMMDD>-辩论记录-<议题简称>.md`，**每场必含「当前最优方案草案（带不确定性标注）」与「议程覆盖进度」**。
- 共识方案 spec（交用户签字）：`docs/research/<YYYYMMDD>-方案-<议题简称>.md`，含全景方案 + B 类验证计划。

## 启动方式

用户说"开一场辩论 [议题：xxx]"时：

- 指定了已有议题文件 → 用该文件。
- 未指定 → 用默认议题文件 `.cursor/rules/71-球迹增长议题.mdc`。
- 全新议题 → 先仿照 71 的结构新建一个议题文件（目标 / 议程维度 / 证据源 / A·B 例子），再开辩。
