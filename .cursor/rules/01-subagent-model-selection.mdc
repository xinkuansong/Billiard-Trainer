---
description: 用户提到要用子智能体（subagent）解决问题时，必须先主动询问子智能体使用什么模型
alwaysApply: true
---

# 子智能体模型选择确认

当用户提出要用**子智能体**（subagent / Task 工具派发）来解决问题、执行任务或委派工作时，在启动任何子智能体之前，**必须先用 AskQuestion 工具主动询问用户**：子智能体要使用什么模型。

## 可选模型

| 选项 | 对应 model slug |
|------|-----------------|
| cursor-grok-4.6（默认） | `cursor-grok-4.6-high-fast` |
| Claude Opus 5.0 | `claude-opus-5-thinking-medium` |

以上是快捷选项，非封闭列表：询问时应以**当前实际可用的模型列表**为准补充其他选项（用户也可通过 Other 自定义）。

## 规则

- **默认模型**：用户未答复、或答复「默认 / 你定」时，使用 `cursor-grok-4.6-high-fast`。不再提供、不再推荐 grok 4.5。
- 用户已在本次消息或本会话中明确指定过模型时，不必重复询问，直接使用。
- 涉及多个子智能体时，确认是全部统一用一个模型，还是分别指定。
- **附带风险推荐**：涉及多批次方案（如「问题集合 vN」类真源文件）时，询问前先按批次风险给出推荐路由（判断密集的横切批/收口批建议强推理模型；条目明确的页面域批可用快速模型并升级验收强度），让用户在推荐基础上确认，而不是裸选。
- **答复的有效范围**：执行多批次方案时，用户确认的模型（含分批路由结果）应**写入真源方案文件的批次表**（每批一列「模型」）。后续任何会话读到批次表中已有模型即视为「已指定」，不再重复询问；仅当方案未写明、或用户中途要求改派时才重新询问。
