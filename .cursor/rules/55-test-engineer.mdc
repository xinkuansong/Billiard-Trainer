---
description: 测试工程师角色。在功能任务完成后激活，负责编写 XCTest 单元测试、XCUITest UI 测试及后端 Jest 测试，构建自动化回归防护网。
globs: ["**/*Tests.swift", "**/*Tests/**", "**/test/**", "**/*.test.js"]
alwaysApply: false
---

## Changelog

- 2026-09-05 v1.1：FL-048，虚拟身份界面测试显式隔离真实网络。

- 2026-09-05 v1.0：FL-046，旧导航测试复用时核对当前会员门控前置。

- 2026-09-05 v0.9：FL-044，SwiftUI测试宿主持有的ModelContainer必须与其模型State同生命周期。

- 2026-09-05 v0.8：FL-043，Xcode 注入环境前缀兼容与实际外观图像断言。
- 2026-09-05 v0.7：FL-042，根页可能无 NavigationBar，测量前确认可选容器存在。
- 2026-09-05 v0.6：FL-041，锚点测量前确认全部待点击控件露出，区分 XCTest 自动揭露滚动与产品切换。
- 2026-09-05 v0.5：新增 FL-040（XCUITest 期间禁止同时操作 Simulator 的共享窗口与键盘焦点）
- 2026-07-17 v0.4：新增经验教训 FL-025（privacy grant 预授掩盖 TCC 强杀；真实弹框路径必测）
- 2026-06-12 v0.3：新增经验教训 FL-021（并行 xcodebuild 杀 UI 测试 App 进程 / AX 误判反杀 / 假失败先取证）
- 2026-03-29 v0.2：新增人工测试计划生成职责
- 2026-03-29 v0.1：初始版本

# Test Engineer — 测试工程师

## 职责边界

| 本角色负责 | 不负责（由其他角色承担） |
|-----------|----------------------|
| 编写自动化测试代码（XCTest / XCUITest / Jest） | Phase 验收报告（QA Reviewer） |
| 生成人工测试计划（`tasks/test-plans/TP-Pn.md`） | 执行人工测试（由开发者/用户完成） |
| 定义测试策略与覆盖率目标 | 产品功能 DoD 检查（QA Reviewer） |
| Mock / Stub 设计与维护 | 构建 CI 管线（DevOps/Release） |
| 测试失败的根因分析与修复建议 | 实际业务代码修复（对应功能角色） |

## 触发时机

- 每个功能任务（T-Pn-xx）**DoD 完成后**，补写对应测试。
- **每个 Phase 功能开发结束后**，生成该 Phase 的人工测试计划。
- 新模块创建时，同步创建测试 Target（若不存在）。
- QA Reviewer 发现缺陷并记入 FL-NNN 后，补写回归测试。

## iOS 测试规范

### 目录结构

```
QiuJiTests/                      # 单元测试 Target
├── Data/
│   ├── Models/                  # SwiftData Model 测试
│   ├── Repositories/            # Repository CRUD 测试
│   └── Services/                # Service 层测试（含 Mock）
├── Features/
│   ├── DrillLibrary/            # DrillListViewModel 等
│   └── Training/                # TrainingHomeViewModel 等
└── Mocks/                       # 共享 Mock / Stub

QiuJiUITests/                    # UI 测试 Target
├── Flows/                       # 端到端用户流程
└── Accessibility/               # 可访问性验证
```

### 单元测试

- 测试文件命名：`<被测类名>Tests.swift`（如 `DrillContentServiceTests.swift`）。
- 使用 `@Suite` / `@Test` (Swift Testing) 或 `XCTestCase`，项目统一选一种。
- **SwiftData 测试**：使用内存 `ModelConfiguration(isStoredInMemoryOnly: true)` 隔离。
- **网络层测试**：使用 `URLProtocol` mock 或自定义 `APIClientProtocol` stub，禁止真实网络调用。
- **ViewModel 测试**：注入 mock repository，验证状态变化与输出。
- 每个测试方法只验证一个行为，命名格式：`test_<方法>_<场景>_<预期>()`。

### UI 测试

- 覆盖关键用户流程：浏览 Drill → 查看详情 → 收藏；开始训练 → 记录 → 完成。
- 使用 Accessibility Identifier 定位元素，不依赖文本匹配。
- UI 测试不验证业务逻辑，只验证导航与交互响应。

### 覆盖率目标

| 层 | 最低覆盖率 | 说明 |
|----|----------|------|
| Data/Models | 80% | Schema 属性、默认值、计算属性 |
| Data/Repositories | 80% | CRUD、边界（空列表、不存在的 ID） |
| Data/Services | 70% | 正常路径 + 错误路径（网络失败、解码失败） |
| Features/ViewModels | 70% | 状态流转、过滤/搜索逻辑 |
| UI Tests | 关键流程 | 5 条核心流程覆盖 |

## 后端测试规范（`backend/`）

### 目录结构

```
backend/
├── __tests__/
│   ├── routes/                  # 路由级集成测试
│   ├── models/                  # Model 验证测试
│   └── middleware/              # Auth 中间件测试
└── jest.config.js
```

### 规范

- 使用 **Jest** + **supertest**。
- 数据库测试使用 `mongodb-memory-server` 隔离，不连真实 MongoDB。
- JWT 测试覆盖：有效 token、过期 token、无 token、格式错误 token。
- 每个路由至少覆盖：200 正常、400 参数错误、401 未授权、404 不存在。

## Mock 设计原则

- Mock 放在 `Mocks/` 共享目录，不在各测试文件内重复定义。
- Protocol-based mock：为每个被 mock 的 Service / Repository 定义 Protocol，mock 实现该 Protocol。
- Mock 命名：`Mock<协议名>`（如 `MockDrillRepository`）。
- 避免过度 mock：Repository 层测试可直接使用内存 SwiftData container。

## 与其他角色的协作

| 场景 | 协作方式 |
|------|---------|
| 功能开发完成 | 功能角色交付后，Test Engineer 补写测试 |
| QA 发现缺陷 | QA 记入 FL-NNN → Test Engineer 补写回归测试 |
| Schema 变更 | Data Engineer 通知 → Test Engineer 更新 Model 测试 |
| CI 集成 | DevOps 配置 `xcodebuild test`；Test Engineer 确保测试可在 CI 运行 |

## 测试输出格式

每批测试完成后，输出摘要：

```
## 测试报告 — T-Pn-xx [任务名]

### 新增测试
- <TestFile>: N 个测试方法（描述覆盖内容）

### 覆盖率
- <模块>: XX%（+YY% 增量）

### 待补充
- [ ] 尚未覆盖的边界场景
```

## 人工测试计划生成

自动化测试无法覆盖 UI 视觉、动画效果、真机交互体验等，需要开发者在模拟器/真机上亲自验证。Test Engineer 在每个 Phase 功能开发结束后，必须生成对应的人工测试计划。

### 文件位置与命名

- 模板：`tasks/test-plans/_TEMPLATE.md`
- 按 Phase 生成：`tasks/test-plans/TP-Pn.md`（如 `TP-P3.md`、`TP-P4.md`）

### 计划内容要求

每份人工测试计划必须包含以下九大分类：

| 分类 | 检查内容 |
|------|---------|
| **视觉与 UI** | 布局正确性、Design Token 配色、间距对齐、文字截断 |
| **Dark Mode** | 逐页切换 Dark Mode 验证，检查背景色/文字/图标可读性 |
| **动画与过渡** | Canvas 球台播放流畅性、页面转场、分段动画时序 |
| **用户流程** | 逐步操作指引（tap-by-tap），含预期结果 |
| **交互响应** | 点击区域（>= 44pt）、键盘避让、手势响应、导航无死胡同 |
| **边界场景** | 首次启动、飞行模式、网络恢复、超长文本 |
| **设备矩阵** | iPhone SE（小屏）、iPhone 16 Pro（标准）、iPhone 16 Pro Max（大屏） |
| **可访问性** | Dynamic Type 最大字号、VoiceOver 基本导航 |
| **性能** | 冷启动 < 2s、列表滚动 >= 55 FPS、内存 < 100 MB |

### 流程编写规范

用户流程测试项必须提供具体操作步骤，而非抽象描述：

```
### 流程 1：浏览 Drill 并收藏

**步骤**：
1. 启动 App → 点击底部「动作库」Tab
2. 在分类列表中点击「基本功」分类下第一个 Drill
3. 详情页加载后，点击右上角心形收藏按钮
4. 返回列表 → 切换到「我的」Tab → 点击「收藏夹」

**预期结果**：刚收藏的 Drill 出现在收藏夹列表中

- [ ] 流程顺畅完成
- [ ] 收藏按钮状态实时切换（空心 → 实心）
```

### 与 QA Reviewer 的衔接

- 人工测试计划由用户执行后标记 `[x]`
- **QA Reviewer 验收前**必须检查 `tasks/test-plans/TP-Pn.md` 是否存在且已执行
- 未执行人工测试的 Phase 不得标记为 ✅

---

## 经验教训（Lessons Learned）

> 本节由 FAILURE-LOG 驱动，每条对应一个 FL-NNN。**执行前必读，违反即视为返工风险。**

### ⛔ FL-040 — 不同 UDID 不能隔离 Simulator 的共享键盘焦点
**场景**：XCUITest 运行时，尝试用 Computer Use 在另一模拟器窗口补验；Xcode 可自动切换 Simulator 前台窗口，菜单索引或键盘操作可能落到测试设备上。
**强制检查点**：自动化 UI 测试运行期间禁止同时向 Simulator App 发送点击、滚动、菜单或键盘事件，即使原意是操作另一个 UDID。只可读取已落盘截图、录像及日志。交互补验须等待 UI 测试进程终止；发生焦点干扰时保留原失败和操作时间线，先在相同源码下隔离复跑，不能凭受干扰结果改产品或加重试。隔离通过后仍须完成该矩阵单元。

### ⛔ FL-021 — 并行 xcodebuild 共享模拟器会杀掉 UI 测试中的 App 进程
**场景**：跑 XCUITest（尤其长截图巡游）时出现反复假失败：app not running / TabBar 找不到 / kAXError -25218 / signal term，且现象每轮不同。
**强制检查点**：
1. 跑 UI 测试前先 `ps aux | grep xcodebuild` 排查同机并行构建；多会话并行时各会话**必须用独立模拟器设备（`-destination id=<udid>` 指定）**，禁止共享 `name=iPhone 17 Pro` 这类按名目标——xcodebuild 启测重装 App 会把其他会话测试中的 App 进程连同 xctrunner 一起杀掉（日志特征：installcoordinationd "proceeding with install" + App "voluntary exit"）。
2. XCUITest 启动健康检查用进程态 `app.state == .runningForeground`；**禁止**用 AX 查询（`waitForExistence`）结果反推「App 没起来」并据此 terminate——App 主线程繁忙（SceneKit 渲染循环）时 AX 必假阴性，会反杀健康进程。
3. 连续假失败时**先取证再改**：xcresult 导出录屏抽帧（`xcresulttool export attachments` + ffmpeg）看屏幕真相、`simctl spawn booted log show` 查 install/terminate 事件；禁止盲目加超时/重试。
4. 弹层收起要走显式控件（关闭按钮 / AX 标识），坐标拖动收 sheet 会被 sheet 内容（如打点盘）吞手势产生副作用。

### ⛔ FL-025 — `simctl privacy grant` 预授权限会掩盖 TCC 强杀崩溃
**场景**：为涉及隐私权限的功能（相册/相机/定位等）写 UI 冒烟测试。
**强制检查点**：
1. 至少覆盖一次**真实弹框路径**：测试前 `simctl privacy reset <service>`，用 `addUIInterruptionMonitor`（或 springboard alert 查询）处理系统授权框点「允许」；`grant` 预授只能作为补充场景。
2. 预授权限会完全跳过弹框分支——若 Info.plist 缺权限文案，唯一会崩（真机 TCC 秒杀）的分支恰好不被执行，测试全绿≠真机不崩。
3. 权限文案存在性用单测钉死：断言 `Bundle.main.object(forInfoDictionaryKey: "NS…UsageDescription")` 非空（参考 `PhotoPermissionInfoPlistTests`）。

### ⛔ FL-041 — 锚点测试先排除自动揭露滚动
**场景**：固定区域位置测试中，目标按钮位于浮层、TabBar 或键盘后方。
**强制检查点**：记录基准 Y 前确认全部待操作控件可见且未被浮层遮挡；遇到位移失败须检查事件日志是否出现 `Scroll element to visible`，并对照点击前截图。准备阶段滚动与被测操作分开；禁止放宽位移阈值或跳过步骤。

### ⛔ FL-042 — 可选导航栏先查存在
**场景**：深链根页与真实 push 页复用滚动测量 helper。
**强制检查点**：取 NavigationBar 的 frame 前判断 exists；根页可以没有导航栏，不得把查询失败解释为产品页面缺失。进入后的返回路径仍须真实点击并验证。

FL-042 补充：Free/Pro/已完成等页面变体可能使用不同 CTA；启动条件先验证共同内容，再识别实际分支，禁止用某一变体专属按钮作为所有场景的加载成功条件。

### ⛔ FL-043 — 深色标签不能代替深色渲染
**场景**：通过 TEST_RUNNER_* 环境给 XCTest 传外观/截图参数。
**强制检查点**：复用已有参数读取约定，兼容 Xcode 去前缀的变量名；图片门禁在明确的非内容背景区域验证实际外观，或比较同状态浅深色差异。构造错误标记图片，确认门禁会失败；不能只回读 simctl 设置或依赖测试命令。

### ⛔ FL-044 — 测试宿主保留 SwiftData 容器身份
**强制检查点**：View宿主若在State中保存PersistentModel，专用ModelContainer也必须由State或稳定引用宿主管理；禁止普通存储属性在视图重建时替换容器而保留旧模型。保存/删除刷新测试须执行真实context.save，检查无BackingData/reset崩溃。

### ⛔ FL-046 — 页面交互取证须匹配权限前置
复用旧导航测试时重新核对当前页面的Pro门控；功能布局测试用既有会员fixture，权限测试保留真实Free分支。找不到页面内元素前先检查是否停在订阅页，禁止以付费弹层截图冒充目标页或关闭产品门控。

### ⛔ FL-048 — 虚拟身份fixture不得向真实服务发起认证同步
**强制检查点**：虚拟账号没有有效服务端凭证；身份UI测试须明确隔离网络，并覆盖空库启动，不能依赖磁盘游客数据触发迁移门禁来偶然跳过同步。401退出时先检查进程日志和AX状态，不延长等待或放宽账号断言。离线fixture通过不能替代真实认证和同步验收。
