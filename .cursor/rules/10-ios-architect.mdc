---
description: iOS 架构师角色。在设计新模块、重构现有代码、技术方案选型时激活。负责 MVVM 结构、模块边界、SPM 依赖决策、性能约束，并输出架构决策记录（ADR）。
globs:
alwaysApply: false
---

## Changelog

- 2026-06-05 v0.6：新增经验教训 FL-020（求解器最优区必须含「母球碰目标球前 0 吃库」纯净性约束，否则歪打正着的 kick 退化解与真直击解评分并列，叠加引擎无序遍历致宏观解翻转/分离角跨 2°/偶发打丢；验证须用大规模矩阵 + 同输入多次重跑），来自物理引擎测试网扩面

- 2026-03-27 v0.1：初始版本
- 2026-06-05 v0.5：新增经验教训 FL-019（所有球含母球的进袋判定必须与画面/钳制轨迹同源，禁止裸引擎信号；曲线走位+CCD 直线预测+宽松接受阈值致「判进画面不进」假阳性），来自 P10 Track B-2.4
- 2026-06-05 v0.4：新增经验教训 FL-018（穿库/越界安全网必须基于引擎记录帧而非回放重采样外推位置；瞬时吃库过冲只钳位不截断），来自 P10 Track B-2.3
- 2026-06-04 v0.2：新增经验教训 PD-009（物理求解器评分量纲纪律 + 进袋判定与画面同源 + 捕获/标记半径解耦 + 测量先于改几何），来自 P10 Track B-1 / ADR-P10-02

# iOS Architect — 架构师

## 职责范围

- 定义模块边界与文件组织结构（`App/` `Features/` `Core/` `Data/`）。
- 确保 MVVM 分层：View 不直接访问 Repository；ViewModel 不持有 View 引用。
- 评估并决策 SPM 依赖引入（见 `tasks/dependencies.md`）。
- 识别性能风险并给出约束（主线程限制、后台队列规范）。

## 项目目录约定

```
BilliardTrainer/
├── App/                    # AppEntry, AppRouter
├── Features/
│   ├── Training/           # 训练 Tab
│   ├── DrillLibrary/       # 动作库 Tab
│   ├── AngleTraining/      # 角度 Tab
│   ├── History/            # 历史 Tab
│   └── Profile/            # 我的 Tab
├── Core/
│   ├── DesignSystem/       # Colors, Typography, Spacing
│   ├── Components/         # 可复用 SwiftUI 组件
│   └── Extensions/
├── Data/
│   ├── Models/             # SwiftData @Model
│   ├── Repositories/       # 本地 CRUD 抽象
│   └── Services/           # REST API、Drill 内容服务
└── Resources/
    └── Drills/             # Bundle 内 fallback JSON
```

## MVVM 规范

```
View → ViewModel (@Observable / ObservableObject)
ViewModel → Repository Protocol
Repository Protocol ← LocalRepository (SwiftData)
                    ← RemoteRepository (自建 REST API)
```

- ViewModel 命名：`<Feature>ViewModel`（如 `DrillListViewModel`）。
- Repository 命名：`<Entity>RepositoryProtocol` + `<Entity>LocalRepository`。

## 架构决策记录（ADR）格式

每次做重大架构决策，在对应 Phase 文件末尾追加：

```markdown
### ADR-Pn-xx：[标题]
- 场景：...
- 选项：A / B / C
- 决策：选 X
- 原因：...
- 日期：YYYY-MM-DD
```

## 边界约束

- **禁止**在 View 中直接调用 `ModelContext`；统一通过 ViewModel → Repository。
- **禁止**在主线程执行 REST API 网络请求。
- **新增 SPM 依赖前**：在 `tasks/dependencies.md` 评估后再引入。

---

## 经验教训（Lessons Learned）

> 本节由 FAILURE-LOG / IMPLEMENTATION-LOG 驱动，每条对应一个 FL/PD-NNN。**执行前必读，违反即视为返工风险。**

### ⛔ FL-020 — 求解器「最优解」必须含进攻路线纯净性约束，否则退化 kick 解会致宏观非确定性
**场景**：新增/调整 `ShotPredictor.solveAimOffset`（或任何"搜索最优瞄准/力度"的评分）；或排查"同一杆每次预测轨迹/分离角不同、偶发打丢、大角度变绕库"。
**强制检查点**：
1. **clean 解硬条件 = 母球碰目标球前 0 吃库 + 目标球落袋前 0 吃库**：评分最优区（压过一切方向解的基线）曾只要求「目标球直接进袋且 0 吃库」，**漏了母球接触前 0 吃库** → 搜索会捞到「母球打丢→绕 4 库→歪打正着碰目标球→恰好落袋」的 **kick 退化解**，与真直击解在评分上**并列**（FL-020：t3p5 等球形最优区出现并列）。最优区条件必须同时含 `run.cueCushionsBeforeContact == 0`，让 kick 解降到方向解支永不胜出；方向解支再加 `cueCushionsBeforeContact * w` 轻惩罚兜底。
2. **评分并列 + 引擎无序遍历 = 宏观解翻转**：一旦最优区出现并列候选，`EventDrivenEngine` 的 `Dictionary`/`Set` 事件遍历浮点求和顺序非确定性会被放大成 **argmin 在两个完全不同的解间运行间随机翻转**（FL-020：同输入 30 次重跑 `cueCushionsBeforeContact` 在 0/4 跳、分离角跨 2°、进袋 28/30）。**这远超无害的 1e-4° 派生标量微抖动**——区别在于"是否存在评分并列的不同解支"。消除并列（让 clean 直击解唯一占最优区）即根治宏观翻转。
3. **验证手段（必须双管）**：(a) 大规模系统化矩阵（数百球形 × 袋 × 切角 × 力度）逐例断言进攻路线纯净性（母球接触前 0 吃库、目标球远处不翻袋）；(b) 对历史/可疑球形**同一输入多次重跑**断言宏观确定性（cuePreBank 恒定、进袋稳定、分离角跨度 <0.5°）。见 `PhysicsMatrixTests.test_matrix_solverPottingContract` / `test_matrix_solverPicksDirectNotKick_deterministic`。只看单次结果会漏掉非确定性。

### ⛔ FL-019 — 所有球（含母球）的进袋判定都必须与画面（钳制轨迹）同源
**场景**：实现/复核任一球的进袋判定、状态栏文案、或求解器的 scratch/进袋评分时。
**强制检查点**：
1. **禁止任一球的进袋状态直接取裸引擎信号**：目标球进袋早已用「显示用钳制轨迹最近点 ≤ 捕获窗(dropRadius−R+4mm)」做一致性闸门，但母球 `cuePocketed` 曾直接取 `run.cuePocketed`（裸 `recorder.isBallPocketed`）→ 出现「判进画面不进」（FL-019：cut9° 带塞母球弧线擦中袋嘴最近 54–65mm > 窗 50.4mm，却报「母球进袋」）。**母球与目标球必须用同一套闸门**——要求该球**钳制轨迹**真正落入**某个袋**的捕获窗（落袋后引擎吸球心 ⇒ 最近点≈0）。
2. **曲线走位 + CCD 直线预测 + resolvePocket 宽松阈值 三者叠加 = 假阳性源**：带塞母球是曲线（squirt+swerve），而袋口 CCD 用定加速度直线/抛物线模型排程进袋事件、`resolvePocket` 接受阈值又宽（`pocket.radius+R*1.5`≈中袋 117.8mm）→ 预测会进漏斗、实际只擦边却仍被裸引擎判落袋。显示同源闸门统一甄别，无需逐项调引擎阈值（引擎宽松阈值属物理标定 backlog）。
3. **诊断手段**：`ShotScenarioRenderTests.test_diag_cueScratch`（含塞场景扫描，统计「上报进袋 / 假阳性(判进画面不进) / 中袋吞快球」）。

### ⛔ FL-018 — 穿库/越界安全网必须基于「引擎记录帧」而非「回放重采样位置」
**场景**：给事件驱动轨迹做「穿库飞出」安全网（钳制/冻结/截断）、或任何基于 `TrajectoryPlayback` 重采样位置做几何判定时。
**强制检查点**：
1. **判飞出用记录帧（物理真值），不用回放外推位置**：`TrajectoryPlayback` 在「事件帧→下一帧」之间用**事件帧速度**做解析外推，而**撞库事件帧仍带朝向库的入射速度**（反弹后的速度要到下一帧才出现）→ 相邻帧间的采样位置会被外推到库线内侧数毫米~十几厘米（**吃库越快冲得越远**）。若按重采样位置 + 小容差（6mm）判飞出，高速吃库必被误判 → 把仍高速运动的球错误冻结在库边、`allDone` 截断后续轨迹（FL-018「吃库后立即停住」）。引擎记录帧在库线处反弹并 `enforceTableBounds` 钳回，正常反弹绝不越线，可靠区分真飞出。
2. **冻结仅用于真飞出；瞬时过冲只钳位不截断**：真飞出 = 记录帧本身越界 ≥6cm 且远离所有袋口嘴（14cm）→ 取首次时刻冻结+截断。事件帧间的瞬时外推过冲 → **仅化妆性钳位**（球心拉回库线）、**保持速度/运动态、不截断**，球继续按真实轨迹运动。
3. **容差须大于最快吃库的接触外推量级**：固定容差无法区分「快速吃库外推」与「真飞出」（两者都可 >6cm）——故判据应是「记录帧」而非「容差大小」。

### ⛔ PD-010 — 截图/可视化诊断驱动物理调试 + 漏斗袋口（而非弹珠箱）+ 求解=上报同保真度
**场景**：物理引擎/袋口/求解器表现「时好时坏、对参数微扰敏感」，肉眼说不清问题；或袋口进袋判定不稳定；或求解器选到的解与最终上报不一致。
**强制检查点**：
1. **先可视化再优化**：照相级渲染会遮住几何，无法判断「轨迹 vs 几何」是否自洽。对「多袋口×切角×塞×力度」矩阵用**纯 2D 顶视诊断渲染**（库边/jaw/落袋孔/标记/真实轨迹/进袋判定一并画出，见 `ShotScenarioRenderTests` 输出 `build/shot_probe/*.png`）出**接触表 PNG**，肉眼+偏移扫描定位根因，再动代码。
2. **真实袋口 = jaw 闸口 + 导球漏斗，不是弹珠箱**：高恢复系数的喉腔侧/后壁会把**对准的球**反复弹射 → 进袋带碎裂成斑点（每 0.1° 翻转，物理不真实）。正确模型：jaw 库（撞鼻=rattle/未进）作闸口 + **落袋捕获圆覆盖 jaw mouth**（干净进开口必落袋）→ 进袋带连续而宽。落袋即**吸球心到袋心**（画面=物理）。〔取代 PD-009 的「喉腔弹珠箱」实现，见 ADR-P10-03。〕
3. **求解 sim 必须与最终/上报 sim 同保真度**：否则求解器在「短模拟进袋带」寻优、结果按「全模拟进袋带」判定，两带可能错位甚至不相交 → 必然偶发「判进画面不进」。统一事件/时间预算。
4. **粗扫步长 ≤ 进袋带宽**：实测角袋进袋带仅 0.2–0.4° 宽，0.4° 步长会跨过窄带漏检 → 求解回退到坏解。收到 0.2°。
5. **直接进袋优先于绕库进袋**：评分计入「目标球落袋前撞库次数」（`objCushionsBeforePocket`），优先**直接进球**路线；**不要**为躲母球 scratch 把直接进球解推到慢/不稳的绕库 banking 解——近全直球母球必跟进时应**如实上报「母球进袋（失误）」**，而非伪造一个 clean 别扭解。
6. **不要在求解器层硬刚噪声景观**：若进袋带本身碎裂混沌，根因多在几何（如弹珠箱），改几何把景观本身变连续，比在求解器里加 band-center/clean-first 等启发式更稳（后者在碎带上反而脆）。
7. **求解目标 = 进球线方向正确，而非强制进袋**：瞄准求解的**首要目标**是「目标球碰后沿正确进球线(target→袋心)离开」，**不是**「一定要进袋」。大切角+小力度进不去是物理可接受的（如实报未进），但方向必须对。**禁止**「未进时退化为最小化 objMinDist（到袋心最近距离）」——这会挑到**绕库擦袋的多库翻袋解**（碰后初始方向错、只是反弹后蹭到袋附近）。正解：hybrid——能直接进袋(0 撞库)给离散基线 −10 优先；否则按**碰后方向 vs 进球线夹角误差**评分（平滑单峰、自动补偿 squirt+swerve+throw、天然排斥 banking）。方向景观平滑 ⇒ 粗扫无需密梳（0.5° 即可，predict 更快）。

### ⛔ PD-009 — 真实物理结构优先于调参 + 求解器评分量纲纪律 + 测量先于改几何
**场景**：实现/调试袋口等物理判定、以「真实模拟为黑盒」的瞄准/进球/走位求解器，或要按某份报告的「现象→根因」假设去改物理几何/常量时。
**强制检查点**：
1. **用真实结构涌现行为，而非调大一个判定圆**：袋口的进/rattle 应由**真实几何结构**（jaw 闸口 + 落袋捕获，见 `TableGeometry.chineseEightBallQiuJi`）自然涌现；**禁止**用「无脑放大捕获半径到能进球」糊弄。〔注：PD-009 原「喉腔侧壁/后壁弹珠箱」实现已被 PD-010 漏斗模型取代——喉腔反弹致进袋带碎裂；现袋口 = jaw 闸口 + 覆盖开口的落袋捕获圆。〕判定半径只代表物理洞口。
2. **分清两种逻辑**：(A) 正向判定（球沿某方向来 → 真实袋口几何决定进/rattle）；(B) 反向求解（固定力度+塞，采样/寻优最优接触点，在 A 下让球落袋）。B 的评分必须调用 A（真实模拟），不另算一套。
3. **评分量纲必须一致**：主距离项用米（~0.01–0.1）时，附加惩罚（scratch/出界/省力）只能是 **mm 量级（≤0.002）的轻推**；**禁止**用 1.0 这类大值——会压过主项把解逼到「啥也不沾的远解」（P10 45° 切角回归坑）。需要硬优先级时用**离散分层基线**（进袋 −10、未进 ≥0）。
4. **进袋/命中判定与画面同源**：用显示用的钳制轨迹最近点判定，而非裸 pocket 事件（袋口穿库会产生「判进但画面没到」假阳性）。物理**落袋孔半径** 与 **视觉标记半径** 必须**解耦**（`*DropRadius` vs `*PocketRadius`），改物理不动视觉。
5. **先实测证伪，再改几何**：把报告里的根因当待证伪命题，先用程序化实测（如 USDZ 网格遍历 `TableGeometryProbeTests`）核对——本任务实测证明库边/袋心/jaw 自洽（证伪"jaw 错位 17mm"），根因实为袋口缺真实结构 + 求解器坏局部最优。
