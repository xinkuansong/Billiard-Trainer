---
description: 产品文档增量更新、元信息、讨论日志与 research 目录约定
alwaysApply: true
---

# 产品文档管理

## 增量更新

- 编辑 `docs/` 下文件时 **保留已有内容**；用追加、修订段落或「变更记录」反映新结论，避免无说明的全文替换。
- 若需推翻旧决策：在 `docs/00-讨论记录.md` 写明 **原决策 → 新决策 → 原因**。

## 元信息

每个 `docs/01`–`docs/08` 文档顶部保留 YAML 或固定表格块（与模板一致）：

- `状态`：草稿 / 进行中 / 待评审 / 已定稿（择一）
- `版本`：如 0.1
- `最后更新`：YYYY-MM-DD

## 讨论日志（`docs/00-讨论记录.md`）

- 每条记录建议包含：**日期**、**主题**、**结论/决策**、**待确认**、**关联文档**（更新了哪些文件）。
- 长讨论可拆成多条，按时间倒序或正序均可，全项目保持一致即可。

## 调研资料（`docs/research/`）

- 原始检索要点、链接、摘录放此目录；文件命名建议：`YYYYMMDD-主题-简述.md`。
- 正式文档只写 **提炼结论**，并引用对应 research 文件。

## 与技能配合

- 章节结构与模板见 `.cursor/skills/ios-product-design/templates.md`。
