---
description: 台球瞄准原理权威定义。在编辑角度训练、SceneKit 场景、瞄准相关 View/ViewModel/Calculator 时激活。所有涉及切球角、幽灵球、横移量的代码和内容必须遵循本规则。
globs: ["**/AngleTraining/**", "**/Scene/**"]
alwaysApply: false
---

## Changelog

- 2026-04-14 v1.0：初始版本——修正瞄准原理体系，统一术语与公式

# 瞄准原理（Aiming Principles）— 权威定义

> 本文件是所有瞄准相关代码、内容、UI 文案的**唯一权威来源**。与本文件冲突时，以本文件为准。

## 1. 术语定义

| 术语 | 英文 | 定义 |
|------|------|------|
| **进球线** | pocket line | 目标球中心 → 袋口 的方向线 |
| **幽灵球中心** | ghost ball center | 目标球中心沿进球线**反向**偏移 2R 的位置（R = 球半径） |
| **击球线** | strike line | 白球（母球）中心 → 幽灵球中心 的方向线 |
| **切球角 α** | cut angle | 进球线与击球线的夹角 |
| **横移量 d** | lateral displacement | 幽灵球中心相对目标球中心在**垂直于击球线方向**上的位移 |
| **接触点偏移** | contact point offset | 接触点相对目标球中心在**垂直于进球线方向**上的距离（= R sin α） |

**注意**：统一使用「切球角」而非「切入角」。代码标识符使用 `cutAngle`，不使用 `entryAngle`。

## 2. 核心公式

### 主公式（横移量）

```
d = 2R × sin(α)
```

- d：横移量——幽灵球中心相对目标球中心，在垂直于击球线方向上的位移
- R：球半径（中八 R = 28.575mm，斯诺克 R = 26.25mm）
- α：切球角（0°–90°）
- 无量纲比：d/R = 2 sin(α)

### 幽灵球定位

```swift
let pocketDir = normalize(pocket - targetCenter)
let ghostBallCenter = targetCenter - 2 * R * pocketDir
```

幽灵球中心**始终在进球线的延长线上**，与目标球中心相距 2R，位于远离袋口的一侧。

### 派生公式（接触点偏移）

```
接触点偏移 = R × sin(α)
```

此公式仅描述目标球表面接触点的位置，**不是**瞄准的主公式。在 UI 中展示时须明确标注为「接触点偏移」，不可与横移量 d 混淆。

## 3. 瞄准原理完整流程

1. **确定进球线**：目标球中心 → 袋口方向
2. **定位幽灵球**：目标球中心沿进球线反向偏移 2R
3. **估计切球角 α**：进球线与击球线（白球→幽灵球）的夹角
4. **计算横移量 d = 2R sin α**：将角度转化为具体偏移
5. **白球沿击球线运动**：白球中心对准幽灵球中心击球
6. **接触瞬间**：白球与目标球的连心线与进球线重合 → 目标球沿进球线入袋

## 4. 厚薄球对照表（标准值）

| 切球角 α | sin(α) | d/R = 2sin(α) | 接触点偏移% | 通称 |
|----------|--------|---------------|-------------|------|
| 0° | 0.00 | 0.00 | 0% | 全球（满球） |
| 7.5° | 0.13 | 0.26 | 13% | ≈1/4 球 |
| 10° | 0.17 | 0.35 | 17% | ≈1/3 球 |
| 14.5° | 0.25 | 0.50 | 25% | — |
| 30° | 0.50 | 1.00 | 50% | 半球（1/2 球） |
| 48.6° | 0.75 | 1.50 | 75% | 3/4 球 |
| 90° | 1.00 | 2.00 | 100% | 极薄球 |

> 「接触点偏移%」= sin(α) × 100%，表示接触点偏离球心的比例（以 R 为单位）。
> 「d/R」= 2 sin(α)，表示横移量与球半径的无量纲比。

## 5. 代码生成约束

### 函数命名

| 函数 | 返回值 | 用途 |
|------|--------|------|
| `cutAngle(cueBall:targetBall:pocket:)` | 角度（度） | 计算切球角 |
| `lateralDisplacement(cutAngle:)` | `2 * sin(α)` | 横移量 d/R（无量纲） |
| `lateralDisplacementMM(cutAngle:ballRadius:)` | `2 * R * sin(α)` | 横移量 d（毫米） |
| `contactPointOffset(cutAngle:)` | `sin(α)` | 接触点偏移（R 的分数） |
| `ghostBallCenter(targetCenter:pocketCenter:ballRadius:)` | `CGPoint` / `SCNVector3` | 幽灵球中心位置 |

### Doc Comment 要求

```swift
/// 横移量 d/R = 2sin(α)。
/// 表示幽灵球中心相对目标球中心在垂直于击球线方向上的位移，以球半径为单位。
/// - Parameter cutAngle: 切球角（度）
/// - Returns: d/R 无量纲比
static func lateralDisplacement(cutAngle: Double) -> Double

/// 接触点偏移 = sin(α)，即接触点偏离目标球中心的距离（以 R 为单位）。
/// 注意：这是派生量，不是瞄准主公式。主公式为 d = 2R sin(α)。
/// - Parameter cutAngle: 切球角（度）
/// - Returns: 偏移量（R 的分数，0–1）
static func contactPointOffset(cutAngle: Double) -> Double
```

### UI 文案约束

- 原理页面主公式展示：**`d = 2R × sin(α)`**，标注「横移量」
- 若同时展示接触点偏移，须明确标注：「接触点偏移 = R × sin(α)」（派生量）
- 对照表列标题使用「d/R」而非「偏移量」
- 术语统一使用「切球角」，禁止使用「切入角」

## 6. 常见错误 — 强制检查点

> 以下错误曾在本项目中出现，实现时必须逐条核对。

### ⛔ 将接触点偏移当作主公式

**错误**：在原理说明、代码注释中写 `偏移量 = sin(α) × R` 并将其作为瞄准的核心公式。
**正确**：主公式是 `d = 2R sin(α)`（横移量）。`R sin(α)` 是接触点偏移，仅用于球面可视化。

### ⛔ 使用「切入角」术语

**错误**：代码或 UI 中使用「切入角」。
**正确**：统一使用「切球角」（cut angle），代码标识符用 `cutAngle`。

### ⛔ 混淆 d 与 d/R

**错误**：`d = 2 sin(α)`（缺少 R）。
**正确**：`d = 2R sin(α)`（有物理单位），`d/R = 2 sin(α)`（无量纲）。

### ⛔ 幽灵球定位方向反转

**错误**：`ghostBallCenter = targetCenter + 2R * pocketDir`（放到了袋口一侧）。
**正确**：`ghostBallCenter = targetCenter - 2R * pocketDir`（在进球线反向延长线上）。
