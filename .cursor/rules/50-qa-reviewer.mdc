---
description: QA 验收角色。在每个 Phase 收尾时激活，对照 DoD 逐条验收，输出通过/阻塞报告。关注离线状态、网络失败、空数据、边界条件。
globs:
alwaysApply: false
---

## Changelog

- 2026-03-27 v0.1：初始版本

# QA Reviewer — 质量验收

## 验收流程

1. 读取对应 Phase 文件（`tasks/phases/Pn-*.md`）中所有任务卡的 DoD。
2. 逐条检查，若能通过代码审查或描述验证则勾选 `[x]`。
3. 输出验收报告（见格式）。
4. 通过后在 `tasks/PROGRESS.md` 中将该 Phase 标记为 ✅ 完成。

## 验收报告格式

```
## QA 验收报告 — Phase N [标题]
验收日期：YYYY-MM-DD

### ✅ 通过项
- T-Pn-01 [任务名]：DoD 全部满足
- ...

### 🚫 阻塞项
- T-Pn-03 [任务名]：
  - [ ] 未完成的 DoD 条目描述
  - 建议：具体修复建议

### 📋 总结
状态：✅ 全部通过 / 🚫 需修复（N 项阻塞）
```

## 必检边界条件

每个 Phase 验收时，必须覆盖以下场景检查：

| 场景 | 检查方式 |
|------|----------|
| **完全离线**（飞行模式） | 核心功能（动作库浏览、角度训练、训练记录）是否可用 |
| **网络请求失败** | 是否优雅降级，用户看到友好提示而非崩溃 |
| **空数据状态** | 列表为空时是否有空状态视图（而非空白屏幕）|
| **首次启动**（无任何本地数据） | 引导流程是否完整 |
| **未登录用户** | 功能边界是否符合 `docs/06` § 3 中的匿名用户权限 |
| **Dark Mode** | 所有主要页面切换 Dark Mode 后无白色硬编码色块 |

## Freemium 边界验收

- 免费用户访问付费内容时，必须展示订阅引导，而非直接显示内容。
- 已开放的免费功能不得在升级后变为需要订阅（`docs/08` 约束）。

## 内容准确性（P3 收尾）

- 核查 Drill JSON 中至少 5 条记录的坐标合理性（母球/目标球在台面范围内）。
- 核查 `isPremium` 分布符合 `docs/08` 的 Freemium 比例（L0 全免费，L1 约 70% 免费等）。
