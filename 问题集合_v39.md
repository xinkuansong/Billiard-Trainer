# 问题集合 v39 — 计划页展示 + 课表节奏（v38 收官后的实机反馈）

> **真源声明**：本文件是本轮需求真源（2026-08-18 立档，**v39.11**，状态：**W0–W7 ✅ 全案收官**）。未 commit。
> 来源：用户实机看官方计划，指出三件事——① 顺序仍不合理（例：五分点在三分点前面，且不限于这一对）；② 一个动作名被拆成两行；③ 动作下出现「用户裁定……」等内部备注。W1 落地后二次截图：单球形剂量行过秃、多球形「N 球形」单独占行太散。
> 基线：v38 全案收官（v38.10，未 commit）。本轮**不重做**货架拆分、剂量可调维、I13 第 ②③④⑤ 条。
> **排期前置**：无。用户不填重列工作表、也不再做人工调课——语义课表由 W0 智能体产出并锁死。

---

## 一、需求回显（语义基准）

用户原话与走查收敛（未拍板的不写成已定）：

1. **计划里的教学顺序要合理**——不限于「五分点 / 三分点」这一对；12 份计划逐日核对。表面像「评分排反」，要先分清是人手课表、门禁逼出来的，还是标签骗人。
2. **日课行不要把一个动作拆成两行**——动作名与剂量不应在 `·` / 数字处断开（截图：`五分点瞄准线练习 · 8 ×` 下一行 `15`；`远台直线出杆检验 · 2` 下一行 `球形`）。W1 已禁中拆。
3. **学员看不到内部备注**——「用户裁定例外（v34 R3）……」不得出现在计划页（截图已实锤）。
4. **剂量行统一格式、多球形要紧凑**（W1 后二次截图，2026-08-18）：单球形不要只写 `6 × 12`；多球形不要把「2 球形」单独占一行。

| # | 需求 | 细化口径 | 状态 |
|---|------|----------|------|
| R1 | **展示：动作名与剂量不中拆** | 禁止 `"\(name) · \(dose)"` 一条可中拆的 `Text`。W1 已落地。剂量行格式以 **R5** 为准，不再把光秃 `m × n` / 单独一行「N 球形」当终态 | 已确认（W1 ✅） |
| R2 | **展示：`doseNote` 不上学员屏** | JSON 里的 `doseNote` 继续留给 I6b 门禁豁免。计划页、动作页「建议训练量」都不渲染原文。本轮不另写学员可读改写 | 已确认（截图） |
| R3 | **顺序：先决看 description，不看课名直觉** | 12 份都按语义改到合理。不该出现在该计划技能线里的动作 **替换**，不硬留。用户不填工作表、不事后手调 | 已确认（2026-08-18） |
| R4 | **不按六轴自动重排日课** | 运行时本来就不按评分重排。禁止为「看起来更顺」写自动排序器。人手课表 + 门禁口径一起改 | 已确认（v38 R1 延续） |
| R5 | **展示：剂量行统一格式** | 见下方版式。序号 `01` 后不加「1 球形」。单球形第二行 = `球形1` + 模式 + `m × n`。多球形「N 球形」与动作名**同一行**，其下直接逐球形明细 | 已确认（2026-08-18 二次截图） |

**R5 计划行版式（采用定义，2026-08-18）**

用户原话：「只有一个球形的，01 这种后面不需要说明球形数量，但是下面不要只是像 6x12 这种，也按照统一的格式，比如球形1 逐位重复 6x12」；「有一个以上球形的，2 个球形不要换到两行，看起来太乱了，不够紧凑」。

```
单球形（现状第二行只有 6 × 12 → 改）
  01  斯登角度停球
      球形1  逐位重复  6 × 12

多球形（现状「2 球形」独占第二行 → 改）
  01  高杆跟进走位                    2 球形
      球形1  逐位重复  4 × 15
      球形2  逐位重复  5 × 15
```

- `01` 是日课行序号，后面只跟动作名，**不加**「1 球形」。
- 剂量明细统一：`球形k` + `逐位重复` / `整链走位` + `m × n`（与现多球形展开行同一套 `SuggestedDoseLine`）。
- 「N 球形」（N≥2）与动作名同一行（名左、数量右或名后空一格），**禁止单独占第二行**，也禁止再拆成「2」/「球形」两行（W1 已禁拆）。
- 多球形默认仍全展开；chevron 可留。单球形无展开。
- 动作页「建议训练量」：若 Resolver 单球形 `title` 从 nil 改为「球形1」，动作页会连带同一格式——允许，算 W1b 范围内。

**术语对照（FL-026，2026-08-18 已确认）**

| 你的原话 | 采用定义（2026-08-18 拍板） |
|----------|------------------------------|
| 「是不是评分的问题」 | **部分是。** 不是 App 运行时打分重排。是 I13 第 ① 条用六轴 scalar 卡周。拍板：scalar **只作参考**，语义合理优先；不该出现在该计划里的动作要换成合适的 |
| 「五分点在三分点前面」 | 按 **description 语义** 调，不按课名直觉。`c023` 与 `c052` 不同轴——不为「三分点必须先于五分点」而改跨计划序。准度Ⅱ 内「极薄 / 三分点」名称与切角上界必须对齐 |

**明确不在本轮范围**

- 新建 / 删除计划文件；不新建 drill id（替换只用库内已有 83 条）。
- 自定义计划（v35）、雷达、剂量数值真源、衰减公式（R7）。
- 按 L / 难 / 六轴写自动排课。
- 精讲全文重写（本轮只改会骗人的课名 / L / 难 / load / 短 description；`c007`/`c023` 按 D-v39-3=A 改 description / coachingPoints / 标准，不改序列）。

**范围红线**

- ⛔ `doseNote` 字段不删（I6b 凭它豁免 c002 / c022）。只停渲染。
- ⛔ 完成标准禁止写「全量测试后工作区零 diff」（未盘点写盘测试全集，PD-027）。
- ⛔ 改 / 删 I13 断言必须附失败机理（PD-027）。禁止删断言求绿。
- ⛔ 改归属 / 引入序必须同步 W0 表 + `V21W5` / `V22W*` 钉死集合，并写失败机理。
- ⛔ 不修绿既有 `pocket ''` 等基线失败（FL-027）。

---

## 二、拍板项（W2 前必须清；W1 可先做）

| # | 问题 | 选项 | 推荐 | 状态 |
|---|------|------|------|------|
| D-v39-1 | **I13 第 ① 条 + 不属本计划的动作** | 用户原话：「六轴 scalar 是参考，语义上合理才是最重要的，而且不该出现在当前计划中也应该替换成合适的」 | **D 扩**：① 改为「首次引入不得早于 W0 建议周」；scalar 只对照、不卡周。不属该计划技能线的动作从该计划 **替换**（库内另选），不硬留 | ✅ **D 扩** |
| D-v39-2 | **课名 / L / 难 / load** | 用户：「所有的都要改……针对所有计划中不合适的」 | **B 扩**：所有会骗人的课名/L/难/load 都改，不限初拟 6 条。名单 W0 收口 | ✅ **B 扩** |
| D-v39-3 | **基本功高低杆载体** | 含在「所有的都要改」 | **A**：改 `c007`/`c023` 正文，锁中杆（不改序列） | ✅ **A** |
| D-v39-4 | **三分点 vs 五分点 / 谁来调课** | 用户：「根据语义合理进行调整；之前计划的人工调整并没有做，后面也不计划做」 | **语义裁定**：不同轴则不改跨计划序；准度Ⅱ 内名称与切角对齐。W0 由智能体出语义课表，**不走人工工作表** | ✅ |
| D-v39-5 | **执行模型** | 用户指定 | 全批 `cursor-grok-4.6-high-fast`；课表线程可并行 | ✅ |

W0 产出前：禁止改 `Plans/*.json` / I13 脚本。W1 展示层可并行。

---

## 三、批次表

> 依赖：**W1**（展示）∥ **W0**（语义课表）→ **W2**（I13 ①）→ **W3 ∥ W4 ∥ W5** → **W6** → **W7**。
> **W1b**（R5 剂量行格式）只改计划行展示，与填课/I13 无文件交集，可与 W3–W5 并行。

| 批次 | 范围 | 量级 | 依赖 | 模型 |
|------|------|------|------|------|
| W0 语义课表（智能体，不手填） | 产出 `docs/research/20260818-v39-语义课表.md`：12 份逐日热身/主课 + 替换清单 + 标签改名清单。先决只引 description。用户不填工作表。**本文件 Changelog 记下路径后才许 W2 / 填课** | 中（纯文档） | 拍板已清 | `cursor-grok-4.6-high-fast` |
| W1 计划页 / 动作页展示 ✅ 2026-08-18 | ①`PlanDetailView.drillTrackRow`：动作名与剂量拆成两个 `Text`，禁止 `"\(name) · \(dose)"` 一条可中拆。②计划页、动作页不渲染 `SuggestedDoseLine.note`。③`doseNote` 留在 Resolver / JSON / I6b。④单测锁计划行 accessibility 不含「用户裁定」。⑤截图：基本功含 `c023` 的一天、含 `c022` 的一天（展开多球形） | 小 | 无 | `cursor-grok-4.6-high-fast` |
| W1b 剂量行统一格式 ✅ 2026-08-18 | 按 R5 改 `drillTrackRow` + `suggestedDoseLines`：单球形第二行 `球形1`+模式+`m × n`；多球形「N 球形」与动作名同行，其下明细不变。读屏跟上。`doseNote` 仍不上屏。不改 Plans / I13 | 小 | W1 | `cursor-grok-4.6-high-fast` |
| W2 I13 第 ① 条 ✅ 2026-08-18 | 按 D-v39-1：周序改为「不得早于 W0 建议周」；删/改 scalar 周序不降。契约 §7 I13 + `i13_week_order_drop` 按新口径改写并附失败机理。**不改课表 JSON** | 中 | W0 | `cursor-grok-4.6-high-fast` |
| W3 分离角摊周 ✅ 2026-08-18 | 按 W0 语义课表改 `plan_separation.json`。默认：W1 只 90°/厚/薄；后 7 条回 W2–W4。替换不属本线的热身 | 中 | W2 | `cursor-grok-4.6-high-fast` |
| W4 特殊球摊周 ✅ 2026-08-18 | 按 W0：W3 只贴库+跳球；W4 才安全+解球。跳球至少复现 1 次 | 中 | W2 | `cursor-grok-4.6-high-fast` |
| W5 全能摊周 + 咬合 ✅ 2026-08-18 | 按 W0：W1 只开球+三球；五球/蛇彩回后周。不属开档的热身换成合适动作 | 中 | W2 | `cursor-grok-4.6-high-fast` |
| W6 其余 9 份 + 标签 + 基本功正文 ✅ 2026-08-18 | 按 W0 改其余计划 JSON；杆法Ⅰ序、杆法Ⅱ热身、走位Ⅱ双首引、准度Ⅲ热身；D-v39-2 标签清单；D-v39-3 `c007`/`c023` 正文。同步 `V21W5` 等钉死序 | 大 | W2 | `cursor-grok-4.6-high-fast` |
| W7 回归 ✅ 2026-08-18 | `make verify-gate` + `make build` + 聚焦计划测试 + I13 自测。截图：展示 2 张（按 R5）+ 分离角或全能 W1 日课 | 中 | W1 + W1b + W3–W6 | `cursor-grok-4.6-high-fast` |

**依赖图**

```
W1 展示 → W1b 剂量行格式 ─────────────┐
W0 语义课表 → W2 I13①                 ├──→ W7 回归
              ├──→ W3 分离角          │
              ├──→ W4 特殊球          │
              ├──→ W5 全能            │
              └──→ W6 其余 / 标签 ────┘
```

W1b 与 W3 / W4 / W5 文件集合不相交，可并行。W6 可能改 W0 序与多份 JSON，**串行、放在三处摊周之后**。

### 各批完成标准

**通用**（W1–W7）：`make build` SUCCEEDED；`ReadLints` 0 新增；改动文件与批次声明一致（`git diff --name-only`）；⛔ 不修绿既有 `pocket ''`。

- **W0**：`docs/research/20260818-v39-语义课表.md` 含 12 份逐日表 + 替换清单 + 标签清单；先决只引 description；本真源 Changelog 记下该路径。
- **W1**：`rg 'name\\) · \\(' QiuJi/Features/Training/Views/PlanDetailView.swift` 无命中；`rg 'line.note' QiuJi/Features/Training/Views/PlanDetailView.swift QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift` 无上屏分支（Resolver 仍可持有 `note`）；I6b 仍绿（`doseNote` 未删）；新增或改写的单测断言「`c002`/`c022` 的 `doseNote` 非空但计划行 accessibility 不含『用户裁定』」；截图两张亲验。`make verify-gate` FAIL 0。
- **W1b**：单球形计划行第二行含「球形1」与模式标签（`逐位重复` 或 `整链走位`），不是光秃 `m × n`；多球形条目行无单独第二行「N 球形」（与动作名同行）；`rg` 确认单球形不再把 `planEntrySummaryText()` 当唯一剂量行。单测：`suggestedDoseLines` 单球形 `title == "球形1"`（改 `XCTAssertNil` 须附失败机理：统一行格式）；读屏仍不含「用户裁定」。截图两张亲验：杆法Ⅰ单球形（斯登 `c016`）、杆法Ⅰ多球形展开（高杆跟进 `c003`）。不改 Plans / I13。
- **W2**：契约 I13 行与脚本一致；`i13_week_order_drop` 按新口径改写或删除并附失败机理；构造性 1 例 exit 1 后还原；`i13_intro_order_vs_w0` 仍在（第 ⑤ 条未动）。
- **W3–W5**：该计划 focused 首次引入序与周 = `20260818-v39-语义课表.md`；I13 对该计划 FAIL 0；抽 1 条 repetition 衰减核「位置数不变、颗数下降」。
- **W6**：`V21W5` / 相关 `V22W*` 断言与新序一致并写失败机理；标签与 `c007`/`c023` 正文按语义课表清单改。
- **W7**：`make verify-gate` FAIL 0；聚焦 `V21W5`+`V22W2`+`V22W4`+`V22W5`+`V37W4PlanShelfUITests`；截图三张（R5 展示 2 + 分离角或全能 W1 日课）。

---

## 四、12 份走查摘要（2026-08-18，事实层）

口径：引入序 / 节奏 / 热身 / 标签。4 路只读子智能体 + 主控抽核 JSON。运行时按人手 JSON 上屏。

| 计划 | 主课轴 | 要不要动课表 | 最硬问题 |
|------|--------|--------------|----------|
| 基本功 | 握杆→站位→手架→出杆→定杆→瞄准线→远台 | 展示必动（`c023` 换行、`c022` 泄露）。课序可选 | `c007`/`c023` 正文过早用高低杆（D-v39-3） |
| 准度Ⅰ | 近台→半台→中袋→小角→中台切→斜角 | 展示：`c002` 会泄露 doseNote。课序不用倒 | `c002` 切角更薄却标 L0/难 2 |
| 杆法Ⅰ | 停点→定杆→跟进→缩杆→远台 | 同周应对调 `c014` 先于 `c016` | `c016` 课名带「角度」，正文「不是切角」 |
| 力度 | 三档→五档→轻推→强弱→控力→全力 | 可不改序 | `c046` 标 L1 却收官 |
| 分离角 | 90°→厚→薄→不吃库→高低杆→定杆→走位→综合→吃库 | **摊周**（W3） | I13①：`c025` scalar=4，后 7 条首次引入全挤 W1 |
| 杆法Ⅱ | 近台挤偏→长台→塞量→变线→高低杆加塞 | 换热身（W6） | W1 用 `c017` 远台缩杆（难 3）热身近台挤偏（难 2） |
| 走位Ⅰ | 不吃库→入位→一库→高低杆一库→四球 | 可不改序 | `c037` L2 夹在一库家族 L1 前（标签） |
| 走位Ⅱ | 半台→两库→对角→直线组合→三库→蛇彩→围 8 | W4 D1 拆开双首引 | `c042`+`c082` 同日首次 |
| 准度Ⅱ | 中袋角度→远台斜线→远台直线→极薄→三分点 | 标签优先于改序 | `c063` 名「极薄」正文 43°–58°；`c052` 名「三分点」正文到 77° |
| 准度Ⅲ | 小角度带塞→中大→远台 | 热身几何 | `c063` 热身不咬合小角度带塞 |
| 特殊球 | 翻角袋→K→翻中袋→贴库→跳球→安全→解球 | **摊周**（W4） | W3 D2 三课同日首次；跳球只出现一次 |
| 全能 | 开球/三球→五球→蛇彩→九球→Ghost→全台 | **摊周**（W5） | W1 提前首引五球+两级蛇彩；`c078` 满剂量约 100′ 热身 |

**五分点 / 三分点（必须用 description，已读 JSON）**

- `c023`：8 号在台面中点附近，同一条瞄准线，变量是杆法/力度。L1 / 难 2 / aim=2。主课在基本功。
- `c052`：切角约 41°→77°，全程中杆，专练厚薄与送袋。L2 / 难 3 / aim=4。主课在准度Ⅱ W4。
- 结论（已拍板）：不同轴，不改跨计划序；准度Ⅱ 内名称与切角上界必须对齐。

**`doseNote` 全库仅 2 条**（2026-08-18 `rg`）

- `drill_c022`：`用户裁定例外（v34 R3）：1 杆球形按 15×5 打 75 颗，轮数不等于杆数`（基本功 W4，多球形，展开明细里）
- `drill_c002`：`用户裁定例外（v34 R3）：斜角入底角袋按每位置 8 颗训练，非默认 15`（准度Ⅰ W3，单球形，标题下）

---

## 五、锚点索引（2026-08-18 读码核实）

| 对象 | 路径 | 用途 |
|------|------|------|
| 日课行拼装 | `QiuJi/Features/Training/Views/PlanDetailView.swift:430–523` | W1：名/量拆 `Text`。W1b：L465–476 第二行现为光秃 `doseText`；L471 `planEntrySummaryText` 多球形 =「N 球形」独占一行；L493+ 展开明细已是 `球形k`+模式+量 |
| 剂量摘要 | `QiuJi/Data/Services/TrainingDoseResolver.swift:126–156` | `planEntrySummaryText` 单球形 = `m × n`、多球形 =「N 球形」。`suggestedDoseLines` L153 单球形 `title = nil`（W1b 改为「球形1」） |
| `SuggestedDoseLine.note` | 同文件 `:28–38` | 字段保留 |
| 动作页建议量 | `QiuJi/Features/DrillLibrary/Views/DrillDetailView.swift:407–443` | W1：L438–443 同样上屏 note |
| I6b / doseNote 契约 | `.kiro/steering/content-data-contract.md` §5.6.2 L381–384 | 例外机制不删 |
| I13 操作定义 | 同文件 §7 I13（2.11） | W2 改第 ① 条 |
| I13 实现 | `scripts/verify_tutorial_sync.py:1083–1228` | 周序不降 L1142–1150；引入序 L1214–1226 |
| I13 自测 | `scripts/selftest_content_invariants.py:382–418, 506–515` | `i13_week_order_drop` / `i13_intro_order_vs_w0` |
| W0 主课序 | `docs/research/20260814-v38-先决与主课名单.md` §3 | 第 ⑤ 条对照；摊周改建议周、原则上不改序 |
| v39 语义课表 | `docs/research/20260818-v39-语义课表.md` | W0 产出；W2 起对照 |
| 12 份课表 | `QiuJi/Resources/Plans/plan_*.json` | W3–W6 |
| 杆法Ⅰ序钉死 | `QiuJiTests/V21W5PlanContentTests.swift:10–30` | W6 若对调 c014/c016 必改，附失败机理 |
| 分离角引用集合 | `QiuJiTests/V22W4PlanSeparationTests.swift` | 摊周若 `allIds` 不变则不改 |
| 货架 UI | `QiuJiUITests/V37W4PlanShelfUITests.swift` | W7 回归，不改份数 |
| 剂量单测 | `QiuJiTests/TrainingHomeViewModelTests.swift:354–433` | W1b：`XCTAssertNil(repLines[0].title)`（L393）与「单球形摘要 = 内联 m × n」（L374–375）会红，须改期望并附失败机理 |

---

## 六、规格测试钉死清单（改序 / 改门禁前必读，PD-027）

| 文件 | 断言要点 | 何种改动会红 | 失败机理（预写） |
|------|----------|--------------|------------------|
| `V21W5PlanContentTests` L26–29 | 杆法Ⅰ focused 序 = c016→c014→… | W6 对调前两堂 | 先决改为「先定杆再打点灵敏度」；更新期望数组，禁止删用例 |
| `V22W2PlanAccuracyTests` | 准度Ⅰ `allIds` | 本轮默认不增删准度Ⅰ引用 | 若只停渲染 doseNote，不红 |
| `V22W4PlanSeparationTests` | 分离角 `allIds` | 摊周不请出/迁入则不红 | 只改 week/day，不改集合 |
| `V22W5PlanEnglishTests` | 主轴 6 条；禁 c076–c078 | 换热身若仍是杆法Ⅰ id | `allIds` 可能增减，按新热身更新 |
| `selftest` `i13_week_order_drop` | 把 c032 首次引入提前到 W2（建议周 3）⇒ exit 1 且含「建议周」 | 旧「对调 weekNumber ⇒ scalar 下降」已废 | W2 已按新口径改写；失败机理见用例注释 |
| `i13_intro_order_vs_w0` | 对调准度Ⅰ前两堂 ⇒ 序 ≠ W0 | W0 序未改则仍有效 | 不删 |
| `TrainingHomeViewModelTests` L393 | 单球形 `suggestedDoseLines[0].title == nil` | W1b 单球形也标「球形1」 | 失败机理：R5 统一行格式；改期望为 `"球形1"`，禁止删用例 |

写盘 UI 测试（W1/W7 截图若走现有 suite）：`V37W4PlanShelfUITests`、`V29W7PlanProgressUITests`、`XV313PremiumPlanWalkthroughUITests`。完成标准**不**要求「测试后零 diff」。

---

## 七、版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v39.0 | 2026-08-18 | 立档。用户三问（顺序 / 换行 / 用户裁定）收成 R1–R4。走查结论：换行与泄露是 `PlanDetailView` 拼装；节奏堆叠是 I13 ①；五分点/三分点不同轴。待拍板 D-v39-1～5。W1 可先做。 |
| v39.1 | 2026-08-18 | 拍板已清。D-v39-1=D 扩（scalar 参考、语义优先、不属本计划的替换）；D-v39-2=B 扩（所有骗人标签都改）；D-v39-3=A（`c007`/`c023` 锁中杆）；D-v39-4=按语义、不手填工作表；D-v39-5=全批 `cursor-grok-4.6-high-fast`。W0 改为智能体产出语义课表。W0+W1 并行开工。 |
| v39.2 | 2026-08-18 | **W0 ✅**。语义课表锁死：`docs/research/20260818-v39-语义课表.md`。83 条主课归属不变。唯一引入序变化：杆法Ⅰ `c014` 先于 `c016`。摊周：分离角后 7 条、特殊球安全/解球、全能五球/蛇彩回到建议周。热身替换：杆法Ⅱ `c017`→`c014`，准度Ⅲ `c063`→`c013`，全能去掉 `c078`/`c054`/`c082`。主控抽核 `c013`/`c014`/`c063`/`c052` description 与表内引文一致。W1 仍在跑。 |
| v39.3 | 2026-08-18 | **W1 ✅**。计划行拆成动作名 + 剂量两个 `Text`；计划页/动作页不渲染 `SuggestedDoseLine.note`；`doseNote` 仍在 Resolver / JSON / I6b。单测锁 c002/c022 note 非空但读屏不含「用户裁定」。截图：`build/v39-w1-logs/`。 |
| v39.4 | 2026-08-18 | **W2 ✅**。I13 ① 改为「focused 首次引入不得早于语义课表 §5 建议周」；删 scalar 周序不降。契约 **2.12**。`i13_week_order_drop` 改写为把 c032 首次提前到 W2（建议周 3）⇒ exit 1。现网 I13 ① **15 条预期红**（对照 §4.2，无误杀；`c082` 周未变、`c018` 建议周列勘误 4→3 后现网 W3 不红）。未改 Plans/Drills。`make verify-gate` 现网会红，待 W3–W6 填课归零。 |
| v39.5 | 2026-08-18 | **方案增补 R5 / W1b**。W1 后二次截图：单球形第二行不要光秃 `6 × 12`，改为 `球形1 逐位重复 6 × 12`；多球形「N 球形」与动作名同行，禁止单独占第二行。W1 换行修复仍有效。W1b 可与 W3–W5 并行。 |
| v39.6 | 2026-08-18 | **W1b ✅**。单球形第二行 `球形1 + 模式 + m × n`；多球形「N 球形」与动作名同行。主控亲验截图 c016 / c003；单测 3/3；build SUCCEEDED。未改 Plans / I13。 |
| v39.7 | 2026-08-18 | **W3 ✅**。分离角后 7 条首次引入回到建议周。该计划 I13 FAIL 0；V22W4 2/2；build SUCCEEDED。**小偏差**：W2 D1 热身课表写 `c025↓`，因 I13 ②（c025 scalar 4 > c084 scalar 2）落地为 `c026↓`。主课周未改。c032 开档咬合只写 `reviewFrom`、未标 decay（计划内首次不得 decay）。 |
| v39.8 | 2026-08-18 | **W4 ✅**。特殊球 W3 只贴库+跳球（c059 D2 首次、D3 复现）；安全/解球 c060/c061 首次回 W4。该计划 I13 FAIL 0；`testPlanAdvancedFirstFocusedOrder` 绿；build SUCCEEDED。**小偏差**：W3 D1 热身课表 `c055↓` → 落地 `c056↓`（I13 ②，c055 scalar 3 > c058 scalar 2）。c059/c061 无 perFormation，复现保持完整剂量未标 decay。 |
| v39.9 | 2026-08-18 | **W5 ✅**。全能 W1 只开球+三球；c068 首次 W2；c069/c071 首次 W3。删 c078/c054/c082 开档咬合。该计划 I13 FAIL 0；build SUCCEEDED。**小偏差**：W3 D1 热身课表 `c068↓` 因 I13 ②（scalar 3>2）且无更低 scalar 可换，落地为无热身。`testPlanFullskillFirstFocusedOrderAndBites` 咬合段预期红，W6 改。 |

| v39.10 | 2026-08-18 | **W6 ✅**。其余 9 份课表 + 标签 + c007/c023 元数据锁中杆。杆法Ⅰ序 c014→c016；I13 ⑤ §3 对照表同步。I13 FAIL 0；V21W5+V22W5 13/13。热身 4 处因 I13 ② 换课/无热身（主课周未改）。精讲正文仍描述序列原杆法（不改序列）。 |
| v39.11 | 2026-08-18 | **W7 ✅ / 全案收官**。`verify-gate` FAIL 0（I13 464/0）；build SUCCEEDED；聚焦 17/17 + 货架 UI 1/1；I13 构造性 2/2。截图 `build/v39-w7-logs/`：c016 单球形 `球形1 逐位重复`、c003「2 球形」同行、分离角 W1 只 90°/厚。c016 帧滚到 W2 头，剂量行仍可见。未改课表/门禁求绿。未 commit。 |

全案收官。遗留：若干热身因 I13 ② 换课或取消（主课周未改）；c007/c023 精讲仍写序列原杆法；既有 `pocket ''` 未修；工作区未 commit。下一动作由用户定（建议 commit，或开 v40）。
