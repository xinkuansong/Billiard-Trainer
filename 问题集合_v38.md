# 问题集合 v38 — 官方计划动作归属与教学序（v37 R4 收口）

> **真源声明**：本文件是本轮需求真源（2026-08-14 立档，**v38.10**，状态：**W0–W7 全案收官 ✅**）。
> 来源：v37 工程收官后用户指出「计划中动作顺序仍不合理」；走查确认这就是 v37 R4（循序渐进）未交付的内容层。用户确认：要把**全库 83 条**纳入重排；「动作增删」= **计划里收哪些 / 不收哪些**（不是新建或删除 drill 文件）；必要时可加计划；先列方案，不直接并行改 11 份 JSON。同日补口：衰减只减重复遍数，不得砍杆。
> **基线**：v37 已入库 `7d229fa`（六轴 / 雷达 / 11 份计划 / I13）。本轮在此基线上改计划归属与课表，不重做雷达与打分。
> **拍板（2026-08-14）**：D-v38-1=B 按需 / D-v38-2=A 拆准度Ⅲ / D-v38-3=全批 `cursor-grok-4.6-high-fast` / D-v38-4=A 计划侧 `FormationRounds.ballsPerRound`（用户原话「只在计划里改」= 不改 drill 剂量真源）。

---

## 一、需求回显（语义基准）

用户原话与收敛（未拍板的不写成已定）：

1. **计划里动作顺序要合理**——同一计划中更基础的动作不得排在更难的后面（例：近台不得在半台之后当「起步」）。这是 v37 R4 的本意，不是新功能。
2. **要从全库挑动作，计划内可增删**——一份计划的主课名单可以加、可以拿掉、可以换到另一份；不是只在现有名单里调先后。
3. **必要时可增加计划**——默认不加；只有「调完归属，现有周数仍塞不下两条先决线」才加。
4. **先方案、后执行**——先决图与主课名单锁死之前，禁止 11 路并行改 `Plans/*.json`。
5. **衰减只减遍数，不砍杆**——「8 杆 × 15」只能变成「8 杆 × 10」；球形「8 杆练 10 遍」只能变成 8×8、8×6。杆数（位置 / 链长）锁死。

| # | 需求 | 细化口径 | 状态 |
|---|------|----------|------|
| R1 | **教学序先于负荷包络** | 先决看课名 / `description` 原文（近/半台、切角数字、有无塞、库数等）。六轴 `load` 只做包络，不合成总分、不单独当排序键（沿用 v37 R1/R7） | 已确认 |
| R2 | **主题句 = 当天主课技能** | `weeks[].theme` 与该周 focused 首次引入必须同一技能线；禁止「主题写近台、第一堂上半台」 | 已确认 |
| R3 | **热身语义上不难于当天主课** | 同技能先决上热身 ≤ 主课。跨计划 `reviewFrom` 咬合允许略重，必须显式标注，不得当「开档主课」 | 已确认 |
| R4 | **计划内名单可增删** | 从 83 条库内挑选。一条动作的去向只能是：某计划 **主课** / **仅复现**（热身或 decay，须有来源）/ **不进官方计划课表**。禁止静默孤儿（库里有、哪份计划都不登记去向）。**不必**每条都当 focused 主课（D-v38-1=B，按需） | 已确认 |
| R5 | **货架按需加减** | 现 11 份可保留；加计划只解决双技能线焊死。本轮**加** `plan_accuracy3` 准度Ⅲ·带塞（Pro，D-v38-2=A）。免费三份（`plan_beginner` / `plan_accuracy` / `plan_cueball`）不撤回 | 已确认 |
| R6 | **衰减复现与引入序分开** | 衰减仍是课次级、可跨周。末日「昨日新课当热身、更早课 decay 当主课」若造成热身难于主课，必须改模板，不得再用衰减当借口。**怎么减量见 R7**（v37 用减 `rounds` 表达衰减，对 repetition 等于砍位置，与 R7 冲突） | 已确认 |
| R7 | **衰减只调「重复维」，不砍杆** | 上屏 `m × n` 的 **m（杆）锁死**，只许降 **n（重复）**。按 mode 对字段（2026-08-14 读码，见 `TrainingDoseResolver.suggestedLineText`）：<br>• **`repetition`（逐位重复）**：文案「位置数 × 每位置颗数」= `defaultRounds × ballsPerRound`（例 8×15）。衰减只能降 `ballsPerRound`（8×10），**禁止**把 `formations[].rounds` 降到 `< defaultRounds`（那是少打几个位置）。<br>• **`sequence`（整链 / 球形）**：文案「整链杆数 × 遍数」= `ballsPerRound × defaultRounds`（例 8×10 = 这条链打 10 遍）。衰减只能降 `defaultRounds`（8×8、8×6），**禁止**改 `ballsPerRound`（杆数 = 序列实测，I6b 锁死）。<br>首次引入仍须完整剂量。多球形仍允许按周整颗不收（v34 阶梯）；**已选中的球形内部杆数必须全覆盖**。计划侧字段 = `FormationRounds.ballsPerRound`（D-v38-4=A） | 已确认 |

**明确不在本轮范围**：

- 新建 / 删除 / 改写 drill 文件（`description` / 精讲 / `load` / 剂量数值）。课内阶梯（如 c002 从约 58° 起）若要改，另立内容批次。
- 自定义计划（v35）、用户画像雷达、六轴自动计算。
- 重做 I12 / 雷达 UI / Schema V4。

**范围红线**：

- ⛔ 不动 v34 剂量数值真源（drill JSON 里的 `defaultRounds` / `ballsPerRound`）；本轮只改计划怎么引用。衰减降的是计划侧重复维，不是改内容真源。
- ⛔ **衰减不得砍杆**（R7）：`repetition` 禁止 `formations[].rounds < defaultRounds`；`sequence` 禁止改 `ballsPerRound`。现网计划里 repetition + `decay` + `rounds` 低于内容（如 `plan_beginner` 对 c006 两球形 4→3）是 v37 误用，W2–W6 重写时必须改掉，禁止照抄。
- ⛔ 不删 I13 现有四条（周包络 / 热身 scalar / 衰减单调 / `reviewFrom` 外键）；只允许在锁死名单后**加**内容层检查。W1 须加「衰减未砍杆」检查（或并入 I11）。
- ⛔ 改归属集合时必须同步改规格测试里的钉死集合（见 §六），并写失败机理（PD-027）。禁止删断言求绿。
- ⛔ 完成标准禁止写「全量测试后工作区零 diff」（未盘点写盘测试全集）。
- ⛔ 生成式配置两头查（FL-031）：若加计划文件，`project.yml` / `project.pbxproj` / `index.json` 必须一起进门禁视野。

---

## 二、拍板项（2026-08-14 已清）

| # | 问题 | 选项 | 推荐 | 状态 |
|---|------|------|------|------|
| D-v38-1 | **83 条是否都必须在某计划当过 focused 主课**（动 D-v37-3 的 3b） | **A.** 守住：只能搬家，不能请出主课席。**B.** 放开：允许「仅复现」或「不进官方课表」，每条必须在 W0 名单表登记去向 | **B** | ✅ **B**（用户：「不一定，按需」） |
| D-v38-2 | **带塞准度（c076/c077/c078，及同线的远台带塞）放哪** | **A.** 拆 `plan_accuracy3` 准度Ⅲ·带塞（Pro，新 id）。**B.** 并进杆法Ⅱ（须改 `V22W5PlanEnglishTests` 禁令）。**C.** 留在准度Ⅱ，但强制整周后置（4 周仍可能挤） | **A** | ✅ **A**（用户：「按推荐」） |
| D-v38-3 | **执行方式与模型** | 按 `01-subagent-model-selection` 确认。W0/W1 判断密集建议主控直做；W2–W6 名单锁死后可按线程并行 | 全批 grok 4.6 | ✅ 全批 `cursor-grok-4.6-high-fast`（后续会话不再询问） |
| D-v38-4 | **`repetition` 衰减怎么表达「每位置颗数降低」**。事实：现 `PlanDrillDose` 只能改 `formations[].rounds`（= 位置数）或 `roundsPerFormation`（倍数 ≥1，只能加不能减）；**没有**计划侧覆盖 `ballsPerRound` 的字段 | **A.** `FormationRounds` 加可选 `ballsPerRound`（decay 时 repetition 用它压每位置颗数，`rounds` 必须 = 内容 `defaultRounds`；sequence 仍只压 `rounds`）。**B.** 顶层一个数统压。**C.** 继续减 `rounds`（⛔ 砍位置） | **A** | ✅ **A**（用户：「只在计划里改」= 不改 drill 剂量真源。W1 仍要给计划 DTO 加此字段，否则 8×15→8×10 写不进 JSON；这不是改动作内容） |

W0 过目前：只许改本真源与研究文档草案，**禁止改 `Plans/*.json`**。

---

## 三、批次表

> 依赖：**W0**（先决图 + 名单，用户过目）→ **W1**（剂量可调维 + 规格测试口径 + `plan_accuracy3` 槽位）→ **W2 ∥ W3 ∥ W4 ∥ W5 ∥ W6**（各线程填课，互不改对方主课名单）→ **W7**（全能 + index + I13 内容层 + 回归）。
> 模型：D-v38-3 已写入下表；后续会话视为已指定，不再询问。

| 批次 | 范围 | 量级 | 依赖 | 模型 |
|------|------|------|------|------|
| W0 先决图 + 主课名单 | ①按技能线给 83 条写先决（只引用 drill 原文数字，禁止脑算几何）。②按需填「主课 / 仅复现 / 不进课表」+ 主计划 id + 建议周次（不必 83 条都 focused）。③写准度Ⅲ：`plan_accuracy3` / 周数 / Pro / 货架位置；带塞准度主课进此份，不进准度Ⅱ、不进杆法Ⅱ。④列出跨计划咬合边。产出 `docs/research/20260814-v38-先决与主课名单.md`，**用户过目后才许 W1** | 中（纯文档） | 拍板已清 | `cursor-grok-4.6-high-fast` ✅ 2026-08-14 过目通过 |
| W1 剂量可调维 + 规格测试口径 | ①`FormationRounds.ballsPerRound` 落地 + 契约 §6.6 按 mode 分叉可调维。②Resolver：repetition + decay 压 `ballsPerRound`、钳 `rounds ≥ defaultRounds`；sequence + decay 仍可 `rounds < defaultRounds`。③I11 / `MODEL_SPEC` 同步（FL-029）；构造性：砍位置 FAIL、改链长 FAIL、合法降遍数过。④`index.json` 加 `plan_accuracy3` 槽 + `project.yml`（FL-031），本批不填课表。⑤§六断言原文 + 失败机理；付费名单纳入新 id。⑥I13 内容层草案 | 中偏大 | W0 过目 | `cursor-grok-4.6-high-fast` ✅ 2026-08-14 |
| W2 准度线程填课 | `plan_accuracy` + `plan_intermediate` + `plan_accuracy3`。只许动本线程计划 JSON 与本线程测试常量。热身/主课按 W0 序；改掉末日热身倒挂模板 | 中 | W1 | `cursor-grok-4.6-high-fast` ✅ 2026-08-14 |
| W3 杆法线程填课 | `plan_cueball` + `plan_english`。杆法Ⅱ继续禁 c076–c078（禁令保留，不改） | 中 | W1 | `cursor-grok-4.6-high-fast` ✅ 2026-08-14 |
| W4 分离角填课 | `plan_separation`。90°/薄厚须先于高低杆、走位、吃库；不吃库先于吃库（课文自述） | 小～中 | W1 | `cursor-grok-4.6-high-fast` ✅ 2026-08-14 |
| W5 走位线程填课 | `plan_positioning` + `plan_positioning2`。半台/两库先于蛇彩；c040 必须 focused | 中 | W1 | `cursor-grok-4.6-high-fast` ✅ 2026-08-14 |
| W6 基本功 / 力度 / 特殊球 | `plan_beginner` + `plan_force` + `plan_advanced`。主线已大致合理，以改主题错位与热身倒挂为主 | 中 | W1 | `cursor-grok-4.6-high-fast` ✅ 2026-08-14 |
| W7 全能 + 货架 + I13 内容层 + 回归 | `plan_fullskill` 只咬合、不抢主课；`index` / 封面 / `V37W4PlanShelfUITests` / Freemium 钉（货架 12 份，准度Ⅲ Pro）；I13 按 W1 草案加「引入序对照 W0 表」（构造性违反 exit 1）；`make verify-gate` + `make build` + 聚焦计划测试 + 货架/准度Ⅰ/走位Ⅱ截图 | 中 | W2–W6 | `cursor-grok-4.6-high-fast` ✅ 2026-08-14 |

**依赖图**：

```
W0（用户过目名单）
    └──→ W1（`plan_accuracy3` 槽 + 剂量可调维）
                    ├──→ W2 准度
                    ├──→ W3 杆法
                    ├──→ W4 分离角     } 名单已锁，可并行
                    ├──→ W5 走位
                    └──→ W6 基本功/力度/特殊球
                              └──→ W7 全能 + I13 内容层 + 回归
```

### 各批完成标准

**通用**（W2–W7）：`make build` SUCCEEDED；`ReadLints` 0 新增；`make verify-gate` FAIL 0；改动文件与批次声明一致（`git diff --name-only`）；⛔ 不改 `Drills/**/*.json` 的 `load` / 精讲 / 剂量数值；⛔ 不修绿既有 `pocket ''` 等基线失败（FL-027）。

- **W0**：研究文档含 83 条去向表（无空行、无双重主课）；先决只引原文；用户过目记录写入本真源 Changelog。
- **W1**：契约 §6.6 按 mode 写清可调维；`PlanDrillDose` / Resolver / I11 / `MODEL_SPEC` 与 D-v38-4 一致；构造性 2 例 exit 1（砍杆 / 改链长）+ 1 例合法降遍数还原后 FAIL 0；§六测试常量与 W0 一致；若加计划，`index.json` 有槽、yml/pbxproj 两头可查。
- **W2–W6**：该线程每份计划的 focused 首次引入序 = W0 表；抽 3 课核 theme 与主课同技能；非咬合热身语义上不重于主课；I13 现有四条对该批计划 FAIL 0；抽 2 条 repetition 衰减核「位置数 = 内容 `defaultRounds`、颗数 < 内容 `ballsPerRound`」；抽 1 条 sequence 衰减核「杆数不变、遍数下降」。
- **W7**：货架 12 份（含 `plan_accuracy3`）；免费三份仍免费；准度Ⅲ在付费名单；I13 新检查有构造性 1 例 exit 1 后还原；截图：货架、准度Ⅰ第一堂主课、走位Ⅱ第一堂主课（证明不再是半台先于近台、蛇彩先于半台——具体以 W0 终稿为准）。

---

## 四、11 份扫描摘要（2026-08-14，事实层）

口径：引入序 / 周主题 / 热身是否反。子智能体 11 路并行、模型 `cursor-grok-4.6`。准度Ⅰ按「先决反了即不合理」与杆法Ⅰ对齐（子智能体原判局部别扭）。

| 计划 | 总评 | 主课引入序（focused 首次） | 最硬倒挂 |
|------|------|---------------------------|----------|
| 基本功 | 局部别扭 | c006→c007→c008→c009→c023→c010→c022→c043 | 五分点瞄准线提前到 W2；W4 远台周插入高级手架 |
| 准度Ⅰ | 不合理 | c001→c011→c012→c013→c002→c032 | 半台先于近台；W2D3 全量切角热身伺候中袋直线 |
| 准度Ⅱ | 不合理 | c053→c076→c033→c072→c062→c063→c078→c052→c077 | 第 1 周上带塞；远台无塞后置 |
| 杆法Ⅰ | 不合理 | c016→c014→c015→c003→c004→c017 | W1D1 热身已是跟进；远台跟进主课先于近中距跟进 |
| 杆法Ⅱ | 不合理 | c073→c018→c020→c021→c074→c075 | 走位加塞整周提前，长台/塞量在后 |
| 力度 | 局部别扭 | c045→c044→c049→c047→c048→c046→c050→c051 | W2D3 走位链塞进强弱杆周 |
| 分离角 | 不合理 | c026→c027→c028→c029→c030→c031→c083→c084→c024→c025 | 90°/薄球第 3–4 周才主课；吃库先于不吃库 |
| 走位Ⅰ | 局部别扭 | c034→c037→c005→c035→c036→c079→c081 | 引入序对；每周末日热身难于 decay 主课 |
| 走位Ⅱ | 不合理 | c042→c080→c038→c041→c039→c040→c082 | 第一堂主课是初级蛇彩 |
| 特殊球 | 局部别扭 | c056→c057→c058→c054→c055→c060→c061→c059 | 先决大体对；中袋翻袋全量热身 / 角袋衰减主课 |
| 全能 | 不合理 | c066→c064→c069→c071→c068→c067→c065→c070 | 十球/十五球蛇彩先于五球；几乎每周 D3 离组 |

跨计划同一模板：W3 周主题抄对、W4 按覆盖/剂量填；末日「新课热身 + 旧课 decay 主课」。I13 全绿盖不住。

---

## 五、锚点索引（2026-08-14 读码核实）

| 对象 | 路径 | 用途 |
|------|------|------|
| 计划 DTO / `decay` / `reviewFrom` | `QiuJi/Data/Services/PlanContentService.swift:5–114` | W1 给 `FormationRounds` 加可选 `ballsPerRound`；SwiftData Schema 不动 |
| 剂量钳制 | `QiuJi/Data/Services/TrainingDoseResolver.swift:194–274` | v37 对 decay 放宽的是 `rounds` 下限；R7 要求 repetition 收回该放宽，改压 `ballsPerRound` |
| 上屏 `m × n` | `TrainingDoseResolver.swift:173–180`（`suggestedLineText`） | repetition = `rounds × ballsPerRound`；sequence = `ballsPerRound × rounds` |
| 误用实例 | `QiuJi/Resources/Plans/plan_beginner.json:105–116` | c006 两球形内容 `defaultRounds=4`，decay 写成 `rounds: 3` = 砍 1 个位置 |
| 六轴存储/展示 | `QiuJi/Data/Services/DrillContentService.swift:6–24` | 排课仍用存储 0–4 |
| 货架 index v7 · 11 份 | `QiuJi/Resources/Plans/index.json` | W1 加 `plan_accuracy3` 槽并升 version；W7 封面/UI 对齐 |
| 11 份课表 | `QiuJi/Resources/Plans/plan_*.json`（含 `plan_positioning2.json`） | W2–W7 按线程改 |
| I11 / I13 | `scripts/verify_tutorial_sync.py:680–736` / `:892–994` | W7 只加检查，不删四条 |
| I13 自测 | `scripts/selftest_content_invariants.py` | 新检查须构造性 exit 1 |
| 准度Ⅰ钉死集合 | `QiuJiTests/V22W2PlanAccuracyTests.swift:10–13,44` | 现 c001/c002/c011/c012/c013/c032 |
| 分离角钉死全量 | `QiuJiTests/V22W4PlanSeparationTests.swift:10–14,59` | 现 10 条恰为 separation 分类 |
| 杆法Ⅱ主轴 + 禁带塞 + Freemium | `QiuJiTests/V22W5PlanEnglishTests.swift:11–14,62–63,89–99` | 禁 c076–c078；免费三份 |
| 货架 UI | `QiuJiUITests/V37W4PlanShelfUITests.swift` | W7 回归 |
| v37 目录提案（对照，非本轮真源） | `docs/research/20260814-v37-计划目录提案.md` | W0 可引用周主题，去向以本真源 + W0 表为准 |
| 契约 I13 | `.kiro/steering/content-data-contract.md` §7 I13（2.9） | W1/W7 升版 |

---

## 六、规格测试钉死清单（改归属前必读，PD-027）

下列断言在**名单与现在不同**时会红。改测试必须附「集合按 W0 变更」的失败机理，禁止删除用例。

| 文件 | 断言要点 | 何种改动会红 |
|------|----------|--------------|
| `V22W2PlanAccuracyTests` L44 | `allIds == {c001,c002,c011,c012,c013,c032}` | 准度Ⅰ增删任何引用（含只当热身的 id 也算 allIds） |
| `V22W4PlanSeparationTests` L59 | `allIds` 恰为 separation 10 条 | 分离角请出/迁入主课或热身 |
| `V22W5PlanEnglishTests` L60–63 | 主轴 6 条须在内；c076–c078 不得出现 | 禁令**保留**（带塞进准度Ⅲ，不进杆法Ⅱ） |
| `V22W5PlanEnglishTests` L89–99 | 免费三份 / 付费名单（含 `plan_positioning2`） | W1/W7 把 `plan_accuracy3` 写入付费；失败机理 = D-v38-2=A 加档 |
| `V22W3PlanForceTests` | 力度计划结构 + 归属（读文件后以现常量为准） | 力度名单增删 |
| `V37W4PlanShelfUITests` | 现 11 份货架上屏 | W7 改为 12 份（+`plan_accuracy3`）；失败机理 = D-v38-2=A |
| `CoverPaletteContrastTests` / `PlanListView` 档位 | L0→L2 等档别名 | 新 `targetLevel` |
| `XV313PremiumPlanWalkthroughUITests` | Pro 计划走查 | 改 id / 解锁文案 |

`allIds` 含 warmup + focused + review。**W1 口径（已钉）**：规格测试继续用**引用集合**（`allIds` = warmup + focused + review，与现网一致）。W2–W6 改名单时按引用集合更新断言并写失败机理；**主课集合**以 W0 去向表「主课」列为准，不另造一套测试口径。标「仅复现」的条仍会计入 `allIds`。

---

## 七、版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v38.0 | 2026-08-14 | 立档。v37 R4 内容层收口：全库归属 + 教学序；待拍板 D-v38-1/2/3。基线 `7d229fa`。 |
| v38.1 | 2026-08-14 | 用户补口 R7：衰减不得砍杆，只降重复维。字段映射按 mode 分叉（repetition 降 `ballsPerRound`，sequence 降 `defaultRounds`）。现机制对 repetition 会砍位置（c006 4→3）。新增待拍板 D-v38-4（推荐 A：`FormationRounds.ballsPerRound`）。W1 扩为剂量可调维落地。 |
| v38.2 | 2026-08-14 | 拍板已清。D-v38-1=B 按需；D-v38-2=A `plan_accuracy3` Pro；D-v38-3=全批 `cursor-grok-4.6-high-fast`；D-v38-4=A（「只在计划里改」= 不改 drill，W1 仍加计划 DTO 字段）。W0 范围钉死准度Ⅲ；W3 保留杆法Ⅱ禁带塞。 |
| v38.3 | 2026-08-14 | W0 草案：`docs/research/20260814-v38-先决与主课名单.md`。83/83 主课、无双重；准度Ⅲ 2 周 Pro 三主课；先决只引 description。**待用户过目**（过目记录写入本 Changelog 后 W0 才算 ✅）。 |
| v38.4 | 2026-08-14 | **W0 过目通过**（用户原话「好的，开始吧」）：§0 五条判断全部维持——83 条各留主课席；准度Ⅲ = 2 周 · Pro · 120′ · c076→c077→c078；准度Ⅱ改名「准度Ⅱ·远台切角」；货架 12 份（准度Ⅲ插在准度Ⅱ与特殊球之间）；分离角第 1 周 90°/厚/薄、不吃库先于吃库。**W1 ✅**：`FormationRounds.ballsPerRound` + Resolver 按 mode 分叉；I11 收紧；存量 repetition 衰减迁成「保 rounds、降颗数」；`index.json` v8 加 `plan_accuracy3` 槽（Plans 已是 folder reference，yml/pbxproj 两头可查）。**范围偏差（记入，不改语义）**：完成标准写「本批不填课表」，但踢出 c076–c078 后 I13 要求 `reviewFrom` 来源计划实际包含该 drill。最小占位：准度Ⅲ W1 三堂各一条完整剂量主课；`plan_fullskill` 的 c078 `reviewFrom` 改 `plan_accuracy3`。不是 W2 整表重排。§六钉死规格测试用引用集合。 |
| v38.5 | 2026-08-14 | **W2 ✅** 准度三份按 W0 重排。引入序：准度Ⅰ c011→c001→c012→c013→c032→c002；准度Ⅱ c053→c033→c072→c062→c063→c052（无带塞）；准度Ⅲ c076→c077→c078。末日改为「热身=已引入较易课 decay、主课=当周主课 decay」，不再用新课当热身伺候旧课。咬合：准度Ⅱ开档 c032 `reviewFrom plan_accuracy`；准度Ⅲ开档 c063 `reviewFrom plan_intermediate`。**偏差**：①课时档维持现网 90′/120′/120′（W0 写 75′/150′/120′；c013 完整剂量装不进 75′）。②咬合首次在本计划出现 = 完整剂量 + `reviewFrom`（I13 首次不得 decay）；第二次起才 decay。③本线程 15 条主课全是 repetition，完成标准「抽 1 条 sequence 衰减」无法在本批抽检。 |
| v38.6 | 2026-08-14 | **W3 ✅** 杆法Ⅰ/Ⅱ按 W0 重排。Ⅰ：c016→c014→c003→c004→c015→c017（停/定 → 近中距跟缩 → 远台）。Ⅱ：c073→c074→c075→c018→c020→c021，禁 c076–c078。咬合：杆法Ⅱ开档 c017 `reviewFrom plan_cueball`。**偏差**：①杆法Ⅰ课时 75′→90′（跟 W0）。杆法Ⅱ维持 120′（W0 写 150′）。②I13 周序不降：c075 scalar 3，c018/c020/c021 均为 2，若按建议周把后三条放第 4 周会后周新引入下降。首次引入提前到第 3 周，第 4 周只做衰减复现；引入**序**未改。③本线程 12 条主课全是 repetition，无 sequence 可抽。 |
| v38.7 | 2026-08-14 | **W4 ✅** 分离角按 W0 重排。引入序 c024→c026→c025→c084→c027→c028→c029→c030→c031→c083（90°/厚/薄先于高低杆与走位；不吃库先于吃库）。开档咬合 c032 `reviewFrom plan_accuracy`。课时 90′→120′（跟 W0）。`V22W4` 引用集合改为 10 主课 + c032（PD-027：咬合热身不是请出主课）。**偏差**：①I13 周序不降：c025 顶层 scalar=4，其后 7 条均为 2。若按建议周把后 7 条放到 W2–W4 当新引入会后周下降。10 条首次引入全部挤进第 1 周，W2–W4 按 W0 主题做衰减复现；引入**序**未改。②本线程 10 条主课全是 repetition，无 sequence 可抽。 |
| v38.8 | 2026-08-14 | **W5 ✅** 走位Ⅰ/Ⅱ按 W0 重排。Ⅰ：c034→c037→c005→c035→c036→c079→c081（序未改，改末日热身倒挂）。Ⅱ：c080→c038→c041→c039→c040→c042→c082（半台/两库先于蛇彩；c040 focused）。咬合：Ⅱ开档 c036 / c079 `reviewFrom plan_positioning`。课时 75′→120′、90′→150′（跟 W0）。**偏差**：W4 末日若只上 c042（scalar 2）则热身 c040（4）会倒挂；c042 与 c082 同堂首次引入，周 max=4。抽检 sequence 衰减：c079/c081/c080/c039/c082 杆数不变、遍数下降；repetition：c034 5×12、c040 10×12。 |
| v38.9 | 2026-08-14 | **W6 ✅** 基本功 / 力度 / 特殊球按 W0 重排。基本功 c006→c007→c008→c009→c010→c023→c022→c043（手架回 W1；c006 衰减改保 rounds=4、降颗数，不再砍位置）。力度 c045→c049→c044→c047→c048→c050→c046→c051（c046 移出强弱杆周）。特殊球 c054→c056→c057→c055→c058→c059→c060→c061（翻角袋先于 K 球；禁止中袋热身伺候角袋）。课时力度/特殊球 75′→90′（跟 W0）。**偏差**：①基本功 c023 scalar=2，若按建议周放第 3 周会后周新引入下降，首次引入提前到第 2 周（与 c009/c010 同周），第 3 周只做五分点复现。②特殊球 c059 scalar=4，c060/c061 首次引入挤进第 3 周，第 4 周只做安全/解球复现。引入**序**未改。抽检 sequence：c046 8→6 遍、链长 5 不变；repetition：c006 4×12、c047 12×12。 |
| v38.10 | 2026-08-14 | **W7 ✅ / 全案收官**。全能引入序 c066→c064→c068→c069→c071→c067→c065→c070；咬合 c082/c054/c078 各 1 次 warmup + `reviewFrom`，不抢主课。货架 12 份（准度Ⅲ Pro）；免费三份仍免费。I13 第 ⑤ 条：focused 首次引入序对照 W0 §3（只比序，不比建议周）；构造性 `i13_intro_order_vs_w0` exit 1。契约 **2.11**。**偏差**：c069/c071 顶层 scalar=2，若按建议周放第 3 周会后周新引入下降；五条连打/蛇彩首次引入挤进第 1 周，W2 只复现五球、W3 只复现蛇彩。抽检 sequence：c064 8→6 链长 3；c069 `manual02` 8→6 链长 10。repetition：c069 `manual01` 保 rounds=8、bpr 15→10。 |

下一步：v38 全案收官。未 commit。
